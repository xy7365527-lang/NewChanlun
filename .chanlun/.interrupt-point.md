> Lead 维护的当前中断点。write_session.sh 优先读本文件作为 session「## 中断点」来源（非链式复制旧 session）。Lead 在 precompact 前或重大状态变更后更新本文件。

**最后更新**: 2026-06-26（重大地基转向：从「装我们的件」→「建在 GPT 完全分类严格推导地基上」）

## 当前阶段：形式化从 GPT 完全分类严格推导地基重启

编排者交付 GPT/codex 形式化包，经**多轮纠正**收敛到关键认知（防 compact 后回退）：
1. 「完全分类」**不是要证/评级的定理**——**缠论本身就是完全分类**（走势终完美 = X/∼≅P 双射存在），这是**公理，不可质疑**。我反复把 231 号有效域怀疑**错位**施加到这个根公理（四级阶梯 L0-L3 / no-go / 评级），是错的。
2. 三层归位：**完全分类=缠论公理（忠实编码，不评级）** / **全定义策略=我们的工作（从公理推导 S_Θ）** / **盈利性=L2 经验（231 只在此层）**。
3. **我们的起点不如 GPT 严格**——故 **build on**（建在它上面）非 absorb（塞进我们较弱的 Strict/ 框架）。
4. **真起点 = 完全分类的严格推导**（GPT「推导完全分类」page 1）：完全分类 = 构造 I:X→P（不变性 x∼y⟹I(x)=I(y) + 完备性 I(x)=I(y)⟹x∼y + 可实现性 ∀p∃x I(x)=p）⟹ **X/∼≅P 双射**。全定义策略 π_Θ=π̄∘C_Θ 建在 C_Θ=这个完全分类器上。

## 本 session 已交付（已验证，全集 lake build 50 jobs 绿）
1. **`formal/Foundation/`（严格地基，build on GPT）**：CompleteClassification.lean（I:X→P 双射 + 递归级唯一 recSpec_complete_unique/recAt_causal + 优先级互斥穷尽 + π 流水线唯一）+ HybridStateMachine.lean（行为等价商 BehEquiv + 10级动作优先级 + 资本阶段 + 风险模式 + 镜像等变 + 动态闭合 + 完整 6 段 hybrid_step_complete_unique）。GPT 原文件原样移入，本仓 lake env 编译通过，namespace NewChanlun/NewChanlunHybrid 无碰撞，无 sorry/admit/axiom。lakefile 加 Foundation lean_lib + defaultTargets。
2. `formal/Strict/HybridStep.lean`（闭环接口）+ `formal/Strict/HybridAssembly.lean`（#86，cc-hybrid-assembly 装配 S_Θ 单一闭环：HybridState 乘积态 + LedgerComp R=Π-A-W + 闭环 T 补账户因果洞）。
3. `docs/formal/full-definition-strategy-v1.md`（装配蓝图）。
4. `.chanlun/genealogy/pending/618`（待重写——四级阶梯框架是被纠正的错误，须改为「完全分类=公理 / 工作=全定义策略 / build-on GPT」三层归位）。

## 恢复后前向方向（Foundation 为脊柱，重基）
1. **忠实实例化 Foundation**：用缠论具体（笔/线段/中枢/走势类型/买卖点）实例化 Foundation 抽象参数（P₁…P₅/F₀/Fₙ/Rec/Class/Intent/Risk/Schedule/T 各段）。把 Strict/Tlayers 60 零件接成 Foundation 的**实例**（向地基对齐，非平行另起）。完备性是**给定的**（忠实编码），不再「证/降级」。
2. **全定义策略建在地基上**：HybridStateMachine 实例化 → S_Θ；HybridAssembly 重基到 Foundation（而非 Strict.globalClassify）。
3. **引擎 #84**（cc-refsem-harness 进行中）：实测**推翻**原诊断——不是「段不足」是 **71% 过分段**（theta_v0=406 vs 参考 a_segment_v1=237），且 codex「inclusion 根因」被消融实验**证伪**（inclusion 仅 Δ-2/1.2%）；真因=**算法范式整体差异**（无状态批处理 vs 增量假设转折点状态机）。修复=重写 segment.rs divide_segments 移植 a_segment_v1 _FeatureSeqState（不碰 Claim10 bit-exact，Claim10:334 有意把动态算法划归 _FeatureSeqState）。SecondKind 已实装（cc-parser-finish 未提交，cargo test parser 57 绿）。
4. **618 重写** + 615/616/617 延伸。

## codex 物证路径
- 轮1 ReferenceSemantics：/tmp/codex_gpt_pkg_discuss.txt。轮2 四级阶梯（已被编排者纠正为错误框架）：/tmp/codex_ladder_decide.txt + tool-results/bhjmj17za.txt。轮3 装配 R1=B：/tmp/codex_strategy_assembly.txt。
- GPT 包解包：/tmp/gpt_formal_pkg_1782446869/。codex CLI：`-c service_tier=fast`。

## 诚实账（formalization-validity-domain，防 compact 后膨胀）
- 完全分类=缠论公理（给定），地基忠实编码它，**不评级/不证 no-go**（那是我被纠正的错误）。
- 盈利性=L2 真问题（231 在此层），待 #84 解阻塞 + #20 数据 + 回测。Foundation 是 L0 地基，信息增量在 L2。
- Foundation 是 build-on GPT（它比我们起点严格），不是 absorb 进我们框架。

## 编排者域 pending（Lead 不擅自）
- 618 重写后是否 /ritual；615/616/617 延伸；#42/#44/#45/#52 旧 pending。
