<!-- D′ projection（机械生成，叙事剥离）。叙事归 git commit+genealogy+events.jsonl。
     恢复时核 git 真相：reducer 从 goal+git 重算，不信任本快照（base_head 不匹配即降级） -->
goal_id: g-20260702T152743Z-95550619
base_head: 7edfec86acbb361b433d0a2eba23939722511609
git_head: 7edfec86acbb361b433d0a2eba23939722511609
base_head_stale: false
derived: true
---
# 中断点 projection：g-20260702T152743Z-95550619

- status: **active** | acceptance: 0/3 passed
- goal: Phase B 全引擎算法优化实装（编排者2026-07-02两次明令『并行全面优化所有算法』的实装段，计划=.chanlun/review-results/algo-opt-plan-20260702.md）+ 段2 L2 全历史 dep

## 验收（可证伪）
- ⬜ 每个落地泳道项 bit-exact 护航全绿：GOLDEN digest + dx 真封检测器 + 既有 cargo test --lib 全绿。可证伪：任一 digest/检测器输出变化或测试红即 fail（例外仅计划
- ⬜ 每落地项前后计时对照（400K bar THETA_PROFILE_STAGES=1 与/或全历史 decompose，在 #13 后 HEAD 上重测基线），修后<修前。A1 受 A0 YAGNI 重开门：A0 实测克
- ⬜ 段2 descend_type1_anchor_depth 全历史分布报告落盘（level1-4 每条 Type2/3 信号：depth 直方图 + 小转大 None 计数，复用 h2_sample_exclusion_

## ready_workstations（reducer 算，spawn 列表）
- `acc-optB-bitexact`：每个落地泳道项 bit-exact 护航全绿：GOLDEN digest + dx 真封检测器 + 既有 cargo test --lib 全绿。可证伪：任一 digest/检测器
- `acc-optB-l2dist`：段2 descend_type1_anchor_depth 全历史分布报告落盘（level1-4 每条 Type2/3 信号：depth 直方图 + 小转大 None 计数，复用 
- `acc-optB-speedup`：每落地项前后计时对照（400K bar THETA_PROFILE_STAGES=1 与/或全历史 decompose，在 #13 后 HEAD 上重测基线），修后<修前。A1 受

## 恢复入口（reducer 重算，非叙事）
`python scripts/ceremony_scan.py` → current_goal + goal_driven workstations。
叙事查 events.jsonl（目的论史）/ git log（做了什么）/ genealogy（发现史）。
