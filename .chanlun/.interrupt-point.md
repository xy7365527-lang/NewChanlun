<!-- D′ projection（机械生成，叙事剥离）。叙事归 git commit+genealogy+events.jsonl。
     恢复时核 git 真相：reducer 从 goal+git 重算，不信任本快照（base_head 不匹配即降级） -->
goal_id: g-20260701T200047Z-8f4f50e7
base_head: 77a489c322ff40704b213a172fd5828b4e2a4fd7
git_head: 7f87e4ef7977d0014a524101e809d82ae2017423
base_head_stale: true
derived: true
---
# 中断点 projection：g-20260701T200047Z-8f4f50e7

- status: **active** | acceptance: 3/4 passed
- ⚠️ **base_head stale**（base_head=77a489c322ff40704b213a172fd5828b4e2a4fd7 ≠ git_head=7f87e4ef7977d0014a524101e809d82ae2017423）：本快照降级为参考，reducer 重算为准（spec §9）
- goal: 对照之前的全部pdf，完整实装全互斥定义策略和完全互斥分类的数学。然后再拿完整的实现回测alpha

## 验收（可证伪）
- ✅ 完全互斥分类完备性：level 塔递归无中间级空洞——稀疏级别满足 level_k有信号⟹level_{k-1}结构非空。当前 Π_full 全历史 level5=1 但 level1-4=0 违反(level5骑在le
- ✅ P2 MACD一票否决消除(外部裁决Q6 Fix1)：候选域改用结构 Cand 判据(方向/可比/新高低/背驰上下文),MACD/Force/volume 降为 feature 非 veto。signal.rs 的 MA
- ✅ GAP3 三阶段 EarningShares 可达(576=C双账本+η⋆(x)=L_wc+κQ 状态依赖barrier)：schedule_adapter 派生 EnterEarning 事件。可证伪：回测 ∃t TS
- ⬜ 完整 Π_full(P1+P2+P4+P7)回测 alpha,beta去污后OOS+codex异质审计(约束4)。预注册estimand冻结,(level,δ,bsp_class) holdout μ̂ 扣BTC bet

## ready_workstations（reducer 算，spawn 列表）
- `acc-alpha-beta-decontaminated`：完整 Π_full(P1+P2+P4+P7)回测 alpha,beta去污后OOS+codex异质审计(约束4)。预注册estimand冻结,(level,δ,bsp_class)

## 恢复入口（reducer 重算，非叙事）
`python scripts/ceremony_scan.py` → current_goal + goal_driven workstations。
叙事查 events.jsonl（目的论史）/ git log（做了什么）/ genealogy（发现史）。
