> Lead 维护的当前中断点。write_session.sh 优先读本文件作 session「## 中断点」来源。precompact 前更新。

**最后更新**: 2026-06-26（compact 前；**方向裁定 A′：Origin 为唯一 canonical base，重锚非重证**）

## 头条决定：A′（codex bimg5je09 binding + 编排者确认）
- **A′ = `formal/Origin/` 六层成唯一 canonical base**；现有 Strict/Tlayers/Foundation/theta_v0 成果**降为待重锚库**——port（重锚，非重证从零）后保留，未在 Origin 接口下证成的降 legacy/reference。
- 三方收敛（编排者直觉 A + Lead 答 + codex 重裁）：① 重映射成本被高估（改锚点非丢算法）；② A 无 genuine blocker（Origin EngineBridge 本就 import 我们的 HybridAssembly）；③ **A 不自动更严格**（核心同源、#89/#90/#91 缺口不自动消，A 价值=重置起点不在退化 Foundation 盖楼）。codex 撤回上次 B。161号（否务实）压过成本。
- **关键纪律**：缺口 #89/#90/#91 在 Origin 上**仍开**，迁移不自动关（诚实 gap register）。"严格"=每个结果在 canonical base 上**重过督导闸**，不在重不重建。

## A′ 执行计划（codex 9 步，恢复后做；Phase1 是门）
- **Phase1（#96，未启动）**：Origin 5 standalone 文件（SourceAxioms/CompleteClassification/ChanlunElements/TrendCompleteClassification/FullDefinitionStrategy）进仓 formal/Origin/（EngineBridge 延 Phase2 因 import HybridAssembly）+ lakefile Origin lib 设 canonical + Foundation 改名义 legacy（docstring 标注非物理改名）+ formal/OriginAdapters/StrictPipeline.lean 适配器骨架。验 lake build。
- **Phase2（并行，Phase1 后）**：port #89→Origin RecursiveLevelSystem（旧 theorem reexport）；port #91→Origin guardrail（禁 label 伪装 CompleteClassifier）；port #93→Origin 双账本桥（重证 ledger inv/TW/stage/OQ-9）；重锚 HybridAssembly→实现 Origin FullDefinitionSystem；重锚 theta_v0 contract（rust 不重写，改 bit-exact 对象指向 Origin pipeline）。

## 本 session 已交付（在 legacy base 上，待 port 到 Origin）
- #89 ✅ 窗口化≥3-witness 递归（RecursiveConstruction+ChanlunInstantiation，xs9 见证过督导闸，54 jobs）。
- #91 ✅ ↔ 极限+降级（CompleteClassificationLimits.lean 15 定理仅 propext，both_directions_fail，BSPLabels 物证，过督导闸）。
- #90 ✅ 账本不同构裁定 + #93 双账本扩维（TotalWealth+HybridAssembly，OQ-9 gate）——**但 #93 codex review 019f0318 找出 3 项未修**（trace 级 gate / twEvent 声明膨胀 / cumNetCash 闭环空洞），cc-gap93-fix 在修（关停时可能 mid-edit，未 commit）。
- #94 rust 闭环实装（cc-engine-closedloop，gap-map U1-U4，关停时可能 mid-edit）。
- #95 cc-arch-absorb 产出 EngineVerdict.lean+FixedThetaParam.lean = **吸收，A′ 下弃用**（Origin 自带 EngineVerdict/FixedTheta，删这两文件）。

## teach = 结构工位（编排者裁定，已真 spawn teach-supervisor 常驻）
- 双角色：教师（持续产课给编排者）+ 督导（锋利问题闸声明膨胀，对工位也对 Lead）。dispatch-dag 已注册 teach-supervisor。
- docs/teach/：lessons 0001（完全分类=行为纤维）/0002（schema vs 忠实实例化）/0003（重建 vs 扩展）+ supervision-loop.md（活台账，已记 #89/#91 PASS、2 次 Lead 纠偏、0004 待施加）+ MISSION.md。
- **恢复后重 spawn teach-supervisor**（持久工位），续产 0004+（已点：闭环 S_Θ / 双账本+OQ-9 / L0-L2 / **结构工位为何不自动拉起**优先课）。

## 结构工位自动化缺口（编排者点破，**待改不只待教**）
平台无原生语义事件（task_complete/file_change，353号）→ dispatch-dag 触发器 platform_support 多 false/partial → .claude/hooks 32 脚本全是 advisory GUARD 非 LAUNCHER → 真实「自动」=Lead 手动解释 DAG（033号）而 Lead 欠执行。= spec-execution-gap。
- **编排者裁定（标准行为）**：teach-supervisor 每条课是 **Lead-actionable 指令源**——Lead 每条都要**看且行动**（operationalize），不只是产给编排者学。
- **课已出 docs/teach/lessons/0004a + Lead 已读 + 据它改计划**：0004a 证明「写 LAUNCHER hook」**结构上不可能**——hooks 是同步子进程，无 Agent/TeamCreate spawn 权柄；30 hook 全 GUARD（platform_support true=0，平台只发机械事件 PostToolUse 不发语义事件 task_complete/谱系结算）。
- **唯一的修 = Lead 操作化 033号 DAG-解释器纪律**（自动化唯一承重点）：每个 task_complete / guard systemMessage 读成「待认领的 spawn」，按 dispatch-dag event_skill_map 主动 invoke 结构工位（genealogist/quality-guard/teach-supervisor 等）——Lead 不欠执行。恢复后立即按此运行（teach-supervisor 是结构工位，事件来就该 spawn，不等编排者点）。可选增强：把 dispatcher GUARD hook 的 systemMessage 改结构化「PENDING SPAWN:[工位]」让认领不可漏（仍 GUARD 非 LAUNCHER）。
- 教训谱系：082（结构=事件驱动 skill）/069（事件物质=hooks，仅半兑现「提醒」未兑现「启动」）/033（Lead=DAG 解释器，停转则 DAG 是死文件）/353（platform 无语义事件）。

## 物证 + 标准化
- Origin 包：/Users/silencehan/Downloads/NewChanlun-complete-classification-origin-20260626/formal/Origin/。codex 物证：.rtas/review-results/ + task output bimg5je09（A′ 9 步）/b8ize5ow8（OQ-9 立场C）。
- **裁决问 codex CLI**（编排者标准化）：`codex exec -s read-only -c service_tier=fast`（自带 auth，别用配额死的 python wrapper）。
- HEAD=15b1fae（#87 退化基线）——**本 session 大量未提交**（#89/#90/#91/#93/#94 + 结构 + teach + 中断点）。precompact commit 防丢失。

## 编排者域 pending
A′ 执行（恢复后 Phase1→Phase2）；#88 重基（并入 A′ 重锚 HybridAssembly）；#42/#44/#45/#52 旧 pending；L2 否证（引擎闭环实装+重锚后才有意义）。
