<!-- D′ projection（机械生成，叙事剥离）。叙事归 git commit+genealogy+events.jsonl。
     恢复时核 git 真相：reducer 从 goal+git 重算，不信任本快照（base_head 不匹配即降级） -->
goal_id: g-l2-nautilus-production
base_head: dcc9e25f95fe459147cf679eeafb04249df33b63
git_head: 9c70c4494f3293b505aaee01313cbdfe09259d00
base_head_stale: true
derived: true
---
# 中断点 projection：g-l2-nautilus-production

- status: **active** | acceptance: 0/4 passed
- ⚠️ **base_head stale**（base_head=dcc9e25f95fe459147cf679eeafb04249df33b63 ≠ git_head=9c70c4494f3293b505aaee01313cbdfe09259d00）：本快照降级为参考，reducer 重算为准（spec §9）
- goal: 严格完成 L2 → 接 NautilusTrader → S_Θ 成生产引擎 → 性能最大化。不用旧 8 层引擎（起点不够严格），用本轮 canonical S_Θ（theta_v0+Lean，从 origin 严格出发）。本事件为 D′ 

## 验收（可证伪）
- ⬜ L2 严格完成：sizing 修复使 S_Θ 产出有效交易（n_trades≥20 脱离 inconclusive，625 铁律），且全窗多标的 OOS 否证/确认（block bootstrap+随机对照，PREREG
- ⬜ 接 NautilusTrader：项目集成 nautilus 依赖，数据流 Nautilus→S_Θ→订单贯通（S_Θ theta_v0 适配 Nautilus strategy/data/execution，端到端可跑
- ⬜ S_Θ 成生产引擎：建 CLI/main 生产入口（当前只 test 跑），回测与实盘路径统一（同一 S_Θ 引擎，非两套代码）
- ⬜ 性能最大化：classifier 已 O(log S)（extract_signals bit-exact），全引擎热路径对齐 ROADMAP O(1)/bar 增量目标，bit-exact 优化（性能优化不改语义）

## ready_workstations（reducer 算，spawn 列表）
- `nautilus-integration`：接 NautilusTrader：从零集成 nautilus 依赖，S_Θ theta_v0 适配 Nautilus strategy/data/execution，数据流 Nau
- `sizing-fix`：sizing 坍缩诊断+修复：6045 decisions→1 order→0 trades，625 铁律区分 (a)bug 过滤过严→修复增 trades vs (b)有效域 Θ

## 恢复入口（reducer 重算，非叙事）
`python scripts/ceremony_scan.py` → current_goal + goal_driven workstations。
叙事查 events.jsonl（目的论史）/ git log（做了什么）/ genealogy（发现史）。
