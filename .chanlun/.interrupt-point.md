<!-- D′ projection（机械生成，叙事剥离）。叙事归 git commit+genealogy+events.jsonl。
     恢复时核 git 真相：reducer 从 goal+git 重算，不信任本快照（base_head 不匹配即降级） -->
goal_id: g-sigma-complete-l2-nautilus
base_head: fab1f08a4dd8d9644064a3ff583108149075a6b5
git_head: b29d3d4b1998f110190a785b7626e9e29d5e8cab
base_head_stale: true
derived: true
---
# 中断点 projection：g-sigma-complete-l2-nautilus

- status: **active** | acceptance: 0/5 passed
- ⚠️ **base_head stale**（base_head=fab1f08a4dd8d9644064a3ff583108149075a6b5 ≠ git_head=b29d3d4b1998f110190a785b7626e9e29d5e8cab）：本快照降级为参考，reducer 重算为准（spec §9）
- goal: S_Θ 完整闭合与生产化：Q4 确定性 ElementId 层→#5 alpha 坐实→L2 全窗否证/确认→O(n) 大规模验证→接 NautilusTrader 生产引擎

## 验收（可证伪）
- ⬜ Q4 确定性 ElementId 层：cargo test --lib 退出码0 + grep ElementId coverage.rs 命中 + bit-exact(增量==全量,spec §13 parent_id
- ⬜ ΔSharpe 非零：至少 1/8 品种 depth>0 腿准入数>0 + ΔSharpe≠0.000
- ⬜ L2/L3 全窗 8 品种 OOS 跑通 + n_beats_random 双口径 + 分层诊断(引擎产不出 vs 不盈利)
- ⬜ O(n) 大规模：全引擎 per-bar exp≈1.0 @16K(profile 大规模非 n=1000)
- ⬜ Nautilus：nautilus 依赖集成 + 数据流 Nautilus→S_Θ→订单 + CLI 生产入口

## ready_workstations（reducer 算，spawn 列表）
- `perf-fullwindow-verify`：全引擎 per-bar exp 大规模验证 @16K(非 n=1000 小窗)
- `q4-id-layer`：确定性 ElementId 贯穿 LeveledMove→CoverageElement→ActiveLeg;Stale 不伪造 parent:None(改 prune);held

## 恢复入口（reducer 重算，非叙事）
`python scripts/ceremony_scan.py` → current_goal + goal_driven workstations。
叙事查 events.jsonl（目的论史）/ git log（做了什么）/ genealogy（发现史）。
