<!-- D′ 元数据：恢复时校验。derived=true；base_head 不匹配即 reduce 重算 -->
goal_id: g-l2-nautilus-production
base_head: aa5b7a73ff
derived: true
---
> Lead 中断点。**核 git 真相**：恢复时核 git log HEAD + git status；D′ 进度=events.jsonl(62 事件)。

**最后更新**: 2026-06-27（**新 goal：严格完成 L2 → 接 Nautilus → 生产引擎 → 性能最大化**）

## 当前 /goal（编排者，4 要素）
`g-l2-nautilus-production`：严格完成 L2 + 把它接到 **Nautilus Trader** 上 + 让 canonical S_Θ 变成**生产引擎** + **性能最大化优化**。编排者定调：**不用旧 8 层引擎**（起点不够严格），用本轮 canonical S_Θ（theta_v0+Lean，从 origin 严格出发）。

## 前置已达成：结构层零遗漏（主 goal g-canonical-complete-no-omission，6 commits）
迁主塔637+L3+E5+B2S2+L4+#53塔+L5总定理(main_theorem 0公理)+异质验收((A)缺口空,classification 97%/strategy G1-G8/rust D2D5B2S2E5)+最小性⟸真证(决策充分性充要⟺)。lake 113 jobs+cargo 1000 passed+olean mtime防陈旧假绿。

## L2 阶段进展（当前 g-l2-nautilus-production）
- ✅ L2 统计检验(metrics significance:block bootstrap+随机对照,PREREG_SEED冻结)+L2 inconclusive(625铁律:n_trades<20不冒充否证)。commit aa5b7a73ff
- ✅ classifier-perf:signal.rs extract_signals O(C×S²)→O(C·logS+S) bit-exact(extract_signals_bit_exact_vs_pre_opt_oracle逐字节等价)。解96.7%瓶颈。commit aa5b7a73ff
- ⏳ **sizing-audit 诊断中(compact 终止,恢复重派)**:sizing坍缩6045 decisions→1 order→0 trades,625铁律区分(a)bug过滤过严→修复增trades vs(b)有效域Θ稀疏→需L3多标的。owner=strategy/mod.rs+risk.rs+exec.rs

## 关键未决（恢复后处理）
1. **E5 102 trades 回退到 0**:b1075ec87c时E5报OKLO 30K→102 trades,当前HEAD实测0 trades(6045→1 order)。根因sizing坍缩,疑L4杠杆约束(risk.rs L^G/L^N)或#53塔升级(classify改输出)引入。sizing-audit诊断坐实。
2. **S_Θ怎么用/实装**:形式化完整(Lean main_theorem)+rust管线结构完整(run_theta_v0:classify→recognize→plan_and_fill_mtm→hybrid_step→trade_pnls),但**sizing坍缩使产不出有效交易**(差最后一公里)。无CLI/main(只test跑)。
3. Nautilus:项目无nautilus依赖,从零集成(L2严格完成后)。

## 恢复指引（依赖序）
核git HEAD(aa5b7a73ff)+events.jsonl(62事件)+本文件。**重派 sizing-collapse-audit**(诊断6045→1 bug/有效域)→ sizing修复使S_Θ产有效交易 → **建CLI/main生产入口** → L2全窗多标的否证/确认(formalization-validity L2可否证) → **接Nautilus Trader**(S_Θ theta_v0适配Nautilus strategy/data/execution,数据流Nautilus→S_Θ→订单) → 生产引擎+持续性能最大化(对齐ROADMAP O(1)/bar增量)。铁律:#93全量真封(lake+cargo+olean mtime防陈旧假绿)+625铁律(inconclusive≠否证)+bit-exact性能优化+膨胀铁律(231 L级:结构L0达成≠L2经验有效)。docs/canonical-coverage-*.md=覆盖矩阵(97%)。
