# 元观察——v120-swarm + v121-swarm 二阶观察

rule_version_baseline:
  claude_md_commit: 97f3ba3186f1919ff6abf02dea4247724d47c79a
  rules_dir_mtime: 2026-03-01 00:27:49 +0000

观察对象: v120-swarm（六线并行产出已完成）+ v121-swarm（rescan 后产生的工位，当前执行中）

任务不可分解因为：四个观察轴（274/275/273/spawn模式）依赖同一组证据（session数据/commit历史/代码产物），拆分会导致重复读取和冗余上下文消耗。

---

## 观察 1（收敛信号）：274号 depth_budget 废除——执行层正确体现

### 现象

274号废除 depth_budget 后，执行层已正确更新：

1. **ceremony.md 步骤5**：递归判断块不再包含 depth_budget 参数，仅注入 topo_address 和 parent_callback（已确认）
2. **ceremony.md 步骤11**：已废除 max_rescan_depth=3 安全阀，终止条件仅为不动点（步骤10）
3. **sub-swarm-ceremony SKILL.md**："递归深度"章节已重写——终止条件仅原子性和不动点，无外部计数器
4. **agent-team-enforce.sh**：spawn 基因检查从三基因降为两基因（topo_address + parent_callback），depth_budget 检查已移除（第90行注释明确标注"274号废除"）
5. **dispatch-dag.yaml**：required_genes 仅 topo_address 和 parent_callback，recursion_rules 第4条明确标注"274号：无外部计数器"

v121-swarm 的 6 个工位均由 v120-swarm rescan 后产生，递归终止由不动点判定（rescan workstations 为空或相同则终止），无计数器截断。

**结论**：274号在行为层的体现完整，无执行缺口。收敛信号——与216号观察到的"递归终止条件"模式一致。

---

## 观察 2（收敛信号 + 一处边界条件补充）：275号局部依赖原则——Lead 行为基本正确

### 现象

275号要求 Lead 不做全局排序，全部并行 spawn。观察 v120-swarm 行为：

1. **commit 0ef4ae5**："六线并行产出"——6 个工位全部并行 spawn，无串行排序
2. **lead-parallel-dispatch.md**：已更新为包含"局部依赖原则"完整章节，禁止的全局排序行为 4 条，ceremony_scan 消费规则 4 条
3. **ceremony_scan.py**：priority 字段已加注释标注为"来源标记，不产生全局排序效力"（第17-20行、第920-921行）

**边界条件补充**：ceremony_scan.py 中 priority 字段虽然语义已更新为"来源标记"，但代码中仍然使用 P0/P1/P2/P3 这种暗示优先级排序的标记值。如果未来有新的消费者看到 P0/P1 这样的值，可能会误解为全局排序依据。这不是当前的执行缺口（当前无消费者按 priority 排序），但是一个潜在的语义误导。

与275号边界条件第2条相关（"priority 字段如果未来有新消费者需要排序语义，必须新建字段"），当前处于安全区域。

**结论**：275号在 Lead 行为中正确体现。无违规观察。收敛信号——218号（Lead 并行化）→275号（全局排序思维割掉）的链条完整。

---

## 观察 3（发散信号）：273号否定拓扑代数——relations.jsonl schema 扩展未执行

### 现象

273号定义了 negates 类型边的 validity/invalidated_by/invalidated_at 三个新字段。273-1 下游推论状态为 open（行动类——待实现）。

验证 relations.jsonl 现有数据：
- 所有 negates 边均不包含 validity 字段（grep "validity" 返回 0 匹配）
- 273号的向后兼容设计声称"读取时默认为 active"，但当前 topology_operator.py 和 relations.jsonl 的读取代码是否已包含这个默认值处理？——需要实现层确认

**四分法分类**：行动类——273-1 下游推论是明确的实现任务（schema 扩展），不需要概念判断。

**注意**：v121-swarm 已有 schema-extend 和 schema-extend-2 两个工位（session 显示 in_progress），如果这两个工位正在执行 273-1 下游推论，则本观察为收敛信号。如果它们在做其他事情，则 273-1 仍为 open。

---

## 观察 4（收敛信号）：六线并行 spawn 模式——高效且符合规则

### 现象

v120-swarm 六线并行工位：
1. ontology-seal（267号封卷）
2. negation-algebra（273号否定拓扑代数）
3. topo-detect（272-1 拓扑检测三层架构）
4. backtest-framework（回测分析模块）
5. oq5-data-verify（OQ5 数据 L2 验证）
6. local-dep-discuss（275号局部依赖原则讨论）

6 个工位之间无数据依赖——全部并行 spawn 合法（218号）。产出覆盖了理论（273/275）、代码（topo-detect/backtest-framework）、数据（oq5-data-verify）三个维度，无闲置工位。

v121-swarm rescan 后产生的 6 个工位中：
- topo-analyst 和 topology-analyst 两个拓扑分析工位
- schema-extend 和 schema-extend-2 两个 schema 扩展工位
- meta-observer 和 meta-observer-2 两个元观察工位

**观察到的模式**：每种类型都有 2 个工位，命名后缀 "-2" 表示 rescan 后的增量补充。这可能是 ceremony_scan 在 rescan 阶段对同一类型的工位重复生成的结果。如果两个同类型工位的任务内容实质相同，则存在冗余。如果内容不同（例如 schema-extend 做 273-1，schema-extend-2 做其他 schema 任务），则合理。

**结论**：并行模式本身高效。工位重复命名模式需要关注——如果是冗余则浪费资源，如果是互补则合理。当前信息不足以判定是哪种情况。

---

## 观察 5（收敛信号）：ceremony_push_and_rescan.sh 原子链——224/225号在执行层已修复

### 现象

ceremony.md 步骤7 已改为 `bash scripts/ceremony_push_and_rescan.sh "commit message"`，将 commit→push→rescan 合并为单一 bash 调用，消除 LLM 决策间隙。这是 224/225号（ceremony 原子性断裂）的修复在执行层的物质形态。

**结论**：224/225号修复已正确集成到 ceremony 序列中。收敛信号。

---

## 自环检查

与历史观察对比：

| 本次观察 | 历史覆盖 | 判定 |
|---------|---------|------|
| 274号行为层体现完整 | 首次观察（274号是 v120-swarm 新产出） | 新观察，确认执行无缺口 |
| 275号 Lead 行为正确 | 首次观察（275号是 v120-swarm 新产出） | 新观察，确认执行无缺口 |
| 273号 schema 扩展未执行 | 273-1 在 session 中标记为 unresolved | 发散信号——但可能被当前工位覆盖 |
| 六线并行高效 | 216号"任务粒度默认偏小"语法记录候选待观察 | 本次无粒度偏小信号，但工位重复命名是新模式 |
| 224/225号原子链修复 | 224/225号首次产出于 v70-swarm | 收敛信号——修复已持久化 |

## 总结

- 4 个收敛信号（274/275/六线并行/原子链修复）
- 1 个发散信号（273号 schema 扩展未执行——但可能正在被 v121-swarm 的 schema-extend 工位处理）
- 0 个语法记录候选
- 0 个未分类 Gap

无需走 /escalate 的发现。所有观察要么是新谱系的首次执行确认，要么是已有修复的收敛确认。273号 schema 扩展是行动类下游推论，由工位自行执行即可。
