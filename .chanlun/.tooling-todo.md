# 代码智能工具待办(2026-06-29 编排者决策)

## 决策: Serena 优先(LSP按需,不受47worktree污染), codebase-memory瘦身后回归(图范式契合理念,当前5.4GB/21秒查不可用)

## 阻塞: rust-analyzer 未装(主session沙箱看不到~/.cargo)
**编排者在完整shell跑**:
    rustup component add rust-analyzer
装完告知, Lead改 .serena/project.yml: languages: [rust, python]
之后Serena对Rust生效(find_symbol/find_referencing_symbols/replace_symbol_body符号级)

## codebase瘦身(次优先): 47 worktrees + vendored污染图(397万节点大部分噪声)
瘦身后 index_repository 重建, 验证查询<5s再回归
