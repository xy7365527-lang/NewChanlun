# 线段（Xianduan / Segment）

**版本**: v2.0
**状态**: 已结算（本版落 [#813](https://github.com/xy7365527-lang/NewChanlun/issues/813) G-1～G-5 五组裁定）
**最后更新**: 2026-08-05
**变更**:
- **v2.0** 落 #813 线段侧定案：**新建 G-1 第三范式／G-2／G-4／G-5 四组条目**（v1.3 里根本没有）／G-1 定 P2、**P3 判违规**（`077:62` 直接否证）／真病名「形式化层裂成两半」／**作废「406 vs 237（70% 过分段）」与本文 v1.3 `:199` 的「2-3 倍」读数**／G-2 方向性为正本、外包络降对拍锚／G-3 Lean 那套判违规／G-4 定 `strict`／G-5 拆两半（G-5b 撤票）／新增相切边界待裁项／受影响代码清单／未测表
- v1.3 /ritual 正式结算：v1状态机4/4审计通过，v0/v1口径决断完成（v0降级参考），谱系001+003均已settled
- v1.2 v1状态机审计+修复：第二种情况（有缺口）的第二特征序列检查从错误的"第一序列延续"改为正确的"同向笔独立包含处理"。37/37测试通过。
- v1.1 实证对比完成：v0贪心2-3:1于v1、退化段归因笔层非口径层（谱系003更新）
- v1.0.1 增加原文谱系
- v1.0 初版
**原文依据**: 编纂版 §七；博文第62、65、67、71、77、78课
**权威考据**: `.chanlun/review-results/issue813-segment-G1-G5-20260805.md`（1099 行）
**概念分离**: 谱系 003

---

## 0. ★★ G-1：三范式定名分 —— 正本定 **P2**，**P3 判违规**

v1.3 只登记了 v0/v1 两个 Python 口径。**实际是三个范式，第三个（Lean 那套）v1.3 完全没有条目。**

| 范式 | 载体 | 接受集 | 名分 |
|---|---|---|---|
| **P1**（三笔重叠，原 v0） | `src/newchan/a_segment_v0.py:148` `segments_from_strokes_v0` | **恒 3 笔**（`:130` `s1=j + 2` 写死；`:157` `while j <= n - 3` 每次跳 3 笔） | **`[新缠论:选择]`** —— 「三笔重叠」四字**原文零命中**（穷举：全 108 课语料检索该四字组合，0 命中） |
| **P2**（特征序列法，原 v1） | `src/newchan/a_segment_v1.py` ／ `rust/src/segment.rs`（`min_seg` 门在 `rust/src/theta_v0/parser/segment.rs:436`） | **≥3 笔** | **`[旧缠论]`** —— `077:62` + 067/071/078 特征序列法 |
| **P3**（Lean 构造层） | `formal/Origin/SegmentConstruction.lean:63-67` `scanSegEnd` → `:80` `segmentsOf` | **在生产笔流上恒 2 笔**（推论，链见 §0.2） | **判违规** —— 被 `077:62` 明确排除 |

### 0.1 决定性原文（逐字全录，`AGENTS.md` 两步核对已过）

`077-第77课.md:62`【正文】（该课 `---------↑正文---------` 界在 `:92`，`:62` 在界**前**；行首**无**「（注：」「(娇注：」一类标记 ⟹ **两步全过**）：

> 有了笔以后，就是线段了。**线段划分的最基本原则，就是线段必须至少有三笔**，这是十分显然的，否则，一笔都能构成线段，那笔和线段又有什么区别？**至于两笔为什么不能构成线段，这理由更简单**，因为两笔，那么线段的两段的分型的性质肯定是一样的，和笔一样，一个完整线段的两段的分型不可能是同性质的。也就是说，和笔一样，线段也不可能从一个顶开始结束于一个顶，或者从一个底开始结束于一个底。由此可见，**线段中包含笔的数目，都是单数的**。

**⟹ 原文专门用一整句解释了「两笔为什么不能构成线段」 ⟹ P3 做的事被原文明确排除。**

### 0.2 ★ 「P3 在生产笔流上恒 2 笔」的推论链（`[新缠论:推论]`，链完整）

1. Lean 的 `Stroke`（`formal/Origin/ChanlunElements.lean:35-41`）**只有 5 个字段**（`direction`／`startIndex`／`endIndex`／`startPrice`／`endPrice`），**没有任何交替不变量**；
2. 但**生产笔严格交替**：`rust/src/theta_v0/parser/stroke.rs:30` 逐字「结果是顶/底**严格交替**的分型序列（相邻必异类）」；`:70` 逐字「`collapse_consecutive`：同类连续取舍 → 顶/底严格交替序列」；**property 测试锁**在 `:457` 逐字「property：相邻笔方向严格交替（new 笔顶底交替的必然结果）」；
3. `scanSegEnd`（`SegmentConstruction.lean:63-67`）的判据行 `:66` 是 `if strokeReverses segDir t then acc + 1` —— **第一根笔就是反转笔**（交替 ⟹ 第 2 根必反向），立即 `acc + 1` 返回；
4. `segmentsOf`（`:80`）以 `scanSegEnd s.direction rest 1` 起算 ⟹ **恒返 2**。

**该文件自己印证**：`SegmentConstruction.lean:213` 逐字「三根笔：上(10→20) 下(20→15) 上(15→25)。第一段上，**第二根反转处切**。」

### 0.3 三范式的关系：既不是包含链，也不是对称互不包含

```
P1 ∩ P3 = ∅        P2 ∩ P3 = ∅        （P3 孤立）
P1 ⊄ P2   ∧   P2 ⊄ P1   ∧   P1 ∩ P2 ≠ ∅   （互不包含且交非空）
```

**五笔手算样例**（考据给出）：同一条 5 笔序列上 **P1 = {[0,2], [2,4]}**（每段恒 3 笔），**P3 = {[0,1], [2,3], [4,4]}**（每段恒 2 笔）⟹ 两者段边界全不同。
P2 在同序列上可给出 `{[0,4]}`（一段 5 笔）—— P2 能出 5 笔段而 P1 不能；P1 能在无特征序列分型处切段而 P2 不能 ⟹ **互不包含**；两者在「恰好 3 笔且特征序列分型落在第 3 笔尾」的序列上重合 ⟹ **交非空**。

> **未测**：上述见证是**手算，未实跑**。见 §16。

### 0.4 ★★ 真病名：「形式化层裂成两半」，不是「Lean 判据错」

| 半 | 位置 | 状况 |
|---|---|---|
| **能进管线的那半，判据不对** | `formal/Origin/Pipeline.lean:136-137`（`:137` = `segmentsOf inp.strokes`） | ＝ P3。且 `:84` **明写**「★**不** import #122 在改的 SegmentFeatureComplete / #124 的 ForceInterface（避冲突；**用构造层不用判据层完整版**）」 |
| **判据对的那半，没接管线** | `formal/Phase2/Claim10_SegmentV1.lean` | 有 P2 的**谓词层**形式化。**零 `import` 行**（完全自包含），且**全 `formal/` 内除 `lakefile.toml` 外无任何文件引用它**（穷举：`grep -rln Claim10_SegmentV1 formal/`）⟹ **与管线零 import 连线** |

**★ 订正定案一处措辞**：定案写 Claim10 是「独立 lake root、与管线零连线」，其中「**跑不起来**」这个说法**不成立** —— 它注册在 `formal/lakefile.toml:20` 的 `Phase2Claims` lean_lib roots 里，而 `Phase2Claims` 在 `:2` 的 `defaultTargets` 中 ⟹ **`lake build` 默认就会构建它**。
**准确说法：它构建得起来，但与管线零 import 连线 ⟹ 判据对的那一半从没接进生产路径。**

**另一处自相矛盾**：`rust/src/theta_v0/parser/segment.rs:384` 声称「confirmed segments：已确认线段（交易可用，**对齐 `Origin.SegmentConstruction.segmentsOf` 输出**）」，而同一段注释 `:375-376` 逐字「L1（对参考 spec 忠实……），**非**对 Origin Lean bit-exact」。
**⟹ 二者自相矛盾，且按 §0.2 推论（`segmentsOf` 恒切 2 笔段）`:384` 那句为假。**（行号订正：定案记「`:376-377`」，实际那个「非」字起于 **`:375`**。）

### 0.5 ⚠️ 不得引 `Claim10:497` 定 G-1

`Claim10_SegmentV1.lean:497` 逐字「这关死"v0=v1"的有效域膨胀——二口径严格不等（**v1 ⊊ v0**），印证谱系 003 的概念分离」。
**但那里的「v0」只是 `length ≥ 3`**（`:476` 逐字 `def Segment.wellFormedV0Skeleton (s : Segment) : Prop := s.strokes.length ≥ 3`），**不是 v0 算法**（P1）。
**⟹ 引它定 G-1 是 [#815](https://github.com/xy7365527-lang/NewChanlun/issues/815) 同型误读。**

---

## 1. ★★ 作废读数：「406 段 vs 237 段（70% 过分段）」

**可溯源但不可复现 ⟹ 作废，不得沿用。**

| 项 | 实况 |
|---|---|
| 出处 | `rust/src/theta_v0/parser/segment.rs:380`（「406 段 vs 参考 237 段，70% 过分段」；修复记录在 **`:382`**）＋ `analysis/segment_refsem_cert.py:7-8`（「OKLO 2000 笔」「theta_v0=406 段 vs 参考=237 段（70% 过分段）」）＋ `rust/src/theta_v0/parser/feature_seq.rs:19-20` |
| 测的是什么 | **已废弃的旧批处理**（`segment.rs::divide_segments`「无状态批处理找首个分型即断段」）**vs Python v1 参考语义** —— **不是三范式的量级差** |
| 统计资质 | OKLO **单标的**、2000 笔**单切片**、**点估计**、覆盖率**未声明**、**level 0** |
| 已修复 | `feature_seq.rs:22` 逐字「本文件移植参考的增量算法以通过认证」 |
| 承载 harness | 已被 `analysis/_attrib_segment_divergence.py:19` 逐字判「**本 harness 已退役：不得作为任何口径的参考基线引用，下方数字是历史记录。**」 |
| 数据 | `_xcheck_oklo_strokes.json` **从未入库**（穷举：`find . -name "_xcheck_oklo*"` 全仓 0 命中）⟹ **无复现路径** |

**⟹ 且本文 v1.3 `:199` 的「v0贪心段数是v1的2-3倍」一并作废、不得沿用** —— 仓内 70%／71%／「2-3 倍」**三方口径打架**，且该读数同样是单标的单切片点估计。**本版已删除该断言**（见 §9 口径 A）。

### 1.1 ★ 补入定案票面丢掉的真读数

`rust/src/theta_v0/parser/feature_seq.rs:20-21` 逐字：「归因证伪「inclusion 是根因」（**外包络→方向性仅 Δ=2 段，1.2%**）」

**⟹ 把包含处理从外包络换成方向性，只差 2 段（1.2%）** —— 与那个 70% 差**两个数量级**。**这是 G-2 唯一的在案量化读数。**

---

## 2. G-2：包含处理口径 —— 正本定**方向性**；外包络降为**对拍锚**

| 档 | 载体 | 名分 |
|---|---|---|
| **方向性包含处理（正本）** | `rust/src/theta_v0/parser/inclusion.rs:47-55`（`:52` 向上 `high=max, low=max`；`:54` 向下 `high=min, low=min`） | **`[新缠论:推论]`** |
| 外包络（保留 min-low / max-high） | `rust/src/theta_v0/parser/segment.rs:195-196` `process_feature_inclusion`（`:195` 自称「中性实现（保留外包络）」） | **零原文依据** |

**`[新缠论:推论]` 的推理链**：`067-第67课.md:20`【正文】把特征序列的包含处理**归约**到普通K线的包含处理（逐字「如同一般K线图中找分型的方法，也存在所谓的包含关系，也可以对此进行非包含处理」）；而普通K线的包含处理**原文本身就是方向性的**（`062:28`【正文】「在向上时，把两K线的最高点当高点，而两K线低点中的较高者当成低点……反之，当向下时……」）⟹ 特征序列的包含处理也应方向性。**归约 + 原文方向性 ⟹ 推论。**

> **⚠️ 附带登记**：`067:20` 行尾带一条编纂者注「（娇：这里的描述有欠缺，出现肯定论点后找论据的逻辑顺序错误。**没有明确同一段前不能进行非包含处理**。）」——**行首无标记 ⟹ 该行按两步判为【正文】**，但注文本身指出原文在此处有欠缺。照实登记。

### 2.1 外包络：**生产不可达，降为对拍锚，不得作生产判据**

**可达性**（本条依 map [#787](https://github.com/xy7365527-lang/NewChanlun/issues/787) Notes N-1 预授权判定）：
唯一消费者是 `segment.rs:287`（`let std_feat = process_feature_inclusion(&feat);`），它在 `analyze_termination`（`:281`）函数体内；该函数**7 处引用全在同一文件内**，三个 mod 文件均未再导出。

**三条限定照实登记**：
1. 它是 **`pub`**（`pub(super) fn process_feature_inclusion`）—— 随时可被重新调用；
2. 它是**与 Lean 静态层的唯一对拍锚**（`Origin.SegmentFeatureSeq.Contains` 配套）—— **有用，别删**；
3. **BUG-08 之前它曾生产可达，且当时没有任何锁。**

**⟹ 正本明写「不得作生产判据」，并**要求补一道锁**防它再次变可达。**

---

## 3. G-3：第二特征序列的构造 —— 生产侧对，**Lean 那套判违规**

生产侧三项**全【正文】**（`067` 界在 `:62`、`078` 界在 `:64`，三行均在界前且行首无标记 ⟹ 两步全过）：

| # | 原文 | 逐字 |
|---|---|---|
| 1 | `067:38` | 「如果从该分型最高点开始的**向下一笔开始的序列**的特征序列出现底分型，那么该线段在该顶分型的高点处结束」 |
| 2 | `067:46` | 「第二个序列中的分型，不分第一二种情况，**只要有分型就可以**」 |
| 3 | `078:54` | 「对于第二种情况的第二特征序列的分型判断，**必须严格按照包含关系的处理来**……其方向是和原线段**一致**」 |

**⟹ Lean 的做法零原文依据，判违规**：`formal/Origin/SegmentAutoConstruct.lean:177` `extractSegEndData` 取「剩余**反向**元素」＋只找「**对偶**分型」，且 `:174` **自承是代理**（逐字「`rest`（分型后剩余特征序列）作为 `revSeq` **代理**」）。
原文要的是**同向笔**（`078:54`「方向和原线段一致」）和**任意分型**（`067:46`「只要有分型就可以」）——两处都反。

**★ 定案票面漏了一条**：`formal/Phase2/Claim10_SegmentV1.lean` **自己就把两个读法并列写了**：
- `:46` 逐字「第二种(第1、2元素有缺口→须第二特征序列出**反向分型**才终结)。"只有两种可能。"」
- `:47` 逐字「第二种简化（:46）：第二特征序列的分型不再分第一二种情况，**只要有分型即可**。」

**⟹ 这个分歧在 Lean 内部也已登记，不是本轮新发现。**

### 3.1 本仓自己的在案实证（可直接引）

本文 v1.3 `:212` 逐字（本版保留在 §5.2）：「**旧实现**：`_has_any_fractal_after(self.std, i+1)` 检查第一特征序列（反向笔）的延续——这是**错误的笔集合**」
**⟹ 正本自己已经承认「用错笔集合」曾是一个实际 bug** ⟹ 这是 [#809](https://github.com/xy7365527-lang/NewChanlun/issues/809)「G-1/G-3 直接依赖 S-1 定出的笔集合」判断的**在案实证**。

---

## 4. G-4：缺口封闭 —— 正本定 `strict`；`optimized` 判**无依据**

| 档 | 依据 | 名分 |
|---|---|---|
| **`strict`（做缺口封闭，正本）** | 只到 **【067课答疑】级**（`067:170`／`:196`，均在 `:62` 界**后** ⟹ 【答疑】） | **`[旧缠论:答疑级]`** |
| `optimized` | **零依据** | **判无依据** |

`067:170`【答疑】逐字：「这里是第一种情况，也就是特征序列缺口被第一笔就封闭的情况……」
`067:196`【答疑】逐字：「麻烦的是第二种情况，在那种情况下，并不是任何三笔都能构成破坏，就算最终特征序列元素间的缺口被封闭了。注意，在第二种情况下，即使封闭，肯定不是被第一个给封闭的……」

**⚠️ 不得拿 `067:46`【正文】那句当 `optimized` 的依据**：那句「在第二种情况下，后一特征序列**不一定封闭**前一特征序列相应的缺口」管的是**「第二特征序列要不要封闭第一序列的缺口」**，**不是**「缺口封闭本身要不要做」——**两件事，别混。**

**可达性**：`ExtendMode::Optimized` 在 theta_v0 **无生产构造点**（穷举：`grep -rn Optimized rust/src/theta_v0/` **仅 1 命中** = 枚举变体声明 `parser/feature_seq.rs:284`；不进 `ParseConfig`、无 env 开关、`scripts/` + `.github/` 零命中）。

**★ 定案票面标「灰区」低估后果**：仓内在案读数 `tests/test_rust_segment_equivalence.py:11` 逐字「BZ 全年 strict 28 段、optimized 159 段」⟹ **两档差 ≈5.7 倍**，量级远大于 G-2 的 Δ=2 段（1.2%）。
**⟹ 不该继续标灰区。**

**⟹ 删 `optimized` 会翻掉 4 个测试 + 4 个 rust 调用点 ⟹ 归 SPEC，不在本票动手。**

> **待办，未收口（不假装已对齐）**：**与「D-5」的口径一致性未收口** —— 考据未拿到 D-5 票面，只给了三条结构对齐点。**照实登记为待办。**

---

## 5. G-5：扫描窗口 —— 拆两半，一半撤票

### 5.1 G-5a（第二特征序列扫描窗口 `50` vs 无限）：**不是伪分歧，待裁**

| 侧 | 值 | 生产状态 |
|---|---|---|
| 族I | `rust/src/segment.rs:413` `pub const MAX_SECOND_SEQ_SCAN: usize = 50;` | **在生产** —— 消费点 `rust/src/orchestrator.rs:215`（`if trigger_k + 1 + MAX_SECOND_SEQ_SCAN > n_strokes`）、`rust/src/segment_layers.rs:540`／`:838`；v1 生产调用点 `orchestrator.rs:860` |
| theta_v0 | **无限**（默认无窗口截断） | **也在生产** |

**★ 且 `50` 兼任 checkpoint margin**：`orchestrator.rs:110` 逐字「关键。段内任一候选缺口分型的第二特征序列扫描窗口（**≤MARGIN=`MAX_SECOND_SEQ_SCAN`**）」，消费在 `:215`。
**⟹ 改它会连带动 checkpoint。**

**⟹ 待裁。正本先如实记「两侧并存 + 兼任关系」，不单方面收口。**

### 5.2 G-5b（`TAIL_WINDOW = 7`）：**撤票**

`rust/src/segment.rs:410` `const TAIL_WINDOW: i64 = 7;`。定案票面那句「窗口 7→∞ 输出不变」**实测是关于这一个的，而这一侧无分歧**（theta_v0 无对应的第二个 `TAIL_WINDOW`）⟹ **撤票。**

> **未测**：「7→∞」仓内**无可跑 harness** —— `rust/src/theta_v0/parser/profile.rs:104` `diag_segment_window_effect`（`:103` `#[ignore]`）**只测 0/50/200**，不测 7 和 ∞。见 §16。

---

## 6. 新增待裁项：**相切边界**（`<` vs `<=`）

**原文未表态 ⟹ `[新缠论:选择]`。**

| 载体 | 判据 | 语义 |
|---|---|---|
| **P1** | `src/newchan/a_segment_v0.py:162` `if overlap_low < overlap_high:` | **`<`** 开区间 —— 边界相切**不算**重叠 |
| **P2** | `rust/src/theta_v0/parser/segment.rs:116` 逐字「三笔几何区间公共交集非空：`max(lows) <= min(highs)`（**含边界相切**）」 | **`<=`** 闭区间 —— 相切**算**重叠 |

### 6.1 ★★ 这条分歧**本仓早已在案登记**（定案与考据都没查到）

`formal/Phase2/Claim10_SegmentV1.lean:471-474` 逐字：

> ★H4 口径差异诚实标注（codex 发现）：本模块 H4 前三笔重叠用 `≤`（闭区间，允许边界相切），claim5 v0 用 `<`（开区间，严格重叠）。**原文（067/077"必须有重合的部分"）未明确边界相切是否算重合**——这是一个 **未结算的口径细节**（边界相切=重合？）。本模块用 `≤` 是较宽口径；integrator 跨模块桥接 v1⟹claim5-v0 前须统一此 `≤`/`<` 差异。**本模块不擅自裁定（保留张力）。**

**⟹ 三点订正／补充：**
1. **「原文未表态」这条已由 Lean 侧独立核过并在案**，本轮不是首次发现；
2. **载体不止 P1/P2 两个** —— Lean 侧 Claim10 H4（`≤`）与 claim5 v0（`<`）是**第三、第四个**同型分歧点；
3. **Claim10 已明确「不擅自裁定，保留张力」** ⟹ 这项待裁**跨形式化层与生产层**，不是纯生产口径问题。

---

## 7. 原文谱系

线段概念在缠师 108 课中的演化链（最长、最复杂）。

### 第62课 (2007-06-30) — 直觉阶段

> 「而所谓的线段，就是至少由三笔组成。」（`062:26`【正文】）

- **首次引入**「至少三笔」；图9 展示「线段破坏」；**无形式化定义**

### 第65课 (2007-07-16) — 笔破坏定义（关键转折）

> 「对于从向上一笔开始的，其中的分型构成这样的序列：d1g1d2g2d3g3…dngn……如果找到i和j，j>=i+2，使得dj<=gi，那么称向上线段被笔破坏。」
>
> **缠中说禅线段分解定理**：线段被破坏，当且仅当至少被有重叠部分的连续三笔的其中一笔破坏……线段破坏的充要条件，就是被另一个线段破坏。

- 笔破坏形式化 `dj <= gi`；线段分解定理；「前三笔必须有重叠的部分」首次明确
- **未独立核实**：本轮未核该两行行号与归属（v1.3 记 `L32`／`L34`）。见 §16。

### 第67课 (2007-08-01) — 特征序列法（核心创新）

- **特征序列**：向上段取向下笔（X序列），向下段取向上笔（S序列）
- **标准特征序列**：对特征序列做包含处理后的序列（`067:20`【正文】，归约到普通K线包含 ⟹ §2）
- **第一种情况**（无缺口）：分型出现即终结
- **第二种情况**（有缺口）：`067:38`【正文】须第二特征序列出分型才终结；`067:46`【正文】「只要有分型就可以」（⟹ §3）
- **缺口封闭**：`067:170`／`:196`【答疑】（⟹ §4）

### 第71课 (2007-08-16) — 包含关系作用域（再分辨）

> 「这里还要强调一下包含的问题……在这假设的转折点前后那两元素，是不存在包含关系的……但假设的转折点后的顶分型的元素，是可以应用包含关系的。」（`071:42`【正文】，该课界在 `:46`）

- **转折点两边不能包含**；**转折点后可以包含**

### 第77课 (2007-09-05) — 线段方向与奇数性

见 §0.1 `077:62`【正文】逐字全录（**G-1 的决定性依据**）。
另：「线段和笔，都是有方向的……以向上笔开始的线段一定结束于向上笔」／「这三笔必须有重叠的部分」（v1.3 记 `:76`／`:78`，本轮未独立核实行号）。

### 第78课 (2007-09-06) — 古怪线段（最后补全）

> 「所有古怪的线段，都是因为线段出现第一种情况的笔破坏后最终没有在该方向由该笔发展形成线段破坏所造成的，这是线段古怪的唯一原因。」（v1.3 记 `L92`，本轮未独立核实；该课界在 `:64`）
>
> 「对于第二种情况的第二特征序列的分型判断，**必须严格按照包含关系的处理来**，这里不存在第一种情况中的假设分界点两边不能进行包含关系处理的要求……而在第二种情况的第二特征序列中，**其方向是和原线段一致**。」（`078:54`【正文】，本轮**已逐字核实** ⟹ §3）

- **古怪线段的唯一原因**；**笔破坏 ≠ 线段破坏**；**硬约束「顶肯定要高于底」**

### 谱系小结

| 阶段 | 课号 | 日期 | 性质 |
|------|------|------|------|
| 直觉阶段 | 62 | 2007-06-30 | 至少三笔 |
| 笔破坏定义 | 65 | 2007-07-16 | `dj<=gi`，线段分解定理 |
| 特征序列法 | 67 | 2007-08-01 | 两种情况完整定义 |
| 包含关系作用域 | 71 | 2007-08-16 | 转折点两边禁包含 |
| **方向与奇数性** | **77** | **2007-09-05** | **「至少三笔」＋「两笔不能构成线段」＋「笔数是单数」⟹ G-1 决定性依据** |
| 古怪线段 | 78 | 2007-09-06 | 第二特征序列方向与包含处理 |

---

## 8. 定义（正本）

线段是由**至少三笔**组成的结构，且前三笔必须有重叠部分。**笔数为奇数。**

### 线段划分定理

线段被终结，当且仅当至少被「有重叠部分的连续三笔」中的某一笔终结。等价表述：**旧线段终结 = 新线段形成**。

### 方向与奇数性

- 方向与起始笔一致（向上笔开始 = 向上线段）
- **线段的笔数一定是奇数**（`077:62` 逐字「线段中包含笔的数目，都是单数的」）
- **⟹ 「恒 2 笔」和「恒 3 笔」都不满足奇数性 + ≥3 的联合要求**：P3（恒 2）违反两条；P1（恒 3）满足两条但**只取下界**，见 §0。

### 硬约束（第78课）

> 「一个线段，两端的一顶一底，顶肯定要高于底」

违反此条件的线段划分是错误的。这是定义的一部分，不是后置过滤。

### 相切边界

**未裁**（§6）。当前两个生产载体不一致（P1 `<` / P2 `<=`），Lean 侧两个载体也不一致。

---

## 9. 口径 A：三笔重叠线段（P1，原 v0）——参考实现

**定义**: 连续三笔 price range 有交集即成段。

**规则**:
- `max(lows of 3 strokes) < min(highs of 3 strokes)` → 重叠成立（**`<`，开区间，相切不算** —— 见 §6）
- 贪心推进：成段后跳三笔继续扫描（`a_segment_v0.py:157`）
- 方向 = 第一笔方向
- **段长恒 3 笔**（`:130` `s1=j + 2` 写死）

**实现**: `src/newchan/a_segment_v0.py:148 :: segments_from_strokes_v0`

**名分**: **`[新缠论:选择]`** —— 「三笔重叠」四字原文零命中（§0）。

**特征**: 无特征序列分析，无分型验证，无结算锚。

> **★ 已删除 v1.3 `:199` 的「v0贪心段数是v1的2-3倍」** —— 该读数与仓内 70%／71% 三方打架，且同属单标的单切片点估计，**已作废**（§1）。**当前两口径的段数比无有效在案读数。**

## 10. 口径 B：特征序列线段（P2，原 v1）—— **正本口径**

**定义**: 反向笔组成特征序列 → 特征序列做包含处理（**方向性**，§2）→ 标准特征序列出现分型 → 旧段终结、新段生成。

**规则**（第67课完整定义）:
1. 构造特征序列：向上线段取反向笔（X序列），向下线段取反向笔（S序列）
2. 特征序列元素当K线做**方向性**包含处理 → 标准特征序列（**外包络那一档不得作生产判据**，§2.1）
3. 标准特征序列上找分型：向上线段找顶分型，向下线段找底分型
4. 分型出现 → 两种情况：
   - **第一种**（无缺口）：分型极值点即线段终点，分型形成即**直接终结**
   - **第二种**（有缺口）：**须**从分型极值点开始的**同向笔**序列（`078:54`「方向和原线段一致」）出现分型才终结，且**只要有分型就可以**（`067:46`），并**必须严格按包含关系处理**（`078:54`）
5. 结算锚验证：新段前三笔必须重叠（相切边界未裁，§6）
6. **缺口封闭做 `strict` 档**（§4）
7. **第二特征序列扫描窗口未裁**（族I `50` / theta_v0 无限，§5.1）

**实现**: `src/newchan/a_segment_v1.py :: segments_from_strokes_v1`；`rust/src/segment.rs`（现役生产）；`rust/src/theta_v0/parser/segment.rs`（theta_v0）

**名分**: **`[旧缠论]`**（`077:62` + 067/071/078）。

**特征**: 有 `_FeatureSeqState` 状态机，有 `BreakEvidence` 断段证据，有结算锚。

## 11. 口径 C：Lean 构造层（P3）—— **判违规**

**实现**: `formal/Origin/SegmentConstruction.lean:63-67` `scanSegEnd` → `:80` `segmentsOf` → `formal/Origin/Pipeline.lean:137` `pipelineSegments`

**接受集**: 在生产笔流上**恒 2 笔**（§0.2 推论链）。

**名分**: **判违规** —— `077:62` 专门排除两笔成段。

**判据对的那半在哪**: `formal/Phase2/Claim10_SegmentV1.lean`（P2 的谓词层），**与管线零 import 连线**（§0.4）。

---

## 12. 共享数据结构

两个 Python 口径共用 `Segment` dataclass（定义在 `a_segment_v0.py`）：`s0/s1`（stroke index 范围）、`i0/i1`（merged bar index 范围）、`direction`、`ep0_*/ep1_*`（端点）、`kind`（settled/candidate）、`break_evidence`、`confirmed`。

## 12.1 边界条件

- 只有两笔：**不成段**（`077:62` 明确排除）
- 连续三笔无重叠：不成段
- **偶数笔的结构不是线段**（`077:62`）
- 古怪线段（78课）：第三笔在第一笔范围内、无法构成特征序列时，破坏笔未在该方向发展成线段破坏 → 旧段延续。与第67课「两种情况」正交
- **相切边界（`<` vs `<=`）：未裁**（§6）

## 13. v1 状态机审计结果（2026-02-16，v1.3 遗留）

| 检验点 | 状态 | 说明 |
|--------|------|------|
| 第一种情况（无缺口→分型出现即直接终结） | ✅ | `scan_trigger()` + 主循环 |
| 第二种情况（有缺口→第二特征序列出分型） | ✅ 已修复 | `_second_seq_has_fractal()` 从同向笔独立构建第二特征序列 |
| 结算锚（新段前三笔重叠） | ✅ | `_three_stroke_overlap` |
| 包含作用域（71课约束） | ✅ | reset机制隐式规避跨段包含 |

**第二种情况修复详情**（v1.2，保留原文以支撑 §3.1）：
- **旧实现**：`_has_any_fractal_after(self.std, i+1)` 检查第一特征序列（反向笔）的延续——**这是错误的笔集合**
- **新实现**：`_second_seq_has_fractal(strokes, seg_dir, b_stroke)` 从分型中心之后收集**同向笔**，独立做包含处理，检查任意分型
- **原文依据**：`067:38`【正文】明确指向新的独立序列
- **测试**：37/37通过

> **⚠️ 该审计的四个 ✅ 全部是 level 0、单窗口、未声明覆盖率的判定，且是在 `stroke_mode="wide"`（旧笔）下做的** —— 笔侧已裁切新笔（`bi.md` v2.0 §5）⟹ **须重跑**。见 §16。

---

## 14. 挂在 S-1（笔口径）上的关系

| 项 | 挂不挂 | 说明 |
|---|---|---|
| **G-1 判据本身** | 不挂 | 「至少三笔 / 奇数 / 特征序列法」不依赖笔的间距口径 |
| **G-1 的所有段数／确认率读数** | **挂** | 笔集合变 ⟹ 段数全变 |
| **G-3 判据本身** | 不挂 | 但**所有段数/确认率读数挂** |
| **「P3 恒 2 笔」这条推论** | **不挂** | 只依赖交替性；三档 S-1 判据在交替序列上都成立（`bi.md` v2.0 §14） |
| **G-2／G-4／G-5 判据层** | 不挂 | |
| **G-2／G-4／G-5 的量化读数** | **挂** | 含 Δ=2 段（1.2%）与 5.7 倍两条 |

**⟹ 笔侧已裁「生产默认切新笔」 ⟹ 线段侧一切段数读数在切档后须重出。**（含 §1 已作废的那批、§4 的 5.7 倍、§1.1 的 Δ=2、以及 v1.3 的 37/37 与 4/4 审计。）

---

## 15. 受影响代码清单（行号本轮逐条 grep 核实）

| # | 文件:行号 | 要改什么 | 为什么 | 归哪张票／SPEC |
|---|---|---|---|---|
| X-1 | `formal/Origin/SegmentConstruction.lean:63-67`（判据行 `:66`） | `scanSegEnd` 恒切 2 笔 ⟹ 须改为 ≥3 笔且笔数为奇数 | `077:62`【正文】明确排除两笔成段（§0.1） | **归 SPEC**（形式化层，不在本票动手） |
| X-2 | `formal/Origin/Pipeline.lean:137`（＋ `:84` 注释） | 管线接**判据层**（Claim10 的 P2）而非构造层 P3 | `:84` 自陈「用构造层不用判据层完整版」⟹ 装了错的那半（§0.4） | **归 SPEC**（形式化层） |
| X-3 | `formal/Phase2/Claim10_SegmentV1.lean` | 补 import 连线／或迁入 `Origin` | **零 import 行、全 `formal/` 除 lakefile 外零引用** ⟹ 判据对的那半没接管线 | **归 SPEC**（形式化层） |
| X-4 | `rust/src/theta_v0/parser/segment.rs:384` | 删／改「对齐 `Origin.SegmentConstruction.segmentsOf` 输出」这句 | 与同段 `:375-376`「**非**对 Origin Lean bit-exact」自相矛盾，且按 §0.2 该句**为假** | **#813 落地（措辞批）** |
| X-5 | `rust/src/theta_v0/parser/segment.rs:380`／`:382` | 「406 段 vs 参考 237 段，70% 过分段」标注为**已作废历史记录** | 不可复现、测的是已废弃旧批处理、harness 已退役、数据从未入库（§1） | **#813 落地（措辞批）** |
| X-6 | `analysis/segment_refsem_cert.py:7-8` | 同 X-5 | 同上 | **#813 落地（措辞批）** |
| X-7 | `rust/src/theta_v0/parser/feature_seq.rs:19-20` | 同 X-5；**并保留 `:20-21` 的 Δ=2 段（1.2%）**（那条是真读数） | 同上；§1.1 | **#813 落地（措辞批）** |
| X-8 | `rust/src/theta_v0/parser/segment.rs:195-196` `process_feature_inclusion` | **补一道锁**（`#[cfg(test)]` 或显式 deprecated 标注 + 单测断言无生产调用者），并在 doc 写明「不得作生产判据，仅 Lean 对拍锚」 | 外包络零原文依据；现为 `pub(super)`，BUG-08 前曾生产可达且无锁（§2.1） | **#813 落地（G-2 补锁）** |
| X-9 | `formal/Origin/SegmentAutoConstruct.lean:177`（＋ `:174` 自承代理） | 「剩余**反向**元素 + 只找**对偶**分型」→ 改「**同向**笔 + **任意**分型」 | `078:54`「方向和原线段一致」＋ `067:46`「只要有分型就可以」（§3） | **归 SPEC**（形式化层） |
| X-10 | `formal/Phase2/Claim10_SegmentV1.lean:46`／`:47` | 两个读法并列 ⟹ 收成一个（按 §3 取 `:47` 那条） | Lean 内部已登记该分歧 | **归 SPEC**（形式化层） |
| X-11 | `rust/src/theta_v0/parser/feature_seq.rs:284` `ExtendMode::Optimized` | 删该档（连带 4 测试 + 4 rust 调用点） | **零原文依据**，无生产构造点，两档实测差 **5.7 倍**（§4） | **归 SPEC**（本票不动手） |
| X-12 | `rust/src/segment.rs:413` `MAX_SECOND_SEQ_SCAN = 50` | **暂不动** —— 待裁 | 两侧都在生产；且 `50` 兼任 checkpoint margin（`orchestrator.rs:110`／`:215`）⟹ 改它连带动 checkpoint（§5.1） | **#813 待裁（G-5a 未收口）** |
| X-13 | `rust/src/segment.rs:410` `TAIL_WINDOW = 7` | **不动** | **G-5b 撤票**（该侧无分歧，§5.2） | **撤票，无归属** |
| X-14 | `src/newchan/a_segment_v0.py:162` `<` vs `rust/src/theta_v0/parser/segment.rs:116` `<=` | 统一相切边界 | **原文未表态** ⟹ `[新缠论:选择]`；且 Lean 侧 `Claim10:471-474` 已登记同型分歧并明写「不擅自裁定」（§6） | **#813 新增待裁项（未收口）** |
| X-15 | G-4 与「D-5」的口径一致性 | 收口 | 考据未拿到 D-5 票面，只给了三条结构对齐点（§4） | **#813 待办（不假装已对齐）** |

**穷举方法声明**：X-1~X-14 的行号均本轮 `grep`／`sed` 逐条核实。**可达性断言（X-8／X-11）的覆盖范围 ＝ `rust/src/`＋`src/`＋`scripts/`＋`.github/`＋`formal/`，未覆盖 `frontend/`、`trading_system/`、`prototypes/`、NT 适配层** ⟹ 一律写「**已查到的有 N 处**」，不写「全部」。

---

## 16. 未测／查不到表（照实，不写成「不受影响」）

| # | 未测／查不到什么 | 去向 |
|---|---|---|
| V-1 | **未跑 Lean** —— §0.2／§0.4／§3 的 Lean 侧结论**全是源码推导**，零 `lake build`／`lake env lean` 验证 | **归 #813 待办** |
| V-2 | **未跑任何 harness** —— §1／§1.1／§4 三组读数**全为出处考据，非复现** | **归 #813 待办** |
| V-3 | **OKLO 数据从未入库**（`_xcheck_oklo_strokes.json` 全仓 0 命中）⟹ 「406 vs 237」与「70%」两组读数**无复现路径** | **作废（§1），不再跟进** |
| V-4 | **「7→∞ 输出不变」仓内无可跑 harness** —— `rust/src/theta_v0/parser/profile.rs:104`（`:103` `#[ignore]`）只测 0/50/200 | **G-5b 已撤票；若日后要裁 G-5a 须新建 harness** |
| V-5 | **`071:42` + `078:54`「分界点两侧禁包含」在生产落没落，未核完**（Lean Phase2 侧已落 `Claim10_SegmentV1.lean:269-271`，生产侧只查到 v1 的 reset 机制「隐式规避」） | **归 #813 待办** |
| V-6 | **未核 `frontend/`、`trading_system/`、`prototypes/`、NT 适配层** ⟹ §2.1／§4 可达性断言的**覆盖边界** | **无人跟进**（须新开票；与 `bi.md` v2.0 U-13 同一件） |
| V-7 | `formal/Origin/SegmentFeatureComplete.lean`（47 处命中）**只读了 2 行** ⟹ 判据层完整版到底写了什么，**未读** | **归 #813 待办**（X-2 的前置：不知道它写了什么就不知道该不该接） |
| V-8 | §0.3 的三范式关系见证（五笔手算样例）是**手算，未实跑** | **归 #813 待办** |
| V-9 | **P2 与 P3 的实际段数差从未测过** —— §1 作废的那个读数测的是别的东西（旧批处理 vs Python v1），**三范式之间的量级差无任何在案读数** | **归 #813 待办**（G-1 的量化空洞） |
| V-10 | v1.3 的「v1 状态机 4/4 审计」与「37/37 测试」都是在 **`stroke_mode="wide"`（旧笔）** 下做的，**level 0、单窗口、覆盖率未声明** ⟹ 笔侧切新笔后**须重跑** | **归 #813 落地（切档批）** |
| V-11 | **G-4 与「D-5」的口径一致性未收口** —— 考据未拿到 D-5 票面 | **归 #813 待办（X-15）** |
| V-12 | 第65课（`L32`／`L34`）、第77课（`:76`／`:78`）、第78课（`L92`）**五处在案引用的行号与归属本轮未独立核实** | **归 #813 待办**（「课号:行号」形式的在案引用不构成已核实） |
| V-13 | **两族（族I vs theta_v0）线段层从未对拍** —— 与笔层同型（`bi.md` v2.0 U-3） | **归 SPEC [#847](https://github.com/xy7365527-lang/NewChanlun/issues/847)** |
| V-14 | `067:20` 行尾编纂者注指出原文「没有明确同一段前不能进行非包含处理」——**这个欠缺在生产落成什么样，未核** | **归 #813 待办**（与 V-5 相邻但不同） |

---

## 17. 谱系引用

- `001-degenerate-segment`：退化段问题（**⚠️ 见 `bi.md` v2.0 §6.1：该谱系 `:56`／`:81` 方向写反、`:89`／`:90` 措辞须降级**）
- `003-segment-concept-separation`：线段两口径的正式分离记录（**⚠️ 只登记了 P1/P2 两个，缺 P3 ⟹ 须补**）

## 18. 影响声明

线段是笔之上、中枢之下的结构。线段定义变更影响：中枢识别（中枢由线段构成）、走势类型划分、买卖点判定。

**当前状态**：
- **正本口径 ＝ P2（特征序列法）**，`[旧缠论]`
- **P1 降为参考实现**（`[新缠论:选择]`，不进正式管线）
- **P3 判违规**，但**它正是 Lean 管线现在接的那一套**（`Pipeline.lean:137`）⟹ **形式化层与正本口径当前不一致**，归 SPEC（X-1／X-2／X-3）
- **三项未裁**：相切边界（§6）／第二特征序列扫描窗口 G-5a（§5.1）／G-4 与 D-5 的一致性（§4）
- **一切段数读数须在笔侧切新笔后重出**（§14）
