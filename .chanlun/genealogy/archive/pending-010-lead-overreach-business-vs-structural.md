---
id: pending-010-lead-overreach-business-vs-structural
timestamp: 2026-04-27
status: 已结算
settlement: 吸收
settled_by: '275'
settled_date: 2026-05-23
settled_classification: 定理
settlement_scope: "有数据依赖的结构工位事后激活（无依赖情形保留张力，见结算节）"
type: meta-rule
negation_source: homogeneous
negation_form: expansion
topo_effect: "absorb:Lead-spawn-asymmetry→275:by-275"
---

## 结算（2026-05-23，定理类自动结算）

**四分法分类：定理**（已结算原则 275号的逻辑推论），故不上浮、不擅自创设新规则。

**结晶四分法：吸收进 275号（局部依赖原则）。**

矛盾消解依据本号自身推导链第 39-40、51 行：结构工位（genealogist 谱系写入）
**数据依赖于**业务工位（W-1..W-10）的 summary 输出。按 275号"附庸的附庸不是我的附庸"，
一个数据依赖于上游输出的节点，其在上游完成后激活是**合法的局部依赖**，不是对 218号
并行原则的违反。218号割的是"无数据依赖却串行"的尾巴；本例存在真实数据依赖，不在其射程内。

**元规则候选不结晶**：本号作为"业务/结构 spawn 不对等"新元规则候选是 N=1 实例，
低于 029a 的 N=3 结晶阈值。路径 A（升格 137/407 扩展）暂不成立——需累积更多实例
（与 W-10、pending-001 同模式者）达 N≥3 后再评估。当前以路径 B（合法数据依赖）结算。

**保留的张力（待累积）**：若后续出现"结构工位本可与业务工位并行（无数据依赖）却被事后激活"
的实例，则脱离 275号射程、构成真违反，届时重开本号并向编排者上浮路径 A。本次结算
仅消解"有数据依赖"这一具体实例，不预先封闭无依赖情形。

**影响声明**：本结算不改动 src/ 或 ceremony_scan.py；仅将本节点从生成态转为已结算（吸收）。
若编排者不认同数据依赖读法，可凭本节点保留的张力条款重开。

# Lead 在本 session spawn 10 业务工位 + 0 结构工位——角色边界候选

## 矛盾

本 session（v76+ 系列）：
- Lead 一次性 spawn 10 个业务工位（W-1..W-10，全量谱系遍历）
- 直到全量遍历完成后才被激活的结构工位（本工位 = genealogist）才进场

这意味着 spawn 阶段：
- **业务工位（理解谱系内容）：10 个**
- **结构工位（谱系写入/张力检查/结晶检测）：0 个**

CLAUDE.md / dispatch-dag.yaml 声称 genealogist 是"常驻结构工位"——按真递归原则（095/097号），结构工位应与业务工位**并行 spawn**，而不是事后激活。

## 是否构成新元规则候选？

候选 1：**业务/结构工位 spawn 不对等**
- 是 137号（RLHF 基底约束）的新实例？
  - 137号：否定性禁令对行为执行层无效，需正面格式
  - 本号：声明"genealogist 常驻"对 spawn 行为无效，需 ceremony_scan 强制 spawn
- 是 407号（compact 三层不对等）的新实例？
  - 407号：声明层在 compact 后效力为零，需 hooks 注入
  - 本号：spawn 阶段的"结构工位常驻"是声明层效力——确实有 RLHF/路径依赖问题
- 候选评估：**两者都印证**——本事件同时是 137 + 407 的扩展实例

候选 2：**业务先行，结构后置**模式
- "先做事，再观察"是 RLHF 默认惯性
- 与 218号（Lead 并行）+ 275号（局部依赖）原则冲突
- 但与"结构工位需要业务工位的输出作为输入"的数据依赖事实一致——genealogist 确实需要 W-1..W-10 的 summary 才能写谱系
- 因此本号既可能是新元规则（违反并行），也可能是合法数据依赖（结构工位下游于业务工位）

## 推导链

- 095号：真递归默认（部分否定 073b）
- 097号：五特征 DAG（任务/审查/结晶/异质审计/状态管理）
- 137号：RLHF 基底约束
- 218号：Lead 并行（割串行尾巴）
- 275号：局部依赖
- 407号：compact 三层不对等
- 本 session 实证：spawn 时 10 业务 + 0 结构 → 结构工位事后激活
- 张力：结构工位（谱系写入）确实数据依赖于业务工位输出 → 不是严格违反并行原则

## 谱系链接

- 095号、097号（真递归 + 五特征）
- 137号、218号、275号、407号（Lead 并行 + 局部依赖 + RLHF）
- 094号（审计层断裂 Gap——结构工位是审计层）
- pending-001（scan 状态不可观测）——同源症状

## 影响声明

- 影响：`scripts/ceremony_scan.py`、Lead spawn 行为、dispatch-dag.yaml event_skill_map
- 改动方向（**本号不擅自结算，请编排者裁决**）：
  - 路径 A：升格为 137/407 的扩展实例——结构工位常驻声明应通过 hook 强制（如 ceremony 启动时 ceremony_scan 必产出至少 1 个结构工位）
  - 路径 B：判定为合法数据依赖——结构工位本质上下游于业务工位，事后激活合法（但需 dispatch-dag 显式标注依赖序）
  - 路径 C：拒绝立项——单实例不达结晶阈值（029a N=3）

## 单实例阈值检查

本号是 N=1 实例，未达结晶阈值。但与下列实例同模式：
- W-10 自标"v232→v236 跨度 3 swarm 未执行 + meta-observer 工位的可靠性低于其他工位类型"
- pending-001 的 scan 状态不可观测同源（结构层缺消费）
如果将"结构层在 spawn 阶段缺位"作为模式归并，可能达 N≥2 → 接近阈值。

## 异质审计降级

本 session 全程 gemini-challenger 不可用。本号涉及 Lead 自身行为，异质审计价值高——下游 Lead 应优先 spawn gemini-challenger 工位质询。
