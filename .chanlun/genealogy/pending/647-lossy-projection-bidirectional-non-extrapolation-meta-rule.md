---
id: "647-WITHDRAWN"
number: 647
type: meta-rule
status: 已撤回   # 编号碰撞 + 内容重复：本条与 meta-observer 的 647-meta-observation-object-identity-must-precede-falsification-design.md 是同一元结构。后者是上位抽象（O_impl ⊊ O_def 致否证错对象），已完整纳入 643/644/645 三实例。本条（有损投影双向不外推）是其子集——唯一净增量「PASS/FALSIFY 双向显式化 + 生成性完备检验」已建议并入 object-identity 647（见下）。撤回理由=no-patch-mentality（删除重复，不让两条 meta-rule 并存讲同一件事）。
date: "2026-06-29"
withdrawn_by: genealogist
withdrawn_reason: |
  本条写入时未察觉 meta-observer 同轮已写 647-object-identity（编号碰撞 = 并行工位各取同号）。
  内容核验后确认本条是 object-identity 的子集：
  - object-identity 的「两亚型」（644 坐标系替换 / 645 有损投影）已含本条的 PASS 方向（644/643 守卫 PASS）+ FALSIFY 方向（645）。
  - object-identity 的「O_impl ⊊ O_def → 否证不外推」= 本条「有损映射像空间结论不外推原空间」的同一命题，措辞不同。
  本条唯一净增量（建议并入 object-identity 647，由 meta-observer 或 /ritual 执行）：
  1. **PASS/FALSIFY 双向显式化**：object-identity 隐含双向（两亚型一个 PASS 一个 FALSIFY），但未显式标「判断的两个真值方向都不外推」。建议在其「两亚型统一」表后补一行：643/644=PASS 方向（投影相等不证原相等），645=FALSIFY 方向（投影否证不证原否证），双向结构完备。
  2. **生成性完备检验**：本条给出元规则的两个可生产新案例（(a) L1 合成 PASS 不外推 L2 真实——统一解释 625/643 层1；(b) benchmark FALSIFY 不外推原能力——436号同构）+ 有效域边界（f 在结论相关维度无损则可外推）。建议并入 object-identity 的 downstream_implications，使其从「事后归纳」升级为「能生产新案例」的生成性规则。
supersedes_pointer: ".chanlun/genealogy/pending/647-meta-observation-object-identity-must-precede-falsification-design.md"
---

# 647（本条）已撤回——并入 object-identity 647

本条（lossy-projection-bidirectional-non-extrapolation）与 meta-observer 的
`647-meta-observation-object-identity-must-precede-falsification-design.md` 是**同一元结构**，
编号碰撞（并行工位各取 647）。后者是上位抽象，已完整纳入 643/644/645 三实例。

本条撤回（no-patch-mentality：删除重复，不让两条 meta-rule 并存讲同一件事）。
唯一净增量（PASS/FALSIFY 双向显式化 + 生成性完备检验）已写入上方 frontmatter `withdrawn_reason`，
建议由 meta-observer 或编排者 /ritual 并入 object-identity 647。

**编号协调**：本轮存在三个 647（lossy-projection[本条,已撤回] / object-identity / pibsp-subvoice）。
撤回本条后剩两个 647 仍碰撞（object-identity 是 meta-rule，pibsp-subvoice 是 bias-correction，
不同轴不应合并）——统一编号待编排者 /ritual 在统一编号空间分配（turning-node pending 先例）。
genealogist 不自行给别的工位的文件重编号。
