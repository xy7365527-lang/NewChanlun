---
id: "666"
number: 666
status: 生成态   # genealogist 结构记录：级别相位 (−1)^ℓ 作为结构状态变量（新概念，657 addendum_C 未捕获）+ beta/alpha 命名纠正（上一轮 parity 报告反了，本 PDF §7 纠正）+ 奇偶交替不可识别性三机制 A/B/C。基于 GPT 咨询《奇偶相位》(parity-phase-consult-20260701.txt 12页)。**不自结算**（级别相位是否「缠论正确形式」须 L2+ 反事实实证 = parity-analysis 正在做；命名纠正=事实更正）。
date: "2026-07-01"
type: bias-correction   # 双段：①级别相位结构变量（657 定理1「须纳 ρ」的 refinement——ρ 的一个具体分量是 (−1)^ℓ 级别相位，657 只泛泛说「纳 ρ」未指认相位分量）；②beta/alpha 命名纠正（上一轮 parity 报告把 (μ+−μ−)/2 当结构 alpha、(μ++μ−)/2 当 beta，方向签名 PnL 下反了）。
depends_on: ["231", "657", "663"]
related: ["645", "656", "664", "665", "660", "alpha-pdf-mu-z-a-positive", "project_regime_is_level_truncation_artifact", "project_recursive_t_architecture_v2", "project_zero_lookahead_backtest"]
title: "★级别相位 (−1)^ℓ 作为结构状态变量 + beta/alpha 命名纠正。GPT《奇偶相位》12页回应全历史 BTC 12/12 级别×方向 μ̂ 奇偶交替（买 L0+L1−L2+L3−L4+L5−，卖反相，sign μℓ,δ=sign(δ(−1)^ℓ)，正期望 δ*ℓ=(−1)^ℓ 偶买奇卖）。三增量：①级别相位——「级别不是中性索引，携带相位信息；同一『买点』语法在不同级别不是同一经济状态」，状态须从 z=(ℓ,δ) 升级到 z=(ℓ,δ,(−1)^ℓ,σhigher,exit rule,确认滞后,父级容器状态)。这是 657「须纳 regime 代理 ρ」的具体分量指认（657 未捕获相位分量），也是 663 pooled 混合的机制化。②beta/alpha 命名纠正：方向签名 PnL 下 (μ++μ−)/2≈aℓ−C 更接近方向中性结构 edge，(μ+−μ−)/2≈bℓ 更接近市场漂移 beta——上一轮 parity 报告命名反了，须标注更正防下游误用。③不可识别性：仅 12 均值符号不能唯一识别主因（机制A 递归几何反相 / B 出场配对滞后 / C 级别=regime 三者都产生同样交替），须反事实（换出场规则/测滞后/加 σhigher/同容器配对）。正确操作=偶级别只做买奇级别只做卖（正类交易负类不交易，X_inv=−X−2C 仅 μ<−2C 反向才正）。peer 663（判据）/664（对象）/665（方法论）同簇。"
negation_source: "GPT《奇偶相位》PDF（parity-phase-consult-20260701.txt，12页，第三方咨询，回应蜂群全历史 BTC 12/12 奇偶交替发现）+ alpha.pdf μ(z,a)>0 判据 + 657（定理1 须纳 ρ，本号指认 ρ 的相位分量）"
negation_form: "expansion"   # expansion：上一轮 parity 报告（规定者）在执行中违反自身——报告声称 (μ+−μ−)/2=结构分量/(μ++μ−)/2=beta，但方向签名 PnL 下代数推导（PDF §7）给出相反归属。规定者产出的命名与其自身依据（方向签名收益分解）不一致=膨胀型否定。级别相位段则是 refinement（对 657「纳 ρ」的分量指认，非否定）。

# 拓扑效果标注（147号下游推论3：negates 非空必填）
# 本号 negates 一个对象：上一轮 parity 报告的 beta/alpha 命名归属
# 以否定事件的实际拓扑后果（retrospective，141号结论1）判断，非 negation_form 预分类
topo_effect: "split:parity-alternation-report-naming:downstream"
# split:parity-alternation-report-naming:downstream — 上一轮 parity 报告节点分裂：一个保留「奇偶交替 12/12 观测事实 + 三候选机制方向（几何反相/出场滞后/上级过滤）成立」，一个携带「beta/alpha 命名归属反了——方向签名 PnL 下 (μ++μ−)/2 是结构 edge 非 beta，(μ+−μ−)/2 是市场漂移 beta 非结构」违反记录。下游（任何引用该报告 beta/alpha 命名的 econ_positive / parity-analysis 叙事）随命名更正而分裂——用旧命名的结论须重标。仅命名归属分裂，观测事实与机制候选保留。

# 矛盾（type=bias-correction：双段——级别相位 refinement + 命名纠正）
contradiction:
  description: |
    全历史 BTC（461万 bar，12626 买卖点）跑出 **级别×方向 μ̂ 奇偶交替 12/12 无例外**：
    买方向 L0+ L1− L2+ L3− L4+ L5−，卖方向反相。写成数学命题：
        sign μℓ,+ = (−1)^ℓ,  sign μℓ,− = −(−1)^ℓ  ⟺  sign μℓ,δ = sign(δ·(−1)^ℓ)
    正期望方向 δ*ℓ = (−1)^ℓ（偶级别做买，奇级别做卖）。

    GPT《奇偶相位》PDF 严格回应，三个增量（657 addendum_C 未捕获，非重复）：

    **增量①：级别相位 (−1)^ℓ 是结构状态变量（新概念）。**
    「级别不是中性索引，而携带相位信息。」若 L0 买点为正、L1 买点为负、L2 又为正，则
    **同一个『买点』语法在不同级别上不是同一个经济状态**。这否定过粗状态 z=(δ,Iγ) 或 z=(买卖点类型)。
    严格状态须升级：
        z = (ℓ, δ) → z = (ℓ, δ, (−1)^ℓ, σhigher, exit rule, 确认滞后, 父级容器状态)
    这**不是** 657 addendum_C 已覆盖的内容——657 只说「须纳 regime 代理 ρ」（泛指），本号指认
    **ρ 的一个具体分量是级别相位 (−1)^ℓ**：级别递归每升一级操作相位翻转一次。657 定理1 说「同一 z
    在不同 r 下 μ 翻转，pooled≤0」，本号给出 r 的一个精确分量（级别相位）+ 交替的精确形式（(−1)^ℓ）。

    **增量②：beta/alpha 命名纠正（上一轮 parity 报告反了）。**
    上一轮 parity 报告提出：(μℓ,+ − μℓ,−)/2 = 结构分量，(μℓ,+ + μℓ,−)/2 = beta 候选。
    PDF §7 严格推导（方向签名 PnL 下）给出**相反**归属：设共同市场漂移 bℓ、结构边际 aℓ，
        E[ΔP|buy,ℓ] = bℓ + aℓ,  E[ΔP|sell,ℓ] = bℓ − aℓ
        μℓ,+ = bℓ + aℓ − C,  μℓ,− = −bℓ + aℓ − C（成本近似相等 C+≈C−=C）
    则：
        (μ+ + μ−)/2 ≈ aℓ − C  → 方向中性结构 edge（买卖两边都因结构正确受益时为正）
        (μ+ − μ−)/2 ≈ bℓ      → holding window 上的市场方向漂移 / beta
    **上一轮报告命名反了。** 依赖买卖样本持有期/成本/时间分布近似可比（否则不能直接分解）。
    严格剥离 beta 须：随机置换方向标签 / 回归控制 holding return / 同容器配对（PDF §8 方法1-3）。

    **增量③：奇偶交替不可识别性（三机制）。**
    仅 12 个均值符号不能唯一识别主因。至少三种机制都产生同样交替：
    - 机制A 递归几何相位反相：ϕℓ=(−1)^ℓ，μℓ,+≈Aℓϕℓ（Aℓ>0）⟹ 每升一级操作相位翻转，几何必然。
    - 机制B 出场配对滞后：出场 τℓ^out 落在 ℓ+1 级反相区间，同方向相邻级别收益符号翻转。
    - 机制C 级别=regime：R 分布随级别变 P(R|ℓ)，pooled 后 μℓ,δ=Σr μℓ,δ,r P(r|ℓ,δ) 出现交替。
    机制参数数目 >12，都能拟合同一组符号 ⟹ 主因不可识别。须反事实：
    换出场规则（固定R/同级别反向/上级失效/下一反向/终点pivot/确认bar）/ 测 ηin ηout / 加 σhigher / 同容器配对。
    若只在「下一反向信号出场」下有交替、固定出场消失 ⟹ 出场口径主导（机制B）；
    多口径都保留 ⟹ 几何反相更可能是真结构（机制A）。

    这不是定义冲突（无两条缠论定义互斥）。增量①是对 657「纳 ρ」的分量 refinement；增量②是命名事实更正
    （expansion：报告命名与其自身方向签名分解依据不一致）；增量③是不可识别性声明（有效域约束）。
    均可分层 ⟹ **不触发中断 #1**。
  layer: 概念   # 状态定义层（级别是否携带相位）+ 命名层（beta/alpha 归属）——非 rust 实装冲突。相位分量指认向下影响 657/663 的「纳 ρ」操作化。
  trigger: "编排者 ChatGPT 咨询《奇偶相位》PDF 回应蜂群全历史 BTC 12/12 级别×方向 μ̂ 奇偶交替发现——直接给出级别相位结构变量、beta/alpha 命名纠正、三机制不可识别性。"

definitions_involved:
  - name: "级别相位 (−1)^ℓ（级别递归每级翻转操作相位）"
    version: "GPT《奇偶相位》PDF §2/§12（parity-phase-consult-20260701.txt）"
    role: "核心新概念。级别不是中性索引，携带相位信息——同一买卖点语法在不同级别是不同经济状态。这是 657「须纳 regime 代理 ρ」的具体分量指认（ρ ⊇ {(−1)^ℓ, σhigher, exit rule, 确认滞后, 父级容器状态}）。657 未捕获相位分量，本号补。"
  - name: "beta/alpha 分解（方向签名 PnL 下）"
    version: "GPT《奇偶相位》PDF §7/§8/问题3"
    role: "命名纠正对象。(μ++μ−)/2≈aℓ−C=方向中性结构 edge（非上一轮报告说的 beta）；(μ+−μ−)/2≈bℓ=市场漂移 beta（非结构）。上一轮 parity 报告命名反了，本号 split 更正。严格剥离须随机置换/回归控制/同容器配对（不能只用 (μ+±μ−)/2 当终局结论）。"
  - name: "alpha.pdf / 全互斥定义策略：可交易性判据 μ(z,a)>0"
    version: "alpha.pdf（一级权威第三方证明）+ 663 真判据"
    role: "正确操作来源。若 OOS 证明 μℓ,δ>0 ⟺ δ=(−1)^ℓ，oracle 策略=偶级别只做买、奇级别只做卖（正类交易，负类不交易）。负类不能简单反做：X_inv=−X−2C，仅 μ<−2C 时反向才正。全互斥分类后所有语法状态都定义，但只交易正条件收益状态 χℓ,δ=1[LCB(μℓ,δ)>θ]。"
  - name: "657 定理1（分类完全 ≠ 收益充分统计量，须纳 ρ）"
    version: ".chanlun/genealogy/pending/657（生成态，addendum_C + tension_check_regime_dual）"
    role: "本号是其 refinement（parent）。657 说「同一 z 在不同 r 下 μ 翻转 pooled≤0，须纳 ρ 代理」（泛指），本号指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替的精确形式。657 已覆盖 PDF 定理1/定理2，本号补 657 未捕获的三增量（相位分量/命名纠正/三机制不可识别性）。"
  - name: "663 判据错误（统计显著性 ≠ μ>0 可交易性）"
    version: ".chanlun/genealogy/pending/663（生成态）"
    role: "印证 + 机制化。663 说真判据 μ(z,a)>0，本号给出 μℓ,δ 交替的精确形式并指出正确操作（正类交易负类不交易）是 663 判据在级别相位维度的实例。奇偶交替是 663 pooled 混合均值的机制化（级别相位是 pooling 变量之一）。"
  - name: "231 形式化有效域规则"
    version: ".claude/rules/formalization-validity-domain.md（settled）"
    role: "约束。增量③不可识别性 = 231 实例：12 均值符号（观测）的有效域不覆盖「唯一识别主因」（定义域=所有生成机制）。声称「奇偶交替=几何必然真结构」而无反事实 = 有效域膨胀。级别相位「是缠论正确形式」须 L2+ 反事实实证（=parity-analysis 正在做），当前仅 L2 观测（12/12 单标的 BTC）。"
  - name: "缠论买卖点定义（一/二/三类，级别无关同义）"
    version: "缠论知识库.md + docs/chanlun/text/blog/INDEX.md（一级权威）"
    role: "张力对象（见 tension_check）。缠论定义层：买卖点一二三类在每个级别定义相同（级别无关）。经济层：PDF 说同买点在不同级别是不同经济状态（期望符号交替）。定义层同义 ⊥ 经济层不同期望——可分层（定义 vs 期望是不同对象）。"

resolution:
  type: 未解决   # 级别相位结构变量已记录（657「纳 ρ」的分量 refinement）；命名纠正已记录（split 上一轮报告）；不可识别性三机制已记录。级别相位「是缠论正确形式」须 L2+ 反事实实证（=parity-analysis 正在做）。最终结算待编排者 /ritual。
  description: |
    概念记录（已完成）：GPT《奇偶相位》PDF 三增量落盘——
    ①级别相位 (−1)^ℓ 是结构状态变量（657「纳 ρ」的分量指认，657 未捕获）；
    ②beta/alpha 命名纠正（方向签名 PnL 下 (μ++μ−)/2=结构 edge、(μ+−μ−)/2=beta，上一轮报告反了，split 更正）；
    ③奇偶交替不可识别性（机制 A/B/C 都产生同样交替，须反事实识别主因）。

    正确操作（若 OOS 稳健）：偶级别只做买、奇级别只做卖（正类交易，负类不交易；负类反做仅 μ<−2C 才正）。
    这与 663 判据（μ>0 出现就做）一致——按级别相位选择正条件方向，非所有级别买卖双向都做。

    严格下一步（行动类，parity-analysis 独占 econ_positive）：反事实实验——换六种出场规则、测确认/出场滞后、
    加上级方向 σhigher、同容器配对——识别奇偶交替主因（几何反相 A / 出场滞后 B / 级别 regime C）。
    不预设结果（231 铁律：可能是出场口径产物 B，加 σhigher 后重组 C，或几何真结构 A）。
  decided_by: 待裁决   # 级别相位结构变量 + 命名纠正 = GPT PDF + 方向签名代数推导；反事实实验 = 行动类（parity-analysis）；genealogist 结构记录三增量 + 657 refinement + 命名 split；最终结算待编排者 /ritual

negated:
  description: "上一轮 parity 报告（parity-alternation-question-20260701.md）的 beta/alpha 命名归属：(μℓ,+ − μℓ,−)/2 = 结构分量（alpha），(μℓ,+ + μℓ,−)/2 = beta 候选。"
  why_negated: "方向签名 PnL 下代数推导（PDF §7）给出相反归属。设 E[ΔP|buy,ℓ]=bℓ+aℓ、E[ΔP|sell,ℓ]=bℓ−aℓ（bℓ=共同市场漂移，aℓ=结构边际），成本近似相等 C+≈C−=C，则 μℓ,+=bℓ+aℓ−C、μℓ,−=−bℓ+aℓ−C。于是 (μ++μ−)/2≈aℓ−C=方向中性结构 edge（买卖两边都因结构正确受益时为正，故是结构项非 beta）；(μ+−μ−)/2≈bℓ=holding window 市场方向漂移=beta 项（非结构）。上一轮报告把两者归属对调——命名与其自身方向签名收益分解依据逻辑不一致（expansion：规定者违反自身规定）。注：此分解依赖买卖样本持有期/成本/时间分布近似可比，否则不能直接分解——但即便如此，归属方向由代数结构决定（结构 edge 是买卖同号项、beta 是买卖异号项），上一轮报告的方向错误独立于可比性假设。"

new_output:
  definitions:
    - "★级别相位 (−1)^ℓ 作为结构状态变量：级别不是中性索引，携带相位信息——同一买卖点语法在不同级别是不同经济状态（正期望方向 δ*ℓ=(−1)^ℓ 交替）。状态须从 z=(ℓ,δ) 升级到 z=(ℓ,δ,(−1)^ℓ,σhigher,exit rule,确认滞后,父级容器状态)。这是 657「须纳 regime 代理 ρ」的具体分量指认——ρ 的一个精确分量是级别相位，657 只泛指未指认相位。"
    - "★beta/alpha 命名纠正（方向签名 PnL 下）：(μ++μ−)/2≈aℓ−C=方向中性结构 edge（非 beta），(μ+−μ−)/2≈bℓ=市场漂移 beta（非结构）。上一轮 parity 报告命名反了。结构 edge=买卖同号项（两边都结构正确则正）；beta=买卖异号项（市场漂移）。严格剥离须随机置换方向标签/回归控制 holding return/同容器配对，不能只用 (μ+±μ−)/2 当终局。"
    - "奇偶交替不可识别性：仅 12 均值符号不能唯一识别主因——机制A（递归几何反相 ϕℓ=(−1)^ℓ）/B（出场配对滞后 τℓ^out 落 ℓ+1 反相区间）/C（级别=regime，P(R|ℓ) 随级别变）都产生同样交替。参数数目 >12。须反事实识别：换出场规则/测滞后/加 σhigher/同容器配对。"
    - "正确操作（若 OOS 稳健）：偶级别只做买、奇级别只做卖（正类交易，负类不交易）。负类不能简单反做——X_inv=−X−2C，仅 μ<−2C 反向才正。全互斥分类后所有语法状态都定义，但只交易正条件收益状态 χℓ,δ=1[LCB(μℓ,δ)>θ]。与 663 判据（μ>0 出现就做）一致。"
    - "短差双向赚的严格条件：往返振幅 Hk−Lk > 入场滞后 + 出场滞后 + 成本，对买卖两边同时成立——震荡/中枢往返 regime 下的条件命题，不成立于强趋势单边结构（BTC 强上涨压制做空腿=数学上成立的机制，须加 σhigher 上级方向过滤或父级容器 regime 变量）。"
  code_changes: "无（genealogist 纯谱系产出，Read/Grep/Glob）。反事实实验实装（换出场规则/测滞后/加 σhigher/同容器配对识别机制 A/B/C）= 行动类，parity-analysis 独占 econ_positive，genealogist 不碰。"
  orchestration_changes: "方法论：①级别索引 ℓ 不是中性——携带相位 (−1)^ℓ，pooling 不同级别的同类买卖点会混合掉交替期望（657 定理1 pooled≤0 的级别相位实例）。写状态须含级别相位分量。②beta/alpha 分解方向由代数结构定：方向签名 PnL 下结构 edge=买卖同号项 (μ++μ−)/2、beta=买卖异号项 (μ+−μ−)/2——引用前须核对签名约定，不凭直觉命名（上一轮报告凭直觉反了）。③观测符号表（12 均值）不能唯一识别生成机制——多机制拟合同一符号，声称「是真结构/几何必然」前须反事实（231 有效域）。④正类交易负类不交易 ≠ 负类反做（X_inv=−X−2C 有 2C 罚金门槛）。"

impact:
  affected_modules:
    - "rust/src/theta_v0/backtest/econ_positive.rs → parity-analysis 独占。反事实实验须在此加：出场规则参数化（六种）、确认/出场滞后测量 ηin/ηout、σhigher 上级方向桶键、同容器配对。识别奇偶交替主因（A/B/C）。genealogist 不碰。"
    - "上一轮 parity 报告（parity-alternation-question-20260701.md）→ beta/alpha 命名 split 更正：任何引用其命名的下游结论须重标（(μ++μ−)/2 是结构 edge 非 beta，(μ+−μ−)/2 是 beta 非结构）。观测事实 12/12 + 三候选机制方向保留。"
    - "状态定义（657 的 z 升级）→ z=(ℓ,δ) 须扩展含级别相位 (−1)^ℓ 分量 + σhigher + exit rule + 确认滞后 + 父级容器状态。parity-analysis 桶键须按 (level,δ) 分（665 已指出 (level,δ) 是先验给定桶键，对选择偏差部分免疫）。"
  affected_definitions:
    - "657（生成态）：本号是其 refinement——657「须纳 regime 代理 ρ」（泛指），本号指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替精确形式 sign(δ(−1)^ℓ)。657 addendum_C 已覆盖 PDF 定理1/定理2，本号补三增量（相位分量/命名/三机制）。维持生成态。"
    - "663（生成态）：本号印证 + 机制化。663 真判据 μ>0，本号奇偶交替是 663 pooled 混合均值的机制化（级别相位是 pooling 变量）；正确操作（正类交易负类不交易）是 663 判据在级别相位维度实例。维持生成态。"
    - "664（生成态）：同簇。664 对象错配（触发段≠交易腿），本号级别相位/命名是判据表达维。奇偶交替的机制B（出场配对滞后）与 664 的出场腿对象错配相关——若 μℓ,δ 交替主因是机制B，则与 664 的 Ab_rev post-signal 交易腿 re-spec 同一诊断域。维持生成态。"
    - "665（生成态）：同簇。665 方向不对称免疫——(level,δ) 是缠论先验给定桶键，对选择偏差部分免疫。本号级别相位 (−1)^ℓ 正是 (level,δ) 桶键的相位结构，强化 665「先验桶键比 OOS 更抗偏差」（相位由级别结构先验定，非从收益挑）。维持生成态。"
    - "231（settled）：本号增量③不可识别性=231 实例（12 均值符号有效域不覆盖唯一识别主因）；级别相位「是缠论正确形式」须 L2+ 反事实（当前仅 L2 单标的观测）。印证。维持 settled。"
    - "656（生成态）：656 定理2 V(Z)≥V(Y) 细分类不劣。本号级别相位是「细分类」的一个维度（把 (ℓ,δ) 细分到含相位）——细分不劣但盈利需 μ>0（656 已立），奇偶交替给出 μ 符号的级别相位结构。印证。维持生成态。"
  downstream_implications:
    - "★级别相位是否「缠论正确形式」= 须 parity-analysis 反事实实验（换出场规则/测滞后/加 σhigher/同容器配对）识别主因 A/B/C。当前 L2 观测（BTC 12/12 单标的）不足以定主因——若机制B（出场口径）主导则相位是出场规则产物非真结构；若机制A（几何反相）在多出场口径/多标的保留则是真结构。不预设结果（231 铁律）。"
    - "beta/alpha 命名更正须广播到所有引用上一轮 parity 报告的产出——结构 edge=(μ++μ−)/2、beta=(μ+−μ−)/2。用旧命名做的 alpha/beta 归因结论须重算（可能把 beta 当 alpha 或反之）。"
    - "状态升级 z=(ℓ,δ,(−1)^ℓ,σhigher,exit rule,确认滞后,父级容器状态)——parity-analysis 的桶键/回测状态须含这些分量，否则 pooled 混合掉交替（657 定理1）。σhigher 上级方向是短差双向赚的关键过滤（震荡容器双向赚 vs 趋势容器单边赚）。"
    - "短差双向赚是条件命题（震荡 regime）非全局定理——BTC 强上涨压制做空腿（Lk+1>Hk 或跌幅不足⟹X−<0）数学上成立，须加 σhigher/父级容器 regime 变量区分震荡 vs 趋势容器。这印证 memory project_regime_is_level_truncation_artifact（regime=级别截断伪影）——震荡/趋势容器 regime 本身可由级别结构（父级容器是否为震荡中枢）内生（657 tension_check_regime_dual：R=f(Z)）。"

related_records:
  parent: "657（分类≠充分统计量，须纳 ρ）——本号 refinement：指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替精确形式。657 未捕获三增量（相位分量/命名/三机制），本号补。"
  children: []
  related:
    - "657（生成态）：parent，本号补其 addendum_C 未捕获的三增量（级别相位分量/beta-alpha 命名/三机制不可识别性）。"
    - "663（生成态）：印证 + 机制化——奇偶交替是 663 pooled 混合均值的级别相位机制化；正确操作（正类交易负类不交易）是 663 μ>0 判据实例。"
    - "664（生成态）：同簇——机制B（出场配对滞后）与 664 出场腿对象错配同诊断域。"
    - "665（生成态）：同簇——级别相位 (−1)^ℓ 是 (level,δ) 先验桶键的相位结构，强化 665 方向不对称免疫。"
    - "656（生成态）：级别相位是「细分类」维度，656 定理2 V(Z)≥V(Y) 细分不劣印证。"
    - "231（settled）：不可识别性 + 相位是否真结构须 L2+ 反事实=231 有效域实例。印证。"
    - "645（grandparent）：判据/对象错配簇根（π^cov≠π^bsp）——本号是簇的级别相位/命名维新成员。"
    - "660（生成态）：χ_t L3 inconclusive——奇偶交替的级别相位若被纳入状态，可能重组 660 的 pooled inconclusive。"
    - "alpha.pdf（μ(z,a)>0 + 不测而测）：正确操作来源——按级别相位选择正条件方向。"
    - "memory project_regime_is_level_truncation_artifact：震荡/趋势容器 regime 可由级别结构内生（657 R=f(Z)）——短差双向赚的震荡 regime 条件与此咬合。"
    - "memory project_recursive_t_architecture_v2：递归 T 每级别一实例——级别相位 (−1)^ℓ 是递归 T 每升一级操作相位翻转的候选形式化（机制A 几何反相）。"
    - "上一轮 parity 报告（parity-alternation-question-20260701.md）：beta/alpha 命名反了，本号 split 更正。"

# 认识论等级标注（formalization-validity-domain 231号，强制）
epistemological_levels:
  - proposition: "级别 ℓ 携带相位信息 (−1)^ℓ，同一买卖点语法在不同级别是不同经济状态"
    level: "L2（BTC 全历史 461万 bar 12/12 观测——单标的单时段，可否证但未交叉验证。是否『缠论正确形式』须多标的/多窗+反事实=L3）"
    increment: "高：级别相位结构变量的识别（657「纳 ρ」的分量指认）——但相位是否真结构（vs 出场口径产物）须反事实识别（L2 观测不足以定主因）"
  - proposition: "方向签名 PnL 下 (μ++μ−)/2≈结构 edge、(μ+−μ−)/2≈beta（上一轮报告命名反了）"
    level: "L0（代数：设 bℓ 漂移 + aℓ 结构边际，方向签名收益分解直接推出归属；PDF §7 严格）"
    increment: "高：命名归属更正（防下游 alpha/beta 误用）——归属方向由代数结构定（结构=买卖同号项，beta=买卖异号项）"
  - proposition: "奇偶交替主因不可识别（机制 A/B/C 都产生同样 12/12 符号）"
    level: "L0（代数：三机制参数数目 >12，都能拟合同一符号表——不可识别性是代数事实）"
    increment: "高：识别机制须反事实（换出场/测滞后/加 σhigher/同容器配对）——12 均值符号有效域不覆盖唯一识别（231）"
  - proposition: "正确操作=偶级别只做买奇级别只做卖（若 μℓ,δ>0⟺δ=(−1)^ℓ OOS 稳健）"
    level: "L2（依赖奇偶交替 OOS 稳健——当前 in-sample 12/12，OOS 稳健性未验证；665 已示 level0 卖 §11 OOS 否证，警示单标的 in-sample 不足）"
    increment: "高（若验证）：oracle 策略——但须 OOS 稳健（665 教训：in-sample 交替 ⊬ OOS 稳健，须 holdout LCB>0 + 多窗 + 抗事件聚集）"
---

# 666 ★级别相位 (−1)^ℓ 作为结构状态变量 + beta/alpha 命名纠正

## 一句话结论

GPT《奇偶相位》PDF（12页）回应蜂群全历史 BTC **12/12 级别×方向 μ̂ 奇偶交替**（买 L0+L1−L2+L3−L4+L5−，
卖反相，sign μℓ,δ=sign(δ(−1)^ℓ)，正期望 δ*ℓ=(−1)^ℓ 偶买奇卖）。三个增量（657 addendum_C 未捕获）：
**①级别相位 (−1)^ℓ 是结构状态变量**——级别不是中性索引，同买点语法在不同级别是不同经济状态，状态须升级；
**②beta/alpha 命名纠正**——方向签名 PnL 下 (μ++μ−)/2=结构 edge、(μ+−μ−)/2=beta，上一轮报告反了；
**③不可识别性**——机制 A/B/C 都产生同样交替，须反事实识别主因。

## 三增量 vs 657 覆盖对照（为何新建 666 非并入 657）

| 增量 | 657 addendum_C 是否覆盖 | 本号增量 |
|------|------------------------|---------|
| PDF 定理1（分类≠充分统计量，须纳 ρ） | ✓ 已覆盖（657+663） | — |
| PDF 定理2（V_{≤L}≥V₀） | ✓ 已覆盖（656） | — |
| **级别相位 (−1)^ℓ 分量指认** | ✗ 657 只泛指「纳 ρ」，未指认相位分量 | 指认 ρ ⊇ {(−1)^ℓ,...} + 交替精确形式 |
| **beta/alpha 命名纠正** | ✗ 657 无 | (μ++μ−)/2=结构、(μ+−μ−)/2=beta |
| **三机制不可识别性 A/B/C** | ✗ 657 无 | 反事实识别清单 |

657 是 20 页版《推导完全分类》PDF 的两定理；本 PDF 是 12 页版，聚焦奇偶交替，含三个 657 未捕获的精确增量。
新建 666 非重复造（657 已吸收的两定理不重写，只补三增量）。

## beta/alpha 命名纠正（split）

方向签名 PnL 下（PDF §7）：设 E[ΔP|buy,ℓ]=bℓ+aℓ、E[ΔP|sell,ℓ]=bℓ−aℓ（bℓ=市场漂移，aℓ=结构边际），
C+≈C−=C：

| 量 | ≈ | 是什么 | 上一轮报告 | 更正 |
|----|---|-------|-----------|------|
| (μ++μ−)/2 | aℓ−C | 方向中性**结构 edge**（买卖同号项） | 误标 beta | **结构** |
| (μ+−μ−)/2 | bℓ | 市场漂移 **beta**（买卖异号项） | 误标结构 | **beta** |

归属方向由代数结构定（结构=买卖两边都因结构正确受益的同号项；beta=市场漂移的异号项），独立于可比性假设。
严格剥离 beta 须随机置换方向标签/回归控制 holding return/同容器配对（PDF §8），不能只用 (μ+±μ−)/2 当终局。

## 不可识别性（三机制）

| 机制 | 形式 | 产生交替的原因 |
|------|------|--------------|
| A 递归几何反相 | ϕℓ=(−1)^ℓ，μℓ,+≈Aℓϕℓ | 每升一级操作相位翻转（几何必然） |
| B 出场配对滞后 | τℓ^out 落 ℓ+1 级反相区间 | 持有到下一反向信号的出场口径产物 |
| C 级别=regime | P(R|ℓ) 随级别变，pooled Σr μℓ,δ,r P(r|ℓ,δ) | 级别对应不同收益 regime |

参数数目 >12，都能拟合同一符号表 ⟹ 主因不可识别。反事实识别：只在「下一反向出场」有交替=机制B；
多出场口径都保留=机制A 更可能真结构。

## 正确操作（若 OOS 稳健）

偶级别只做买、奇级别只做卖（**正类交易，负类不交易**）。负类不能简单反做——X_inv=−X−2C，
仅 μ<−2C 反向才正（有 2C 罚金门槛）。全互斥分类后所有语法状态都定义，但只交易正条件收益状态
χℓ,δ=1[LCB(μℓ,δ)>θ]。与 663 判据（μ>0 出现就做）一致——按级别相位选择正条件方向，非双向都做。

**665 教训**：in-sample 12/12 交替 ⊬ OOS 稳健（665 已示 level0 卖 §11 OOS 否证）。相位是否真结构须
holdout LCB>0 + 多窗 + 抗事件聚集 + 反事实。

## genealogist 边界

记录三增量（级别相位分量/命名纠正 split/三机制不可识别性）+ 657 refinement。**不自结算**——级别相位
「是缠论正确形式」须 L2+ 反事实实证（=parity-analysis 独占 econ_positive 正在做），命名纠正是事实更正，
最终结算待编排者 /ritual。genealogist 不碰代码、不裁、不改判据文件。

## 张力检查（019d/020）

### 检查范围（同簇 645/663/664/665 ∪ 1-hop ∪ Hub）
- 同簇：663（判据）/664（对象）/665（方法论）/645（簇根）——本号居「级别相位/命名表达」维。
- 1-hop：657（parent）/663/231/656/660。
- Hub：231（有效域）/645（簇根）/657（regime 双记录张力节点）。

### 张力1（编排者点名）：级别相位「同买点在不同级别是不同经济状态」vs 缠论「买卖点定义级别无关」
**真张力（表面对立），但可分层 ⟹ 不触发中断 #1、不 escalate。** 两者是同一对象（买卖点）的**不同层**：
- **定义层（缠论）**：买卖点一/二/三类在每个级别定义相同——一类买点=中枢背驰后回抽不破前低，级别无关同义。
  这是**语法定义**（什么算买点），级别无关成立。
- **经济层（PDF）**：同一语法买点在不同级别的**条件期望 μ 符号交替**——同买点语法在不同级别是不同经济状态。
  这是**期望归属**（这个买点在此级别赚不赚），级别相关。
- **弥合点**：定义层「同义」⊥ 经济层「不同期望」——是不同对象（语法 vs 期望）。缠论从不声称「同类买点在
  所有级别期望相同」，只声称「定义相同」。PDF 不否定定义层同义，只指出经济期望层级别相关（657 定理1：
  分类完全 ≠ 收益充分统计量——语法完全分类 ⊥ 收益期望）。**可分层 ⟹ 无不可分层矛盾 ⟹ 不触发中断 #1。**
  记录价值：明确「买卖点级别无关」是定义断言，不外推到「期望级别无关」（否则=657 定理1 违反=pooled 混合）。

### 张力2：vs 657（parent）——refinement 非冲突
657 说「须纳 regime 代理 ρ」（泛指）。本号指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替精确形式。
是 657 的分量细化，非否定。657 addendum_C 覆盖 PDF 定理1/2，本号补三增量。可分层，无矛盾。

### 张力3：vs 663——印证机制化
663 真判据 μ>0。本号奇偶交替=663 pooled 混合均值的级别相位机制化；正确操作（正类交易负类不交易）=663
μ>0 判据实例。印证强化，无矛盾。

### 张力4：命名纠正 vs 上一轮 parity 报告——expansion split
上一轮报告命名与其自身方向签名分解依据不一致（规定者违反自身规定=expansion）。split：保留观测事实
12/12 + 三候选机制方向，否定 beta/alpha 命名归属。可分层（观测 ⊥ 命名），无中断 #1。

### 概念分离信号检测（中断 #1）
- 级别相位 vs 缠论级别无关：可分层（定义层 vs 经济期望层，不同对象）⟹ 不触发。
- 命名纠正：观测 ⊥ 命名归属，可分层 ⟹ 不触发。
- **无不可分层定义矛盾 ⟹ 不触发中断 #1。** 本号 bias-correction（相位 refinement + 命名更正），最终结算属编排者 /ritual。

### 递归运动结构完成检测（020）
- 第0层：本号写入（级别相位分量 + 命名 split + 三机制不可识别性）。
- 第1层：本号 × 657 碰撞 → ρ 的级别相位分量指认（净新发现高：657 泛指「纳 ρ」被细化为精确相位形式 (−1)^ℓ）。
- 第2层：本号 × 663/664/665 簇碰撞 → 级别相位维新成员 + 机制B 与 664 出场腿同诊断域（净新发现中：簇从判据/对象/方法论扩到相位表达维）。
- 第3层：本号 × 231/656 碰撞 → 不可识别性=有效域实例 / 相位是细分类维（净新发现降：印证已立规则）。
- 涉及范围：scope₁(657 ρ 分量细化=定理级) > scope₂(663/664/665 簇维) > scope₃(231/656 印证)=顶分型。
- **背驰 ∧ 分型 ⟹ 递归运动结构性完成。** 反事实实验=行动类（parity-analysis 独占 econ_positive）；
  最终结算待编排者 /ritual。**不触发新 /escalate**（无定义冲突，张力1 可分层）。

## 回溯扫描（职责3）

- **657（生成态）**：本号 refinement——补其 addendum_C 未捕获的三增量（级别相位分量/命名/三机制）。657 已覆盖 PDF 两定理，不重复。维持生成态。
- **663/664/665（生成态）**：本号同簇新成员（级别相位/命名维），印证不否定。维持生成态。
- **656（生成态）**：级别相位=细分类维，印证 656 定理2。维持生成态。
- **231（settled）**：不可识别性 + 相位真结构须 L2+ 反事实=231 实例。印证。维持 settled。
- **上一轮 parity 报告**：topo_effect split——beta/alpha 命名归属更正，观测事实 + 机制候选保留。
- **无 settled 被本号回溯破坏。** 本号 bias-correction（相位 refinement + 命名更正），反事实实验=行动类（parity-analysis），最终结算待编排者 /ritual。
