---
id: "678"
number: 678   # 候选编号（当前最大占用=677 GAP3 hwm_gain）。撞车让向下一空号，最终编号 /ritual 统一分配。
title: "同一能指『中枢』在 judge_third 归属链 vs last_zs 跨度选择链下指向不同对象——两条中枢选择链在真实 BTC 1m 几何下结构性不重合（same_center_any=0/84），对象身份分裂第三例"
status: 生成态   # genealogist 结构记录：概念分离（对象身份分裂）。C3 死门根因层。依赖 #44 codex C3 口径重设计裁决（进行中）。最终结算待编排者 /ritual。
date: "2026-07-02"
type: concept-separation   # 同一能指指向两个不同对象（对象身份分裂）
depends_on: ["673", "647", "675"]
related: ["637", "673", "675", "647", "231", "level-hole-series"]
negated_by: []
negates: []   # 否定的是隐含前提「judge_third.center ≡ last_zs.center」（非编号谱系节点）——bias-correction 无编号目标，split 语义见 topo_effect
topo_effect: "split:678:local"   # 能指『中枢』节点分裂：judge_third 归属链对象（leave 段当下最近中枢）⊥ last_zs 跨度选择链对象（s 空间跨度过滤最后中枢）；scope=local（C3 gate + judge_third/last_zs 两代码路径）；/ritual 确认
---

# 概念分离 678：能指『中枢』的对象身份分裂（judge_third 归属链 ⊥ last_zs 跨度选择链）

## 一句话结论

C3 死门（`XzdEvidence::gate_pass` 的 C3 硬门在真实 BTC 1m 上命中率退化）的根因不是时间/因果问题，而是**同一能指「中枢」在两条代码路径下指向结构性不同的对象**：`judge_third` 按「leave 段当下最近中枢」归属 `center`，`last_zs` 按「`s` 空间跨度过滤取最后中枢」选择——两条中枢选择链在本窗 BTC 1m 几何下不重合。这是「同一能指在不同代码路径指向不同对象=对象身份分裂」模式的**第三例**（673 号 Cand^δ 谓词范围误用、level 塔空洞系列为前两例）。

## 矛盾（对象身份分裂）

| 选择链 | 归属规则 | 对象 |
|--------|---------|------|
| `judge_third` 的 `center` | leave 段当下**最近**中枢 | 归属链对象 |
| `last_zs` | `s` 空间**跨度过滤**取最后中枢 | 跨度选择链对象 |

同一能指「中枢」在 C3 判据中被当作单一对象使用（`same_center` 隐含前提两链同对象），实测两链结构性不重合。

## 证据（L2 真实数据）

`xzd-c2only-degradation-20260702.md §2.3`（level==1 子集，84 样本）：

| 诊断字段 | 命中 | 命中率 |
|---------|------|-------|
| `last_zs_exists` | 84 | 100.00% |
| `same_side_l0_type3_any` | 83 | 98.81% |
| **`same_center_any`** | **0** | **0.00%** |
| `same_side_causal_ok` | 83 | 98.81% |

**关键区分**：`same_side_causal_ok=98.81%` 排除时间/因果问题（`source_index<=confirm_index` 几乎总成立，last_zs 在因果窗口内存在）；`same_center_any=0%` 坐实这是**纯粹的 center 归属问题**——last_zs 存在且因果合法，但其 center **从不**等于 judge_third 的 center。两条选择链是两个不同对象，非同一对象的时间错位。

交叉证据：`xzd2-impl-codex-audit §5.1`（审计代理 §3.4 开放疑点的回应——`same_side_causal_ok` 是本次新增分裂断点）。

## 认识论等级（231号，强制）

| 命题 | 等级 | 增量 |
|------|------|------|
| judge_third 按 leave 段最近中枢、last_zs 按 s 跨度过滤——两条选择链源码不同 | L0（源码事实：`econ_positive.rs` XzdEvidence/XzdC3Diag 字段 + judge_third/last_zs 归属逻辑） | 高：两链选择规则不同的结构判定 |
| 两链在真实 BTC 1m 几何下 same_center_any=0/84 | L2（真实数据：BTC 1m 84 样本，level==1 子集） | 高：对象身份分裂的实测坐实（非合成，可否证） |
| same_side_causal_ok=83/84 排除时间问题 → 纯 center 归属问题 | L2（同源同快照诊断字段，无额外前视） | 高：否定「时间错位」替代解释 |

## 谱系关联（对象身份分裂模式簇）

- **673（Cand^δ 谓词范围误用，同批 pending）**：第一例——单一 `Cand^δ_ℓ` 谓词被用于 Type1/2/3 三种不同领域概念（背驰段极值/一类点后回调/中枢离开回抽），谓词范围误用=对象身份未分离。本号是同模式在「中枢」能指上的复现（选择链层，非谓词层）。
- **level 塔空洞系列（level-hole）**：第二例——归因已定 vs H2 范围误用（`acc_classification_level_hole` 探针，本号诊断探针即接入 `acc_classification_level_hole_dx`）。
- **675（探针必须走生产路径，禁诊断坐标系分叉）**：方法论约束——本号的诊断字段（same_center_any 等）与生产路径 same-snapshot 同源（`sub_last_zs_type3` 同快照，无额外前视），满足 675 生产路径要求，非诊断坐标系分叉的又一伪证。
- **647（对象身份必先于否证设计，=647-object-identity）**：元规则依据——C3 死门的否证设计（gate_pass 命中率）在锁定「中枢」对象身份（judge_third 对象 vs last_zs 对象）**之前**进行，导致把对象身份分裂误读为门命中率退化。本号是 647「O_impl=O_def 须先锁定」的又一实例：被测对象（C3 门命中的 center）身份未先厘清。
- **637（中枢核心区间三口径分离，同批 pending）**：同域近邻——都是「中枢」概念的对象多义性，但 637 是区间口径（ZG/ZD 计算三口径），本号是中枢选择链（归属 vs 跨度），不同轴。

## 依赖标注（#44）

**codex 正在裁 C3 口径重设计（task #44，进行中）**。本条目的最终结算依赖 #44 裁决方向——若 #44 裁定统一两条选择链（单一 center 对象），则本对象身份分裂由该裁决消解；若 #44 裁定保留两链（各有有效域），则本号分离升为正式概念分离入 settled。**本号先落生成态记录固化根因，不预判 #44 方向。**

## 影响声明

- **根因层**：C3 死门（`XzdEvidence::gate_pass` C3 硬门命中率退化）根因 = 对象身份分裂，非时间/因果 bug。C3 硬门已移除、`sub_last_zs_type3` 降诊断字段（`xzd-c2only §结论`），代码可重启（改 `gate_pass()` 读取）。
- **概念层**：能指「中枢」在 judge_third 归属链 vs last_zs 跨度链下需显式分离为两个对象（命名候选：`NearestCenterOnLeave` vs `SpanFilteredLastZs`），防未来 same_center 隐含前提复发。
- **下游**：C3 口径重设计（#44）的对象定义须先锁定「哪条选择链是 C3 判据的 canonical center」——否则重设计仍在分裂的对象上做否证（647 循环）。
- **不改代码**（本号是概念分离诊断，纯谱系产出）；C3 gate/命名收口待 #44 裁决 + Lead 派实装工位。

## 张力检查（019d/020）

- **检查范围**：同轮（C3 死门系列：637/673/level-hole）∪ 1-hop（673/675/647/231）∪ Hub（673 为对象身份分裂簇根）。
- **张力1 vs 673**：同模式不同轴（谓词范围 vs 选择链），印证非冲突——两者共同支撑「对象身份分裂」元模式（第一+第三例）。可分层。
- **张力2 vs 647**：本号是 647 的实例（对象身份未先锁定→否证设计误读），印证非冲突。
- **张力3 vs 675**：本号诊断字段满足 675 生产路径要求（同快照同源），不违反，印证。
- **张力4 vs 637**：同域「中枢」多义但不同轴（区间口径 vs 选择链），可分层无矛盾。
- **递归完成检测**：第0层本号写入；第1层×673 碰撞→对象身份分裂元模式（净新发现：选择链层是新轴）；第2层×647 碰撞→「对象身份先于否证」实例（净新发现降：元规则已知）；第3层×675 碰撞→诊断路径合规（净新发现骤降=背驰）。scope₁(673 元模式+选择链定位)>scope₂(647 实例)>scope₃(675 合规)=顶分型。**背驰∧分型⟹递归结构性完成。** 不触发新 /escalate（对象身份分裂第三例=已知元模式的复现，非新矛盾类型）。
- **元模式信号**（供 meta-observer / 结晶）：「同一能指在不同代码路径指向不同对象=对象身份分裂」已达**三例**（673/level-hole/本号）——若第四例复发，达 623 结晶阈值候选。本号仅标注信号，不代 meta-observer 起元观察条目。

## 回溯扫描（职责3）

- 673（生成态）：本号印证其对象身份分裂内核，不否定，维持生成态（同批 /ritual）。
- 647-object-identity（staging §1-C 语法记录）：本号是其又一实例，强化其结晶依据，不否定。
- 675（staging §1-C）：本号诊断字段合规其生产路径要求，印证，不否定。
- 637（生成态）：同域近邻不同轴，不破坏。
- **无 settled 被本号回溯破坏。** 本号是新概念分离（对象身份分裂第三例），最终结算待 #44 codex 裁决方向 + 编排者 /ritual。
