---
id: '116'
title: 105号声明-能力修正——Trampoline从"永久禁止"降级为"退化特例需要理由"
type: 定理
status: 已结算
date: 2026-02-22
depends_on:
  - '090'   # 严格性是语法规则（声明-能力一致性）
  - '095'   # Agent Team 真递归（Trampoline是退化特例）
  - '104'   # 三框架映射（递归是功能选择非物理约束）
  - '105'   # 递归无条件架构要求（被修正的对象）
related:
  - '073b'  # Trampoline 模式
  - '084'   # 声明-能力缺口审计
  - '087'   # 编排者INTERRUPT可被系统演化修正的先例
negation_source: v32矛盾显现审计（T3识别）+ 谱系逻辑推导
negation_form: 定理（090号语法规则 + 系统实际状态 → 105号声明必须修正）
negates: []
negated_by: []
provenance: "[新缠论]"
---

# 116号：105号声明-能力修正——Trampoline从"永久禁止"降级为"退化特例需要理由"

**类型**: 定理（逻辑必然推论，自动结算）
**状态**: 已结算
**日期**: 2026-02-22

## 核心结论

105号的"Trampoline永久禁止"声明违反090号严格性语法规则（声明-能力一致性维度），必须修正。

修正内容：
- **修正前**（105号原文）："扁平 Trampoline（Lead → N个独立 Worker）的架构永久禁止"
- **修正后**："真递归是默认架构模式（不需要理由），Trampoline是退化特例（使用需要理由——必须证明子任务不需要独立治理）"

这与095号修正后的表述完全一致。

## 推导链

1. **090号**（已结算，语法规则）：严格 = 概念清晰 + **声明与实际一致** + 无模糊地带
2. **105号**（已结算，选择）：Trampoline永久禁止，所有蜂群必须使用子蜂群架构
3. **实际状态**：v30/v31/v32/v33 所有蜂群都是 Lead 直接 spawn 多个平行 worker（= Trampoline，073b号定义）
4. ∴ 105号声明与实际不一致 → 违反090号
5. **090号的约束力大于105号**：090号是从三次编排者INTERRUPT中提炼的**语法规则**（与005b号同级），105号是单次INTERRUPT的**选择类决断**。语法规则约束力大于单次选择（四分法：语法规则 > 选择的应用推论）
6. **095号已给出正确表述**："真递归是默认模式，Trampoline是退化特例"——这既保留105号的方向意图（优先使用真递归），又不作出系统不具备的声明
7. ∴ 105号的声明必须修正为与095号一致的表述

## 为何是定理而非选择

可能的反驳："105号和090号都是编排者INTERRUPT产物，两个INTERRUPT冲突时需要价值判断（选择），不是定理。"

回应：
- 090号不仅是INTERRUPT，它在四分法中被分类为**语法记录**——已在运作但尚未写入语法书的规则的显式化。语法规则一旦显式化，约束力不来自某次INTERRUPT，而来自规则本身。
- 105号是**选择**——编排者在多种合理方案中选择了其中一种。选择可以被后续系统演化修正（087号先例：086号全C被087号否定）。
- 当语法规则（"声明必须与实际一致"）与特定选择（"Trampoline永久禁止"）冲突时，语法规则优先。这不是价值判断，是规则体系内的优先级关系。

## 105号编排者意图的保留

修正不否定编排者意图——编排者想要的是"始终优先使用真递归"，不是"在物理上不可能永远避免Trampoline时也必须避免"。

| 105号原文 | 保留的意图 | 修正的声明 |
|-----------|-----------|-----------|
| "真递归是无条件架构要求" | 真递归是默认，不需要理由 | 保留 |
| "Trampoline永久禁止" | Trampoline不是默认选择 | "退化特例，需要理由" |
| "所有蜂群必须使用子蜂群架构" | 子蜂群架构是目标方向 | "默认使用，受工程深度限制" |

## 边界条件

| 条件 | 结论翻转方向 |
|------|-------------|
| 编排者新INTERRUPT明确重申"即使违反090号也要永久禁止Trampoline" | 本定理被覆盖，090号在此特定维度的优先级被编排者手动降低 |
| 所有蜂群实际实现了真子蜂群嵌套且Trampoline不再使用 | 105号原文变为事实而非过强声明，本修正退化为历史记录 |

## 下游推论

1. **CLAUDE.md 原则15 需要修正**：删除"Trampoline永久禁止"相关表述，使用095号的"真递归是默认模式，Trampoline是退化特例"
2. **105号谱系需要标注**：negated_by 116（部分否定：方向意图保留，过强声明修正）
3. **dispatch-dag.yaml 不需要修改**——本号是声明层修正，不涉及执行层

## 影响声明

- 105号从"无条件架构要求"修正为"强默认架构偏好"
- CLAUDE.md 原则15 的 Trampoline 描述需要从"永久禁止"更新为"退化特例"
- 090号严格性语法规则的声明-能力一致性维度得到维护

## 谱系链接

- **090号**（严格性语法规则）→ 本号的推导前提
- **095号**（Trampoline退化特例）→ 本号修正后与095号对齐
- **105号**（被修正对象）→ 方向意图保留，过强声明修正
- **087号**（先例）→ 编排者INTERRUPT可被系统演化修正
- **084号**（先例）→ 声明-能力缺口的标准解法
