---
id: "651-652"
title: 651/652 降级归并【已结算 2026-06-30：编排者 /ritual 授权 + genealogist R4 论证，两号不独立立号，增量投放既有规则】
status: 已结算
settlement_date: "2026-06-30"
type: 回溯结算（降级归并）
layer: 元编排/方法论层
session: 68088c95
discovered_by: meta-observer（第二轮）
created: 2026-06-30
related: [651, 652, 036, 275, "no-unnecessary-escalation", 161, 231, 650]   # 036=spec-execution-gap 结晶谱系; 275=局部依赖原则
decided_by: 编排者 /ritual 授权 + genealogist R4 谱系责任方独立论证（同意 claude-challenger 降级判定）

# ============================================================
# 降级归并依据（genealogist R4 论证已在 651/652 pending 文件文末记录，此处汇总结算）
# ============================================================
settlement:
  scope: |
    本记录结算 651（formalization-validity-domain 角色分裂盲区）与 652（局部矛盾误判全局阻塞=161镜像）
    两号的降级归并。两号均为 meta-observer 第二轮观测，经 claude-challenger 质询 + genealogist R4
    谱系责任方独立核验，判定**无独立拓扑增量，不独立立号**，真实增量投放既有规则。
  case_651:
    judgment: "降级——归 spec-execution-gap（036号）实例，非 formalization-validity-domain 扩展。"
    reason: |
      能指挪用坐实。formalization-validity-domain（231号）的有效域轴=代数可施加 vs 经验成立（数据
      L0-L3 验证等级）；651 轴=契约声明 vs 实装支持（schema 实装轴）。两轴非同一。GOAL_RESUME 可读
      不可写本质 = spec-execution-gap（声明-能力一致性，036号已结晶 skill）的 B 方向（产出→消费断裂：
      GOAL_RESUME 被 reader 消费但无 writer 产生）。651 把 reader「定义域当有效域」类比 230「定义域=
      有效域假设」是借了 validity-domain 能指，所指（数据经验收缩）没跟过来。
    increment_placement: |
      651 去掉能指挪用后的真实内容 = 「spec-execution-gap 应在 reader/writer 事件契约分裂场景触发」
      ——一条 spec-execution-gap 的实例/示例补充（reader/writer 事件契约分裂 = 声明-能力缺口的工具层
      新实例：每个消费端须有产生端，无产生者的消费 → 补产生或删消费）。原 651 下游推论第3条（扩展
      validity-domain 第5禁止模式）应改投向 spec-execution-gap 而非 validity-domain。
    note: |
      650 的 GOAL_RESUME 可读不可写缺口本身的修复（立场 B+A2，已结算见 650）与本归源裁决正交——本裁决
      只改「651 归哪条规则」，不改 650 修复方向。
  case_652:
    judgment: "降级合并——并入 275（局部依赖原则）推论 + no-unnecessary-escalation 边界条款，不独立立号。"
    reason: |
      自我矛盾坐实 + 161镜像是叙事非结构。652 核心元命题（「矛盾的有效域必须局部化判定，不可越级
      传导」）与 275「附庸的附庸不是我的附庸」是同一条命题——652 自己在二阶观测段写明「两者都违反
      275」。一旦认定它是 275 实例，「161 镜像变体」就是同一现象换参照系的复述，无新拓扑（无新否定边、
      无新对象、无新机制）。「局部放大为全局」与「把可推进误判为不可推进」是同一误判两面。挂 161 需
      「镜像」修辞桥，但实际可执行的检查（"被停工作数据依赖该矛盾吗？"）100% 来自 275/
      lead-parallel-dispatch 局部依赖原则，与 161 无关 = 事后归纳叙事。
    increment_placement: |
      652 唯一有操作价值的产出（下游推论1/2）拆两处投放：
      (a) no-unnecessary-escalation 边界条款：格式B 的 `blocked` 排除分类须附「被停工作数据依赖该
          阻塞项」的证明，否则 = 该推进不推进（161 镜像在此语境操作化，作为条款不作为独立号）。
      (b) 275 推论：局部依赖原则的适用域从 spawn 调度扩展到阻塞判定（判断阻塞时也问「这个工作依赖那个
          矛盾的输出吗？不依赖 → 不阻塞」）。
    note: "一阶事实（本轮 Lead 把 650 误当全局阻塞 → Stop-Guard 纠正）作为发生史保留在 652 pending 文件。"
  no_workaround_self_check: |
    降级不是「我觉得该降级」——是 claude-challenger 质询暴露能指挪用（651）/自我矛盾（652）后，
    genealogist 谱系责任方独立核验坐实，编排者 /ritual 授权。降级保留真实增量（投放既有规则），不丢失
    拓扑信息（651→spec-execution-gap 实例，652→275推论+no-unnecessary-escalation条款）。两号 pending
    文件作发生史保留（谱系012：发现过程不可压扁）。
  tension_check: |
    vs 036（spec-execution-gap 结晶，settle）：651 投放为其工具层新实例（reader/writer 事件契约分裂），
      印证扩展不破坏 036 核心（声明-能力一致性）。
    vs 275（局部依赖，settle/规则）：652 投放为其阻塞判定推论，印证扩展不破坏 275 核心（附庸的附庸）。
    vs no-unnecessary-escalation（规则）：652 投放为其格式B blocked 边界条款，印证补强不破坏。
    vs 231（settle）：651 降级正是因 651 误把 spec-execution-gap 缺口挪用为 231 有效域扩展，降级修正
      能指挪用，印证 231 边界（数据经验收缩轴 ≠ 声明实装轴）。
    无 settled 被本降级破坏。
  rule_text_amendment_status: |
    规则文本补充（spec-execution-gap 加 reader/writer 契约分裂实例 + no-unnecessary-escalation 加格式B
    blocked 数据依赖证明条款 + 275 加阻塞判定推论）属 /ritual 步骤1「写入定义」=规则文件修正动作。
    genealogist 工具集无 Edit → **规则文本内联补充转交 Lead 用 Edit 工位执行**（待补充）。补充内容已在
    本结算记录 increment_placement 完整定义，规则内联文本是其投影。
---

# 651/652 降级归并【已结算 2026-06-30】

## 结算一句话

651（validity-domain 角色分裂盲区）与 652（161 镜像）两号 meta 观测经 claude-challenger 质询 +
genealogist R4 独立核验，判定**无独立拓扑增量，不独立立号**。651 真实增量 = spec-execution-gap（036号）
工具层新实例（reader/writer 事件契约分裂=声明-能力缺口 B 方向）；652 真实增量拆为 no-unnecessary-
escalation 格式B blocked 边界条款 + 275 阻塞判定推论。两号 pending 文件作发生史保留。

## 651 降级：能指挪用

| 轴 | validity-domain（231） | 651 实际 |
|----|------------------------|----------|
| 内容 | 代数可施加 vs 经验成立（L0-L3 数据验证） | 契约声明 vs 实装支持（schema 实装） |
| 判定 | 数据/语境维度收缩 | 声明-能力维度断裂 |

651 把 reader「定义域当有效域」类比 230「定义域=有效域假设」，借了 validity-domain 能指但所指（数据
经验收缩）没跟过来。GOAL_RESUME 可读不可写 = spec-execution-gap B 方向（消费链断裂：无产生者的消费）。
→ 归 036 实例。

## 652 降级：自我矛盾 + 161镜像是叙事

652 核心元命题（"有效域必须局部化判定，不可越级传导"）= 275（"附庸的附庸不是我的附庸"）同一命题。
652 自己写明"两者都违反 275"。挂 161 需"镜像"修辞桥，但可执行检查 100% 来自 275 局部依赖，与 161 无关。
→ 增量拆 no-unnecessary-escalation 格式B blocked 条款 + 275 阻塞判定推论。

## 转交 Lead（规则文本内联补充，待 Edit 工位）

1. **spec-execution-gap skill**：加 reader/writer 事件契约分裂实例（声明-能力缺口工具层新实例）。
2. **no-unnecessary-escalation 规则**：格式B `blocked` 排除分类须附"被停工作数据依赖该阻塞项"证明。
3. **lead-parallel-dispatch（275）**：局部依赖原则适用域从 spawn 调度扩展到阻塞判定。

（genealogist 工具无 Edit；内联补充内容已在 settlement.increment_placement 完整定义。）
