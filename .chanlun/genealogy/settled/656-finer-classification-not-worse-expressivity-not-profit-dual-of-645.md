---
id: "656"
number: 656
type: domain   # 域内新定理：细分类不劣 V(Z)≥V(Y) + 优势=表达力/统计识别非盈利保证。645 覆盖≠盈利（反面）的对偶正面。
status: 生成态   # genealogist 结构记录：alpha2 Doc3 §13 定理2（peer 漏纳）。L0 结构定理（V(Z)≥V(Y)），盈利仍需 L2/L3 μ>0。最终结算待编排者 /ritual。
date: "2026-06-30"
source: genealogist（编排者 alpha2.pdf 38页对照，Doc3 p25-38 全互斥分类 C_Θ:X→Z；Lead 全读核验 peer goal 5项 acceptance 忠实，本号是 Doc3 §13 独有增量）
depends_on: ["231", "645"]
related: ["604", "605", "615", "648", "v1-fullwindow-l3-falsified"]
negation_source: "编排者 alpha2.pdf Doc3 §13 定理2严格证明（粗分类 π_Y 可被细分类 π_Z(z)=π_Y(φ(z)) 模拟 ⟹ sup_Z≥sup_Y）+ §19 全互斥=alpha 命题形式化实验台"
negation_form: "separation"   # 把「全互斥分类的优势」分离为三个不同对象：表达力优势(V(Z)≥V(Y)) / 统计识别优势(μ(z,a)可分类估计) / 先验盈利保证(被否——不蕴含)
title: "alpha2 Doc3 §13 定理2「细分类不劣于粗分类」V(Z)≥V(Y)——编排者「全互斥更有优势」的严格回答。优势=表达力优势(粗策略可被细策略模拟,sup_Z≥sup_Y)+统计识别优势(μ(z,a)可分类估计)，**不是**先验盈利保证。§19：全互斥=alpha命题形式化实验台(使「三类买卖点有用」从模糊句变严格命题 ∃z,a:E[R|C_Θ=z]>0)。645 覆盖≠盈利(反面)的对偶正面：覆盖不保证盈利，全互斥分类提供识别盈利的表达力。V(Z)≥V(Y) 是 L0 结构定理，盈利仍需 L2/L3 μ>0 真实数据(231 铁律)"

topo_effect: "separate:全互斥分类优势:{表达力优势 V(Z)≥V(Y)[L0结构定理,粗策略可被细策略模拟] | 统计识别优势 μ(z,a)可分类估计 | 先验盈利保证[被否:V(Z)≥V(Y)不蕴含 sup_Z>0]} | dual:645-覆盖≠盈利[反面] → 656-分类提供识别表达力[正面] | bound:V(Z)≥V(Y)是L0,盈利需L2/L3 μ>0(231铁律)"

contradiction:
  description: |
    编排者问「全互斥策略是否更有优势」。alpha2 Doc3 §13 定理2 严格回答：**细分类不劣于粗分类 V(Z)≥V(Y)**。
    证明：任何粗分类策略 π_Y 可被细分类策略 π_Z(z)=π_Y(φ(z))（φ=细→粗的聚合映射）模拟，
    故 sup_Z V(π_Z) ≥ sup_Y V(π_Y)——细分类的策略空间包含粗分类策略空间。

    **关键分离（防止优势被误读为盈利保证）**：全互斥分类 C_Θ:X→Z 的优势是三个不同对象：
    - **表达力优势**：V(Z)≥V(Y)（L0 结构定理，细策略空间 ⊇ 粗策略空间）。
    - **统计识别优势**：μ(z,a)=E[R|C_Θ=z, A=a] 可分类估计（每个互斥类单独估收益，识别哪类有 alpha）。
    - **先验盈利保证**：**被否**——V(Z)≥V(Y) 不蕴含 sup_Z>0。细分类只保证「不比粗的差」，
      不保证「绝对盈利」。盈利仍需 L2/L3 真实数据 μ(z,a)>0（231 铁律：结构定理≠盈利）。

    **与 645 的对偶关系**：645（覆盖≠盈利）= **反面**（π^cov 覆盖投影不保证盈利，8/8 否证）。
    本号 = **对偶正面**：覆盖不保证盈利，但全互斥分类提供**识别盈利的表达力**（V(Z)≥V(Y) + μ 可分类估计）。
    §19：全互斥=alpha 命题的形式化实验台——使「三类买卖点有用」从模糊句变严格可证伪命题
    ∃z,a:E[R|C_Θ=z]>0。这是 645 wrong-object 之后的正面建设：不是「π^bsp 是真对象」（648-D 已立），
    而是「全互斥分类 C_Θ 是检验 π^bsp 是否有 alpha 的表达力载体」。
  layer: 定义   # alpha2 Doc3 §13 定理2（策略价值函数 V + 分类 C_Θ:X→Z）。新定理，非冲突——是编排者问题的严格回答 + 优势三分离。
  trigger: "编排者 alpha2.pdf 38页提供；Lead 全读核验 peer g-alpha-causal-selector 5项 acceptance 忠实 alpha2，发现 Doc3 §13 定理2 是 peer 漏纳的独有增量。"

definitions_involved:
  - name: "alpha2 Doc3 §13 定理2「细分类不劣」V(Z)≥V(Y)"
    version: "买卖点alpha2.pdf Doc3 p25-38（编排者提供，一级权威）"
    role: "新定理来源。粗策略 π_Y 可被细策略 π_Z(z)=π_Y(φ(z)) 模拟 ⟹ sup_Z≥sup_Y。编排者「全互斥更有优势」的严格回答。"
  - name: "645 命题A（覆盖≠盈利，π^cov 8/8 否证）"
    version: ".chanlun/genealogy/pending/645（生成态）"
    role: "对偶。645=反面（覆盖不保证盈利）；本号=正面（分类提供识别盈利表达力）。同源对偶，不矛盾。"
  - name: "231 形式化有效域规则"
    version: ".claude/rules/formalization-validity-domain.md（settled）"
    role: "约束。V(Z)≥V(Y) 是 L0 结构定理（代数：策略空间包含）；盈利 sup_Z>0 是 L2/L3 经验问题（μ(z,a)>0 真实数据）。把 L0 表达力优势误读为盈利保证=有效域膨胀（231 禁止模式3）。"

resolution:
  type: 未解决   # 新定理已记录（V(Z)≥V(Y) + 优势三分离）。是否加 acceptance 第6项=选择类(待 Lead/编排者)。最终结算待编排者 /ritual。
  description: |
    定理2 记录（已完成）：细分类不劣 V(Z)≥V(Y)（L0 结构定理），全互斥优势=表达力+统计识别，
    **非先验盈利保证**。645 覆盖≠盈利的对偶正面。

    **acceptance 第6项评估（Lead 问 b）**：**建议保持 5 项，不加第6项。** 理由：
    细分类是**手段**（表达力载体），非**验收目标**（盈利）。g-alpha-causal-selector 的验收目标是
    χ_t 因果选择器是否产生 alpha（L2/L3 μ>0），定理2 保证的是「细分类不会比粗的差」（手段层 L0），
    不是验收目标。加第6项「细分类表达力测试」会把 L0 结构定理（V(Z)≥V(Y) 恒真，无需测）当作
    可验收项——L0 恒真测试信息增量零（231/645 T2 同理）。**表达力 V(Z)≥V(Y) 是 L0 定理无需 acceptance；
    盈利 μ>0 已在现有 5 项（χ_t 收益验收）覆盖。** 若编排者要显式标注「全互斥是实验台」语义，
    可在 acceptance 注释引用本号，不增独立验收项。
  decided_by: 蜂群内部   # 编排者 alpha2 §13 定理 + genealogist 结构记录(优势三分离 + acceptance 评估)；是否加第6项=选择类待 Lead/编排者；最终结算待 /ritual

negated:
  description: "(1) 全互斥分类的优势 = 先验盈利保证（细分类 ⟹ 更赚钱）。(2) 定理2 V(Z)≥V(Y) 需加为 acceptance 第6项验收。"
  why_negated: "(1) 定理2 只证 V(Z)≥V(Y)（不劣），不证 sup_Z>0（盈利）——粗策略可被细策略模拟保证「不更差」，绝对盈利仍需 μ(z,a)>0 真实数据（231 铁律）。把表达力优势读成盈利保证=有效域膨胀。(2) V(Z)≥V(Y) 是 L0 结构定理恒真（无需 L2 测），加 acceptance=测恒真命题（信息增量零，645 T2 同理）；盈利验收已在现有 5 项 χ_t 收益项覆盖。细分类是手段非验收目标。"

new_output:
  definitions:
    - "定理2：细分类不劣 V(Z)≥V(Y)——粗策略 π_Y 可被细策略 π_Z(z)=π_Y(φ(z)) 模拟 ⟹ sup_Z≥sup_Y（L0 结构定理）。"
    - "全互斥优势三分离：表达力优势(V(Z)≥V(Y)) + 统计识别优势(μ(z,a)可分类估计) ⊥ 先验盈利保证(被否)。"
    - "645 对偶正面：覆盖≠盈利(645反面) ↔ 全互斥分类提供识别盈利表达力(656正面)。"
    - "§19 全互斥=alpha 命题形式化实验台：使「三类买卖点有用」从模糊句变严格命题 ∃z,a:E[R|C_Θ=z]>0。"
  code_changes: "无（本号是定理记录，纯谱系产出）。全互斥分类 C_Θ:X→Z 实装属 g-alpha-causal-selector goal（peer），非本号。"
  orchestration_changes: "方法论：①「X 更有优势」类断言须分离「表达力优势(L0 结构)」与「盈利优势(L2/L3 经验)」——前者恒真无需验收，后者需真实数据(231)。②L0 结构定理(V(Z)≥V(Y))不加为 acceptance(测恒真信息增量零)；acceptance 只验 L2/L3 可证伪项(μ>0)。③手段(细分类表达力)与验收目标(χ_t 盈利)分离——手段不进 acceptance。"

impact:
  affected_modules:
    - "g-alpha-causal-selector goal（peer）→ 本号评估：保持 5 项 acceptance，不加第6项（细分类是手段非验收目标，V(Z)≥V(Y) L0 恒真无需测）。"
    - "全互斥分类 C_Θ:X→Z（alpha2 Doc3）→ 表达力载体；盈利验收在现有 χ_t 收益项。"
  affected_definitions:
    - "645（生成态）：本号是其对偶正面（覆盖≠盈利 ↔ 分类提供识别表达力），不否定，互补。维持生成态。"
    - "231（settled）：本号印证（V(Z)≥V(Y) L0 ≠ 盈利 L2/L3）。维持 settled。"
    - "604/605（settled，完全分类族）：本号定理2（细分类不劣）与完全分类纲领同域（C_Θ:X→Z 全互斥），印证不否定。维持 settled。"
  downstream_implications:
    - "全互斥分类的价值=识别表达力(检验 ∃z,a:μ>0 的载体)，非自带 alpha——goal 验收仍是 χ_t 是否产生 L2/L3 μ>0，定理2 不改验收目标。"
    - "648-D（π^bsp 可激活）+ 656（全互斥=表达力载体）联合：π^bsp 离散择时 + C_Θ 全互斥分类 = 检验缠论买卖点 alpha 的完整表达力框架（待 L2/L3 实测，不预设结果）。"
    - "acceptance 第6项不加（保持 5 项）——细分类表达力是手段层 L0 恒真，盈利验收已覆盖。"

related_records:
  parent: "645（覆盖≠盈利反面）——本号是其对偶正面（分类提供识别盈利表达力）"
  children: []
  related:
    - "645（生成态）：对偶（反面覆盖≠盈利 ↔ 正面分类表达力）。"
    - "231（settled）：V(Z)≥V(Y) L0 结构 ≠ 盈利 L2/L3，本号印证有效域规则。"
    - "604/605（settled）：完全分类纲领同域（C_Θ:X→Z 全互斥）。"
    - "648（已结算 D）：π^bsp 可激活 + 本号全互斥表达力载体 = 缠论买卖点 alpha 完整检验框架。"

epistemological_levels:
  - proposition: "定理2 细分类不劣 V(Z)≥V(Y)（粗策略可被细策略模拟）"
    level: "L0（alpha2 §13 严格证明：策略空间包含，纯结构）"
    increment: "高：V(Z)≥V(Y) 的结构判定（但对盈利零增量——不蕴含 sup_Z>0）"
  - proposition: "全互斥优势=表达力+统计识别，非先验盈利保证（sup_Z>0 仍需 μ(z,a)>0 真实数据）"
    level: "L0（分离判定）+ 待 L2/L3（μ>0 真实数据，g-alpha-causal-selector goal 验收）"
    increment: "高：优势三分离（防表达力误读为盈利保证=有效域膨胀）"
  - proposition: "acceptance 保持 5 项不加第6（细分类是手段非验收目标，V(Z)≥V(Y) L0 恒真无需测）"
    level: "L0（231/645 T2：L0 恒真命题加 acceptance=测恒真信息增量零）"
    increment: "中：acceptance 边界判定（手段 ⊥ 验收目标）"
---

# 656 alpha2 §13 定理2「细分类不劣」V(Z)≥V(Y)——645 覆盖≠盈利的对偶正面

## 一句话结论

编排者「全互斥策略更有优势」之问，alpha2 Doc3 §13 定理2 严格回答：**细分类不劣于粗分类 V(Z)≥V(Y)**
（粗策略 π_Y 可被细策略 π_Z(z)=π_Y(φ(z)) 模拟 ⟹ sup_Z≥sup_Y）。**关键：优势=表达力优势(V(Z)≥V(Y))
+统计识别优势(μ(z,a)可分类估计)，不是先验盈利保证（V(Z)≥V(Y) 不蕴含 sup_Z>0，盈利仍需 L2/L3 μ>0）。**
这是 645 覆盖≠盈利（反面）的**对偶正面**：覆盖不保证盈利，但全互斥分类提供识别盈利的表达力。

## 优势三分离

| 对象 | 内容 | 等级 |
|------|------|------|
| 表达力优势 | V(Z)≥V(Y)（细策略空间 ⊇ 粗策略空间） | L0 结构定理（恒真） |
| 统计识别优势 | μ(z,a)=E[R\|C_Θ=z,A=a] 可分类估计 | L0 方法 + L2/L3 估计 |
| 先验盈利保证 | **被否**——V(Z)≥V(Y) 不蕴含 sup_Z>0 | 盈利需 L2/L3 μ>0（231 铁律） |

## acceptance 第6项评估（Lead 问 b）

**建议保持 5 项，不加第6项。** 细分类是**手段**（表达力载体）非**验收目标**（盈利）。V(Z)≥V(Y) 是 L0
结构定理恒真（无需测，645 T2 同理：测恒真信息增量零）；盈利验收已在现有 5 项 χ_t 收益项覆盖。
若编排者要显式标「全互斥=实验台」语义，可在 acceptance 注释引用本号，不增独立验收项。

## genealogist 边界

genealogist 记录定理2 + 优势三分离 + acceptance 评估（建议保持 5 项）。是否加第6项=选择类，待 Lead/编排者。
不自结算（待 /ritual）。

## 张力检查（019d/020）

- **vs 645（parent，对偶）**：645 反面（覆盖≠盈利）↔ 本号正面（分类提供识别表达力）。同源对偶，互补不矛盾。
- **vs 231（settled）**：V(Z)≥V(Y) L0 结构 ≠ 盈利 L2/L3，本号印证有效域规则（表达力误读为盈利=膨胀）。
- **vs 604/605（settled，完全分类族）**：定理2 与完全分类纲领同域（C_Θ:X→Z 全互斥），印证不否定。
- **vs 648（已结算 D）**：π^bsp 可激活 + 656 全互斥表达力载体 = 缠论买卖点 alpha 完整检验框架。
- **中断 #1**：无两条定义互斥——是把「优势」分离为表达力/识别/盈利三对象（可分层）。**不触发。**
- **递归完成（020）**：第0层本号写入；第1层×645→对偶正面(净新发现高)；第2层×231→L0≠盈利(净新发现中)；第3层×604/605→完全分类同域(净新发现降)。背驰∧分型⟹结构完成。

## 回溯扫描（职责3）

- **645（生成态）**：对偶正面，互补，不否定。维持生成态。
- **231/604/605/648（settled/结算）**：印证（有效域/完全分类/检验框架），不否定。维持。
- **无 settled 被破坏。** 定理2 记录 + acceptance 评估（保持 5 项），是否加第6项待 Lead/编排者，最终结算待 /ritual。
