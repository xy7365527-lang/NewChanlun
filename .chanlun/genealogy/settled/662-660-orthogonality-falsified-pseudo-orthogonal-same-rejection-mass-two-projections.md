---
id: "662"
number: 662
type: bias-correction   # 660 内部一条命题的自我否证：DIM-1(过拟合)⊥DIM-2(功效)「正交」被证伪为「同一 rejection mass 机制两投影」
status: 已结算   # genealogist 结构记录。codex 异质审(4fef076dab)+L2修复(73df0499d7)否证 660 正交命题。660 主结论(inconclusive 伪否证+根因分离)仍立，只更正「两维度正交」这一条。概念层更正不自结算(编排者 /ritual 辨认域)。
settled_date: "2026-07-02"
settled_by: "genealogist via /ritual（编排者明令『并行全部推进』授权；裁决来源 codex 裁决①-⑤ + staging 归档）"
date: "2026-06-30"
source: genealogist（Lead 报本轮 L2 修复产出否证 660 正交命题的概念发现）
depends_on: ["660"]
related: ["161", "635", "645", "656", "231", "g-20260630T155255Z-f65436f2", "661"]
negation_source: "codex 异质审(commit 4fef076dab) + L2 修复(commit 73df0499d7) + lcb_vs_naive_l2.rs:57 单调性诊断(LCB 放行集 ⊆ 裸μ 放行集)自证"
negation_form: "expansion"   # 不切断 660，是在 660 上扩展更正：把「两个正交维度」更正为「同一机制的两个投影」。660 主结论保留，正交命题携带违反记录

topo_effect: "split:660:{主结论[保留:inconclusive 伪否证+根因四分(功效不足+样本饥饿 vs 过拟合/真无信号/口径/lookahead)仍立] | 正交命题[携带违反记录:DIM-1⊥DIM-2 被否证→同一 rejection mass 机制两投影]} | scope:local(仅 660 的『两维度正交』子命题，不波及主结论下游)"

contradiction:
  description: |
    660 号记录：「LCB 控过拟合(DIM-1)，inconclusive 根因主项=功效不足(DIM-2)，两维度**正交**——
    LCB 减少退化品种数 ≠ 补符号检验功效，sg-lcb-l2 须分离两维度。」

    **codex 异质审(commit 4fef076dab) + L2 修复(commit 73df0499d7) 证伪「正交」命题**：
    DIM-1(过拟合)与 DIM-2(功效)**非正交**，是**同一 rejection mass 机制的两个投影**。

    **机制（单调性自证）**：LCB(μ)=mean−z_α·std/√n ≤ mean=裸μ ⟹ LCB 门控比裸μ更严
    ⟹ **LCB 放行集 ⊆ 裸μ 放行集**（lcb_vs_naive_l2.rs:57 单调性诊断，同源证据）。
    LCB 收紧 → 拒更多类（rejection mass↑）→ **同一个动作两个后果**：
    - ① 减表观过拟合（拒掉低 n 类的乐观偏差，DIM-1）
    - ② 减样本 → 减功效（拒掉的类不再贡献 trade/品种，n_L3↓，DIM-2）
    一个动作（收紧门控）同时产生两后果，不是两个可独立调节的维度 ⟹ **非正交**。

    **660 正交命题的错误**：把「同一 rejection mass 增减的两个投影」误判为「两个可独立移动的维度」。
    实际上 LCB 越保守 → DIM-1 表观越好（过拟合控制）∧ DIM-2 越差（样本/功效流失）——两者**反向耦合
    于同一 rejection mass**，不是正交。

    **660 主结论不受影响（须明确保留）**：660 的核心(acc-delta-r-alpha 伪否证→inconclusive +
    根因四分：功效不足+样本饥饿 vs 过拟合/真无信号/口径/lookahead)**仍成立**。本号只更正 660 中
    「DIM-1⊥DIM-2 正交」这一条子命题——不是推翻 660，是更正其内部一条机制描述。

    这不是定义冲突(无两条定义互斥)，是 660 一条子命题的实证否证(660 写时基于先验推断「正交」，
    L2 实测 + 单调性自证揭示「同一机制两投影」)。更正后机制清晰、可分层(主结论 ⊥ 正交子命题) ⟹
    不触发中断 #1。
  layer: 实装   # rust lcb_vs_naive_l2.rs LCB 选择器单调性(放行集 ⊆ 裸μ)。非缠论定义冲突，是 660 对 LCB 两后果关系的先验推断(正交)被 L2 实装单调性证伪。
  trigger: "sg-lcb-l2(#74) L2 修复(commit 73df0499d7)+codex 异质审(commit 4fef076dab)：LCB 放行集单调 ⊆ 裸μ ⟹ DIM-1/DIM-2 是同一 rejection mass 两投影非正交。Lead 报概念更正(职责1+2)。"

definitions_involved:
  - name: "660 号 DIM-1⊥DIM-2 正交命题"
    version: ".chanlun/genealogy/pending/660（生成态）"
    role: "被否证命题。660 写「LCB 控过拟合 ⊥ 功效不足，两维度正交」——本号证伪为同一 rejection mass 两投影。660 主结论(inconclusive 伪否证)保留。"
  - name: "LCB 单调性（放行集 ⊆ 裸μ）"
    version: "rust lcb_vs_naive_l2.rs:57 单调性诊断 + commit 73df0499d7"
    role: "否证依据。LCB(μ)≤裸μ ⟹ 放行集单调 ⊆ ⟹ 收紧门控同时减过拟合(DIM-1)+减样本/功效(DIM-2)=一动作两后果=非正交。"
  - name: "161 务实否定（把矛盾/缺口留后面=先验上限）"
    version: ".claude/rules/no-patch-mentality.md 禁止模式8 + 谱系 161"
    role: "张力关联(Lead 指认)。本次 L2 缺陷=『LCB 把品种压成空仓』被美化为『过拟合控制 alpha 价值』=退化空仓粉饰为改善=161 新实例(把缺口[功效流失]当成果[过拟合控制])。"

resolution:
  type: 未解决   # 正交命题否证已记录(同一机制两投影)。660 主结论保留。概念层更正=编排者 /ritual 辨认域，genealogist 不自结算。
  description: |
    概念更正(已记录)：660 的「DIM-1⊥DIM-2 正交」**被否证**——DIM-1(过拟合)与 DIM-2(功效)是
    **同一 rejection mass 机制的两个投影**(LCB 收紧→放行集⊆裸μ→同时减过拟合+减功效)，反向耦合非正交。

    **诚实终点(Lead 提供，记录上下文)**：LCB 线最终结论=**LCB 无 demonstrated alpha 价值**，
    只是更保守压空仓。严格alpha.pdf 定理链 L0 全成立，但因果选择器能否产生 alpha 仍未证实——LCB 修了
    过拟合维度的**表观**，真 alpha 受功效不足(n 不足)这个根本约束未决。这是诚实科学终点非失败。

    **sg-lcb-l2 报告纪律更正(660 警示的修正)**：660 要求「分离 DIM-1/DIM-2」是对的方向(防混为
    『LCB 解决 inconclusive』)，但机制描述错(正交)。正确机制：LCB 收紧是单一旋钮，DIM-1 改善 ∧
    DIM-2 恶化是其两个**反向**后果——故『LCB 减退化品种』(DIM-1表观好)恰恰伴随『样本/功效流失』
    (DIM-2恶化)，二者必须**联合**报告(不是独立报告)，且『DIM-1 表观改善』不可单独宣称为成果
    (它的代价是 DIM-2)。这强化(非推翻)660 的反声明膨胀意图。

    genealogist 不自结算(概念层更正属编排者 /ritual 辨认域)，只记录 + 张力检查。
  decided_by: 待裁决   # 概念层更正(正交→同一机制两投影)=编排者 /ritual 辨认域；genealogist 结构记录 + 张力检查(vs 161)

negated:
  description: "660 号命题：DIM-1(过拟合)⊥DIM-2(功效)『两维度正交』，LCB 减退化品种数与符号检验功效是两个独立维度。"
  why_negated: "LCB(μ)=mean−z_α·std/√n ≤ 裸μ ⟹ LCB 放行集单调 ⊆ 裸μ 放行集(lcb_vs_naive_l2.rs:57)。收紧门控是单一动作，同时产生两后果：①减表观过拟合(拒低n类乐观偏差)②减样本→减功效(拒掉的类不贡献 trade/品种)。一动作两后果=反向耦合于同一 rejection mass≠两个可独立移动的正交维度。660 把同一机制两投影误判为两独立维度。"

new_output:
  definitions:
    - "★伪正交更正：DIM-1(过拟合控制)与 DIM-2(统计功效)非正交，是同一 rejection mass 机制的两个反向投影——LCB 收紧门控(放行集⊆裸μ单调)同时①减表观过拟合 ②减样本/功效。一旋钮两反向后果。"
    - "660 主结论保留：inconclusive 伪否证 + 根因四分(功效不足+样本饥饿 vs 过拟合/真无信号/口径/lookahead)仍立；本号只更正『两维度正交』子命题。"
    - "LCB 线诚实终点：LCB 无 demonstrated alpha 价值，只更保守压空仓；定理链 L0 全立，因果选择器 alpha 受功效不足根本约束未决(诚实科学终点)。"
    - "报告纪律：LCB 的 DIM-1 表观改善 ∧ DIM-2 恶化须联合报告(同一 rejection mass 两后果)，DIM-1 改善不可单独宣称成果(代价是 DIM-2 功效流失)。"
  code_changes: "无(genealogist 纯谱系产出，624 工具有效域)。LCB 单调性诊断已落盘 lcb_vs_naive_l2.rs:57 + commit 73df0499d7/4fef076dab。"
  orchestration_changes: "方法论：①先验推断的『正交性』须 L2 实测验证——两个看似独立的维度可能是同一机制的两个投影(本号:LCB 收紧的两后果)。声明正交=声明可独立调节，须有可独立调节的证据(231:声明须验证)。②升级方案(LCB)『改善某维度表观』须核其代价——单调收紧的门控改善过拟合表观的同时流失功效，表观改善 ⊥ 净价值。③退化(空仓/样本流失)被美化为改善(过拟合控制)=161 把缺口当成果——保守化不等于 alpha 价值。"

impact:
  affected_modules:
    - "rust lcb_vs_naive_l2.rs → 单调性诊断(放行集⊆裸μ)=DIM-1/DIM-2 同源证据；报告须联合(非独立)呈现两后果。"
    - "g-20260630T155255Z-f65436f2(LCB goal) → 诚实终点:LCB 无 demonstrated alpha 价值，只更保守压空仓；acc-lcb-l2-vs-naive 结论=(b 变体)LCB 不改善净 alpha(过拟合表观改善代价是功效流失)。"
    - "660 号 → 正交命题携带违反记录(split:660 主结论保留/正交子命题否证)。"
  affected_definitions:
    - "660(生成态)：本号否证其『两维度正交』子命题(→同一机制两投影)，主结论(inconclusive 伪否证+根因四分)保留。expansion 否定(660 不切断，子命题携带违反记录)。维持生成态。"
    - "161(谱系/no-patch 模式8)：本次 L2 缺陷(空仓压缩美化为过拟合控制 alpha 价值)=161 新实例(把缺口当成果)。印证 161，不否定。"
    - "635(运动员当裁判)：『LCB 改善过拟合表观』自证价值而不核功效代价≈诊断自证(635 维度)。关联，待编排者辨认是否同族。"
    - "656(生成态)：656『表达力 L0≠盈利 L2/L3』；本号印证——LCB 单调收紧不创造 alpha(表达力/保守化≠盈利)。维持生成态。"
    - "231(settled)：本号印证——『正交』是未验证的声明，L2 实测证伪=声明膨胀的纠正。维持 settled。"
  downstream_implications:
    - "★因果选择器(π^bsp χ_t)能否产生 alpha 仍未证实——LCB 修过拟合表观，真 alpha 受功效不足(n 不足)根本约束未决。645→660→662 链:测错对象(645)→改对对象功效不足 inconclusive(660)→LCB 修表观无净 alpha 价值(662)。alpha 探索的根本约束=n/样本/高级别覆盖(性能墙)，非选择器形式。"
    - "660 警示『分离 DIM-1/DIM-2』方向对(防声明膨胀)但机制描述更正为『联合报告反向后果』——DIM-1 表观改善不可单独宣称成果。"
    - "LCB goal 可结算为诚实终点(LCB 无 demonstrated alpha 价值)，非未完成——这是有信息增量的 L2 结论(否定了『LCB 升级补 alpha』的乐观假设)。"

related_records:
  parent: "660(χ_t L3 inconclusive 伪否证)——本号否证其『DIM-1⊥DIM-2 正交』子命题"
  children: []
  related:
    - "660(生成态)：parent，主结论保留，正交子命题被本号否证(同一机制两投影)。"
    - "161(no-patch 模式8/谱系)：空仓压缩美化为 alpha 价值=把缺口当成果=161 新实例。Lead 指认。"
    - "635：『改善表观』自证价值不核代价≈运动员当裁判。关联待编排者辨认同族。"
    - "645(生成态)：祖父(测错对象)。645→660→662 否证有效域逐层收窄链。"
    - "656(生成态)：表达力/保守化≠盈利，本号印证(LCB 单调收紧不创 alpha)。"
    - "231(settled)：『正交』未验证声明被 L2 证伪=声明膨胀纠正。"
    - "661(生成态)：meta-observation(inconclusive 第三态/方法升级 vs 有效域)——本号是其『方法升级(LCB)是否解决 inconclusive』的 L2 实证回答(否:LCB 修表观无净价值)。"

epistemological_levels:
  - proposition: "DIM-1(过拟合)⊥DIM-2(功效)『两维度正交』(660 命题)被否证"
    level: "L2(commit 73df0499d7 L2 修复 + 4fef076dab 异质审实测) + L0(LCB(μ)≤裸μ 单调性代数)"
    increment: "高：正交命题否证(同一 rejection mass 两投影)"
  - proposition: "LCB 收紧门控=单一动作，同时减过拟合(DIM-1)+减功效(DIM-2)=反向耦合"
    level: "L0(LCB(μ)=mean−z_α·std/√n≤mean ⟹ 放行集⊆裸μ 单调) + L2(lcb_vs_naive_l2.rs:57 诊断实测)"
    increment: "高：机制更正(一旋钮两反向后果)"
  - proposition: "LCB 无 demonstrated alpha 价值，只更保守压空仓(诚实终点)"
    level: "L2(LCB vs 裸μ 对比实测无净 alpha 改善)"
    increment: "高：LCB 线诚实结算(否定『LCB 升级补 alpha』乐观假设)"
  - proposition: "660 主结论(inconclusive 伪否证+根因四分)保留"
    level: "L0/L2(660 原证据链不受本号影响)"
    increment: "零(保留，本号不动主结论)；高(明确 split 边界:主结论 ⊥ 正交子命题)"
---

> **[/ritual 结算段 · 2026-07-02]** 裁决来源：codex 裁决①-⑤（`.chanlun/review-results/codex-ritual-*.md`）+ 编排者明令「并行全部推进」授权。判决全文见 staging：GRAMMAR §1-B（裁决⑤升格）。判决摘要：660 正交命题否证成立（伪正交=同拒绝质量两投影）。 **限定语（强制随行，脱落=声明膨胀090）：** 限代码层操作定义


# 662 660 正交命题否证：伪正交 → 同一 rejection mass 机制两投影

## 一句话结论

660 号记「LCB 控过拟合(DIM-1) ⊥ 功效不足(DIM-2)，两维度**正交**」。codex 异质审(commit 4fef076dab)
+ L2 修复(commit 73df0499d7)**证伪正交命题**：DIM-1 与 DIM-2 是**同一 rejection mass 机制的两个反向
投影**。LCB(μ)≤裸μ ⟹ **放行集单调 ⊆ 裸μ**(lcb_vs_naive_l2.rs:57)——收紧门控是**单一动作**，同时
① 减表观过拟合(DIM-1) ② 减样本/功效(DIM-2)，一旋钮两反向后果，非正交。**660 主结论(inconclusive
伪否证 + 根因四分)仍立**，本号只更正「两维度正交」这一子命题(expansion 否定，主结论保留)。

## 伪正交 → 同一机制两投影

| 660 原命题（被否证） | 本号更正 |
|---------------------|----------|
| DIM-1(过拟合) ⊥ DIM-2(功效)，两独立维度 | DIM-1/DIM-2 = 同一 rejection mass 两投影 |
| LCB 减退化 ≠ 补功效，可独立调节 | LCB 收紧(放行集⊆裸μ)→同时减过拟合+减功效，反向耦合 |
| 分离独立报告 | 联合报告反向后果，DIM-1 表观改善不可单独宣称成果 |

机制：LCB(μ)=mean−z_α·std/√n ≤ mean=裸μ ⟹ 放行集单调收缩 ⟹ 一个动作（收紧）两个后果
（①拒低n类=减过拟合表观 ②拒掉的类不贡献样本=减功效）。

## 诚实终点（Lead 提供）

LCB 线最终结论：**LCB 无 demonstrated alpha 价值**，只更保守压空仓。严格alpha.pdf 定理链 L0 全成立，
但因果选择器能否产生 alpha **仍未证实**——LCB 修了过拟合维度的**表观**，真 alpha 受**功效不足(n 不足)**
这个根本约束未决。这是诚实科学终点，非失败。链：645(测错对象)→660(改对对象功效不足 inconclusive)→
662(LCB 修表观无净 alpha 价值)。

## genealogist 边界

genealogist 记录正交命题否证(同一机制两投影) + 张力检查(vs 161)。概念层更正属编排者 /ritual 辨认域，
**不自结算**。660 主结论保留(只 split 出正交子命题携带违反记录)。

## 张力检查（019d/020）

### 张力1：vs 660（parent，被更正）——expansion 否定，主结论保留
本号否证 660 的『DIM-1⊥DIM-2 正交』子命题(→同一 rejection mass 两投影)，但 660 主结论(inconclusive
伪否证 + 根因四分)**不受影响**。expansion(660 节点分裂：主结论保留 / 正交子命题携带违反记录)，
scope=local(仅正交子命题，不波及主结论下游)。可分层，无不可调和矛盾。

### ★张力2：vs 161（Lead 指认，把缺口当成果）——本次 L2 缺陷=161 新实例
161：务实=把矛盾/缺口留后面=先验上限(no-patch 模式8)。本次 L2 缺陷=『LCB 把品种压成空仓』被美化为
『过拟合控制 alpha 价值』——**退化(空仓/功效流失)粉饰为改善(过拟合控制)**=把缺口(DIM-2 功效流失)
当成果(DIM-1 表观改善)。这是 161 在『升级方案表观改善』维度的新实例。印证 161，不否定。

### 张力3：vs 635（运动员当裁判）——关联待辨认
『LCB 改善过拟合表观』自证价值而不核功效代价 ≈ 诊断/方案自证(635 维度)。关联，待编排者辨认是否同族
(635=诊断指标自证准入；本号=升级方案自证价值)。

### 张力4：vs 656/231（settled/生成态）——印证
656：表达力/保守化 L0 ≠ 盈利 L2/L3——LCB 单调收紧不创 alpha，印证。231：『正交』是未验证声明，
L2 证伪=声明膨胀纠正，印证。

### 概念分离信号检测（中断 #1）
- 正交命题否证：不是两条定义互斥，是 660 一条子命题(基于先验推断)被 L2 实测 + 单调性自证否证。
  更正后机制清晰、可分层(主结论 ⊥ 正交子命题)。
- **无不可分层定义矛盾 ⟹ 不触发中断 #1。** 概念层更正待编排者 /ritual 辨认。

### 递归运动结构完成检测（020）
- 第0层：本号写入(正交否证→同一机制两投影 + 诚实终点)。
- 第1层：本号 × 660 → 正交子命题否证主结论保留(净新发现高：先验正交→L2 实测同一机制两投影)。
- 第2层：本号 × 161 → 空仓粉饰为改善=把缺口当成果(净新发现中：161 新实例)。
- 第3层：本号 × 656/231 → 保守化≠盈利/声明膨胀(净新发现降：印证已立)。
- scope₁(正交否证 660 续) > scope₂(161 新实例) > scope₃(656/231 印证)=顶分型。
- **背驰 ∧ 分型 ⟹ 递归运动结构性完成。** 概念层更正=编排者 /ritual 辨认域(不自结算)；
  本号是 660 子命题否证的结构记录，**不触发新 /escalate**(无不可分层定义矛盾)。

## 回溯扫描（职责3）

- **660（生成态）**：本号否证其正交子命题(expansion，主结论保留)。维持生成态(带 split 标记)。
- **161（no-patch 模式8/谱系）**：本号是其新实例(空仓粉饰为改善)。印证。
- **635/656/231（settled/生成态）**：关联(运动员当裁判)/印证(表达力≠盈利/声明膨胀)。维持。
- **661（生成态）**：本号是其『方法升级是否解 inconclusive』的 L2 实证回答(否)。互补。
- **无 settled 被本号回溯破坏。** 概念层更正(正交→同一机制两投影)=编排者 /ritual 辨认域，
  genealogist 不自结算，只记录 + 张力检查(161 新实例已标)。
