---
id: "666"
number: 666
status: 生成态   # genealogist 结构记录：级别相位 (−1)^ℓ 作为结构状态变量（新概念，657 addendum_C 未捕获）+ beta/alpha 命名纠正（上一轮 parity 报告反了，本 PDF §7 纠正）+ 奇偶交替不可识别性三机制 A/B/C。基于 GPT 咨询《奇偶相位》(parity-phase-consult-20260701.txt 12页)。**不自结算**（级别相位是否「缠论正确形式」须 L2+ 反事实实证 = parity-analysis 正在做；命名纠正=事实更正）。
# ★证伪更新（2026-07-01，commit f212c9323c/b7cc9e9bdc，报告 review-results/oddeven-causation-counterfactual-20260701.md）：
# 增量① 的**可交易 alpha 断言被 L2 反事实证伪**——随机置换方向标签检验（ChatGPT 方法1，200次固定种子，保入场/出场/持有期/成本+买卖总数，只 shuffle δ）：
# perm_p=0.6915≫0.05，S_obs=1050.80 < S_perm均值=1154.83（打乱δ后结构量反增）⟹ 方向标签不携超随机漂移的结构信息
# ⟹ 三机制不可识别性（增量③）被**部分消解**：主因 = 机制C（beta/regime 漂移）；机制B（口径4放大器，换口径降8-10/12）确认；**机制A（几何真结构）否证**。
# 级别相位从「alpha 候选/结构变量」**降级为「beta 漂移在 (level,δ) 网格的确定性投影」**。观测事实 12/12 保留、增量②命名纠正保留、「级别携带相位信息 z 须升维」保留——仅「相位是可交易 alpha 候选」被证伪。
# perm 检验 = L2（BTC 单标的全历史）。异质审查（codex）因 429 曾缺席、现 CLI 已通待补 ⟹ perm 结论**待 codex 异质否定**。见下方「证伪层（2026-07-01）」小节。
date: "2026-07-01"
type: bias-correction   # 双段：①级别相位结构变量（657 定理1「须纳 ρ」的 refinement——ρ 的一个具体分量是 (−1)^ℓ 级别相位，657 只泛泛说「纳 ρ」未指认相位分量）；②beta/alpha 命名纠正（上一轮 parity 报告把 (μ+−μ−)/2 当结构 alpha、(μ++μ−)/2 当 beta，方向签名 PnL 下反了）。
depends_on: ["231", "657", "663"]
related: ["645", "656", "664", "665", "660", "alpha-pdf-mu-z-a-positive", "project_regime_is_level_truncation_artifact", "project_recursive_t_architecture_v2", "project_zero_lookahead_backtest"]
title: "★级别相位 (−1)^ℓ 作为结构状态变量 + beta/alpha 命名纠正。GPT《奇偶相位》12页回应全历史 BTC 12/12 级别×方向 μ̂ 奇偶交替（买 L0+L1−L2+L3−L4+L5−，卖反相，sign μℓ,δ=sign(δ(−1)^ℓ)，正期望 δ*ℓ=(−1)^ℓ 偶买奇卖）。三增量：①级别相位——「级别不是中性索引，携带相位信息；同一『买点』语法在不同级别不是同一经济状态」，状态须从 z=(ℓ,δ) 升级到 z=(ℓ,δ,(−1)^ℓ,σhigher,exit rule,确认滞后,父级容器状态)。这是 657「须纳 regime 代理 ρ」的具体分量指认（657 未捕获相位分量），也是 663 pooled 混合的机制化。②beta/alpha 命名纠正：方向签名 PnL 下 (μ++μ−)/2≈aℓ−C 更接近方向中性结构 edge，(μ+−μ−)/2≈bℓ 更接近市场漂移 beta——上一轮 parity 报告命名反了，须标注更正防下游误用。③不可识别性：仅 12 均值符号不能唯一识别主因（机制A 递归几何反相 / B 出场配对滞后 / C 级别=regime 三者都产生同样交替），须反事实（换出场规则/测滞后/加 σhigher/同容器配对）。【★2026-07-01 证伪更新：随机置换 perm_p=0.69 证伪机制A（几何真结构），坐实机制C（beta漂移）为主因——级别相位降级为 beta 漂移在 (level,δ) 网格的确定性投影，「可交易 alpha 候选」断言被否证，观测事实12/12+命名纠正保留。待 codex 异质否定。】正确操作=偶级别只做买奇级别只做卖（正类交易负类不交易，X_inv=−X−2C 仅 μ<−2C 反向才正）。peer 663（判据）/664（对象）/665（方法论）同簇。"
negation_source: "GPT《奇偶相位》PDF（parity-phase-consult-20260701.txt，12页，第三方咨询，回应蜂群全历史 BTC 12/12 奇偶交替发现）+ alpha.pdf μ(z,a)>0 判据 + 657（定理1 须纳 ρ，本号指认 ρ 的相位分量）+ ★随机置换反事实（oddeven-causation-counterfactual-20260701.md，perm_p=0.69 证伪机制A可交易性）"
negation_form: "expansion"   # expansion：上一轮 parity 报告（规定者）在执行中违反自身——报告声称 (μ+−μ−)/2=结构分量/(μ++μ−)/2=beta，但方向签名 PnL 下代数推导（PDF §7）给出相反归属。规定者产出的命名与其自身依据（方向签名收益分解）不一致=膨胀型否定。级别相位段则是 refinement（对 657「纳 ρ」的分量指认，非否定）。★2026-07-01 追加：级别相位「可交易 alpha 候选」自身在 perm 反事实下被证伪（expansion 的第二层——本号增量①的乐观断言违反其自身 231 有效域约束「须反事实识别主因」，反事实一做，机制A 即否证）。

# 拓扑效果标注（147号下游推论3：negates 非空必填）
# 本号 negates 两个对象：
#   (1) 上一轮 parity 报告的 beta/alpha 命名归属（split，见 topo_effect 主字段）
#   (2) ★本号自身增量①的「级别相位=可交易 alpha 候选」断言（refine，见 topo_effect_falsification）
# 以否定事件的实际拓扑后果（retrospective，141号结论1）判断，非 negation_form 预分类
topo_effect: "split:parity-alternation-report-naming:downstream"
# split:parity-alternation-report-naming:downstream — 上一轮 parity 报告节点分裂：一个保留「奇偶交替 12/12 观测事实 + 三候选机制方向（几何反相/出场滞后/上级过滤）成立」，一个携带「beta/alpha 命名归属反了——方向签名 PnL 下 (μ++μ−)/2 是结构 edge 非 beta，(μ+−μ−)/2 是市场漂移 beta 非结构」违反记录。下游（任何引用该报告 beta/alpha 命名的 econ_positive / parity-analysis 叙事）随命名更正而分裂——用旧命名的结论须重标。仅命名归属分裂，观测事实与机制候选保留。
topo_effect_falsification: "refine:666-increment1-tradable-alpha:local"
# ★2026-07-01 refine:666-increment1-tradable-alpha:local — 本号自身增量①节点被 perm 反事实 refine（不是 split，不是 sever）：
#   为何 refine 而非 split：split 会保留一个「携带违反记录但仍成立」的分支；此处「可交易 alpha 候选」断言不是被违反而是被**直接否证**（perm_p=0.69），
#   但增量①的其余内容（级别携带相位信息、同买点语法不同级别不同经济状态、z 须升维、657「纳 ρ」的相位分量指认）**全部保留且更强**（现在 ρ 的这个分量被精确定性为 beta 投影）。
#   为何 local 而非 downstream：证伪缩小的是增量①的有效域（可交易性），不推翻其结构指认——下游对「相位携带信息/z 升维」的引用不变，仅「相位可作独立 alpha entry」的引用须撤。
#   拓扑后果：增量①节点保留骨架，替换其叶子标签（alpha 候选 → beta 投影）。观测事实 12/12 + 增量② + 增量③框架 = 不动，仅增量③的「不可识别」被 perm 部分消解为「已识别主因=C」。

# 矛盾（type=bias-correction：双段——级别相位 refinement + 命名纠正）
contradiction:
  description: |
    全历史 BTC（461万 bar，12626 买卖点）跑出 **级别×方向 μ̂ 奇偶交替 12/12 无例外**：
    买方向 L0+ L1− L2+ L3− L4+ L5−，卖方向反相。写成数学命题：
        sign μℓ,+ = (−1)^ℓ,  sign μℓ,− = −(−1)^ℓ  ⟺  sign μℓ,δ = sign(δ·(−1)^ℓ)
    正期望方向 δ*ℓ = (−1)^ℓ（偶级别做买，奇级别做卖）。

    GPT《奇偶相位》PDF 严格回应，三个增量（657 addendum_C 未捕获，非重复）：

    **增量①：级别相位 (−1)^ℓ 是结构状态变量（新概念）。**
    「级别不是中性索引，而携带相位信息。」若 L0 买点为正、L1 买点为负、L2 又为正，则
    **同一个『买点』语法在不同级别上不是同一个经济状态**。这否定过粗状态 z=(δ,Iγ) 或 z=(买卖点类型)。
    严格状态须升级：
        z = (ℓ, δ) → z = (ℓ, δ, (−1)^ℓ, σhigher, exit rule, 确认滞后, 父级容器状态)
    这**不是** 657 addendum_C 已覆盖的内容——657 只说「须纳 regime 代理 ρ」（泛指），本号指认
    **ρ 的一个具体分量是级别相位 (−1)^ℓ**：级别递归每升一级操作相位翻转一次。657 定理1 说「同一 z
    在不同 r 下 μ 翻转，pooled≤0」，本号给出 r 的一个精确分量（级别相位）+ 交替的精确形式（(−1)^ℓ）。

    【★2026-07-01 证伪：增量① 的「相位是可交易 alpha 候选」断言被 perm 反事实否证——见下方「证伪层」。
    保留的部分：级别携带相位信息、同买点语法不同级别不同经济状态、z 须升维、ρ 的相位分量指认。
    被否证的部分：该相位分量是「几何真结构」（机制A）——perm_p=0.69 坐实它是 beta/regime 漂移（机制C）的投影，
    非独立可交易 alpha。ρ 的这个分量现被精确定性为「beta 漂移在 (level,δ) 网格的确定性投影」。】

    **增量②：beta/alpha 命名纠正（上一轮 parity 报告反了）。**
    上一轮 parity 报告提出：(μℓ,+ − μℓ,−)/2 = 结构分量，(μℓ,+ + μℓ,−)/2 = beta 候选。
    PDF §7 严格推导（方向签名 PnL 下）给出**相反**归属：设共同市场漂移 bℓ、结构边际 aℓ，
        E[ΔP|buy,ℓ] = bℓ + aℓ,  E[ΔP|sell,ℓ] = bℓ − aℓ
        μℓ,+ = bℓ + aℓ − C,  μℓ,− = −bℓ + aℓ − C（成本近似相等 C+≈C−=C）
    则：
        (μ+ + μ−)/2 ≈ aℓ − C  → 方向中性结构 edge（买卖两边都因结构正确受益时为正）
        (μ+ − μ−)/2 ≈ bℓ      → holding window 上的市场方向漂移 / beta
    **上一轮报告命名反了。** 依赖买卖样本持有期/成本/时间分布近似可比（否则不能直接分解）。
    严格剥离 beta 须：随机置换方向标签 / 回归控制 holding return / 同容器配对（PDF §8 方法1-3）。
    【★2026-07-01：方法1（随机置换方向标签）已执行——见证伪层。这不仅剥离 beta，更把整个奇偶交替归因于 beta。】

    **增量③：奇偶交替不可识别性（三机制）。**
    仅 12 个均值符号不能唯一识别主因。至少三种机制都产生同样交替：
    - 机制A 递归几何相位反相：ϕℓ=(−1)^ℓ，μℓ,+≈Aℓϕℓ（Aℓ>0）⟹ 每升一级操作相位翻转，几何必然。
    - 机制B 出场配对滞后：出场 τℓ^out 落在 ℓ+1 级反相区间，同方向相邻级别收益符号翻转。
    - 机制C 级别=regime：R 分布随级别变 P(R|ℓ)，pooled 后 μℓ,δ=Σr μℓ,δ,r P(r|ℓ,δ) 出现交替。
    机制参数数目 >12，都能拟合同一组符号 ⟹ 主因不可识别。须反事实：
    换出场规则（固定R/同级别反向/上级失效/下一反向/终点pivot/确认bar）/ 测 ηin ηout / 加 σhigher / 同容器配对。
    若只在「下一反向信号出场」下有交替、固定出场消失 ⟹ 出场口径主导（机制B）；
    多口径都保留 ⟹ 几何反相更可能是真结构（机制A）。
    【★2026-07-01：反事实已执行，不可识别性被**部分消解**——perm 检验（方法1）识别主因=机制C（beta漂移），
    多出场口径反事实识别机制B=口径4的12/12放大器（换口径降8-10/12），机制A 被否证。见证伪层。】

    这不是定义冲突（无两条缠论定义互斥）。增量①是对 657「纳 ρ」的分量 refinement；增量②是命名事实更正
    （expansion：报告命名与其自身方向签名分解依据不一致）；增量③是不可识别性声明（有效域约束）。
    均可分层 ⟹ **不触发中断 #1**。
  layer: 概念   # 状态定义层（级别是否携带相位）+ 命名层（beta/alpha 归属）——非 rust 实装冲突。相位分量指认向下影响 657/663 的「纳 ρ」操作化。
  trigger: "编排者 ChatGPT 咨询《奇偶相位》PDF 回应蜂群全历史 BTC 12/12 级别×方向 μ̂ 奇偶交替发现——直接给出级别相位结构变量、beta/alpha 命名纠正、三机制不可识别性。★2026-07-01 追加触发：parity-analysis 执行 ChatGPT 方法1 随机置换反事实（commit f212c9323c），perm_p=0.69 证伪机制A可交易性。"

definitions_involved:
  - name: "级别相位 (−1)^ℓ（级别递归每级翻转操作相位）"
    version: "GPT《奇偶相位》PDF §2/§12（parity-phase-consult-20260701.txt）"
    role: "核心新概念。级别不是中性索引，携带相位信息——同一买卖点语法在不同级别是不同经济状态。这是 657「须纳 regime 代理 ρ」的具体分量指认（ρ ⊇ {(−1)^ℓ, σhigher, exit rule, 确认滞后, 父级容器状态}）。657 未捕获相位分量，本号补。★2026-07-01：该相位分量经 perm 反事实定性为 **beta 漂移在 (level,δ) 网格的确定性投影**（机制C），非几何真结构（机制A 否证）——分量指认成立，但其经济性质=beta 投影非可交易 alpha。"
  - name: "beta/alpha 分解（方向签名 PnL 下）"
    version: "GPT《奇偶相位》PDF §7/§8/问题3"
    role: "命名纠正对象。(μ++μ−)/2≈aℓ−C=方向中性结构 edge（非上一轮报告说的 beta）；(μ+−μ−)/2≈bℓ=市场漂移 beta（非结构）。上一轮 parity 报告命名反了，本号 split 更正。严格剥离须随机置换/回归控制/同容器配对（不能只用 (μ+±μ−)/2 当终局结论）。★2026-07-01：随机置换（方法1）已执行——perm_p=0.69 表明 δ 标签打乱后 S 不减反增（S_obs=1050.80<S_perm=1154.83），奇偶交替整体归于 beta 漂移，方向标签不携超漂移结构信息。"
  - name: "alpha.pdf / 全互斥定义策略：可交易性判据 μ(z,a)>0"
    version: "alpha.pdf（一级权威第三方证明）+ 663 真判据"
    role: "正确操作来源。若 OOS 证明 μℓ,δ>0 ⟺ δ=(−1)^ℓ，oracle 策略=偶级别只做买、奇级别只做卖（正类交易，负类不交易）。负类不能简单反做：X_inv=−X−2C，仅 μ<−2C 时反向才正。全互斥分类后所有语法状态都定义，但只交易正条件收益状态 χℓ,δ=1[LCB(μℓ,δ)>θ]。★2026-07-01：perm 证伪后，μℓ,δ>0 的正类**不可作独立 entry 升基座**——perm 检验证明 μ̂>0 是 beta 漂移的确定性投影，winner's curse 被反事实坐实（非仅怀疑）。要交易须先加 σhigher regime 门控隔离往返段 + OOS 重验。"
  - name: "657 定理1（分类完全 ≠ 收益充分统计量，须纳 ρ）"
    version: ".chanlun/genealogy/pending/657（生成态，addendum_C + tension_check_regime_dual）"
    role: "本号是其 refinement（parent）。657 说「同一 z 在不同 r 下 μ 翻转 pooled≤0，须纳 ρ 代理」（泛指），本号指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替的精确形式。657 已覆盖 PDF 定理1/定理2，本号补 657 未捕获的三增量（相位分量/命名纠正/三机制不可识别性）。★2026-07-01：perm 证伪把 657 的 R=f(Z) 论断在级别相位维**深化**——μ̂ 符号本身（不只是「显著性不够」）就是 beta 投影，即 μℓ,δ 的符号完全由 ρ（beta/regime）决定，Z（级别语法分类）本身不携方向收益信息。这是 657「分类≠收益充分统计量」的最强实证：连分类的 μ 符号都是 ρ 的确定性函数。"
  - name: "663 判据错误（统计显著性 ≠ μ>0 可交易性）"
    version: ".chanlun/genealogy/pending/663（生成态）"
    role: "印证 + 机制化。663 说真判据 μ(z,a)>0，本号给出 μℓ,δ 交替的精确形式并指出正确操作（正类交易负类不交易）是 663 判据在级别相位维度的实例。奇偶交替是 663 pooled 混合均值的机制化（级别相位是 pooling 变量之一）。★2026-07-01 深化：663 说「统计显著性≠可交易性」——perm 检验证明奇偶交替连 μ̂>0（不只是显著性）都是 beta 投影非结构。这比 663 判据更深：663 是「显著但不可交易」，perm 结果是「μ̂ 符号本身=beta 漂移的确定性投影，非结构 edge」——winner's curse 从「可能」升为「已坐实」（663/665 簇的置换实证锚点）。"
  - name: "231 形式化有效域规则"
    version: ".claude/rules/formalization-validity-domain.md（settled）"
    role: "约束。增量③不可识别性 = 231 实例：12 均值符号（观测）的有效域不覆盖「唯一识别主因」（定义域=所有生成机制）。声称「奇偶交替=几何必然真结构」而无反事实 = 有效域膨胀。级别相位「是缠论正确形式」须 L2+ 反事实实证（=parity-analysis 正在做），当前仅 L2 观测（12/12 单标的 BTC）。★2026-07-01：反事实实证已产否定性结果（231 铁律「否定性结果比确认更有价值」的兑现）——perm_p=0.69 缩小了级别相位的有效域，从「可交易 alpha 候选」缩到「beta 漂移投影」。这是 231 强调的「否定性结果缩小有效域边界」的典型。perm 检验本身 = L2（BTC 单标的），待 L3 跨标的（震荡型标的可能翻案，见边界条件）+ codex 异质否定。"
  - name: "缠论买卖点定义（一/二/三类，级别无关同义）"
    version: "缠论知识库.md + docs/chanlun/text/blog/INDEX.md（一级权威）"
    role: "张力对象（见 tension_check）。缠论定义层：买卖点一二三类在每个级别定义相同（级别无关）。经济层：PDF 说同买点在不同级别是不同经济状态（期望符号交替）。定义层同义 ⊥ 经济层不同期望——可分层（定义 vs 期望是不同对象）。★2026-07-01：perm 证伪进一步区分——经济层「不同期望」本身是 beta 投影（不同级别的持有窗对 BTC 单边趋势的不同投影），非缠论买卖点语法赋予的结构差异。定义层同义 + 经济层期望差异 = beta 漂移的级别投影，都不与缠论买卖点定义冲突。"

resolution:
  type: 未解决   # 级别相位结构变量已记录（657「纳 ρ」的分量 refinement）；命名纠正已记录（split 上一轮报告）；不可识别性三机制已记录。★2026-07-01：增量① 的可交易 alpha 断言经 perm 反事实证伪（降级为 beta 投影），不可识别性部分消解（主因=机制C）。级别相位「是缠论正确形式（可交易结构）」= **已否证**（机制A）；「携带相位信息 / z 须升维」= 保留。最终结算待编排者 /ritual + codex 异质否定。
  description: |
    概念记录（已完成）：GPT《奇偶相位》PDF 三增量落盘——
    ①级别相位 (−1)^ℓ 是结构状态变量（657「纳 ρ」的分量指认，657 未捕获）；
    ②beta/alpha 命名纠正（方向签名 PnL 下 (μ++μ−)/2=结构 edge、(μ+−μ−)/2=beta，上一轮报告反了，split 更正）；
    ③奇偶交替不可识别性（机制 A/B/C 都产生同样交替，须反事实识别主因）。

    ★2026-07-01 证伪更新（反事实实证已产出，见证伪层小节）：
    - 增量① 的「级别相位=可交易 alpha 候选」经随机置换方向标签检验证伪（perm_p=0.6915≫0.05，S_obs<S_perm）
      ⟹ 级别相位降级为「beta 漂移在 (level,δ) 网格的确定性投影」。观测事实 12/12 + z 须升维 + 相位分量指认保留。
    - 增量③ 不可识别性部分消解：主因=机制C（beta/regime 漂移，perm 坐实）；机制B=口径4放大器（换口径降8-10/12）；机制A 否证。
    - 正确操作从「按级别相位建仓」修正为「不建仓，除非先加 σhigher regime 门控 + OOS 重验」（perm 已否证独立可交易性）。

    严格下一步（行动类，parity-analysis 独占 econ_positive）：加 σhigher regime 门控后重跑 perm——若门控内 perm_p<0.05
    则往返 regime 下方向携结构信息（机制A 在某 regime 复活）；L3 跨标的（震荡型标的）重验；codex 异质否定 perm 结论。
    不预设结果（231 铁律）。
  decided_by: 待裁决   # 级别相位结构变量 + 命名纠正 = GPT PDF + 方向签名代数推导；反事实实验 = 行动类（parity-analysis 已执行 perm+多口径）；genealogist 结构记录三增量 + 657 refinement + 命名 split + ★perm 证伪降级 refine；最终结算待编排者 /ritual + codex 异质否定

negated:
  description: "(1) 上一轮 parity 报告（parity-alternation-question-20260701.md）的 beta/alpha 命名归属：(μℓ,+ − μℓ,−)/2 = 结构分量（alpha），(μℓ,+ + μℓ,−)/2 = beta 候选。(2) ★本号自身增量①的「级别相位 (−1)^ℓ 是可交易 alpha 候选 / 几何真结构（机制A）」断言。"
  why_negated: |
    (1) 命名归属：方向签名 PnL 下代数推导（PDF §7）给出相反归属。设 E[ΔP|buy,ℓ]=bℓ+aℓ、E[ΔP|sell,ℓ]=bℓ−aℓ（bℓ=共同市场漂移，aℓ=结构边际），成本近似相等 C+≈C−=C，则 μℓ,+=bℓ+aℓ−C、μℓ,−=−bℓ+aℓ−C。于是 (μ++μ−)/2≈aℓ−C=方向中性结构 edge（买卖两边都因结构正确受益时为正，故是结构项非 beta）；(μ+−μ−)/2≈bℓ=holding window 市场方向漂移=beta 项（非结构）。上一轮报告把两者归属对调——命名与其自身方向签名收益分解依据逻辑不一致（expansion：规定者违反自身规定）。注：此分解依赖买卖样本持有期/成本/时间分布近似可比，否则不能直接分解——但即便如此，归属方向由代数结构决定（结构 edge 是买卖同号项、beta 是买卖异号项），上一轮报告的方向错误独立于可比性假设。
    (2) ★可交易 alpha 断言（2026-07-01）：随机置换方向标签反事实（ChatGPT 方法1，200次固定种子 Fisher-Yates，保每信号 entry/出场/持有期/成本+买卖总数，只 shuffle δ）——S=Σ_class|μ̂|（真标签下方向携带的结构信息量）。S_obs(真标签)=1050.80，S_perm均值=1154.83（打乱δ后反而更大），perm_p(#perm≥obs)=0.6915≫0.05。若机制A（几何真结构）成立，打乱 δ 应破坏结构 ⟹ S_obs 应显著大于 S_perm ⟹ perm_p<0.05；实际 perm_p≈0.69 且 S_obs<S_perm ⟹ 真方向标签携带的结构信息不超过随机置换，甚至略小 ⟹ 奇偶交替是 beta/regime 漂移伪影（机制C），非几何真结构（机制A 否证）。多出场口径反事实（12/12 仅在口径4=下一反向出现，换 pivot/固定持有降 8-10/12）进一步表明 12/12 完美性是口径4特有的放大（机制B 放大器），非稳健结构。
    保留：级别携带相位信息、同买点语法不同级别不同经济状态、z 须升维、ρ 的相位分量指认——这些不被 perm 证伪（perm 否证的是「相位=可交易 alpha」，不是「相位携带信息」；相位确实携带信息，只是该信息=beta 投影非独立 alpha）。

new_output:
  definitions:
    - "★级别相位 (−1)^ℓ 作为结构状态变量：级别不是中性索引，携带相位信息——同一买卖点语法在不同级别是不同经济状态（正期望方向 δ*ℓ=(−1)^ℓ 交替）。状态须从 z=(ℓ,δ) 升级到 z=(ℓ,δ,(−1)^ℓ,σhigher,exit rule,确认滞后,父级容器状态)。这是 657「须纳 regime 代理 ρ」的具体分量指认——ρ 的一个精确分量是级别相位，657 只泛指未指认相位。★2026-07-01：该相位分量的经济性质经 perm 反事实定性为 beta 漂移在 (level,δ) 网格的确定性投影（机制C），非可交易 alpha（机制A 否证）。"
    - "★beta/alpha 命名纠正（方向签名 PnL 下）：(μ++μ−)/2≈aℓ−C=方向中性结构 edge（非 beta），(μ+−μ−)/2≈bℓ=市场漂移 beta（非结构）。上一轮 parity 报告命名反了。结构 edge=买卖同号项（两边都结构正确则正）；beta=买卖异号项（市场漂移）。严格剥离须随机置换方向标签/回归控制 holding return/同容器配对，不能只用 (μ+±μ−)/2 当终局。★2026-07-01：随机置换已执行——perm_p=0.69 表明整个奇偶交替（不只是 (μ+−μ−)/2 项）都归于 beta 漂移，方向标签不携超漂移结构信息。"
    - "★奇偶交替主因（perm 反事实识别，2026-07-01）：主因=机制C（beta/regime 漂移，随机置换 perm_p=0.69 坐实）。机制B（出场配对滞后）=口径4的12/12放大器（换 pivot/固定持有出场降 8-10/12）。机制A（递归几何反相真结构）**否证**（若为真则 perm_p<0.05）。原「不可识别性」被反事实部分消解——12 均值符号 + 反事实实验组合能识别主因。综合：奇偶交替=BTC单边趋势(beta) × 级别持有窗相位 × 口径4配对边界的三重投影放大，非可交易方向 alpha。"
    - "正确操作（★2026-07-01 修正）：perm 已否证级别相位的独立可交易性 ⟹ **不建议基于奇偶规律建仓**，除非先加 σhigher regime 门控（更高级别方向门控隔离往返段）+ OOS 重验。原「偶级别只做买奇级别只做卖」仅在门控后 perm_p<0.05 复验通过时成立。负类不能简单反做——X_inv=−X−2C，仅 μ<−2C 反向才正（2C 罚金门槛）。全互斥分类后所有语法状态都定义，但只交易正条件收益状态 χℓ,δ=1[LCB(μℓ,δ)>θ ∧ σhigher 门控通过]。"
    - "短差双向赚的严格条件：往返振幅 Hk−Lk > 入场滞后 + 出场滞后 + 成本，对买卖两边同时成立——震荡/中枢往返 regime 下的条件命题，不成立于强趋势单边结构（BTC 强上涨压制做空腿=数学上成立的机制，须加 σhigher 上级方向过滤或父级容器 regime 变量）。★2026-07-01：perm 结果印证——强趋势市（BTC 全历史涨）只顺大级别一边满足，逆大级别短差亏；缺 σhigher 门控 ⟹ 无法只在往返 regime 下双向做（编排者候选b「强上涨压制做空腿」数学成立）。"
  code_changes: "无（genealogist 纯谱系产出，Read/Grep/Glob/Write 仅碰 .chanlun/genealogy）。反事实实验实装（换出场规则/测滞后/加 σhigher/同容器配对识别机制 A/B/C + 随机置换方法1）= 行动类，parity-analysis 独占 econ_positive（已完成 perm+多口径，commit f212c9323c/b7cc9e9bdc），genealogist 不碰。"
  orchestration_changes: "方法论：①级别索引 ℓ 不是中性——携带相位 (−1)^ℓ，pooling 不同级别的同类买卖点会混合掉交替期望（657 定理1 pooled≤0 的级别相位实例）。写状态须含级别相位分量。②beta/alpha 分解方向由代数结构定：方向签名 PnL 下结构 edge=买卖同号项 (μ++μ−)/2、beta=买卖异号项 (μ+−μ−)/2——引用前须核对签名约定，不凭直觉命名（上一轮报告凭直觉反了）。③观测符号表（12 均值）不能唯一识别生成机制——多机制拟合同一符号，声称「是真结构/几何必然」前须反事实（231 有效域）。★2026-07-01 增补：④反事实的黄金标准=随机置换标签检验——若打乱要检验的标签后统计量不降（perm_p 大），则该标签不携超随机信息，是伪结构。这是 231「否定性结果缩小有效域」的操作化——先做置换再声明可交易性，不预设几何必然。⑤正类交易负类不交易 ≠ 负类反做（X_inv=−X−2C 有 2C 罚金门槛）。"

impact:
  affected_modules:
    - "rust/src/theta_v0/backtest/econ_positive.rs → parity-analysis 独占。反事实实验已在 tests/econ_oddeven_diagnosis.rs 加：oddeven_causation_counterfactual（随机置换方法1 + 多出场口径 + counterfactual_helpers_l1）。识别奇偶交替主因=机制C。下一步（加 σhigher regime 门控后重跑 perm）待 parity-analysis。genealogist 不碰。"
    - "上一轮 parity 报告（parity-alternation-question-20260701.md）→ beta/alpha 命名 split 更正：任何引用其命名的下游结论须重标（(μ++μ−)/2 是结构 edge 非 beta，(μ+−μ−)/2 是 beta 非结构）。观测事实 12/12 + 三候选机制方向保留。★2026-07-01：oddeven-causation-counterfactual-20260701.md 是本号增量①的证伪源——引用「级别相位=可交易 alpha」的任何下游须撤（perm 已否证）。"
    - "状态定义（657 的 z 升级）→ z=(ℓ,δ) 须扩展含级别相位 (−1)^ℓ 分量 + σhigher + exit rule + 确认滞后 + 父级容器状态。parity-analysis 桶键须按 (level,δ) 分（665 已指出 (level,δ) 是先验给定桶键，对选择偏差部分免疫）。★2026-07-01：相位分量保留在 z 中（它携带 beta 投影信息，须建模以剥离），但不作独立 entry 特征——它是 regime 代理的一部分（657 ρ），非 alpha 信号。"
  affected_definitions:
    - "657（生成态）：本号是其 refinement——657「须纳 regime 代理 ρ」（泛指），本号指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替精确形式 sign(δ(−1)^ℓ)。★2026-07-01 深化：perm 证伪把 657「分类≠收益充分统计量」推到最强——μℓ,δ 的**符号本身**（不只是显著性）就是 ρ（beta/regime）的确定性函数，Z（级别语法分类）不携方向收益信息。这是 657 的最深实证。维持生成态。"
    - "663（生成态）：本号印证 + 机制化。663 说真判据 μ>0，本号奇偶交替是 663 pooled 混合均值的机制化。★2026-07-01 深化：663 判据是「统计显著性≠可交易性」；perm 检验证明奇偶交替连 μ̂>0 都是 beta 投影非结构——winner's curse 从「可能」升为「已坐实」（置换实证）。μ̂ 正类不可作独立 entry 升基座。这比 663 原判据更深（不只是「显著但不够」，而是「μ 符号=beta 确定性投影」）。维持生成态。"
    - "664（生成态）：同簇。664 对象错配（触发段≠交易腿），本号级别相位/命名是判据表达维。奇偶交替的机制B（出场配对滞后）与 664 的出场腿对象错配相关——★2026-07-01 反事实确认机制B=口径4放大器（下一反向出场配对边界），与 664 的 Ab_rev post-signal 交易腿 re-spec 同一诊断域。维持生成态。"
    - "665（生成态）：同簇。665 方向不对称免疫——(level,δ) 是缠论先验给定桶键，对选择偏差部分免疫。本号级别相位 (−1)^ℓ 正是 (level,δ) 桶键的相位结构。★2026-07-01：perm 检验是 665「先验桶键比 OOS 更抗偏差」的延伸——即便桶键先验给定，桶内 μ̂ 符号仍可能是 beta 投影（perm_p=0.69），先验桶键抗选择偏差 ⊬ 桶内信号是结构 alpha。665/663 簇的 winner's curse 置换实证锚点。维持生成态。"
    - "231（settled）：本号增量③不可识别性=231 实例。★2026-07-01：反事实实证产否定性结果（perm_p=0.69），兑现 231「否定性结果比确认更有价值——缩小有效域边界」。级别相位有效域从「可交易 alpha 候选」缩到「beta 投影」。perm 检验本身=L2（BTC 单标的），待 L3 + codex 异质否定。印证强化。维持 settled。"
    - "656（生成态）：656 定理2 V(Z)≥V(Y) 细分类不劣。本号级别相位是「细分类」的一个维度。★2026-07-01：perm 证伪不推翻 656——细分类不劣（含相位的 z 信息量 ≥ 不含），但盈利需 μ>0（656 已立），perm 表明含相位的细分类里 μ 符号是 beta 投影非结构 alpha ⟹ 细分不劣但该细分维不带来独立可交易 edge。印证 656「细分不劣 ⊬ 盈利」。维持生成态。"
  downstream_implications:
    - "★级别相位是否「缠论正确形式（可交易结构）」= **已否证**（机制A，perm_p=0.69）。当前 L2（BTC 单标的）。是否在其他 regime/标的复活须：加 σhigher regime 门控后重跑 perm（门控内 perm_p<0.05 则往返 regime 复活机制A）+ L3 跨标的（震荡型标的可能翻案）+ codex 异质否定。不预设结果（231 铁律）。「相位携带信息 / z 须升维」保留（perm 未否证这一层）。"
    - "beta/alpha 命名更正须广播到所有引用上一轮 parity 报告的产出——结构 edge=(μ++μ−)/2、beta=(μ+−μ−)/2。用旧命名做的 alpha/beta 归因结论须重算。★2026-07-01：perm 结果进一步——整个奇偶交替归于 beta（不只是 (μ+−μ−)/2 项），任何把奇偶交替当 alpha 的归因须撤。"
    - "★μ̂ 正类不可作独立 entry 升基座（2026-07-01）：663「6/12类μ̂>0」是 beta 漂移在 (level,δ) 网格的确定性投影，非 6 个可交易级别方向信号。要交易奇偶必须先加 σhigher regime 门控隔离往返段 + OOS 重验。这是 663/665 簇 winner's curse 的置换实证兑现——从「怀疑」升为「坐实」。"
    - "状态升级 z=(ℓ,δ,(−1)^ℓ,σhigher,exit rule,确认滞后,父级容器状态)——parity-analysis 的桶键/回测状态须含这些分量，否则 pooled 混合掉交替（657 定理1）。σhigher 上级方向是短差双向赚的关键过滤（震荡容器双向赚 vs 趋势容器单边赚）。★2026-07-01：相位分量作 regime 代理建模（剥离 beta），非独立 alpha entry 特征。"
    - "短差双向赚是条件命题（震荡 regime）非全局定理——BTC 强上涨压制做空腿（Lk+1>Hk 或跌幅不足⟹X−<0）数学上成立，须加 σhigher/父级容器 regime 变量区分震荡 vs 趋势容器。★2026-07-01 perm 印证——强趋势市只顺大级别一边满足，缺 σhigher 门控无法双向做。这印证 memory project_regime_is_level_truncation_artifact（regime=级别截断伪影）——震荡/趋势容器 regime 本身可由级别结构内生（657 tension_check_regime_dual：R=f(Z)），而 perm 证明 μ̂ 符号=R 的确定性投影，坐实 R=f(Z) 的强形式。"

related_records:
  parent: "657（分类≠充分统计量，须纳 ρ）——本号 refinement：指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替精确形式。657 未捕获三增量（相位分量/命名/三机制），本号补。★2026-07-01：perm 证伪把 657 深化——μℓ,δ 符号本身=ρ 的确定性函数。"
  children: []
  related:
    - "657（生成态）：parent，本号补其 addendum_C 未捕获的三增量（级别相位分量/beta-alpha 命名/三机制不可识别性）。★perm 证伪深化 657 至最强形式（μ 符号=ρ 确定性投影）。"
    - "663（生成态）：印证 + 机制化 + ★perm 深化——663「显著≠可交易」升级为「μ̂ 符号=beta 确定性投影」，winner's curse 从怀疑升为坐实（置换实证）。"
    - "664（生成态）：同簇——机制B（出场配对滞后=口径4放大器）与 664 出场腿对象错配同诊断域（反事实确认）。"
    - "665（生成态）：同簇——级别相位 (−1)^ℓ 是 (level,δ) 先验桶键的相位结构；★perm 延伸 665——先验桶键抗选择偏差 ⊬ 桶内信号是结构 alpha。"
    - "656（生成态）：级别相位是「细分类」维，656 定理2 V(Z)≥V(Y) 细分不劣印证；★perm 印证 656「细分不劣 ⊬ 盈利」。"
    - "231（settled）：不可识别性 + 相位是否真结构须 L2+ 反事实=231 有效域实例；★perm 否定性结果兑现 231「否定性结果缩小有效域」。印证。"
    - "645（grandparent）：判据/对象错配簇根（π^cov≠π^bsp）——本号是簇的级别相位/命名维新成员。"
    - "660（生成态）：χ_t L3 inconclusive——奇偶交替的级别相位若被纳入状态，可能重组 660 的 pooled inconclusive。★perm 表明相位=beta 投影，纳入后作 regime 代理非 alpha。"
    - "alpha.pdf（μ(z,a)>0 + 不测而测）：正确操作来源——★perm 证伪后按级别相位选择方向须先加 σhigher 门控 + OOS 重验。"
    - "memory project_regime_is_level_truncation_artifact：震荡/趋势容器 regime 可由级别结构内生（657 R=f(Z)）——★perm 坐实 R=f(Z) 强形式（μ̂ 符号=R 确定性投影）。"
    - "memory project_recursive_t_architecture_v2：递归 T 每级别一实例——级别相位 (−1)^ℓ 是递归 T 每升一级操作相位翻转的候选形式化（机制A 几何反相）。★perm 否证机制A ⟹ 该相位翻转在 BTC 单标的域是 beta 投影非几何真结构。"
    - "上一轮 parity 报告（parity-alternation-question-20260701.md）：beta/alpha 命名反了，本号 split 更正。"
    - "★oddeven-causation-counterfactual-20260701.md（review-results/）：本号增量①可交易 alpha 断言的证伪源（perm_p=0.69 + 多出场口径），refine 降级依据。"

# 认识论等级标注（formalization-validity-domain 231号，强制）
epistemological_levels:
  - proposition: "级别 ℓ 携带相位信息 (−1)^ℓ，同一买卖点语法在不同级别是不同经济状态"
    level: "L2（BTC 全历史 461万 bar 12/12 观测——单标的单时段，可否证但未交叉验证。是否『缠论正确形式』须多标的/多窗+反事实=L3）"
    increment: "高：级别相位结构变量的识别（657「纳 ρ」的分量指认）——★2026-07-01 保留（perm 未否证「相位携带信息」这一层，只否证「相位=可交易 alpha」）"
  - proposition: "★级别相位 (−1)^ℓ 是可交易 alpha 候选 / 几何真结构（机制A）"
    level: "L2（BTC 全历史随机置换方向标签反事实，perm_p=0.6915≫0.05，S_obs=1050.80<S_perm=1154.83）→ **否证**"
    increment: "★负（否定性结果，231 高价值）：机制A 否证，级别相位降级为 beta 漂移在 (level,δ) 网格的确定性投影（机制C）。缩小有效域边界。待 L3 跨标的（震荡型可能翻案）+ codex 异质否定。"
  - proposition: "★奇偶交替主因=机制C（beta/regime 漂移），机制B=口径4放大器"
    level: "L2（随机置换 perm_p=0.69 坐实机制C；多出场口径 12/12→8-10/12 识别机制B 放大器；BTC 单标的）"
    increment: "★高（否定性识别）：原不可识别性（增量③）被反事实部分消解——12 符号 + 反事实组合识别出主因。待 L3 + codex。"
  - proposition: "方向签名 PnL 下 (μ++μ−)/2≈结构 edge、(μ+−μ−)/2≈beta（上一轮报告命名反了）"
    level: "L0（代数：设 bℓ 漂移 + aℓ 结构边际，方向签名收益分解直接推出归属；PDF §7 严格）"
    increment: "高：命名归属更正（防下游 alpha/beta 误用）——归属方向由代数结构定（结构=买卖同号项，beta=买卖异号项）。★perm 进一步：整个奇偶交替归于 beta。"
  - proposition: "正确操作=偶级别只做买奇级别只做卖（若 μℓ,δ>0⟺δ=(−1)^ℓ OOS 稳健）"
    level: "L2（★2026-07-01 修正：perm 已否证独立可交易性 ⟹ 不建议建仓，除非先加 σhigher regime 门控 + OOS 重验；665 已示 level0 卖 §11 OOS 否证）"
    increment: "★负→条件正：原 oracle 策略被 perm 证伪（in-sample 12/12 ⊬ 可交易，方向标签不携超漂移信息）；仅在 σhigher 门控内 perm_p<0.05 复验通过时成立。"
---

# 666 ★级别相位 (−1)^ℓ 作为结构状态变量 + beta/alpha 命名纠正

## 一句话结论

GPT《奇偶相位》PDF（12页）回应蜂群全历史 BTC **12/12 级别×方向 μ̂ 奇偶交替**（买 L0+L1−L2+L3−L4+L5−，
卖反相，sign μℓ,δ=sign(δ(−1)^ℓ)，正期望 δ*ℓ=(−1)^ℓ 偶买奇卖）。三个增量（657 addendum_C 未捕获）：
**①级别相位 (−1)^ℓ 是结构状态变量**——级别不是中性索引，同买点语法在不同级别是不同经济状态，状态须升级；
**②beta/alpha 命名纠正**——方向签名 PnL 下 (μ++μ−)/2=结构 edge、(μ+−μ−)/2=beta，上一轮报告反了；
**③不可识别性**——机制 A/B/C 都产生同样交替，须反事实识别主因。

**★2026-07-01 证伪更新**：反事实已执行——随机置换方向标签 perm_p=0.69 **证伪增量①的「可交易 alpha」断言**
（机制A 否证，主因=机制C beta 漂移），不可识别性部分消解。级别相位降级为「beta 漂移在 (level,δ) 网格的
确定性投影」。观测事实 12/12 + z 须升维 + 命名纠正保留。详见「证伪层」小节。

## 证伪层（2026-07-01）★

**证伪源**：`.chanlun/review-results/oddeven-causation-counterfactual-20260701.md`（L2，BTC 全历史 461万 bar，
commit f212c9323c/b7cc9e9bdc，方法来源 ChatGPT parity-phase-consult 方法1）。

### 决定性反事实：随机置换方向标签（ChatGPT 方法1）

保持每条信号 entry/出场/持有期/成本 + 买卖总数不变，只 shuffle δ 标签（200次固定种子 Fisher-Yates）。
统计量 S=Σ_class|μ̂|（真标签下方向携带的结构信息量）：

| 量 | 值 | 含义 |
|----|----|------|
| S_obs（真标签） | 1050.80 | 真方向标签下的结构信息量 |
| S_perm 均值 | 1154.83 | 打乱 δ 后**反而更大** |
| perm_p(#perm≥obs) | **0.6915** | ≫0.05 |

**判定（否定性，决定性）**：若机制A（几何真结构）成立，打乱 δ 应破坏结构 ⟹ S_obs 显著大于 S_perm ⟹ perm_p<0.05。
实际 perm_p≈0.69 且 S_obs<S_perm ⟹ **真方向标签携带的结构信息不超过随机置换，甚至略小** ⟹ 奇偶交替是
**beta/regime 漂移伪影（机制C）**，非几何真结构（机制A 否证）。

### 辅助反事实：多出场口径（识别机制 B）

| 口径 | 描述 | 奇偶匹配 |
|------|------|---------|
| 口径4 | 下一反向信号（现口径，确认bar价） | **12/12** |
| 口径5 | 终点 pivot（无确认滞后） | 8/12 |
| 口径6 H500/H1000/H2000 | 固定持有 | 8/9/10 /12 |

12/12 完美性仅在口径4出现，换口径降 8-10/12 ⟹ 机制B（口径4 配对边界+确认滞后叠加）是**放大器**，非纯口径产物
（口径5/6 仍保留 8-10/12 部分交替）。

### 三机制最终判定

| 机制 | 判定 | 证据 |
|------|------|------|
| A 几何反相真结构 | **否证** | 随机置换 perm_p=0.69 |
| B 出场口径产物 | **部分**（口径4放大器） | 12/12 仅口径4，换口径降 8-10/12 |
| C regime/beta 漂移 | **主因** | 置换检验坐实 |

### refine 降级（topo_effect_falsification）

级别相位 **从「alpha 候选/结构变量」降级为「beta 漂移在 (level,δ) 网格的确定性投影」**。
- **保留**：级别携带相位信息、同买点语法不同级别不同经济状态、z 须升维、657「纳 ρ」相位分量指认、命名纠正（增量②）、观测事实 12/12。
- **否证**：级别相位=可交易 alpha 候选 / 几何真结构（机制A）。μ̂ 正类不可作独立 entry 升基座（winner's curse 置换坐实）。
- **为何 refine 非 split**：split 保留一个「携带违反记录但仍成立」的分支；此处可交易性断言是被**直接否证**（perm_p=0.69），
  但增量①骨架全保留 ⟹ 替换叶子标签（alpha → beta 投影），非分裂节点。scope=local（只缩增量①有效域，不推翻结构指认）。

### 认识论 + 异质审查缺口

perm 检验 = **L2**（BTC 单标的全历史）。**待 codex 异质否定**——codex 因 429 配额耗尽曾缺席
（project_heterosource_openai_quota_exhausted），现 CLI 已通，perm 结论未经异质否定，须补 codex diagnose。
翻案条件（边界）：L3 跨标的若震荡型标的 perm_p<0.05 则机制A 在某 regime 成立（当前 BTC 单边域否证）；
加 σhigher regime 门控后门控内 perm_p<0.05 则往返 regime 下方向携结构信息。

## 三增量 vs 657 覆盖对照（为何新建 666 非并入 657）

| 增量 | 657 addendum_C 是否覆盖 | 本号增量 |
|------|------------------------|---------|
| PDF 定理1（分类≠充分统计量，须纳 ρ） | ✓ 已覆盖（657+663） | — |
| PDF 定理2（V_{≤L}≥V₀） | ✓ 已覆盖（656） | — |
| **级别相位 (−1)^ℓ 分量指认** | ✗ 657 只泛指「纳 ρ」，未指认相位分量 | 指认 ρ ⊇ {(−1)^ℓ,...} + 交替精确形式（★perm 定性为 beta 投影） |
| **beta/alpha 命名纠正** | ✗ 657 无 | (μ++μ−)/2=结构、(μ+−μ−)/2=beta |
| **三机制不可识别性 A/B/C** | ✗ 657 无 | 反事实识别清单（★已执行，主因=C，A 否证） |

657 是 20 页版《推导完全分类》PDF 的两定理；本 PDF 是 12 页版，聚焦奇偶交替，含三个 657 未捕获的精确增量。
新建 666 非重复造（657 已吸收的两定理不重写，只补三增量）。

## beta/alpha 命名纠正（split）

方向签名 PnL 下（PDF §7）：设 E[ΔP|buy,ℓ]=bℓ+aℓ、E[ΔP|sell,ℓ]=bℓ−aℓ（bℓ=市场漂移，aℓ=结构边际），
C+≈C−=C：

| 量 | ≈ | 是什么 | 上一轮报告 | 更正 |
|----|---|-------|-----------|------|
| (μ++μ−)/2 | aℓ−C | 方向中性**结构 edge**（买卖同号项） | 误标 beta | **结构** |
| (μ+−μ−)/2 | bℓ | 市场漂移 **beta**（买卖异号项） | 误标结构 | **beta** |

归属方向由代数结构定（结构=买卖两边都因结构正确受益的同号项；beta=市场漂移的异号项），独立于可比性假设。
严格剥离 beta 须随机置换方向标签/回归控制 holding return/同容器配对（PDF §8）。
**★2026-07-01：随机置换已执行——perm_p=0.69 表明整个奇偶交替（不只是 (μ+−μ−)/2 项）都归于 beta 漂移。**

## 不可识别性（三机制）→ ★已识别主因

| 机制 | 形式 | 产生交替的原因 | ★2026-07-01 判定 |
|------|------|--------------|-----------------|
| A 递归几何反相 | ϕℓ=(−1)^ℓ，μℓ,+≈Aℓϕℓ | 每升一级操作相位翻转（几何必然） | **否证**（perm_p=0.69） |
| B 出场配对滞后 | τℓ^out 落 ℓ+1 级反相区间 | 持有到下一反向信号的出场口径产物 | **部分**（口径4放大器） |
| C 级别=regime | P(R|ℓ) 随级别变，pooled Σr μℓ,δ,r P(r|ℓ,δ) | 级别对应不同收益 regime | **主因**（置换坐实） |

原「参数数目 >12 都能拟合同一符号表 ⟹ 不可识别」被反事实**部分消解**——12 符号 + 反事实实验组合识别出主因=C。

## 正确操作（★2026-07-01 修正）

原「偶级别只做买、奇级别只做卖」被 perm 证伪独立可交易性 ⟹ **不建议基于奇偶规律建仓**，除非先加 σhigher
regime 门控（更高级别方向门控隔离往返段）+ OOS 重验。仅在门控内 perm_p<0.05 复验通过时，χℓ,δ=1[δ=(−1)^ℓ
∧ LCB(μℓ,δ)>θ ∧ σhigher 门控通过] 成立。负类不能简单反做——X_inv=−X−2C，仅 μ<−2C 反向才正（2C 罚金门槛）。

**665 教训 + perm 坐实**：in-sample 12/12 交替 ⊬ OOS 稳健（665 已示 level0 卖 §11 OOS 否证），且 perm_p=0.69
证明连 in-sample 的方向信息都不超随机——winner's curse 从「怀疑」升为「置换坐实」。

## genealogist 边界

记录三增量（级别相位分量/命名纠正 split/三机制不可识别性）+ 657 refinement + ★perm 证伪降级（refine）。
**不自结算**——级别相位「是缠论正确形式（可交易结构）」已被 perm 否证（机制A），但「携带相位信息/z 须升维」保留；
命名纠正是事实更正；最终结算待编排者 /ritual + codex 异质否定。genealogist 不碰代码、不裁、不改判据文件。

## 张力检查（019d/020）

### 检查范围（同簇 645/663/664/665 ∪ 1-hop ∪ Hub）
- 同簇：663（判据）/664（对象）/665（方法论）/645（簇根）——本号居「级别相位/命名表达」维。
- 1-hop：657（parent）/663/231/656/660。
- Hub：231（有效域）/645（簇根）/657（regime 双记录张力节点）。

### 张力1（编排者点名）：级别相位「同买点在不同级别是不同经济状态」vs 缠论「买卖点定义级别无关」
**真张力（表面对立），但可分层 ⟹ 不触发中断 #1、不 escalate。** 两者是同一对象（买卖点）的**不同层**：
- **定义层（缠论）**：买卖点一/二/三类在每个级别定义相同——一类买点=中枢背驰后回抽不破前低，级别无关同义。
  这是**语法定义**（什么算买点），级别无关成立。
- **经济层（PDF）**：同一语法买点在不同级别的**条件期望 μ 符号交替**——同买点语法在不同级别是不同经济状态。
  这是**期望归属**（这个买点在此级别赚不赚），级别相关。
- **弥合点**：定义层「同义」⊥ 经济层「不同期望」——是不同对象（语法 vs 期望）。缠论从不声称「同类买点在
  所有级别期望相同」，只声称「定义相同」。PDF 不否定定义层同义，只指出经济期望层级别相关（657 定理1：
  分类完全 ≠ 收益充分统计量——语法完全分类 ⊥ 收益期望）。**可分层 ⟹ 无不可分层矛盾 ⟹ 不触发中断 #1。**
  记录价值：明确「买卖点级别无关」是定义断言，不外推到「期望级别无关」（否则=657 定理1 违反=pooled 混合）。
  **★2026-07-01：perm 进一步——经济层「不同期望」本身是 beta 投影（不同级别持有窗对 BTC 单边趋势的不同投影），
  非缠论买卖点语法赋予的结构差异。仍可分层（定义层同义 + 经济层 beta 投影，都不与缠论定义冲突）。**

### 张力2：vs 657（parent）——refinement 非冲突（★perm 深化）
657 说「须纳 regime 代理 ρ」（泛指）。本号指认 ρ 的具体分量（级别相位 (−1)^ℓ）+ 交替精确形式。
是 657 的分量细化，非否定。**★2026-07-01：perm 证伪深化 657——μℓ,δ 的符号本身就是 ρ（beta/regime）的
确定性函数，Z（级别语法分类）不携方向收益信息。这是 657「分类≠收益充分统计量」的最强实证**（不只是显著性不够，
连符号都是 ρ 的确定性投影）。深化非冲突，可分层，无矛盾。

### 张力3：vs 663——印证机制化（★perm 深化）
663 真判据 μ>0。本号奇偶交替=663 pooled 混合均值的级别相位机制化。**★2026-07-01：663「显著性≠可交易性」升级为
「μ̂ 符号=beta 确定性投影」——winner's curse 从怀疑升为置换坐实。** μ̂ 正类不可作独立 entry 升基座。这比 663
原判据更深（不只是「显著但不够可交易」，而是「μ 符号本身=beta 漂移的确定性函数」）。印证强化，无矛盾。

### 张力4：命名纠正 vs 上一轮 parity 报告——expansion split
上一轮报告命名与其自身方向签名分解依据不一致（规定者违反自身规定=expansion）。split：保留观测事实
12/12 + 三候选机制方向，否定 beta/alpha 命名归属。可分层（观测 ⊥ 命名），无中断 #1。

### 张力5（★2026-07-01 新增）：本号增量① 自身可交易 alpha 断言 vs perm 证伪——refine（自我修正）
本号增量①原声「级别相位=可交易 alpha 候选」，但其自身 231 有效域约束要求「须反事实识别主因」。反事实一做
（perm_p=0.69），机制A 即否证 ⟹ 本号增量①的乐观断言违反其自身 231 约束（expansion 第二层：规定者违反自身）。
refine 解决：保留增量①骨架（相位携带信息/z 升维/分量指认），否证叶子（可交易 alpha → beta 投影）。
**可分层（结构指认 ⊥ 经济性质），无不可分层矛盾 ⟹ 不触发中断 #1。** 这是 no-patch（防乐观结论固化）的兑现——
不删 666，不留错误断言，直接 refine 降级。

### 概念分离信号检测（中断 #1）
- 级别相位 vs 缠论级别无关：可分层（定义层 vs 经济期望层，不同对象）⟹ 不触发。
- 命名纠正：观测 ⊥ 命名归属，可分层 ⟹ 不触发。
- ★可交易 alpha 证伪：结构指认 ⊥ 经济性质，refine 可分层 ⟹ 不触发。
- **无不可分层定义矛盾 ⟹ 不触发中断 #1。** 本号 bias-correction（相位 refinement + 命名更正 + ★perm 降级 refine），最终结算属编排者 /ritual + codex 异质否定。

### 递归运动结构完成检测（020）
- 第0层：本号写入（级别相位分量 + 命名 split + 三机制不可识别性）。
- 第1层：本号 × 657 碰撞 → ρ 的级别相位分量指认（净新发现高：657 泛指「纳 ρ」被细化为精确相位形式 (−1)^ℓ）。
- 第2层：本号 × 663/664/665 簇碰撞 → 级别相位维新成员 + 机制B 与 664 出场腿同诊断域（净新发现中：簇从判据/对象/方法论扩到相位表达维）。
- ★第2.5层（2026-07-01）：本号 × perm 反事实碰撞 → 增量① 降级 + 663/665 winner's curse 从怀疑升为坐实（净新发现中高：置换实证锚点确立，但方向是收缩=否证）。
- 第3层：本号 × 231/656 碰撞 → 不可识别性=有效域实例 / 相位是细分类维（净新发现降：印证已立规则，perm 否定性结果兑现 231）。
- 涉及范围：scope₁(657 ρ 分量细化=定理级) > scope₂(663/664/665 簇维 + perm 坐实) > scope₃(231/656 印证)=顶分型。
- **背驰 ∧ 分型 ⟹ 递归运动结构性完成。** 反事实实验=行动类（parity-analysis 独占 econ_positive，perm+多口径已完成，σhigher 门控重跑待做）；
  最终结算待编排者 /ritual + codex 异质否定。**不触发新 /escalate**（无定义冲突，张力1/5 均可分层）。

## 回溯扫描（职责3）

- **657（生成态）**：本号 refinement——补其 addendum_C 未捕获的三增量。★perm 证伪深化 657 至最强形式（μ 符号=ρ 确定性投影）。维持生成态。
- **663/664/665（生成态）**：本号同簇新成员（级别相位/命名维），印证不否定。★perm 坐实 663/665 winner's curse（从怀疑升为置换实证）。维持生成态。
- **656（生成态）**：级别相位=细分类维，印证 656 定理2。★perm 印证 656「细分不劣 ⊬ 盈利」。维持生成态。
- **231（settled）**：不可识别性 + 相位真结构须 L2+ 反事实=231 实例。★perm 否定性结果兑现 231「否定性结果缩小有效域」。印证。维持 settled。
- **上一轮 parity 报告**：topo_effect split——beta/alpha 命名归属更正，观测事实 + 机制候选保留。
- **★本号增量①（2026-07-01）**：topo_effect_falsification refine——可交易 alpha 断言经 perm 证伪降级为 beta 投影，结构指认骨架保留。
- **无 settled 被本号回溯破坏。** 本号 bias-correction（相位 refinement + 命名更正 + ★perm 降级），反事实实验=行动类（parity-analysis），最终结算待编排者 /ritual + codex 异质否定。
