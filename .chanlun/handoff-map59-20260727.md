# 交接：map #59 分层线 + #421 接线线（2026-07-27）

上一会话上下文耗尽移交。**本文件不是决策记录**——决策都在 GitHub 票上，本文件只说「接手的人第一步做什么」。

## 你接手时的现场

### 立刻可取的活（frontier）

| 票 | 类型 | 状态 | 第一步 |
|---|---|---|---|
| **#451** | task | **解阻塞，可取** | 守卫改按角色豁免 + 恢复绿。裁定见 #449 §2 |
| **#416** | grilling | 主问已答，残余待裁 | 只剩两问，见下 |
| **#421** | SPEC 实装 | **有别的工位在跑** | 别碰，先确认那个工位还活着 |

### 有一件悬着的机械动作

**#449 还开着**，等一个后台 codex 核验回来。

- 命令产物：`/private/tmp/claude-501/-Users-silencehan-Projects-NewChanlun/14a4bbdf-6284-4ae9-9a0f-f57f82b9089e/tasks/bckvhhrwx.output`
- 提示词副本：`/tmp/codex-lookahead.txt`
- **文件可能已被系统清掉**。清掉了就照 `/tmp/codex-lookahead.txt` 重跑一次 `codex exec -s read-only`，或直接自己读码验。
- 它验的是什么：**#449 新理由 B（未来函数防线）是否成立**。四点 = 证书在哪构建 / 构建是否受 as-of 截断 / 有无具体的未来信息回流路径 / `nest_index.rs`+`turn_class.rs` 本身有没有该路径。
- **验完把结论追加到 #449 然后关票。** 查实为真则理由 A+B 并列；**查实为假则撤下 B，禁令由理由 A 单独承载——禁令死活不受影响**（#449 §1 已写明这一点，不用再问编排者）。

## #449 裁定了什么（一句话版）

**判据层禁引 nest 产物这条禁令：维持，但理由整个换掉。**

旧理由「两套实现互为验证」被 #450 实测打掉——不是两套是三套，且交集为空。新理由 = **对照实现的独立性**（有实测支撑）+ **未来函数防线**（待验）。

两个违规文件 `nest_index.rs` / `turn_class.rs` **按角色豁免**——查证它们不是判据、是 nest 管线部件，只是文件放错目录。守卫改成认模块声明不认文件名。

**条款 9 的批准缺口同时封上**：本裁定即为它的正式批复（修订版），只批这一条，DRAFT 文书其余不动。

**已登记的弱点**：角色自述可骗，模块自称 nest 就能逃。物理搬家是唯一自执行的做法，**否于成本未估、不是原理**——出现第二例逃逸就重开这一案。

## #416 只剩两问

主问（只读观测还是硬门）**已由 #449 答毕**：

> 禁令维持 ⟹ 只能读候选层 ⟹ 候选层三类点 99.42% 未经下沉确认 ⟹ **天然见证不到背驰** ⟹ 只能当只读观测器；「必要条件」这个自我定性须重写；可见性不放开。

残余：
1. 买卖点生命周期含不含 D7 firstRetrace
2. **树面分歧**——`interval_necessity.rs` / 守卫 / 两个违规件**全部仅存在于 main**，与工作面分支 76 ahead / 117 behind、文件集差 22。动手前先定在哪棵树上做。

## #421 双工位冲突已裁

**留正在跑的那个工位**（在 #421 上直接实装，已推进 TDD 第 3 切片，6 个文件未提交）。#426 已关（范围吸收非取消），#427/#454/#429 的阻塞全部改挂 #421。

**收口时必须补齐两件**（已写在 #421 上）：
1. 逐条对照 #426/#427 的验收清单自查留痕——**特别是影子评审 #428 从「观测面」升格为硬性验收的那条**（力度不可验时身份滞留：#425 验收条款无例外，实装侧自划的例外未经授权，#409 实测 0.00% 只说明当前不发生、不构成规格豁免）
2. 走 `/code-review` 两轴，新上下文、禁自评

**接手前先确认那个工位还活着**（查 `/tmp/kimi-nest-mainline` 的未提交改动 mtime）。死了的话它那 6 个文件的未提交改动要么接手要么丢弃，别默认丢。

## #450 留下三件没出票的（图上是雾）

按影响面排序：

1. **level0 免检的错误前提**（最大）——注释说「无次级别、递归底」是假的，`sub_moves` 是构造时写死为空，笔级别全链引擎 `rust/src/bi_zhongshu_bsp.rs` 完整存在只是没接进塔。三档 = 接笔引擎进塔（**84.11% 的信号会重新过门**）/ 保留免检但订正理由（零信号变更）/ 维持现状。**是判据变更，改信号集，须编排者裁。**
2. **三套 Cand^δ 怎么收敛**——`NestCandidateEvent` / `judge_first` / `div_cand` 两两不等值，**无形式化定义可裁**，得先有定义。探针建议先改口径声明、挂 #257，**不动判据**（补趋势门会让那条通道 748→0）。
3. **三条注释订正**（最轻，零信号变更，随时可做）：level0 免检的理由、`div_cand` 的「含盘整背驰」（它既不定位中枢也不调 `judge_pan_div`）、`CONTEXT.md:61`「确认层实装已在识别侧」对三类为假（`judge_third` 只判几何、零背驰调用）。

报告全文：`chanlun/review-results/issue450-bypass-census-20260727.md`

**#450 的越读限制**：只测 BTC 1m 单 HEAD 快照，其余 7 品种未测；「交集为 0」是 `lvl-1` 级 + 同 `source_index` 严格口径，换「最低下沉级」或「±k bar 容差」变体未测、结果可能非 0。引用这些数字时别丢掉这一段。

## 两条本轮踩出来的教训

1. **派工前先查有没有工位在做它的父票。** #426 就是这么撞车的——我没查，两个工位做同一件事，一个白跑。
2. **凡「注入反例后测试仍绿」，必须先验证反例真落了盘。** 影子评审 #428 遇到一次全绿，`diff` 一查发现编辑根本没写进文件。不验这一步会把「没测到」误判成「断言恒真」。
