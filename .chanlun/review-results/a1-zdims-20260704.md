# A1 残余 Jchain + d 维——PDF 逐式核对结果包（Task #162）

工位：ws-a1dims | 任务 #162（A1-zdims-residual）| 日期：2026-07-04
方法：**逐页 Read 原始 PDF**（`docs/formal-chain/`），二手报告（shard4/gap-master2-final）只作导航。
HEAD：fe582c3847（#175 后，MuClass 15 维含 eta_bucket）。

## 核心结论（premise-overturning）

**A1 两条残余（Jchain / d）在与权威 PDF 逐式核对后 both 消解——都不是「缺失的 z 维」，无 z 装配实装可做。** 强行加维 = 声明膨胀（090号）/自造（编排者令「勿自造」）。详见下。

- **Jchain**：权威 z（`完整的策略.pdf` §6/§16）**含** Jchain，但代码 `mu_estimator.rs:136-138` 已论证其 = (origin_level, level) 代数派生、无独立自由度，且 §3 N^δ 定义强制逐级相邻（不可跳级）**证实**该论证。恒等式护栏 `origin_level = level + nest_depth` 已在 `selector.rs:250-251` 机器可检。**已覆盖，非缺口。**
- **d（失效/止损距离）**：权威最完整形态 z（`完整的策略.pdf` §16，20 维）**不含 d**。d 只出现在**另一份** PDF（`缠论的全互斥定义策略.pdf` §九/§十）的**示意性**（带「…」）Z_t 中，且该 PDF §十把 d 与 β/c/m 并列归为**非拓扑度量**、属**风险投影层**（K_Θ/止损/sizing）而非 μ 分类状态 z。d 已在风险层生产+消费（`risk.rs` structural_stop + `|entry-stop|`）。**不是 z 维缺口，已在正确的层实装。**

**报告错源**：shard4 读的是 `缠论的全互斥定义策略.pdf`（其 z 列了 d），未与 `完整的策略.pdf` §16 权威最完整形态交叉核对（后者省 d、把 d 的同族 β/c/m 收入 z、把 d 归风险层）。gap-master2-final A1「Jchain+d 残余」继承了该错源。

## PDF 页码/定义 ↔ 代码行 映射表（编排者强制）

| PDF 出处（页） | 原文定义 | 权威性 | 对应代码行 | 判定 |
|---|---|---|---|---|
| `完整的策略.pdf` §3（p2-3） | 区间套 `N^δ_{ℓ,e}=Cand^δ_ℓ ∧ [J^δ_{ℓ-1}⊆J^δ_ℓ] ∧ N^δ_{ℓ-1↓e}`；`J^δ_e⊆J^δ_{e+1}⊆…⊆J^δ_ℓ`；注：「区间包含非端点相等…不能作跨级 rung」 | 唯一权威（INDEX.md 裁定） | `nest.rs:140-173`（NestRung/NestCertificate，rungs 高→低逐级），`nest.rs:200-218`（n_delta 递归，每级 -1） | Jchain **级别结构**={e,…,ℓ} 由递归强制逐级相邻，结构上不可表达跳级 |
| `完整的策略.pdf` §6（p4） | canonical z=`(ℓ,e,δ,I_γ,Ndepth,CandType,ForceState,Jchain,σ_higher,σ_p,role,posState,shortDiff,H,TStage,ηBucket,RiskMode,CostBucket,MarginState,ExitType)` | 唯一权威 | `mu_estimator.rs:90-167`（MuClass 15 维 + Jchain 代数派生注 136-138） | z **含 Jchain**、**不含 d** |
| `完整的策略.pdf` §16（p10） | 「最完整、最严格」z_v = 同 §6 20 维清单（Jchain 在、d 不在） | 唯一权威（最终形态） | 同上 | 权威最完整形态**明确排除 d** |
| `mu_estimator.rs:136-138` 论证 | 「Jchain 由 N^δ 逐级相邻，链签名 ≅ (ℓ,e)，无独立自由度，由 origin_level+level 完整携带」 | — | `selector.rs:250-251` debug_assert `origin_level = level + nest_depth`（Jchain 恒等式机器可检）；`selector.rs:547-566` 单测 | 论证**被 §3 PDF 证实**；护栏已在 |
| `缠论的全互斥定义策略.pdf` §九（p22） | `Z_t=(ℓ,δ,I_γ,r,σ_p,ω,κ,β,d,c,m,…)`（**带「…」示意性，非穷举**） | 二级（非唯一权威；本 PDF 与权威 §16 z 形态不同） | 无（不装配进 canonical z） | d 在此**示意**出现 |
| `缠论的全互斥定义策略.pdf` §十（p22-23） | 纯拓扑态 `{ℓ,δ,I_γ,r,σ_p,κ}` **vs** 非拓扑度量 `β_t=背驰强度, d_t=失效/止损距离, c_t=成本, m_t=保证金`；「把『背驰力度』『风险距离』『成本门槛』这些操作语义严格量化」 | 二级 | `risk.rs:74-105`（structural_stop：1/2 买=pivot_low、3 买=ZG、卖镜像），`risk.rs:194-198`（`risk_dist_usd=|entry-stop|·tick_size`），`runner.rs:517-553`（gate 消费 structural_stop） | d = 非拓扑度量，属**风险投影层**，**已生产+消费**；非 μ 分类 z 维 |

## 为什么 d 不入 μ 分类 z（三条独立理由）

1. **权威排除**：`完整的策略.pdf` §16 最完整形态 20 维显式列出 β(ForceState)/c(CostBucket)/m(MarginState)，**独缺 d**——同族四度量收三排一，是权威作者的刻意选择，不是渲染遗漏。
2. **层归属**：d 的语义消费点是风险可行集 K_Θ(x_t)（§11「必须包含…止损…」）与 sizing（`|entry-stop|` 每手最大亏损）——风险投影层，非分类状态。`缠论的全互斥定义策略.pdf` §十亲口把 d 归「非拓扑度量」。
3. **分桶灾难**：d=|entry−structural_stop| 含**当前价格尺度**（连续量）。入 μ 桶键 ⟹ 按连续风险距离碎片化分桶 ⟹ K 爆炸 = §29-30 winner's curse（`mu_estimator.rs:218-234` UClass 降维本就为抗此）。d 的止损**规则**已由 (I_γ, level pivot) 决定，无独立分类信息。

## 结果包六要素

1. **结论**：A1 两残余 both 消解。Jchain=已覆盖（代数派生 + §3 证实 + 恒等式护栏 selector.rs:251 机器可检）；d=不在权威 canonical z、属风险层已实装（risk.rs structural_stop + |entry-stop|）。**零 z 装配代码改动**——加维即声明膨胀/自造，被 090号/编排者令禁止。
2. **定义依据**：`完整的策略.pdf` §3（p2-3 N^δ 逐级相邻）、§6（p4 canonical z 含 Jchain 不含 d）、§16（p10 最完整形态排除 d）；`缠论的全互斥定义策略.pdf` §九（p22 示意 z 带「…」）、§十（p22-23 d=非拓扑度量归风险层）。INDEX.md 裁定 `完整的策略.pdf` 为形式化唯一权威。
3. **边界条件**：本结论**翻转**当 (a) 编排者裁定 μ 应升级为「风险感知 μ」需把 d 离散化入桶（则须新预注册 + 抗碎片化设计，非本任务范围），或 (b) 认定 `缠论的全互斥定义策略.pdf` 示意 z（含 d）覆盖 `完整的策略.pdf` §16（与 INDEX.md 权威裁定冲突，须走 change request）。Jchain 结论翻转当：nest.rs 出现跳级 rung（当前结构不可表达）。
4. **下游推论**：gap-master2-final 的 A1「Jchain+d 残余」条目**错源须订正**（shard4 混淆两 PDF 的不同 z 形态）。#162→#165(A7) 解锁链依赖 #162 处置——因本发现翻转任务前提（no-workaround 触发），**不自行标 completed / 不解锁 #165**，交 Lead/编排者裁定。
5. **谱系引用**：090号（声明膨胀禁止）、231号（诚实 None 不伪造）、no-workaround（规格-实现根本不一致须上浮）、no-patch-mentality（勿在错前提上自造实装）；#138 G3（Jchain 代数派生首次登记）、#149/#175（z 装配模板）。建议 genealogist 立条：**「两 PDF z 形态差异的权威裁定——完整的策略 §16 为准，d 归风险层」**。
6. **影响声明**：新增本文件，**零代码改动**。对 gap-master2-final A1 提订正建议（不直接改，交汇编/裁定）。不动 MuClass/ZExt/selector/perm_test/wverify/econ_positive（B22 三处同批改**不触发**——无新维入桶）。

## 编译隔离说明

主工作树当前**不可编译**（`src/trading/positional_fusion.rs:1625` 缺 `entry_certificate`/`generation`——ws-a9pos2 接力半成品，非本任务域）。本任务**零代码改动** ⟹ 无需隔离 worktree 验证：无新维、无新测试、theta_v0 域未触碰。若 Lead 裁定需实装（边界条件 a/b），届时按 ws-etab 先例（HEAD+纯本任务 patch 隔离）验证。
