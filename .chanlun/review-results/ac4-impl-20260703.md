# ac4-impl（Task #145）结果包：A/C 次级别走势类型化 + 盘整背驰承接路由 + 高级别方向

工位：ws-ac4b（接棒 ws-ac4，前任死机遗留 9 文件未 commit 中间态 + /tmp 漏斗产物）。
commit：`d215ca14b5`（主实装，9 文件 +940/-104）+ `a5463baae0`（codex-r2 修复）+ 文档批次。goal g-type1-canonical-centers 修复序第 4 步。

## 1. 结论

三件工作全部落地，`cargo test --release --lib` **1460 全绿**（基线 1444 + 新增 16 测试，含 codex-r2 两测试）：

**① Q5——A/C 从单一线段升级为次级别走势区间（含 codex 前审两条致命修复）**
- `locate_departure_move_a`（原 locate_trend_seg_a）：A = 离开 C_prev 的整个次级别走势区间
  I(A)=[λ_A,ρ_A]（多段含中间反向段 bar）。前任版本"同向段首尾桥接"被 codex 判致命（#1）：
  无 reentry 边界，把「失败离开→回中枢→重新离开」两个独立 episode 桥接为一。本轮修复：
  episode 边界 = 窗口内**最后一个回中枢段**（反向段端点回到核心 [zd,zg] 内侧，与 judge_pan_div
  `reenters` 同一判据），A = 当前 episode。
- λ_C（I(C) 起点）：前任 `c_start_cache` 按 c_idx 缓存"中枢后首个同向段"同样无 reentry 检测
  （codex #2 致命）。修复：共享原语 `episode_start_in` + `departure_move_c_start`，λ_C 按
  (c, dir, seg.start) 逐段计算（episode 语义下 λ_C 依赖 seg 前回中枢段集合，不可按 c_idx 缓存；
  成本 O(窗口)，与 judge_pan_div 同成本类）。
- **单一来源闭 fork**（codex #4）：生产 extract 循环 / type1_funnel_dx 探针 / judge_first_orig
  oracle / judge_pan_div 四处全走同一 helper——codex 自给的翻转条件（"共享 helper ⟹ 坐标
  fork 风险自然消除"）达成，区间级 parity 断言按其边界条件不再必要。
- C 侧单段兼容独立测试锁定（codex #3）：`q5_c_side_single_segment_bit_compatible` +
  `departure_move_c_start_reentry_bounds_episode`。

**② Q4——盘整背驰承接路由（前任已实装，本轮核对后续用 + 补审计）**
- `judge_pan_div`（PanDivCert 唯一构造点）：中枢按 ownership 落 Consolidation 块
  （`center_block_kind`，与趋势门同一 decompose 单一来源）⟹ 同一中枢两次同向离开（A 破核心 +
  回中枢段存在 + C 破核心）+ C<A 背驰 ⟹ 产证书。**零 six-bit，不产 BspPoint**（不冒充 B1/S1）。
- 管道：`LevelState.pan_div` 与 cached_bsp 同 key memo（hit/miss/cascade 三态锁步）。
- econ 承接：`pan_div_gate_pass` 二通道（Nest=`descend_type1_anchor_depth` 次级别 Type1 锚 /
  XZD=`xiaozhuanda_confirm().gate_pass()`），任一通过 ⟹ RawSignal（trigger=
  `NestTrigger::PanDivConsolidation` 独立归因桶，四桶 partition 不变量断言更新）；两门皆闭 ⟹
  诚实丢弃。dx 镜像 bit-exact 复制。本轮改动：judge_pan_div 的 λ_C 改走共享 helper（消平行实现）。
- BTC 牛市窗（2020-10-01..2021-04-15）实测证书在流：pan_div L0=79 / L1=17 / L2=1（A 侧 episode 化后终值；episode 化前 45/13/1）。

**③ Q7——高级别单元方向 = 次级别走势类型方向（本轮新实装）**
- `project_to_units{,_resume}` 方向源：`center_own_dir_at(blocks, i)`（ownership Trend(d) 块 ⟹ d；
  `center_trend_gate` 的逐点二分版，逐点等价性测试锁定，与趋势门同一 decompose 单一来源）。
- Consolidation 块 / i==0 ⟹ 非方向性 ⟹ **endpoint 比较降级 fallback**（`fold_direction` 文档
  显式标注降级；盘整离开腿方向消解归小转大/区间套语境，econ 层 XZD/Nest 通道）。
- C_0 有意 None（≠ `center_block_kind` 的"C_0 归 B₁"）：ownership 的 C_0 规则需要 R(0,1)
  （未来信息），因果投影层不可消费——偏差已文档化。
- 增量前缀稳定性：confirmed 单元方向只依赖 R(i-1,i)（两端中枢 sealed 后标签冻结，decompose
  模块头不变量）；frontier 单元由既有 A3 §2.5 `truncate(prefix_count)` 每 bar 重投影覆盖；
  debug_assert 全量投影对拍护栏保留。1460 测试含增量 vs 全量对拍全绿。

## 2. 新旧信号数对照（验收明文要求）

基线 = #144 完成时（gate3-impl-20260703.md §4：**全历史一类 678** = buy1 275 + sell1 403，
五级分布 397/217/56/8/0；该文档 §边界条件已预告"678 不是冻结判据值，#145 会诚实改变计数"）。

| 口径 | L0 | L1 | L2 | L3 | L4 | 合计 |
|------|----|----|----|----|----|------|
| 旧（#144，单段 A/C + endpoint 方向） | 397 | 217 | 56 | 8 | 0 | **678** |
| 新（#145，区间 A/C + episode 定界 + Q7 方向） | 575 (b244/s331) | 349 (b147/s202) | 102 (b34/s68) | 31 (b14/s17) | 0 | **1057** (b439/s618) |

变化归因：Q5 区间化双向改判背驰（I(A) 变宽 ⟹ A 面积增 ⟹ 判背驰变多；I(C) 变宽 ⟹ 反向）；
Q7 使 L≥1 单元方向从 endpoint 占位改为走势类型方向 ⟹ 上级趋势门/判据链大幅重排
（L1 217→349、L2 56→102、L3 8→31——上级增量主要来自 Q7）。
2021 顶区（2020-10-01..2021-04-15）sell1 簇：L0×34 / L1×27 / L2×17，含 2021-02-20/21 与
2021-04-14（BTC 实际顶部前夜）；牛市窗 buy1 仅 2（方向合理性正验证）。
漏斗 parity（675号，探针=生产）：环6 = buy1+sell1 逐级相等（36/27/17 窗口、575/349/102/31 全历史）。

## 3. 定义依据

- Q5：`docs/formal-chain/一类买卖点.pdf` Q5 裁决——"A/C 应为次级别走势类型，不应默认是单一
  线段；多段 departure 时力度对比覆盖整个走势区间 I(A)/I(C)，Area=Σ|hist| over 区间"。
  episode 定界依据：前审 `.chanlun/review-results/ac4-codex-q5-20260703.md` #1/#2 裁决 +
  judge_pan_div 回中枢段判据（同 diff 内已存在的对称逻辑，前审指出的"不对称实装"补齐）。
- Q4：同 PDF Q4——"盘整背驰不冒充同级 B1/S1 也不消失：PanDiv⟹∃e<ℓ Conf_e 或 PanDiv⟹
  XZD_{ℓ↓e}"；第24课:34-36 盘整背驰 = 同一中枢两次同向离开。
- Q7：同 PDF Q7——"高级别单元方向 = 其代表的次级别走势类型方向；Consolidation 非方向性；
  endpoint 比较降级 fallback 标注"；第31课盘整无方向；decomp-design-20260703.md §终
  （"#145 的 A/C 获得 MoveBlock 方向来源"）。
- 破中枢几何仍用 seg 端点（因果触发）：段是单向对象、终点即极值，多段 move 的 min/max 首次
  越界 ⟺ 某同向段端点越界——等价性注记在 judge_first_cached 函数头。

## 4. 边界条件（结论何时翻转）

1. **episode 判据的严格性**：若后续裁定"回中枢段"应以整段区间（而非端点）回到核心为准，或
   核心应取外缘 [DD,GG] 而非 [zd,zg]，episode 边界移位，A/λ_C 定位随之变。当前取端点+核心口径
   与 judge_pan_div 冻结判据一致（单一来源，改则同改）。
2. **C_0 因果 None**：若编排者裁定投影层可消费 R(0,1)（放弃增量前缀稳定、接受回溯改写成本），
   C_0 应改回 ownership 归 B₁——当前实现选择因果一致性，已在 center_own_dir_at 文档声明。
3. **牛市窗中枢产出差异**：前任 /tmp 遗留跑数（bullwin0415 L4 环6=2）与本轮不可直接对比——
   前任跑数落在 #142-#144 中间态（漏斗探针尚无局部趋势门分环、中枢链非 canonical 终态）。
   本轮以 commit d215ca14b5 全套语义为准。
4. **GOLDEN 未翻转的解释**（验收明文"诚实重算+解释"）：`extract_signals_bit_exact_digest_guard`
   GOLDEN=0x90c7_9ee6_17e1_1392 不变且 PASS——电池五组合成 case 均为单段离开、无 reentry 结构，
   Q5 语义变更在其上退化为 bit 恒等（单段兼容路径），非掩盖。多段/reentry 语义由 5 个专项测试
   回归锁定（q5_multi_segment_departure_a/c、q5_c_side_reentry、locate_departure_move_a_reentry_
   splits_episodes、departure_move_c_start_reentry_bounds_episode）。codex 前审 #6（电池盲区）的
   残余风险由专项测试承接；若后续要求把多段 case 并入 digest 电池，GOLDEN 须随之诚实重算。

## 5. 下游推论

- **#146（rerun5）前提确认**：type1>0 前提在新粒度下依然成立且计数从 678 → 1057（新 prereg
  的 alpha 检验样本量上升约 56%）。全部依赖 type1=0 旧结论的下游可以重跑。
- PanDiv 承接桶（PanDivConsolidation）进入 μ̂ 归因分桶——#146 prereg 应把该桶列为独立通道
  （不并入 Type1TrendDivergence）。
- Q7 改变 L≥1 全部单元方向语义：任何直接消费 `UnitRange.direction` / `RMove::Segment.direction`
  （L≥1）的下游（selector σ_higher、voice、B2 方向链）行为可能移位——本轮 1460 测试含 econ/
  strategy 全绿，但真实数据回测口径（#146）须整体重跑，不可沿用旧冻结数。

## 6. 谱系引用

- 675号（探针不允许坐标 fork）：共享 helper 单一来源是该谱系在 λ_C 上的操作化。
- 090号（严格性）/no-patch-mentality：前任"桥接"实现按 codex 裁决重写为 episode 定界，
  未走"保留旧逻辑加分支"。
- formalization-validity-domain（231号）：本包数字为 L2（BTC 单标的真实数据）；跨标的 L3
  归 #146。
- 前审谱系：`.chanlun/review-results/ac4-codex-q5-20260703.md`（fail 裁决 + 逐条核对）。

## 7. 影响声明

- 改动模块：`classifier/{divergence,signal,decompose,mod,recursive_tower}.rs` +
  `backtest/{econ_positive,l3_delta_r_alpha,l3_pi_probe,runner}.rs`（后四者为 pan_div 载荷适配
  + 承接路由）。
- 行为变化：一类信号计数 678→1057（L2 实测）；L≥1 单元方向语义变更；新增 PanDiv 信号通道
  （既有 Nest/Xzd 信号判定路径零改动，PanDiv 是新增入口）。
- 未触碰：buy1/sell1 冻结力度原语 `AbcDivergence::diverges`（C<A 严格小于，codex 前审 #5
  accept）；破中枢几何判据；三类/二类判据链。
- 未 commit 的非本工位文件：`.chanlun/genealogy/dag.yaml`（684-685 边，他工位）、
  `.claude/*`、`.agents/*`、`analysis/_db_sec_pull_brn.py`——留在工作树。

## 8. codex 异质审计记录（验收明文：每项审计落盘）

前审（前任发起，fail）：`ac4-codex-q5-20260703.md` + 完整 transcript
`codex-review-20260703-213013-6dad.md`。
本轮三路复审/新审 transcript（CLI 自动持久化于 .chanlun/review-results/codex-review-*.md）：

- Q5 复审（episode 定界修复闭合 #1/#2/#3/#4）：见下方裁决摘要。
- Q4 审计（承接路由/证书判据链/memo 锁步/z 口径）：见下方裁决摘要。
- Q7 审计（前缀稳定性/C_0 偏差/fallback 残留）：见下方裁决摘要。

### 8.1 Q5 复审（codex-review-20260703-220357-d65f.md）——verdict: conditional

前审四条全部闭合（concessions 明列 #1/#2/#3）：locate_departure_move_a reentry=accept、
λ_C 修复=accept、episode 判据=accept、旧 fixture 更新=accept、C 单段兼容=accept。
唯一 needs_work：judge_pan_div A 侧未 episode 化（跨 Q4 项）→ **已修**（见 8.2）。

### 8.2 Q4 审计（codex-review-20260703-220428-8c0c.md）——verdict: fail → 三条处置

1. **A 侧 episode 化（重要，reject）**：成立。已修（commit a5463baae0）：I(A)=[λ_A,ρ_A]，
   λ_A 经共享 helper，ρ_A=episode 内末同向段终点；新增 pan_div_a_side_multi_segment_episode_
   interval 测试。实测 pan_div L0 45→79 / L1 13→17（A 变宽 ⟹ 背驰门通过增多，方向符合预期）。
2. **seen_pan 门前写入（重要，needs_work）**：核对后判**非缺陷、是全通道冻结约定**——bsp 通道
   同样先 seen.insert 后评门（econ_positive.rs:316 先例），τin 语义=信号在确认 bar 用当时证据
   评定一次。已按 codex 给出的替代路径（"把时序作为裁决前提写入规格"）在代码注释显式化；
   若未来裁决改为"证据出现即承接"须全通道同改，非 PanDiv 特例。
3. **memo key 失效（建议，needs_work）**：pan_div 依赖集（centers+blocks 单一来源派生）⊆ bsp
   依赖集；同 key 失效纪律继承 bsp memo 既证不变量（confirmed 前缀不可变 + 长度三元组守卫 +
   cascade_reset）。无新增失效面；若 bsp memo 不变量被推翻，两者同倒（锁步设计意图）。

### 8.3 Q7 审计（codex-review-20260703-221031-3801.md）——verdict: fail → 五条处置

1. **fallback 方向泄漏（致命，reject）**：codex 指 Consolidation/C_0 的 endpoint fallback 方向
   无 provenance 流入 extract_first_third_for_level 被当真实走势方向消费。**判定：跨裁决张力，
   上浮（非本工位可自决）**——#121 codex-t1 裁定A（"units 承担线段角色，复用 L0 判据"）授权
   units-as-segments 整体消费方向；Q7 严格读法要求非方向性单元不得锚定方向判定。两裁决对
   consolidation-owned 单元的处置不相容。注意：Q7 前**所有**上级方向都是 endpoint 占位（泄漏
   面=100%），Q7 后残余=盘整块单元——本工位实装收窄了泄漏，未消灭。provenance 可无字段派生
   （fallback ⟺ center_own_dir_at(blocks,i)==None，blocks=LevelState.moves 单一来源），排除
   语义（fallback 单元能否作破段/episode 成员）需裁决。已上浮 main。
2. **release 无 stale-direction 护栏（重要）**：与本仓所有 resume 缓存同纪律（bsp memo/centers
   前缀/07b 门控均为 debug_assert+电池护栏，release 依赖冻结不变量：sealed 关系标签不变）。
   Q7 未新增弱于既有纪律的面；接受为已知代价，不单独加固（加固须全缓存族同改）。
3. **空 blocks 测试掩盖（重要，reject）**：成立。已补 project_to_units_block_dir_overrides_
   endpoint_fallback（块方向覆盖 endpoint + i=0 fallback + 空块对照 + resume 一致，1460 全绿）。
4. **seen_pan 时序**：同 8.2-2（审计跑在 a5463baae0 前的旧上下文）。
5. **A 侧 episode 化**：同 8.2-1，已修。

### 8.4 遗留上浮项（传 main）

Q7-#1 跨裁决张力：#121 裁定A vs Q7 非方向性——盘整块 ownership 单元的 endpoint fallback 方向
在上级一/三类判据中的消费合法性。若裁定"排除"，上级信号计数将再变（#146 prereg 冻结前须决）。
