# Codex decide — 2026-07-02 17:13:08 UTC

## 元数据

- **mode**: decide
- **subject**: 642号：acceptance[2]『定义冲突/选择类』降级为『H2引擎自举工程缺口/定理类』——host注入key错配bug已修复（parent_id容器注入）且3个H2回归测试全通过，该降级判定（连同已解决状态）是否成立？
- **model**: codex-cli
- **timestamp**: 2026-07-02 17:13:08 UTC
- **context-file**: /tmp/codex-resubmit-642.md

## Prompt

## 技术选型请求

642号：acceptance[2]『定义冲突/选择类』降级为『H2引擎自举工程缺口/定理类』——host注入key错配bug已修复（parent_id容器注入）且3个H2回归测试全通过，该降级判定（连同已解决状态）是否成立？

## 上下文

# 642号：acceptance[2] H2引擎自举缺口（非定义冲突）——重裁请求（补全"修复后通过H2"证据）

## 裁决②中codex的原判（驳回）

> 两次独立审计结论一致只能提高可信度，不能逻辑上排除"定义冲突"；缺具体工程缺口定位+修复后通过H2的证据，结论强度不足。

## 本次补全1：具体工程缺口定位（原文已给出，完整重述）

642号原文（2026-06-29写）诊断出的精确bug：

```
coverage.rs:1246  let tree_endpoint_idx = build_tree_endpoint_index(&work[..tree_end]);
                  //                       ↑ keying = (e.level, e.rho)
coverage.rs:1253  if let Some(&host_idx) = tree_endpoint_idx.get(&(c.level, c.source_index)) {
                  //                                              ↑ 候选**同级** key（c.level）
coverage.rs:1262  let next_idx = ancestor_close_by_id(&work, &raw);
                  //  → ancestors_by_id 要求候选**高一级** parent_id（c.level+1）∈ raw
```

诊断：注入的host在候选**同级**（c.level），AncOK要求的父在**高一级**（c.level+1）。同级host入raw不满足高一级父在场要求 ⟹ depth>0子腿的真Compose父仍缺席 ⟹ AncOK剪 ⟹ active depth>0腿=0 ⟹ ΔSharpe=0。

修复方向（642原文提出，当时**未落地**）：把host注入key从同级`(c.level, c.source_index)`改为候选的`parent_id`容器（c.level+1）。

## 本次补全2：修复已落地 + 通过H2的直接证据（642原文当时缺失，现已补齐）

### 修复代码现状（当前`coverage.rs:1655-1701`，与642原文诊断时的代码已不同）

642原文写作时（2026-06-29）标注 `resolution.type: 未解决`，`code_changes: 无`，明确说明修复"待Lead派有Write工位执行"。**本次重裁前核实：该修复已在此后的commit（介于642写作时间点与当前HEAD f2ba46cede之间）中落地**，当前代码为：

```rust
// ★工位 H 修复（级别容器.pdf §13）：开仓激活的位置节点身份 = carrier 容器 hostOf(g)
// （其 ElementId 在候选元素 id 上携带），不是买卖点叶子。故候选自身入 raw 即等价于
// 「激活 carrier 上的位置节点」。子声部的 parent_id（= par_C(carrier)）由
// ancestor_close_by_id 检查是否在 raw（= 持仓父位置节点在 A_t）。
//
// ★删除旧 G host 注入（级别容器.pdf §6/§7 + §13 否定）：旧 G 把 (c.level, c.source_index)
// 命中的同级 host 叶子注入 raw（叶子作持仓 = a_carrier 仍 0），是 PDF §6/§7 反证的
// "开叶子"错形式，且对 depth>0 子无效（注入的是叶子自身非父carrier）。位置节点身份
// 改为 carrier id 后，候选 id=carrier id 入 raw 作位置节点。
//
// ★(I-1) 修正（codex 行级坐实）：候选自身入 raw 只激活本级 carrier 位置节点；
// depth>0 子声部候选的准入还需其父 carrier（= par_C(carrier)，候选 parent_id）在 raw
for c in &buckets.open {
    let idx = candidate_start + c.gamma_index;
    if idx < work.len() && !raw.contains(&idx) {
        raw.push(idx);
    }
    if idx < work.len() {
        if let Some(parent_pid) = work[idx].parent_id {
            let parent_in_raw =
                raw.iter().any(|&r| work.get(r).map(|e| e.id == parent_pid).unwrap_or(false));
            if !parent_in_raw && registry.registry_live(&parent_pid) {
                restore_ancestor_chain_from_registry(&mut work, &mut raw, registry, parent_pid, &id_idx, &mut overlay_seen);
            }
        }
    }
}
```

**修复本质**：不再用`(level, source_index)`同级key做host查找，而是直接用候选元素自带的`parent_id`字段（结构化的高一级Compose父引用），若父不在raw且registry中live，则递归恢复整条祖先链。这精确对应642原文诊断"注入候选parent_id容器而非同级host"的修复方向。

### H2测试通过的直接机器证据（本次新增，原文当时不存在）

`coverage.rs`中存在3个专门针对该bug的回归测试（标注"H2 裁决"/"H2 settled"），本次重裁前实际执行：

```
$ cargo test --lib --release -- \
    theta_v0::strategy::coverage::tests::engine_bootstrap_container_bsp_admits_depth_child_from_empty \
    theta_v0::strategy::coverage::tests::engine_bootstrap_does_not_admit_orphan_shortdiff_without_container_bsp \
    theta_v0::strategy::coverage::tests::cross_bar_held_container_admits_depth_child_next_bar

running 3 tests
test theta_v0::strategy::coverage::tests::engine_bootstrap_does_not_admit_orphan_shortdiff_without_container_bsp ... ok
test theta_v0::strategy::coverage::tests::engine_bootstrap_container_bsp_admits_depth_child_from_empty ... ok
test theta_v0::strategy::coverage::tests::cross_bar_held_container_admits_depth_child_next_bar ... ok

test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 1476 filtered out
```

三个测试分别验证：
1. `engine_bootstrap_container_bsp_admits_depth_child_from_empty`——空prev_active下，容器BSP确认使depth>0子腿准入（H2死循环根因消除的正面断言）。
2. `engine_bootstrap_does_not_admit_orphan_shortdiff_without_container_bsp`——无容器BSP时ShortDiff子腿仍被剪枝（自举不膨胀守卫，639(c)保护，防止修复引入过度放行的回归）。
3. `cross_bar_held_container_admits_depth_child_next_bar`——**跨bar持仓父链测试**（真实生产场景，非同bar共现）：bar1容器BSP开腿→跨bar持有→bar2无容器BSP但有子BSP，验证子腿借跨bar持仓容器准入。

第3个测试的场景描述明确对应642原文诊断的真实数据特征（"L1容器BSP极稀疏，几乎不与L0子BSP同bar共现"），是最贴近生产路径的验证。

## 请求裁决的具体命命题

**命题**：642号诊断的coverage.rs:1253 host注入key错配bug，其修复（host key从同级(c.level,c.source_index)改为候选parent_id容器）已在当前代码落地，且3个专门的H2回归测试（含最贴近生产路径的跨bar持仓父链测试）全部通过，构成"修复后通过H2"的直接机器证据。据此，acceptance[2]与deltasharpe memory的原"定义冲突/选择类"判定应确认降级为"H2引擎自举工程缺口/定理类"（已解决）。

请基于以上完整工程缺口定位 + 修复代码现状 + 3个H2测试全部通过的执行输出裁决：该降级判定（连同"已解决"状态）是否成立？

## 如实说明（若裁决仍需更多证据）

若上述H2测试通过的证据仍不足以支持"完全解决"（例如codex认为还需要真实市场数据下的ΔSharpe重测，而非仅结构性单元测试），请明确指出：单元测试级别的AncOK准入验证（本次提供）与真实数据ΔSharpe非零验证（642原文"待L2坐实，不预设结果——231铁律：修复≠盈利，trades≠alpha"）是两个不同强度的"通过"标准，本次重裁仅确认前者（工程缺口已修复，机制正确），不代为确认后者（尚未做真实数据ΔSharpe重测，231有效域规则下不预设该结果）。


## 要求

请做出明确的技术选择。

输出格式：
- **决策**：[你的选择]
- **推理链**：[为什么选这个]
- **替代方案**：[被拒绝的方案及理由]
- **边界条件**：[何时应推翻此决策]
- **风险**：[可能的问题]


## Response

**决策**：成立。642号应从 `定义冲突/选择类` 降级为 `H2引擎自举工程缺口/定理类`，并标记为“机制级已解决”。

限定语：这个“已解决”只覆盖 H2/AncOK 准入机制，即 parent/container 注入路径已修复并有结构性回归测试通过；不等价于真实市场数据下 `ΔSharpe` 已非零或盈利已成立。

**推理链**：旧问题不是两套定义冲突，而是实现没有忠实承载既有定义。AncOK 要求 depth>0 子声部的高一级 Compose 父在 `raw` 中，但旧 host 注入用同级 `(c.level, c.source_index)`，注入的是 leaf/同级 host，不是父 carrier，因此子腿被剪是实现错配。

当前代码已经改成候选 carrier 自身入 `raw`，再按 `work[idx].parent_id` 检查父 carrier；若父不在 `raw` 且 registry live，则调用 `restore_ancestor_chain_from_registry` 恢复父链，最后仍交给 `ancestor_close_by_id` 正常裁剪。见 [coverage.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1655)。

你给出的 3 个 H2 回归测试覆盖了正例、孤儿负例、跨 bar 持仓父链。当前源码里还多了一个更直接的 registry-live 父注入测试 `open_candidate_parent_injected_from_registry_admits_depth_child`，覆盖“父不在 prev_active、仅 registry-live”的真实缺口路径，见 [coverage.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:3179)。这足以支持“工程缺口已修复，机制正确”。

**替代方案**：维持 `定义冲突/选择类`，不成立。现在没有需要二选一的定义；正确语义是 carrier/parent_id 结构链，代码是在补足它。

把它提升为“真实数据 alpha 已解决”，也不成立。结构性单测证明 AncOK 准入机制修复，不证明 `ΔSharpe`、成交质量或收益改善。

**边界条件**：若目标 commit 的 3 个 H2 测试实际失败，或后续代码重新引入同级 leaf-host raw 注入，应推翻。若真实 L2/L3 数据显示 registry-live 父链恢复造成孤儿放行、重复 ElementId、 stale 父复活，也应重开。若 acceptance[2] 的通过标准明确包含真实数据 `ΔSharpe != 0/positive`，则只能说 H2 机制已解决，不能说 acceptance 全量通过。

**风险**：当前实现仍用 carrier id 作为 position node 简化版；同一 carrier 多次 entry 的实例区分可能不足。registry liveness 是关键依赖，若 live 状态污染，会把错误父链恢复进 `raw`。另外，本轮我只读核对了当前源码；测试通过证据按你提供的执行输出采信，未在只读环境中重跑 `cargo test`。
