# Codex decide — 2026-07-02 19:38:53 UTC

## 元数据

- **mode**: decide
- **subject**: 小转大通道 C3 判据死门根因已确定为两条中枢归属选择链结构性不重合（judge_third leave段时间归属 vs last_zs s跨度空间过滤，same_center_any=0/84 但同侧因果窗口内Type3存在率98.81%）。请在(a) C3改用same_side_causal_ok弱化判据重新参门 / (b) C3永久保持诊断字段+门=C2-only终态 / (c) 回溯第43课原文重新考古小转大确认语义(先形成更大中枢再看突破方向)再设计 三个候选中做出全权终局裁定，并给出精确实装规格。
- **model**: codex-cli
- **timestamp**: 2026-07-02 19:38:53 UTC
- **context-file**: /tmp/codex-c3-caliber-ctx.md

## Prompt

## 技术选型请求

小转大通道 C3 判据死门根因已确定为两条中枢归属选择链结构性不重合（judge_third leave段时间归属 vs last_zs s跨度空间过滤，same_center_any=0/84 但同侧因果窗口内Type3存在率98.81%）。请在(a) C3改用same_side_causal_ok弱化判据重新参门 / (b) C3永久保持诊断字段+门=C2-only终态 / (c) 回溯第43课原文重新考古小转大确认语义(先形成更大中枢再看突破方向)再设计 三个候选中做出全权终局裁定，并给出精确实装规格。

## 上下文

# 背景：小转大通道 C3 判据死门根因已确定，需裁定重设计方向

## 系统位置
`rust/src/theta_v0/backtest/econ_positive.rs` 的小转大确认通道（Xzd）。信号域 = Type2/3
且本级 `descend_type1_anchor_depth==None`（本级无背驰段可套，区间套通道不适用）。

判据链：C1（本级无背驰锚，域判定）∧ C2（本级二类买卖点存在）∧ C3（本级走势最后一个次级中枢
出现三类买卖点，思维导图定理128：**必要非充分条件**）。当前 `gate_pass()` 已退化为仅 C2（前序
codex 终局裁定，见下方"已完成的前序裁定"），C3 降为诊断字段不参门。本次要裁定的是：**C3 该不该
以及如何重新设计参门**，而不是继续维持"C3=废弃诊断字段"的现状。

## 已完成的前序裁定（不可推翻的既定事实，仅供背景）
`.chanlun/review-results/xzd2-impl-codex-audit-20260702.md` §5.1：level>=2 是 100% 结构性死门
（`LevelState.bsp` 在 level>=1 从不含 Type1/Type3，代码级确定性结论，与本次裁定无关，**本次裁定
范围限定在 level==1 子集**）。level==1 无法单凭静态代码判死，需诊断探针，探针已跑（见下）。

## #41 探针 L2 实测结果（350K bar 真实 BTC，`.chanlun/review-results/xzd-c2only-degradation-20260702.md` §2.3）

lvl==1 子集（routed=84，即小转大域 level==1 的全部信号）：

| 诊断字段 | 命中数 | 命中率 | 含义 |
|---|---|---|---|
| last_zs_exists | 84 | 100.00% | s 跨度内几乎总存在次级中枢 |
| same_side_l0_type3_any | 83 | 98.81% | 同向 L0 Type3 点几乎总存在 |
| same_side_causal_ok | 83 | 98.81% | 且几乎总在因果窗口内（source_index<=confirm_index），**几乎与上一行完全重合** |
| same_center_any | 0 | 0.00% | 但这些 Type3 点的 center **从未**等于 last_zs |
| same_side_same_center（=当前 C3 真实判据） | 0 | 0.00% | 当前 C3 硬判据命中率 |

**探针结论（本次要裁定的核心矛盾）**：不是时间确认问题（Type3 点在确认时刻已可见，98.81%），
是纯粹的 **center 归属问题**——两条"最后中枢/归属中枢"选择链在 BTC 1m 几何下结构性不重合：

- `judge_third`（唯一构造 Type3 BspPoint 的函数）的 `center` 参数 = `nearest_confirmed_center_idx`，
  按 **leave 段时间点** 反查"当下最近已确认中枢"。
- `last_zs`（当前 C3 查询目标，`xzd_c3_diag`/`xzd_sub_last_zs_type3` 内部实现）=
  `sub_centers.iter().filter(|c| c.start_index >= s.start_index && c.end_index <= s.end_index).max_by_key(|c| c.end_index)`，
  按 `s`（本级走势）的 **空间跨度过滤** 后取 end_index 最大者。

两者一个按"leave 段发生时最近"取，一个按"s 全跨度内被完全包含 + end_index 最大"取，选择原则不同，
在 84/84 的实测窗口里从未重合。

## 现有 C3 相关代码（`econ_positive.rs`，已实装，供参考）

```rust
// xzd_sub_last_zs_type3（当前 C3 硬判据实现，:893-909）
fn xzd_sub_last_zs_type3(s: &LeveledMove, sub_centers: &[Center], sub_bsp: &[BspPoint], side: Side) -> bool {
    let Some(last_zs) = sub_centers
        .iter()
        .filter(|c| c.start_index >= s.start_index && c.end_index <= s.end_index)
        .max_by_key(|c| c.end_index)
    else {
        return false;
    };
    sub_bsp.iter().any(|q| {
        (match side { Side::Long => q.bits.buy3, Side::Short => q.bits.sell3 })
            && q.center.map_or(false, |c| c.start_index == last_zs.start_index && c.end_index == last_zs.end_index)
    })
}

// xzd_c3_diag 新增的诊断分项（:920-953，same_side_causal_ok 是本轮新增，用于区分"时间问题"vs"归属问题"）
// same_center_any: 是否有任意侧 Type3 其 center == last_zs（不分方向，仍是 center 精确匹配）
// same_side_causal_ok: 同向 Type3 候选中是否存在 source_index<=confirm_index 者（不检查 center 匹配）
```

```rust
// XzdEvidence::gate_pass()（当前已退化为仅C2，:855-857）
pub(super) fn gate_pass(&self) -> bool {
    self.type2_confirmed
}
```

C2 判据（跨条目查找同级二类买卖点，:876-884，正确性已被前序 codex 审计确认，非本次讨论范围）：
```rust
fn xzd_type2_confirmed(bsp_of_level: &[BspPoint], source_index: usize, side: Side) -> bool {
    bsp_of_level.iter().any(|q| {
        q.source_index == source_index
            && match side { Side::Long => q.bits.buy2, Side::Short => q.bits.sell2 }
    })
}
```

## 缠论原文依据（一级权威——缠师原始博文，`docs/chanlun/text/blog/`）

### 第43课（本次候选(c)指定重新考古的原文，逐字节选）

> `043:18`「转折必然由背驰导致，但背驰导致的转折并不一定是同一级别的……缠中说禅背驰-转折定理：
> 某级别趋势的背驰将导致该趋势最后一个中枢的级别扩展、该级别更大级别的盘整或该级别以上级别的
> 反趋势。」

> `043:34`（第43课"二、背驰级别小于当下的走势级别"——这正是小转大的定义性描述）「这种情况下，
> 是走势已经明显没有相应级别的背驰，例如，一个30分钟的走势类型，明确显示没有出现30分钟的背驰，
> 也就是背驰段最终不成立，但却出现一个1分钟级别的背驰，用一个最简单的形式向上的a+A+b+B+c，A、B
> 是30分钟中枢，在c中出现1分钟背驰，而c对b在30分钟级别并没有出现背驰，这时候并不必然保证c的1
> 分钟转折的最终走势就一定不跌回B里，但即使这个回跌出现，其形式也和第一种情况不同。**这第二种
> 情况，必然要先形成一个比1分钟级别要大的中枢，然后向下突破，最终形成回跌到B中的走势。**」

> `043:38`「有人可能要问……这区别太大了，在第一种情况中，其回跌是必然的，而第二种情况，在形成
> 一个比1分钟大的中枢后，**并不必然回跌，可以往上突破**，使得a+A+b+B+c继续延伸。」

> `043:194`（问答，39课流程细化）「这程序很清楚，一般都按盘整背驰来，但有一种特殊情况是不出现
> 盘整背驰的，就是小级别变大级别的情况，这种情况，**如果不破前期高点，那只是一个新中枢，所以
> 当然持有，如果破了，那就一定要走**，除非出现强力不背驰创新高的情况。」

> `043:198`（注）「后面的句子就是考虑了小级别背驰导致大级别转折的情况。属于段间没有盘背小转大
> 下的转折，跌破Ai+K高点」

**第43课的确认机制核心**：小转大的确认不是"匹配到某个特定中枢"，而是**"c段背驰后，先长出一个
比小级别更大的中枢（这个中枢是新形成的、事后才能确认的），然后看这个新中枢是向上突破（延伸，
小转大不成立/信号失效）还是向下突破（转折，小转大确认成立）"**。中枢的形成本身是动态过程
（"先形成"），突破方向才是判据，不是"这个新中枢的 center 必须等于某个预先按规则选定的 last_zs"。

### 其他已考古原文（`xiaozhuanda-design-20260702.md` §1，本次一并提供避免重复检索）

> `053:28`「在小级别转大级别的情况下，第二类买卖点就是最佳的，因为在这种情况下，没有该级别的
> 第一类买卖点。」（C2 权威依据，已实装且非本次讨论范围）

> `044:52`（娇注实盘四点）「1 本级别或者次级别中枢反向突破；2 次级别中枢出现3买卖；3 本级别3买
> 转2卖，3卖转2买；4 调整或者反抽的位置超过正常最后个次级别走势的位置到达第二个中枢GGDD——形成
> 同分3买卖的高低点从而扩展大中枢前走势类型结束。」

> `044:56`（娇注）「这个该级别走势最后个次级别中枢是动态的，不一定为小级别背驰点前的中枢。」
> ——当前代码 `last_zs` 的"s 跨度内 end_index 最大"取法正是试图实现"动态最后中枢"，但取的是
> **空间跨度过滤**，与 `judge_third` 的 **leave 段时间归属** 是两条不同规则。

> 思维导图定理`128`「小转大（小背驰-大转折）定理：小级别背驰引发大级别反向的**必要条件**——该
> 级别走势的最后一个次级别中枢出现第三类买卖点。」——"必要条件"措辞，非充分条件，前序 codex
> 审查（§6-3）已指出原设计稿表述偏强。

> `066:198`「一般来说，小转大都有一个小平台，等小平台确认向上再介入更好，因为小转大的平台，是
> 可以往下突破的。」——"平台可以往下突破"=信号可失败，事后确认语义。

> `039:34`（注，39课同级分解流程）「没有盘背就等同级别分解的段组成中枢后(小转大现象）**中枢前
> 后的段比较盘背或者不创新高新低进行买卖操作**。」——与43课"新中枢+突破方向"同构：先看段组成
> 中枢，再看中枢前后段的盘背/新高新低关系，不是 center 精确匹配某个预选中枢。

## 三个候选处置方案（待裁）

**(a) C3 改用 `same_side_causal_ok`（同侧+因果窗口存在性）参门重新实装**
- 具体：`gate_pass()` 从 `type2_confirmed` 改为 `type2_confirmed && same_side_causal_ok`（或类似
  弱化版本——放弃 center 精确匹配，只要求"同向 Type3 点在因果窗口内存在"）。
- 后果：命中率 98.81%（83/84），对当前 C2-only 114 条几乎不收紧（level==1 子集 84 条里的 83 条
  仍过，另外 level>=2 30 条本就走 C2-only 不受影响，全域约 83+30=113/114）。
- 语义风险：`same_side_causal_ok` 完全丢弃了"最后一个次级中枢"这个中枢归属约束，蜕化为"存在同向
  Type3 点"——这是否还忠于"次级别中枢三类点确认"的原文语义（需要判断丢弃 center 匹配是否是语义
  空心化，还是原文本就不要求 center 精确匹配到某个预选中枢）。

**(b) C3 永久保持诊断字段，门=C2-only 终态**
- 具体：维持现状（`gate_pass()`=仅C2），`sub_last_zs_type3`/四项诊断字段永久只做报告标注，不再
  尝试参门重设计。
- 后果：吞吐维持 114/114（全部由 C2 决定）。语义风险：完全放弃 C3 代表的"次级别结构确认"这一
  原文强调的信息增量（`044:52` 四点实盘注意点全部弃用），C2 本身"近似自证"（前序裁定 §5.2 已指出）
  ——即小转大信号的"确认"实质上退化为"存在"，用户结果包 §边界条件已承诺"若命中率显著>0需重新评估"，
  本候选=主动放弃该承诺后续路径。

**(c) 回溯第43课原文重新考古小转大确认语义再设计**
- 具体：按上方第43课原文重新设计 C3——判据不再是"匹配预选 last_zs 的 center"，而是"c段背驰后
  是否新形成一个更大级别中枢，且该中枢被向下突破（对多头信号镜像为向上突破）"。这与`044:52`点1
  "中枢反向突破"、`039:34`注"中枢前后段比较盘背或不创新高新低"是同一机制的不同表述。
- 成本：需要新的判据函数（"新中枢+突破方向"而非"center 匹配"），工作量介于(a)(b)之间但大于两者；
  需要重新定义"突破"在代码里的可编码形式（如"离开该中枢的段是否创新低/新高"或"该中枢的 ZG/ZD
  是否被跌破/升破"）。与 #13（W-VERIFY alpha 全量重测）的时点关系：若裁(c)，需在 #13 之前完成
  实装+L2 复测，会推迟 #13 起跑；若裁(a)，改动小可与 #13 并行或先行完成不阻塞；若裁(b)，零改动
  不阻塞 #13。

## 请裁定

1. 三选一（或提出经过论证的第四种方案，但必须给出与(a)/(b)/(c)同等严格度的判据设计+原文依据）。
2. 若裁 (a) 或 (c)：给出重新实装的精确规格（判据函数签名/输入输出/与现有 `XzdEvidence`/
   `xzd_c3_diag` 的关系），供后续实装工位直接施工，不留模糊地带。
3. 明确本裁定是否阻塞 #13（W-VERIFY alpha 全量重测），给出理由。
4. 本裁定是全权终局裁定，不再上浮编排者，请给出确定性结论，不要停留在"需要更多数据"这类拖延式
  表述——如果证据不足以支持某个选项，明确指出并选择证据支持最充分的选项。


## 要求

请做出明确的技术选择。

输出格式：
- **决策**：[你的选择]
- **推理链**：[为什么选这个]
- **替代方案**：[被拒绝的方案及理由]
- **边界条件**：[何时应推翻此决策]
- **风险**：[可能的问题]


## Response

**决策**：选 **(c)**。C3 重新设计为 `level==1` 专用的“背驰后新中枢 + 反向突破”硬门；`level>=2` 维持既定 C2-only，不纳入本次翻案。

精确实装规格：

```rust
struct XzdC3BreakoutDiag {
    new_center_exists: bool,
    new_center_breakout_ok: bool,
}

fn xzd_c3_new_center_breakout(
    source_index: usize,
    confirm_index: usize,
    side: Side,
    sub_centers: &[Center],
    sub_moves: &[LeveledMove],
) -> XzdC3BreakoutDiag
```

判据：

```rust
// 新中枢：小转大 source 之后、confirm 之前已确认的次级中枢
let z = sub_centers.iter()
    .filter(|c| c.start_index >= source_index && c.end_index <= confirm_index);

// 突破：该中枢之后、confirm 之前的次级走势突破核心区间
Side::Long  => m.rmove.hi() > z.zg, // 多头确认：向上突破
Side::Short => m.rmove.lo() < z.zd, // 空头确认：向下突破
```

`XzdEvidence` 增加：

```rust
pub c3_new_center_exists: bool,
pub c3_new_center_breakout_ok: bool,
```

`gate_pass()` 改为：

```rust
pub(super) fn gate_pass(&self) -> bool {
    self.type2_confirmed && (self.level != 1 || self.c3_new_center_breakout_ok)
}
```

旧字段 `sub_last_zs_type3` / `same_side_*` 全部保留为诊断，不再参门。`xiaozhuanda_confirm` 需额外接收 `sub_moves: &[LeveledMove]`；调用侧从 `tower[lvl - 1]` 传入，只读 slice，不引入缓存、索引结构或新抽象。补一个最小单测：post-source center 存在且 Long 上破 ZG 为 true；center 存在但未突破为 false。

**推理链**：第43课语义不是“Type3 的 `center` 必须等于某条预选 last_zs”，而是“小级别背驰后先形成更大中枢，再看突破方向”。现有 C3 死门的根因正是两条 center 归属链不重合；继续修 center 精确匹配是在修实现伪影，不是在贴近原文。

`same_side_causal_ok` 只证明“确认时可见同向 Type3”，不能证明“最后/新形成中枢被突破”。83/84 的高命中率反而说明它几乎不收紧 C2，是诊断字段，不是门。

**替代方案**：(a) 拒绝。它把 C3 空心化成“同侧 Type3 存在”，丢掉中枢对象和突破方向，代码简单但概念错。

(b) 拒绝。C2-only 可作为临时基线，但永久化等于放弃第43课和044/039对“中枢形成后突破”的结构确认，且会让“确认”退化成“存在”。

**边界条件**：若 L2 复测显示 post-source 新中枢在模型中根本不可表达，应先修 center/tower 输出，而不是退回 (a)。若正式形式化规格以后把 C3 明确定义成另一种 center identity 规则，再按规格替换。若性能采样证明线性扫描成热点，再用已排序 `end_index` 做 `partition_point`，不要预先建索引。

**风险**：会减少或延迟 `level==1` 小转大信号，#13 的 alpha 数字会和 C2-only 不可比。`source_index` 边界有 off-by-one 风险，所以必须用最小单测钉住 `start_index >= source_index`。ZG/ZD 是最小结构突破口径；若后续要 GG/DD，那是 C4，不要混进本次 C3。

本裁定 **阻塞 #13 的最终 W-VERIFY alpha 验收**。可以跑 C2-only 作为基线，但不能把它当最终小转大语义的验收结果。
