# Codex decide — 2026-07-02 20:14:50 UTC

## 元数据

- **mode**: decide
- **subject**: C3 新判据（codex #44 裁定(c)）level==1 子集真实数据命中 0/84（source_index 之后无任何 sub_centers 满足过滤条件）。已排查排除级别方向读反(候选4)与坐标错位(678模式)。请在(1)修center/tower输出使新中枢可表达/(2)维持定义level==1通道等价关闭/(3)判据放宽为中枢延伸而非新确认/(5)先加诊断探针区分算法限制vs真实几何事实 中做出全权终局裁定
- **model**: codex-cli
- **timestamp**: 2026-07-02 20:14:50 UTC
- **context-file**: /tmp/codex-c3-zerohit-ctx.md

## Prompt

## 技术选型请求

C3 新判据（codex #44 裁定(c)）level==1 子集真实数据命中 0/84（source_index 之后无任何 sub_centers 满足过滤条件）。已排查排除级别方向读反(候选4)与坐标错位(678模式)。请在(1)修center/tower输出使新中枢可表达/(2)维持定义level==1通道等价关闭/(3)判据放宽为中枢延伸而非新确认/(5)先加诊断探针区分算法限制vs真实几何事实 中做出全权终局裁定

## 上下文

# 背景：C3 新判据（codex #44 终局裁定(c)）level==1 子集真实数据命中 0/84——需终局处置裁定

## 已完成的前序裁定链（不可推翻的既定事实）

1. `.chanlun/review-results/xzd2-impl-codex-audit-20260702.md` §5.1：level>=2 是 100% 结构性死门（代码级确定性，与本次裁定无关）。
2. `.chanlun/review-results/codex-decide-20260702-193853-5bbe.md`（task #44）：C3 判据从「center 精确匹配预选 last_zs」重设计为「背驰后新中枢+反向突破」（第43课语义），终局裁定 (c)，已给出精确规格并已实装（task #47）。裁定原文关键边界条件：
   > 「若 L2 复测显示 post-source 新中枢在模型中根本不可表达，应先修 center/tower 输出，而不是退回 (a)。」
3. task #47 实装完成，`cargo test --release econ_positive`：31 passed 0 failed 7 ignored。
4. **本次触发点**：`ECON_L2_MAX_BARS=350000` 真实 BTC 复测（`acc_classification_level_hole_dx`）：level==1 子集 84 条信号，`c3_new_center_exists`（source_index~confirm_index 间是否存在已确认次级中枢）= **0/84 = 0.00%**。这命中了 #44 裁定明文的「不得静默接受」边界条件，需 codex 终局复审处置方向。

## 现有代码（`rust/src/theta_v0/backtest/econ_positive.rs`，已按 #44 规格精确实装，逐行核对无逻辑偏差）

```rust
/// C3 死门诊断分项二：「背驰后新中枢+反向突破」（第43课语义，codex #44 终局裁定(c)）。
struct XzdC3BreakoutDiag {
    new_center_exists: bool,
    new_center_breakout_ok: bool,
}

fn xzd_c3_new_center_breakout(
    source_index: usize,
    confirm_index: usize,
    side: Side,
    sub_centers: &[Center],
    sub_moves: &[LeveledMove],
) -> XzdC3BreakoutDiag {
    let new_centers: Vec<&Center> = sub_centers
        .iter()
        .filter(|c| c.start_index >= source_index && c.end_index <= confirm_index)
        .collect();
    let new_center_breakout_ok = new_centers.iter().any(|z| {
        sub_moves
            .iter()
            .filter(|m| m.start_index >= z.end_index && m.end_index <= confirm_index)
            .any(|m| match side {
                Side::Long => m.rmove.hi() > z.zg,
                Side::Short => m.rmove.lo() < z.zd,
            })
    });
    XzdC3BreakoutDiag { new_center_exists: !new_centers.is_empty(), new_center_breakout_ok }
}

pub(super) fn gate_pass(&self) -> bool {
    self.type2_confirmed && (self.level != 1 || self.c3_new_center_breakout_ok)
}
```

调用侧（`build_gate_certificate`，`sub_centers`/`sub_moves` 来源）：
```rust
// 调用现场（econ_positive.rs 主循环）：
let sub_centers: &[Center] = if lvl > 0 { &cls_i.levels[lvl - 1].centers } else { &[] };
// build_gate_certificate 内部：
let sub_moves: &[LeveledMove] = lvl
    .checked_sub(1)
    .and_then(|l| tower.get(l))
    .map(|m| m.as_slice())
    .unwrap_or(&[]);
```

`lvl==1` 时 `sub_centers = cls_i.levels[0].centers`（**L0 级中枢**，1分钟原始 K 线段账本级别）。

## 已排查根因（本 agent 本轮代码审计，非重复前序诊断）

### 排查项 A：tower[lvl-1] 方向是否读反（团队候选(4)）——**已排除**

`recursive_tower`/`classifier/mod.rs` 注释明文：`Classification.levels` 索引 = 级别 ℓ（0=L0=1分钟线段账本），
索引递增=级别递增（更大）。`tower[lvl-1]` 对 `lvl==1` 取的是 `tower[0]`=L0=**次级别**（比 lvl 小一级），
与 044:56「这个该级别走势最后个次级别中枢是动态的」的「次级别」语义方向一致——**不是**级别方向读反。
候选(4)不成立，排除。

### 排查项 B：sub_centers 是否坐标错位/陈旧快照（678 模式第四例候选）——**已排除**

`classifier/mod.rs:938-940` 注释明文承诺并被增量塔单测验证：
> `Classification.levels[k].centers`：增量累积的中枢序列 == 全量 `detect_centers`（resume bit-exact）

即 `cls_i.levels[lvl-1].centers` 在每个 bar i 都是「从 L0 段序列全量扫描」的完整累积列表，非陈旧快照、
非坐标系错位（`UnitRange.start_index/end_index` 在 `center.rs:49-52` 明确注释为「L0 原始 K 序坐标」，
跨级别统一坐标系，`segment_to_unit` 直接复用 `seg.start_index/end_index`）。678 号「两条选择链对象身份
分裂」模式在此处**不适用**——本次只有一条选择链（`filter + collect`），无归属链/跨度链二义。

### 排查项 C：中枢探测算法本身的结构性限制（新发现，未见于前序诊断）

`classifier/mod.rs:145-168`（`detect_centers_with`，L0/上级共用骨架）：

```rust
fn detect_centers_with(units: &[UnitRange], build: ...) -> Vec<Center> {
    let mut centers = Vec::new();
    let mut i = 0usize;
    while i + 2 < units.len() {
        match build(&units[i], &units[i + 1], &units[i + 2]) {
            Some(c) => { centers.push(c); i += 3; }  // 成立支：前进3段，不重叠
            None => { i += 1; }                       // 不成立支：前进1段
        }
    }
    centers
}
```

文件自带注释（`mod.rs:129`）明确承认这是已知简化：
> 「★诚实范围：v0 用**非重叠三段窗口**识别中枢（连续三段成真枢则前进 3 段）。延伸中枢
> （同一中枢吸收后续段）的完整 start/finish 区间识别留待后续。」

这是「post-source 新中枢在模型中不可表达」候选(1)的具体机制候选：非重叠消费窗口 + 无「延伸中枢」
合并，意味着一旦某窗口被吸收进已确认中枢，后续段落必须重新从头攒够独立的、不重叠的三段才能确认
下一个新中枢——比一个允许滑动/重叠窗口或允许中枢延伸的实现，客观上产出更少、更晚确认的中枢。

**但这不必然是 0/84 的唯一解释**——另一个同样成立的候选：level==1 的 Type2/3 信号（C1 区间套不适用）
本身定义为「level==1 走势尚未在该级别确认背驰反转」，即 level==1 走势在 source_index 之后**仍在延续
原趋势**；而 L0 中枢确认要求「方向交替」（`center.rs` `DirAlternates`），若 level==1 仍处趋势延续段，
其内部 L0 分段在方向上更可能保持趋势一致（少交替），从而客观上降低了 source_index 之后短期内形成
新 L0 中枢（需要方向交替的盘整）的概率——这可能是**真实市场几何事实**，非实现缺陷。

排查项 C 的两个子候选（v0 算法限制 vs 真实几何事实）**无法仅靠静态代码审计区分**，需要 codex 判断
应如何处置：是否值得为了验证这一区分而先修 `detect_centers_with` 支持延伸中枢/重叠窗口再重测，还是
直接接受当前候选(2)/(3)。

## 678 谱系摘要（对象身份分裂，供背景参考，本次判定为不适用但供交叉核对）

678 号谱系记录的是「judge_third 归属链 vs last_zs 跨度选择链」两条不同选择规则在 same_center_any 上
不重合（0/84）——这是**旧 C3 判据**（task #41 之前）的死门根因。**新 C3 判据（task #47 已实装）已经
绕开了这条路径**：新判据不再依赖 `judge_third.center` 或旧 `last_zs`，而是直接对 `sub_centers` 做
`start_index >= source_index` 过滤（单一选择链，见排查项 B）。故 678 号的「对象身份分裂」矛盾**已被
新判据消解**，本次 0/84 是**全新的、独立的**命中率问题，根因候选见排查项 C，与 678 不同源。

## 第43课原文（一级权威，逐字节选，供候选(1)(3)判断依据）

> 043:18「转折必然由背驰导致，但背驰导致的转折并不一定是同一级别的……缠中说禅背驰-转折定理：某级别
> 趋势的背驰将导致该趋势最后一个中枢的级别扩展、该级别更大级别的盘整或该级别以上级别的反趋势。」

> 043:34「……这第二种情况，必然要先形成一个比1分钟级别要大的中枢，然后向下突破，最终形成回跌到B
> 里的走势。」

> 043:38「……并不必然回跌，可以往上突破，使得a+A+b+B+c继续延伸。」

> 043:194「如果不破前期高点，那只是一个新中枢，所以当然持有，如果破了，那就一定要走。」

> 044:56「这个该级别走势最后个次级别中枢是动态的，不一定为小级别背驰点前的中枢。」

## 处置候选（三+两新增，供 codex 全权终局裁定）

**(1) post-source 新中枢在模型中不可表达 ⟹ 先修 center/tower 输出**
- 具体：`detect_centers_with` 增加「延伸中枢」支持或改非重叠窗口为滑动/重叠窗口，使新中枢能更快被
  确认。大动作，涉及 `center.rs`/`mod.rs` 核心扫描算法，影响全部级别（不止 level==1 C3 判据消费方）。
- 风险：改动面广，可能破坏其他依赖「非重叠三段窗口」bit-exact 行为的既有测试/下游（`classify_move`
  裁决基于同一 `centers` 序列，牵一发动全身）。

**(2) 维持定义 ⟹ level==1 小转大通道实践等价关闭**
- 具体：不改代码，接受 level==1 分支在当前 350K 窗口下恒为 0/84 通过，Xzd 有效通道收窄为 level>=2
  的 C2-only（30 条）。
- 语义：承认「新中枢+突破」判据虽忠于第43课语义，但在当前 v0 中枢探测算法的粒度/窗口约束下，对
  level==1 这一最小颗粒场景不可行——非"定义错"，是"当前实现粒度下该定义在此子集恒假"。

**(3) 判据放宽为「post-source 中枢延伸/更新」而非「新确认中枢」**
- 具体：不要求 `sub_centers` 中出现**全新**中枢对象，而是接受「已存在中枢的核心区间在 source_index
  之后被突破」（即使该中枢的 start_index 早于 source_index）——回归某种「延伸」语义但不改中枢探测
  算法本身，只改 C3 判据对「新」的定义（从"中枢对象诞生时刻在 source 之后"放宽为"突破发生时刻在
  source 之后"）。
- 风险：判据是否还忠于「先形成一个新中枢再看突破方向」（043:34 强调「先形成」）——若中枢是 source_index
  之前就已形成的旧中枢，突破可能只是该旧中枢的自然延续突破，不是"新中枢诞生+突破"这一完整叙事的一部分。

**(4) 实装级别方向订正**——本 agent 代码审计已排除（见排查项 A），tower[lvl-1] 方向正确，不成立。

**(5)（新增，排查项 C 发现）中枢探测算法先做最小改动验证再定夺**：先给 `detect_centers_with` 加一个
低成本诊断（不动生产路径本身，只加只读探针：用「重叠滑窗」重新扫描 source_index 之后的 L0 units，
统计若允许重叠窗口是否能产出非零新中枢数），以区分排查项 C 的两个子候选（v0 算法限制 vs 真实几何
事实）。若诊断显示重叠窗口下新中枢命中率显著非零 ⟹ 支持候选(1)；若仍恒为 0 ⟹ 排除算法限制，坐实
候选(2)/(3)二选一。

## 请裁定

1. 五选一（或提出经过同等严格度论证的第六候选）。
2. 若裁 (1) 或 (5)：给出精确实装规格（诊断探针或算法改动的函数签名/输入输出/是否影响生产路径 bit-exact）。
3. 若裁 (2) 或 (3)：给出 gate_pass()/相关判据函数的精确最终代码。
4. 明确本裁定是否阻塞 #13（W-VERIFY alpha 全量重测），给出理由。
5. 本裁定是全权终局裁定，不再上浮编排者，请给出确定性结论。

## 要求

请做出明确的技术选择。

输出格式：
- **决策**：[你的选择]
- **推理链**：[为什么选这个]
- **替代方案**：[被拒绝的方案及理由]
- **边界条件**：[何时应推翻此决策]
- **风险**：[可能的问题]


## 要求

请做出明确的技术选择。

输出格式：
- **决策**：[你的选择]
- **推理链**：[为什么选这个]
- **替代方案**：[被拒绝的方案及理由]
- **边界条件**：[何时应推翻此决策]
- **风险**：[可能的问题]


## Response

**决策**：选 **(5) 先加诊断探针**。生产 gate 暂不改，`level==1` 仍按 #44/#47 的“post-source 新中枢 + 反向突破”执行；禁止现在直接改成旧中枢延伸判据。

最终代码语义保持：

```rust
pub(super) fn gate_pass(&self) -> bool {
    self.type2_confirmed && (self.level != 1 || self.c3_new_center_breakout_ok)
}
```

探针规格：

```rust
struct XzdC3OverlapProbeDiag {
    overlapping_new_center_count: usize,
    overlapping_breakout_count: usize,
    first_overlapping_center: Option<(usize, usize)>,
}
```

新增只读函数：

```rust
fn xzd_c3_overlap_window_probe(
    source_index: usize,
    confirm_index: usize,
    side: Side,
    sub_units: &[UnitRange],      // 必须是 detect_centers_with 原本消费的同一层次级别 unit ledger
    sub_moves: &[LeveledMove],
) -> XzdC3OverlapProbeDiag
```

规则：对 `sub_units.windows(3)` 做滑动一格扫描，复用同一套中枢构造条件、方向交替、`ZD/ZG` 计算；只统计 `start_index >= source_index && end_index <= confirm_index` 的 post-source 新中枢；突破判断完全复用现有 `xzd_c3_new_center_breakout` 的 breakout 规则。默认不运行，需 `ECON_C3_OVERLAP_PROBE=1` 打开；不得写入 `Classification.levels[*].centers`，不得改变 `tower`，不得改变正常输出 digest。

**推理链**：0/84 只证明“当前生产中枢账本下 C3 L1 恒假”，还不能证明是算法限制，也不能证明是真实几何事实。直接裁 (1) 会改 `detect_centers_with` 这个全级别核心账本，影响面过大；直接裁 (2) 会把实现限制误签成市场事实；裁 (3) 则把“新中枢形成后突破”偷换成“旧中枢后续突破”，概念不忠实。探针是最小可判别动作：生产路径不动，但能把“非重叠窗口压掉新中枢”与“确实无新中枢几何”分开。

**替代方案**：  
(1) 暂拒。只有当滑动窗口探针出现 `overlapping_breakout_count > 0`，才升级为修 center/tower 输出。  
(2) 暂拒作为当前终局语义。只有探针仍为 0，才确认 level==1 通道在当前定义和数据下实践关闭。  
(3) 拒绝。除非形式化定义明确把 pre-source 旧中枢延伸纳入 C3，否则代码层不能放宽。  
(4) 已排除，不再进入候选。

**边界条件**：  
若同一 84 条样本中 `overlapping_breakout_count >= 1`，推翻当前生产中枢扫描策略，转 (1)：实现可表达延伸/滑动确认的新中枢，并用 golden digest 重验全部受影响路径。  
若 `overlapping_new_center_count == 0`，或有滑动新中枢但 `overlapping_breakout_count == 0`，确认 (2)：C3 level==1 在该真实数据窗口下关闭，不再为它改定义。  
若未来权威规格把“旧中枢 source 后延伸并突破”定义为 C3，才重开 (3)。

**风险**：探针若复制中枢构造逻辑，可能和生产 builder 漂移，所以应抽出共享纯函数或直接复用现有 builder。探针会增加一次诊断运行成本，但不影响默认 release 路径。#13 的正式 W-VERIFY alpha 全量签收应阻塞到探针结果出来；已跑结果只能标记为当前 gate baseline，不能作为最终 C3 裁定后的 alpha 结论。
