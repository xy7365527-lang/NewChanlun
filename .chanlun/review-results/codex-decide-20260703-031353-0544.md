# Codex decide — 2026-07-03 03:13:53 UTC

## 元数据

- **mode**: decide
- **subject**: G2裁定：σ_higher是否须进canonical z桶键（PDF §6/§12 vs codex#81修正4冲突重裁）
- **model**: codex-cli
- **timestamp**: 2026-07-03 03:13:53 UTC
- **context-file**: /tmp/codex-q1-g2-sigma-higher.md

## Prompt

## 技术选型请求

G2裁定：σ_higher是否须进canonical z桶键（PDF §6/§12 vs codex#81修正4冲突重裁）

## 上下文

# 裁定1（G2）：σ_higher 是否进 z 桶键

## 冲突双方

**A. PDF 权威（docs/formal-chain/完整的策略.pdf §6，编排者钦定"严格实装版验收规格"）**：

> §6 完整互斥状态 z：
> z = (ℓ,e,δ,I_γ,Ndepth,CandType,ForceState,Jchain,σ_higher,σ_p,role,posState,shortDiff,H,TStage,ηBucket,RiskMode,CostBucket,MarginState,ExitType)
>
> 解释：σ_higher = 入场时上级方向态；σ_p = 父声部方向（两个不同维度，都在 z 里）。
>
> 原文论证：「你们已有实验说明，σ_higher 的收益符号会随级别翻转，L1 是主要 beta 来源，不能用全局『顺上级/逆上级』一刀切。所以完整状态必须含 (ℓ,δ,σ_higher)。否则会把『低级别逆上级短差』和『高级别逆上级飞刀』混在一起。」
>
> §12 alpha 选择器复用同一个 z：μ(z,a)=E[X(a)|Z=z]，即 §6 定义的 z 就是 alpha 桶键，PDF 没有区分"状态描述用的 z"和"检验桶键用的 z"——是同一个对象。
>
> §15 checklist P4 明确列为完整性必要项：「σ_higher：上级方向与级别联立进入状态键。」缺失判定为"不完整"。

**B. codex #81 修正4（已落地 commit，`.chanlun/review-results/z-bucket-impl-20260702.md`）**：

> 4. sigma_higher 不并入 z（修正4）：诚实保持诊断字段——它是结构层上级走势净方向，非持仓树父声部 σ_p，二者不合并。

> 九维对照表原文：「σ_higher | 上级方向 | 结构层上级走势净方向 | ⚠️ 数据源在，未接入 z（诚实）| `sigma_higher_at`→`SignalDecomp.sigma_higher`（666 号，CSV 诊断）。codex 修正4：结构层量≠持仓树 σ_p，不并入 canonical z（否则冗余/混淆）。保留独立诊断，不入桶键」

现状代码：`MuClass`（`rust/src/theta_v0/backtest/mu_estimator.rs`）7 维 = (level, delta, i_class, parent_dir, short_swing, position, horizontal)，不含 σ_higher。σ_higher 只作为 `SignalDecomp.sigma_higher: i8` 独立诊断字段存在（`econ_positive.rs`），不参与任何分桶/置换检验桶键。

## 关键实证教训（须核验是否适用于本冲突）

memory 固化的结构性发现（`project_iclass_delta_collinearity_perm_degeneracy`，2026-07-03，p3 full-z 判定实测）：

> full-z 桶键中 i_class（6-bit 掩码）与被检验的 δ 共线——buy3(i4) 与 sell3(i32) 分属不同桶后，桶内 δ 单一，δ-置换（层内重排方向标签）结构性退化：主 root buy 子桶 n=1612（大样本、非碎裂）perm_p 仍从 4 元组口径的 0.005 退化到 1.000。
>
> 一般教训：任何与 δ **决定性相关**的维度（方向性 i_class、side-specific 角色值等）进入置换检验的桶键 = 自毁检验，且这比"小样本碎裂"更根本——加样本救不了。
>
> 对照：现行 W-VERIFY/perm_test 路径用 4 元组 (ℓ, bsp_class压缩, δ, σ_higher) 手取桶键（`rust/src/theta_v0/backtest/perm_test.rs`，不读 `MuClass`），此口径 perm_p=0.050 保住检验力——**σ_higher 本身已经在这个工作的 4 元组桶键里**，且未导致退化。4 元组之所以工作，是因 bsp_class 压缩版（1/2/3）方向对称。

**关键区分**：σ_higher（上级走势净方向，独立于交易方向 δ 的结构量）与 i_class（buy3/sell3 这类**方向性谓词**，一个 i_class 值唯一决定 δ 符号）是**不同性质的维度**。σ_higher 在同一上级走势状态下，δ=+1（顺势）和 δ=-1（逆势）都可能出现（这正是 PDF 引用的"奇偶交替"实验的前提——逆上级/顺上级都有样本，否则无法检验"奇偶交替"）。目前没有证据表明 σ_higher 决定性地共线于 δ。

## 相关代码

- `rust/src/theta_v0/backtest/mu_estimator.rs:69` `MuClass`（canonical z，7维，无 σ_higher）
- `rust/src/theta_v0/backtest/econ_positive.rs:111-118` `SignalDecomp.sigma_higher`（独立诊断字段，666号实验产物，来自上级 LeveledMove 端点 close 净差符号）
- `rust/src/theta_v0/backtest/perm_test.rs` 4 元组桶键（含 σ_higher，工作中，perm_p=0.050）
- `rust/src/theta_v0/backtest/selector.rs:144` `z_of_candidate`（role→z 桥，MuClass 生产者）

## 裁定请求

请裁定 σ_higher 进 z 的严格形式，从下列或你认为更严格的选项中选择并给出理由：

1. **全进**：σ_higher 加为 `MuClass` 第8维，同时参与 alpha 桶键（置换检验）与仓位分维（q_Θ）——PDF 字面要求，但需先核验是否会像 i_class 一样与 δ 共线导致 perm 检验退化（现有 4 元组口径已包含 σ_higher 且工作正常，是否可作为"不共线"的实证支持？还是 4 元组的 σ_higher 与 MuClass canonical z 语境不同，不能直接套用其"不退化"结论？）。
2. **只进状态描述/仓位分维，不进置换检验桶键**：PDF §6 的 z 用于状态分类/仓位函数 q_Θ，§12 alpha 桶键是另一个独立对象（尽管 PDF 字面写的是同一个 z）——如果这个区分不成立（PDF 明确 §12 复用 §6 的 z），此选项是否事实上违反 PDF 权威？
3. **维持 codex #81 修正4（不进 z）**：现状已合规，PDF 的"完整状态"要求可以通过独立诊断字段 + 现有 4 元组 perm_test 路径满足，不需要在 canonical MuClass 里再加一维。

请明确：这是「PDF 权威 > 内部 codex 历史裁定，须推翻修正4，实装 σ_higher 进 z」，还是「现口径（诊断字段 + 4元组 perm_test）已经满足 PDF 语义，修正4 站得住」，或第三种条件性方案。给出严格论证（不接受"两边都对"的模糊裁定），并说明若须实装，改动范围（`MuClass` 维度+`z_of_candidate`+所有分桶消费点）与是否触发历史 alpha 结论重跑。


## 要求

请做出明确的技术选择。

输出格式：
- **决策**：[你的选择]
- **推理链**：[为什么选这个]
- **替代方案**：[被拒绝的方案及理由]
- **边界条件**：[何时应推翻此决策]
- **风险**：[可能的问题]


## Response

**决策**：推翻 codex #81 修正4。`σ_higher` 必须进入 canonical `MuClass`，作为独立维度，不与 `σ_p` 合并。  
这是选 **“全进”的严格修正版**：进 `MuClass`、进 `μ(z,a)`/`q_Θ` 的状态桶；置换检验可以用含 `σ_higher` 的桶，但必须重新预注册并避开 `i_class` 这类 δ-决定性维度导致的退化。

**推理链**：PDF §6 把 `σ_higher` 写进完整 `z`，§12 又直接用同一个 `z` 定义 `μ(z,a)`，§15 checklist 明确要求“上级方向与级别联立进入状态”。内部历史裁定不能高于规格。codex #81 修正4说“`σ_higher != σ_p`”是对的，但结论“所以不进 z”是错的；正确结论是二者都进 z，且字段名/语义必须分开。

`i_class` 的退化教训不能外推到 `σ_higher`。`i_class=buy3/sell3` 这类值会决定 δ；`σ_higher` 是入场时上级走势净方向，不决定当前交易方向。它应作为 δ-free 结构维进入状态键。仍要做共线性审计，但它不是已知自毁维度。

我核验到当前代码里 `MuClass` 在 [mu_estimator.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/mu_estimator.rs:80) 仍没有 `σ_higher`，`SignalDecomp` 在 [econ_positive.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/econ_positive.rs:118) 有诊断字段，`perm_test` 的 4 元组 [perm_test.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/perm_test.rs:37) 当前实际取的是 `parent_dir`，不是 `SignalDecomp.sigma_higher`。所以“诊断字段 + 4 元组”不能声称已满足 PDF full-z 语义。

**替代方案**：  
2 不选。若把状态 `z` 和 alpha 桶键拆开，必须显式改名成投影估计量 `φ(Z)`，不能继续声称是 PDF §12 的 `μ(z,a)`。  
3 不选。诊断字段不是 canonical 状态键，不能满足“完整状态必要项”。  
朴素 full-z 置换也不选：把方向性 `i_class` 原样塞进 δ-置换桶，会复现退化。修的是 `σ_higher` 归属，不是给所有 full-z 置换背书。

**边界条件**：  
可推翻本裁定的条件只有这些：PDF 被正式修订为 `z_state != z_alpha`；实测证明 `σ_higher` 在生产样本中被 δ 或其他桶维决定性推出；或 `σ_higher_at` 被证明不是入场时可测量。样本碎裂只能影响 selection/检验投影，不推翻 canonical z。

**风险**：  
状态空间扩大，历史 alpha 结论凡声称“full-z/PDF §12”都要重跑。最大工程风险是 `None/Some(0)` 混桶：建议 `MuClass` 加 `sigma_higher: Option<i8>`，`None` 表示构造口径未知，`Some(0)` 表示真实无上级/持平。落地范围：`MuClass`、`from_certificate`、`z_of_candidate` 或新增 `z_of_candidate_with_sigma`、`econ_positive` 生产链、`perm_test` 桶键/预注册、所有分桶消费点和测试。
