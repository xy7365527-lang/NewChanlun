# Codex decide — 2026-07-03 03:17:26 UTC

## 元数据

- **mode**: decide
- **subject**: G2/G4/G7 三项缺口实施顺序+分阶段可行性+阻塞判定+优先级排序
- **model**: codex-cli
- **timestamp**: 2026-07-03 03:17:26 UTC
- **context-file**: /tmp/codex-q1-spec-decide-ctx.md

## Prompt

## 技术选型请求

G2/G4/G7 三项缺口实施顺序+分阶段可行性+阻塞判定+优先级排序

## 上下文

# 上下文：G2/G4/G7 review 裁定后的实施决策

前一轮 Codex review（`.chanlun/review-results/codex-review-20260703-031309-7fe6.md`）已裁定
三项皆为真缺口：

- **G2**（σ_higher 不在 canonical z）：needs_work，裁定并入，翻转 #81"不并入"裁定。
- **G4**（统计层用 τ^reverse 而非 typed exit）：reject 当前简化，严重性"致命"，修复需要把
  walk-forward μ 训练管线接回真实状态机（π fill loop / `interp::interpret` / `KThetaRiskGate`），
  不是小补丁。
- **G7**（K_Θ 缺最大毛头寸约束）：reject 现状，严重性"致命"，修复需要在 `LegTarget` 折叠成
  `net_target_units` 之前先用 `gross_target_units` 约束/缩放（或更彻底：K_Θ 从"净标量可行集"
  升级为"声部组合可行集"）。

三项工作量差异巨大：G2 是局部字段扩维（中等，几个文件的接口签名改动 + 碎片化防护）；G4/G7
都被判定需要架构级改动（G4 = walk-forward 管线重接生产状态机；G7 = K_Θ 从标量升级为声部级）。

## 需要你决策的问题

1. **实施顺序**：G2/G4/G7 之间是否存在依赖关系（例如 G4 的"typed exit"判定是否需要先有
   G7 的声部级持仓状态才能正确识别"根仓清仓 vs 核心仓减仓 vs 短差子声部关闭"？还是三者
   相对独立，可以并行推进）？

2. **G4/G7 是否可以分阶段**：架构级改动风险高，是否存在一个"诚实降级但不做架构改动"的
   中间态——比如：
   - G4：不重接生产状态机，改为在现有 walk-forward 管线里**只**改进退出规则的精确度
     （例如"下一个同 level 同腿反向信号"而非"任意反向信号"），同时在所有下游 alpha 报告
     强制标注"简化 typed exit 近似，非真实 π 层语义"的有效域限定语——这算不算一种可接受
     的诚实中间态，还是这仍然不满足 PDF §9 的"必须使用正规出场"要求，属于 no-patch-mentality
     禁止的半成品？
   - G7：不做 K_Θ 声部级重构，只加一个"事后诊断门"（算完 p* 后检查对应的声部分解是否会
     导致毛敞口超限，超限则整体拒绝该 bar 的开仓候选、退回上一个可行 p*）——这是不是
     review 里说的"假修复"（"只在 pi_theta_position 后用净 p 调 leverage_ok 是假修复，
     因为毛分解已经丢了"）？如果是假修复，为什么——具体在什么场景下这种事后诊断门会
     漏掉真实的毛敞口超限？

3. **阻塞判定**：G4/G7 的缺口是否阻塞当前 goal（g-full-strategy-pi 的全实装收口）已完成的
   alpha 结论（`project_wverify_alpha_retest_pass`、`project_stheta_v1_fullwindow_l3_falsified`
   等 memory 记录的既有 L2/L3 结果）？还是这些结果可以维持"在当前简化口径下"有效，只需要
   补充有效域声明，不需要立即重跑？

4. **优先级排序**：如果只能先做一项（资源/时间受限的现实情况），G2/G4/G7 里哪个应该最先做，
   哪个可以最后做——给出排序理由（不是"都重要"这种回避，给出具体的风险/影响比较）。

请给出具体决策，不要停留在"取决于团队优先级"这种空泛回答——基于代码证据和 PDF 权威给出
你的判断，并说明每项判断在什么条件下会翻转。


## 要求

请做出明确的技术选择。

输出格式：
- **决策**：[你的选择]
- **推理链**：[为什么选这个]
- **替代方案**：[被拒绝的方案及理由]
- **边界条件**：[何时应推翻此决策]
- **风险**：[可能的问题]


## Response

**决策**：实施顺序定为 **G2 → G7 → G4**。G2 先落，因为是 canonical `z` 的字段忠实性修正，成本低且会污染后续统计键；G7 次之，因为 G4 的 typed trade ledger 应消费最终的 KΘ 风险/声部可行集；G4 最后做，且必须重接真实 π fill loop，不能在 `build_mu_from_bars` 里继续手搓退出状态机。

**推理链**：G2 独立，当前 `MuClass` 无 `sigma_higher`，而 PDF §6/§12 明确要它进 `z`；实现上选 `sigma_higher: Option<i8>`，生产路径必须填 `Some`，裸证书/旧构造填 `None`，避免把“未知”混成“无上级/0”。`UClass` 默认继续丢弃它，防碎片化。证据见 [mu_estimator.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/mu_estimator.rs:80)、[selector.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/selector.rs:145)。

G7 不能事后用净 `p*` 查。`strategy_target_legs` 后仍有声部腿，`net_target_units` 明确会丢失双开毛敞口；所以最小正确方案是在 [coverage.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1745) 生成 `legs` 后、[net_target_units](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1751) 前做 `gross_target_units` 约束/等比例缩放。先用现有 `risk.gamma * base_units` 作毛 cap，后续有实盘需求再拆 `gross_gamma/net_gamma`。净折叠丢信息的代码注释在 [coverage.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1261)，毛敞口函数已存在于 [coverage.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1282)。

G4 的中间态只能叫 **legacy/approx diagnostic**，不能关闭 G4。把“任意反向信号”改成“同 level 同腿反向信号”可以减少误差，但仍不是 PDF §9 的 `CloseRoot/ReduceCore/CloseShortDiff/RiskExit/Hold`。当前代码确实是 `τ^reverse`，见 [l3_delta_r_alpha.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/l3_delta_r_alpha.rs:201)。正确方案是生产 π loop 输出不可变 `TypedTradeLedger { entry_z, voice_id, entry_bar, exit_bar, exit_type, residual_inputs }`，再从 ledger 构造 `MuObservation/ResidualTrade`。

**替代方案**：  
拒绝 G4 “改进反向规则 + 标注后算修复”。它可作为诚实降级报告口径，但不满足 PDF §9。  
拒绝 G7 “算完 `p*` 后检查净仓/回退上一个 p*”。典型漏例：Long 100 + ShortDiff Short 100，净 `p=0`，毛 `G=200`；净检查永远通过。Long 110 + Short 100 与 Long 10 的净值同为 10，`p*` 后无法反推出是哪一种。  
拒绝 G4 先于 G7 完整落地；否则 typed ledger 还要在 G7 改变可行集后重跑/重写。

**边界条件**：  
G2 可翻转：PDF 正式拆成 `z_state != z_alpha`，或实测 `sigma_higher` 被其他维度确定性推出。  
G7 可翻转：生产配置硬禁所有多空双开/ShortDiff，且测试锁死；否则毛 cap 必须进腿级。  
G4 可翻转：逐笔对照证明 `τ^typed≈τ^reverse`，且 μ/χ/W-VERIFY/L3 结论稳定；没有这个证据前不能降级为已修。

**风险**：  
G2 会扩大桶数，历史 “full-z/PDF §12” 声明需重跑；但 UClass 可保留抗碎片化。  
G7 等比例缩放会改变净目标和历史回测，需要 golden digest。  
G4 最大风险是重接 fill loop 后改动所有 alpha 输入分布；既有 `project_wverify...`、L3norm 等结果可保留为“当前简化口径”记录，但不能再被用作 `π^full`/typed-exit/gross-KΘ 完成证明。若要收口 `g-full-strategy-pi`，G4/G7 完成后必须重跑相关 μ/χ/W-VERIFY/L3。
