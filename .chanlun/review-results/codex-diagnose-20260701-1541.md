# Codex diagnose — 2026-07-01 15:41:57 UTC

## 元数据

- **mode**: diagnose
- **subject**: 命题②'收益率/夏普尺度证伪'是真否证还是r=pnl/hold归一化引入的Jensen型算术伪影(把underpowered=inconclusive假翻成证伪)?
- **model**: codex-cli
- **timestamp**: 2026-07-01 15:41:57 UTC
- **context-file**: /tmp/codex-sharpe-audit-ctx.md

## Prompt

## 诊断目标

命题②'收益率/夏普尺度证伪'是真否证还是r=pnl/hold归一化引入的Jensen型算术伪影(把underpowered=inconclusive假翻成证伪)?

## 上下文

# Codex 异质诊断上下文：夏普/收益率尺度"证伪"是真否证还是归一化伪影？

你是代码层异质否定源。任务：对一个"翻证伪"的统计结论做严格 diagnose。防止同质蜂群把
"underpowered=inconclusive"翻过头，翻成一个由归一化算术引入的**假证伪**。

## 背景（三层链条）

对 BTC 单标的（12626 条交易信号台账），检验一个假设：在 (级别ℓ, 相对上级方向 q) 网格上，
逆上级方向 (q=−1) 是否有可交易 alpha。判据：μ̂>0 ∧ 置换检验 p<0.05 ∧ OOS 段收益的 95%
block-bootstrap 下界 LCB>0。

**第1层（原始结论）**：用**原始 pnl**做判据3，LCB_OOS 全负 → 候选 G=∅。

**第2层（前一轮 codex 审查，已结算）**：判定原始 pnl 尺度下 LCB≤0 **不是证伪，是 underpowered
=inconclusive**。理由：单笔 pnl 的 CV=σ/μ 高达 10-20，转正需 n_eff>(1.645·CV)² ≈ 271-3426，
而 L1/L2 的 OOS n 只有 38-188 → 判据3 对这些单元根本无检出力。`LCB≤0` 只表示"未能 95% 排除 0"，
不能推出 μ≤0。命题应标 **待验/inconclusive**，不是证伪。

**第3层（本次被审查对象——它声称"翻证伪"）**：报告 `lq-sharpe-scale`。做法：判据3 改用
**收益率归一化 r_i = pnl_i / hold_i**（hold=持有 bar 数）。声称：
- 归一化把单笔 CV 从 19-219 压到 2.69-6.01（降 80-97%）。
- n_eff 门槛 (1.645·CV)² 从数千降到 13-68，全部 OOS_n（37-2269）越过门槛 → **判据3 有检出力了**。
- 有检出力后 LCB_r **全负**，且 4 窗 walk-forward 一致负。
- 因此：命题从 inconclusive **翻成"证伪（在收益率/夏普尺度）"**。
- 同时承认双尺度分裂：mean(pnl)=+25.5（正）vs mean(r)=−0.40（负）。声称"pnl 尺度仍 inconclusive，
  r/夏普尺度证伪，两尺度回答不同问题，并列成立"。

## 审查代理已跑的算术自检（关键证据，请独立判断这些是否成立）

**自检1（CV 压缩的机制）**：对每个 (ℓ,q) 单元，pnl 与 hold 的相关 corr(pnl,hold)≈+0.54（正相关：
赢家持有期长）。

**自检2（决定性反事实）**：
| (ℓ,q) | CV(pnl) | CV(pnl/固定常数) | CV(pnl/hold各自) |
|-------|---------|-----------------|------------------|
| (0,−1)| 32.91   | 32.91           | 3.44             |
| (0,+1)| 59.77   | 59.77           | 4.67             |
| (1,−1)| 18.97   | 18.97           | 3.65             |
| (1,+1)| 218.86  | 218.86          | 6.01             |
| (2,−1)| 49.94   | 49.94           | 2.69             |
| (2,+1)| 37.84   | 37.84           | 3.23             |

除以**固定常数**（全样本平均 hold）CV **完全不变**；除以**各自 hold** CV 才骤降 80-97%。
→ CV 压缩纯粹来自 hold 分母的逐笔异质性，不是 pnl 信号本身方差缩小。

**自检3（符号系统性翻转）**：
| (ℓ,q) | mean(pnl) | mean(r=pnl/hold) | 符号 |
|-------|-----------|-------------------|------|
| (0,−1)| +39.77    | −0.378            | 翻转 |
| (0,+1)| +20.32    | −0.391            | 翻转 |
| (1,−1)| +66.75    | −0.530            | 翻转 |
| (1,+1)| −5.02     | −0.357            | 一致 |
| (2,−1)| +20.15    | −0.725            | 翻转 |
| (2,+1)| +25.84    | −0.444            | 翻转 |

6/7 单元 mean(pnl)>0 但 mean(r)<0。因 pnl 与 hold 正相关，E[pnl/hold] ≠ E[pnl]/E[hold]
（Jensen 方向）：pnl/hold 系统性压低长持有赢家（大 pnl 除大 hold）、放大短持有输家（小亏损除小 hold）。

**自检4（Lead 提出的第三度量——按 bar 对齐的策略收益序列夏普）**：
把每笔 pnl 按 pnl/hold 摊到其持仓区间 [entry,exit) 的每个 bar，构造真实时间轴上的每-bar 组合收益序列，
再算夏普：
| (ℓ,q) | n_bars    | mean    | sharpe  |
|-------|-----------|---------|---------|
| (0,−1)| 2,742,011 | +0.052  | +0.0316 |
| (0,+1)| 3,753,941 | +0.041  | +0.0181 |
| (1,−1)| 490,486   | +0.059  | +0.0524 |
| (2,−1)| 104,213   | +0.025  | +0.0215 |
| (2,+1)| 186,924   | +0.027  | +0.0291 |

按 bar 对齐（时间轴）的组合收益全为**正**夏普——符号根本没翻。

## 必须回答的诊断问题

1. **CV 压缩是真检出力还是算术假象**：自检2 证明 CV 压缩 100% 来自除以变量 hold 而非除常数。
   那么 n_eff 门槛 (1.645·CV)² 从数千降到 13-68 是不是虚的——因为门槛公式里的 CV 被算术压缩了，
   "越过门槛=有检出力"的推论是否失效？功效框架 LCB>0⟺n_eff>(1.645·CV)² 的前提是 CV 反映**信号**
   的变异，若 CV 被分母 hold 异质性人为压小，该框架还成立吗？

2. **符号翻转是收益变负还是加权重排**：mean(pnl)>0 但 mean(pnl/hold)<0 的翻转，是"单位时间效率
   真的为负"（经济事实），还是 Jensen 型算术产物（pnl 与 hold 正相关 → 除以 hold 系统性重排权重，
   长持有赢家被压、短持有输家被放）？r=pnl/hold 的经济意义是"每 bar 收益"吗？还是它把不等持有期的
   笔混在一起算术平均，产出一个不对应任何可实现策略收益的量？

3. **哪个度量对应"可交易性"**：三个候选度量——(a) mean(pnl) 直接、(b) mean(pnl/hold) 按信号平均、
   (c) 按 bar 对齐的时间轴组合收益夏普。哪个是可交易性的正确度量？(b) 对一个持有 1000 bar 赚 100 的信号
   给 r=0.1，对持有 10 bar 亏 5 的信号给 r=−0.5——按 (b) 后者"更差"，但资金实际赚的是前者。(c) 全正。
   (b) 全负。它们能"并列成立、回答不同问题"吗，还是其中至少一个是错误度量（度量了不对应可实现盈亏的量）？

4. **双尺度分裂结算是否严谨**：报告说"pnl inconclusive + r 证伪，并列不覆盖"。这个并列成立吗？
   如果 (b) 是错误度量（自检2/3/4 强烈指向此），那 r 尺度的"证伪"是伪证伪，命题应退回
   **inconclusive**（pnl underpowered，r 尺度用了错误度量 → 两者都不足以证伪）。

5. **4 窗 walk-forward 一致负能否救 r 尺度**：4 窗一致 LCB_r<0 被报告当作"时序稳定否证"。但如果
   r=pnl/hold 本身是错误度量（每窗都算错），一致负只是"每个窗口都被同一个算术偏置压负"，不是时序稳定性
   证据。一致性能否弥补度量本身的错误？

## 关键判定（请给出明确结论）

命题②"收益率/夏普尺度证伪"是：
- (A) **真否证**——归一化正确暴露了单位时间负效率，命题②在 r 尺度确实被证伪；还是
- (B) **归一化伪影**——r=pnl/hold 引入 Jensen 型算术偏置，把 inconclusive 假翻成证伪，命题②应退回
  inconclusive（pnl underpowered + r 尺度度量错误，两者都不足以证伪）。

若判 (B)，请说明：正确的风险调整度量应该是什么（是否就是自检4 的 bar 对齐时间轴夏普），以及
"双尺度分裂并列成立"的结算是否应被推翻。

不要复述我的自检结论——独立判断它们是否成立，指出我可能的误读。


## 诊断要求

请执行严格根因诊断：
1. 失败现象：观测到什么？
2. 直接原因：哪行代码导致了失败？
3. 根本原因：为什么会写出那行代码？是设计错误还是实现疏忽？
4. 分类判定：实现错误 or 定义冲突？
5. 修复方案（仅限实现错误）：精确的修复步骤

如果是定义冲突，输出：
- **冲突方**：A 说什么 vs B 说什么
- **不可弥合理由**：为什么不能同时满足两边


## Response

**结论：判 (B) 归一化伪影。**  
`lq-sharpe-scale` 没有把命题②从 underpowered 推进到真否证；它把判据3的对象换成了“按信号等权的 `pnl/hold` 均值”，再把这个量误解释为“每 bar 可交易收益/夏普尺度”。

**诊断链**

失败现象：报告声称 `CV_r` 降到 2.69-6.01、`n_eff` 门槛降到 13-68，且 `LCB_r` 与 4 窗 walk-forward 全负，因此命题②在收益率/夏普尺度被证伪。这个结论写在 [lq-sharpe-scale-20260701.md](/Users/silencehan/Projects/NewChanlun/.chanlun/review-results/lq-sharpe-scale-20260701.md:13) 和 [lq-sharpe-scale-20260701.md](/Users/silencehan/Projects/NewChanlun/.chanlun/review-results/lq-sharpe-scale-20260701.md:86)。

直接原因：脚本在 [/tmp/lq_sharpe_scale.py](/tmp/lq_sharpe_scale.py:34) 定义 `r = pnl / hold`，随后在 [/tmp/lq_sharpe_scale.py](/tmp/lq_sharpe_scale.py:162) 对 `r` 的等权样本均值做 LCB，在 [/tmp/lq_sharpe_scale.py](/tmp/lq_sharpe_scale.py:196) 到 [/tmp/lq_sharpe_scale.py](/tmp/lq_sharpe_scale.py:206) 用 `CV_oos_r` 判定“有检出力→真否证”，walk-forward 也在 [/tmp/lq_sharpe_scale.py](/tmp/lq_sharpe_scale.py:223) 继续使用同一个 `r` 列表。

根本原因：统计对象错配。`mean(pnl/hold)` 是“每笔信号等权的平均斜率”，不是可实现策略的每 bar 收益。可交易时间收益至少应是 `sum(pnl) / sum(hold)`，或更严格地构造时间轴组合收益 `R_t = Σ active pnl_i/hold_i` 后算均值/Sharpe，并明确资本、并发和 exposure 归一化。

我直接用 `/tmp/btc_663_ledger_sigma.csv` 复核：  
`CV(pnl/固定平均hold)` 与 `CV(pnl)` 完全相同；只有除以逐笔 `hold_i` 后 CV 才骤降。并且关键符号翻转来自等权重排：例如 `(0,-1)` 的 `mean(pnl/hold)=-0.378`，但真实时间轴活跃组合均值为 `+0.0396`、Sharpe `+0.0275`；`(1,-1)` 为 `-0.530` vs `+0.0588`、Sharpe `+0.0524`。除 `(1,+1)` 本身 `mean_pnl<0` 外，正 PnL 单元的时间轴收益不是全负。

**关键判定**

1. CV 压缩对 `E_signal[pnl/hold]` 这个人工目标是真的，但对“可交易 alpha/每 bar 策略收益”的检出力是假的。`n_eff>(1.645·CV)^2` 只能用于它实际抽样的变量，不能把等权信号斜率的低 CV 转移成时间轴收益的高功效。

2. 符号翻转不是收益真的变负，而是抽样权重从“持有时间/资金暴露”变成了“每笔信号一票”。长持有赢家被降权，短持有输家被升权。更准确地说，这是 length-weighting / ratio-estimator 错配；称作 Jensen 型伪影可以，但根因是测度错了。

3. 可交易性应看 `(a)` 原始 PnL 的认证，或更好看 `(c)` 时间轴组合收益 Sharpe。`(b) mean(pnl/hold)` 可以作为描述性“单信号斜率”，但不能证伪可交易性。

4. “pnl inconclusive + r 证伪并列成立”应推翻。可以并列成立的是两个描述统计：`mean_pnl>0` 与 `mean_signal(pnl/hold)<0`。不能并列成立的是“r/夏普尺度已证伪可交易 alpha”。

5. 4 窗 walk-forward 不能救。代码每窗仍取 `r=pnl/hold` 等权列表；同一个错测度重复四次，只证明偏置稳定。按时间轴口径复核时，窗口符号是混合的，不是全窗一致负。

**分类判定：实现/报告设计错误，不是定义冲突。**  
修复不需要改变原始三判据定义，只需要把“收益率/夏普尺度”的实现对象改成可交易时间序列。

**修复方案**

1. 保留 `mean(pnl/hold)`，但降级命名为 `signal_equal_slope_mean`，不得称为“每 bar 策略收益”或“可交易 Sharpe”。  
2. 新增 `R_t` 构造：对每个 `(ℓ,q)` 和每个 OOS/WF 窗口，把每笔 `pnl_i/hold_i` 加到 `[entry, exit)` 的每个 bar；按资本/exposure 规则归一化。  
3. 用 `R_t` 计算 mean、Sharpe、block bootstrap LCB/UCB；block 单位应是时间 bar，不是交易行。  
4. 若要“证伪 μ>0”，报告 UCB≤0 或针对最小经济收益 `ε` 的反证检验；`LCB<0` 本身不是反证统计量。  
5. 当前命题②结算应退回：`pnl 尺度 underpowered=inconclusive；r 等权信号斜率不可作为证伪；正确时间轴尺度未给出真否证`。
