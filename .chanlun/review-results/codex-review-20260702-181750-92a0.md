# Codex review — 2026-07-02 18:17:50 UTC

## 元数据

- **mode**: review
- **subject**: 674号裁决C 落地实装——ledger.rs 双账本单向有损投影 + coverage.rs 646 rider 注
- **model**: codex-cli
- **timestamp**: 2026-07-02 18:17:50 UTC
- **context-file**: /tmp/codex-576c-review-ctx.md

## Prompt

## 审查目标

674号裁决C 落地实装——ledger.rs 双账本单向有损投影 + coverage.rs 646 rider 注

## 上下文

# 审查对象：674号（原576号）裁决C 落地实装——双账本单向有损投影

## 背景（674号裁决要点）

系统并存两个 machine-checked **不同构**的账本会计模型：
- 模型A `LedgerComp`（R=Π-A-W，财务守恒，可逆平移系统）
- 模型B `TwState`（TW=free+holding+withdrawn，缠师第31课取本金三阶段，stage 单向不可逆）

`formal/Origin/TotalWealth.lean` machine-checked 三条不同构反例：
`not_isomorphic_stage_collapses` / `not_isomorphic_conservation_switches` / `not_isomorphic_exogenous_price`。
`not_isomorphic_stage_collapses` 原文（Lean，行480-490）：

```
theorem not_isomorphic_stage_collapses : ∃ (s₁ s₂ : TWState),
    s₁.free = s₂.free ∧ s₁.holding = s₂.holding ∧ s₁.withdrawn = s₂.withdrawn
    ∧ s₁.stage ≠ s₂.stage
    ∧ s₁.tw = s₂.tw := by
  refine ⟨
    { free := 100, holding := 0, withdrawn := 0, ..., stage := TStage.costReduction, ... },
    { free := 100, holding := 0, withdrawn := 0, ..., stage := TStage.earningShares, ... },
    rfl, rfl, rfl, ?_, rfl⟩
  decide
```

编排者裁决立场C（拒 A/B 取 C）：**双层并置**——两个账本各自保留（互不合一），
之间**唯一被允许**的转换是**单向有损投影**（TW→R账本视角，遗忘 stage/legs 维度），
**禁止**任何暗示双向同构的命名/函数（如 `to_ledger_iso`）。

## 被审查代码：rust/src/theta_v0/strategy/ledger.rs

（完整摘录，含模块头注释、LedgerComp、TwState 关键片段、LedgerView、投影函数、测试）

```rust
//! 双账本分量——契约锚点 = Origin canonical（task #102 A′ Phase2 step8 重锚）。
//! ...
//! ## 双账本并置（task #90 不同构裁定 → task #93 闭环扩维 → #127 两端均锚 Origin）
//! `R=Π-A-W`（收益表视角，`Origin.FullDefinitionStrategy.LedgerState`）与
//! `TW=free+holding+withdrawn`（现金流+持仓视角，`Origin.TotalWealth.TWState`）**不同构**
//! （#90 已证，`Origin.TotalWealth` §3 三反例在 native 定义上重新成立：外生价格维度 / stage
//! 不可逆 vs 操作可逆 / 守恒律切换三条阻断）。完整持仓系统两者都需要——故 closed_loop 把两者
//! 并置在 AssemblyState，T 同步线程化两者（见 transition.rs）。#127 重锚后**两端均锚 Origin
//! canonical**：R=Π-A-W 锚 `Origin.FullDefinitionStrategy.LedgerState`，TW/OQ-9 锚
//! `Origin.TotalWealth`（不再有 legacy 锚点缺位）。
//!
//! ★674号裁决C 落地（codex ritual 2026-07-02，拒 A/B 取 C）：两账本**显式分离**（两独立结构
//! `LedgerComp`/`TwState`，各自 native 锚 Origin），二者间**唯一被允许的转换**是命名明确的**单向
//! 有损投影** [`forget_stage_to_ledger_view`]（TW→R账本视角，遗忘 stage/legs 维度）。**禁止**任何
//! 暗示双向同构的命名/函数（如 `to_ledger_iso`）——不同构是 L0 定理（#90 machine-checked），双向
//! 同构不存在。单向性由非单射测试 `projection_forgets_stage_non_injective` 锚定。

/// 取本金三阶段 `TStage`（契约锚 `Origin.TotalWealth.TStage`，缠师第31课）。
/// 单向不可逆迁移（OQ-9）：CostReduction(0) → CapitalRecovered(1) → EarningShares(2)。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TStage {
    CostReduction,
    CapitalRecovered,
    EarningShares,
}

impl TStage {
    pub fn rank(self) -> u32 {
        match self {
            TStage::CostReduction => 0,
            TStage::CapitalRecovered => 1,
            TStage::EarningShares => 2,
        }
    }
}

// TwState 字段（省略完整定义，字段集）：
// free: i64, holding: i64, withdrawn: i64, notional_in: i64,
// stage: TStage, open_legacy_legs: u32, cum_net_cash: i64, hwm_gain: i64
// tw() = free + holding + withdrawn

/// 账本分量 `LedgerComp`（契约锚 `Origin.FullDefinitionStrategy.LedgerState`，R=Π-A-W 恒等载体）。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct LedgerComp {
    pub i0: i64,
    pub pi: i64,
    pub a: i64,
    pub w: i64,
    pub r: i64,
}

impl LedgerComp {
    pub fn initial(i0: i64) -> LedgerComp {
        LedgerComp { i0, pi: 0, a: 0, w: 0, r: 0 }
    }
    pub fn inv_holds(&self) -> bool {
        self.r == self.pi - self.a - self.w
    }
}

/// TW 账本在 R 账本视角下的**有损投影目标** `LedgerView`（674号立场C：单向有损投影，非双向同构）。
///
/// 只承载 TW 的 **stage-无关财务标量**（free/holding/withdrawn/notional_in/cum_net_cash/hwm_gain）；
/// **遗忘**取本金状态机维度 `stage`（单向不可逆相位）与 `open_legacy_legs`（OQ-9 腿计数）——这两分量
/// 在 R=Π-A-W 账本里**无对应物**（R 账本是可逆平移系统，无阶段无腿）。
///
/// ★674号裁决C 的可执行护栏：投影**单向**（TW→view）**且有损**（stage/legs 丢失）。**不提供逆**
/// `view→TwState`——两个仅 stage 不同的 TwState 投到同一 view（[`forget_stage_to_ledger_view`] 的
/// 非单射测试 `projection_forgets_stage_non_injective`），故逆不存在（对齐 `Origin.TotalWealth.
/// not_isomorphic_stage_collapses` L0 反例）。禁止任何暗示双向同构的命名/函数（如 `to_ledger_iso`）。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct LedgerView {
    pub free: i64,
    pub holding: i64,
    pub withdrawn: i64,
    pub notional_in: i64,
    pub cum_net_cash: i64,
    pub hwm_gain: i64,
}

/// 单向有损投影 `forget_stage_to_ledger_view`（674号立场C）：TW→R账本视角，**遗忘 stage 维度**。
///
/// 名字明示遗忘（`forget_stage`）——非 `to_ledger_iso`（禁止暗示双向同构，674号）。丢弃 `stage`
/// （单向不可逆相位）与 `open_legacy_legs`（OQ-9 腿计数），保留财务标量。**有损 ⟹ 无逆**（不提供
/// `view→TwState`）。这是两个不同构账本范畴（#90/674号 machine-checked）之间**唯一**被允许的转换。
pub fn forget_stage_to_ledger_view(s: &TwState) -> LedgerView {
    LedgerView {
        free: s.free,
        holding: s.holding,
        withdrawn: s.withdrawn,
        notional_in: s.notional_in,
        cum_net_cash: s.cum_net_cash,
        hwm_gain: s.hwm_gain,
    }
}

// ── 测试（tests mod 内）──────────────────────────────────────────────────

/// ★674号立场C 投影单向性：`forget_stage_to_ledger_view` **非单射**（有损 ⟹ 无逆 ⟹ 单向）。
///
/// 两个仅 `stage` 不同的 TwState 投到同一 `LedgerView` ⟹ 不能从 view 反推 stage ⟹ 逆不存在
/// ⟹ 投影**单向**。这是 `Origin.TotalWealth.not_isomorphic_stage_collapses`（L0 反例：两态三量
/// 全同仅 stage 不同 ⟹ B→A 投影非单射）在 Rust 投影层的可执行兑现。禁止双向同构（无 `to_ledger_iso`）。
#[test]
fn projection_forgets_stage_non_injective() {
    let base = TwState {
        free: 100, holding: 500, withdrawn: 30, notional_in: 500,
        stage: TStage::CostReduction, open_legacy_legs: 2, cum_net_cash: 7, hwm_gain: 12,
    };
    // 仅 stage 不同 ⟹ 同一 view（stage 被遗忘 = not_isomorphic_stage_collapses）。
    let differ_stage = TwState { stage: TStage::EarningShares, ..base };
    assert_eq!(
        forget_stage_to_ledger_view(&base),
        forget_stage_to_ledger_view(&differ_stage),
        "仅 stage 不同 ⟹ 投影相同（stage 被遗忘，非单射 ⟹ 无逆）"
    );
    // 仅 open_legacy_legs 不同 ⟹ 同一 view（OQ-9 腿计数被遗忘）。
    let differ_legs = TwState { open_legacy_legs: 0, ..base };
    assert_eq!(
        forget_stage_to_ledger_view(&base),
        forget_stage_to_ledger_view(&differ_legs),
        "仅 open_legacy_legs 不同 ⟹ 投影相同（OQ-9 腿被遗忘，非单射）"
    );
    // 财务标量被保留（投影只丢状态机维度，不丢财务信息）。
    let v = forget_stage_to_ledger_view(&base);
    assert_eq!(
        (v.free, v.holding, v.withdrawn, v.notional_in, v.cum_net_cash, v.hwm_gain),
        (100, 500, 30, 500, 7, 12),
        "财务标量保留（stage-无关分量无损）"
    );
}
```

## 被审查代码2：rust/src/theta_v0/strategy/coverage.rs（646号 rider 注）

```rust
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct LegTarget {
    pub e_idx: usize,
    pub side: VoiceSide,
    /// s_e：目标单位数（role/depth 权重）。
    ///
    /// ★646号 rider（命名区分，不改语义）：`LegTarget.units` 是 **sizing 层资本加权目标敞口**
    /// （`base_units × w_depth`，f64 连续量），**不是** §9 voice 层的单位计数 `q_v`（手数，整数）——
    /// 二者是不同投影空间的量（646号裁决 CONFIRM：范畴错误，depth_weight 不等权应保留）。
    pub units: f64,
    pub role: OperationRole,
}
```

## 已核实的事实（供你复核，不要求信任）

1. **无隐藏逆**：全仓库 grep `forget_stage_to_ledger_view|LedgerView` 除 ledger.rs 本文件外**无其他引用**——
   没有任何函数试图从 `LedgerView` 反推 `TwState`，没有 `to_ledger_iso` 或类似命名。
2. **无生产消费者**：`forget_stage_to_ledger_view` 目前**没有任何调用点**（除测试外）——它是纯护栏/契约声明，
   未被 wire 进 `closed_loop`（AssemblyState/transition.rs）的实际数据流。
3. **测试与 Lean 定理逐字对齐**：Rust 测试 `projection_forgets_stage_non_injective` 的两个见证态
   （base: free=100/holding=500/withdrawn=30，differ_stage 仅改 stage）与 Lean `not_isomorphic_stage_collapses`
   的见证态结构一致（三财务量相同，仅 stage 不同 ⟹ tw 相同）——但 Lean 见证用 holding=0，Rust 见证用 holding=500，
   不是逐值复制，是**结构同构**的复现（三量相等+仅stage不同的存在性）。

## 审查焦点（请逐条给出判断）

1. **投影是否真正单向有损**：`LedgerView` 是否存在任何隐藏的逆构造路径（即使未被外部调用，
   是否有内部代码结构暗示可以从 view 反推 stage）？命名 `forget_stage_to_ledger_view` 是否
   完全避免"双向同构"暗示（对照禁令：不得出现 `to_ledger_iso` 等命名）？

2. **LedgerView 遗忘维度是否忠实对齐 `not_isomorphic_stage_collapses`**：该 Lean 定理的反例结构是
   "三个财务量相同 + stage 不同 ⟹ tw 相同"（即 TW 守恒量塌缩）。Rust 测试验证的是"整个 view
   结构体相等"（更强的断言：不仅 tw 相同，而且 free/holding/withdrawn/notional_in/cum_net_cash/hwm_gain
   逐分量相同）。这个更强断言是否仍然忠实于原定理的语义？是否存在过度声明或声明不足？

3. **投影无生产消费者是否可接受**：`forget_stage_to_ledger_view` 未被 wire 进 closed_loop（无实际调用点），
   仅作为契约护栏+测试存在。按 YAGNI/ponytail 判断，这是否可接受？还是必须提供一个实际消费点
   （如 closed_loop 里某处需要 R 视角时调用它）才算"完整实装"，否则属于死代码/声明膨胀（架构声明
   了转换存在，但代码库里没有任何地方真正需要这个转换）？

4. **646 rider 命名区分是否足够**：`LegTarget.units` 与 §9 voice 层 `q_v` 的范畴区分，仅靠文档注释
   （无类型层强制，如 newtype wrapper）是否足够防止未来误用（如某处不小心把 units 和 q_v 混用）？

请给出你的独立裁定：每条焦点 成立/不成立/部分成立，并给出具体理由和修复建议（如有）。


## 审查要求

请从以下角度审视代码：
1. 逻辑自洽性：函数/方法内部逻辑是否自洽？
2. 边界安全：边界条件、空值处理、异常路径是否完备？
3. 性能：是否有明显的性能瓶颈或反模式？
4. 惯用法：是否遵循语言/框架的惯用模式？
5. 定义忠实度：代码是否忠实反映了定义？（如果上下文中包含相关定义）

如果发现问题，请按以下格式输出：
- **问题**：精确描述
- **位置**：文件路径:行号
- **严重性**：致命 / 重要 / 建议
- **修复建议**：如何解决

如果没有发现问题，输出"无否定"并说明你检查了什么。

## 立场声明输出协议（必须遵守）

在你的回复末尾，附加以下 YAML 格式的结构化立场声明。
这是系统用于追踪代码审查过程中立场变化的协议，不可省略。

```yaml
---stance-declaration---
verdict: pass | fail | conditional
stances:
  <具体代码问题>: <你的立场（如 reject / accept / needs_work）>
concessions:
  - <如果你在本轮放弃了之前的某个质疑，在此列出>
---end-stance---
```

字段说明：
- `verdict`: 你对审查目标的整体判定
  - `pass`: 代码通过审查，无否定
  - `fail`: 代码未通过审查，存在问题
  - `conditional`: 有条件通过，部分问题存在
- `stances`: 你在本轮持有的具体质疑（KV 对）。每个 key 是具体代码问题，value 是你的立场
- `concessions`: 可选。如果你在本轮接受了之前质疑的某个点被 Opus 回应，列出被撤回的质疑

示例：
```yaml
---stance-declaration---
verdict: conditional
stances:
  error_handling: needs_work
  api_surface: accept
concessions:
  - hook_design
---end-stance---
```


## Response

**裁定：conditional**

1. 投影单向有损：成立，但命名有轻微过度声明。  
[ledger.rs:527](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/ledger.rs:527)-[552] 没有逆函数，也没有 `to_ledger_iso`。`forget_stage_to_ledger_view` 明确丢弃 `stage/open_legacy_legs`，测试覆盖非单射。否定点是 `LedgerView` 并不是 `LedgerComp`/`R=Π-A-W` 投影，它保留的是 TW 的 stage-free 标量。建议改名为 `StageFreeTwLedgerView` 或文档降格为“TW 的 R-compatible/stage-free view”，不要说成已经投到 R 账本状态。

2. 对齐 `not_isomorphic_stage_collapses`：成立。  
[ledger.rs:916](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/ledger.rs:916) 的 witness 比 Lean 反例更强，不违背原定理：Lean 要求 free/holding/withdrawn 相同且 stage 不同，Rust 额外保持 `notional_in/cum_net_cash/hwm_gain` 相同，只是在 Rust 扩展状态上给出更窄 witness。不是过度声明。注意 `hwm_gain` 不在 Origin `TWState` 内，所以文档若说“逐字对齐 Origin”不能把它混进去。

3. 无生产消费者：部分成立。  
作为“契约护栏 + 非单射测试”，当前可接受；不必为了显得完整而硬塞 closed_loop 调用。但它不能被宣传为生产路径已完成，因为 `forget_stage_to_ledger_view` 目前没有行为影响。修复建议：二选一，要么把文档写清楚“当前是 contract sentinel，无生产 consumer”；要么等真实 R 视角消费者出现时再 wire。不要造无需求消费点。

4. 646 rider：不成立，注释不够。  
[coverage.rs:1128](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1128)-[1138] 仍然暴露裸 `pub units: f64`，所谓“命名区分”实际只在注释里；而 voice 层 `q_v` 是 [voice.rs:117](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/voice.rs:117) 的手数语义。未来混用靠人脑防守。建议引入 newtype，例如 `CapitalWeightedUnits(f64)` / `VoiceLots(u32)`，或至少把字段名改成 `sizing_units` / `target_exposure_units`。同时给 `VoiceConfig.depth_weights` 或构造入口加 finite、非负校验，避免 NaN/负权重流入 [coverage.rs:1167](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1167) 后污染净额计算。

**问题**
- **问题**：`LedgerView` 名称和注释暗示 R 账本视角，但结构并未映射到 `LedgerComp/R=Π-A-W`。  
  **位置**：[ledger.rs:527](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/ledger.rs:527)  
  **严重性**：建议  
  **修复建议**：改名或降格文档，避免把 stage-free TW view 说成 R ledger state。

- **问题**：`LegTarget.units` 仅靠注释区分 sizing 敞口与 voice `q_v`，类型边界未闭合。  
  **位置**：[coverage.rs:1135](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/strategy/coverage.rs:1135)  
  **严重性**：重要  
  **修复建议**：newtype/字段重命名，并补 finite/nonnegative config 边界。

未运行测试；当前环境是只读审查。代码图 MCP 被取消，已用仓库文件和 `rg` 复核符号/调用关系。

```yaml
---stance-declaration---
verdict: conditional
stances:
  ledger_projection_single_direction: accept
  ledger_view_r_ledger_naming: needs_work
  stage_collapse_alignment: accept
  no_production_consumer: accept_as_contract_only
  legtarget_units_doc_only_boundary: reject
concessions: []
---end-stance---
```
