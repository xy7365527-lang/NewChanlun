# Codex review — 2026-07-02 18:19:38 UTC

## 元数据

- **mode**: review
- **subject**: A3设计稿——per-level dirty_from证书(§2)+04/03证书化(§3)+project_to_units_resume truncate修正(§2.5)的正确性/完备性/测试覆盖审查
- **model**: codex-cli
- **timestamp**: 2026-07-02 18:19:38 UTC
- **context-file**: /tmp/codex-review-a3-ctx.md

## Prompt

## 审查目标

A3设计稿——per-level dirty_from证书(§2)+04/03证书化(§3)+project_to_units_resume truncate修正(§2.5)的正确性/完备性/测试覆盖审查

## 上下文

# 审查上下文：A3 frontier 证书 + cached_units 快照设计稿

## 审查对象

设计稿全文（只读+设计，未落地，未 commit）：

---
（以下为 `.chanlun/review-results/a3-design-20260702.md` 全文）

# A3 设计稿 — frontier 证书 + cached_units 快照（护航组高风险，阶段1 只读+设计）

**工位**：泳道 A / ws-a3（algo-opt-plan-20260702.md §二泳道A A3 + §三护航组）
**认识论等级**：设计稿本身 L0（从代码不变量推导，不含数据）；落地后计时对照为 L1（CPU 度量零信息增量，231号）。
**性质**：本文只读+设计，不改代码、不 commit。实施须先过 codex-challenger 审（约束4 硬节点）。
**代码锚**：`rust/src/theta_v0/classifier/mod.rs`（主循环 954-1136）、`recursive_tower.rs`（compose_level_resume 396-426 / project_to_units_resume 458-471）。

## 0. 问题定位（A0 profile 校准后的真实靶）

A0 实测（1M bar，占分类器插桩总耗时）：

| 阶段 | 代码锚 | 1M ms | %插桩 | 性质 | A3 处置 |
|------|--------|-------|-------|------|---------|
| 05_compose_resume | mod.rs:1037 | 31281.2 | 60.0% | 中枢检测续扫（未拆解，黑箱） | 任务(a)：先拆解定性，不盲改 |
| 04_cached_units_copy | mod.rs:1006 | 4010.9 | 7.7% | 每 bar 全量 units 快照拷贝 | 任务(c)：证书 truncate+extend → O(tail) |
| 03_frontier_compare | mod.rs:986 | 2446.0 | 4.7% | frontier 窗全量比较 | 任务(b)：L1+ stable 从 0 抬到 dirty_from |

05（60%）是最大靶但是计算不是拷贝，A3 分两条独立线：(a) 05 拆解 profile-first，本稿不改 05；(b/c) 证书——03+04（合计 12.4% 插桩）降为 O(tail)。

## 1. 任务(a)：05_compose_resume 内部拆解方案（插桩设计，本稿不实施）

（略——与 codex 本次审查焦点弱相关，只需知道：05 是黑箱，本设计不动 05 本体，只加 env-gated 插桩测量）

## 2. 任务(b)：per-level dirty_from 证书（禁 did_extend 全局标志）

### 2.1 为什么禁 did_extend

`did_extend`（mod.rs:952）是全局 bool：任一级 extend 非空即置真。它无法定位"哪个下标以下不可变"。verdict 反例：pop-and-rescan 在 did_extend=false 时仍改写尾部——

主循环 :1024-1033：当 `had_emitted_window`（`resume_from < consumed`），pop 掉最后一个 center/upper_move（:1031-1032），compose 从 `resume_from` 重扫重新产出。若重扫在当前 bar 未新增窗口（tail 空 ⟹ did_extend 不置），但重扫产出的 frontier upper_move 值可能已变（bar-1464 seg[9] end 1384→1170 同类：段数不变、末段原地改写）。全局标志 did_extend=false 会误判"前缀未动"，漏掉尾部改写 ⟹ 投影/BSP memo 复用陈旧。

根因：正确的不可变前缀边界是每级一个下标，不是一个全局布尔。

### 2.2 证书 = 已存在的 prefix_count（最小改写下标，零新簿记）

主循环 :1044 已经在算这个下标——`compose_level_resume(..., lc.upper_moves.len())` 的第 6 参 `prefix_count`。它取值时机 = pop 块（:1031-1032）之后、extend（:1053）之前，正是"本级 upper_moves 经 pop 后的不可变前缀长度"：

- 无 pop 无 cascade：`prefix_count = 旧 upper_moves.len()`（前缀全稳）。
- had_emitted_window：`prefix_count = 旧 len − 1`（pop 掉 frontier 那一个，其余稳）。
- cascade_reset：upper_moves 已 clear（:996），`prefix_count = 0`（全重扫）。

证书定义：`prefix_count[L]` = 本级 upper_moves 的不可变前缀长度。它同时是：
1. 本级 `projected_units` 需重投影的起点（投影 `[prefix_count..]` 可能变，`[..prefix_count]` 必不变）。
2. 下一级 units 的不可变前缀 `dirty_from[L+1] = prefix_count[L]`。

为什么下标精确（含投影的前驱依赖）：`project_to_units` 的 `projected[idx] = fold_direction(moves[idx], moves[idx-1])`（recursive_tower.rs:443/462）。upper_moves`[..prefix_count]` 不变 ⟹ `projected[..prefix_count]` 不变（`projected[prefix_count-1]` 依赖 `moves[prefix_count-1]` 和 `moves[prefix_count-2]`，二者均在稳定前缀内）。`projected[prefix_count]` 依赖 `moves[prefix_count]`（extend 首元，可能新）⟹ 从 `prefix_count` 起脏。无需 −1 偏移，证书恰好落在 prefix_count。

### 2.3 L0 的 dirty_from

L0 的 units = `l0_units`（segment 投影），无父级 prefix_count。其不可变前缀 = mod.rs:856 的 `reuse = l0.segments_confirmed_len.min(l0_units_cache.len())`——parser confirmed 前缀跨 bar bit-stable（#106 证书，同 moves_tower_l0）。故 `dirty_from[0] = reuse`（与现 03 的 `stable = confirmed_len.min(scanned)` 同源，:981-982）。

### 2.4 证书线程（top-down，天然可达）

级别自顶向下处理（L0→L1→…），处理 L 时父级 L−1 已算完 prefix_count[L−1]：

```
loop 前：dirty_from = reuse            // L0
loop 内 L：
  用 dirty_from 做 03 stable + 04 keep（见 §3）
  … pop / compose(prefix_count=lc.upper_moves.len()) / extend …
  重投影 projected_units（truncate 到 prefix_count 再 append，见 §2.5）
  dirty_from = prefix_count            // 传给下一级 L+1
```

新增状态 = 主循环一个 `let mut dirty_from: usize`。无新 cache 字段、无全局标志。

### 2.5 顺带修正 project_to_units_resume 的 append-only 脆弱性（soundness 加固）

现状（脆弱但当前 sound）：`project_to_units_resume`（:1123）是纯 append（`for idx in cache.len()..moves.len()`），不感知 pop。had_emitted_window pop 后，projected_units 未 truncate，其 `[prefix_count]` 保留上 bar 值。当前之所以不炸：唯一会让该值陈旧的情形（frontier 改写）被 03 检出 ⟹ cascade ⟹ :1001 清空 projected_units 全重投影，pop 非 cascade 时重扫产出的 frontier bit-identical（窗口已 sealed 或前缀稳定），陈旧值恰好等于新值。这是隐式依赖 cascade 兜底的 subtle soundness，非显式证书。

证书化修正：重投影显式 truncate 到 prefix_count：

```rust
lc.projected_units.truncate(prefix_count);          // 丢弃 pop 掉的 frontier 投影
recursive_tower::project_to_units_resume(&lc.upper_moves[..], &mut lc.projected_units);
```

- bit-exact：pop 非 cascade 时重投影的 `[prefix_count]` 与旧值 bit-identical（同输入同 fold）⟹ 结果不变，只多算 1~3 个 frontier 元素（O(tail)，可忽略）。cascade 时 prefix_count=0，与现 clear+全投影一致。
- 加固：不再隐式依赖"cascade 会兜底 frontier 陈旧"，dirty_from 显式覆盖 pop 尾部。这消除 verdict 点名的 did_extend 类漏判。
- 现 :1001 的 `projected_units.clear()` 可保留（cascade 时 prefix_count=0，truncate(0)==clear，冗余但无害）。

## 3. 任务(c)：04 cached_units_copy 显式纳入证书半边

### 3.1 现状

```
03: frontier_mutated = units[stable..scanned] != cached_units[stable..scanned]   // 检测器
04: cached_units.clear(); cached_units.extend_from_slice(&units);                 // 每 bar 全量快照
```

- L0：`stable = confirmed_len.min(scanned)`（已用证书）。
- L1+：`stable = 0`（:984，保守全量比较，"不假定相等"）。
- 04：无论级别每 bar 全量拷贝 units → cached_units。

### 3.2 证书化 04（truncate + extend，O(tail)）

`cached_units` = 上 bar 的 units 快照。`units[..dirty_from]` 可证 == 上 bar units[..dirty_from]（§2.2），而 cached_units[..dirty_from] = 上 bar units[..dirty_from]（04 上 bar 所写）⟹ cached_units[..dirty_from] == 本 bar units[..dirty_from]，前缀无需重拷：

```rust
// 04 证书化
let keep = dirty_from.min(lc.cached_units.len()).min(units.len());
debug_assert!(
    units[..keep] == lc.cached_units[..keep],
    "dirty_from 证书违反：cached_units[..{keep}] 应等于 units 前缀（bit-exact 护栏）"
);
lc.cached_units.truncate(keep);
lc.cached_units.extend_from_slice(&units[keep..]);
```

- 终长约束（:969 截断语义）：`keep + (units.len() − keep) == units.len()` ⟹ `cached_units.len() == units.len()` 恒成立（含 units 回缩：keep ≤ units.len()，truncate 到 keep 再补到 units.len()）。
- debug_assert 全量比较作 debug 护栏：debug 构建跑 O(keep) 全前缀比较，证书若被违反立即炸；release 跳过（这就是省下的 4010ms 的来源）。

### 3.3 证书化 03（L1+ stable 抬到 dirty_from）

```rust
let stable = dirty_from.min(scanned);   // L0: reuse.min(scanned)（不变）；L1+: 父 prefix_count（从 0 抬起）
let frontier_mutated = units[stable..scanned] != lc.cached_units[stable..scanned];
```

- soundness：dirty_from 是可证不可变前缀 ⟹ `[..stable]` 不可能变 ⟹ 不比较该区间不漏 mutation。真正触发 cascade 的 mutation 恒在 frontier `[stable..scanned]`（bar-1464 末段在 scanned 内），仍逐值比。
- L0 无变化（reuse ≈ confirmed_len）；收益全在 L1+（原 stable=0 全窗比较 → 前缀跳过）。
- 与 04 共用 §3.2 的 debug_assert 护栏（同 dirty_from、同 cached_units），一处 debug 全比覆盖 03+04 两处。

## 4. bit-exact 论证汇总（codex 审重点）

| 声明 | 依据 | 翻转条件 |
|------|------|---------|
| dirty_from[L+1] = prefix_count[L] 是可证不可变前缀 | pop 只删末 1 / cascade clear 到 0 / 无 pop 保全长；投影 fold 前驱依赖落在稳定前缀内（§2.2） | 若 pop 改为删 >1 或改写非末尾 ⟹ 证书失效（当前 pop 恒 `.pop()` 单个，:1032） |
| 04 truncate+extend ≡ 全量 clear+extend | cached_units[..keep] == units[..keep]（§3.2）；终长 == units.len() | debug_assert 全前缀比较；GOLDEN digest |
| 03 stable=dirty_from ≡ stable=0 | 不可变前缀不含 mutation，frontier 窗仍全比 | 同上 debug_assert |
| 重投影 truncate(prefix_count) ≡ append-only | pop 非 cascade 时 frontier 投影 bit-identical；cascade 时 prefix_count=0 退化全投影 | GOLDEN + bit_exact_per_bar |

护栏三层：(1) release 保留的 debug_assert（debug/test 构建全前缀比较）；(2) GOLDEN digest（signal.rs:1666）；(3) 新增稀疏变异定向 oracle（§5）。

## 5. 验收（护航组强制）

1. `bit_exact_per_bar`（incremental.rs:116）+ `bit_exact_synthetic`（:164）+ `gen_fastpath_bit_exact_debug`（:1074）+ signal GOLDEN digest（:1666）全绿。
2. `cascade_reset_on_frontier_interior_rewrite`（mod.rs:1752）全绿——证书不得破坏 cascade 语义。
3. 新增定向 oracle：稀疏变异事件对拍。理由：`bit_exact_synthetic` 2000 bar 对 pop-and-rescan 覆盖弱。新测构造 had_emitted_window 反复触发（末段古怪线段重划序列，bar-1464 型），逐 bar 对拍"证书增量 04/03/09"vs"全量 clear+extend / stable=0 / clear+全投影"，assert 逐字段等。这是 did_extend 证伪的正向锁。
4. 计时对照（400K + 1M，THETA_PROFILE_STAGES=1）：03+04 目标 O(n)→O(tail)（占比 12.4%→近 0）；05 不在本半边验收。
5. debug 构建须跑 §3.2 debug_assert 不炸（证书正确性的 CI 层锁）。

## 6. 实施边界（阶段2，须过 codex 审后）

改动清单（预告，本稿不实施）：
- `recursive_tower.rs:396-426`：compose_level_resume 内加插桩（任务 a，独立）。
- `mod.rs` 主循环：新增 `let mut dirty_from`；03（:986-988 stable）、04（:1006-1009 truncate+extend + debug_assert）、09（:1122-1124 truncate(prefix_count)）；循环尾 `dirty_from = prefix_count`。
- 不动：05 compose 本体、cascade 语义、BSP memo、A1 的 Rc 半边。
- 依赖：A1 已落地（cached_bsp/centers Rc 化）；A3 与 A1 同文件串行，不并行。

---

## 相关生产代码原文（供核对设计claim与实码是否一致）

### mod.rs 主循环 940-1136（当前生产代码，未改动）

```rust
    let mut moves_tower: Rc<Vec<LeveledMove>> = moves_tower_l0;
    let mut cascade_reset = false;
    let mut did_extend = false;

    for level_idx in 0..=l_max {
        if units.len() < min_parts {
            break;
        }
        let is_l0 = level_idx == 0;
        if cache.levels.len() <= level_idx {
            cache.levels.push(LevelCache::default());
        }
        let lc = &mut cache.levels[level_idx];

        let scanned = (lc.scan_cursor.consumed + 2).min(units.len()).min(lc.cached_units.len());
        let stable = if is_l0 {
            l0.segments_confirmed_len.min(scanned)
        } else {
            0
        };
        let frontier_mutated = stage_profile::time("03_frontier_compare", || {
            units[stable..scanned] != lc.cached_units[stable..scanned]
        });
        if units.len() < lc.last_input_len || frontier_mutated {
            cascade_reset = true;
        }
        if cascade_reset {
            lc.scan_cursor = WindowScanCursor::default();
            Rc::make_mut(&mut lc.upper_moves).clear();
            Rc::make_mut(&mut lc.centers).clear();
            lc.cached_outcome = None;
            Rc::make_mut(&mut lc.cached_bsp).clear();
            lc.cached_bsp_key = None;
            lc.projected_units.clear();
        }
        lc.last_input_len = units.len();
        stage_profile::time("04_cached_units_copy", || {
            lc.cached_units.clear();
            lc.cached_units.extend_from_slice(&units);
        });

        let resume_start = lc.scan_cursor.resume_from;
        let had_emitted_window = lc.scan_cursor.resume_from < lc.scan_cursor.consumed;
        if had_emitted_window {
            debug_assert!(
                !lc.centers.is_empty() && !lc.upper_moves.is_empty(),
                "had_emitted_window ⟹ 至少一个已产出中枢可回退"
            );
            Rc::make_mut(&mut lc.centers).pop();
            Rc::make_mut(&mut lc.upper_moves).pop();
        }
        let (tail_centers, tail_upper, new_cursor) =
            stage_profile::time("05_compose_resume", || {
                compose_level_resume(
                    &units,
                    &moves_tower[..],
                    is_l0,
                    level_idx as u32 + 1,
                    resume_start,
                    lc.upper_moves.len(),
                )
            });

        did_extend |= !tail_upper.is_empty();
        stage_profile::time("06_extend_centers_upper", || {
            Rc::make_mut(&mut lc.centers).extend(tail_centers);
            Rc::make_mut(&mut lc.upper_moves).extend(tail_upper);
        });
        lc.scan_cursor = new_cursor;
        debug_assert!(
            lc.centers.len() == lc.upper_moves.len(),
            "增量塔：centers 与 upper_moves 一一对应（每窗口一中枢）"
        );

        tower_snapshots.push(std::mem::take(&mut moves_tower));

        let outcome = classify_move_incremental(&lc.centers, &mut lc.cached_outcome);
        let moves: Vec<MoveKind> = outcome_to_kind(outcome).into_iter().collect();

        let seg_len = if is_l0 { l0.segments.len() } else { 0 };
        let bsp_key = (lc.centers.len(), lc.upper_moves.len(), seg_len);
        let bsp: Rc<Vec<BspPoint>> = if lc.cached_bsp_key == Some(bsp_key) {
            stage_profile::time("07c_bsp_memo_clone", || Rc::clone(&lc.cached_bsp))
        } else {
            let mut b: Vec<BspPoint> = if is_l0 {
                stage_profile::time("07a_extract_signals_l0", || {
                    signal::extract_signals_with_hist(&lc.centers, &l0.segments, hist, &close_src)
                })
            } else {
                Vec::new()
            };
            let second = stage_profile::time("07b_extract_second", || {
                extract_second_for_level(&lc.upper_moves[..], hist, &close_src)
            });
            b.extend(second);
            b.sort_by_key(|p| p.source_index);
            let rc = Rc::new(b);
            lc.cached_bsp = Rc::clone(&rc);
            lc.cached_bsp_key = Some(bsp_key);
            rc
        };

        let level_centers = stage_profile::time("08_levels_centers_clone", || Rc::clone(&lc.centers));
        levels.push(LevelState {
            moves,
            centers: level_centers,
            bsp,
        });

        stage_profile::time("09_project_to_units_resume", || {
            recursive_tower::project_to_units_resume(&lc.upper_moves[..], &mut lc.projected_units);
        });
        units = stage_profile::time("10_projected_units_clone", || lc.projected_units.clone());

        if units.is_empty() {
            break;
        }
        moves_tower = Rc::clone(&lc.upper_moves);
    }
```

### recursive_tower.rs 380-471（当前生产代码，未改动）

```rust
pub fn compose_level_resume(
    units: &[UnitRange],
    subs_moves: &[LeveledMove],
    is_l0: bool,
    level: u32,
    start_i: usize,
    prefix_count: usize,
) -> (Vec<Center>, Vec<LeveledMove>, WindowScanCursor) {
    let build = if is_l0 {
        super::center::center_from_segments
    } else {
        super::center::center_from_window
    };
    let (windowed, cursor) = detect_centers_windowed_resume(units, build, start_i);
    let tail_centers: Vec<Center> = windowed.iter().map(|(c, _)| *c).collect();
    let tail_upper: Vec<LeveledMove> = windowed
        .iter()
        .enumerate()
        .map(|(i, (c, win))| {
            let subs = [
                subs_moves[win[0]].clone(),
                subs_moves[win[1]].clone(),
                subs_moves[win[2]].clone(),
            ];
            let id = ElementId { level, ordinal: (prefix_count + i) as u64 };
            LeveledMove::compose(&subs, *c, level, id)
        })
        .collect();
    (tail_centers, tail_upper, cursor)
}

pub fn project_to_units(moves: &[LeveledMove]) -> Vec<UnitRange> {
    moves
        .iter()
        .enumerate()
        .map(|(idx, m)| {
            let prev = if idx == 0 { None } else { Some(&moves[idx - 1]) };
            UnitRange {
                start_index: m.start_index,
                end_index: m.end_index,
                direction: m.fold_direction(prev),
                lo: m.rmove.lo(),
                hi: m.rmove.hi(),
            }
        })
        .collect()
}

/// 契约：调用方保证 moves 前缀（[..cache.len()]）与上次 append 一致（cascade_reset 清空 cache
/// 后从 0 重投影 ⟹ 退化为全量，bit-exact）。前缀缩/改写 ⟹ 调用方须先 cache.clear()。
pub fn project_to_units_resume(moves: &[LeveledMove], cache: &mut Vec<UnitRange>) {
    debug_assert!(cache.len() <= moves.len(), "投影缓存比 moves 长 ⟹ 前缀回缩未清空（违反契约）");
    for idx in cache.len()..moves.len() {
        let m = &moves[idx];
        let prev = if idx == 0 { None } else { Some(&moves[idx - 1]) };
        cache.push(UnitRange {
            start_index: m.start_index,
            end_index: m.end_index,
            direction: m.fold_direction(prev),
            lo: m.rmove.lo(),
            hi: m.rmove.hi(),
        });
    }
}
```

`WindowScanCursor`（recursive_tower.rs:324-330）：`consumed` = 扫描退出断点；`resume_from` = 最后一个成立窗口的起点（无成立窗口时 `resume_from == consumed`，即续进不回退）。`resume_from <= consumed` 恒成立（PDF §九增量等价定理）。

## 审计工位（ws-a3audit）自行验证的一条推导链（供 codex 复核，不代表结论，请独立核实）

针对设计稿 §2.5 "现状（脆弱但当前 sound）"一句，审计工位推导如下，请核实是否成立、是否有遗漏：

设某级本 bar 进入循环前 `lc.upper_moves.len() = N`（同时 `lc.projected_units.len() = N`，因上 bar 结束时两者已同步）。本 bar 若 `had_emitted_window && !cascade_reset`：
1. pop 后 `upper_moves.len() = N-1`，`prefix_count = N-1`。
2. `cascade_reset=false` 意味着 `frontier_mutated=false`，即对 L1+（`stable=0`）有 `units[0..scanned] == cached_units[0..scanned]`（bit-exact，04 写入的上 bar 快照）；且 `resume_from <= consumed <= scanned`（当 `consumed+2` 未被长度截断时）。因此 `units[resume_from..consumed]`（构成被 pop 掉的那个窗口的输入数据）在本 bar 与上 bar 完全一致。
3. `compose_level_resume` 是纯函数（同输入同输出，确定性左折叠），从 `resume_start=resume_from` 重扫的前缀数据不变 ⟹ 重扫必然**至少**重新产出与被 pop 掉的那个窗口 bit-identical 的第一个 tail 元素（同 `win` 下标、同 subs、故 compose 出的 `LeveledMove` 除 `id.ordinal` 外逐字段相同；`id.ordinal = prefix_count + 0 = N-1`，与被 pop 元素的原 ordinal 相同）⟹ `tail_upper.len() >= 1`。
4. 故 extend 后 `upper_moves.len() = N-1+T >= N`，即 `moves.len() >= cache.len()`（`project_to_units_resume` 的 debug_assert `cache.len() <= moves.len()` 不会因 pop 而违反）。
5. 但若 `T==1`（重扫仅复现被 pop 的那一个窗口，未产出更多），则 `moves.len() == N == cache.len()`，此时 `project_to_units_resume` 的 `for idx in cache.len()..moves.len()` range 为空——**完全不执行**，`projected_units[N-1]` 保留上 bar 的陈旧值。该陈旧值是否等于"新值"，依赖第 3 步"tail 首元素 bit-identical"的结论（是，因为 subs 和 id 都相同 ⟹ `LeveledMove` 逐字段相同 ⟹ `project_to_units` 的 `fold_direction(prev)` 计算的 prev 也相同 ⟹ 投影值相同）。若 `T>1`，则 `moves.len() > cache.len()`，for 循环只从 `cache.len()`（即 N）开始重投影，**跳过了 index N-1（即 prefix_count 位置)**——同样依赖第 3 步的"陈旧值=新值"结论才 sound。

**审计工位的问题**：上述推导是否有遗漏的第三种失效路径？特别是：(a) 若某一步 `scanned < consumed`（被 `.min(units.len()).min(lc.cached_units.len())` 截断）导致比较区间不能覆盖 `[resume_from..consumed]`，第 2 步的"数据不变"结论是否还成立？(b) `id.ordinal` 复用是否可能因为 `prefix_count` 计算时机（pop 后取值）与其他并发路径（如 cascade 同 bar 发生在其他 level）产生错位？(c) 当前生产代码"隐式依赖"这条推导链但没有显式断言/测试锁定它——如果未来任何一处改动破坏了第 3 步的"重扫确定性"前提（例如 05 拆解阶段——任务 a——引入了非确定性缓存或提前返回优化），这个 bug 会在 release 构建里静默产生错误结果而不 panic，因为 debug_assert 只锁 `cache.len() <= moves.len()`（数值关系），不锁"陈旧值语义上等于新值"（内容关系）。这是否应该在验收清单（设计稿 §5）中显式补一条"即使不落地 §2.5 的 truncate 修正，也要为这条隐式不变量补一个定向测试/debug_assert"？

## 审查任务

请针对本设计稿的 §2（任务b：dirty_from 证书）+ §3（任务c：04/03 证书化）+ §2.5（project_to_units_resume 修正）给出代码层异质审查，重点回答以下三个问题（团队 Lead 指定）：

1. **正确性**：「dirty_from[L+1] = 父级 prefix_count[L]（pop 后 extend 前取值）」是否正确？对照 `project_to_units_resume` 实码（`prev = moves[idx-1]`，1 阶前驱依赖），投影 fold 的前驱依赖是否恒落在稳定前缀内？

2. **完备性**：证书失效边界（设计稿 §4 表格列出的"pop 恒单删末尾 / fold 仅 1 阶前驱"两条翻转条件）是否完备？有没有第三种能让 `dirty_from` 证书失效的路径（例如多级级联时的边界情况、`l_max` 提前 break、`min_parts` 早停与 dirty_from 传递的交互、L0 的 `segments_confirmed_len` 与 `reuse` 语义边界等）？

3. **测试覆盖**：三层锁（release 保留的 debug_assert 全量比较 + GOLDEN digest + 新增稀疏变异定向 oracle）对 pop-and-rescan 场景的�covered 是否充分？特别是 bar-1464 型先例（frontier 段内改写、did_extend 曾经漏判的场景）是否被新 oracle 明确复现？还是否需要额外补充某类边界用例？

另外请评估审计工位在"自行验证的一条推导链"一节提出的 (a)(b)(c) 三个追问是否指向真实的设计缺口，还是可以被现有设计已经覆盖/排除。

给出：通过 / 需修改 / 退回，并列出具体理由和（如需修改）修改建议。


## 审查要求

请从以下角度审视代码：
1. 逻辑自洽性：函数/方法内部逻辑是否自洽？
2. 边界安全：边界条件、空值处理、异常路径是否完备？
3. 性能：是否有明显的性能瓶颈或反模式？
4. 惯用法：是否遵循语言/框架的惯用模式？
5. 定义忠实度：代码是否忠实反映了定义？（如果上下文中包含相关定义）

如果发现问题，请按以下格式输出：
- **问题**：精确描述
- **位置**：文件路径:行号
- **严重性**：致命 / 重要 / 建议
- **修复建议**：如何解决

如果没有发现问题，输出"无否定"并说明你检查了什么。

## 立场声明输出协议（必须遵守）

在你的回复末尾，附加以下 YAML 格式的结构化立场声明。
这是系统用于追踪代码审查过程中立场变化的协议，不可省略。

```yaml
---stance-declaration---
verdict: pass | fail | conditional
stances:
  <具体代码问题>: <你的立场（如 reject / accept / needs_work）>
concessions:
  - <如果你在本轮放弃了之前的某个质疑，在此列出>
---end-stance---
```

字段说明：
- `verdict`: 你对审查目标的整体判定
  - `pass`: 代码通过审查，无否定
  - `fail`: 代码未通过审查，存在问题
  - `conditional`: 有条件通过，部分问题存在
- `stances`: 你在本轮持有的具体质疑（KV 对）。每个 key 是具体代码问题，value 是你的立场
- `concessions`: 可选。如果你在本轮接受了之前质疑的某个点被 Opus 回应，列出被撤回的质疑

示例：
```yaml
---stance-declaration---
verdict: conditional
stances:
  error_handling: needs_work
  api_surface: accept
concessions:
  - hook_design
---end-stance---
```


## Response

结论：**需修改**。核心公式 `dirty_from[L+1] = prefix_count[L]` 在当前代码的一阶投影依赖下成立；但设计稿的失效边界不完备，测试验收也还不够锁住 pop-and-rescan 证书路径。

**问题**：`min_parts` / `units.is_empty()` 早停会留下更高级 `LevelCache` 陈旧，`stable=dirty_from` 可能跳过与旧 `cached_units` 的必要比较。  
**位置**：[mod.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/mod.rs:955)、[mod.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/mod.rs:1128)、[mod.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/mod.rs:411)  
**严重性**：重要  
**修复建议**：早停前截断/清空不可达高级缓存：进入循环前 `units.len() < min_parts` 时 `cache.levels.truncate(level_idx)`；投影后 `units.is_empty()` 时 `cache.levels.truncate(level_idx + 1)`。否则某级隔若干 bar 后重新出现时，它的 `cached_units` 不是上一 bar 快照，`dirty_from` 只证明父级相对上一 bar 的前缀稳定，不证明相对该级旧快照稳定。

**问题**：§4 的翻转条件不完备。除了“pop 单删末尾”和“fold 仅 1 阶前驱”，还必须列入：同源逐 bar/cache 不换流、`config.level` 不变、未到达级别缓存失效、`project_to_units` 不引入后继/全局依赖。  
**位置**：[mod.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/mod.rs:403)、[mod.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/mod.rs:827)  
**严重性**：重要  
**修复建议**：把这些写入 A3 §4，并给早停级别补测试。L0 的 `segments_confirmed_len` 语义本身可接受；全量 `parse_layer` 填 0，会退化全量，不是漏洞。

**问题**：§5 测试不足以证明新证书路径。现有 `project_to_units_resume_matches_full` 只测 append-only/clear，不测 `truncate(prefix_count)+append`；bar-1464 相关测试多为 ignored/诊断性质。  
**位置**：[recursive_tower.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/recursive_tower.rs:803)、[incremental.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/incremental.rs:520)、[incremental.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/backtest/incremental.rs:1071)  
**严重性**：重要  
**修复建议**：新增 always-run 合成 oracle，显式覆盖 `had_emitted_window`、`T==1`、`T>1`、长度回缩后再达 `min_parts`、高级别跳过后重入。每 bar 断言：`cached_units == units`、`projected_units == project_to_units(upper_moves)`、证书路径输出等于强制全量路径。真实 CL bar-1464 可保留 ignored，但不能作为唯一锁。

**问题**：设计稿把 `debug_assert!` 写成“release 保留”的护栏，事实不成立。  
**位置**：A3 §3.2 / §4 拟落地断言；对应当前 03/04 在 [mod.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/mod.rs:987) 和 [mod.rs](/Users/silencehan/Projects/NewChanlun/rust/src/theta_v0/classifier/mod.rs:1007)  
**严重性**：建议  
**修复建议**：改称 debug/test 护栏，并把 debug CI 明确列为验收；若要 release 运行，使用 env-gated `assert!` 或采样校验，不能叫 `debug_assert!` release 锁。

指定问题回答：

1. `dirty_from[L+1] = prefix_count[L]` 正确。对 `idx < prefix_count`，`project_to_units` 只读 `moves[idx]` 和 `moves[idx-1]`；二者都在稳定前缀内，所以不需要 `prefix_count - 1`。`idx == prefix_count` 才是首个可能脏点。

2. 完备性不通过。多级 cascade 同 bar 内本身没问题，因为下级 reset 后传上来的 `prefix_count=0`；`l_max`/config 变化也由现有契约要求 clear。真正缺口是早停级别的持久缓存，以及设计稿未把 cache 血缘契约列为证书前提。

3. 测试覆盖不充分。设计稿说“新增稀疏变异 oracle”方向正确，但必须具体到 pop-and-rescan 的 `T==1/T>1`、03/04 证书快照、09 truncate 投影，以及 bar-1464 型 frontier 段内改写的可复现路径。

审计工位追问：  
(a) `scanned < consumed` 在正常同源、未 shrink 路径下不是缺口；shrink 会触发 cascade。相关真实缺口是“级别被跳过后 cached_units 不是上一 bar 快照”。  
(b) `id.ordinal` 复用无并发错位；循环串行，cascade 先 clear，prefix_count 会是 0。  
(c) 是真实风险；若不落地 §2.5，必须补内容级 debug/test。若落地 §2.5，也要补 `projected_units == project_to_units(upper_moves)` 的定向锁。



---stance-declaration---
verdict: conditional
stances:
  dirty_from_prefix_count_correctness: accept
  fold_prev_dependency_no_minus_one: accept
  min_parts_early_break_stale_higher_cache: reject
  completeness_table_missing_cache_lineage_and_skipped_levels: needs_work
  project_to_units_truncate_test_coverage: needs_work
  debug_assert_as_release_guard: reject
  audit_scanned_less_than_consumed: accept_with_early_break_caveat
  audit_id_ordinal_reuse: accept
  audit_implicit_append_only_invariant: needs_work
concessions: []
---end-stance---
