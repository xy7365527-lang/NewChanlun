# Codex review — 2026-07-03 07:47:41 UTC

## 元数据

- **mode**: review
- **subject**: 全定义互斥策略严格数学证明文档（proofs-full-strategy-20260703.md）阶段三 codex 侧异质审——证明推导链逻辑洞/锚点语义相符/认识论等级诚实性/OPEN义务完备性/互斥穷尽边界条件
- **model**: codex-cli
- **timestamp**: 2026-07-03 07:47:41 UTC
- **context-file**: /tmp/codex-review-ctx.md

## Prompt

## 审查目标

全定义互斥策略严格数学证明文档（proofs-full-strategy-20260703.md）阶段三 codex 侧异质审——证明推导链逻辑洞/锚点语义相符/认识论等级诚实性/OPEN义务完备性/互斥穷尽边界条件

## 上下文

# 审查任务：全定义互斥策略严格数学证明文档——阶段三异质审（codex侧）

## 背景

审查对象：`docs/formal-chain/proofs-full-strategy-20260703.md`（HEAD=ef273ac7fb 版全文，任务 #131 阶段三交付物）。
这是缠论量化策略 theta_v0 的形式化证明整合文档，覆盖：
- §A 全定义唯一性定理（PDF §13，11 条假设 ⟹ ∀x ∃!O_{t+1}=π_Θ(x)）
- §B 策略互斥定理（PDF §14，Σ_j 1[C_j]=1，固定优先级互斥化）
- §C 各层不变量（bit-exact 增量塔、AncOK 祖先包含、区间套、运行时守恒、TW 账本）
- §D 证明义务登记表（8 条，6 CLOSED + 残余 OPEN 子项）
- §E/§F 结果包

审前必知（已知验证态边界，非声明膨胀）：假设 1（高级别 bit-exact）的 L2 锚点是 `#[ignore]` 真实数据测试的历史手工跑（commit c546b5633c），CI 不自动跑——文档 §F-3 已披露。

## 审查维度（五项，请逐项给出发现）

1. **证明推导链是否有逻辑洞**（假设→结论的跳步/循环论证）——尤其关注 §B 互斥定理的证明是否真的独立于实装（是否循环论证），以及 §A 的 11 假设复合链是否每一步都严格保持"全函数+唯一"性质。
2. **锚点与陈述是否语义相符**（非仅存在——数字对不对、函数做的事情是否真的对应文档声称的数学内容）。
3. **认识论等级标注是否诚实**（L0/机器证明/L2 有无冒充，是否有 L1 管线正确性被包装成经验验证）。
4. **OPEN 义务登记是否完备**（§D 8 条登记表，有无未登记的缺口——尤其"残余诚实缺口"部分是否掩盖了实质性问题）。
5. **互斥+穷尽证明的边界条件**（§B 的 2^10 穷举是否真的覆盖所有情形；§A 的 K_Θ≠∅/LexArgmin 键单射等边界条件是否有反例空间）。

## 已知发现（审查前置——请独立验证并判断严重性，不要因为我已经发现就跳过复核）

### 发现1：§A 与 §E 对同一"∃!机器证明看守"锚点引用了两个不同且都不准确的行号

- 文档 §A 开头（"证明结构"小节）与 §A 结论均引用：`rust/src/theta_v0/strategy/coverage.rs:4457` `pi_theta_step_deterministic_unique_order`。
- 文档 §E（"边界条件"第1条）引用同一函数为：`coverage.rs:3571`。
- 实际 grep 验证：该函数当前在 `coverage.rs:4491`（既不是 4457 也不是 3571）。
- 3571 与 4491 相差 920 行，明显是很早期（阶段一，m=8 时期）就存在的引用，从未在阶段二重验中更新。
- 该函数内容确认存在且语义相符（测试 `pi_theta_step_deterministic_unique_order`：同输入两次调用 `pi_theta_step` 比较订单是否逐字段相等，符合"∃!结论=决定性"的证明看守语义）。

### 发现2：§E 引用的互斥穷尽测试函数名在当前代码库中不存在

- 文档 §E（"边界条件"第2条）写道："若存在谓词组合 Σ≠1 ⟹ `mutex_total_exhaustive_2pow8` 红"。
- 实际 grep 全仓库：只存在 `mutex_total_exhaustive_2pow10`（`mutex.rs:296`），从未存在过 `_2pow8` 命名的函数。
- 文档正文 §B（第 198-211 行）与 §D-5 明确使用的是 `mutex_total_exhaustive_2pow10`（m=10，因为 #124 把 P1..P8 重分解为 P1..P10）。
- §E 同段还写"9 场景桶级等价机器证明"，而 §B/§D-5 正文明确是"12 场景"（`shadow_fold_bucket_equivalence`，mutex.rs:574）。
- 文档头部声明（第 11 行）："§A/§B/§C 受 q2 改动影响的锚点行号已全部重新 Read 验证并修正漂移（interp.rs / coverage.rs / mutex.rs / econ_positive.rs / transition.rs 五文件）"——**注意这里没有提到 §E**。
- 判断请求：这是否构成"OPEN 义务登记不完备"或"锚点语义不符"的实质问题？§F（阶段二结果包）对自己的"阶段二时点记录"有显式历史标注（第387行"本节正文保留为阶段二时点记录"），但 §E 没有任何类似的"此节为阶段一遗留、未随阶段二更新"的声明——这是否是声明膨胀的一种（090号：残留过时表述未被诚实标注为过时）？

## 已验证准确的锚点（对照组，供交叉判断残余锚点整体可信度）

以下锚点经 grep + Read 核实，行号与语义均与文档陈述一致：

- `ledger.rs:364` `TwEvent::Realize(i64)` — 准确
- `ledger.rs:676` `tw_step_preserves_tw` — 准确
- `ledger.rs:735` `tw_step_realize_drift_equals_dpi` — 准确
- `ledger.rs:264` `eta_star` — 准确
- `runner.rs:2148` `pi_loop_realized_profit_reaches_earning_shares` — 准确
- `runner.rs:2550` `earning_shares_unreachable_l0_same_price_zero_pnl` — 准确
- `runner.rs:2102` `tw_ledger_producer_drift_equals_quantized_realized_pnl` — 准确
- `mutex.rs:143` `mutex_class` — 准确
- `mutex.rs:296` `mutex_total_exhaustive_2pow10` — 准确
- `mutex.rs:574` `shadow_fold_bucket_equivalence` — 准确
- `coverage.rs:2155` `feasible_candidates` — 准确
- `nest.rs:200/205` `n_delta`/`n_delta_rec` — 准确
- `nest.rs:62` `is_sub` — 准确
- `six_state.rs:155` `six_state_exhaustive_and_bot_dimension` — 准确

## 关键代码全文（供逻辑推导链核实）

### mutex.rs 模块头 + mutex_class + 穷尽测试（§B 互斥定理核心实装）

```rust
// mutex.rs:1-150（模块文档 + Predicates + mutex_class 实装）
//! 全互斥买卖点解释器——固定优先级互斥化 `C_j = P_j ∧ ⋀_{k<j} ¬P_k`
//! 定理 1（alpha2 Doc2 §9）：Σ_{j=0}^m 1[C_j] = 1（全互斥 + 全定义）。
//! 证明：若无 P_j 成立则 C_0 唯一成立；否则取最小成立索引 r，则 C_r 唯一成立
//! （所有 j<r 因 P_j=0 不成立，所有 j>r 因 ¬P_r=0 不成立）。

pub const M: usize = 10;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum MutexClass {
    C0,
    Cj(u8),
}

pub struct Predicates {
    pub risk_liquidate: bool,      // P1
    pub close_overlay: bool,       // P2
    pub tw_withdraw: bool,         // P3
    pub tw_enter_earning: bool,    // P4
    pub close_root: bool,          // P5
    pub reduce_core: bool,         // P6
    pub close_short_diff: bool,    // P7
    pub open_root: bool,           // P8
    pub open_short_diff: bool,     // P9
    pub record_struct_break: bool, // P10
}

impl Predicates {
    fn p(&self, j: usize) -> bool {
        match j {
            1 => self.risk_liquidate,
            2 => self.close_overlay,
            3 => self.tw_withdraw,
            4 => self.tw_enter_earning,
            5 => self.close_root,
            6 => self.reduce_core,
            7 => self.close_short_diff,
            8 => self.open_root,
            9 => self.open_short_diff,
            10 => self.record_struct_break,
            _ => unreachable!(),
        }
    }
}

pub fn mutex_class(p: &Predicates) -> MutexClass {
    for j in 1..=M {
        if p.p(j) {
            return MutexClass::Cj(j as u8);
        }
    }
    MutexClass::C0
}

#[cfg(test)]
mod tests {
    /// 穷举全部 2^10=1024 谓词组合，断言每个组合恰好一个 C_j 成立。
    /// 本测试独立重算每个 C_j 的指示函数（不调 mutex_class，避免循环论证），
    /// 逐组合求和断言 ==1，并交叉验证命中类号与 mutex_class 一致。
    #[test]
    fn mutex_total_exhaustive_2pow10() {
        for bits in 0u32..(1 << M) {
            let p = decode(bits);
            let mut sum = 0usize;
            let mut hit_class: Option<MutexClass> = None;
            let any = (1..=M).any(|j| p.p(j));
            if !any {
                sum += 1;
                hit_class = Some(MutexClass::C0);
            }
            for j in 1..=M {
                let prefix_all_false = (1..j).all(|k| !p.p(k));
                let cj = p.p(j) && prefix_all_false;
                if cj {
                    sum += 1;
                    hit_class = Some(MutexClass::Cj(j as u8));
                }
            }
            assert_eq!(sum, 1, "bits={bits:010b}: Σ_j 1[C_j]={sum} ≠ 1");
            assert_eq!(mutex_class(&p), hit_class.unwrap(), "实装 ≠ 独立重算");
        }
    }
}
```

**审查要点**：`mutex_class` 实装（取最小成立索引 j，找不到则 C0）与穷举测试的"独立重算"逻辑（`prefix_all_false = (1..j).all(|k| !p.p(k))`）是否真的是两条独立路径，还是本质同构因而穷尽测试仍构成某种弱循环论证？请评估。

### nest.rs 区间套递归核（假设 3 实装）

```rust
pub fn n_delta(&self) -> bool {
    Self::n_delta_rec(self.side, &self.terminal, &self.base_interval, &self.rungs)
}

fn n_delta_rec(side: Side, terminal: &BspBits, base: &NestInterval, rungs: &[NestRung]) -> bool {
    match rungs.split_first() {
        // 基例 ℓ=e：N^δ_{e↓e} = Conf^δ_e。
        None => terminal.confirm_side(side),
        // 递归步 ℓ>e：Cand^δ_ℓ ∧ [J^δ_{ℓ-1} ⊆ J^δ_ℓ] ∧ N^δ_{ℓ-1↓e}。
        Some((top, rest)) => {
            let child = rest.first().map_or(base, |r| &r.interval);
            top.cand
                && is_sub(child, &top.interval)
                && Self::n_delta_rec(side, terminal, base, rest)
        }
    }
}
```

审查要点：`rungs` 递归消耗（`split_first`）保证终止性（每步严格缩短列表），基例返回 `terminal.confirm_side`。请评估这个递归结构是否真的实现了文档声称的
`N^δ_{ℓ↓e}=Cand^δ_ℓ ∧ [J^δ_{ℓ-1}⊆J^δ_ℓ] ∧ N^δ_{ℓ-1↓e}`（尤其 child 的选取逻辑 `rest.first().map_or(base, ...)` 是否总是对应正确的下一级区间）。

### transition.rs stage_progression（假设 8，TW 三阶段状态机，白名单/黑名单边界）

```rust
/// ★★stage 驱动字段边界（codex GAP3 裁定 A' 推导链第 6 条，清单⑧——白名单/黑名单硬边界）：
/// - 白名单：free、holding、withdrawn、notional_in、open_legacy_legs、risk_mode。
/// - 黑名单：hwm_gain（未实现峰值）、当前 MTM equity、forced_pnl（报告用假设强平）、任何未平仓路径依赖浮盈。
pub(crate) fn stage_progression(policy: &RiskPolicy, s: &TwState, risk_mode: RiskMode) -> Option<TwEvent> {
    let risk_normal = matches!(risk_mode, RiskMode::Normal);
    match s.stage {
        TStage::CostReduction => {
            if s.notional_in > 0 && s.holding >= s.notional_in {
                let recover_target = (s.notional_in - s.withdrawn).max(0);
                // 足额一次性退本金：仅当 free ≥ recover_target 时才派 RecoverCapital(recover_target)；
                // 否则等待（None）让重估浮盈继续累积 free。
                // ...（省略后续分支，见 transition.rs:330+）
            }
        }
        // ...
    }
}
```

审查要点：white/black-list 边界声明是否被代码结构性保证（即：函数签名/字段访问范围是否让"黑名单字段进入判据"在编译期或至少 review 时可检测），还是仅靠注释约束（后续人为改动可能悄悄违反且无测试兜底）？

## 请给出的结果

按 review 模式标准输出：分级发现列表（BLOCKER/CONCERN/PASS），每条给出：位置、问题描述、建议修复方式（如适用）、以及你对"发现1/发现2是否构成阶段三审查应打回的问题"的独立判断（不要因为我已给出初步分析就直接采纳，请独立评估严重性并说明理由）。


## 审查要求

请从以下角度审视代码：
1. 逻辑自洽性：函数/方法内部逻辑是否自洽？
2. 边界安全：边界条件、空值处理、异常路径是否完备？
3. 性能：是否有明显的性能瓶颈或反模式？
4. 惯用法：是否遵循语言/框架的惯用模式？
5. 定义忠实度：代码是否忠实反映了定义？（如果上下文中包含相关定义）

如果发现问题，请按以下格式输出：
- **问题**：精确描述
- **位置**：文件路径:行号
- **严重性**：致命 / 重要 / 建议
- **修复建议**：如何解决

如果没有发现问题，输出"无否定"并说明你检查了什么。

## 立场声明输出协议（必须遵守）

在你的回复末尾，附加以下 YAML 格式的结构化立场声明。
这是系统用于追踪代码审查过程中立场变化的协议，不可省略。

```yaml
---stance-declaration---
verdict: pass | fail | conditional
stances:
  <具体代码问题>: <你的立场（如 reject / accept / needs_work）>
concessions:
  - <如果你在本轮放弃了之前的某个质疑，在此列出>
---end-stance---
```

字段说明：
- `verdict`: 你对审查目标的整体判定
  - `pass`: 代码通过审查，无否定
  - `fail`: 代码未通过审查，存在问题
  - `conditional`: 有条件通过，部分问题存在
- `stances`: 你在本轮持有的具体质疑（KV 对）。每个 key 是具体代码问题，value 是你的立场
- `concessions`: 可选。如果你在本轮接受了之前质疑的某个点被 Opus 回应，列出被撤回的质疑

示例：
```yaml
---stance-declaration---
verdict: conditional
stances:
  error_handling: needs_work
  api_surface: accept
concessions:
  - hook_design
---end-stance---
```


## Response

**结论：fail。阶段三不应通过。**

本地工作区为空，codebase-memory MCP 未能列出项目；以下基于你给出的已核验锚点、代码片段和文档引用关系审查。

## BLOCKER

- **问题**：§E 仍引用不存在的 `mutex_total_exhaustive_2pow8`，并写“9 场景桶级等价”，而当前证明锚点是 `mutex_total_exhaustive_2pow10` 和 12 场景。这不是单纯行号漂移，是证明证据对象和覆盖规模写错。
- **位置**：`docs/formal-chain/proofs-full-strategy-20260703.md` §E 边界条件第 2 条；实际锚点 `rust/src/theta_v0/strategy/mutex.rs:296`、`:574`
- **严重性**：致命
- **修复建议**：改为 `2^10=1024`、`mutex_total_exhaustive_2pow10`、`12 场景`；如果 §E 是历史记录，必须像 §F 一样显式标为历史不可作为当前证据。否则这是锚点语义不符 + 残留过时表述未诚实标注，阶段三应打回。

## CONCERN

- **问题**：§A / §E 对 `pi_theta_step_deterministic_unique_order` 的行号分别写 `4457`、`3571`，实际为 `coverage.rs:4491`。函数存在且语义接近，但锚点声明错误。
- **位置**：`docs/formal-chain/proofs-full-strategy-20260703.md` §A、§E；实际 `rust/src/theta_v0/strategy/coverage.rs:4491`
- **严重性**：重要
- **修复建议**：修正行号，或改用符号锚点加 commit hash。该问题本身不推翻证明，但足以阻止“锚点已全部重验”的阶段三验收。

- **问题**：`pi_theta_step_deterministic_unique_order` 只能证明“同一实现同一输入两次返回一致”，不是数学意义上的 `∃!` 唯一性证明。它不能排除“实现选了一个结果，但抽象定义允许多个候选”的情况。
- **位置**：`coverage.rs:4491`；文档 §A “∃!机器证明看守”
- **严重性**：重要
- **修复建议**：把它降级标注为 L2/L1 决定性回归；§A 需要单独给出 `K_Theta != empty`、LexArgmin 全序/确定 tie-break、候选到订单映射单值的证明或 OPEN 项。

- **问题**：`mutex_total_exhaustive_2pow10` 对 `mutex_class` 不直接循环调用，数学上覆盖 10 个布尔谓词的 `2^10` 组合；但它复用 `Predicates::p`，所以不能证明 `p(j)` 映射本身无错，也不能证明各 `P_j` 的缠论语义正确。
- **位置**：`mutex.rs:143`、`mutex.rs:296`
- **严重性**：重要
- **修复建议**：文档改称“布尔互斥骨架穷尽证明 + 实装交叉检查”；若要增强独立性，测试内直接用字段数组重算，不通过 `p.p(j)`。

- **问题**：`n_delta_rec` 的递归结构只在 `rungs` 已按从高到低相邻层排序、且 `base_interval` 确为 `J_e` 时忠实实现定义。代码本身没有检查层级相邻性或顺序。
- **位置**：`nest.rs:200`、`nest.rs:205`
- **严重性**：重要
- **修复建议**：把 rung 顺序/相邻性列为显式前提，或在构造处断言；否则 §A 假设 3 不能只锚定该递归核。

- **问题**：`stage_progression` 的白名单/黑名单边界靠注释，不靠类型或测试约束。函数接收完整 `TwState`，后续代码可直接读取黑名单字段且编译器不会阻止。
- **位置**：`transition.rs` `stage_progression`
- **严重性**：重要
- **修复建议**：若文档声称“结构性保证”，改成窄输入投影类型；否则降级为 review 约束，并补一个黑名单字段扰动不影响输出的最小测试。

## PASS

- §B 的互斥定理作为纯布尔定义成立：有最小成立索引则唯一命中 `C_r`，无谓词成立则唯一命中 `C0`。
- `2^10` 穷举覆盖所有 10 位布尔组合；不覆盖谓词语义、数据构造语义或策略定义忠实度。
- 给出的 `n_delta_rec` 终止性自洽；每步 `split_first()` 严格缩短切片。
- 未见明显性能问题；这些路径规模小，1024 穷举和递归切片检查不是瓶颈。

---stance-declaration---
verdict: fail
stances:
  "§E stale mutex anchor and scenario count": reject
  "§A pi_theta_step_deterministic_unique_order bad line anchors": needs_work
  "determinism test claimed as ∃! mathematical proof": reject
  "mutex exhaustive test independence scope": needs_work
  "n_delta_rec ordering precondition": needs_work
  "stage_progression whitelist blacklist structural guarantee": needs_work
concessions: []
---end-stance---
