# 完整裁决（综合）：缠论完整实装 · 五问 · 2026-07-01

> 编排者要求：把所有 codex 裁决综合成一份完整裁决。
> 来源：codex-w1-verdict / codex-gap3-verdict / codex-recheck-completeness-p4（真 codex CLI 0.142.5，非同质降级）+ full-implementation-gap-audit + gap3-pdf-landing-plan + genealogist pending 分类 + w1-rework 完成报告。
> 认识论：全部 L0/L1（代码事实 + 概念裁决，非 L2 alpha 有效性——alpha 待 W-VERIFY 重跑）。异质裁决≠实施授权：codex 推荐的选择类项（576/η⋆）仍待编排者最终确认。

## 一句话结论

之前所有 alpha 否证（奇偶交替/σ_higher/(ℓ,q)三判据/663）都建立在**简化实装**上，有效域必须限定为"**无区间套跨级递归定位 + 口径4 出场 + 类型混合池**"——不能外推"完整缠论无 alpha"。使该否证**有效**的最小充分集 = **P1（区间套）+ P4（类型分桶）+ P7（正规出场）**。P1 已由 w1-rework 修复（多级 N^δ 真进生产路径），P4/P7 是小改动，GAP3 提现端另需 576 账本裁决解锁。

## 五问裁决

### A · W1 区间套：单级门 vs 多级递归 → **返工正确，多级不可拆**
codex 裁决：单级 DivCand 门**不是**忠实的 `Cand^δ` 实装；多级 N^δ 递归**必须**、**不可拆成渐进阶段**——J 嵌套链是构成性的（constitutive），非可选。Lead 自决返工判定**正确**（定理类，非选择类；no-patch"不可分定义禁止渐进回避"成立）。
状态：**已修复**。w1-rework 的 `build_multilevel_nest_cert`（econ_positive.rs:341）从塔逐级构造 rungs 真调 `n_delta`，第 229 行单级门已替换，10/10 测试含多级 J 嵌套/Cand=0 传播/dir=−δ。假否证根源"区间套零调用"根除。

### B · GAP3 · 576 账本不同构 → **codex 选 C（双账本并置）**
`R=Π−A−W`（closed_loop 主账本，W 恒 0）vs TW 三阶段（提现端 W）不同构。codex 裁 **C：双账本并置**——R 和 TW 各管自己的语义层，接口对齐（与 planner 一致）。非 A（单账本降 TW 为观测）、非 B（单 TW）。
→ **选择类，待编排者确认**（codex+planner 双荐 C）。

### C · η⋆ canonical 形式 → **状态依赖 barrier，非常量**
codex 裁决：`η⋆(x_t) = L_wc_{t+1} + κ·Q_t`（状态依赖风险 barrier），**不是**硬编码常量（如 10000）。κ = 每股成本下限风险参数，**须声明、不可从价格数据推导**。
→ **待编排者定 κ 值**（风险参数，只有你能设）。

### D · 完整实装最小充分集 → **P1+P4+P7 确认无遗漏**
codex 真复核确认（推翻此前同质降级的不确定性）：单标的旧缠论"完整"= 知识库定义/Rust实装/回测调用**三贯通**。最小充分集 = **P1+P4+P7**，无遗漏。P3（§12 等价关系）是跨标的另一维度，单标的回测定义上不覆盖，不阻塞。P2（力度背驰）= §9.3 已授权 MACD 面积为最完整可编码判据，**不存在**超越它的"完整力度背驰"定义——真问题不是换算法，是**一票否决门 = 选择偏差**（signal.rs:286 return None 使未过候选永不进样本、永不可检验）。

### E · P4 类型抹除 → **复核确认：字段透传已完成，缺分桶**
codex 真复核确认同质诊断：`SignalDecomp.bsp_class` + CSV 已输出（字段透传完成）；**真缺口 = μ̂ 分桶键 `(level,δ)` 不含 bsp_class**（econ_positive.rs:503），导致混合池稀释（buy1/buy2/buy3 混一桶，单类型 α 被平均掩盖不可辨）。最小改动 = 分桶键加 bsp_class：`(u32,i8)→(u32,i8,u8)`（:503/:505）。补充：P7 出场口径也门控"无 alpha"否证有效性，须与 P1+P4 一起上。

## 完整实装统一图景

| 机制 | 状态 | 性质 |
|------|------|------|
| 笔/段/中枢/走势/唯一分解/级别递归/背驰识别/三类买卖点识别/二类下钻（M1–M11） | ✅ 完整 | 三贯通 |
| P1 区间套 N^δ 多级递归 | ✅ **本轮修复**（w1-rework） | 曾接线缺口，已接 |
| P4 类型分桶 | ⬜ 字段透传完成，分桶未做 | 小改动 |
| P7 正规出场口径 | ⬜ sell.rs:50 已实装，econ 走口径4 未接 | 接线缺口 |
| P2 力度背驰 | ⚠ §9.3 授权 MACD，真问题=一票否决选择偏差 | 非换算法 |
| GAP3 提现端 EarningShares | ⬜ 引擎侧装齐，闭环 schedule_adapter 占位 | 待 576+η⋆ 解锁 |
| P3 §12 等价关系（跨标的） | ⬜ spec 缺口 | 另一维度，不阻塞单标的 |

## 落地顺序（可执行）

1. **P1 区间套** — ✅ 已完成（w1-rework）。待 W-VERIFY 重跑。
2. **P4 类型分桶** — 分桶键加 bsp_class（econ_positive.rs:503/505），纯增统计不碰识别层。[行动类，codex 确认]
3. **P7 正规出场** — econ 口径4 → closed_loop CloseRoot/ReduceCore（sell.rs:50）。[行动类]
4. **GAP3 提现端** — 576 选 C（双账本并置）+ η⋆ barrier → 解锁 EarningShares。[待编排者确认 576/κ]
5. **W-VERIFY** — P1+P4+P7 齐后重跑全历史，产 L2/L3 可否证 μ̂/LCB；更新所有 alpha 否证的有效域声明。
6. **后置** — P2 一票否决门改造（消选择偏差）/P5/P6/P8 精度项；P3 §12 跨标的另立里程碑。

## 结果包（六要素）

- **结论**：五问裁决如上；最小充分集 P1+P4+P7；现有 alpha 否证有效域限定为简化实装子集。
- **定义依据**：三方定义 N^δ 多级递归合取（codex C1 + ChatGPT + Lead）；§9.3 MACD 授权；576 账本 R=Π−A−W vs TW；三份 codex 裁决 trust-but-verify 代码事实。
- **边界条件**：若 576 选非 C、或 η⋆ κ 值改变、或 P3 纳入单标的范围 → 图景翻转。P1 若 W-VERIFY 揭示信号集退化过度 → 多级门参数需调（但不退回单级）。
- **下游推论**：P1+P4+P7 补齐后方可声明"单标的旧缠论完整实装无/有 alpha"；EarningShares 可达性依赖 576+η⋆；类型分桶后可能发现 buy1/2/3 的 α 分歧（当前被平均掩盖）。
- **谱系引用**：interval-nesting-not-called（假否证根源）+ 576（账本不同构）+ 663/665（选择偏差）+ full-implementation-gap-audit（P1/P2/P4 三根源）。
- **影响声明**：W1 已改 econ_positive.rs（多级 n_delta 进生产）+ cand_predicate.rs；P4/P7 待改；不碰引擎识别层 bit-exact。

## 待编排者最终确认（仅 3 项，其余可自主推进）

1. **576 账本**：确认 C（双账本并置）？— codex+planner 双荐
2. **η⋆ 的 κ 值**：每股成本下限风险参数，须你设值
3. **31 个 pending 的 /ritual 排期**：genealogist 已分类（9 语法记录 + 22 选择），待你 ritual
