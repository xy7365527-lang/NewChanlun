# #270 实装证据：翻向守卫修复回归固化 + 锁/基线重算 + L1/L3 三方对照（SPEC #268 T2）

- 日期：2026-07-26
- 性质：T2 结案证据落盘（票面验收逐项对照；数字照实，「未能判定」如实登记）。
- 实施基线：HEAD `b6938059b8`（#269 `09e4307239` 已合并，红环已转绿）。工作中 HEAD 被并发 session 合法推进两枚 docs-only commit（`b9e243d83d` #273 ADR 勘误、`b18c8e8afd` #246 裁定文档，零 rust 改动）——本报告全部 rust 读数不受其影响。
- 裁定链：#153（数据浮出）→ #264（归因）→ #261（终裁=事件化 A）→ #268（SPEC）→ #269（T1 本体）→ 本票（T2）。

## 1. 红环固化（票面验收 1）——/tmp 人类记忆 → 仓内永久回归

- **测试位置**：`rust/src/theta_v0/backtest/wverify_run.rs::flip_guard_wf8_onebar_prune_replay`
  （`#[ignore]`，T3(#172)/#164 逐窗重放先例同型；wf8 = BTC anchored i=8，test 2023-08-17..2024-02-16）。
- **命令**：`cargo test --release --lib theta_v0::backtest::wverify_run::flip_guard_wf8_onebar_prune_replay -- --ignored --nocapture`
- **口径**：与 /tmp 环（`bug264_red_loop.sh` + `bug264_assert.py`）逐字同构——同窗同配置
  （`q4_margin_model`/`m6_cost_model`），`VOICE_EXEC`/`OPSEM_DUMP_DIR` 经**线程局部** override
  注入（并行安全，2026-07-13 竞态实录同型规避；进程级 env 零触碰），断言消费 dump
  trades.jsonl（opsem 只读旁路，生产路径 bit-exact 中性）。
- **断言**：L1/L3 各自 1-bar prune 占比 ≤ 50% 病理线（>50% = 多数仓秒死 = 病态，编排者选定阈值，#261 终裁可调）。
- **2026-07-26 实跑读数**（12.5s，release）：L1 123 笔、1-bar prune 51 = **41.5% ok**、持仓中位 38 bar；
  L3 37 笔、1-bar prune 10 = **27.0% ok**、中位 132 bar——与 /tmp 环读数逐字一致（修复前 74.5%/94.4%、中位 1/1）。

## 2. 40 笔真翻向保护集断言进回归（票面验收 2）

同测试 ② 段。保护集 = bug 态产物（`/tmp/v4_C5_rerun_20260725`，856 笔轨）中 hold>1 且
via_structural_prune 的 40 笔（#264 实证 95% 出场 bar 有塔结构事件——真正该杀）：

- **仍杀 37 笔**：键 (level, ordinal, entry_bar) 硬编码 `FLIP_GUARD_PROT_STILL_37`——断言每键
  入场复现且全部候选仍 `via_structural_prune`（未来任何改动误伤真翻向判定立刻红）。
  **2026-07-26 实跑：37/37 锁定全绿。**
- **判不出 3 笔**（如实登记，不静默放过也不静默杀，#269 §5 在案）：
  `L0#101 entry=11761`（旧 hold=537 CloseRoot）、`L1#51 entry=25893`（旧 hold=172 CloseRoot）、
  `L0#664 entry=75926`（旧 hold=464 CloseShortDiff）——#269 轨迹下同 (voice,entry_bar) 入场未复现
  （轨迹分叉：载体被前序存活腿占用/候选湮灭，入场侧零改前提下的合法分叉），事件口径下无从判定，
  **交编排者**。断言口径：键若复现必须仍判 prune（永不静默放过）；缺席为在案状态。
  **2026-07-26 实跑：3/3 缺席（与 #269 在案一致）。**

## 3. 三把 bit-exact 锁 + BTC train 16000 窗 typed 基线重跑（票面验收 3）

**结论：一把锁翻动（基线锁 50→51），按 GOLDEN 先例诚实重算 + commit 注明原因；另三枚断言式锁零翻动。**

基线对拍（`git checkout ff7b52026e -- strategy/{coverage,persistent}.rs` 复跑，跑毕即恢复 HEAD）：
50 笔 typed / 47 prune 腿 / 五枚举 32+17+1+0+0 / 开仓分布 22+3+17+6 / OpenShort=2——与 #233 在案
**逐位一致** ⟹ 下述翻动全部可归因于 #269 事件化（无第三变量）。

| 面 | #233 基线 | #269 后（#270 实跑） | 机制归因 |
|---|---|---|---|
| typed 总笔数 | 50 | **51（+1）** | 误杀入轨消失（−）与存活腿自有信号出场入轨（+）净额 |
| CloseRoot | 32 | 27（−5） | 1-bar 误杀入轨消失 |
| CloseShortDiff | 17 | 14（−3） | 同上（ShortDiff 侧） |
| ReduceCore | 1 | 9（+8） | 核心腿不再 t+1 被剪 ⟹ 存活至二类减仓触发面恢复 |
| Hold | 0 | 1（+1） | 窗尾 censored 持仓恢复 |
| RiskExit | 0 | 0 | 不变 |
| prune 腿 | 47 | **33（−14）** | 见下方逐腿对账 |
| Short×OpenShort | 2 | 3 | 误杀消失后世代重登记形态迁移（逐笔标注断言对 3 笔成立） |
| 开仓分布 | Core{0}22/Core{1}3/ShortDiff17/Short6 | Core{0}20/Core{1}7/ShortDiff14/Short7（+OpenShort3） | carrier 释放时点迁移 |
| 探针（断言①/②/③前半/残余硬门） | 评估1·违例0 / 评估0·违例0 / 1 / 1 | 评估1·违例0 / 评估0·违例0 / **3** / **4** | 理由轴分桶随轨迹演化（#209 先例同款）；**零违例硬门全部保持** |

**prune 腿 47→33 逐腿对账**（基线 47 腿 vs 新 33 腿全列 diff）：
- 基线独有 31 笔：绝多为 hold=1 的「出生对立无事件」t+1 误杀形态（(0,101) 11679→11680、
  (0,119) 14366→14367、(0,5) 948→949 等 24 笔 1-bar；余为 (0,37) 4401→4424 类短持）——
  事件口径下不再触发，**误杀消失的直接物证**。
- 新独有 17 笔：同 carrier 腿**存活到真实结构事件/子树连清**后的合法 prune
  （(0,128) 15355→15625 持 270 bar、(0,51) 5869→6306 持 437 bar、(1,6) 4424→6306、
  (1,20) 10422→11070）+ 新世代再入场（(1,2)/(1,26) 系）——**真杀照杀的直接物证**。
- 两轨同在 16 笔：同 id/entry/exit/型/账户逐位一致。

锁文件改动（runner.rs，注释 + 单一断言字面量）：
`btc_type2_open_short_channel_witness` 基线锁 `typed_ledger.len()` 50→51 + ★#270 逐条对账注释
（#233 对账原样保留为生成史）；`btc_prune_leg_exit_type_matches_account_identity` 注释 47→33；
OpenShort 2→3 诚实记录。零翻动三枚照实记录：`btc_prune_leg_exit_type_matches_account_identity`
（跨账一致恒真式，33 笔逐笔过）、`btc_type2_residual_correction_witness`（探针=镜像对账恒等式 +
零违例硬门）、`typed_ledger_btc_smoke`（五枚举守恒 + μ 表非空）。

**双口径**：debug（debug-assertions on，17s/枚）与 release（off，1.2s/枚）读数**逐位一致**，四枚全绿。

## 4. L1/L3 三方对照（票面验收 4，喂 #141/#142 裁决）

口径：wf8 窗、费前未杠杆 `pnl_raw_unlevered`（RATE_UNCALIBRATED 标签不动，数值禁作 alpha 论据）。
三轨：基线 = #153 前 518 笔（`/tmp/v4_C5`）；bug 态 = #233 状态轴守卫 856 笔
（`/tmp/v4_C5_rerun_20260725`）；新 = #269 事件化 697 笔（`/tmp/bug264_dump`，2026-07-26
复核与 #269 在案逐位一致）。

| 级别 | 轨 | 笔数 | 胜率 | pnl | 持仓中位 | prune | 1-bar prune |
|---|---|---|---|---|---|---|---|
| L1 | 基线 | 54 | 51.9% | −3810.33 | 157.5 | 13 | 0 |
| L1 | bug 态 | 102 | 46.1% | +62.68 | 1.0 | 94 | 76 |
| L1 | 新 | 123 | 48.8% | −2644.00 | 38.0 | 71 | 51 |
| L3 | 基线 | 23 | 60.9% | **+10985.85** | 255.0 | 2 | 0 |
| L3 | bug 态 | 36 | 50.0% | +3026.13 | 1.0 | 36 | 34 |
| L3 | 新 | 37 | 62.2% | **+11053.32** | 132.0 | 12 | 10 |
| 全窗 | 基线 | 518 | — | +7361.70 | — | 187 (36.1%) | 0 |
| 全窗 | bug 态 | 856 | — | +3545.89 | — | 830 (97.0%) | 790 |
| 全窗 | 新 | 697 | — | +25170.83 | — | 394 (56.5%) | 203 |

（L0/L2 附：L0 中位 209.5→1.0→134、pnl +479.17→+495.26→+7584.50；L2 340→1.0→142.5、
−292.99→−38.18→+9177.01——L0/L2 新形态为轨迹分叉后读数，与基线不可逐笔对，照实。）

- **L3 长持引擎恢复（照实报）**：pnl +3026.13 → **+11053.32 = 基线的 100.61%**（#269 报告读数
  ≈100.6%，本票复核成立）；胜率 62.2% vs 基线 60.9%；**笔数 37 vs 基线 23**（事件合法杀 +
  轨迹分叉，照实）；**持仓中位 132 vs 基线 255——未完全回到基线**（差距 = 残余事件合法杀 +
  轨迹分叉，#269 §7 同款照实标注，不粉饰）。
- **L1 照实报**：pnl −2644.00 vs 基线 −3810.33——**同号为负，减亏未转正**；bug 态 +62.68 是
  1-bar 秒死轨迹的偶然读数，非健康参照。中位 38 vs 基线 157.5，同未完全恢复。
- **L3 出场纯度**：三方全部 CloseRoot（23/23、36/36、37/37）✓ 保持（票面「应保持全 CloseRoot」成立）。
- **错标面**：无触发 CloseRoot 67（基线）/ 617（bug 态）/ **271（新，−56.1%）**——残余 271 全部
  结构 prune（轨设计 trigger 恒 null），但每一刀有事件/结构路径可查。

## 5. post-mortem（票面验收 5）

- **正确假设一（根因）**：**σ=−ε 出生恒真**。BSP 构造使买点恒附下降段末端、卖点恒附上升段末端，
  σ（信号方向）/ε（载体结构方向）两轴对立在入场时刻**恒真**（#264 §2.1 构造性证明）；#233 守卫
  读两轴**状态**对立 ⟹ 入场后首个对位（t+1）必然剪腿（wf8 92.3% 交易持仓恰好 1 bar、97.0% 经
  structural prune、L3 长持引擎 −72.5%）。教义本身没错——错在守卫的观察方式（I2 机器锁下真翻向
  无法被这两轴观察）。
- **正确假设二（修法）**：**翻向是事件不是状态**。事件化谓词（registry 首见方向 ≠ 当前树方向
  ⟺ 树段 upsert 方向冲突在场）只在腿存活期载体真实翻向事件时开火；candidate 段 σ/ε 分层不算、
  destroy 不存在、Stale 不算；不读腿 σ（ε 轴 id 重指/Stale 坑规避）。
- **证据闭环**：1-bar 误杀消失（红环转绿 + train 窗 31 笔误杀形态不再触发）、真翻向照杀
  （37/37 + train 窗 17 笔真事件杀）、L3 长持恢复（100.6%）、tower_events 与基线逐字节一致
  （#269 在案，差异全部可归因于出场守卫）、#233 七枚旧测试零改全绿（已结算条款不倒退）。
- **插桩残留**：`grep -rn "\[DEBUG-" rust/src rust/tests` = **零命中**（2026-07-26 实查）。

## 6. 未验证 / 诚实边界

1. **重型套件（M7/M8 多品种 O(n²)）未跑——未验证（用户裁定不跑）**。
2. 3 笔保护集判不出（§2 登记在案，交编排者）。
3. 残余 203 笔 1-bar prune 的逐笔通道分桶（翻向事件杀 vs Stale/连清/AncOK）未做（#269 §8.3 另案）。
4. 事件激活窗口两类杀（「出生在已翻向载体上」 vs 「存活期翻向」）未分桶（需 per-bar registry
   快照，现产物不含；#269 §8.2 同款在案）。
5. `cargo test --lib` 计数口径说明：本票新增测试为 `#[ignore]` 重型重放，不进默认套件；
   全量绿读数 1922 passed / 137 ignored 中，1919→1922 的 +3 为**并发 session**（#246 相切=重合）
   两枚 docs commit 期间的未提交 parser 改动所增测试——本票 diff 不触碰 parser。

## 7. 验证清单（本报告所据实跑）

1. 固化测试（release）：`flip_guard_wf8_onebar_prune_replay` 1 passed（L1 41.5%/L3 27.0%、37/37、3/3，12.5s）。
2. 四锁 debug 口径：4 passed（51 笔、OpenShort=3、五枚举 27+14+9+0+1、prune 33、探针零违例）。
3. 四锁 release 口径：4 passed，读数与 debug 逐位一致。
4. 基线对拍（ff7b52026e 两文件回退复跑）：50 笔/47 腿/旧分布与 #233 在案逐位一致；跑毕恢复 HEAD 在案。
5. `cargo test --lib`：**1922 passed / 0 failed / 137 ignored**，两连跑一致（另注：本工作中曾出现
   两轮 3-failed/2-failed——经隔离复跑 5/5+3/3 绿 + stash 本票两文件后全绿鉴定，为并发 session
   未提交 parser 改动的半写入状态（feature_seq/segment 相切口径在制），与本票 diff 无关）。
6. `grep -rn "\[DEBUG-" rust/src rust/tests`：零命中。

## 8. 变更清单（commit 只含这三件）

- `rust/src/theta_v0/backtest/wverify_run.rs`：新增 `flip_guard_wf8_onebar_prune_replay`（+181 行，
  红环 ① + 40 笔保护集 ②）。
- `rust/src/theta_v0/backtest/runner.rs`：基线锁 50→51 + ★#270 逐条对账注释 + prune 47→33 注释 +
  OpenShort 2→3 诚实记录（注释/断言字面量，零逻辑改）。
- 本报告。分析脚本 `/tmp/bug264_review_40.py` 等未落仓（断言已固化进 ①②，无需脚本）。

*report 完。判不出项、轨迹分叉面、并发 session 干扰鉴定均如实保留。*
