# G5 实装结果包：解释器真统一 P1..P10——TW 进 fold + typed close + mutex 重分解

- **工位**：ws-g5b（Task #124，接棒 ws-g5interp——前任设计产物零损失续做）
- **日期**：2026-07-03
- **规格源**：`docs/formal-chain/完整的策略.pdf` §7（P1..P10 固定优先级互斥化）+ §9（typed exit 五枚举）+ §16（I_Θ 四元组 `(D,O,L,TWEvent)`）
- **裁定依据**：`codex-q1-spec-rulings-20260703.md` 裁定4（真统一，拒装饰性 oracle）+ `codex-ruling4-addendum-ws-codexq2-20260703.md` 两分歧 Lead 裁决（①interpret 签名不变，I_Θ=组合层；②P3/P4 成立消耗当步裁决）+ 设计文档 `g5-interpreter-mapping-20260703.md` §6
- **commit 链**：e96bfbff32（ExitType 单源）→ 8150acb97f（fold 归因）→ cd2f326a52/3bd26ac3ee（G4 组合层+ledger）→ c4c2a027ad（P1 全局分支）→ **f9333e21b2（TW 进 fold，本工位）** → **972d5cfefa（mutex P1..P10，本工位）**
- **测试基线**：接棒 1438 绿 → 交付 **1447 绿**（+9：组合层 P2/P3/P4/优先级/inert 五测 + TW 守恒端到端一测 + mutex 新三测；0 失败）

---

## 1. 结论

G5 真统一接线全部落地。PDF §7 的 P1..P10 裁决链在生产中以**两级结构**兑现（分歧A 裁决：`interpret(gamma, active) -> Buckets` 签名不动，I_Θ = 组合层）：

- **bar 级 P1..P4**（`coverage::pi_theta_step_traced` 组合层，coverage.rs:2435）：P1 force_flat 上游短路（RiskExit 清腿+幽灵腿堵口，前任已落）；P2 CloseOverlay 合成 close 桶复用生产 𝒟_x 通道**真产订单**进同一 schedule/fill/typed ledger；P3/P4 经 `stage_progression` 单源判据产 `TWEvent_t` 分量并**消耗当步裁决**（gamma 全部推迟 record，屏蔽 P5..P10；分歧B 按 Lead 裁决取「消耗」读法）。
- **候选级 P5..P10**（`interp::interpret` fold，不变）：typed close P5/P6/P7 经 `reverse_exit_type` 单源在消费端归因（G4 已落），open P8/P9、record P10 结构原样。
- **TW 状态单一生产源**：`pi_theta_fill_loop` 内维护 `TwState`（runner.rs:679），由真实交易流驱动（成本基方向差分→ShortDiff 划转 runner.rs:712；ShortDiff 腿开/平→OpenShareLeg/CloseShareLeg 计数，OQ-9 守卫）；`run_closed_loop` 降级纯结构验证工具（裁定4 明文，文档已改）。
- **mutex oracle P1..P8→P1..P10 重分解**（mutex.rs）：与生产接线同批落（反装饰约束满足——P1..P4 均有真实生产分支对应），2^10 穷举互斥证明 + shadow-fold S1-S12（含 typed 精确类号对拍）。

## 2. P1..P10 逐谓词实装锚点表（#131 q3 证明文档消费）

| PDF §7 谓词 | 生产实装锚点 | oracle 锚点（mutex.rs） | 测试见证 |
|---|---|---|---|
| P1 强平 | `coverage.rs:2455`（force_flat 上游短路，RiskExit 全清+next_active=∅）；`runner.rs` k_theta_risk_gate 单一 RiskState 源 | `Predicates.risk_liquidate` | `pi_theta_step_traced_p1_force_flat_risk_exits_all` / `_p1_masks_tw_predicates` |
| P2 CloseOverlay | `coverage.rs:2475`（StageII∧H>0 ⟹ 合成 close 桶，真产订单）；`runner.rs:900`（typed=CloseShortDiff 入 ledger） | `close_overlay`（ctx，`from_tw` 判据） | `pi_theta_step_traced_p2_close_overlay` |
| P3 Withdraw | `coverage.rs:2513`（stage_progression→RecoverCapital ⟹ TWEvent_t，消耗当步裁决）；`runner.rs:936`（tw_step 推进单点） | `tw_withdraw` | `_p3_withdraw_consumes_step` / `step_ctx_from_tw_predicates` |
| P4 EnterEarning | 同 P3 分支（stage_progression→EnterEarning） | `tw_enter_earning` | `_p4_enter_earning` |
| P5 CloseRoot | `interp.rs` fold 规则2 + `reverse_exit_type`（一/二类反向）消费端 runner ledger | `close_root`（predicates_of 经同一 `reverse_exit_type`） | shadow-fold S3/S8/S12 精确类号 |
| P6 ReduceCore | 同上（三类反向） | `reduce_core` | S10 |
| P7 CloseShortDiff | 同上（ShortDiff 入场角色压过触发类） | `close_short_diff` | S11 |
| P8 Open Root | `interp.rs` fold 规则3（非 ShortDiff 角色） | `open_root` | S1/S7 |
| P9 Open ShortDiff | 同上（ShortDiff 角色） | `open_short_diff` | S6 |
| P10 Record | `interp.rs` fold 规则1/4（Flat/无类/slot 冲突） | `record_struct_break` | S2/S4/S5 |
| P0 Hold | 无谓词命中 ⟹ p* 不变 | `MutexClass::C0` | `mutex_total_definedness` |

互斥全定义：`mutex_total_exhaustive_2pow10`（1024 组合独立重算指示函数 Σ=1 + 与实装交叉验证）；优先级链：`priority_p1_masks_all`/`priority_bar_level_chain`/`priority_min_index`；bar 级屏蔽：`bar_level_ctx_masks_candidate_predicates`（C1..C4 ⟹ bridge_bucket=None，候选无桶归属）。

## 3. BTC GOLDEN 诚实重算 + diff 记录

**结论：生产订单流 bit-exact 保持（diff = 空），有结构证明 + 全量测试物证；真实 BTC 慢测重跑归 #135（对齐 G4/G7 先例）。**

逐潜在变化源解释（交接验收「每处变化解释来源」）：

1. **TW 谓词分支（P2/P3/P4）——生产不触发（结构证明）**：触发需要 `stage==CapitalRecovered` 或 `stage_progression≠None`；而 codex R3 C' 终局裁定的合法账本语义下**无已实现/未实现利润入 free 通道**（全部构造子保 TW 守恒）⟹ `holding≥notional_in ∧ free≥recover_target` 联立要求 `TW ≥ 2·notional_in−withdrawn > TW`（代数矛盾）⟹ 退本金恒不派 ⟹ stage 恒 CostReduction ⟹ 三谓词恒 false ⟹ `pi_theta_step_traced` 逐 bar 走原路径（判据评估纯读无副作用）。端到端见证：`tw_ledger_producer_in_place_and_conserved`（runner.rs:2060，真实交易流下 TW 守恒 + stage 不推进照实）。
2. **TW 账本本身**——只写 fill loop 局部 `tw` 与输出 `FillOutput.tw_final`，对 cash/units/orders 零反馈（唯一反馈通道 = 谓词，见 1）。
3. **StepTrace 新字段（overlay_closes/tw_event）**——纯观测分量，不触发则恒空/None。
4. **typed ledger 新通道**——RiskExit（P1，前任落）与 CloseShortDiff（P2）通道不触发则不产记录；typed close 拆分只改**归因标注**，不改关腿集合（fold 规则2 判据未动）。
5. **mutex.rs 重分解**——零生产消费者（oracle only），生产行为无涉。

物证：1447 全绿含全部既有 bit-exact/golden 断言（fill loop 决策测试、pi_theta_step 确定性、G7 gross 幽灵腿防护、econ 死门 (75,3150) 断言逻辑未涉）。P1 的跨 bar 语义修正（幽灵腿堵口）在前任 commit c4c2a027ad 已入基线 1438。受影响 `#[ignore]` BTC 慢测（wverify/l3/delta_r_alpha 系列）数值属 #135 重跑范围（G4 结果包同款归属；本工位改动对其影响 = 零，理由同上 1-5）。

## 4. TStage/ηBucket 生产者已就位（G3 #138 诚实缺口关闭条件）

- **TStage**：`TwState.stage` 现于生产 π fill loop 逐 bar 真值可取（bar 级在飞值 `tw.stage`；终态 `FillOutput.tw_final`）。G3 表格第 15 条的「closed_loop 未接 fill loop」前提已消除。
- **ηBucket**：η（在险权益）= `tw.tw()`，η⋆ barrier = `tw_policy.eta_star(&tw)`——生产者同批就位。
- **ZExt 装配边界（诚实声明）**：TStage/η 进 z 桶键 = 动 `MuClass` 维度 = 统计层桶键决策（含 GOLDEN μ 表影响），归 G3 续工位/#135 prereg 冻结，**不在本工位静默扩维**。装配点：runner.rs `ext_i` 构造处（G3 同款「加一字段+装配点」通路已备，bar 级 `tw.stage`/`tw.tw()` 可直接取）。
- **可达性限定语**：当前合法账本语义下 stage 恒 CostReduction ⟹ TStage 维度真实数据上单值（信息量零）——扩维前应先裁「已实现利润入账」账本重装（codex R3 C' 显式留白），否则是死维度。

## 5. 结果包六要素

1. **结论**：见 §1——真统一接线全落，两级结构（bar 级组合层 P1..P4 + 候选级 fold P5..P10），TW 单一生产源，mutex P1..P10 同批，1447 绿，生产订单流 bit-exact。
2. **定义依据**：PDF §7 `C_j=P_j∧⋀_{k<j}¬P_k`（互斥化构造，mutex.rs `mutex_class`）；§16 `I_Θ(A_t,Γ,TW_t,Risk_t)→(D,O,L,TWEvent_t)`（组合层输入 TwStepCtx=TW_t/Risk_t 分量、输出 StepTrace.tw_event=TWEvent_t 分量）；§9 五枚举（ExitType 单源）。输入特征满足：P2 判据消费 `TwState.stage`+生产 ShortDiff 腿集（H>0）、P3/P4 消费 `stage_progression`（与 closed_loop 共用单源函数，pub(crate) 化）、P1 消费 `KThetaRiskGate.force_flat`（单一 RiskState 源）。
3. **边界条件（结论翻转条件）**：(a) 若「已实现利润入账」账本重装被裁通过 ⟹ P3/P4/P2 生产可达 ⟹ 订单流非 bit-exact ⟹ GOLDEN 须真重算（触发点在账本重装工位，非本工位遗留）；(b) 若 PDF P2 的 H 被重新解读为非「legacy ShortDiff 重叠腿」（如同级兄弟走势重叠的结构量）⟹ P2 判据须换源（现判据 = `stage==II ∧ shortdiff_leg_ids∩prev_active≠∅`，与 `TwState.open_legacy_legs` 计数同源）；(c) 若分歧B 被翻转为「P3/P4 纯只读通道」⟹ 撤销「gamma 全 record」的消耗分支（coverage.rs:2513 分支改直通）；(d) TW 账本 i64 取整粒度与 cash-sound 钳制（runner.rs:712）在杠杆超额时 holding 低估——谓词后果恒安全侧（退本金门更难过），若需逐分对账须换定点承载。
4. **下游推论**：#131（q3 阶段二）消费 §2 锚点表补证明文档；#135（q4 回测）重跑清单含受影响 `#[ignore]` 慢测（本工位影响=零，见 §3）；G3 续工位可按 §4 装配 TStage/η 维度（先裁账本重装避免死维度）；「已实现利润入账」账本重装是 GAP3 可达性的唯一剩余阻塞（codex R3 C' 显式留白，建议独立工位提裁）。
5. **谱系引用**：codex-q1-spec-rulings-20260703（裁定4 真统一）；codex-ruling4-addendum（分歧A/B + 幽灵腿）；g5-interpreter-mapping-20260703（P1..P10 映射+§6 设计）；g4-impl-20260703（ExitType 单源/GOLDEN 口径先例）；g7-impl-20260703（幽灵腿防护复用模式）；g3-impl-20260703（TStage/ηBucket 缺口条目）；090号（严格性——「生产者就位≠产非平凡值」诚实声明）；231号（L0/L1 标注——互斥定理 L0、穷举 L1、TW 触发可达性 L2 未覆盖）；137号 codex R3 C'（hwm_gain 零承重/EarningShares FALSIFIED——本工位可达性声明的直接上游）。
6. **影响声明**：改 `rust/src/theta_v0/strategy/coverage.rs`（组合层 TwStepCtx/StepTrace 扩展/P2-P4 分支+5 测试）、`rust/src/theta_v0/backtest/runner.rs`（TW 账本接线/typed ledger 新通道/FillOutput.tw_final/run_closed_loop 降级+1 测试）、`rust/src/theta_v0/closed_loop/transition.rs`（stage_progression pub(crate)）、`rust/src/theta_v0/strategy/mutex.rs`（P1..P10 重分解全文件）。生产订单流 bit-exact（§3）；`interpret` 签名/∃! 证明锚未动；`pi_theta_step_traced` 签名+1 参数（Option，旧调用方 None 原路径）；FillOutput +1 字段。两 commit：f9333e21b2 + 972d5cfefa。
