# #1254 原文考据：类第二类买点、#817 悬空归属与 `QuasiSecondCert` 现态

> 票型：research；**只研究，不裁定**。  
> 研究基线：`main@0199749c48cd84ea04825fd9aad8c9c4d45fb783`（2026-08-26）。  
> 权威次序：`docs/chanlun/text/blog/` 原始博文正本 ＞ `docs/chanlun/text/chan99/` 编纂版 ＞ 其他说明；教义现态以 `.chanlun/definitions/` 为准，代码只作现状证据。  
> 引文检查：逐字、`↑正文` 边界、行首注、行内注四项分别核对；课号行号一律取 `docs/chanlun/text/blog/`。

## 0. 方法与限制

- 对三条指定锚点逐行读取，并全量检索 `docs/chanlun/text/blog/` 的「类第二类／类似第二类／类第一类／类似第一类」及普通二类点近义判据；再以 `chan99/` 作二级权威复核。
- 代码知识图谱 MCP 因本会话审批策略拒绝读取，按仓内规则退回精确 `rg` + 定点源码阅读。
- `gh issue view 817` 本轮返回 `error connecting to api.github.com`，无法重新拉取 GitHub live thread。故 #817 现态以当前教义正本、git history 和仓内既有全评论考据作证；不把未能 live 重拉说成已现场核验。
- 指定原文、`.chanlun/definitions/qujiantao.md`、`.chanlun/definitions/maimai.md`、`cert.rs`、`signal.rs` 均与 HEAD 一致（定点 `git diff --exit-code` 无输出）。

## 1. `027:68`：逐字、归属与上下文

### 1.1 逐字

**`027-第27课.md:68`【正文】**：

> 类似的，在大级别里，如果不出现新低，但可以构成类似第二类买点的买点，在MACD上，显示出类似背驰时的表现，黄白线回拉0轴上下，而后一柱子面积小于前一柱子的。一个最典型的例子，就是季度图上的600685，2005年的第三季度的2.21元构成一个典型的类第二类买点。在实际操作中，2.21元的相应区间的寻找，也是按上面级别逐步往下找背驰段的方法实现。

逐字核查结果：

- `↑正文` 在 `027:78`，故 `68 < 78`，该行在正文界内，不是课后答疑。
- 行首无「（注：」「(娇注：」「（娇注：」等编注标记。
- 行内无「注：／娇注：／娇：」插入；整行可归缠师正文。
- 上引未删字、未改标点；原文件里的 `MACD`、`0轴`、`2.21元`、`600685` 均照录。

### 1.2 足够上下文：`:64–70` 是同一段推演

- `027:64` 先限定主题：「上面说的是背驰构成的买点」。
- `027:66`【正文】先命名类第一类买点：大级别里，盘整背驰构成「类似第一类买点的买点」，并明写「类第一类买点」，级别「至少应该是周线以上」。
- `027:68` 以「类似的」承接，给出类第二类买点的描述：大级别、不出现新低、MACD 类背驰表现、黄白线回拉 0 轴上下、后一柱子面积小于前一柱子，并以 600685 的 2.21 元为例；落点继续用逐级下钻找背驰段。
- `027:70` 把用途收在「大牛市底部／长线底部」，并说日线也能用但可靠性不那么绝对。

因此，`027:68` 能直接支持的只到以下五项：

1. 名称确有「类第二类买点」；
2. 有「大级别」有效域，近邻 `:66` 把同段类第一类至少限定到周线以上；
3. 买侧写了「不出现新低」；
4. 写了 MACD 类背驰表现及前后柱子面积比较；
5. 位置用区间套式逐级下钻定位。

原文**没有**机械化说明：回抽从哪一段起算、面积比较究竟取哪两段/几段、`大级别` 如何映射到生产 Level、卖侧镜像字段如何编码。把这些写成实现判据都需要另有裁定，不能伪装成 `027:68` 逐字。

## 2. 其他课的呼应、限制与反例

### 2.1 全量精确词检索

对全部 `blog/` 检索 `类第二类|类似第二类`：原始命中 **4 次、3 行**；语义去噪后只有 **2 行**：

| 命中 | 性质 |
|---|---|
| `015:724` | 假阳性：「第一类第二类买点」连续书写导致子串命中，不是「类第二类」概念 |
| `027:68` | 正式命名与描述句 |
| `086:70` | 明写「只是比喻」的限制句 |

`blog/` 中「类二类」简称为 **0** 命中。故不能用高频惯用语证明它有稳定、完整的独立判据；一手语料的直接命名承重实际上集中在 `027:68` 一行。

作为对照，对全部 **110 个** `blog/*.md` 统计普通「第二类买点」：共 101 个文本词项，落在 80 个物理行、28 个文件；`↑正文` 前为 42 个词项/28 行/13 文件，界后为 59 个词项/52 行/23 文件。这里是文本出现次数，不是独立主张数（答疑压缩副本、转引和重复问答均未去重）。这组高频与「类第二类」只有一条正式命名正文形成鲜明差异。

### 2.2 `086:70`：正文中的作者自限句，不是编注

**`086:70`【正文】**（界在 `086:84`）：

> 但是，其中有一种买卖点，往往具有大级别的操作意义，就是大级别中枢震荡中，次级别的买卖点。例如，一个5分钟的震荡里面的1分钟级别买卖点，就具有5分钟级别的操作意义。因为该买卖点后，无非两种情况：1、就是继续5分钟中枢震荡；2、刚好这次的次级别买卖点后的次级别走势构成对原中枢的离开后，回抽出第三类买卖点，这样，原来这个买卖点，就有点类第一类买卖点的样子，那第三类买卖点，就有点新走势的类第二类买卖点的样子了。（注意，这只是比喻，不是说这就是大级别的第一、二买卖点。）

归属核查：`70 < 84`；行首无编注；行内括号以「注意」起，不是「注：／娇注：」校注格式，且语法连续，是缠师对本段比喻的正文自限。其承重结论恰是**不得把这里的“新走势的类第二类”当成大级别真一、二类点定义**。它只能解释操作意义，不能覆盖或改写 `027:68`。

### 2.3 `029:392/396`：`:392` 是混合归属行

该课 `↑正文` 在 `029:90`，故 `:392/:396` 都属于界后答疑，不是课程正文。上下文说话人链为：`:384` 缠中说禅时间戳，`:386` 提问者「缠文观止」，`:388–390` 问题 1，`:392` 混排答复 1 与问题 2，`:394` 分隔 `==`，`:396` 作者答复 2。

**`029:392`【答疑混合归属】**：

> 背驰不存在失效的问题，只要本ID理论存在的两个前提存在，这个结论就不会改变，这两个前期，明天的帖子都会说到。2、 第一类买点多是趋势背驰，第二三类买点多是盘整背驰，那二三类买点还是次级第一类买点吗？应该是次级“类第一类买点”吧？

- 开头「背驰不存在失效……」是缠师对问题 1 的答复。
- 从「2、第一类买点多是……」起，是提问者的问题 2；「应该是次级‘类第一类买点’吧」**不是缠师命名**。

**`029:396`【答疑，缠师答复】**：

> 这个问题前面有定理的。所有买点，归根结底都是第一类买点，要找第二、三类，其精确的，都要下次级别以下找第一类。

作者答复没有接受「次级类第一类」措辞；它说的是二、三类点的精确位置要到次级别以下找第一类点。该句可支撑递归构成关系，**不能**补出类第二类点的本级准入形状。

### 2.4 普通二类点证据必须与「类第二类」分开

另有多条正文直接谈普通第二类点：

- `014:34`【正文】：大级别第二类买点由次一级别相应走势的第一类买点构成；这是递归构成关系，不是类第二类的本级准入式。
- `053:28`【正文，界 `:36`】：高点一次级别下、再一次级别上，「不创新高或盘整背驰」构成第二类卖点，买点反过来。
- `057:40`【正文，界 `:48`】：「次级别上，次级别下，不创新低或盘整背驰」构成第二类买点。
- `060:50`【正文，界 `:60`】：把盘整背驰点 55「当成第一类买点」时，作者立即括注「严格来说，盘整背驰无所谓第一类买点，只是这样来类比」，随后称 57 为第二类买点。这是类比结构的近邻旁证，但没有命名 `QuasiSecond`。
- `101:32`【正文，界 `:68`】：逐字正文至行内娇注前为「第二类买点跌破第一类买点，也就是第二类买点比第一类买点低，这是完全可以的，这里一般都构成盘整背驰……」。行内「（娇：扩张ABC 或者顺势ABC）」及 `:34` 娇注不得算正文。
- `107:66` 位于 `↑正文=:36` 之后，是界后解盘，不升格为正文；它再次说第二类卖点可由「第一次次级别回拉不破顶或盘整背驰」构成，只作答疑/解盘佐证。

这组统计产生一条重要的**边界结论**：普通二类点的原文允许「不创新极值 **或** 盘整背驰」，且 `101:32` 明许跌破一类点；所以不能把 `027:68` 的「大级别不出现新低 + MACD 类背驰」直接外推成所有第二类点的硬闸。反过来也不能拿普通二类点的宽口径消掉 `027:68` 对类第二类点写下的限定。

### 2.5 二级权威 `chan99/`

- `chan99/0016:80/:82` 收录 `027:66/:68` 的类一类/类似二类段，但 `:82` 只保留 `027:68` 第一部分，遗漏 600685、2.21 元「典型的类第二类买点」及逐级下钻落点。
- `chan99/0043:44` 收录 `086:70`，仍保留「这只是比喻」自限。
- 思维导图精确词检索为 0。

因此编纂版只能二级佐证，且对 `027:68` 有实质遗漏；定义取证必须回到 blog 正本全文。

## 3. #817：已裁面与闭票后的悬空指针

### 3.1 当前正本显示 #817 自身没有未裁组

`.chanlun/definitions/qujiantao.md` 是 #817 的教义正本：

- `:3–8` 明写裁定票 #817、版本 v1.1、状态「完整正本——6 组全裁」，「未裁」段为空。
- `:16–24` 列出 N-1～N-6 与两件上游归此的；`:26` 明写 v1.0 起可关票。
- `:1264–1270` 的 §10.1 再次明写「未裁：空」；未闭的只有 §10.2 未测事实/实施，不是待裁教义分歧。

所以不得写成“#817 正本内部还有一个未裁的类二类组”。

### 3.2 #817 与 `027:68` 真正已经裁到的范围

当前正本与 git history 可确认：

1. **N-1 坐标/编号域已裁。** `qujiantao.md:245–260` 裁坐标为分型顶点回指的原始 K 序号 `source_index`，名分 `[新缠论:选择]`。`027:68` 在 `:255–258` 只作为“价格侧也有承重引文”的证据，用来证明原文轴留白；它没有被用来裁类第二类准入。
2. **N-5 `Cand^δ_ℓ` 已裁。** `:760–775` 裁为 Dir ∧ Comparable ∧ Extreme，力度不进候选身份。这里裁的是区间套候选元素谓词，不是 `QuasiSecondCert`。
3. **六组及两件 extras 已裁完。** 本地提交链包含 N-1、N-2、N-4、N-5、N-6、extras 与 v1.0/v1.1 正本合入，和现行正本状态一致。

本轮无法 live 拉票面；工作树预存的 `.chanlun/review-results/issue1254-quasi-second-original-20260825.md:56–83` 记录其曾全读 #817 body/comments：票面六组不含「类第二类/类第一类」，评论中 `027:68` 只出现在 N-1 坐标裁定。该记录是二级的既有工作草稿，本报告只把它用于交叉核对，现态仍以当前正本和 git 为准。

### 3.3 已在别处裁、但不要冒充 #817 裁定

- `docs/adr/0001-graded-exit-and-shortdiff-doctrine.md:285` 已裁命名/通道：「类第一类后的回抽点 = 类第二类（027:68 命名），归盘背通道观测」；并把 `086:70` 分清为比喻。这是 #584/#587 轴，不是 #817 N 组。
- `.chanlun/definitions/maimai.md:209–227`（#816，后由 #884 落地）已裁**普通二类点**的构成形，且明确跌破一类点仍是合法二类点，「不创新低/高」不得作普通二类准入硬闸。它不等于裁完 `027:68` 的类第二类点。

### 3.4 真正留白：不是 #817 内部残余，而是闭票后悬空归属

`rust/src/theta_v0/classifier/signal/cert.rs:56–60` 把以下问题写成「归 #817（未裁）」：

- 算不算类第二类；
- 回抽从哪起算；
- 面积比较的几段口径；
- 载体何时接入 `LevelState`、坐标查询与消费。

但 #817 正本已经全裁且 §10.1 为空，票面六组也没有这一题。故其准确性质是：**#885 建载体时留下、在 #817 闭票后未被承接的悬空/过期归属指针**，不是 #817 正本里的「未裁组」。另外仍未见正本明确机械化：

- `027:66/:68` 的「大级别／至少周线以上」如何映射生产 Level；
- `027:68` 的 MACD 前后柱面积比较取哪两个结构段；
- 类第一类父点、回抽起止和区间套落点如何绑定；
- 卖侧镜像虽可作对称推演，但 `027:68` 本行只逐字写了买侧，具体字段名分未在 #817 落纸。

这是权威链内部的**引用去向不一致**：教义正本说 #817 无未裁，代码注释仍把未裁判据指向 #817。按本任务“只写指定报告”的写入限制，本轮不另建 `.chanlun/genealogy/pending/` 文件；不一致已在本报告显式登记，供 Lead 路由。

## 4. `QuasiSecondCert` 代码现态

现行定义在 `rust/src/theta_v0/classifier/signal/cert.rs:62–73`：

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct QuasiSecondCert {
    pub source_index: usize,
    pub side: Side,
    pub quasi_first_index: usize,
    pub retrace: (usize, usize),
}
```

字段注释把它解释为：回抽结束点坐标、Long/Short 方向、前导类第一类点坐标、回抽闭区间。类型头 `:47–60` 同时诚实写明：只建载体、不新定判据、暂无生产构造点、不得当信号消费。

全 `rust/src/**/*.rs` 精确检索结果只有 **3 处**：

1. `cert.rs:3` 模块文档提及；
2. `cert.rs:62` 类型定义；
3. `signal.rs:122` `pub use cert::{PanDivCert, QuasiSecondCert};` 重导出。

`QuasiSecondCert {` 只有定义本身这一处；没有结构体字面量构造、没有字段读取、没有 `LevelState` 字段、没有 six-bit/BspPoint/生命周期/订单流接线。结论是：**当前它是公开重导出的占位数据类型，零生产构造、零消费**。

代码注释还包含两项需保持名分边界的陈述：

- `side` 的 Short 镜像是对称扩展，不是 `027:68` 本行逐字；
- `:56–60` 的「归 #817（未裁）/由 #817 实施票接线」已随 #817 闭票变成悬空指针。

## 5. 研究结论（不裁）

1. `027:68` 是无行首/行内编注的【正文】，直接命名并描述类第二类买点；它给了大级别、不出现新低、MACD 类背驰、柱面积比较和逐级下钻五项，但没有给出完整机械判据。
2. `086:70` 也是正文，却由作者亲自限定为「只是比喻」；不能当类第二类定义。`029:392` 是混合归属行，类一买问题属提问者；作者答复在 `029:396`，只支持向次级别以下找一类点的递归关系。
3. 普通二类点证据与类第二类必须分开：`101:32` 明许普通二类跌破一类点，#816/#884 也已拆掉普通二类的 `no_new_low` 硬闸；这既不能推翻 `027:68`，也不能拿来替它补判据。
4. #817 正本内部没有未裁组。它已裁到与 `027:68` 相关的只有 N-1 坐标选择；N-5 `Cand` 等是别的问题。`cert.rs` 把类二类准入和接线写成「归 #817（未裁）」是闭票后的悬空/过期归属指针。
5. `QuasiSecondCert` 当前四字段、公开重导出，但零构造零消费；代码没有偷偷形成事实判据。

本报告只提供源头与现态证据，不选择判据、不修改定义、不改代码。
