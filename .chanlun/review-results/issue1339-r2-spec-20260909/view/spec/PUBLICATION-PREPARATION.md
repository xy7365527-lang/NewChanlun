> 链接转换阅读版；正文语义与原稿一致。签署原字节：[原稿](../../payload/spec/PUBLICATION-PREPARATION.md)，SHA256 `8dae5231dc1d2de5122035335f4e3f1364279a8ad86fb5935480fa1e276dc410`。当前批准状态见归档根APPROVAL.json；原稿中的待审状态保留为发生时记录。

# #1323 SPEC 批准后的发布准备

名分：可审阅的发布/对账清单；本轮没有向 GitHub 写入、拆票或变更原生依赖。实际发布采用用户批准的最终 SPEC 字节及全部规范附件，不发布初审版本。

## 拟发布对象

- 标题：SPEC：完整生产程序、结构全分类与逐重经营（#1323，R2）
- 正文：最终 SPEC 全文；首行声明图内实施总单，Part of #1323。
- 规范附件：全需求/分类/状态/验收覆盖；残留决定与当前后裁补充；技术合同；测试缝和验收有效域。
- triage：遵守 to-spec 的 ready-for-agent 口径，但 SPEC 总单不是可直接拾取的 Sandcastle 实施票，不自动加 sandcastle。
- 归属：发布后用真实 issue ID 关联 #1323；不得使用本稿 TB 编号伪充 GitHub 票号。
- 源稿和独评实体须有长期可访问落点，不能只写 /tmp 链接就声称已归档或关票。入仓、提交及 main 合入按其明确授权执行。

## #1339 拟回写的状态

需求与完整分类/观察范围已确认；RF-01 和 RF-02 已确定；R2 架构、20项合同继承/替代及明确披露的不可撤精确交付窗口已由用户采纳。本轮已合成为整图实施总单，独评和最终字节以交付入口为准。

该说明不宣布完整程序已实现，也不以 SPEC 草案或架构采纳关闭 #1339。关票仍需符合落地、实体报告及 main 的既有纪律。真正语义/资金缺项按 SPEC 具名保留，不能给旧 closed 票追加一个未经登记的虚拟接单结论。

## #1323 图正文拟对账

1. 保持六项 Destination 原文、整图带实装声明和红线不变。
2. 在 Decisions-so-far 新增 R2 采纳、RF01/RF02及 SPEC 的实际发布链接；旧方案和独立重做的发生史保留，不倒改其签署。
3. 将“等待用户选择新方案方向”“原推荐尚未采纳”的当前状态更新为“R2已采纳；SPEC及逐项残留依赖按最新总单推进”，不能把旧历史候选直接标为采纳。
4. 将 Frontier 指向已发布 SPEC 下的合资格切片/残留决定，并准确标阻塞。原 #1327–#1333 仍暂停，直到逐票决定改写或替代并实际更新原生关系；不由文字替换自动恢复拾取。
5. #1338 既有图文和 #1336 原始方案保持原件及历史名分，所缺归档/合入状态单列，不把 R2 采纳当其 main 落地。
6. #1096/#1304/#1319/#1320/#1279 只按真实票面和本总单验收处理；本轮不改其状态，不宣称新依赖已建立。

实际写图前重新读取 body 和 updatedAt，与读取基线比对后按图写入协议应用；遇并行更新重新合并一次，不覆盖他人修改。当前只读基线在 evidence/MAP-1323.json。

## 拆票与批准范围

TB-01–11 是纵向切片建议，完整正文和验收组已在 SPEC。旧写入口隔离 TB-09 前置于场所验收 TB-08；带仓迁移与默认启用 TB-10 在其后，TB-11 汇总整体验收。编号不代表排程顺序。拆票时先做 tracker 与文档两路查重，逐票写入输入/外部结果、AT/上游ID、独立测试、真实未决依赖和执行范围；需要裁定的条目先留具名决定票，实施票不得代裁。

同一依赖可服务多个切片。成功路径依赖未裁内容时，不能仅因“缺政策会返回AwaitingPolicy”就标无阻塞。一个切片取得批准并满足原生阻塞/认领/执行条件后才进入相应实施流程，接回独评、完成必要验证再按明确合入范围集成。

本文件不是可执行的自动化指令，不启动 claimer、harvest、服务或交易。
