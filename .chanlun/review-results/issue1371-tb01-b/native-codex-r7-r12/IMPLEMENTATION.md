# #1371 R12 实施与验证

名分：绑定开放票 #1371 的工作草稿，尚未合入 main；本目录保存报告实体及必要证据附件。

原候选 `4922543038fbcfef1a4d711a306708ace3f62e88` 的完整 Codex 原生 R7 独评发现两处消费者缺陷，总评 FAIL。修复产品提交为 `e5b03629adde165610161b4df3e2d62d22163568`，分支 `codex/1371-codex-r12`，基线 `2212eba31e0b7419907e622e79f02f7c2cc37706`。

用户确认由新的 Codex 原生子代理独评替代原 R6 未完成结论。旧 R6 保留 INCOMPLETE，#1448 同 ID 迁移义务仍未满足；未启动 Prime R7。R7 和 R12 使用同一原生子代理 `/root/r7_codex_product_review`，原 R7 FAIL 原件不回写成 PASS。

## 修复行为

增量包含缺失、重复或错端点的替代项时，消费者过去可能仍推进到第四代并声称一致。现按该批撤回项的 `object_id/superseded_by` 推导完整预期替代项，排序后逐字段深等；遗漏、额外项、重复和任一端点错误均在提交前拒绝。

Gap 声明新会话 B 的 cut1、重建查询却返回旧会话 A 的同数字 cut1 时，过去会静默提交 A。现要求 Gap 会话身份完整，且重建 Snapshot 会话与其相同，通过后一次提交。合法迁移至 B/cut1 仍可完成。

产品仅改 `s_session/browser/index.html`；永久检查位于 `s_session/tests/r12_consumer.cjs`，夹具为已保存真实 HTTP 原字节及来源 SHA。Rust、Python 只读服务、launcher 与固定原候选相同，未改形式化代码或交易路径。

## 已实际执行的验证

- 新永久 10 项消费者检查：旧 HTML 为 3 PASS / 7 FAIL，修复 HTML 为 10 PASS。每个坏载荷检查完整逻辑状态不变及同页原游标健康重试；模拟 DOM，不称实际浏览器。
- 既有 R11 28 项消费者用例使用捕获的原 HTTP body 重送运行，28 PASS；该次不访问网络，不称新的 HTTP/native 执行。
- 实际 Chrome 页面三组：空替代集合、错误旧端点、Gap 错会话均拒绝且保留 A3；保持同一页面切至健康响应，分别推进 A4、A4、合法迁移 B1。测试使用 TestOnly 原字节输送服务；不独立读取页面内部六个 Map，其完整性由函数级检查与源码复审承担。
- 页面记录 28 请求（25 HTTP200、3 favicon204）、全部发送完成；10 个 AX/原 JPEG 描述符含一个错误点击未成功种子的记录，保留但不计通过，实际有效阶段为 9 个。服务实际退出 0，端口 18962 无监听；临时测试标签已关闭。

同字节后端复用原候选的本机构建、35 项 Rust 检查、正常推进及三次真实 SIGKILL/恢复原件。旧 R5 正式 launcher 原回执从既有报告的压缩附件找到，可按限定字节复用；旧 R5 其他丢失大矩阵未恢复。上述历史证据不计为 R12 新执行。

## 适用范围

只验收 #1371 明示的 CC-006/local_shape TestOnly 精确退化 OHLC、相邻严格无包含、首方向已知、无 tie 结构子域。8 AC 和 17 个来源义务的本票分配部分分别判断；不销完整来源义务、TB-01、#1323 的六个 Destination 或经济域。C 的分页/保留/慢消费和真实压力仍归 C。

修复版独评及根验收结论见本目录后续加入的 R12 报告与 ROOT-ACCEPTANCE；本实施记录不代替独立结论。未 push、未创建 PR、未合入或关票。
