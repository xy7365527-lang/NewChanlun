**PR #1449 R14 DevSkim 增量分诊：门禁 FAIL，未确认新增可行动安全缺陷，保留 1 个旧 needs_review**

本轮把新版全部结果与 750f 已分诊结果逐键比较。当前 **9,233 个唯一位置键在未变 baseline 之外**，真实 Actions 门禁因此失败；静态分类不能把这个检查改称通过。相对 750f，新增 1,273 键、消失 1 键，其中一进一出是同一默认回环地址的行号移动；剩余 **1,272 个新增键均在 R13/R14 证据文档中**。对这些新增词法声明已有具名摘要、内容寻址或压缩容器反证，没有确认新增凭据泄露或弱加密执行调用。

这不等于实际安全缺陷数为零。未变的 `rust/src/theta_v0/nautilus/strategy.rs:314` 限价 entry 映射待办继续为 `needs_review`；基线内与扫描未覆盖面没有在本次重新审计。机器报告把实际安全缺陷总数保留为 `null`，没有虚填 0。本报告不授权修改 baseline、ignore、扫描器、工作流或合入。

**源码与产物绑定**

- 比较起点：`750f933bddffc8251a880555b9beae2cdce75690`；新版 PR head：`256cd822e0d872f1fb65d433ca0b15b01fa621a2`；产品提交：`6189c7e0e5499fc757ddb8369ca557104b526c57`。
- DevSkim run `34709866745` 已完成、结论 `failure`。扫描与普通 artifact 上传均 `success`；gate 在 2026-09-12 18:10:41 UTC 完成并失败。保存日志第 373 行明确为 9,233 个基线外键、17,484 条原始结果，第 9607 行为 exit 1。
- 不能只拿 run.head_sha 代替 checkout 证明。日志第 185–188 行记录实际 checkout 合并提交 `6c52b9e62cfd85390f750b15de3464d2c573432e`，父节点为原 main `2212eba31e0b7419907e622e79f02f7c2cc37706` 与新版 head。GitHub commit API 的合并 tree 和本地 head tree 均为 `6fd745ce161d8c4e4cc341483435bef24b810831`，逐 tree 身份相同。
- 通过 `gh` 下载本 run 的 `devskim-sarif`，artifact ID `10302193750`。压缩包 71,962,203 字节，SHA256 `19e96646cfd2db4fdcd8a4941b96c1382da3c55216119a2a869c8270a986f9eb` 与 GitHub artifact digest 相同；包内只有目标 SARIF。
- SARIF：109,483,815 字节；SHA256 `f64c3023864096b80e71205adf172b332b8f22d43617bd560a1a39199fc51602`；driver `devskim` / `1.0.90+fb2d676ce4`。
- gate、baseline、workflow、Dockerfile、entrypoint 均与 750f 逐字节相同。gate SHA256 `bb5a293ab036624244eb5b29bc2358c352929fc65ea61845e50b53c7f4e2bd51`；baseline SHA256 `df25ff2ec9a0898de03a4450fc3506444f08100ca0657a32665086991e202595`。完整绑定保存在工作目录 `PR1449-DEVSKIM-R14/`。

**精确数量与变化**

沿用实际脚本语义：`ruleId|第一个 physicalLocation URI 仅去前导 file://|startLine`；缺省 `?` / `0`。键不含列号、消息或严重性，不解码 URI，不因人工误报判断自动豁免。机器报告保留每条 raw，键合并仅用于与门禁相同的统计，不丢重复输入。

| 项目 | 750f | R14 新版 |
|---|---:|---:|
| 原始结果 | 16,195 | 17,484 |
| 唯一位置键 | 16,150 | 17,422 |
| 同键重复 raw | 45 | 62 |
| baseline 条目/唯一键 | 8,201 | 8,201 |
| 当前仍出现的 baseline 键 | 8,189 | 8,189 |
| baseline 中当前未出现 | 12 | 12 |
| 基线外唯一键 | 7,961 | 9,233 |
| 基线外 raw | 7,975 | 9,264 |

16,149 个共通键的匹配文本 SHA256 多重集合全部相同。新增 1,273 键、消失 1 键；唯一消失键是 `DS162092|s_session/s_readonly_server.py|1223`，对应新增 `DS162092|s_session/s_readonly_server.py|1261`。Git diff 的 equal-block 映射证明整行字节相同，所报 `127.0.0.1` 仍是既有 `--host` 默认值；不是修复或新远程 debug 入口。旧 baseline 原行 351 的来源映射继续有效。

| 规则 | 新版基线外键 | 对 750f 新增键 | 消失键 |
|---|---:|---:|---:|
| DS117838 | 631 | 214 | 0 |
| DS148264 | 3 | 0 | 0 |
| DS162092 | 41 | 1 | 1 |
| DS173237 | 8,549 | 1,057 | 0 |
| DS176209 | 5 | 0 | 0 |
| DS187371 | 1 | 0 | 0 |
| DS197836 | 3 | 1 | 0 |
| 合计 | 9,233 | 1,273 | 1 |

扣除行移后，新增文档为 1,272 键/1,289 raw。没有真正消失的旧声明可计作安全修复。12 个 baseline 缺席键与旧版相同，仅作信息，不自动当作修复证明。

**新增文档逐类证据**

目录前缀均为 `.chanlun/review-results/issue1371-tb01-b/native-codex-r13-r14/`。`README.md:3、:5、:9` 明确这是版本绑定的验证包，索引用于映射证据，附件按内容地址取原字节；根 `SECURITY.md` 仍是模板，没有拿它臆定部署或信任边界。

| 类别 | 键 / raw | 判定与具名依据 |
|---|---:|---|
| N01：版本、报告、文件与二进制摘要 | 1,057 / 1,074 | `not_actionable`。所有匹配均为带引号的 40/64 位十六进制，并逐项关联到候选/基准/提交、`sha256`、`source_manifest_sha256`、`new_binary_sha256`、`review_report_sha256` 或附件内容键。 |
| N02：索引 artifact_key | 213 / 213 | `not_actionable`。`EVIDENCE-INDEX.json` 全部 213 行均满足 `artifact_key == sha256`，指向有效附件，具名原件长度与摘要相同，没有额外秘密前缀。 |
| N03：令牌规则跨压缩容器 | 1 / 1 | `not_actionable`。DS117838 位于 `EVIDENCE-ATTACHMENTS.json:1`；匹配从已验证附件的 base64 数据内部开始，横跨 9,545,864 个字符及 100 个后续 data 字段，不是一个认证令牌。 |
| N04：低熵哈希规则跨压缩容器 | 1 / 1 | `not_actionable`。DS197836 同在容器第 1 行，起止均在 base64 数据内部，横跨 9,169,452 个字符及 38 个 data 字段；不存在该匹配所声称的时间值进入密码/哈希函数的执行调用。 |

N01 的 81 次 40 位 raw 对应 **5 个唯一值，全部经 `git cat-file --batch-check` 核为现存 commit 对象**。不是仅凭十六进制外形排除凭据。其余 993 次 raw 对应 **249 个唯一 SHA256 值**：逐项 JSON 字段及邻接路径表明是证据、文件或二进制摘要；18 个无独立字段名的同行匹配也核为附件内容地址键。具名上下文包括 `COMMIT-DECLARATIONS.json:4、:73、:74`，`r13/REVIEW.json:2486、:3451`，`r14/REVIEW.json:730、:731、:877`，以及 `r14/READING-LOG.json:114`。

摘要用途与原字节重算分开记录：直接核新包指向的 275 个可解析具名来源，以及附件和冻结 Git blobs，覆盖 **176/249 个唯一 SHA256、791/993 个 raw**；另 **73 个唯一值、202 个 raw 仅核字段用途**。后者主要是报告列出的 SQLite/marker/二进制历史摘要；没有递归扩大成历史全图重审，也没有把它们报作本轮重新验证的原字节。逐 raw 的 `digest_verification` 明列这条边界。

压缩包共 **119 个唯一 xz+base64 附件、103,519,756 个解码原字节**，全部解码、长度和 SHA256 正确。213 条索引的原绝对路径都存在，213 份具名原件全部实算匹配。N03/N04 的精确跨度与所属 data 字段保存在 `CONTAINER-SPANS.json`。这是对所报词法命中及容器完整性的反证，不声称附件解码全文接受过完整秘密审计。

**旧项保留与可行动性**

旧版基线外 7,961 键全部延续：7,960 个同键，以及 1 个上述行移键。未变文件以相同 Git blob、相同匹配文本继承旧证据；行移项另附 equal-block 映射。旧 MD/JSON 原件摘要未变，没有覆盖原完整分诊。

当前基线外合计 **9,232 键为 not_actionable，1 键为 needs_review**；对应 raw 为 9,263 与 1。`needs_review` 的精确键仍为 `DS176209|rust/src/theta_v0/nautilus/strategy.rs|314`：旧限价 entry 映射待办与恒返回 None 的实现保留，没有新证据将它销项，也没有凭此确认安全漏洞。它在唯一的 needs_review 队列中为 rank 1。其余 7 个未变旧项继续沿用原静态结论；历史 #1316/#1385 的承接背景不等于本扫描项已获豁免。

本工位没有发现新扫描指向的可确认凭据或密码学执行缺陷，但实际安全风险仍不能全部排除：上述待办的完整影响未在此关闭；8,189 个当前 baseline 键只清点；扫描未覆盖的产品、配置与依赖路径不在本分诊承诺内。因此仅“新增 confirmed 扫描声明为 0”成立，不能写“实际安全缺陷数为 0”。

**处置与覆盖边界**

全部 raw、唯一键、baseline 差集、新增/消失/行移和新增字段用途均已枚举。机器报告为全部 9,264 条基线外 raw 各保留一项，包括 17 条新同键重复；已基线化结果仅留清点，不重新 verdict。人工核对按具名上下文和类别进行，没有把脚本枚举称为 17,484 处逐行人工审计。

本报告提供 1,272 个新增文档键的具体反证、1 对行移证明及旧项未变依据，供根代理与用户决定如何处理真实失败检查及最终版本批准。**门禁仍然 FAIL，不能由本报告自动转成 PASS。**没有调整 baseline、忽略项、扫描器或工作流，没有写产品、Git/GH 状态或评论，没有重跑扫描、产品或测试，也未启动 Prime。新建内容仅为票属工作目录内的分析记录与本报告。

机器报告：`PR1449-DEVSKIM-R14-TRIAGE.json`。SARIF、下载/run/job 元数据、checkout tree 绑定、完整键差集、逐 raw 字段与摘要/容器证据均在相邻 `PR1449-DEVSKIM-R14/` 中；旧 `PR1449-DEVSKIM-TRIAGE.md/.json` 保持原样。
