# #1372 当前候选浏览器补验

实际本地 Chrome 使用 R5-b 正式 Q（epoch2），固定候选 `3ff8c47ea4ab2aeb4672722e204d9f20b35f9b7f`。原160输入轨迹此时已结束，本包不冒称又做了持续输入试验。通过同源API和生产Client分别完整安装当前160（57页）、点击历史控件得到AsKnown96（28页）、公开load156（55页），再真实Watch157–160四批后回到160；159含3撤回/3替代，gap=null。页内当前渲染状态与完整候选相同。

实际点击目录CC-006、对象input_refs、关系、修订见证；`revised-detail.json`保留完整可见文本，截图`revised-instance.png`目视可读seq98、evt-0002 revision2、price1030502及replaces关系。独立语义审阅另接，不以截图替代全字段比较。页面标题沿用原B观察器，不把标题当成新版本身份。

目录首轮即时采样只读到116→116→1，没有观察到完整切换结束；原件保留为NOT_VERIFIED。另开命名会话，在每次点击后等待checkbox与目标行数同时满足，另存`filter-completion-recapture/`：116→1→116，后台目录始终116，完整candidate字符串始终相同。该补验不改过滤器实现，也不覆盖早先即时读数。

所有结构语义导出先在浏览器JSON编码后转义为ASCII，外层/内层各解析一次；原envelope、脚本及decoded文件均保存，语义导出没有U+FFFD。Browse大块可访问性snapshot仍有替换字符，逐份数量见FACTS；只用来定位控件，不能据它们声称完整结构文字准确。原件未按权威内容修补。

首会话网络采集在关闭后完整复制964件至`network/`。两个命名浏览器均已停止；随后通过同一正式launcher/controller只停止R5-b自有Q99864/S99536，回执在`../POST-RUN-STOP.json`。停止后的观察警告原样保存，最后两者均确认stopped。无活服务留给后续封存。

本包只新增当前候选的GUI正控和界面过滤补验；六类坏帧、旧帧回放、迟到历史页等沿用同字节Client的原R4/无损补验及其独评，不伪称在此又全部重跑。具体源SHA、完整候选/真实Watch/过滤回执与编码界限见FACTS.json。完整C结论由最终合并证据与独立评审决定。
