# #1374 TB-03-A 交付报告草稿

> 名分：绑定 GitHub #1374 的验收工作草稿；按世代宪法 §5 随票保留与归档。
> 对象：[TB-03-A 非空持久重、固定 Q 与 Voice 身份](https://github.com/xy7365527-lang/NewChanlun/issues/1374)。本报告不是关票声明。
> 状态：源码分段独评、正常双跑、真实 GUI 与故障恢复已验并入仓。随后 CI 暴露的唯一模块排序已修复，尾部独评与新构建通过；PR #1454 的最终 CI 以该 PR 实际状态为准。

本片在正式 E/B 会话中保存不同重的非空原始经济事实，保留各自固定 Q、实际量、利润分量及数量与成本历史。新增不可变 Voice 开局记录，以独立认证的受控 γ、来源 K 载体、方向和代际构成身份，经同一公共查询与只读页面显示原始依据。重复原消息或同内容别名返回原回执；同身份异内容发生冲突；历史切面不回填后知事实。

macOS 上三个实际阻碍已修复：完整请求收到后等待 EOF 的停顿、数据库路径被系统映射后误判只读卷、等待业务回执时占用服务控制锁。修复分别通过独立的 206、10、10 项定向检查，之后在正式经济事实和故障执行中复用。传输未增加业务重试。

代码位于隔离分支 `codex/1374-codex`，基线为 `65298686cf3e7c9e94e0b9ceeb83aebad2b5530b`。产品交付 commit 为 `c0ed0497a1bcd3cfca7e5b73a8af1cae229e0863`；本报告随后的提交仅包含验收文档，适用纯文档编译豁免。产品已成功构建和运行的字节与本提交逐项一致；推送与 main 合入状态由 PR 的实际记录确认。完整证据索引、当前源码哈希、所审快照与原文副本见 [EVIDENCE.json](EVIDENCE.json)；本包仅携带紧凑审查原文，日志、数据库、二进制和执行工具保留在索引指向的仓外证据目录。

## 本叶验收对照

| 票面行为 | 当前可核对结果 | 状态与边界 |
|---|---|---|
| 至少两个不透明重身份具有不同非空事实与历史 | 原六事件覆盖 `chong-7f31` 与 `chong-c284`；新 Voice 工作例覆盖 `chong-gamma-v1` 与独立的 `chong-c284`。完整账簿及历史与各自原始事实比对。 | 已验。数量均为明确声明的 TestOnly 输入。 |
| 重、Voice、操作级别、结构级别、定位深度分字段；身份幂等与冲突 | 不可变身份为 `(carrier_ref, gamma_ref, sigma, n)`，根代际为 0；Voice ID 绑定域、会话、重及四元组。绑定、查询、原消息重送、别名和异内容冲突均有正式正常双跑及独验。 | 已验受控单根开局。父子经营的真实成功链未由此证明。 |
| 未知本金和阶段不默认，已知成本具来源 | 本金与阶段未知继续未知；已有已知量、资本化/提取见证与成本逐值核对，存在的非零依据不能被零值吞掉。Voice 生命周期为 `NotRecorded`，责任尾为 `Unknown`。 | 已验具名测试域。不声称 Active、阶段初始化、业务退出或尾责清退。 |
| 正式入口、实际 owner、公共查询/变化与真实浏览器 | 原六事件通过实际 HTTP 与浏览器；新 Voice GUI-v2 完成正式 seed/bind/observe/stoparchive，root 的 20 项真实浏览器检查通过。 | 新 Voice GUI 最终独验已通过；辅助脚本独评与实际浏览器验收分别记账。 |
| 独立来源、同输入新进程重放、所改持久边界恢复 | 原经济六事件正常双跑与 E/B 提交前后八轮故障均独验；新 Voice 正常双跑独验完成。 | 新 Voice Bind 提交前/后各两轮故障完成实际执行、作者对拍和独立验收；各自绑定新 Voice 边界。 |

## 具名证据与统计

下面数字是各次报告的检查、断言或对象数，不是去重后的测试用例总量；作者运行与独立复核分别列出，不相加。

| 证据组 | 作者实际执行 | 独立复核 | 索引 ID |
|---|---|---|---|
| 共享消息与输送抽取 | 复用 S 的 65 个通过 / 1 个既有 ignored、4 个协议测试与实际 8 条 socket 轨迹 | 55 项新检查；后续最终 transport 修复另有 206 项 | `shared_wire`、`transport_final` |
| E/B owner 来源、原消息与永久回执修复 | 原初审暴露四项缺陷，修复后重新验证 | v2 实际 8 场景、28 请求，四项缺陷关闭；后续 statfs 与 Voice 核心分别追加独评 | `owner_initial`、`owner_v2`、`filesystem_delta`、`voice_core` |
| 查询与控制器 | 定向语法、契约与控制路径检查 | 查询 18 场景；控制器 22 条记录；发送锁增量 10 条记录 | `query_final`、`controller`、`controller_send_delta` |
| 原六事件正式正常双跑 | 每轮 1,022 项 owner 检查、376 项 HTTP 检查；152 份结果逐字节对拍；一次真实浏览器 19 项 | 2,437 项；六个归档数据库的完整 SQL 核对 | `economic_normal`、`economic_archive` |
| 原 E/B 提交前/后故障 | 四个边界各两轮，共八轮 | 2,429 项及 5 个负控；216 份 SQL、148 个请求、96 个视图、24 个归档 DB 副本 | `economic_faults` |
| 定义 γ 工作例 | 固定 50,070 条 synthetic OHLC，两张受控 γ | 14,441 项原值断言、11 项 Lean 检查、13 份 Lean 依赖重编译及新进程复现 | `gamma_definition` |
| 实际 γ/K/开局来源认证 | 固定包包含 25 个制品、7,233,022 字节 | 2,756 项；12 个独立端点输出逐字相同；真实来源包加载测试成功 | `voice_authority`、`voice_authority_receipt` |
| Voice 核心与页面契约 | 核心原 16 个测试；页面 24 项定向检查 | 核心 6 个新增独立测试；页面 37 项含 18 份实际归档快照和 16 个拒绝变异 | `voice_core`、`voice_ui` |
| 新 Voice 正式正常双跑 | 每轮 261 项；97 个比较对象；两组新 E/B 进程和数据库 | 5,558 项及 8 个负控；70 条命令、48 份请求、20 份公开视图、66 份 SQL、6 个归档 DB 副本 | `voice_normal`、`voice_normal_comparison` |
| 新 Voice GUI-v2 | 真实入口四阶段及 root 浏览器 20 项通过，五个自建服务正式停止归档 | 3,028 项及 6 个负控；69 条命令、21 份经济请求、16 次原 HTTP、24 份完整 SQL、4 个 DB 副本、9 份 AX、6 个去重 CDP 请求及 2 份完整可见 JSON | `voice_gui_helper`、`voice_gui_root`、`voice_gui` |
| 新 Voice Bind 提交前/后故障 | 第二尝试两边界各两轮；每边界 93 个对象对拍通过，自建进程停止归档 | 12,468 项及 6 个负控；200 条真实命令、100 个原请求、60 份完整视图、120 份完整 SQL、12 份归档 DB 副本 | `voice_fault_driver`、`voice_faults`、`voice_fault_before_comparison`、`voice_fault_after_comparison` |

新 Voice 正常双跑的最终源冻结为 `de6ce23b62c740cd7add99286cb8f8c26b5148b65ddce7a2141d7abe394609f1`；实际正常独评报告为 `2dd03bdf2af739d5af5664a7031494097b2ee4f8d5b393303ada5df8700be269`。两轮完整 SQL、原请求及非 ReadView 原回复逐字一致，完整业务视图仅进程身份允许变化。读取归档数据库时只在完整 DB/WAL/SHM 副本上操作，原归档保持不变。

## 来源与成功范围

本次真实绑定认证包 SHA256 为 `5f20cf8b3359a1e7d89cfa369a19e1576fa51dfcda7eef8e10a51407cd836ee2`，独立认证回执为 `85d9adea47705768fd71de0a39a72e85313b77b4814082746bb9a5963de48d8f`。其中实际 Type3 terminal 的操作级别为 1，区间套执行参数为 2、确认参数为 3、深度为 1；它们不改写 Voice 代际。来源载体是完整 K 载体森林中按规范端点唯一定位的对象，不以非延伸 `OperationSequence`、当前 Active 块或候选锚点替代。

γ 的首次确证观察使用源输入序号 50069，不能当作墙钟纳秒；运行时以明示的 TestOnly 仿射时钟映射。定义 γ 的区间套与背驰确认已经独验；原生候选实际为 6 个、原生链与 Closed 为 0，旧统计以追加勘误订正，原冻结未覆盖。Closed/Strict/N7 属于各自身份与消费要求，此处不声明通过，也不把它们另加为所有定义 γ 的通用前置。

本次未改生产 `formal/` 或 Lean 源；γ 原值与 Lean 工作例位于仓外冻结证据，不是生产形式化修改。最终三个程序的构建成功记录及 SHA256 见 `voice_build` 与 `voice_binaries`，实际执行使用冻结副本。

## 源码覆盖与交付身份

索引逐项列出当前 30 份代码与构建文件、哈希相符的评审来源及旧快照；排序尾部已追加到对应文件的连续审查链。owner 初审的四项缺陷由 v2 逐项关闭，`store.rs` 的后续卷识别与 Voice 增量分别由各自独评覆盖；UI 后续三组数据契约修复由 v2 复核。不能因旧报告文件名存在，就把其旧源码哈希当成当前批准。

`rust/src/lib.rs` 共享抽取报告明示仅覆盖共享导出块，其末尾 `economic_session` 四行导出及 Cargo feature/目标启用条件已由独立 `economic_export_delta` 报告补审，9 项检查通过。两份报告合起来覆盖本片改动，仍不宣称全 `lib.rs` 重审。root 已将实际产品提交的 30 份代码/构建文件逐项对照源码哈希，集合相等；`final_delivery_scope` 独立核对亦通过，报告直接绑定实际产品 commit。`ledger.rs` 采用 owner 初审与 v2 关闭缺陷的连续审查链，不把初审的 `REQUEST_CHANGES` 改写成独立批准。

第二次新 Voice 故障执行已完成四轮对拍。首尝试 `faults/before_commit/run-1` 的业务与停止已经完成，但归档比较器误拒 SIGKILL 遗留 socket；失败现场保留且不计通过。root 修正归档审计对 socket 原路径、inode/device 的核验，并允许具名新 case；产品代码未因此变更。

实际故障独评为 `b2cab3de57e7d867889932c059fbbc631d1c2938ea95a49768bebb157f7c60bb`。提交前故障两轮均在恢复后 1401 获知、1402 提交；提交后故障两轮保留原 1101/1102 及原永久回执。健康重与 E 完整持久数据未变，16 个原 owner PID 均不存在，原 socket 身份与正式停止记录相符。

交付身份：产品 `c0ed0497a1bcd3cfca7e5b73a8af1cae229e0863`，31 个文件包含 30 份代码/构建文件和 1 份工位登记；已构建来源、连续独评与实际运行冻结相符。随后纯文档提交只承载本报告、索引及 25 份独评原文副本（含后续排序尾部）；其准确提交号可从本目录 Git 历史读取。最终独立身份核对的 1,914 项检查全部通过；运行冻结覆盖各自实际使用的源码，未包含的三份测试由对应查询/UI 独评覆盖，不把单次运行冻结夸称为全 30 份文件覆盖。合入 main、主线 CI、关票和工位处置均未在本草稿发生。

本报告只支持 #1374 的持久容器、已知经济事实与受控单根 Voice 只读范围；真实资金、订单、阶段政策、完整父子经营及生产准入未由此证明。#1323 的六个 Destination 与 #1361 等父域保留各自义务；已完成前置 #1359 保持其原验收范围。独立草案 #1453 的合入授权不由本报告产生。


## PR 格式检查尾部

[PR #1454](https://github.com/xy7365527-lang/NewChanlun/pull/1454) 首轮常规 CI 中，Python、页面观察与 fixture 漂移检查成功，Rust 两档 `cargo check` 成功，格式检查仅要求将 `economic_session/mod.rs` 的 `mod voice;` 移到 `mod store;` 后；Rust 后续测试当轮因格式失败而未执行，不能记为通过。

修复提交为 `28694b5f364307da9e630acc64314ea3221e6831`，只含这一行移动。`fmt_tail` 独评 77 项通过，确认五个声明均为普通私有模块、无文本先后依赖，其余 29 个产品文件与已验源码逐字一致。全量 `cargo fmt --all -- --check` 与三个正式程序的新构建均成功；新二进制单独冻结，旧正常/故障/GUI 原件保持不变，未将新构建冒称为新的实际运行。索引以旧业务评审加这处尾部评审覆盖最终源码。

逐代码提交可编译：`c0ed0497a1bcd3cfca7e5b73a8af1cae229e0863` 对应 `voice_build`；`28694b5f364307da9e630acc64314ea3221e6831` 对应 `fmt_tail_build`。首报告 `b5b1ab14ea5947c77fe0b9e8d6f734d5a777d260` 和尾部报告均仅改报告文档，适用纯文档编译豁免。main 合入与关票状态仍未发生。
