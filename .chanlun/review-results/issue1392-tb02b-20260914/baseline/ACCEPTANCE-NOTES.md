# #1392 新笔有限域验收记录

名分：本票工作草稿。范围为已签独立 Oracle 的无同价竞争域；不替代教义正本，不代表 #1323 总图完成。

本轮固定入口为 `/Users/silencehan/Projects/NewChanlun-1392-tb02b`，基线提交 `0c11896361a62e08935f031d5402a8d1edf166ee` 加当前未提交差异。正式 S binary 的 SHA256 为 `2c74b446c70a3ff0c3eafdc3a390a17c9b836954e79bbb337f8aed92284180dc`；437 份源文件绑定见同目录 `1392-release-build-r2.json`，各 runtime 另做起止哈希校验。

`independent-suite-r2/INDEPENDENT-COMPARISON.json` 已通过 17 条轨迹、82 个声明前缀、164 个 arm-prefix、476 份公开候选记录。每条轨迹使用两套全新 S/Q/Chromium，完整 semantic-core 逐字节一致。比较器不导入产品 reducer：包含组、分型窗口和实际极值根直接对冻结手算 Oracle；实际进入候选的端点对独立核算三条件与全部失败位。真实候选覆盖 `001/011/101/111`，未将全部可构造相邻对扩成产品发布义务。初态、实时变化、AsKnown 与事后重访均有原件。

纠错使用真实 raw7 revision2，分别是 prefix9→cut10、prefix14→cut15。旧 cut9/14 的三种采样及事后重访必须存在，旧快照保持；新版实体必须开启二代际，首次获知绑定新 cut、输入前沿、原始修订回执和提交时钟，全部旧笔有带 `raw_fact_revision` 的撤回事件。独立复核针对旧证书价格、错误间距、错误失败位、缺来源坐标、删除旧切点重访、沿用旧实体代际实施了八个内存负控，全部被比较器拒绝。

首次确认的充分性说明 `[新缠论:推论]`：

1. 前提固定为初始包含方向已定、无端点或极值同价竞争、量价精确整数、原始次序追加、无历史插入与修订；merged 间隔与实际 raw 极值根之间的根数阈值均为3。历史修改另开事实代际，不是该追加不变式的前提。
2. `RIGHT_GROUP_SEALED_COMPONENT` 中先由 raw1→5 成笔，再跳过 raw5→6 的不足间距异型候选，以更高的 raw7 替换未确认尾端；随后实际选中 raw7→11 的反向笔，三条件为111。不是仅凭相反分型存在就确认。
3. raw13=(15,11) 与 raw12=(13,9) 非包含，使 raw11 底分型的右邻组封口。顺序包含仅改变当前尾组，已引用的三组成员、价格及唯一极值根不再被本域追加改写。
4. 确定性扫描在同一已冻结前缀作出相同的跳过、同型取舍与成笔决定。反向笔选中之后，前笔不再是尾笔；后续同型严格更极端分支只修改最后一笔，异型失败不回退既有笔，异型成立只追加。删除尾笔的同价未定分支被本叶前提排除，因此已获证的前笔端点不可被本域合法追加替换。
5. 修复后的纯核在首次遇到“已选中反向笔 + 右邻封口”时保存前笔证书，后继尾笔继续延伸不清掉旧证书。S 的确认关系读取实际发布并保留首次获知的同一证书。以上是控制流归纳依据；有限样本本身不宣称证明无限输入域。

源审计确认 `.chanlun/definitions/bi.md:298,313–319,333–345` 与已签 SPEC 要求成立、同型延伸、确认前缀及证据可知性，但没有裁定 prefix14 是唯一或最早确认时点。raw11 底分型的向上尾组也可给 prefix13 的几何稳定性证明；本实现采用更直接的封口充分证书，没有把等待到14改写成教义。S-4 两族确认语义未在此结清。

新增反例保留在 `confirmation-tail-probe/before.json`：在上述14根之后追加 H/L=(11,7),(7,3),(9,5),(12,8)，后继 DOWN 延伸到 raw15。旧纯核在 prefix17 丢掉前笔证书；S 虽保留确认状态，却漏掉 confirms 关系。`confirmation-tail-red-rebuild.log` 记录持久层回归在该关系断言真实失败。修复后纯核4项通过；持久层批次中其他6项通过，新增一项在使用同一 AsKnown 查询口径后单独通过。相关日志为 `confirmation-tail-lib-green.log`、`confirmation-tail-session-green.log`、`confirmation-tail-session-recheck.log`。Astra 独立审查确认控制流及证书/关系一致性在上述域内成立。

运行故障与边界：r1 的26个采集成功，但确认组件两次 fullPage 截图因页面高达528980像素导致 Chromium/Skia位图分配崩溃，失败原件完整保留。r2仅把截图改成 viewport，全部文本、HAR、实际状态断言继续保留；34次采集全部成功。代码构建另撞到 cdylib/rlib 无hash文件被不同 feature 构建覆盖：只刷新 `lib.rs` mtime重编恢复；该文件内容未改。之后本工位库/会话构建统一使用 `s_session` feature，外部纯核探针使用独立 target目录。

四个真实事务故障点的8次运行已全部通过：持久 Begin 后、不可变批次后、对象/关系/变化/索引提交前、提交后回执前。每次均核实际 SIGKILL、原接纳输入在 Ready 前恢复、独立 Q 存活、原消息13份最终权威收据、完整当前/历史公开事实和浏览器状态；每点两 arm 的全部持久表及完整语义核心逐字节一致。最终原件为 `issue1392-tb02b/runtime/release-r3-faults-20260914/RESULT.json`，执行器为同目录 `run-faults-r2.py`。

前一故障尝试 `release-r2-faults-20260914` 两次失败也保留：驱动错误要求旧 writer 的原消息直接重投成功。现役 S 在 lookup 之前核 writer epoch，恢复后应拒绝旧 epoch 请求；原提交结果通过原消息四元身份的 receipt 查询取回。修复仅在驱动：要求旧 writer 精确 StaleWriter、提交前沿不变，随后逐消息查询仍须 Committed；同 writer 重投仍直接要求 Committed。独立 Python 审查确认未改写原消息、未放松隔离/恢复/收据要求。产品 binary 与34次正常采集相同，该驱动修改只针对恢复后旧 writer 分支。

尚需按本票整体交付门核完结果包和适用合入批准；没有生产部署、真实交易、#1392关票或 #1323关图动作。
