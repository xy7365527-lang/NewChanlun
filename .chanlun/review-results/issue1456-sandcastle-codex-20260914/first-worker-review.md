判定：**六份文档的限定范围复核通过，未发现阻断性缺陷。** 文件齐全、复制完整、计数与版本对应一致，Git 差异没有越出指定六文件。此判定不代表 PR #1455 的实际 CI 通过、获得合入批准，或 #1374、TB-03、整图完成。

复核身份：Sandcastle 外部 Codex CLI 只读工蜂；管理节点 `/root/tb03b_contract_preparation`；父任务 `01a07fb3-9da9-7ca0-9be2-e15de6eac7b3`。

工作目录固定为 `/Users/silencehan/Projects/NewChanlun-1455-sandcastle-review`。以下 `D/` 表示 `.chanlun/review-results/issue1374-tb03a-20260914/`。

**一、Git 范围与文件完整性**

实际核得：

- HEAD：`c64e72501b4210c0851f08e020a683b5180ab731`。
- 本地 `origin/main`：`abf5c0f8491fc066a0f2bc47ab4defdb120035c1`，与指定产品基线相同。
- HEAD 的唯一父提交就是该产品基线。
- 对固定基线及本地 `origin/main` 的两点、三点差异均仅包含指定六文件，共 **560 行新增、0 行删除**。
- 工作树与暂存区干净，没有未跟踪文件。六文件之外的产品源码、配置和既有证据均无差异。

| 文件 | 状态 | 实际字节数 | 行数 |
|---|---|---:|---:|
| `.chanlun/agent-roster-2026-09-13.md` | 修改，仅追加两行 | 4,285 | 20 |
| `D/DELIVERY-TAIL.md` | 新增 | 3,413 | 28 |
| `D/devskim_main_review.json` | 新增 | 11,166 | 239 |
| `D/devskim_main_review.md` | 新增 | 2,637 | 17 |
| `D/devskim_pr_review.json` | 新增 | 9,831 | 247 |
| `D/devskim_pr_review.md` | 新增 | 4,364 | 27 |

六份均为实际非空文件，非符号链接。两个新增 JSON 均能完整解析，未发现重复键。

**二、具体核验发现**

1. **四份报告确为原文复制，来源指针有效。**

   `D/DELIVERY-TAIL.md:13`、`:14` 指向两组仓外原目录，`:16` 明确解释相对路径及历史状态；`D/EVIDENCE.json:8` 给出的证据根在本机可达。

   已将四份入仓报告与具名本地原件逐字节比较，全部相等。另核对两个报告 JSON 中全部 **29 处“路径＋大小＋SHA256”引用**，涉及 **27 个不同本地文件**，大小与摘要全部匹配，无缺失或摘要不符。此项只核文件身份，未执行比较工具或重新推导 SARIF 内容。

   三条仓内交叉绑定亦相符：

   - `D/devskim_pr_review.json:221` 绑定 PR Markdown。
   - `D/devskim_main_review.json:214` 绑定主线 Markdown。
   - `D/devskim_main_review.json:39` 绑定 PR JSON。

2. **PR 扫描与 main 扫描分别记录，数字口径一致。**

   依据 `D/devskim_pr_review.md:3`、`D/devskim_pr_review.json:13`、`D/devskim_main_review.md:3` 及其 JSON 对应字段：

   | 扫描对象 | Run ID | raw | unique 位置键 | baseline 外 |
   |---|---:|---:|---:|---:|
   | 历史 base `65298686` | 34759110350 | 34,663 | 34,589 | 26,402 |
   | PR #1454 首轮 `b5b1ab14` | 34781051428 | 35,122 | 35,048 | 26,861 |
   | PR #1454 最终 `5d5d6236` | 34781472772 | 35,168 | 35,094 | 26,907 |
   | 合入 main `abf5c0f8` | 34782751153 | 35,168 | 35,094 | 26,907 |

   四次记录的扫描器版本一致：`1.0.90+fb2d676ce4`。主线 JSON 记录 SARIF `2.1.0`、列语义 `utf16CodeUnits`、11 条规则，各比较对象一致。

   `D/devskim_pr_review.json:117` 起的分项算术成立：

   - 基线 8,201 键，14 键未出现，实际仍出现 8,187 键；各轮 `unique − baseline外` 均为 8,187。
   - 各轮 `raw − unique` 均为 74，未混用两种计数。
   - base→最终 PR：512 增、7 消，净增 505；7 个行移键加 505 个新内容键等于 512。
   - 新内容分类为 `502＋1＋1＋1＝505`；报告标识细分为 `481＋21＝502`。
   - 首轮→最终 PR：145 增、99 消，净增 46，与三种总计数的变化一致。
   - 共同基线外键 26,395，加 512 得 26,907，加 7 得历史 base 的 26,402。

3. **“同树、同扫描内容范围”没有被写成“原始 SARIF 字节相同”。**

   `D/DELIVERY-TAIL.md:5` 所列 main 两父提交与本地 Git 元数据吻合。获批 PR head 与产品基线的树确实同为：

   `9bc6b379220769276ef5070d3a5312acb6231863`

   `D/devskim_main_review.json:103` 与 Markdown `:11`、`:13` 一致：报告记载完整 result 多重集相等、增消均为 0，但原始 SARIF 摘要不同。两个原始 SARIF 的本地字节摘要也分别匹配报告，未出现“同大小即逐字相同”的误判。

   本次没有重新计算完整 result 多重集；对该结论核到的是报告一致性和其具名原件身份。

4. **红灯、历史批准与本补件边界保留正确。**

   `D/devskim_pr_review.json:10` 为 `FAIL`、`:11` 为 `merge_approved: false`；主线 JSON `:7` 的 PASS 限于扫描范围比较，`:9` 仍为 `FAIL`，`:10` 未授予新豁免。

   `D/DELIVERY-TAIL.md:22` 如实保留 **26,907 个基线外键导致门禁失败**及 **3,155 KiB 摘要超过 1,024 KiB 限制**。`:24` 明确要求补件读取自己的实际 PR 检查，未让旧例外自动覆盖新提交。

   `D/devskim_pr_review.json:165`、PR Markdown `:21`、主线 Markdown `:17` 和交付尾文 `:24` 均保留 #1385 的 `needs_review`，没有销项或据扫描红灯直接认定产品漏洞。

5. **roster、交付尾文与既有报告的状态边界没有实质矛盾。**

   `.chanlun/agent-roster-2026-09-13.md:20` 与 `D/DELIVERY-TAIL.md:28` 一致：#1374 仍 OPEN，补件合入及工位清理尚待完成，父 TB、完整 SPEC 和 #1323 六个 Destination 的义务继续保留。

   既有 `D/REPORT.md:61`、`:72` 和 `D/EVIDENCE.json:4`、`:15` 保留较早“未合入”状态；交付尾文 `:16` 已明确这些是冻结历史记录，因此不构成与后续合入记载的冲突。既有产品 30 文件、25 份独评副本等数量属于原交付范围，没有被当成本次六文件数量。

**三、未核事实与证据限制**

- **PR #1455／HEAD `c64e7250` 的实际 CI、DevSkim 状态未核。** 六文档中的扫描数据绑定 PR #1454 和产品基线，不能据此给当前补件标 PASS，也不能臆称其实际扫描计数或红灯原因。
- `D/DELIVERY-TAIL.md:20` 关于常规 CI run `34782751175` 四项 SUCCESS 的说法，未通过原始 CI 记录独立确认；允许证据中的该处仅提供链接，本次未访问。
- PR JSON `:181`、`:191` 引用的临时 checkout 提交 `65cd98e0cd122653f2d02ec31deb1e73400452d1`、`9a2e1ead32f56057e00034aea0e0b48e5dff2196` 不存在于本地 Git 对象库，无法独立复核其树和父关系。已核实的正式 PR head 与 main 同树关系不受此限制。
- GitHub ZIP 归档 digest 未独立验证；两个 JSON 已明示 `archive_digest_independently_verified: false`。本次核对的是解压后具名本地文件摘要。
- 用户历史批准、GitHub 当前票态、#1385 当前承接状态及其他工位实际清理状态，只核到文档记载一致，未核远端实时事实。本地 `origin/main` 也不等于已确认远端此刻最新状态。
- 未重新验证各告警的产品语境、全部业务证据、五项产品验收或全仓安全性。具名扫描证据没有发现不可达文件，但文件身份相符不能替代这些语义复核。

本次仅执行只读文档检查、Git 元数据核查和具名本地原件摘要比较；未重扫安全、未运行测试或构建，未改文件、提交、合入或推送，未读取认证凭据，未调用 GitHub 或外部服务，未启动其他工蜂、队列或 harvest，未创建或认领票，未纳入旧 #1321／#1322／#1325／#1326 方案原件。
