# Issue #486 实施报告（2026-07-27）

## 结论

#486 的限定实现已完成并提交到隔离工位分支：

- 工位：`/tmp/wt-486`
- 分支：`ticket-486`
- 基线：`11b1a02bbe536256aed6cf50c2d9c5d53397ca33`
- 提交：`8e3cc9bd1406b4009f1db480907179f82e6cfaf7`
- 提交说明：`fix(classifier): #486 高级别三类改用结构方向锚`

实现、夹具层 TDD、debug/release 全量库测试以及 counterfactual 漏斗均达到本票口径。wf8 开关双臂自身逐字节一致，开臂 `other_violation_count=0`；但关臂 `trades.jsonl` 未与 `/tmp/vocab_align_dump` 旧金标准逐字节一致，因此本报告不声明整票验收全通过。

同工位只回退 #486 的两个生产传参点后，wf8 关臂重新逐字节命中旧金标准，证明该差异由本票恢复的 L≥1 三类点进入既有策略链直接造成，而不是脏工位或无关改动。若要求“恢复新三类点”与“旧 trades 完全不变”同时成立，需要另行裁定消费/路由边界；本票未越界增加门控。

## 改动清单

唯一提交文件：

- `rust/src/theta_v0/classifier/mod.rs`

生产改动：

1. 批式 L≥1 调用点 `extract_first_third_for_level` 不再把 provenance anchors 交给 BSP 判据，而是从本级 `UnitRange.direction` 构造全 `Some(Direction)` 的结构方向锚。
2. 增量 L≥1 调用点 `extract_first_third_resume` 同样改传本级 `UnitRange.direction` 构成的结构方向锚。
3. provenance 数组继续保留原有平行长度契约；未改变塔 ownership 链。
4. 未改 `judge_third_cert`：`Some(Direction)`、方向匹配、回试方向、严格 `> ZG / < ZD`、`OwnerRef` 指向所破中枢等契约保持原样。
5. 未改变 L0 调用路径，也未改变一类路径的 p117 授权。

测试改动：

- 将 `q7_ruling_c_first_class_structural_direction_third_class_provenance_kept` 改写为 `q7_ruling_c_first_and_third_class_structural_direction_authorized`，锁定 provenance fallback 为 `None` 时按结构方向产三类，并核对 `OwnerRef`。
- 新增结构方向与几何方向不符时拒绝的夹具。
- 新增买侧 `retest == ZG`、卖侧 `retest == ZD` 均拒绝的夹具。
- 新增 L0 三类点逐字段断言，覆盖索引、六个买卖点位、pivot、`OwnerRef`、`struct_break_dir`、`force`。

边界核对：

- 未改 `coverage.rs`、`persistent.rs`、`center.rs`、`recursive_tower.rs`。
- 未改归属链、Reset 语义或 37 课第一合取。
- 提交相对基线的 `git diff --name-only` 只有 `rust/src/theta_v0/classifier/mod.rs`。
- 工位内 `analysis/data_cache` 是按票据命令建立的未跟踪软链，未进入提交。
- 未 push，未 merge，未修改主仓工作区中的源码。

## TDD 红绿记录

### RED

先改测试、不改生产传参，执行：

```bash
cd /tmp/wt-486/rust
CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --lib q7_ruling_c_first_and_third_class_structural_direction_authorized -- --nocapture
```

逐字读数：

```text
assertion `left == right` failed: #486：provenance fallback=None 时，L≥1 仍按结构方向产三买
  left: 0
 right: 1
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 2238 filtered out
```

### GREEN

只修改两个 L≥1 生产传参点后执行：

```bash
cd /tmp/wt-486/rust
CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --lib q7_ruling_c_first_and_third_class_structural_direction_authorized -- --nocapture
CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --lib issue486_ -- --nocapture
```

逐字读数：

```text
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2238 filtered out
test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 2236 filtered out
```

夹具覆盖结论：

| 夹具 | 读数 |
|---|---:|
| L≥1、合法几何、leave provenance fallback=`None` | 产出 1 个三买 |
| 结构方向与几何方向不符 | 0 个三类点 |
| `retest == ZG` | 0 个三买 |
| `retest == ZD` | 0 个三卖 |
| L0 输出 | 逐字段等于既有预期 |
| 新产三买 `OwnerRef` | 精确等于 `OwnerRef::Center(所破中枢)` |

## 全量库测试

在最终源码状态执行：

```bash
cd /tmp/wt-486/rust
CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --lib
CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --release --lib
```

逐字读数：

| 命令 | passed | failed | ignored | filtered out | 用时 |
|---|---:|---:|---:|---:|---:|
| `cargo test --lib` | 2098 | 0 | 141 | 0 | 6.47s |
| `cargo test --release --lib` | 2096 | 0 | 141 | 0 | 1.09s |

编译期间存在仓库既有 warning；本票测试结果为全绿。

## Counterfactual 对照

### 口径与命令

将 `/tmp/wt-473f` 的纯观测探针临时移植到本工位。探针按以下键去重：

```text
(level, CenterId=(start_index, zd, zg), leave 区间, retest 区间, side)
```

探针只记录候选、几何、证书与确认集合；探针源码及临时接线在测量后全部移除，未进入提交。授权后运行命令：

```bash
cd /tmp/wt-486/rust
M8_WIN_FILTER=wf8 VOICE_EXEC=1 THETA_CENTER_OSCILLATION=1 OPSEM_DUMP_DIR=/tmp/wt486-probe-post.1NEYx9 CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
```

随后只在同工位临时恢复两个生产调用点的授权前 provenance 传参，保持同一探针与其余源码不变，执行：

```bash
cd /tmp/wt-486/rust
M8_WIN_FILTER=wf8 VOICE_EXEC=1 THETA_CENTER_OSCILLATION=1 OPSEM_DUMP_DIR=/tmp/wt486-probe-pre.9FaeDy CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
```

两次探针测试均为：

```text
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2236 filtered out
```

测量后恢复最终实现、删除探针，并重新执行了本报告前述 debug/release 全量库测试。

### L≥1 汇总

| 指标 | 授权前 | 授权后 | 变化 |
|---|---:|---:|---:|
| 候选评估次数 | 107,700 | 107,700 | 0 |
| 几何通过次数 | 16,040 | 16,040 | 0 |
| provenance `None` 空拒次数 | 14,122 | 0 | -14,122 |
| 空拒占几何通过比例 | 88.04% | 0.00% | -88.04pp |
| 进入证书判定次数 | 1,918 | 16,040 | +14,122 |
| 唯一几何 | 446 | 446 | 0 |
| 唯一证书 | 48 | 446 | +398 |
| 唯一确认三类点 | 48 | 445 | +397 |

授权前汇总逐字复现验收基线：`14,122 / 16,040 = 88.04%` 空拒、证书判定 `1,918`、唯一确认三类点 `48`。授权后空拒降为 `0`，证书判定次数与几何通过次数同为 `16,040`，唯一证书与唯一几何同为 `446`。

### 分级唯一集合差异

| 级别 | 唯一几何 前→后 | 几何新增/删除 | 唯一证书 前→后 | 证书新增/删除 | 唯一确认 前→后 | 确认新增/删除 |
|---|---:|---:|---:|---:|---:|---:|
| L1 | 254→254 | 0/0 | 24→254 | 230/0 | 24→253 | 229/0 |
| L2 | 125→125 | 0/0 | 18→125 | 107/0 | 18→125 | 107/0 |
| L3 | 67→67 | 0/0 | 6→67 | 61/0 | 6→67 | 61/0 |
| 合计 | 446→446 | 0/0 | 48→446 | 398/0 | 48→445 | 397/0 |

授权后确认结果分类为：

| 级别 | broken | stale | silent | miskill | 合计确认 |
|---|---:|---:|---:|---:|---:|
| L1 | 73 | 139 | 41 | 0 | 253 |
| L2 | 20 | 67 | 38 | 0 | 125 |
| L3 | 2 | 44 | 21 | 0 | 67 |
| 合计 | 95 | 250 | 100 | 0 | 445 |

有 1 个授权后唯一证书未进入唯一确认集合：

```text
level=1
CenterId=(start_index=46928, zd=2699001000000, zg=2723286000000)
leave=[53872, 54209]
retest=[54229, 54674]
side=sell
```

本票不改确认/去重层，因此未继续裁定该 1 个证书的消费原因。

## wf8 双臂

### 命令全文

关臂：

```bash
cd /tmp/wt-486/rust
M8_WIN_FILTER=wf8 VOICE_EXEC=1 OPSEM_DUMP_DIR=/tmp/wt486-wf8-off.j9VXBl CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
```

开臂：

```bash
cd /tmp/wt-486/rust
M8_WIN_FILTER=wf8 VOICE_EXEC=1 THETA_CENTER_OSCILLATION=1 OPSEM_DUMP_DIR=/tmp/wt486-wf8-on.VLD6uq CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
```

两臂测试读数：

```text
关臂：test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2236 filtered out; finished in 12.52s
开臂：test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2236 filtered out; finished in 17.43s
```

### 双臂自身对照

| 产物 | 关臂字节数 | 开臂字节数 | 关臂 SHA-256 | 开臂 SHA-256 | `cmp` |
|---|---:|---:|---|---|---:|
| `trades.jsonl` | 1,060,373 | 1,060,373 | `e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec` | `e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec` | 0 |
| `tower_events.jsonl` | 354,946 | 354,946 | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | 0 |

四层报告两臂逐位相同：

```text
execR=+4626831 MaxDD=0.1085 stage=I(降成本) R=+5154033 LCB(R)=-2176392 → INCONCLUSIVE
```

开臂读数：

```text
other_violation_count=0
```

### 与旧金标准对照

| 产物 | 本票关臂字节数 | 金标准字节数 | 本票关臂 SHA-256 | 金标准 SHA-256 | `cmp` | 判定 |
|---|---:|---:|---|---|---:|---|
| `trades.jsonl` | 1,060,373 | 948,340 | `e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec` | `d79e360d638f720ff97a3cf9632d6d6ed30ee2bf303e7b41ded3672fe135d4e4` | 1 | **失败** |
| `tower_events.jsonl` | 354,946 | 354,946 | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | 0 | 通过 |

因此，“关臂 dump 与金标准逐字节一致”这一验收子项失败，不能声明通过。

### 差异归因的同工位反证

只临时回退 #486 两个生产传参点、保持同一基线与工位，其余提交测试不动，执行：

```bash
cd /tmp/wt-486/rust
M8_WIN_FILTER=wf8 VOICE_EXEC=1 OPSEM_DUMP_DIR=/tmp/wt486-wf8-preauth.FaZflg CARGO_TARGET_DIR=/tmp/wt-486-target cargo test --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
```

读数：

```text
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 2236 filtered out; finished in 12.41s
execR=+4040483 MaxDD=0.0917 stage=I(降成本) R=+4417092 LCB(R)=-1921382 → INCONCLUSIVE
```

回退授权后的 `trades.jsonl=948,340B`、`tower_events.jsonl=354,946B`，二者均与 `/tmp/vocab_align_dump` `cmp=0`。随后恢复 #486 最终实现并重新跑完 debug/release 全量库测试。

这个反证只说明差异源是本票恢复的三类点进入既有消费链；它不授权在 #486 内修改消费链、归属链或 Reset 语义。

## 验收结算

| 验收项 | 状态 | 读数/说明 |
|---|---|---|
| 夹具层 RED | 通过 | 授权前 `0 != 1`，测试失败 |
| 夹具层 GREEN | 通过 | 主夹具 1/1；边界夹具 3/3 |
| 合法几何 + provenance `None` | 通过 | L≥1 产 1 个三买 |
| 结构方向不符仍拒绝 | 通过 | 0 个三类点 |
| `retest == ZG/ZD` 仍拒绝 | 通过 | 买卖两侧均 0 |
| L0 逐字段不变 | 通过 | 逐字段夹具通过 |
| `OwnerRef` 所破中枢 | 通过 | 精确等值断言通过 |
| debug `cargo test --lib` | 通过 | 2098/0/141 |
| release `cargo test --release --lib` | 通过 | 2096/0/141 |
| counterfactual 空拒 | 通过 | 14,122→0 |
| counterfactual 唯一证书 | 通过 | 48→446，新增 398 |
| counterfactual 唯一确认点 | 通过 | 48→445，新增 397 |
| wf8 双臂逐字节相同 | 通过 | 两个产物均 `cmp=0` |
| wf8 开臂其他违规 | 通过 | `other_violation_count=0` |
| wf8 关臂 tower 金标准 | 通过 | 354,946B，`cmp=0` |
| wf8 关臂 trades 金标准 | **失败** | 1,060,373B 对 948,340B，`cmp=1` |
| 禁区与范围 | 通过 | 最终提交仅改 `classifier/mod.rs` |

总体结论：实现目标和 counterfactual 目标达成，测试全绿；但票据列出的 wf8 旧 `trades.jsonl` 金标准不变量未满足，故整体验收状态为**未全通过**。

## 未能判定项

1. 授权后 446 个唯一证书中有 1 个未进入确认集合；本票未深入确认/去重层。
2. 要在新增 397 个唯一确认三类点的同时保持旧 wf8 trades 逐字节不变，需要对下游消费或开关范围作新裁定；该动作超出 #486 范围。
3. wf8 四层输出为 `INCONCLUSIVE`，这是产物原文，不改写为经济性通过或失败。
