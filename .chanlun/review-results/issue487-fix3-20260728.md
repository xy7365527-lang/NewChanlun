# Issue #487 第三修复车（P2 witness 输出接线）验收报告

日期：2026-07-28  
工位：`/tmp/wt-487`  
分支：`ticket-487`  
父提交：`23aa830f43ad2f03a3720345b0dffe6072dacfc5`  
本车提交：`1db0bae4b606d88212c9b4ca1a240e2811d5feab`

## 结论

三轮复审遗留的唯一 P2 已闭合：`CampaignWiringWitness::historical_multi_owner_same_event`
现已接入 `[m8][#357]` 标准验收行。wf8 开关双臂均实际打印该桶，本窗读数均为 `{}`。

本车只改 `rust/src/theta_v0/backtest/wverify_run.rs`：

- 在 `historical_bound_by_level={:?}` 后紧邻增加
  `historical_multi_owner_same_event={:?}`；
- 在 `w.historical_bound_by_level` 后紧邻传入
  `w.historical_multi_owner_same_event`；
- 增加一行 #487 产物级观测口径注释。

没有改其它 witness 桶、参数顺序语义、路由、清算、anchor 门或 #466 D0。提交差异为
`1 file changed, 3 insertions(+)`，`git diff --check` 通过。

最终验收：

- `cargo test --lib`：2117 passed / 0 failed / 141 ignored；
- wf8 关臂：1 passed / 0 failed；
- wf8 开臂：1 passed / 0 failed；
- 两臂新桶：`historical_multi_owner_same_event={}`；
- 三类清算 52、`cover_and_close` 46、跨侧 `write_off_unclosed` 6；
- `historical_bound_by_level={}`、`center_mis_kill_by_level={}`、
  `other_violation_count=0`；
- 关臂 trades/tower 对 `/tmp/vocab_align_dump` 均 `cmp=0`；
- 开臂 trades/tower 对关臂均 `cmp=0`；
- 双臂四层报告逐字节 `cmp=0`。

四层经济结论原文仍为 `INCONCLUSIVE`，本报告不把回归一致改写成经济性通过。

## 代码改动

标准验收格式串新增：

```text
settlement_by_side={:?} historical_bound_by_level={:?} \
historical_multi_owner_same_event={:?} \
center_mis_kill_by_level={:?}
```

对应参数保持同序：

```rust
w.settlement_by_side,
w.historical_bound_by_level,
// ★#487：多 Owner 同事件的产物级观测桶（合法多投递，不参与路由或清算判据）。
w.historical_multi_owner_same_event,
w.center_mis_kill_by_level,
```

该变更只补齐产物级可观测性，不改变 `23aa830f43` 已落地的多 Owner 同事件结算语义。

## 全量库测试

执行：

```bash
cd /tmp/wt-487/rust
export CARGO_TARGET_DIR=/tmp/wt-487-target
cargo test --lib
```

结果：

```text
running 2258 tests
test result: ok. 2117 passed; 0 failed; 141 ignored; 0 measured; 0 filtered out
```

编译只有仓库既有 warning。

## wf8 双臂

### 命令与日志

关臂：

```bash
cd /tmp/wt-487/rust
M8_WIN_FILTER=wf8 VOICE_EXEC=1 \
OPSEM_DUMP_DIR=/tmp/issue487-fix3-off.DrMmin \
CARGO_TARGET_DIR=/tmp/wt-487-target \
cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
  -- --ignored --nocapture
```

开臂：

```bash
cd /tmp/wt-487/rust
M8_WIN_FILTER=wf8 VOICE_EXEC=1 THETA_CENTER_OSCILLATION=1 \
OPSEM_DUMP_DIR=/tmp/issue487-fix3-on.Bg5YPS \
CARGO_TARGET_DIR=/tmp/wt-487-target \
cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
  -- --ignored --nocapture
```

两臂各 `1 passed / 0 failed`。日志：

- `/tmp/issue487-fix3-off.log`
- `/tmp/issue487-fix3-on.log`

四层报告快照：

- `/tmp/issue487-fix3-off-report.md`
- `/tmp/issue487-fix3-on-report.md`

### 新桶实际输出

关臂验收行相关片段：

```text
historical_bound_by_level={} historical_multi_owner_same_event={} center_mis_kill_by_level={}
```

开臂验收行相关片段：

```text
historical_bound_by_level={} historical_multi_owner_same_event={} center_mis_kill_by_level={}
```

因此本次接线已使新桶在标准 wf8 产物行可见；当前生产窗没有触发同事件多 Owner，
读数照实为 `{}`。

### 清算与守卫读数

开臂三类来源：

```text
L1 long  buy=28 sell=1
L1 short buy=3  sell=11
L2 long  buy=6  sell=2
L2 short sell=1
合计=52
```

清算终局：

```text
L1 long  cover_and_close=28  write_off_unclosed=22
L1 short cover_and_close=11  write_off_unclosed=14
L2 long  cover_and_close=6   write_off_unclosed=3
L2 short cover_and_close=1   write_off_unclosed=1
L3 long  write_off_unclosed=1
cover_and_close 合计=46
```

跨侧六笔：

```text
Long 被三卖终结：L1=1、L2=2，共 3
Short 被三买终结：L1=3，共 3
合计=6
```

守卫：

```text
historical_bound_by_level={}
historical_multi_owner_same_event={}
center_mis_kill_by_level={}
other_violation_count=0
other_violation_by_kind={}
```

这些读数与第二修复车一致；本车只让此前不可见的新桶进入同一验收行。

### 金标准、双臂产物与四层

| 产物 | 大小 | SHA-256 | 关臂 vs 金标准 | 开臂 vs 关臂 |
|---|---:|---|---:|---:|
| `trades.jsonl` | 1,060,373B | `e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec` | `cmp=0` | `cmp=0` |
| `tower_events.jsonl` | 354,946B | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | `cmp=0` | `cmp=0` |

金标准目录：`/tmp/vocab_align_dump`。开臂 #466 D0 旁路仍产出：

```text
/tmp/issue487-fix3-on.Bg5YPS/rebase_observability.jsonl  5,593,218B
```

双臂四层报告逐字节 `cmp=0`，共同读数：

```text
execR=+4626831
MaxDD=0.1085
stage=I(降成本)
R=+5154033
LCB(R)=-2176392
结论=INCONCLUSIVE
```

## 提交与边界

独立新增提交：

```text
1db0bae4b606d88212c9b4ca1a240e2811d5feab
fix(theta): expose multi-owner witness in wf8 (#487)
```

边界复核：

- 父节点严格为 `23aa830f43ad2f03a3720345b0dffe6072dacfc5`；
- 未 amend、未 rebase；
- commit 仅含 `rust/src/theta_v0/backtest/wverify_run.rs`；
- 工位既存未跟踪 `analysis/data_cache` 未触碰、未入提交；
- 主仓既存脏改动未触碰；本报告按票面要求单独写入主仓；
- 未 push、未 merge。
