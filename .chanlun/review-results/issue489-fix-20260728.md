# Issue #489 第二修复车实施与验收报告

日期：2026-07-28  
工位：`/tmp/wt-489`  
分支：`ticket-489`  
首车提交：`b9eef9173ce1c080cd0f606eb17128b3d7a9a042`  
本车提交：`1ed6ee6d79d88c5f80f4a85571047c12ab86a6ce`

## 结论

评审所列 5 项均已按裁定修复，核心切口未回改：

- `reset_settlement_count` 已从不可能命中的 `"reset"` 来源字符串过滤，改为读取
  Reset 事件的实际 `SuspensionEventOutcome`；
- Reset 的终结、核销、回补或延续任一路输出非空时，按事件记 1 次；现有 wf8
  实测仍为 0，但已有合成非空输出的负面对照证明该门可被击中；
- `fill.rs` 的链推进与点产出两个生命周期消费点均在取得 `ev_out` 后记录该门；
- 指定旧语义注释、额外搜出的活动态旧注释与 ADR 活动态旧表述均已改为
  “Reset 广播 + 活中枢漏发见证”口径；
- opsem `alive_center_leak` 已登记 additive-only schema 边界；
- ADR 题三挂钩结算注记已补 main 侧审计产物与 GitHub #487 关票评论来源，
  `merge __TBD__` 原样保留。

debug/release 全量测试、wf8 双臂、金标准与四层对拍全部通过。票面读数未动。

## 改动文件

1. `rust/src/theta_v0/strategy/oscillation_campaign.rs`
   - `CampaignWiringWitness` 新增私有事件计数
     `reset_nonempty_lifecycle_outcome_count`；
   - 新增 `record_reset_termination_outcome(event, outcome)`；
   - `reset_settlement_count()` 改读实际事件输出计数；
   - 新增 Reset + 合成非空 termination 输出的活门负面对照。
2. `rust/src/theta_v0/backtest/fill.rs`
   - 链推进与点产出两个 `on_lifecycle_event` 消费点接入新 witness 方法；
   - 链推进点补穷尽性注释；
   - 现有 Reset 端到端用例明确：0 来自实测输出为空，不是类型上不可能。
3. `rust/src/theta_v0/backtest/opsem_dump.rs`
   - 清扫 Reset=死亡旧 schema 注释；
   - 为 `alive_center_leak` 补 additive-only、无严格仓内消费者、金标准不含
     lifecycle 流的边界注记。
4. `rust/src/theta_v0/backtest/wverify_run.rs`
   - 清扫容读格、`death_form` 与 Reset 断言说明中的旧死亡语义。
5. `rust/src/theta_v0/classifier/center_lifecycle.rs`
   - `tolerated_kills` 注释改为只统计 Broken；
   - 清扫一处已作废段序列墓碑里的“同死”措辞。
6. `docs/adr/0001-graded-exit-and-shortdiff-doctrine.md`
   - 补充二的活动态旧“同死”表述更新为 #474 后广播口径；
   - 补充十三题三挂钩结算注记补可审计来源。

## TDD 红绿

测试缝：

- 现有 Reset 端到端路径：观察真实 `ev_out` 为空，读数为 0；
- `CampaignWiringWitness` 公共记录方法：直接喂 Reset + 合成非空
  `SuspensionEventOutcome`，观察读数为 1。

红灯：

```bash
CARGO_TARGET_DIR=/tmp/wt-489-target \
cargo test --lib reset_nonempty_termination_outcome_trips_live_regression_gate
```

结果：退出码 101，编译错误精确为：

```text
no method named `record_reset_termination_outcome`
found for struct `CampaignWiringWitness`
```

绿灯：

| 用例 | 结果 |
|---|---:|
| `reset_nonempty_termination_outcome_trips_live_regression_gate` | 1 passed / 0 failed |
| `gate_reset_broadcast_keeps_center_and_suspension_until_third_class_point` | 1 passed / 0 failed |

负面对照构造的 termination 使用现存 `RebaseVanished + WriteOffUnclosed` 形状；
它不声称 Reset 可产生该来源，只用于证明“Reset + 任意非空实际输出”会击中回归门。

## 旧语义全仓扫描

扫描：

```bash
rg -n -i '同死|Reset.{0,20}杀|died' \
  rust/src docs --glob '*.rs' --glob '*.md'
```

实际修正：

- `opsem_dump.rs`：旧 `reset death_form=doctrinal` / died=死亡说明；
- `wverify_run.rs`：旧 `broken/reset` 共用容读死亡、`一类同死`、Reset doctrinal 说明；
- `center_lifecycle.rs`：旧 `broken+reset` 共计容读死亡说明；
- `center_lifecycle.rs`：已作废段序列墓碑中的“同死”措辞；
- ADR 补充二：仍处于活动文本中的“一类点令段序列与中枢同死”旧表述。

保留项均不是活动态旧语义：

- `died_*` 是 #489 明示保留的旧线格式字段，现有相邻注释均明确其为漏发见证；
- `CampaignLifecycleEvent::Died` / `lifecycle_died` 是 campaign 全平死亡，和 Reset 无关；
- ADR 中仍出现的“同死”位于删除线或“无原文依据”的翻案登记，属于可审计历史记录；
- `Reset 不被杀/不进入杀判据` 等否定性注释与现实现一致。

本清扫只改注释/文档，不改生命周期行为。

## 全量测试

环境：

```bash
cd /tmp/wt-489/rust
export CARGO_TARGET_DIR=/tmp/wt-489-target
```

| 命令 | 结果 | 日志 |
|---|---:|---|
| `cargo test --lib` | 2120 passed / 0 failed / 141 ignored | `/tmp/issue489-fix2-cargo-test-lib.log` |
| `cargo test --release --lib` | 2118 passed / 0 failed / 141 ignored | `/tmp/issue489-fix2-cargo-test-release-lib.log` |

两侧相对首车均只增加本车的 1 个活门测试。编译仅有仓库既有 warning。
`git diff --check`、`git diff --cached --check` 与提交后
`git diff HEAD^ HEAD --check` 均通过。

## wf8 双臂

### 命令与产物

关臂：

```bash
env -u THETA_CENTER_OSCILLATION \
  M8_WIN_FILTER=wf8 VOICE_EXEC=1 \
  OPSEM_DUMP_DIR=/tmp/issue489-fix2-off.vNAOPU \
  CARGO_TARGET_DIR=/tmp/wt-489-target \
  cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
  -- --ignored --nocapture
```

开臂：

```bash
THETA_CENTER_OSCILLATION=1 \
  M8_WIN_FILTER=wf8 VOICE_EXEC=1 \
  OPSEM_DUMP_DIR=/tmp/issue489-fix2-on.UflKsp \
  CARGO_TARGET_DIR=/tmp/wt-489-target \
  cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
  -- --ignored --nocapture
```

两臂各 `1 passed / 0 failed`。日志与四层快照：

- `/tmp/issue489-fix2-wf8-off.log`
- `/tmp/issue489-fix2-wf8-on.log`
- `/tmp/issue489-fix2-wf8-off-report.md`
- `/tmp/issue489-fix2-wf8-on-report.md`

### 票面读数

| 读数 | 关臂 | 开臂 | 首车基线 |
|---|---:|---:|---:|
| Reset 实际非空清算输出 | 0 | **0** | 0 |
| 活中枢漏发 | 0 | **58** | 58 |
| 三类 Broken 清算 | 0 | **57** | 57 |
| 跨侧终结 | 0 | **6** | 6 |
| RebaseVanished | 0 | **8** | 8 |
| MisKill | `{}` | **`{}`** | `{}` |
| `other_violation_count` | 0 | **0** | 0 |

开臂分布：

```text
reset_alive_center_leak_by_level_side =
{(0,"long"):5, (0,"short"):5, (1,"short"):27, (3,"short"):21}
合计 = 58

broken_by_level = {1:47, 2:9, 3:1}
合计 = 57

RebaseVanished = L1 long 3 + L1 short 3 + L2 long 1 + L2 short 1
合计 = 8
```

独立解析验收行所得：

```text
reset=0 leak=58 broken=57 cross=6 rebase=8 miskill={} other=0
```

### 金标准与双臂一致

| 产物 | 大小 | SHA-256 | 关臂 vs 金标准 | 开臂 vs 关臂 | 开臂 vs 金标准 |
|---|---:|---|---:|---:|---:|
| `trades.jsonl` | 1,060,373B | `e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec` | `cmp=0` | `cmp=0` | `cmp=0` |
| `tower_events.jsonl` | 354,946B | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | `cmp=0` | `cmp=0` | `cmp=0` |

金标准目录：`/tmp/vocab_align_dump`。

双臂四层报告均为 2,635B，SHA-256 均为
`7ff58145fd28c496e3d2ef9ce8545fd01c203578f7653c65931eb5e2efd2ddcb`，
逐字节 `cmp=0`。共同读数：

```text
execR=+4626831
MaxDD=0.1085
stage=I(降成本)
R=+5154033
LCB(R)=-2176392
结论=INCONCLUSIVE
```

本报告不把回归一致改写为经济性通过。

`center_lifecycle.jsonl` 双臂同为 543,227B，SHA-256 均为
`083fa44978265412147524d7edce00f408e5d38861848b211f2ad8537e6b6dc6`，
逐字节 `cmp=0`。

开臂 #466 D0 旁路仍产出 76 行、5,778,679B；关臂为空是既有门控行为，
不要求该文件双臂一致。

## 提交与边界

```text
1ed6ee6d79d88c5f80f4a85571047c12ab86a6ce
fix(theta): make reset settlement witness live (#489)
```

- 父提交严格为首车 `b9eef9173ce1c080cd0f606eb17128b3d7a9a042`；
- 独立追加提交，未 amend、未 rebase；
- 提交只含上述 6 个文件；
- `KillTrigger` / `SuspensionTerminationSource` 变体删除、`died_*` 漏发见证与事件照发均未回改；
- #59 线文件未触碰；#487/#466 语义未改；
- 工位既存未跟踪 `analysis/data_cache` 未触碰、未入提交；
- 主仓既存脏面未触碰；本报告按票面要求单独写入主仓；
- 未 push、未 merge。
