# Issue #489 第三修复车（注释漏扫 + ADR 证据档）实施与验收报告

日期：2026-07-28  
工位：`/tmp/wt-489`  
分支：`ticket-489`  
首车提交：`b9eef9173ce1c080cd0f606eb17128b3d7a9a042`  
第二车提交：`1ed6ee6d79d88c5f80f4a85571047c12ab86a6ce`  
本车提交：`a9eadccb31d10ebbca099b3471ab4baeb9ff8c43`

**统计口径**：本报告按指定 wf8 窗口重跑开/关双臂，读取
`[m8][#357]` 的 `suspension_by_source`、Reset 漏发、Broken、跨侧终结和守卫桶；
成交、塔事件及四层报告以逐字节 `cmp` 对 `/tmp/vocab_align_dump` 和双臂互相比对。

## 结论

复审报告 `/tmp/codex-research-456/review-489fix.md` 指出的注释漏扫与统计溯源缺口
均已闭合，改动纯注释/文档，不改任何行为：

- `opsem_dump.rs` 明确拒杀与“指名道姓”只适用于 Broken（三类点）；
  一类点忽略 target、不解析链、直接广播 Reset，非空 `died_*` 仅为活中枢漏发见证；
- `center_lifecycle.rs` 明确 `resolve_kill_target` 只剩 ThirdClass 触发；
  一类点即使载体链外或缺席仍广播 Reset，且不产生 MisKill；
- ADR 补充十三把题三读数标为 wf8 同源直验档，写明统计口径、审查对象与双环境一致；
  工作稿不单独作审查依据，并明确“未做异质复核、升档另开票”。

“判据不回查”结论与 `merge __TBD__` 占位均未改。

## 改动文件

1. `rust/src/theta_v0/backtest/opsem_dump.rs`
   - 修正点载体路由注释；
   - 将 `target=None` 的拒杀条件明确限定到三类点。
2. `rust/src/theta_v0/classifier/center_lifecycle.rs`
   - 修正测试分区标题，删除“一类点进入死亡解析”的旧语义。
3. `docs/adr/0001-graded-exit-and-shortdiff-doctrine.md`
   - 按 `stat-provenance` 两档纪律补题三来源/证据档与未审降级边界。

提交差异为 `3 files changed, 10 insertions(+), 8 deletions(-)`；
`git diff --check`、`git diff --cached --check`、提交后工作树检查均通过。

## 全量库测试

环境：

```bash
cd /tmp/wt-489/rust
export CARGO_TARGET_DIR=/tmp/wt-489-target
```

| 命令 | 结果 | 日志 |
|---|---:|---|
| `cargo test --lib` | 2120 passed / 0 failed / 141 ignored | `/tmp/issue489-fix3-cargo-test-lib.log` |

编译仅有仓库既有 warning。

## wf8 双臂

### 命令与产物

关臂：

```bash
env -u THETA_CENTER_OSCILLATION \
  M8_WIN_FILTER=wf8 VOICE_EXEC=1 \
  OPSEM_DUMP_DIR=/tmp/issue489-fix3-off.vWr3Zn \
  CARGO_TARGET_DIR=/tmp/wt-489-target \
  cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
  -- --ignored --nocapture
```

开臂：

```bash
THETA_CENTER_OSCILLATION=1 \
  M8_WIN_FILTER=wf8 VOICE_EXEC=1 \
  OPSEM_DUMP_DIR=/tmp/issue489-fix3-on.zV63tv \
  CARGO_TARGET_DIR=/tmp/wt-489-target \
  cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
  -- --ignored --nocapture
```

两臂各 `1 passed / 0 failed`。日志与四层快照：

- `/tmp/issue489-fix3-wf8-off.log`
- `/tmp/issue489-fix3-wf8-on.log`
- `/tmp/issue489-fix3-wf8-off-report.md`
- `/tmp/issue489-fix3-wf8-on-report.md`

### 票面读数

| 读数 | 关臂 | 开臂 | 第二车基线 |
|---|---:|---:|---:|
| Reset 实际非空清算输出 | 0 | **0** | 0 |
| 活中枢漏发 | 0 | **58** | 58 |
| 三类 Broken 清算 | 0 | **57** | 57 |
| 跨侧终结 | 0 | **6** | 6 |
| RebaseVanished | 0 | **8** | 8 |
| MisKill | `{}` | **`{}`** | `{}` |
| `other_violation_count` | 0 | **0** | 0 |

开臂明细：

```text
reset_alive_center_leak_by_level_side =
{(0,"long"):5, (0,"short"):5, (1,"short"):27, (3,"short"):21}
合计 = 58

broken_by_level = {1:47, 2:9, 3:1}
合计 = 57

RebaseVanished = L1 long 3 + L1 short 3 + L2 long 1 + L2 short 1
合计 = 8

cross_side_termination_count = 6
center_mis_kill_by_level = {}
other_violation_count = 0
```

本轮读数与第二修复车逐项一致；注释/ADR 修订未造成行为漂移。

### 金标准与双臂一致

| 产物 | 大小 | SHA-256 | 关臂 vs 金标准 | 开臂 vs 关臂 | 开臂 vs 金标准 |
|---|---:|---|---:|---:|---:|
| `trades.jsonl` | 1,060,373B | `e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec` | `cmp=0` | `cmp=0` | `cmp=0` |
| `tower_events.jsonl` | 354,946B | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | `cmp=0` | `cmp=0` | `cmp=0` |

金标准目录：`/tmp/vocab_align_dump`。

双臂四层报告均为 2,635B，SHA-256 均为
`7ff58145fd28c496e3d2ef9ce8545fd01c203578f7653c65931eb5e2efd2ddcb`，
逐字节 `cmp=0`；本轮关臂报告对第二车关臂报告也为 `cmp=0`。共同读数：

```text
execR=+4626831
MaxDD=0.1085
stage=I(降成本)
R=+5154033
LCB(R)=-2176392
结论=INCONCLUSIVE
```

本报告不把回归一致改写为经济性通过。

`center_lifecycle.jsonl` 双臂同为 543,227B，SHA-256 均为
`083fa44978265412147524d7edce00f408e5d38861848b211f2ad8537e6b6dc6`，
逐字节 `cmp=0`。

## 统计溯源边界

ADR 题三挂钩采用的是同源 wf8 直验：工位 #487 三车与 main 侧
merge `e3f1b08d02` 复验束在同一 `suspension_by_source` 口径下得到跨侧终结 6。
`.chanlun/review-results/issue487-fix3-20260728.md` 是未入仓工作稿，不单独构成审查依据；
GitHub #487 关票评论也是同源直验。

因此本次只将既有用户裁定“判据不回查”保持不动，并把来源状态诚实标为
**未做异质复核**。若要把该统计升为异质复核档，需另票用 opsem lifecycle 流
独立重算跨侧账。

## 提交与边界

```text
a9eadccb31d10ebbca099b3471ab4baeb9ff8c43
docs(theta): align reset comments and evidence (#489)
```

- 父提交严格为 `1ed6ee6d79d88c5f80f4a85571047c12ab86a6ce`；
- 独立追加提交，未 amend、未 rebase；
- 提交只含上述 3 个文件，且 Rust 改动全为注释；
- 工位既存未跟踪 `analysis/data_cache` 未触碰、未入提交；
- 主仓既存脏面未触碰；本报告按票面要求单独写入主仓；
- 未 push、未 merge。
