# Issue #489 实施与验收报告

日期：2026-07-28  
工位：`/tmp/wt-489`  
分支：`ticket-489`  
父提交（#487 merge）：`e3f1b08d02388856ff0dd6597986acbe2b2dbedd`  
本票提交：`b9eef9173ce1c080cd0f606eb17128b3d7a9a042`

## 结论

#489 已按 ADR 补充十三/十四完成终态切换：

- Reset 仍广播走势类型终结，但不再 take 主格或容读格中枢，不登记中枢死亡；
- Reset 不再终结挂起，也不再映射为 `WriteOffUnclosed`；
- `RebaseVanished -> WriteOffUnclosed` 原样保留；
- Reset 到场时仍有活中枢，改记为漏发警报：汇总桶按级别/侧计数，逐条
  `center_lifecycle.jsonl` 保留 bar、级别、侧及组成 `CenterId` 的
  `(died_si,died_zd,died_zg)` 等字段；
- 一类点全平的主策略行为未改；
- #487 的紧邻配对、historical-bound、多 Owner 全投递与 `seen=已投递` 未回改；
- #466 D0 重基观测旁路仍工作，核心观测逐字段对拍不变；
- 未触碰 #59 线的 `coverage.rs / persistent.rs / center.rs / recursive_tower.rs`。

最终双侧全测、wf8 双臂、生命周期产物锁均通过。关臂、开臂的成交、塔事件及
四层报告逐字节一致；成交与塔事件均命中既有金标准。

## 改动文件

1. `docs/adr/0001-graded-exit-and-shortdiff-doctrine.md`
   - 补充十三追加 #489 终态切换完成注记，merge SHA 保留 `__TBD__`；
   - 回填题三挂钩：#487 跨侧终结 6 笔（多头被三卖 3 + 空头被三买 3），判据不回查。
2. `rust/src/theta_v0/classifier/center_lifecycle.rs`
   - Reset 改为只读快照后广播，不再 `resolve_kill_target` / `take_alive`；
   - `killed_center_id()`、`death_form()` 对 Reset 恒为 `None`；
   - 删除 `KillTrigger::FirstClass`，Reset 不再具有 stale/MisKill/死亡能力；
   - 改写原 Reset 同死/容读/误杀测试，锁定广播-only 新语义。
3. `rust/src/theta_v0/strategy/center_oscillation_trade.rs`
   - 删除 `SuspensionTerminationSource::Reset` 与 Reset 清空挂起分支；
   - 删除 Reset→`WriteOffUnclosed` 映射，保留 RebaseVanished；
   - 将 #472 过渡测试改为“Reset 零清算 + RebaseVanished 仍核销”的反例/正例锁。
4. `rust/src/theta_v0/strategy/oscillation_campaign.rs`
   - 新增 `reset_alive_center_leak_by_level_side`；
   - 新增 Reset 清算、Broken/级、跨侧终结汇总读数；
   - 更新 witness 测试，锁定漏发桶分侧分级与 Reset 清算恒 0。
5. `rust/src/theta_v0/backtest/fill.rs`
   - 两条生命周期事件路由均接入 Reset 活中枢漏发见证；
   - 新增接线测试：Reset 事件照发、中枢不 take、挂起不终结、零核销，随后由自己的三类点清算。
6. `rust/src/theta_v0/backtest/opsem_dump.rs`
   - Reset 行 `death_form=null`，新增 `alive_center_leak`；
   - 保留兼容字段承载活中枢身份、bar、级别和侧。
7. `rust/src/theta_v0/backtest/wverify_run.rs`
   - `[m8][#357]` 接入 Reset 清算、漏发桶、Broken/级、跨侧终结；
   - 生命周期 wf8 产物锁改为 Reset 无死亡、无 stale/MisKill、只见证主格。

缓存 symlink `analysis/data_cache` 未暂存、未提交。

## TDD 红绿

先改测试取得红灯，再改生产实现：

1. 策略层 `reset_broadcast_keeps_suspension_pending`：
   - 红：旧实现返回 Reset 终结并清空挂起；
   - 绿：Reset 终结/延续均为空，挂起保持。
2. 生命周期层 `first_class_point_broadcasts_without_taking_alive_center`：
   - 红：旧实现 `take_alive()`，在场中枢变空；
   - 绿：事件照发，alive 不变，死亡身份/形态/挂起终结判据均为空。
3. 接线层 `gate_reset_broadcast_keeps_center_and_suspension_until_third_class_point`：
   - 红：新漏发桶尚不存在，随后旧 Reset 清算路径也与新断言冲突；
   - 绿：Reset 零动作/零核销，漏发桶 +1，原挂起随后由自己的三类买点闭合。
4. #472 原测试未删除：改写为
   `reset_does_not_settle_while_rebase_vanished_still_writes_off_unclosed`，
   同时锁 Reset 反例与 RebaseVanished 正例。

靶向回归：

| 范围 | 结果 |
|---|---:|
| `center_oscillation_trade` | 49 passed |
| `center_lifecycle` | 25 passed |
| witness 新读数过滤测试 | 3 passed |
| `center_oscillation_wiring_tests` | 18 passed |
| 一类点主策略全平锁 | 1 passed |

上述接线套件同时覆盖 #487 historical-bound/多 Owner 与 #466 D0 相关既有锁。

## 全量测试

环境：

```bash
cd /tmp/wt-489/rust
export CARGO_TARGET_DIR=/tmp/wt-489-target
```

| 命令 | 结果 | 日志 |
|---|---|---|
| `cargo test --lib` | 2119 passed / 0 failed / 141 ignored | `/tmp/issue489-cargo-test-lib.log` |
| `cargo test --release --lib` | 2117 passed / 0 failed / 141 ignored | `/tmp/issue489-cargo-test-release-lib.log` |

`git diff --check` 与 `git diff --cached --check` 均通过。仓库现有
`cargo fmt --check` 会报告大量与本票无关的全仓基线格式差异；本票未执行全仓机械格式化，
避免把无关面带入最小改动。

## wf8 双臂

### 命令与产物

关臂：

```bash
unset THETA_CENTER_OSCILLATION
M8_WIN_FILTER=wf8 VOICE_EXEC=1 \
OPSEM_DUMP_DIR=/tmp/issue489-wf8-off.fel8bV \
cargo test --release --lib \
theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
-- --ignored --nocapture
```

结果：1 passed / 0 failed，12.83s；日志
`/tmp/issue489-wf8-off.log`，四层快照
`/tmp/issue489-wf8-off-report.md`。

开臂：

```bash
THETA_CENTER_OSCILLATION=1 M8_WIN_FILTER=wf8 VOICE_EXEC=1 \
OPSEM_DUMP_DIR=/tmp/issue489-wf8-on.ibG0lL \
cargo test --release --lib \
theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos \
-- --ignored --nocapture
```

结果：1 passed / 0 failed，16.98s；日志
`/tmp/issue489-wf8-on.log`，四层快照
`/tmp/issue489-wf8-on-report.md`。

关臂新桶均为空/零：

```text
reset_settlement_count=0
reset_alive_center_leak_by_level_side={}
broken_by_level={}
cross_side_termination_count=0
center_mis_kill_by_level={}
other_violation_count=0
```

### #487 最终态与 #489 对照

#487 基线取最终合并态/第三修复车：三类清算 52、跨侧 6；Reset 来源由其
`/tmp/issue487-fix3-on.log` 原始验收行重算为 27。

| 读数 | #487 最终态 | #489 | 归因 |
|---|---:|---:|---|
| Reset 清算 | 27（L1 long 18 + L1 short 8 + L3 long 1） | **0** | Reset 核销半边完全撤销，无残留 |
| Reset 广播 | 59 | 59 | 事件照发 |
| 活中枢漏发警报 | 无该桶 | **58** | 59 条广播中 58 条到场时主格仍有活中枢，1 条场空 |
| 三类 Broken 清算 | 52 | **57** | Reset 不再抢杀后，5 笔继续活到自己的三类点 |
| Broken/级 | `{1:43, 2:9}` | **`{1:47, 2:9, 3:1}`** | L1 +4、L3 +1 |
| `cover_and_close` | 46 | **51** | 与新增 5 笔三类点闭合一致 |
| 跨侧终结 | 6 | **6** | 多头被三卖 3 + 空头被三买 3，判据不回查 |
| RebaseVanished 清算 | 8 | **8** | #472 终态半边原样保留 |
| `write_off_unclosed` 总数 | 41 | **14** | 减少 27 恰为旧 Reset；剩余 = Rebase 8 + 跨侧三类 6 |
| MisKill | 0 | **0** | `center_mis_kill_by_level={}` |
| 其它违规 | 0 | **0** | `other_violation_count=0` |

开臂漏发分桶：

```text
{(0,"long"):5, (0,"short"):5, (1,"short"):27, (3,"short"):21}
合计=58
```

结构化逐条复核 `center_lifecycle.jsonl`：

```text
reset_rows=59
reset_leaks=58
reset_death_form_nonnull=0
reset_missing_identity=0
```

其中 `reset_missing_identity=0` 的身份完整性口径为：所有漏发行都具备
`bar`、`level`、`trigger_side` 及 `died_si/died_zd/died_zg` 等 CenterId 组成字段。

额外运行产物级测试：

```bash
OPSEM_DUMP_DIR=/tmp/issue489-lifecycle-replay \
cargo test --release --lib \
theta_v0::backtest::wverify_run::center_lifecycle_wf8_events_replay \
-- --ignored --nocapture
```

结果：1 passed / 0 failed，13.00s；读数
`reset=59 reset_alive_leak=58 miskill=0`。

### 金标准与双臂一致

| 产物 | 大小 | SHA-256 | 关臂 vs 金标准 | 开臂 vs 关臂 | 开臂 vs 金标准 |
|---|---:|---|---:|---:|---:|
| `trades.jsonl` | 1,060,373B | `e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec` | `cmp=0` | `cmp=0` | `cmp=0` |
| `tower_events.jsonl` | 354,946B | `1d8dff0d29e8925fd2931e05259fad11b71745354482c413f4138d8123dfb34f` | `cmp=0` | `cmp=0` | `cmp=0` |

金标准目录：`/tmp/vocab_align_dump`。

双臂四层报告均为 2,635B，SHA-256 均为
`7ff58145fd28c496e3d2ef9ce8545fd01c203578f7653c65931eb5e2efd2ddcb`，
逐字节 `cmp=0`。共同读数：

```text
execR=+4626831
MaxDD=0.1085
stage=I(降成本)
R=+5154033
LCB(R)=-2176392
结论=INCONCLUSIVE
```

本报告不把回归一致改写为经济性通过。

`center_lifecycle.jsonl` 双臂也逐字节一致：543,227B，
SHA-256 `083fa44978265412147524d7edce00f408e5d38861848b211f2ad8537e6b6dc6`。

## #466 D0 旁路核验

关臂的 `rebase_observability.jsonl` 为空、开臂有产物，是 #466 既有门控行为，不要求两臂
`cmp=0`。#489 开臂产物为 76 行、5,778,679B；#487 最终产物也是 76 行。

相对 #487，变化只出现在 `suspend*` 观测字段：Reset 不再清空挂起后，后续重基时看到更多
pending 身份，这是本票直接语义后果。递归剔除所有 `suspend*` 字段后，两版 D0 产物
SHA-256 完全相同：

```text
0d9964811b92bc2acfccddc14dce8d21b88dd93a63c3851f6572630c8692b1a7
```

因此链映射、重基序列、身份检查与 construction witness 未漂移；#466 D0 旁路未被破坏。

## 提交与边界

```text
b9eef9173ce1c080cd0f606eb17128b3d7a9a042
fix(theta): make reset broadcast-only (#489)
```

仅提交到工位分支 `ticket-489`；未 push、未 merge、未改主仓工作区代码。主仓仅按票面要求
新增本验收报告。
