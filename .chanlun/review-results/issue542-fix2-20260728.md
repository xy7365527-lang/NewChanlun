# #542 第三修复车增量报告

日期：2026-07-28

**统计口径**: 仓内 Rust 单测通过数；POSIX `cmp`/SHA-256 逐字节对拍；wf8 固定窗口冻结脚本与附录 A 397 点/90 笔连接脚本。

## 结论

- 工位：`/tmp/wt-542`
- 分支：`ticket-542`
- 首车提交：`94773903afc10053e99546a46e99811593b1a87b`
- 第二车提交：`1ae0e34b23d2476df68de5e9f03ebdfffbc68be5`
- 第三车提交：`7be19e3f323c072da4593a0678fe5a6fbd094dd6`
- `trades.jsonl` 行的标签已精确化：它是平仓/窗口结算时 `write_trade`
  写出的决策账本完整成交记录（含 exit）；其中 `certificate` 身份来自开仓时
  决策层候选的生产者签发身份，经决策账本 open 快照承载，不声明逐笔执行成交因果。
- `ThirdClassEntryIdentity.center_zd/center_zg` 改用领域别名 `Tick`；
  序列化 helper 同步接收 `Option<Tick>`，仍以原十进制格式输出。
- `judge_third_cert` 的买卖两臂统一调用一个局部纯构造 helper，条件、位图和证书路径未变。
- `BspBits` 守卫继续保留“六 bit 同、载荷不同仍刻意判等”，并直接断言两份
  Debug 字符串逐字节相等，锁住 payload-blindness。
- JSON 字段、顺序、值及交易行为均未改变。

## 改动文件

1. `rust/src/theta_v0/types.rs`
   - `center_zd/center_zg: Tick`；
   - Debug 守卫改为不同载荷的 Debug 字节直接相等。
2. `rust/src/theta_v0/classifier/signal.rs`
   - 新增局部 `third_class_entry_identity` helper，买卖两臂同调。
3. `rust/src/theta_v0/backtest/opsem_dump.rs`
   - 精确化成交行/certificate 口径；
   - `opt_i64_str` 改为 `opt_tick_str(Option<Tick>)`。
4. `rust/src/theta_v0/backtest/runner.rs`
   - 测试注释同步精确口径。

## 测试

```bash
cd /tmp/wt-542/rust
CARGO_TARGET_DIR=/tmp/wt-542-target cargo test --lib
CARGO_TARGET_DIR=/tmp/wt-542-target cargo test --release --lib
```

| 命令 | 通过 | 失败 | ignored |
|---|---:|---:|---:|
| `cargo test --lib` | 2113 | 0 | 134 |
| `cargo test --release --lib` | 2111 | 0 | 134 |

三条 #542 定向守卫分别为 `1 passed; 0 failed`：

- `bsp_bits_schema_only_eq_and_debug_intentionally_ignore_attribution_payload`
- `third_class_entry_identity_survives_candidate_clone_and_copy_without_crosstalk`
- `opsem_dump_third_class_entry_carries_complete_producer_identity`

`git diff --check` 与提交前 `git diff --cached --check` 均通过。

## 三项逐字节对拍

### 1. 第三车 dump 对首车 dump

第三车：

- 关臂：`/tmp/issue542-fix2-wf8-off.jp2mad/trades.jsonl`
- 开臂：`/tmp/issue542-fix2-wf8-on.qGKpyB/trades.jsonl`

首车：

- 关臂：`/tmp/issue542-wf8-off.NwcxCk/trades.jsonl`
- 开臂：`/tmp/issue542-wf8-on.rDOej6/trades.jsonl`

```text
first_car_trades_cmp_off=0
first_car_trades_cmp_on=0
```

第三车两臂各 779 行、1,163,427 B，SHA-256 均为：

```text
06243bac166a241f03eb996e4565dddb64168792537e3bb362c59f52c1e4f9b9
```

### 2. 旧 schema 投影对冻结金标准

```text
projection_rows=779 cmp_exit=0 projected=/tmp/issue542-fix2-wf8-off-projected-old-schema.jsonl gold=/tmp/vocab_align_dump/trades.jsonl
projection_rows=779 cmp_exit=0 projected=/tmp/issue542-fix2-wf8-on-projected-old-schema.jsonl gold=/tmp/vocab_align_dump/trades.jsonl
```

两份投影和冻结金标准 SHA-256 均为：

```text
e10a51dc5d2f4d7ef14d3730b4951280270f8e24f80ce7b4b53ed176c50584ec
```

### 3. wf8 双臂四层报告

```text
arm_four_layer_report_cmp=0
```

两份报告各 2,635 B，SHA-256 均为：

```text
7ff58145fd28c496e3d2ef9ce8545fd01c203578f7653c65931eb5e2efd2ddcb
```

补充：双臂 `trades.jsonl` 也为 `arm_trades_cmp=0`。

## wf8 双臂与附录 A

两臂重跑各 `1 passed; 0 failed`。两臂日志均为：

```text
center_mis_kill_by_level={}
other_violation_count=0
```

即 `MisKill=0`、`other_violation_count=0`。四层共同读数维持：

| 项 | 读数 |
|---|---:|
| `n_orders` | 1362 |
| `execR` | +4626831 |
| `R` | +5154033 |
| `MaxDD` | 0.1085 |
| `LCB_OOS(R)` | -2176392 |
| 终 Stage | `I(降成本)` |
| 三态 | `INCONCLUSIVE` |

附录 A 双臂输出相同：

```text
appendix_points=397 expected_links=90 reproduced_links=90 mislinks=0 complete_identity_rows=454 null_identity_rows=325 partial_identity_rows=0
```

即 397 点全集读入，90/90 直接连接复现，误连 0；完整身份行 454、
空身份行 325、半条身份 0。

## 边界

- 未改既有 `issue542-impl-20260728.md` 与 `issue542-fix-20260728.md`。
- `/tmp/vocab_align_dump/trades.jsonl` 只读，未重订。
- 未改 Order/pending/fill 结构，未新增执行侧身份账。
- 工位既有未跟踪 `analysis/data_cache` 未触碰、未提交。
- 未 amend、未 rebase、未 push、未 merge。
