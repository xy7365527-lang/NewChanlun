# #606 S1 第二修复车：影子评审 F1-F7 处置报告

- 日期：2026-07-28
- 票据：#606（评审全文 `/tmp/codex-research-456/review-606.md`）
- 工位：`/tmp/wt-606`（`CARGO_TARGET_DIR=/tmp/wt-606-target`），分支 `ticket-606`
- 基线：`d3625ede0d` + `2970876739`（S1 返工态），本车追加一个修复 commit
- 对照工位（干净 HEAD，零改动）：`/tmp/wt-606-control`（HEAD `8ed3572c02`，`CARGO_TARGET_DIR=/tmp/wt-606-control-target`）

## 1. F1-F7 处置清单

| # | 严重度 | 处置 | 落点 |
|---|---|---|---|
| F1 | 中高 | **已修**：`OtherwiseDomainSidecarCollector::finish` 在 `enabled` 时显式 `otherwise_domain_sidecar_take()` 丢弃收尾空槽，thread_local 收回 `None`。回归测试 `finish_releases_thread_local_capture_slot` 证明同进程二次 run 零泄漏。 | `opsem_dump.rs::finish` |
| F5 | 中 | **已修**：signal.rs 侧 `GRADE_SIDECAR` 值类型从单值覆盖改 `Vec`（同键 push 不丢），collector 侧新增 `merge_frame_captures` 按签名枚举全部候选级别、逐条分配，跨级同身份不再互吞。回归测试 `cross_level_identical_signature_both_preserved`。 | `signal.rs::GRADE_SIDECAR` + `opsem_dump.rs::merge_frame_captures` |
| F4 | 中 | **已修**：`OtherwiseDomainKey` 删 `center_end_index`，对齐 `CenterId`（si,zd,zg）语义，延伸不改身份，与锁①测试键一致。回归测试 `center_extension_across_frames_collapses_to_one_record`。 | `opsem_dump.rs::OtherwiseDomainKey` |
| F6 | 中低 | **已修**：level 反查失败不再静默落 L0——独立 `unresolved: usize` 计数（`OtherwiseDomainSidecarSummary.unresolved`），该条不进 `records`。回归测试 `unresolved_when_no_level_bsp_matches`。 | `opsem_dump.rs::OtherwiseDomainSidecarSummary`/`merge_frame_captures` |
| F2 | 中 | **已修（诚实化）**：doc 从「按 (level,source_index,side) 与独立统计的 bsp 计数核对相等」订正为「基数对拍，不排除漏一多一相抵」；断言消息同步改称「锁②基数对拍」。未新增独立 bsp 计数分量（该计数本质会重实现 collector 自身逻辑，非真正独立，采用评审给出的诚实降级方案）。 | `wverify_run.rs` doc + 断言消息 |
| F3 | 中 | **已修（诚实化）**：doc 明示锁③是 `Present`/`Missing` 二分对同一 `records` 求和的构造性恒等，「不作验收证据陈列，只作统计展示」；断言保留（无害）但不再称「锁」。 | `wverify_run.rs` doc + 断言消息 |
| F7 | 低 | **已修（口径登记）**：`OtherwiseDomainSidecarSummary` 文档新增段落，明示 upsert 保留的是身份**末次重判** grade，非 Reset **广播时刻**那一帧的 grade 快照；与 Reset 行数相等的只是记录数，非 grade 字段本身。 | `opsem_dump.rs::OtherwiseDomainSidecarSummary` 文档 |
| 顺手 | — | **已改**：memo 命中帧（~99.2% bar）不调 extract、无捕获——相关注释「逐帧」改「逐调用帧」，并明示 memo 命中帧无捕获不损并集。 | `opsem_dump.rs` 模块头/`observe_frame` doc |

## 2. 改动文件

| 文件 | 改动 |
|---|---|
| `rust/src/theta_v0/classifier/signal.rs` | `GRADE_SIDECAR` 值类型 `FirstClassGradeRecord`→`Vec<FirstClassGradeRecord>`（push 代替 insert 覆盖）；新增 `#[cfg(test)] otherwise_domain_sidecar_is_active()` 测试探针；相关文档更新（F5） |
| `rust/src/theta_v0/backtest/opsem_dump.rs` | `OtherwiseDomainKey` 删 `center_end_index`（F4）；新增 `OtherwiseDomainSignature`/`otherwise_domain_candidate_levels`/`merge_frame_captures` 自由函数（F5/F6，`observe_frame` 收窄为薄封装）；`OtherwiseDomainSidecarSummary` 增 `unresolved` 字段（F6）；`finish()` 显式 take 收尾（F1）；文档订正（F7 + 顺手）；新增 `otherwise_domain_tests` 模块 4 条单测 |
| `rust/src/theta_v0/backtest/wverify_run.rs` | 「三锁」文档改「两把真锁 + 一个恒等展示」，断言消息同步诚实化（F2/F3） |

## 3. 验收

### 3.1 单元测试

- `cargo test --lib`：**2135 passed / 0 failed / 136 ignored**
- `cargo test --release --lib`：**2133 passed / 0 failed / 136 ignored**（与 debug 差 2，历史基线同款差值，非本车引入，见 S1 报告 §4.4/§8.5 同款先例）
- 新增 4 条 F1/F4/F5/F6 回归单测（`opsem_dump::otherwise_domain_tests::*`）单独跑通：4 passed / 0 failed

### 3.2 wf8 59/59 全量 + 三锁/两锁读数（未翻动）

```text
cargo test --release --lib theta_v0::backtest::wverify_run::otherwise_domain_wf8_three_locks_and_buckets -- --ignored --nocapture
```

```text
frames=264960 一类点总数=59 (center_lifecycle reset=59)
native(趋势一类)=22 otherwise(否则域)=37
(level,side=0long/1short)→(native,otherwise)={(0,0):(2,3),(0,1):(4,2),(1,1):(5,22),(3,1):(11,10)}
五桶(level,side)→[missing_leave,missing_retest,same_direction,leave_not_outside,retest_reentered]=
  {(0,0):[0,0,0,0,3],(0,1):[0,0,0,0,2],(1,1):[0,7,14,1,0],(3,1):[0,10,0,0,0]}
```

与 S1 返工态（`2970876739`）逐位相同——F1-F7 均为诚实化/边界订正，未见触发（本窗无跨级签名碰撞、无中枢延伸跨帧重复捕获场景），故数值不翻动，行为不变。

### 3.3 行为零变化（工位 vs 干净 HEAD）

命令（关臂，off-arm）：

```bash
unset THETA_CENTER_OSCILLATION THETA_OTHERWISE_DOMAIN_SIDECAR
M8_WIN_FILTER=wf8 VOICE_EXEC=1 OPSEM_DUMP_DIR=<dir> \
cargo test --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
```

命令（开臂，on-arm）：同上加 `THETA_CENTER_OSCILLATION=1`。

| 对比 | trades.jsonl | tower_events.jsonl | center_lifecycle.jsonl |
|---|---|---|---|
| 关臂：我工位 vs 干净 HEAD | `cmp=0` | `cmp=0` | — |
| 开臂：我工位 vs 干净 HEAD | `cmp=0` | `cmp=0` | `cmp=0` |

开臂读数（与 #489/S1 态逐位一致）：

```text
reset_alive_center_leak_by_level_side={(0,"long"):5,(0,"short"):5,(1,"short"):27,(3,"short"):21}  合计=58
broken_by_level={1:47,2:9,3:1}  合计=57
cross_side_termination_count=6
center_mis_kill_by_level={}（MisKill=0）
other_violation_count=0
center_lifecycle.jsonl 内 kind=="reset" 行数 = 59
```

**结论**：F1-F7 全部落在 `THETA_OTHERWISE_DOMAIN_SIDECAR` 门控分支内部（signal.rs 的 `GRADE_SIDECAR` 值类型改动、opsem_dump.rs 的 collector 内部逻辑改动、wverify_run.rs 的文档/断言消息改动）——门未开（生产恒态、`m8_e2e_all_systems_oos` 不设该 env）时这些改动完全不可达，trades/tower_events/center_lifecycle 三产物开臂/关臂均与干净 HEAD 逐字节相同，行为零变化维持。

## 4. 统计口径行

```text
schema=otherwise_domain_wf8_v1_fix2
git_sha(worktree base)=2970876739（S1 返工态，本车在此基础上追加修复 commit）
data_sha256=btc_1m_full.json（analysis/data_cache 符号链接，未改动）
window=wf8(2023-08-17..2024-02-16, 264960 bars)
env=THETA_OTHERWISE_DOMAIN_SIDECAR=1（关闭 THETA_CENTER_OSCILLATION，M8 未设）
pair_policy=fixed_first
lock①=键唯一（真断言，全窗无碰撞）
lock②=基数对拍（sidecar 记录数 == center_lifecycle Reset 行数；非按键独立计数核对，不排除漏一多一相抵，F2 订正）
展示③=账平展示（Present/Missing 二分构造性恒等，非验证，F3 订正）
unresolved=0（本窗无 level 反查失败，F6 新增计数）
grade 口径=末次重判（非 Reset 广播时刻快照，F7 登记）
```

## 5. 复现物

- 工位：`/tmp/wt-606`（本报告对应 commit，见下）
- 对照工位：`/tmp/wt-606-control`（HEAD `8ed3572c02`，零改动）
- 新增回归测试：`rust/src/theta_v0/backtest/opsem_dump.rs::otherwise_domain_tests`（4 条，纯单元测试，无需 BTC 数据）
- wf8 验收：`rust/src/theta_v0/backtest/wverify_run.rs::otherwise_domain_wf8_three_locks_and_buckets`（`#[ignore]`，需 BTC 数据）
