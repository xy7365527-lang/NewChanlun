# #606 S1 抛光车——终审 4 条不阻断项处置

终审结论（`/tmp/codex-research-456/review-606fix3.md`）：**通过**（换结构方向正确，无功能性缺陷），
4 条发现均属验证力/名分层，本车逐条抛光，不改判据本体。

## 1.（中）逐级断言——D5

原②基数对拍只核对总数（`sidecar.records.len() == center_lifecycle reset 行数`），不排除"一桶漏记、
另一桶多记"总数相抵的假阴性。补 D5：`records` 按 `(level, side)` 分桶总数（native+otherwise）与
`center_lifecycle.jsonl` 的 reset 行按 `(level, trigger_side)` 分桶重新分 `alive_center_leak` 真/假
两类计数，逐桶断言：

```
diff(level,side) = records桶总数 - reset_leak=true桶计数
assert_eq!(diff, reset_leak=false桶计数)
```

代码位置：`rust/src/theta_v0/backtest/wverify_run.rs`，`otherwise_domain_wf8_grade_buckets` 测试内
（reset 行解析改为一次性取出 `level`/`trigger_side`/`alive_center_leak` 三字段，供②与 D5 共用，不
重复读盘）。

**实跑读数**（`cargo test --release --lib
theta_v0::backtest::wverify_run::otherwise_domain_wf8_grade_buckets -- --ignored --nocapture`，照实测
写，未硬编码期望值）：

```
records(level,side)→总数        = {(0,0):5, (0,1):6, (1,1):27, (3,1):21}
reset(level,trigger_side)→leak=true  计数 = {(0,0):5, (0,1):5, (1,1):27, (3,1):21}
reset(level,trigger_side)→leak=false 计数 = {(0,1):1}
```

逐桶核对：`{0,L}5v5`(diff=0) / `{0,S}6v5`(diff=1) / `{1,S}27v27`(diff=0) / `{3,S}21v21`(diff=0)——
四桶 diff 之和 = 1，恰等于该桶（唯一）`leak=false` 计数，与终审文档预告的读数逐位一致。断言实跑绿。

## 2.（低）fallback 不捕获——单测锁

新增 `rust/src/theta_v0/classifier/signal.rs::tests::fallback_full_recompute_does_not_capture_grade_sidecar`：
复用既有 `force_proxies_juxtaposed_on_first_class_candidate` fixture（保证产一个 `buy1`），
`otherwise_domain_sidecar_begin()` 后直调 `extract_signals_with_hist_anchored`（`level=None` 全量
fallback 路径），断言产出确有 `buy1`（防测试空转）且 `otherwise_domain_sidecar_take()` 取回空
`Vec`——钉死"只有 `extract_first_third_resume`（incremental resume）传 `Some(level)` 才写入
`GRADE_SIDECAR`"这一契约，不再只是 `judge_segment` 文档里的口头声明。

## 3.（低）名分残留

- `wverify_run.rs:2034` dump 目录名 `otherwise_domain_wf8_three_locks_{}_{}` → 改为
  `otherwise_domain_wf8_grade_dump_{}_{}`（不含 `three_locks` 字样）。
- 全仓 ASCII grep 清零：

```
grep -rln "three_locks" --include=*.rs .   → 0 命中
grep -rl  "three_locks" . | grep -v '^\./\.chanlun/review-results/'  → 0 命中
```

仅历史报告（`issue606-impl-20260728.md`/`issue606-fix2-20260728.md`/`issue606-fix3-20260728.md`）
仍含该词，按纪律不动（历史记录，非当前代码/测试名）。

## 4.（低）同工位 on/off 对拍

原 §6.3 对照臂 = 干净 HEAD `8ed3572c02`（无 sidecar 代码）vs 工位（关臂），缺"同一份代码、只切
sidecar env"这条更直接的证据。本车补：同 commit 下 `m8_e2e_all_systems_oos`（wf8，关臂，
`THETA_CENTER_OSCILLATION` 未设）分别以 `THETA_OTHERWISE_DOMAIN_SIDECAR` 未设/`=1` 各跑一次，
`OPSEM_DUMP_DIR` 各自独立目录，`cmp` 二进制比对：

```bash
M8_WIN_FILTER=wf8 VOICE_EXEC=1                                OPSEM_DUMP_DIR=/tmp/onoff_wf8_off cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
M8_WIN_FILTER=wf8 VOICE_EXEC=1 THETA_OTHERWISE_DOMAIN_SIDECAR=1 OPSEM_DUMP_DIR=/tmp/onoff_wf8_on  cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture

cmp /tmp/onoff_wf8_off/trades.jsonl        /tmp/onoff_wf8_on/trades.jsonl        # exit=0
cmp /tmp/onoff_wf8_off/tower_events.jsonl  /tmp/onoff_wf8_on/tower_events.jsonl  # exit=0
```

`trades.jsonl`/`tower_events.jsonl` 二进制逐位相同（`cmp` exit=0），两次跑均
`execR=-312337 MaxDD=0.0776`（读数逐位同 §6.3）。补足"同工位 sidecar on/off"这一缺口证据——
D4 sidecar 是纯只读旁挂，打开与否对生产 trades/tower 输出零影响。

## 5. 统计口径行

`cargo test --lib` → **2135 passed; 0 failed; 136 ignored**（较 fix3 报告的 2134 净增 1 = 本车新增
的 fallback 不捕获单测；较 fix3 报告的 59/59 无变化——D5 只加断言，未改判据/数据）。
`otherwise_domain_wf8_grade_buckets`（wf8 全窗，`--ignored`）：`一类点总数=59`
（`native=22 otherwise=37`），逐级/五桶表与 fix3 §6.2 逐位相同。

## 6. commit

（见提交记录，紧随本次改动）
