# #746 评审 FAIL 返工——实装记录

- 日期：2026-07-29
- 票据：#746（复评审原文 `/tmp/codex-research-456/review-746.log`，判定 **FAIL**：①②④ PASS，③三条默认臂错，⑤验收材料不在案）
- 工位：`/private/tmp/wt-746`（分支 `ticket-746`），从 `6e4193bf4e`（merge #745）切
- **未 commit、未 push、未 merge。**

## 1. 三条默认臂订正（`rust/src/theta_v0/env_registry.rs`，五列内容，非代码语义）

| 键 | 原写（错） | 订正后（实测两处/两侧调用点核对） |
|---|---|---|
| `NEST_GATE_SMOKE_BARS` | 单臂「未设 ⟹ 50_000」 | 两臂并存：`nest_gate_diag_terminal_events`（runner_tests.rs:4865）未设 ⟹ 50_000；`nest_gate_smoke_stats`（runner_tests.rs:5168）未设 ⟹ 20_000（漏第二臂，已补） |
| `ECON_L2_MAX_BARS` | 单臂「未设 ⟹ 300_000（调用点硬编码 fallback）」 | 两臂并存：多数调用点（`MAX_BARS`/`MAX_BARS_DEFAULT` 常量）未设 ⟹ 300_000；`l2_depth_distribution_dx`（econ_positive.rs:5253）与 `l2_btc_earning_shares_unreachable_hwm_debearing`（runner_tests.rs:6031）两处未设 ⟹ `usize::MAX`（不截断，跑全历史，漏第二臂，已补） |
| `DELTAFREE_DUMP` | 单写「未设 ⟹ /tmp/finalpha/deltafree_pertrade.tsv」（其实是**读侧** fallback） | 分写/读两侧：**写侧** `dump_deltafree_pertrade`（wverify_run.rs:270）未设/空 ⟹ `/tmp/wv_full_zdecision.tsv`；**读侧** `deltafree_exact_recompute`（wverify_run.rs:2950，离线复算入口）未设 ⟹ `/tmp/finalpha/deltafree_pertrade.tsv`（原写侧真源写错，已订正为写侧优先并标注读侧 fallback 非写侧真值） |

逐行核对命令与结果见下方「附：核对轨迹」。

## 2. 测试补强

### (a) const 集合↔REGISTRY 数组对拍（新测试 `const_declarations_match_registry`）

`env_registry.rs` 自读own source（`include_str!("env_registry.rs")`），逐行解析
`pub const FOO: &str = "FOO";` 形式声明，与 `REGISTRY` 数组逐键对拍——新增 `pub const` 不同步
登记 REGISTRY 时本测试红（`registry_has_46_entries` 只锁数组长度，锁不住这个原缺口）。

### (b) `no_stray_env_literals_outside_registry` 正则扩到 helper 传字面量 + 16 处 set_var/remove_var

- 正则从 `env::var(_os)?\("[A-Z_0-9]*"\)` 扩到
  `(env::(var|var_os|set_var|remove_var)|env_flag)\("[A-Z_0-9]+"`——覆盖 `env_flag("X",…)` 这类
  helper 间接传字面量的模式（正是 `THETA_LINEAGE_TRACE` 首轮 grep 漏检的模式本身，原规则只锁
  `env::var(...)` 直读，现补防御性覆盖），以及 `env::set_var`/`env::remove_var` 裸字面量。
- 选择「逐处也收进注册表」而非只扩正则不修实处：16 处残留 `set_var`/`remove_var` 裸字面量
  （`runner_tests.rs` 7 处、`gamma_dump.rs` 3 处、`wverify_run.rs` 6 处）已全部改引用
  `env_registry::` 对应常量（`OPSEM_DUMP_DIR`/`KAPPA_BARRIER_NUM`/`KAPPA_BARRIER_DEN`/
  `OPSEM_GAMMA_DUMP_DIR`/`THETA_CENTER_OSCILLATION`/`THETA_OTHERWISE_DOMAIN_SIDECAR`/
  `DELTAFREE_DUMP`），零行为（字面量值不变，只改字面量持有点）。收口后
  `grep -rn 'set_var("\|remove_var("' rust/src/theta_v0` 零命中。
- 全仓 `env_registry::` 引用点（登记表本身除外）从原 82 处升到 **98 处**（82 + 本轮 16 处
  set_var/remove_var 迁移）。逐文件分布见「附：迁移清单」。

## 3. wf8 cmp=0 实测

命令（与 #745 报告同一金标准锚，独立 dump 目录避免互相污染）：

```
M8_WIN_FILTER=wf8 VOICE_EXEC=1 OPSEM_DUMP_DIR=/tmp/i746-check cargo test --release --lib \
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture
```

结果：`execR=-477478 MaxDD=0.0841`（与 #745 基线一致）；`trades.jsonl` **800 行**，

```
$ sha256sum /tmp/i746-check/trades.jsonl
b980389f9793d0049006bfce46cb78d7606e7d7eb06ab58827adf832bb423da8  /tmp/i746-check/trades.jsonl
```

与 #745 报告登记的基线 SHA `b980389f9793d0049006bfce46cb78d7606e7d7eb06ab58827adf832bb423da8`
**逐字节一致（cmp=0）**——本票 21 处调用点字面量→`env_registry::` 常量置换 + 16 处
set_var/remove_var 迁移，对生产路径零行为影响成立。

## 4. 测试计数口径

两个不同的 `cargo test` 调用口径不可互相替代，分别锁：

| 命令 | 结果 | 覆盖范围 |
|---|---|---|
| `cargo test --lib theta_v0` | **1989 passed; 0 failed; 103 ignored**（`; 645 filtered out`） | 仅 `theta_v0` 子树（本票改动的全部范围） |
| `cargo test --lib`（全 lib，无 filter） | **2599 passed; 0 failed; 138 ignored**（`0 filtered out`） | 全 crate（`theta_v0` 之外还有 `recursive_t`/`trading` 等其他模块） |

原评审指出「1986/0 与实测 lib 测试总数 2736（含 ignored）无从对账」——二者本就不是同一集合：
前者是 `--lib theta_v0` 过滤后的子集通过数，后者是全 lib（passed+ignored）总数，两个命令的
`filtered out` 字段（645 vs 0）就是证据。本轮新增 1 个 const↔REGISTRY 对拍测试后，
`theta_v0` 子集 passed 数从票面基线上浮为 1989；全 lib passed+ignored 合计 2737
（2599+138，含本次新增测试），与旧口径「2736」的 1 差额即本轮新增测试。

## 附：核对轨迹（三条默认臂逐点 grep）

```
$ grep -n "NEST_GATE_SMOKE_BARS" rust/src/theta_v0/backtest/runner_tests.rs
4865:    let max_bars: usize = ...NEST_GATE_SMOKE_BARS).ok().and_then(|s| s.parse().ok()).unwrap_or(50_000);
5165:    let max_bars: usize = ...NEST_GATE_SMOKE_BARS).ok().and_then(|s| s.parse().ok()).unwrap_or(20_000);

$ grep -n "unwrap_or(usize::MAX)\|unwrap_or(MAX_BARS" rust/src/theta_v0/backtest/econ_positive.rs rust/src/theta_v0/backtest/runner_tests.rs
econ_positive.rs:5254:  .unwrap_or(usize::MAX);
runner_tests.rs:6032:   .unwrap_or(usize::MAX);
（其余 econ_positive.rs 调用点均 unwrap_or(MAX_BARS/MAX_BARS_DEFAULT) = 300_000）

$ grep -n "DELTAFREE_DUMP" rust/src/theta_v0/backtest/wverify_run.rs
270:  （写侧 dump_deltafree_pertrade）unwrap_or_else(|| "/tmp/wv_full_zdecision.tsv".into())
2950: （读侧 deltafree_exact_recompute）unwrap_or_else(|_| "/tmp/finalpha/deltafree_pertrade.tsv".into())
```

## 附：迁移清单（98 处 `env_registry::` 引用，登记表本身除外）

```
18 backtest/runner_tests.rs
15 backtest/econ_positive.rs
13 backtest/wverify_run.rs
 9 backtest/incremental.rs
 7 classifier/mod.rs
 7 backtest/admission.rs
 5 lineage_book.rs
 4 backtest/gamma_dump.rs
 4 backtest/fill.rs
 3 classifier/rebase_txn.rs
 3 backtest/opsem_dump.rs
 2 classifier/incremental/invalidate.rs
 1 classifier/tests/level_signals.rs
 1 classifier/stage_profile.rs
 1 classifier/signal.rs
 1 classifier/incremental/mod.rs
 1 classifier/cand_delta.rs
 1 backtest/segment_gn.rs
 1 backtest/perm_test.rs
 1 backtest/capture_oos.rs
```
