# #748（C4）classifier/mod.rs 瘦身——诊断驱动器与入口分层：实施报告

工位：`/private/tmp/wt-748`（main HEAD `3245d8f996` 切出，`CARGO_TARGET_DIR=/tmp/wt-748-target`）。
纪律：纯移动、零逻辑改动；不 commit。

## 1. 迁出清单

| 迁出内容 | 原位置（mod.rs 迁前） | 新位置 | 行数 |
|---|---|---|---|
| `cp_replay_diagnostics`（内嵌 mod） | mod.rs:141-202 | `classifier/diag/cp_replay_diagnostics.rs` | 67 |
| `cand_delta_tower`/`_cached`/`_entry_tower` + `cache_series_ok` + `cand_delta_tower_with_series` | mod.rs:775-935 | `classifier/diag/cand_delta.rs` | 190 |
| `cp_recall_upper_bound_audit` | mod.rs:939-976 | `classifier/diag/cp_recall_audit.rs` | 51 |
| `stage_profile`（内嵌 mod） | mod.rs:1545-1671 | `classifier/diag/stage_profile.rs` | 131 |
| `oracle_probe`（内嵌 mod，`#[cfg(test)]`） | mod.rs:1677-1783 | `classifier/diag/oracle_probe.rs` | 111 |
| `LevelCache`/`AreaCache`/`TowerCache`（含 impl）/`update_closes_cache`/`compute_macd_hist_incremental`/`rebuild_macd_state_to` | mod.rs:1030-1538 | `classifier/tower_cache.rs`（重建声明——同名 #576 曾开、#760 判孤儿删除，本次真正 `pub mod tower_cache;` 接线，非孤儿） | 563 |

`classifier/diag/mod.rs`（15 行）声明五个诊断子模块并 `pub use` 出 `cand_delta`/`cp_recall_audit` 的公开函数，
`classifier/mod.rs` 顶部以 `pub use diag::{...}` + `pub use tower_cache::TowerCache;` 保全仓调用路径
（`classifier::cp_replay_diagnostics::...`、`classifier::stage_profile::...`、`classifier::oracle_probe::...`、
`classifier::cand_delta_tower(...)`、`classifier::cand_delta_tower_cached(...)`、
`classifier::cand_delta_entry_tower(...)`、`classifier::cp_recall_upper_bound_audit(...)`、
`classifier::TowerCache`）逐字节不变，18 个外部消费文件（`src/bin/*.rs` 9 个 + `src/theta_v0/backtest/*.rs`
4 个 + `recursive_tower.rs`）零改动。

可见性调整（非放宽，仅补足跨文件访问，机制同 #576 先例）：
- `LevelCache`/`AreaCache`/`TowerCache` 全字段标 `pub(super)`（可见域 = `classifier` 及其后代，含
  `diag::cand_delta` 的 `cache_series_ok` 读 `TowerCache` 私有字段）；`update_closes_cache`/
  `compute_macd_hist_incremental` 标 `pub(super) fn`（`mod.rs` 主循环 + `mod tests` 双消费点）；
  `rebuild_macd_state_to` 保持纯私有（仅本文件内消费）。
- `segment_to_unit`/`unit_to_segment`（mod.rs 原私有 fn，diag 两文件消费）标 `pub(crate) fn`。
- `cache_series_ok`（diag/cand_delta.rs 内部函数）标 `pub(crate) fn`，`mod.rs` 顶部
  `use diag::cand_delta::cache_series_ok;` 保 `mod tests` 内既有单测（`cand_cache_guard_accepts_incremental_contract`）
  不改一字符可用。

## 2. mod.rs 收窄

| | 行数 |
|---|---|
| 迁移前（main HEAD） | 6464 |
| 迁移后 | 5389 |
| 净减 | 1075（-16.6%） |

mod.rs 现收窄为：五入口（`classify`/`classify_with_tower`/`classify_with_tower_events`/
`classify_with_tower_incremental`/`classify_with_tower_events_incremental`）+ 塔构造主循环
（`classify_impl`/`compose_level`/`extract_second_for_level` 等私有裁决辅助）+ `mod tests`
（未动，行内单测仍 3087 行区间原样保留）+ `mod incremental_profile`（未动）+ 上方 re-export 门面。

## 3. 增量/全量对拍锁复核

`DIAG_CANDCACHE` env-gated 对拍断言（`cand_delta_tower_cached` 内 `assert_eq!(out, full, ...)`，票面所指
E2E-S1 增量/全量对拍锁）随 `cand_delta.rs` 整体迁移，字符零改动、gate 语义不变——原样随迁不失效。

## 4. 验收

- **wf8 金标准 cmp=0**：`M8_WIN_FILTER=wf8 VOICE_EXEC=1 OPSEM_DUMP_DIR=... cargo test --release --lib
  theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture`
  迁移前后两次独立跑（迁移前 = 本票在本工位建的基线，迁移后 = 迁移完成后重跑）：
  - `trades.jsonl`：800 行，SHA256 均为
    `b980389f9793d0049006bfce46cb78d7606e7d7eb06ab58827adf832bb423da8`（与 #747 票面登记基线一致）——cmp=0。
  - `tower_events.jsonl`：`diff -q` 零差异——cmp=0。
- **lib 全绿**：`cargo test --lib` → **2629 passed; 0 failed; 140 ignored; 0 filtered out**（本工位独立建的
  迁移前基线同为 2629/140，迁移后不变；与 #747 票面 2610/138 的差异是会话间新增测试，非本票引入）。
- **`cargo check --lib --bins`**：0 error（18 个外部消费 bin 全部照旧编译通过，含 `cp_capability_smoke`/
  `strict_nest_check`/`p123_fast_replay` 等）。
- **diag 模块可达性冒烟**：`CP_SMOKE_DATA_ROOT=... CP_SMOKE_MAX_BARS=5000 cargo run --release --bin
  cp_capability_smoke` 正常跑完出报表（该 bin 直接调用 `cp_replay_diagnostics::{enable,snapshot,disable}`、
  `cand_delta_tower`、`cand_delta_tower_cached`、`cp_recall_upper_bound_audit`、`cand_delta_entry_tower`
  五个迁移点，零 panic）。

## 5. 结论

纯移动零行为票，五份验收线全过：迁出清单见 §1、mod.rs 6464→5389 行、增量/全量对拍锁随迁不失效、
lib 全绿（2629/0/140，与迁移前基线一致）、wf8 trades+tower_events 双锚 cmp=0。未 commit，工位保留在
`/private/tmp/wt-748` 待用户核验/合入指示。
