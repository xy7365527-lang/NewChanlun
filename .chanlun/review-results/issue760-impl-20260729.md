# #760（C7-E1）kimi 线孤儿簇整删——实施报告

- 日期：2026-07-29
- 票据：[#760](https://github.com/xy7365527-lang/NewChanlun/issues/760)（parent #743）
- 名分依据：`.chanlun/review-results/legacy-generation-census-20260729.md` §4 + 追加（stage_profile.rs）
- 工位：`/private/tmp/wt-760`（`ticket-760`，从 main HEAD `2a6587560c` 切出），独立 `CARGO_TARGET_DIR=/tmp/wt-760-target`
- 性质：亲手实施，未派子代理

## 零引用证据（逐件核）

| 文件/目录 | 零引用证据 |
|---|---|
| `classifier/cand_delta.rs` | 无 `mod cand_delta;` 声明；`cand_delta_tower_cached` 等三函数真实调用点(bin×8+opsem_dump.rs)全部解析到 `mod.rs:766/800/836` 同名内联定义，本文件同名函数被完全遮蔽 |
| `classifier/tower_cache.rs` | 无 `mod tower_cache;` 声明；仅被同簇孤儿 `sublevel.rs`/`incremental/mod.rs` 互引 |
| `classifier/sublevel.rs` | 无 `mod sublevel;` 声明；仅被同簇孤儿 `incremental/mod.rs` 互引 |
| `classifier/incremental/`（5 文件） | 无 `mod incremental;` 声明；`use super::pipeline::...` 指向 #745 已删文件；与现役 `backtest::incremental`（`backtest/mod.rs:51 pub mod incremental;`，7个bin+runner/econ_positive生产调用）确认为两个不同实体，未混淆 |
| `classifier/tests/`（5 文件） | 目录从未被 `mod` 声明纳入编译树，被 `mod.rs:3049` 内联 `mod tests { ... }` 完全遮蔽；内部引用全部指向本簇孤儿 |
| `classifier/incremental_profile.rs` | 无 `mod incremental_profile;` 声明；被 `mod.rs:6118` 内联同名块完全遮蔽 |
| `classifier/stage_profile.rs`（追加自 #746 发现） | 无 `mod stage_profile;` 声明；`stage_profile::time/dump/reset/stage/record_span` 真实调用点(bin+backtest 4文件+集成测试)全部解析到 `mod.rs:1536 pub mod stage_profile { ... }` 内联定义 |

全簇 `#[path]` 属性扫描 + `Cargo.toml` bin/test 声明扫描均零命中，无隐藏编译入口。

## 删除

7 个单元（3 文件 + 2 目录 + 1 重复文件 + 追加 stage_profile.rs）已 `rm`，`git status` 确认全部为 `D`（无新增/未预期变更）。

## 验收

- `cargo build --release --all-targets`（含全部 bin）：Finished，无新增编译错误。
- `cargo test --release --lib theta_v0`：**1983 passed; 0 failed; 103 ignored**。
- wf8 单窗金标准对拍（`M8_WIN_FILTER=wf8 VOICE_EXEC=1 OPSEM_DUMP_DIR=/tmp/i760-check cargo test --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored --nocapture`）：`execR=-477478 MaxDD=0.0841`（与 #745 基线一致）；`trades.jsonl` 800 行，SHA `b980389f9793d0049006bfce46cb78d7606e7d7eb06ab58827adf832bb423da8` **逐字节 cmp=0**（与票面基线一致）。

未 commit（工位保留供后续裁决）。

## 补注（影子评审条目 1，2026-07-29）

被删 `classifier/tests/` 中唯一带 `#[test]` 的 `q7_ruling_c_first_class_structural_direction_third_class_provenance_kept`（level_signals.rs:208）断言 `buy3=0`，系 #576 搬走的 Q7-#1 裁定C **旧口径**；现役口径由 `mod.rs:3783` 覆盖（#486 后，`buy3=1`）——方向相反但属 #486 已裁反转的历史遗留，**非覆盖回退**。「零行为」≠「零信息损失」：该测试语义早已被 #486 裁定废弃，删除只是物理清偿。
