# #763（C7-E4）trading/ 28 个 v2 文件处置——实装记录

- 日期：2026-07-30
- 票据：[#763](https://github.com/xy7365527-lang/NewChanlun/issues/763)（parent #743，blocked_by #762，blocking #764）
- 名分表：`.chanlun/review-results/legacy-generation-census-20260729.md` §1.4/§2/§3（原判：28 文件
  deprecated 待退役，拟集中 `legacy/`）
- 前票教训：`issue762-impl-20260730.md` §5 明确警示"#763 若涉及 trading/ 28 个 v2 文件的 python
  调用核查，须同样扩面覆盖 `analysis/`，禁用 Rust 类型名 grep，一律从 `lib.rs` pymodule 导出名反查"
- 工位：`/private/tmp/wt-763`（分支 `ticket-763`，从 main HEAD `3245d8f996` 切），独立 target
  `/tmp/wt-763-target`
- 性质：**零行为变更**（仅头部 GUARD-ROLE 标记注释，无逻辑/判据改动，无文件删除/移动）
- **未 commit、未 push、未 merge、未碰主仓。**

## 1. 扩面核查（先验后动）

`lib.rs` pymodule 导出名反查（`#[pyclass]`/`wrap_pyfunction!` 清单，:2654-2658）——与 `trading/`
相关的四个导出面：`PyOrganicTape`（"OrganicTape"）/`run_organic_rust`/`run_recursive_rust`/
`run_positional_rust`/`PyUnnStream`（"UnnStream"）。全仓（含 `analysis/` 166 脚本，非仅
`trading_system/`）按导出名 grep：

| 导出名 | 名分表原判 | 本票扩面结果 |
|---|---|---|
| `run_organic_rust` | 未列 | **50+ 处** `analysis/*.py` 真实调用（`nr.run_organic_rust(...)`），`git log -1` 集中 2026-06-11~2026-07-26 |
| `run_positional_rust`（`mode=` 分派 `unn`/`urs`/`nrf`/`iso`/`pcf`/`nif*`/`rnf`/`fusion_*`） | 未列 | **50+ 处**，覆盖 `positional.rs` match 表几乎全部 `PolarityMode` 变体 |
| `run_recursive_rust` | 未列 | 1 处（`analysis/recursive_position_backtest.py:93`） |
| `UnnStream` | 2 处冷调用（`trading_system/` 三份脚本内） | 复核一致 + `analysis/verify_unn_stream.py` 追加 1 处 |

**原判"生产 0 + PyO3 导出 + 2 处冷 python 调用"不成立**——原核查仅看 `trading_system/`
三份脚本，遗漏 `analysis/` 60+ 处真实调用面，与 #762 对 fugue_v3"零调用"判断的漏查同构。

逐文件核查（28 件，经 mode 分派链 + 族内 `use super::X` 依赖网逐一映射）：

- **28 件全部有非测试调用者**，分三档：
  1. **python 直达**（16 件）：`runner`/`config`/`positional`/`unified_necessity`/`recursive_position`/
     `axiom_voice`/`dual_voice`/`isolated_fugue`/`nested_fugue`/`nested_interval_fugue`/
     `positioning_chain_fugue`/`recursive_nested_fugue`/`unified_recursive`/`unified_voice`/
     `unified_osc`/`positional_fusion`——经 `run_organic_rust`/`run_positional_rust`（mode 分派）/
     `run_recursive_rust` 直接或经 `use super::X` 一层可达。
  2. **现役族编译期硬依赖**（5 件）：`types`/`tape`/`positional`/`center_book`（活物，不计入 28）/
     `depth_ref`——`spiral::signal`/`fugue_v3::{accounting,layer,ffi,...}`/`recursive_t::{stream,
     t_engine,rec_stream,...}`（#762 已改判**现役**）生产级 `use crate::trading::{types,tape,
     positional,depth_ref}`（`spiral/signal.rs:27-30`、`fugue_v3/accounting.rs:21-22`、
     `recursive_t/stream.rs:58` 等），三条现役族继续钉住这些文件，与 #762 判定同构联动。
  3. **族内传递依赖**（7 件）：`allocator`/`fatigue_gate`/`ledger`/`level_operating_unit`/`master`/
     `nested_fugue`/`trend_exhaustion`——被 `runner.rs`（档1 python 直达）`use super::X` 直接消费。
  4. **测试基础设施**（`#[cfg(test)] mod`，3 件）：`consolidation_ablation`/
     `sublevel_confirmation_ablation`/`trade_behavior`——非 python/生产可达，但均含真实 `#[test]`
     断言（消融守卫套件，`--ignored` 长测试，产出对照 JSON 硬校验），不适用"零活引用⟹删除"
     判据（该判据前提是生产/python 可达性缺失，非测试基础设施存废判断）。

**28 件无一满足"零活引用者"删除门槛**——票面"删除或集中 legacy/"二选一均不成立，改判 GUARD-ROLE。

## 2. 两个活物零触碰

`center_book.rs`（`consume_death_certificate` 方法体）与 `third_point_book.rs` 整文件：
`git -C /private/tmp/wt-763 diff --stat -- rust/src/trading/center_book.rs
rust/src/trading/third_point_book.rs` 输出为空——**零 diff，零触碰**。

## 3. 处置：GUARD-ROLE 留档，零删除，零移入 legacy/

- `trading/mod.rs`：头部依赖图后追加全量理由块（GUARD-ROLE: organic-fugue-v2-trading-layer，
  含四证据链：analysis/ 扩面调用面、mode 分派映射、#762 三族现役硬依赖、测试基础设施豁免）。
- 其余 28 文件：各自顶部追加一行指回标记（`GUARD-ROLE: organic-fugue-v2-trading-layer——名分：
  现役（详见 trading/mod.rs 头部 GUARD-ROLE 块，#763 C7-E4 核定；零删除/零移入 legacy/）。`）。
- `center_book.rs`/`third_point_book.rs`：不触碰（§2）。
- `Cargo.toml`：`grep -n "trading" rust/Cargo.toml` 仅命中 `nautilus-trading`（外部 crate 名，
  与本目录无关）——无 `[[bin]]`/独立 feature 条目需清理，与 #762 同结论。

## 4. 验收

- `cargo build --lib`：Finished，exit 0，无新增编译错误。
- `cargo build --bins --lib`（生产面全量）：Finished，exit 0。
- `cargo test --lib trading::`：**361 passed; 0 failed; 4 ignored**。
- wf8 单窗金标准对拍（`M8_WIN_FILTER=wf8 VOICE_EXEC=1 OPSEM_DUMP_DIR=/tmp/i763-check cargo test
  --release --lib theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos -- --ignored
  --nocapture`）：`execR=-477478 MaxDD=0.0841`（与基线一致）；`trades.jsonl` **800 行**，SHA
  `b980389f9793d0049006bfce46cb78d7606e7d7eb06ab58827adf832bb423da8` **逐字节 cmp=0**
  （与 #745/#760/#761/#762 同一基线值）。
- `git status --short`：29 个 `M`（`mod.rs` + 28 文件），`center_book.rs`/`third_point_book.rs`
  diff 为空。

## 5. 遗留

- 与 #762 完全同构的方法论复发：名分表原判的"零/低调用"结论系统性遗漏 `analysis/` 目录，
  #745/#761/#762/#763 四票连续四次在同一缺口上翻案——建议后续任何"python 零调用⟹deprecated"
  判定，核查步骤强制写出"经 `lib.rs` 导出名表逐一核对 `analysis/`+`trading_system/` 全仓，非
  Rust 内部名 grep 仅查 `trading_system/`"，否则不予采信（沿用 #762 §5 已固化判据）。
- 顶层散件（bi_engine/segment/zhongshu/…）复核（名分表 §5 N+5）仍待四族+trading/ 全部清空/
  标记完毕后开票，本票不涉及。

*本报告基于亲手 grep/Read/cargo build/test 核查，未使用 Task/子代理。*
