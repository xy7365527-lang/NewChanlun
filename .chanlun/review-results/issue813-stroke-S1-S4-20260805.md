# #813「笔」侧 S-1～S-4 考据报告（不裁定，只摆事实与论据）

- 票：[#813] 笔与线段的教义正本（wayfinder:grilling，父图 #787）
- 范围：**只做「笔」侧四组 S-1～S-4**。线段侧 G-1～G-5 不在本报告。
- 基线：`88f6f3c09b`（本地 `main`）。分支 `research/i813-stroke`。
- 性质：**考据报告，不做裁定**。裁定由人做。

## 0. 开工归正（#909 陷阱第 14 次）

本 worktree 初始 HEAD = `19b4015927`（祖传线：`rust/src/theta_v0/`、`formal/` 均不存在，
`git log --oneline | wc -l` = 1171）。已 `git fetch --all` 后 `git reset --hard 88f6f3c09b`
（本地 `main` 的 HEAD，`docs(#911)`）。归正后 `rust/src/theta_v0/parser/`、`formal/Origin/`、
`.chanlun/definitions/` 均在。**未使用 `origin/main`。**

---

## 1. 票面前提复核：逐条结论

**票面写于 #809 清点期，本轮实核发现下列偏差。**

| # | 票面断言 | 实核 | 判定 |
|---|---|---|---|
| P1 | `a_stroke.py:176` 是双条件判据 | `:176` 是 **docstring 里指向 `analysis/engine_vs_tv_comparison.md §2.4` 的一行注释**。真判据在 **`:181`**（`return merged_gap >= 2 and raw_gap >= new_raw_gap_min`），量在 `:178`/`:180` 算 | **错（行号）**，订正为 `:181` |
| P2 | 双条件里的「≥3」 | 不是硬编码，是 **形参默认值 `new_raw_gap_min: int = 3`（`:170`）**，调用方可传 4（docstring `:174-176` 明写「调用时可传 4 收紧以过滤噪声笔，对齐 TV 宽笔」） | **不精确**：是可调参数，非常数 |
| P3 | `theta_v0/parser/stroke.rs:60` 是单条件判据 | `:60` 是 **doc comment**。真判据在 **`:62-65` `fn gap_ok`**（`diff as u32 > min_gap`），消费点 `:88` | **错（行号）**，订正为 `:62-65` |
| P4 | 「`source_index` 之差 `>3`」 | 是 `> config.new_stroke_min_gap`，**default 3**（`stroke.rs:8` doc 明写 default 3）。同样是可配参数 | **不精确** |
| P5 | `SegmentFeatureSeq.lean:374` | **正确**。`def IsNewStroke` 在 `:374`，body `:375` = `d.noSharedBar = true ∧ d.barsBetween ≥ 3` | **对** |
| P6 | Lean 侧「不考虑包含」 | 只对**第二个条件**成立。`:371` 逐字写「顶/底分型**经包含处理后**不共用 K 线」——`noSharedBar` 是**包含处理后**的量。`:372` 才是「不考虑包含关系，至少 3 根」 | **半错**：票面把限定词扩到了整个判据 |
| P7 | 「三者」——即分歧只有三个载体 | **漏掉第四个，且它才是生产路径**：`rust/src/stroke.rs:84-97 check_gap` 是 `a_stroke.py:_check_gap` 的 **逐字 Rust 移植**（同样 `merged_gap >= 2 && raw_gap >= new_raw_gap_min`，同样 `merged_to_raw[cand.idx].0 - merged_to_raw[start.idx].1 - 1`），由 `rust/src/bi_engine.rs` 消费，经 `rust/src/lib.rs:123/137` 暴露成 pyo3 的 `BiEngine(stroke_mode="new")`、`:923/961` 的 `RecursiveOrchestrator(stroke_mode="wide")` | **错（漏载体）** |
| P8 | 「三者在有包含合并的区间上给出不同笔集合」 | **未经推导的断言**。逐条件推出的关系见 §3：无包含区间上 Python/Rust-族I 与 theta_v0 **完全等价**；有包含区间上是**互不包含**（双向差集都非空，各给出手算样例）；Lean 侧**不是同一类对象**（谓词而非构造器，`barsBetween` 是抽象 `Nat` 字段，无计算式）⟹「三者给出不同笔集合」这句对 Lean 侧**不可判定真假** | **需大幅订正** |
| P9 | 「零对拍」 | 见 §4：**不是从来没对拍过**——族 I 的 Python↔Rust 有 bit-exact 对拍在案（`tests/test_new_bi_pipeline.py`、`tests/test_rust_move_equivalence.py`）。缺的是 **族 I ↔ theta_v0** 的对拍，以及 **Lean ↔ 任一实装** 的对拍 | **需订正措辞** |
| P10 | S-2「族 I 支持 `mode=strict` 老笔」＋「配置档并存」 | 支持确实在（`a_stroke.py:247` / `bi_engine.rs:162-166`），但**默认档不是 `new`**：族 I 对外入口默认 **`stroke_mode="wide"`**（`ab_bridge_newchan.py:229`、`nested_pipeline.py:49`、`server.py:426`/`:483` 的 query 默认、`lib.rs:961` 的 pyo3 默认）。⟹ **三档并存**（wide / strict / new），不是两档 | **不完整** |

---

## 2. 四组逐一定位（订正后行号 + 逐字判据）

### S-1 「至少 N 根独立 K」算的是什么量

**载体一（族 I / Python）** `src/newchan/a_stroke.py:164-182`：

```
164:def _check_gap(
...
170:    new_raw_gap_min: int = 3,
171:) -> bool:
172:    """§4.3 gap 检查：宽笔/严笔/新笔三种模式。
...
178:    merged_gap = cand.idx - start.idx
179:    if use_new_bi:
180:        raw_gap = merged_to_raw[cand.idx][0] - merged_to_raw[start.idx][1] - 1  # type: ignore[index]
181:        return merged_gap >= 2 and raw_gap >= new_raw_gap_min
182:    return merged_gap >= min_gap
```

溯源标签（文件自带）：docstring `:174` 引「缠师《忽闻台风可休市》新笔定义」；`:175-176` 引
`analysis/engine_vs_tv_comparison.md §2.4`（**2026-06 及更早的 `analysis/`，按纪律只作线索**）。

消费点：`a_stroke.py:265`（主循环 `strokes_from_fractals`）。
`use_new_bi` 的开关在 `:246`：`use_new_bi = mode == "new" and merged_to_raw is not None`
——**mode="new" 但不传 `merged_to_raw` 会静默降级**到 `:182` 的 `merged_gap >= min_gap`（`:247` 令
wide/new 都取 `min_gap = 4`）。已查到三处这样调用：`src/newchan/ratio_engine.py:53`
（`df_merged, _m2r = merge_inclusion(...)` 拿到了映射却**丢弃不传**）、`src/newchan/equivalence.py:215`、
`scripts/validate_ratio_pipeline.py:54`。

**载体二（族 I / Rust 移植，生产路径）** `rust/src/stroke.rs:84-98`：

```
84:pub fn check_gap(
 87:    use_new_bi: bool,
 88:    min_gap: i64,
 90:    new_raw_gap_min: i64,
 92:    let merged_gap: i64 = cand.idx as i64 - start.idx as i64;
 93:    if use_new_bi {
 94:        let raw_gap: i64 = merged_to_raw[cand.idx].0 as i64 - merged_to_raw[start.idx].1 as i64 - 1;
 95:        return merged_gap >= 2 && raw_gap >= new_raw_gap_min;
 96:    }
 97:    merged_gap >= min_gap
```

与载体一**逐字同构**（含 `-1` 的位置）。消费点 `rust/src/bi_engine.rs:92-101`
（`advance_checkpoint_step`），档位在 `bi_engine.rs:161-166`：

```
161:        let use_new_bi = stroke_mode == "new";
162:        let min_gap = if stroke_mode == "wide" || stroke_mode == "new" {
163:            4
164:        } else {
165:            min_strict_sep
166:        };
```

**载体三（theta_v0）** `rust/src/theta_v0/parser/stroke.rs:58-65`：

```
58:/// 两分型间是否满足新笔间隔约束（reference-theta-v0.md:21）。
59:///
60:/// 「两极值 K 间按原始 K 计数排除两端至少 N 根」——两端点 source_index 之差需 > N
61:/// （即中间至少 N 根独立原始 K，两端不共用 K 自动满足 diff>=1）。
62:fn gap_ok(a: &Fractal, b: &Fractal, min_gap: u32) -> bool {
63:    let diff = b.source_index.saturating_sub(a.source_index);
64:    diff as u32 > min_gap
65:}
```

消费点 `:88`（`build_strokes` 主循环）。溯源：`:1` `[缠论可导,77/81课]`、`:3` `reference-theta-v0.md:21 逐字`。
`:15` 明写「间隔用**原始 K 序号**（`source_index`），不是合并后序号」。

**★ `:61` 那句「两端不共用 K 自动满足 diff>=1」是错的**，见 §3.4。

**载体四（Lean）** `formal/Origin/SegmentFeatureSeq.lean:362-375`：

```
362:structure NewStrokeData where
363:  topIdx : Index
364:  bottomIdx : Index
365:  barsBetween : Nat
366:  noSharedBar : Bool
...
371:  1. 顶/底分型经包含处理后不共用 K 线（与旧笔一致，绝不放松，081:110）。
372:  2. 顶分型最高 K 线与底分型最低 K 线之间（不含），不考虑包含关系，至少 3 根（081:111）。
374:def IsNewStroke (d : NewStrokeData) : Prop :=
375:  d.noSharedBar = true ∧ d.barsBetween ≥ 3
```

**关键性质**：`barsBetween` / `noSharedBar` 是**结构字段**，Lean 侧**没有从 K 线数据算出它们的函数**
（全文件 grep `barsBetween` 只有 `:365`（声明）、`:375`（判据）、`:382/:387/:392`（三个 literal
见证定理）五处）。⟹ Lean 是**判据 schema**，不是构造器。文件自己在 `:399-407` 的
`★still-MISSING-A` 里承认只形式化了「判据层」、`segmentsOf` 全自动构造**未证**。

### S-2 旧笔存废

- theta_v0 侧：`rust/src/theta_v0/parser/stroke.rs:5` 逐字
  「**启用新笔，旧笔禁用**（《忽闻台风可休市》/第81课新笔定义）」——文件级 doc，**无 strict 档代码路径**。
- 族 I 侧：三档并存。Python `a_stroke.py:232` `mode: str = "new"`、`:247`
  `min_gap = 4 if mode in ("wide", "new") else min_strict_sep`、`:233` `min_strict_sep: int = 5`；
  Rust `bi_engine.rs:161-166` 同构。
- **默认档实核**：族 I 的**对外**默认是 `wide`，不是 `new`：
  `src/newchan/ab_bridge_newchan.py:229 stroke_mode: str = "wide"`、
  `src/newchan/nested_pipeline.py:49 stroke_mode: str = "wide"`、
  `src/newchan/server.py:426`/`:483` `request.query.get("stroke_mode", "wide")`、
  `rust/src/lib.rs:961 stroke_mode = "wide"`（`RecursiveOrchestrator` pyo3 默认）。
  只有 `rust/src/lib.rs:137 stroke_mode = "new"`（`BiEngine` pyo3 默认）是 new。
- ⟹ **票面「配置档并存（两档）」应订正为三档，且「老笔（strict）」与「新笔（new）」之外
  还有一个实际当默认用的 `wide`（merged_gap ≥ 4），它既不是原文的旧笔也不是原文的新笔。**

### S-3 gap 不足时怎么回退

- **`j += 1` 侧（族 I，两个载体）**：`src/newchan/a_stroke.py:265-267`
  ```
  265:        if not _check_gap(start, cand, use_new_bi, min_gap, merged_to_raw, new_raw_gap_min):
  266:            j += 1
  267:            continue
  ```
  `i` 不动 ⟹ 起点 `fxs[i]` 保留，只丢候选。Rust 移植同构：`rust/src/bi_engine.rs:92-101`，
  `:100 *cp_j = j + 1;`（`cp_i` 不动）。
- **`i += 2` 侧（theta_v0）**：`rust/src/theta_v0/parser/stroke.rs:101-104`
  ```
  101:        } else {
  102:            // 间隔不足：越过 b 与其后同类候选，回到与 a 异类的下一候选（保持交替）。
  103:            i += 2;
  104:        }
  ```
  **★ `:102` 的注释与代码不符**：`i += 2` 后新起点是 `alt[i+2]`，在严格交替序列里它与旧 `a`
  **同类**，且旧 `a` 被**整个丢弃**。注释说「回到与 a 异类的下一候选」是错的（`:75` 的
  「越过该候选对」才是实际语义）。这条注释错误本身是 S-3 的一个证据：两侧行为差异比票面写的更大
  ——不只是「放弃起点」，是**同时放弃起点和候选两个分型**。

### S-4 已确认的笔能不能回写改写

- **回写侧（族 I，两个载体）**：`src/newchan/a_stroke.py:147-161`
  ```
  147:def _extend_prev_stroke(
  ...
  151:    prev = strokes[-1]
  152:    new_i1 = cand.idx
  155:    strokes[-1] = Stroke(
  156:        i0=prev.i0, i1=new_i1,
  ...
  161:    )
  ```
  调用点 `a_stroke.py:255-261`：`locked = len(strokes) > 0`，同类更极端候选 ⟹ `_extend_prev_stroke`。
  Rust 移植 `rust/src/stroke.rs:159 pub fn extend_prev_stroke(strokes: &mut [Stroke], ...)`，
  调用点 `rust/src/bi_engine.rs:84`（checkpoint 路径）与 `:512`（易变尾部路径）。
- **禁回写侧（theta_v0）**：`rust/src/theta_v0/parser/stroke.rs:112-120` 的「前缀不变性」段：
  `:115` 「一旦某笔成笔（i+=1），其端点 fractal 固定」；`:119-120`
  「保留 confirmed strokes 前缀（端点 fractal 已固定）」。**这不是一句口号，是增量实现的正确性前提。**
- **★ 关键限定（票面没写）**：族 I 的回写**只动 `strokes[-1]`**，而 `strokes[-1]` 正是
  `a_stroke.py:210-222 _mark_last_unconfirmed` 对外标成 `confirmed=False` 的那一笔
  （`:274 _build_stroke` 内部一律建 `confirmed=True`，`:221` 出口再把末笔翻成 `False`）。
  Rust 侧同理：`bi_engine.rs:148-151` 明写「不可变冻结前缀 `[0, synced_prefix)`
  逐位等于 `cp_strokes[..synced_prefix]`」、「`cp_strokes` 前缀 `[..len-1]` 永久固定
  （仅 last 被 extend/push）」。
  ⟹ **两侧「已确认」的定义可能其实一致，分歧在措辞而非行为**。这一条是 S-4 最可能被判**伪分歧**
  的方向，但要坐实还缺一件：族 I 在**首笔**情形（`locked` 从 False 翻 True 的那一步）以及
  `extend` 使 `strokes[-1].i1` 越过后续已被消费的下游对象时，是否真无外泄。**本轮未测，写「未测」。**

---

（S-1 接受集推导、对拍状态、可达性、原文考据、《市场天道》、形态清单、反例自评、
`.chanlun/definitions/{bi,xianduan}.md` 核对 — 第二次交付续写）
