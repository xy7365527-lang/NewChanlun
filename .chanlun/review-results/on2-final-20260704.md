# on2 O(n²) 修复 波次1+波次2 终验收（final）

日期：2026-07-04
基线 HEAD（波次1修复前）：`ebcd8f2a0f`
波次1 验收 HEAD：`150c67e76b`
波次2 验收 HEAD：`e3ae45a863`（cascade 增量失效 SHIP）
编译：`cargo --release`（opt-level=3 / lto=false / debug-assertions=false → `debug_assert!` 已剥离）
认识论：正确性/加速 = L1（管线等价 + 管线度量，231号）；e 局部化前提 = L2（真实 CL 分布）

---

## ① 全量测试（硬门）

命令：`cargo test --release --lib -p newchan_rust`

```
test result: ok. 1488 passed; 0 failed; 113 ignored; 0 measured; 0 filtered out
```

**全绿。0 红。硬门通过。** 波次1=1484 → 波次2=1488（+4：forest_epoch 3 覆盖测试 + cascade 合成回归网）。113 ignored 均为需真实数据/长跑的 `#[ignore]`。

## ② bit-exact 复跑（铁律，本波次核心放行条件）

| 测试 | 域 | 结果 |
|------|-----|------|
| `bit_exact_synthetic` | 增量塔 vs 全量重算（合成） | **2 passed / 0 failed** |
| `incremental_macd_bit_exact_synthetic` | MACD 增量 | passed（同批） |
| `bit_exact_per_bar`（CL，`--ignored`） | 每 bar 增量输出 == legacy `classify_with_tower(parse(..=i))`，bit-identical | **2 passed / 0 failed（1.36s）** |
| `bit_exact_per_bar_cached_vs_nocache`（CL，interp T_i→K_i oracle） | forest_epoch 双 key 无陈旧 | passed（同批） |
| `cascade_incremental_eq_full_clear_synthetic`（always-run） | 增量失效 P>0 == 全清 P=0 | 随全绿守卫 |

cascade 增量失效实装期发现的 2 处发散（bar-10299 `cached_second_count` 越界、bar-27947 B2 共享边界误丢）均为**实现 bug 已修，非设计洞**——修复后 CL bit-exact 全绿坐实设计 sound。

## ③ 400K / 1M 分段计时（THETA_PROFILE_STAGES=1，BTC 逐 bar classify_with_tower_incremental）

命令：`THETA_PROFILE_STAGES=1 A0_PROFILE_BARS={400000,1000000} cargo test --release -p newchan_rust --lib profile_clone_cluster_a0 -- --ignored --nocapture`

### 墙钟演进表

| 窗口 | 波次前基线 | 波次1（`150c…`） | 波次2（`e3ae…`，本验收） | 端到端加速 |
|------|-----------|-----------------|------------------------|-----------|
| 400K | **6.00s** | 3.84s | **3.71s** | **1.62x（-38%）** |
| 1M   | ~21.4s | 21.39s | **19.43s** | **~1.10x（波次2 增量）** |

**400K→1M 标度指数演进**：波次1 exp = ln(21.39/3.84)/ln(2.5) ≈ **1.874** → 波次2 exp = ln(19.43/3.71)/ln(2.5) ≈ **1.807**。残余超线性下降 0.067（cascade 增量失效削掉 07b 前缀重扫的超线性分量；仍 >1 因 07a_extract_signals_l0 残余 O(n²) 未攻，见 ⑤）。

### cascade 增量失效攻击的 stage 前后（1M，A/B 同二进制对照 `THETA_CASCADE_FULLCLEAR`）

| stage | P=0（旧 #65 全清） | P>0（默认增量，本验收） | 降幅 |
|-------|-------------------|----------------------|------|
| 07b_extract_second（#65 坐实 98.6% cascade 前缀重扫，真残余 O(n²)） | 1640ms | **111ms**（fresh 145ms） | **93%↓**；标度指数 2.11→1.72 |
| 05_compose_resume | 2162ms | **1792ms**（fresh 1905ms） | 常数因子 |
| 09_project | 110ms | **97ms** | 常数因子 |

400K 同攻击：07b 237ms→23ms、标度 2.11→1.72（`637-ab-differential`）。

### 波次1 落地的 stage（400K，O(n²)→O(1)/O(tail)）

| stage | 修复前 | 修复后 | 加速 | commit |
|-------|-------|-------|------|--------|
| 09_project_to_units_resume | 1238ms | 34ms | 36x | `b5744f219b` |
| 00b_l0_units_clone | 381ms | 5.7ms | 64x | `a5986d64e2` |
| 10_projected_units_clone | 167ms | 25ms | 6.7x | H4 同文件 |

### forest_epoch（波次2，interp 域 K_i 森林命中 O(全塔)→O(1)）

| 指标 | 值 |
|------|-----|
| of_forest 段隔离计时（CL 16K，`diag_gen_fastpath` extract_s） | 0.33s → **0.054s = 6.1×** |
| forest 段（8K，含 candidate 共同基底） | 0.052s → **0.020s = 2.60×** |
| epoch 命中率（生产判据，16K） | **98.51%**（订正前长度判据=0%） |
| false_hits（epoch 相等但森林真变） | **0**（soundness ✓） |
| bump 率（8K） | 2.17% vs of_forest 真变 0.78%（2.8× over-invalidate，E4 空-L0 clear，sound-safe） |

**关键订正**：设计 §3 长度判据在 CL 实测 bump 率=100%（O(n²) 未消）——frontier resume 每 bar truncate/repush 复现字节相同内容。改为**逐值内容判据**后 bump 降至 2.17%、命中率 98.51%。这是设计 §7 明列失败分支的正面落地，非绕过。

## ④ 全部 FIXED / NO-SHIP 清单

### FIXED（bit-exact-sound 落地）

| 项 | 波次 | commit |
|----|------|--------|
| 09_project_to_units_resume O(n²)→O(tail) | 1 | `b5744f219b` |
| 00b_l0_units_clone O(n²)→O(1) Rc | 1 | `a5986d64e2` |
| 10_projected_units_clone | 1 | H4 同文件 |
| A1 Rc化 + C3 partition_point 二分（第二波两泳道） | 1 | `4c23604f4c` |
| forest_epoch（K_i 森林命中 O(全塔)→O(1)） | 2 | `f15253ce3f` |
| cascade_reset 增量失效（07b 前缀重扫 93%↓） | 2 | `e3ae45a863` |

### NO-SHIP（残余 O(n²)，各文件域内无 bit-exact-sound 降阶杠杆）

| stage / 域 | 1M 计时 | 根因 | 裁决 |
|-----------|--------|------|------|
| 07a_extract_signals_l0（signal.rs） | 4481ms | cascade 前缀重扫（共享底座，07a-resume frontier 不可降阶） | NO-SHIP（codex Q1=B/Q3） |
| 07a_extract_first_third_ln（signal.rs） | 1434ms | cascade 90% miss / 79% 成本 | NO-SHIP |
| 05_compose_resume（recursive_tower H9） | 1905ms | Fix A/B 已落地；残余 05c2a/b 有界 O(n·k) 收益递减 | NO-SHIP |
| of_forest / tree_segment（interp.rs H6） | — | 唯一 sound 路径=classifier 域新增 forest_epoch → **波次2 已 FIXED**（越域路径落地） | 波次1 NO-SHIP → 波次2 FIXED |
| collect_signals（econ_positive.rs） | — | O(n²) 全继承自 classify_at 底座，econ 自身 C1/C2/C3 已优化 | NO-SHIP |
| candidate 全量拼接 + merge 全量快照（strategy 段 exp=1.85） | — | codex Q4 诚实声明的独立残余，不在 forest_epoch/cascade 有效域 | NO-SHIP（诚实声明） |

## ⑤ 剩余诚实声明（波次2 后仍存的超线性）

- **全引擎 strategy 段 exp 仍 1.85**：candidate 每 bar 全量拼接 + merge 全量快照——codex Q4 明列的独立残余，不在本波次 forest_epoch / cascade_reset 有效域。
- **A0 逐 bar 热路径 exp=1.807**：主残余项 = 07a_extract_signals_l0（1M=4481ms，cascade 前缀重扫共享底座）。frontier-resume 不可 bit-exact 降阶（H8/H9/`3aee6dd4c7` 负结果先例），各 signal.rs 文件域内无 asymptotic 杠杆。
- **cascade 前提 L2 坐实**：放行条件3 falsification gate（300K CL）cascade_events=3879、e0(P==0)=10.05%、mean keep_frac=0.856 ⟹ e 未坍缩，"frontier 改写局部化"前提**成立（非证伪）**。若 e0→1 / keep→0 则前提翻转。

## 结论

波次1+波次2 消除 6 个 O(n²) 热点（09/00b/10 + A1/C3 + forest_epoch + cascade 增量失效）。400K 墙钟 **6.00s→3.71s（1.62x / -38%）**，1M **~21.4s→19.43s**，标度指数 **1.874→1.807**。全量 1488 绿 / 0 红，bit-exact 铁律全绿（合成 + CL per-bar bit-identical）。残余超线性（07a 前缀重扫 + strategy candidate/merge）经 codex/H8/H9 先例裁定 NO-SHIP——各文件域内无 bit-exact-sound 降阶路径，全为诚实声明的独立残余。
