# p2 实装：D1 桥接 + shadow-fold 等价 test + Q2 删除 mutex_interp

- **工位**：ws-pi1（goal g-full-strategy-pi，p2 实装）
- **日期**：2026-07-02
- **裁定依据**：`.chanlun/review-results/codex-q2-d1-ruling-20260702.md`（codex 全权终裁）
- **状态**：代码完成，**production build 全绿**；**test 运行阻塞**于 ws-parserfix 未提交的 `parser/segment.rs:964` 非法字面量（外部 WIP，不在本工位边界）——见 §影响声明

## 结论（改动清单）

**D1（`strategy/mutex.rs`）**：
1. `predicates_of(c: &Candidate, working: &[(ActiveLeg,bool)], opened_slots: &[(u32,VoiceSide)]) -> Predicates`——按裁定 §1a 修正签名补 `opened_slots`（interp.rs 规则3/4 依赖本 fold 已开 slot）。从 **PDF 谓词语义独立重导** P2..P7（非复制 interp 规则序，故对拍非循环）：反向平仓→P2/P3，slot 空→P4(ShortDiff)/P5/P6，slot 占→P7。**P1 恒 false**（裁定 §3 出范围）、**P8 不主动置位**。
2. `ActionBucket{Close,Open,Record}` + `bridge_bucket(MutexClass)->ActionBucket`——裁定 §1b 桥接表：C1/C2/C3→Close，C4/C5/C6→Open，C7/C8/C0→Record。
3. `tests::shadow_fold_bucket_equivalence`——按 `theta_key` 排序逐候选，用同一 `working`/`opened` 快照算 `predicates_of→mutex_class→bridge_bucket` 桶，对拍真 `interpret()` 聚合输出按 `gamma_index` 反查的 interp 桶。**桶级一致断言（裁定 §1c 第一层，必过）**；精确 Cj 一致为可选诊断（未实装为断言——`mutex_class` 只是三桶级 oracle）。9 场景覆盖生成域：空 active / 跨级多空开 / fold 内重复 slot / 反向平仓 / 同向 slot 记录 / 无类无向记录 / ShortDiff 开仓 / 双向腿共存反向平 / 三桶同刻混合。
4. P7 字段 `same_dir_record_add` → **`same_slot_record`**（诚实命名，裁定 §4——生产无「加仓」子动作，只覆盖「记录」半句，字段注释明示）。
5. 文件头补身份声明：`mutex_class` = D1 等价测试 oracle，**非生产路径**（零生产消费者）。

**Q2**：
6. 删除 `closed_loop/mutex_interp.rs`（含其 test，13.6KB）+ `closed_loop/mod.rs:60 pub mod mutex_interp;`。确认零外部消费者（grep `choose_action`/`mutex_interp::` 无命中）。

**支撑改动**：
7. `interp.rs`：`fn theta_key` → `pub(crate) fn theta_key`（shadow-fold test 需同序排序；1-词可见性放宽，无行为改动）。

## 定义依据

- 桥接表/签名/生成域/桶级 vs 精确 Cj 分层，全部逐条对齐 codex-q2-d1-ruling §1a/§1b/§1c。
- `predicates_of` 的 P2/P3=`reverse_signal`(§9 closePred 复用)、P4=`Vertical::ShortDiff`、P7=slot 占用——对齐 PDF《缠论的全互斥定义策略》Part B 四谓词语义（本仓库第一权威）。

## 边界条件（结论翻转条件）

- **若 shadow-fold test 跑出「桶级」不一致**（非仅精确 Cj）：不 workaround，STOP 报 team-lead 并 `/escalate`——权威序 PDF > interp > mutex_class（裁定 §1d），分叉本身要落谱系。当前代码完成但**未运行**，此翻转条件待 test 可跑后验证。
- P1 风险强平的候选级投影未纳入（裁定 §3 出范围，`KThetaRiskGate.force_flat` 上层独立兜）——后续独立裁定项。

## 下游推论

- D1 桶级等价一旦 test 通过：p1 报告 P0-1（8 谓词双实装未统一）从「双实装未证等价」升级为「已证桶级等价」，interp 多候选并行接线合规坐实。
- Q2 删除后 `mutex.rs` 是唯一谓词解释器 oracle，无双权威镜像腐化风险（no-patch-mentality 090）。

## 谱系引用

- codex-q2-d1-ruling（直接裁定依据）；full-strategy-pi-conformance §Q1（多候选并行合规，D1 收窄依据）；no-patch-mentality 090（Q2 删除）；formalization-validity-domain 231（语义正确性≠经验有效性，mutex_class 作语义 oracle 不豁免 wverify 经验检验）。

## 影响声明

- 改动文件：`strategy/mutex.rs`（+predicates_of/bridge_bucket/ActionBucket/test/rename/头注释）、`strategy/interp.rs`（theta_key 可见性）、`closed_loop/mod.rs`（删 mod 行）、删除 `closed_loop/mutex_interp.rs`。**未碰 parser/**、未碰 P1/KThetaRiskGate（裁定出范围）。
- **验证状态**：`cargo build --lib`（production，无 test）**全绿**（3.75s Finished）——所有生产代码编译通过。`cargo test --lib` **无法运行**：唯一编译错误在 `parser/segment.rs:964`（ws-parserfix 未提交 WIP 的非法 hex 字面量 `0xADVA_9C13_2718_2818`，在 `#[test] fn advancing_euf_resolves_and_reflags` 内），破坏共享 test-compile 单元 ⟹ 全队 `cargo test` 阻塞，非本工位改动。**待 segment.rs 可编译后运行 `cargo test --release --lib` 验证本 test + 基线 1404**。
