# 极性-级别协变：群论必然性 prove 形式化（unn 第 9 条候选）

> 工位：群论必然性 prove 形式化（必然性侧第一性原理依据，与 prop4-bidir 经验侧正交）
> 日期：2026-06-23 ｜ 分支：prop4-nest-readingB-20260623 ｜ 任务 #23
> 范围：**设计 prove + 形式化证明，不实装**（与编排者指示一致）
> 认识论等级：群论推导 = **L0**（定义/代数）；与 539/553/560 的链接重诊断 = **L0**（源码+谱系）；
> 收益侧充分性 = **L3 未决**（诚实标注，见 §四）

---

## ★★ 主判决（编排者二次重定后：完全分类覆盖才是解法，N9 降为推论）

> **重定历史**：编排者两次纠正本工位。① 极性翻转由**走势完成买卖点**触发（type1/2/3），不是经验
> 趋势方向（"你怎么知道趋势何时终结？只有缠论买卖点能告诉你"）。② **解法是完全分类覆盖（积极
> 完备性），N9 只是其推论**（"走势的完全分类才是解法"）。本判决以 ② 为主体，N9（§1-§八）保留为推论。

**主判决**：**「操作映射 Ω 必须覆盖买卖点完全分类的所有轨道（升跌完备性穷尽）」是
缠论两完备性定理（第17课走势分解 + 第21课买卖点完备性）+ D∞ 轨道枚举 Burnside 完备
（506→46→2→1 穷尽）的必然推论——状态空间被完全划分，Ω 必须是该划分上的全函数（每轨道一确定
操作，无遗漏轨道落入默认分支）。** 强/弱诚实区分：
- **状态空间完全划分 = L0 必然（强）**：Burnside 穷尽（轨道-稳定子划分定理）+ 升跌完备性定理
  （第21课原文：市场走势完全由线段构成，端点是 type1/2/3 买卖点，无第四类）。
- **Ω 全函数性（覆盖每轨道）= 良定义系统的强必然**（无未定义行为 + 完全分类 ⟹ Ω 必全）；但
  **每轨道的具体操作内容 = 缠论语义**（第21课延续/扩张/新生 + 第19课力度），非纯群论。
- **N9（极性翻转只在 φ=0 缝）= 完全分类覆盖的一个推论**（type1 走势完成这一**单轨道**的操作时机
  约束），codex 异质质询已 confirmed（§八），保留为推论。

**539 重诊断（编排者强主张，本工位概念+代码双重坐实）**：**539 = Ω 不覆盖完全分类（漏轨道/粗化
轨道）的架构产物**。代码现场（§九.4）：信号层产出完全分类（`BSPKind` type1/2/3×买卖 + 中枢三态，
divergence.rs），但操作接口 `LevelView`/`route_bsp` **坍缩为二元 `{buy,sell}`**（types.rs:277
"纯 BSP 驱动，不分 type1/2/3"）⟹ 不同轨道（二卖成/败、中枢延续/扩张/新生）落入同一粗分支或默认
（死扣）。两失效形式：① **过覆盖**（在非买卖点状态操作：背驰段≠买卖点、经验趋势≠买卖点 → 强牛
误翻空，= N9 违反是其特例）；② **欠覆盖**（真买卖点轨道如「二卖失败中枢新生」无操作 → 死扣）。
**L3 诚实红线**：「完全 Ω ⟹ 539 消失」是编排者 L0 架构论证；其**净收益验证 = L3（task #22 判决场）**，
不声称 L0 消除（声明膨胀禁止）。

---

## ★ 一句话判决（N9 推论，诚实，强/弱区分）

**「极性协变级别走势方向」是 D∞ 覆盖作用结构的 L0 必然推论（强），但其必然性正确定位在
作用 groupoid 的合法性谓词 P2（τ 仅在 φ=0 合法），不在编排者主张的裸呈现关系 srs=r⁻¹。**
裸关系 srs=r⁻¹ 只给出 T₅₇（做空=做多镜像），是**必要非充分**；协变不变量由 P2 钉死。
这是对编排者直觉的**精化（refinement），非否证**——协变确实是群论必然，不是合理新约束；
引擎甚至已编码该映射（`dir_to_polarity`），缺的只是把它作为**核心仓翻转当前违反的不变量**强制。
**【二次重定后定位】**：本节 N9 = §九 完全分类覆盖的**一个推论**（type1-flip 单轨道的操作规则 +
"无 fake-trigger flip" 子规则），不是解法主体。完整解法见 §九。

---

## 一、D∞ 半直积 → 极性协变级别：群论推导

### 1.1 能指消歧（强制——orbit doc 已警告 σ 能指碰撞）

编排者命题 `D∞ = ⟨r,s | s²=1, srs=r⁻¹⟩`，r="该级别走势方向"，s="极性（多/空）"。
对接 `docs/orbit_enumeration_completeness.md` 的规范命名（D∞=⟨h,τ | τ²=e, τhτ⁻¹=h⁻¹⟩）：

| 编排者符号 | orbit doc 符号 | 缠论语义 | 群论身份 |
|---|---|---|---|
| **s（极性）** | τ | 多/空 = 手性 ε∈{±1} | 阶-2 对合（反射）；s²=1 = T₂₄ = M=N 翻转定理 |
| **r（走势方向）** | h（角向走势进度） | 该级别走势的展开方向（Up=前向/Down=反向） | 无限阶平移生成元（正规子群 ⟨r⟩≅ℤ 的生成元） |
| （级别本身） | σ=h²³（径向） | 级别阶梯 r∈{0..10} | σ 升一级别——**与编排者 r ≠ 同物，勿混** |

> **能指碰撞处置**：编排者用 "r" 指"走势方向"（= orbit doc 的 h，角向），不是 orbit doc 的径向
> σ=h²³。本文 r ≡ h（角向走势进度生成元）。τ 同时共轭 h→h⁻¹ **和** σ→σ⁻¹（因 σ=h²³），故
> srs=r⁻¹ 在角向和径向两个投影上都成立——这是协变的双重根源（角向 T₅₇ 镜像 + 径向莫比乌斯扭）。

### 1.2 srs=r⁻¹ 本身给出什么：T₅₇ 镜像（强，但不是协变）

半直积 ℤ⋊ℤ₂ 与直积 ℤ×ℤ₂ 的唯一区别 = ℤ₂ 对 ℤ 的作用：
- **直积 ℤ×ℤ₂**（srs⁻¹=r）：s 与 r 对易 ⟹ **s 是真正独立自由度**，可全局固定极性而不与走势方向交互。
- **半直积 ℤ⋊ℤ₂=D∞**（srs⁻¹=r⁻¹）：s 把 r 共轭为 r⁻¹ ⟹ **翻极性 = 反转走势取向（镜像）**。

读为缠论：`srs=r⁻¹` ⟺ **做空是做多的镜像**（翻极性后，盈亏方向相对走势反号）= **T₅₇**
（orbit doc NR-3 共轭面 / `project_bidirectional_accounting` 方向协变传导 `basis←basis−d·π/units`，
d=±1 是同一守恒律两极性面）。**这是 srs=r⁻¹ 的直接、强、字面读法。**

**但 T₅₇（镜像）≠ 协变（s_k=d_k）。** 关键诚实区分（两个不同逻辑类型）：
- **srs=r⁻¹** 是**群呈现中两生成元的代数关系**（关于群结构）。
- **协变 s_k=d_k** 是**状态上的约束/截面**（选出合法 (r,s) 子集，或函数 s=f(r)）。

群 D∞ 含全部元素 {rⁿ, rⁿs}。`rⁿs` = "在走势位置 n、极性翻为空"——这是**完全合法的群元素**。
所以**裸呈现关系不禁止任意级别自由翻转 s（整仓翻转）**。结论：

> **srs=r⁻¹ 单独不蕴含协变。** 它给镜像（T₅₇），是协变的**必要前提**（直积下极性可全局独立，
> 故必须半直积），但**不充分**——不锁定 s 何时改变。编排者"协变是 srs=r⁻¹ 的必然推论"的
> 字面表述**定位偏移**（声明膨胀风险，formalization-validity-domain 模式 3：把代数关系直接
> 当成状态约束）。

### 1.3 协变的真正群论根源：P2 合法性谓词（作用 groupoid 层）

协变由 orbit doc §3.2 的**四类分类合法性谓词 P2** 钉死（已 L0 结算）：

> **P2（手性翻转相位）**：τ（极性翻转）**仅在 φ=0 合法**；φ≠0 翻手性非法（orbit doc §3.2 非法操作）。
> 依据：T₅₁（莫比乌斯缝——手性只在背驰点翻）+ NR-3（多空两书角向不连通，唯一通道 φ=0 手性缝）+
> 第30课延异（交易不可逆/有时间性）。

φ=0 = 背驰点 = **走势完成/转折点**（角向圈闭合的奇点，orbit doc §1.1：唯一奇点 φ=0 = 背驰 ∧ 手性翻转）。

**协变不变量的群论证明（L0）**：

1. 设级别 k 当前走势方向 d_k，持仓极性 s_k。初始锚定 s_k = dir_to_polarity(d_k)（根恒多 = 541
   莫比乌斯丛初始截面；走势上行→多）。
2. 唯一能改变 s_k 的操作是 τ（手性翻转）。由 P2，τ 仅在 φ=0 合法。
3. φ=0 = 走势完成点 = d_k 翻转点（走势走完一段，反向新段开始，d_k→d_k.flip()）。
4. 故 τ 改变 s_k 的**同一时刻** d_k 也改变 ⟹ 协变 s_k=dir_to_polarity(d_k) 在 τ 前后**同步保持**。
5. 任何在 φ≠0 翻 s_k（走势中途翻转，d_k 未变）= **非法 τ@φ≠0**（违 P2）⟹ 破坏协变。

> **定理 PC1（极性-级别协变不变量，L0）**：在合法操作下，s_k = dir_to_polarity(d_k) 是
> **守恒不变量**——因唯一改变 s_k 的操作（φ=0 手性缝 τ）与 d_k 的改变同时发生。
> 破坏协变 ⟺ 中途翻转 τ@φ≠0 ⟺ P2 违反。**协变 = 镜像结构（T₅₇）在 P2 约束下的唯一一致截面。**

**这是必然推论，不是新约束**：PC1 不引入任何 P2/NR-3/T₅₁/541 之外的新公理；它是这些已结算 L0
事实在"持仓极性"这一可观测量上的**重新表达**。协变是 D∞ 覆盖作用的莫比乌斯丛**唯一无矛盾截面**
（541：根恒多 = 莫比乌斯丛无全局截面 ⟹ 极性不能全局固定独立于走势相位 ⟹ 必须协变 + 在缝翻转）。

### 1.4 引擎已编码协变映射（决定性证据：协变是必然，非新约束）

`rust/src/recursive_t/rec_engine.rs:152`：
```rust
/// 走势几何方向 → 操作极性（Up=Long / Down=Short）。
pub fn dir_to_polarity(d: Direction) -> Polarity {
    match d { Direction::Up => Polarity::Long, Direction::Down => Polarity::Short }
}
```
**这正是 dir_to_polarity = PC1 的协变映射 s_k = dir_to_polarity(d_k)，已在代码中。** 引擎承认协变
（sink 开次级别腿用 `flip_pol(pdir)`、recover 升回父向、route_bsp 子级"永不独立翻转"——全部协变
by construction）。**缺的不是映射，是把它作为核心仓的强制不变量**——核心级翻转当前违反它（§三）。

---

## 二、unn 第 9 prove 设计：`prove_polarity_covariant_with_level`

### 2.1 规范函数（仿 `prove_self_level_symmetric` 的"规范 + make-observable"模式，137号）

```rust
/// **协变规范（PC1，L0）**：级别 k 持仓极性的协变期望 = 该级别走势方向的极性。
/// = dir_to_polarity(node.direction)，已是引擎既有映射（rec_engine.rs:152）。
fn covariant_polarity_standard(level_trend_dir: Direction) -> Polarity {
    dir_to_polarity(level_trend_dir)   // Up→Long, Down→Short
}
```

### 2.2 prove 函数（violation = panic）

```rust
/// **N9（极性-级别协变，PC1）**：每个活跃级别 k 的持仓极性 s_k 必 == 该级别走势方向极性
/// dir_to_polarity(d_k)——**除 φ=0 缝翻转断面**（合法 τ@φ=0：走势完成同时翻 s 与 d，测度零过渡）。
/// 违反 ⟺ 中途翻转 τ@φ≠0（持仓极性与本级别走势方向反号、且非缝 bar）= P2 违反 = 539/553/560
/// 整仓翻转穿仓的架构根源。violation = panic（make-decision-observable，137号）。
fn prove_n9_polarity_covariant(
    instances: &[TInstance; MAX_LEVEL],
    is_seam_bar: &[bool; MAX_LEVEL],   // 级别 k 本 bar 是否处 φ=0（走势完成/背驰确认断面）
    bar: i64,
) {
    for k in 0..MAX_LEVEL {
        let inst = &instances[k];
        if !inst.is_active() { continue; }
        if is_seam_bar[k] { continue; }   // φ=0 缝：合法翻转断面（测度零过渡，非状态），豁免
        let standard = covariant_polarity_standard(inst.node.direction);
        assert_eq!(
            inst.direction, standard,
            "N9 协变违反@bar {bar} level {k}：持仓极性 {:?} ≠ 本级别走势方向极性 {:?}\
             （走势 dir={:?}）——非缝 bar 持仓与本级别走势反号 = 中途翻转 τ@φ≠0（P2 违反）\
             = 整仓翻转吃跌死域（539/553/560）。合法翻转仅在 φ=0 缝（走势完成同时翻 s 与 d）。",
            inst.direction, standard, inst.node.direction
        );
    }
}
```

### 2.3 精确判据 + violation 条件

| 维度 | 内容 |
|---|---|
| **不变量** | ∀ 活跃级别 k：`instances[k].direction == dir_to_polarity(instances[k].node.direction)` |
| **豁免** | `is_seam_bar[k]`（级别 k 本 bar 处 φ=0：走势完成/背驰确认断面）——合法 τ@φ=0，测度零过渡（`project_bidirectional_accounting`：FLAT=测度零过渡非状态；翻转=会计断面 cascade settle） |
| **violation** | 非缝 bar，活跃级别 k 持仓极性与本级别走势方向**反号** = 中途翻转 τ@φ≠0 = P2 违反 |
| **panic 信息** | 暴露 (bar, level, 持仓极性, 走势方向, 规范极性) ——使协变违反在架构层可观测（137号） |
| **非重言** | `inst.direction`（持仓状态，由 enter/flip/sink 写）独立于 `node.direction`（走势测量）——二者反号是运行时可观测事件，非永真断言（仿 prove_self_level_symmetric 的内联 gate 独立性） |

> **认识论层级区分（codex 异质质询精化，§八）**：**PC1 定理本身 = L0**（协变是必然不变量）；
> 但 **N9 在工程层的捕获能力 = L2/L3 依赖**——N9 区分"合法缝翻转"vs"违规中途翻转"完全依赖
> `is_seam_bar[k]` 的判定精度（"级别 k 此刻是否真在 φ=0"）。该判定精度正是 555/553「精确走势完成
> 识别」、539「A′/A″ 事件闸门」要解决的问题。若 `is_seam_bar` 不精确（漏标/错标 φ=0），N9 退化为
> 噪声守卫。**故"PC1 是 L0 必然"与"N9 能有效捕获违反"是两个等级，不可混同**（前者 L0，后者 L2/L3）。

### 2.4 消费点（prove 应嵌入的操作后断面）

每个写 `instances[k].direction` 的 τ 原子操作后调用：`enter`/`route_bsp` 核心级 flip（926-928）/
`nest_try_flip` 整仓翻转（944）/`emergence_upgrade` ascend（733）/`sink`（784）/`recover`。
**关键**：`is_seam_bar[k]` 必须 = "级别 k 的走势此刻在 φ=0（背驰确认/走势完成）"——这正是
539 §三 A′/A″ 要恢复的「次级别确认作**事件**闸门」、553 §3.3「每级别精确走势完成识别」、
560 开放轴 (c)「严格逐级区间套」。**N9 把这些散落的开放轴统一为单一协变不变量。**

---

## 三、539 = 缺这条 prove 的架构产物：论证（诚实分层）

### 3.1 当前架构在哪里允许 s 独立翻转（违反协变）

`route_bsp`（rec_engine.rs:874-934）逐字暴露违反点：

- **子级分支 `Some(p)`（877-906）**：「区间套约束，**永不独立翻转**」——走 sink（开 `flip_pol(pdir)`）/
  drain/recover。**协变 by construction**（次级别腿协变于次级别走势，§3.3 不是违反）。
- **核心级分支 `None`（907-932）**：「**唯一独立翻转点**」。反向 BSP ⟹
  ```rust
  self.clear_all(c);            // 清全塔趋势底仓
  self.enter(j, dir, node, c);  // 同 level 反向 enter（整仓翻转）
  ```
  门控**仅 `dir != cdir`**（BSP 方向 ≠ 当前核心方向）——**没有检查核心级别的走势 d_core 是否真翻转**。
  这就是 553 cascade flip（核心翻空毁 anchor 底仓，OKLO +458.9→−106.6💥）和 539 整仓翻转的代码现场。
- **NEST `nest_try_flip`（979-1002）**：**旁路 route_bsp**，背驰段武装 + a0 定位 ⟹ `clear_all + enter`
  全仓翻转。这是 560 读法乙 7/8 穿仓的现场。

**三处共同模式**：核心/顶层持仓极性由 BSP/背驰段信号翻转，**未验证该持仓级别自身走势方向已反转**
= P2 违反（τ@φ≠0）= N9 violation。

### 3.2 强（confirmed）：539 的灾难形式 = 缺 N9 的架构产物

| 谱系 | 失败形式 | N9 是否捕获 | 证据 |
|---|---|---|---|
| 560 读法乙 NEST | 顶层背驰段整仓翻空，d_top 仍 Up（强牛）→ 6 个强牛标的全穿仓 | ✅ **捕获**（非缝 bar，s_top=Short ⊥ d_top=Up）| 7/8 灾难，P1 1/8 |
| 553 cascade flip | 核心 emergent_top.completed 粗识别翻空，毁 anchor 底仓 | ✅ **捕获**（核心 s 翻空 ⊥ 核心 d Up）| 8/8 ≤ anchor，OKLO 穿仓 |
| 552 §八 B 路 | 深层空腿全程持空不切换，强牛失血 | ✅ **捕获**（空腿 s=Short ⊥ 其级别 d 在强牛段 Up）| BTC −87.4%/−45% |

**这部分编排者主张 confirmed**：539 的灾难形式（整仓翻转穿仓）= 当前所有架构（OFF/ANCHOR/cascade/
读法乙）允许 s 在核心/顶层独立翻转（缺 N9 协变约束）的架构产物，**非市场规律**。补 N9 ⟹ 这些违反
在架构层 panic 被禁——做空只能发生在 d_k 局部反向（次级别回调）的级别，**强牛底仓被强制锁多
（吃大涨），无需 if-else 硬编码 regime**（编排者命题正解，= 552 anchor 的 L0 第一性原理依据）。

### 3.3 协变是**逐级别**的：次级别空腿不是违反（编排者命题关键点）

sink 开 `flip_pol(pdir)` 次级别空腿——看似 s=Short 而父 d=Up。**不是 N9 违反**：协变是
**s_k = d_k（级别 k 自身走势）**，不是 s_k = d_parent。强牛回调时次级别**自身走势 d_sub=Down**
（回调=次级别下跌走势），故 s_sub=Short 协变于 d_sub。这正是编排者「次级别 s 局部空（吃回调，
级别正交不对立）」——**N9 用 `instances[k].node.direction`（k 自身走势）判定，自动处理"级别正交"**。

> ⚠ **诚实标注（有效域边界）**：sink 当前用 `flip_pol(pdir)` 作为"次级别逆父向"的**代理**，
> 不是测量次级别自身走势 d_sub。严格 N9 要求 `node.direction` = 次级别**实测**走势方向。
> 若 sink 腿的 node.direction 实际未对齐次级别实测走势（代理失配），N9 会捕获——这与 553
> §3.3「正确形式 = 每级别骑该级别走势」、555 区间套 c 段钻取是同一要求。N9 是该要求的不变量化。

### 3.4 弱（L3 未决）：539 的残余 regime 税 ≠ 缺 N9（不可声明膨胀）

**必须诚实分离**（formalization-validity-domain 模式 3）。539 含两个子命题：

| 539 子命题 | N9 是否解决 | 等级 |
|---|---|---|
| (a) 整仓翻转穿仓（核心/顶层 s ⊥ d）| ✅ N9 架构层禁止 | L0（§3.2 confirmed；N9 捕获精度 = L2/L3，见 §2.3）|
| (b1) 确认滞后**存在**（φ=0 缝 = 背驰确认后，向心读过去 NR-4 ⟹ 缝兑现晚于回调起点）| ❌ N9 不解决 | **L0 不可约**（codex 精化，§八）|
| (b2) (b1) 是否使做空腿强牛**净亏到**称"市场规律"| ❌ N9 不解决 | **L3 未决** |

560 §下游推论 1 明判：**「解 556 ⟹ 暴露 539」，539 regime 税不可约**。553 §三：对称双吃**正确形式
（每级别精确协变切换）未试**，可达性 L3 未决（不声称可达/不可达）。故：

> **N9 是必要的结构条件（禁止 (a) 灾难形式），但不是吃跌收益的充分条件。** (b) 的根源是
> **确认滞后**（533/547）——即使 N9 强制协变，次级别 τ@φ=0 缝在 a0 兑现时回调可能已结束
> （560 背驰段 44% 宽状态 vs 完成 0.2% 事件，确认滞后是逻辑的）。
>
> **codex 异质质询精化（§八）**：(b) 须再分两层——
> - **(b1) 确认滞后的存在 = L0 不可约**：φ=0 缝 = 背驰确认后才可识别（NR-4 向心读过去：踪迹在当下
>   可读但事件已发生），故缝兑现**结构性地**晚于回调起点。这不依赖任何 L3 数据，纯从"确认机制是
>   回溯性而非预测性"代数导出。除非把确认机制重构为预测性（引入前视，违 NR-4），(b1) 不可消除。
> - **(b2) (b1) 是否使做空腿强牛净亏到称"市场规律" = L3 未决**：滞后存在 ≠ 净亏多少；后者是 regime
>   函数（震荡可吃、强牛失血），是经验量。
>
> **声称"补 N9 ⟹ 539 完全是架构产物非市场规律"= 声明膨胀**（把有效域=定义域）。诚实表述：
> **N9 把 539 灾难形式 (a) 重诊断为架构产物（L0 强）；确认滞后 (b1) 是 L0 不可约的结构税（N9 不解）；
> 仅 (b2)"是否净亏到算市场规律"才 L3 未决。** 编排者"539 非市场规律"主张：对 (a) 成立、对 (b1) 错
> （(b1) 是结构必然不是架构 bug）、对 (b2) 未决。N9 提供 (b2) 的判决场干净基线（协变强制后，剩余
> 失血 = 纯 (b1) 滞后税，其净收益符号交 prop4-bidir L3）。

---

## 四、与 prop4-bidir 经验侧的正交性（必然性侧定位）

- 本工位 = **必然性侧第一性原理依据**：N9 形式化「为什么对称双吃正确形式是协变的」（L0）。
- prop4-bidir 经验侧 = N9 强制后的**收益判决场**（L3）——回答 539 残余 (b) 可达性。
- 二者正交（`no-patch-mentality`：必然性累积不被回测否定，但回测划定有效域，231号）。
- **N9 是 553「对称双吃正确形式」、555「区间套 c 段钻取」、539「A′ 事件闸门」、560「开放轴 (c)
  严格逐级区间套」缺失的统一 L0 不变量**——四个散落开放轴是 N9 在不同消费点的显形。

---

## 五、结果包六要素

1. **结论**：极性-级别协变是 D∞ 覆盖作用结构的 L0 必然推论（强），定位在作用 groupoid 合法性
   谓词 P2（τ@φ=0），非裸呈现关系 srs=r⁻¹（后者给 T₅₇ 镜像，必要非充分）。设计 unn 第 9 prove
   `prove_n9_polarity_covariant`：活跃级别持仓极性 == dir_to_polarity(本级别走势方向)，豁免 φ=0
   缝，违反=panic。539 灾难形式（整仓翻转穿仓）= 缺此 prove 的架构产物（L0 强）；539 残余 regime
   税 = L3 未决（不声明膨胀）。

2. **定义依据**：
   - D∞=⟨h,τ|τ²=e,τhτ⁻¹=h⁻¹⟩、P2（τ@φ=0 合法）、NR-3（两书角向不连通）、四类分类——
     `docs/orbit_enumeration_completeness.md` §1.1/§3.2/§4（全 L0）。
   - T₅₁（莫比乌斯缝）、541（根恒多=莫比乌斯丛无全局截面）、第30课延异。
   - dir_to_polarity（rec_engine.rs:152）= 协变映射 s_k=dir_to_polarity(d_k) 已在代码。
   - 输入特征满足定义：核心级翻转（route_bsp:907-932 `dir!=cdir` 门，无 d_core 反转检查）+ NEST
     整仓翻转（944）= τ@φ≠0 = P2 违反的代码现场。

3. **边界条件**（结论翻转条件）：
   - 若 srs=r⁻¹ 能**单独**蕴含协变（即能从裸呈现关系导出 s 不能在 φ≠0 翻），则"定位在 P2"的
     精化翻转、编排者原始归因成立。**反例**：群 D∞ 含 rⁿs ∀n（任意位置翻极性的合法群元素），
     裸关系不禁中途翻转——故精化成立。
   - 若 539 残余税 (b) 在 N9 协变正确形式下 L3 强牛**不再失血**，则编排者「539 完全非市场规律」
     成立，(b) 从"L3 未决"升为"架构产物"。**当前 553/560 证据指向 (b) 不可约（确认滞后逻辑性），
     但正确形式未试，未定。**
   - 若 sink 次级别腿 node.direction 实测对齐次级别走势（非 flip_pol 代理），则 §3.3 代理失配
     消除，N9 在子级恒真。

4. **下游推论**：
   - N9 成立 ⟹ 做空在架构层被限定到 d_k 局部反向的级别（次级别回调），强牛底仓强制锁多——
     **regime 由结构自动处理，删 if-else 硬编码 regime 门**（编排者命题正解 = 552 anchor 的 L0 依据）。
   - N9 统一 539(A′事件闸门)/553(精确切换)/555(c段钻取)/560(逐级区间套) 四开放轴为单一不变量。
   - N9 把 539 (a)/(b) 干净分离：协变强制后剩余失血 = 纯 (b)，为 prop4-bidir L3 提供判决基线。
   - N9 与 N7（spawn 自层）/N8（守恒）正交：N7 管"在哪 spawn"、N8 管"股数守恒"、N9 管"极性方向"。

5. **谱系引用**（曾发生概念分离的领域）：
   - **能指碰撞**：σ=h²³（径向）vs 527号 σ∈{±1}（手性）——本文 r≡h（角向），已消歧（§1.1）。
   - **545**：emergent_top 方向锚（b 所指）= 协变在核心仓的**失败尝试**（判据震荡，−1069% 做空陷阱）
     ——N9 是 545 (b) 所指「核心仓应该多还是空」的正确不变量形式（545 §11.6「换级联判据」= N9 的
     is_seam_bar 来源）。
   - **544**：手性交替 L0→观测降级——N9 不重蹈（N9 是 L0 不变量，非过强声明；收益侧明标 L3 未决）。
   - **231**：有效域≠定义域——539 (a) L0/(b) L3 分层是第 N 例。
   - 不确定是否有更早的"协变"专属谱系：检索 settled/ 未见独立"polarity covariance"节点，本文是首次
     将散落的 P2/NR-3/541/545(b)/553/560 协变要求结晶为单一不变量。

6. **影响声明**：
   - **不改动代码或定义**（设计 + 形式化，不实装，编排者指示）。
   - **新增**：本文件（review-results）。
   - **影响模块（若未来实装）**：`unified_necessity.rs`（加 prove_n9 + covariant_polarity_standard）、
     `rec_engine.rs`（route_bsp 核心级 flip 926-928 / nest_try_flip 944 / sink 784 后嵌断面 + 提供
     is_seam_bar）、`LevelView`（暴露级别走势 φ=0 缝标记）。
   - **重诊断 539**：从"清仓判据 regime 门控开放轴"补充「(a) 灾难形式 = 缺 N9 协变 prove 的架构产物
     (L0)；(b) 残余税 L3 未决」的横切根因。
   - **重诊断 553/560**：整仓翻转穿仓的 L0 根因 = N9 协变违反（τ@φ≠0），提供 553「正确形式」缺的
     L0 必然性依据。

---

## 六、认识论等级标注（formalization-validity-domain 强制）

| 命题 | 等级 | 信息增量 | 同义反复 vs 增量 |
|---|---|---|---|
| srs=r⁻¹ ⟺ T₅₇ 镜像（非协变） | L0（代数） | 正 | 增量：纠正编排者归因（裸关系≠协变）；codex 三路径证伪失败 confirmed |
| PC1 协变不变量（定位 P2） | L0（群论+延异裁决） | **低（作为新数学）** | **同义反复区**：重表达 P2/NR-3/541，无新公理 |
| PC1 的可强制性（violation=panic） | L0（PC1 定理本身） | **高** | 增量：当前未强制，核心 flip 违反它 |
| **N9 工程捕获能力**（区分合法缝 vs 违规中途翻转）| **L2/L3 依赖** `is_seam_bar` 精度 | —— | codex 精化：≠ PC1 的 L0 级别（依赖 555/553 精确缝识别）|
| 539 (a) 灾难形式 = 缺 N9 架构产物 | L0（源码+谱系链接） | **高** | 增量：539/553/560 首次链到 P2 groupoid 合法性 |
| 539 (b1) 确认滞后**存在** | **L0 不可约**（NR-4 回溯性确认） | 正 | codex 精化：编排者"非市场规律"对 (b1) 错——结构必然非架构 bug |
| 539 (b2) 滞后是否净亏到算"市场规律" | **L3 未决** | —— | **诚实标注：不声明**（净收益符号是 regime 函数，prop4-bidir 判决）|
| N9 充分性（吃跌收益）| **L3 未决** | —— | 不声称——N9 必要非充分，收益是 prop4-bidir 判决 |

> **核心诚实声明**：N9 作为新数学的信息增量**低**（同义反复——P2/NR-3/541 已结算，N9 是其在持仓
> 极性上的重表达）。N9 的价值**不在新数学**，在 (a) **架构层可强制**（把已知必然变成 panic 守卫，
> 当前核心 flip 违反它）+ (b) **重诊断**（539/553/560 经验失败首次定位到 P2 groupoid 合法性违反，
> 给 553「正确形式」缺的 L0 依据）。**这正是编排者说的"必然性推理 prove 里缺的东西"——
> 不是缺一条新定理，是缺把已有必然（P2 协变）实装为架构不变量。**

---

## 七、建议谱系号 + 与 560/539/unn 的关系

**建议立号：561**（settled/ 最高为 560）。

| 关系 | 谱系号 | 性质 |
|---|---|---|
| **depends_on** | orbit doc P2/NR-3（L0 来源）、541（莫比乌斯丛无截面）、545（emergent_top 方向锚=协变失败尝试）| 561 是这些的协变不变量结晶 |
| **provides L0 backing for** | 553（对称双吃正确形式=协变，553 缺的 L0 依据）、555（c 段钻取=is_seam_bar 来源）| 561 = 553/555 的第一性原理侧 |
| **重诊断（横切根因）** | 539（(a) 灾难形式 L0 / (b) 残余税 L3）、560（整仓翻转 7/8 穿仓 = N9 违反）| 561 给 539/560 经验失败的 L0 协变根因 |
| **正交** | unn N7（spawn 自层）/N8（守恒）——N9 管极性方向，独立轴 | 561 = unn 第 9 prove |
| **能指消歧** | 527（σ 手性）vs orbit σ=h²³（径向）——561 用 r≡h 已消歧 | 防碰撞 |

**negation_form 建议**：`separation`（539 灾难形式 (a) 从残余税 (b) 分离——L0 架构产物 ≠ L3 市场规律）
+ 协变作为 545(b) 方向锚所指的正确形式（aufhebung：否定 emergent_top 锚、保留方向协变、提升为不变量）。

**选择类待编排者**（不擅自结算）：
1. 561 是否独立立号 vs 回填 unn/553——按 012 谱系优先于汇总，561 携带 553/539 没有的新内容（P2→协变
   不变量推导 + N9 设计），建议独立立号；但叙述层是否并入"命题4 有效域边界"是编排者价值判断。
2. N9 是否实装——本工位仅设计（编排者指示不实装）。实装触发 prop4-bidir L3 判决场（539 残余税 (b2)
   可达性），需独立授权。

---

## 八、异质质询结果（codex-challenger，2026-06-23，默认证伪立场）

中心承重断言（§1：srs=r⁻¹ 必要非充分、P2 是协变根源）经 codex 异质对抗复核（agentId
`a2c6fa871202b9263`，diagnose 模式）。**判决：confirmed**（三条证伪路径全部失败）。

| 证伪路径 | 尝试证明 srs=r⁻¹ 单独蕴含协变 | 失败节点 |
|---|---|---|
| **A 莫比乌斯/w₁** | srs=r⁻¹ ⟹ w₁≠0 ⟹ 无全局截面 ⟹ 协变 | 「无全局常截面」只排除"ε 永不变"，**不锁定翻转 φ 位置**——ε 在 φ=5 翻、在 φ=0 翻都使丛非平凡。H¹(D∞,ℝ₋)=ℝ 给的是跨级别位移率，非"ε 仅 φ=0 翻"约束 |
| **B 合法性群层判定** | D∞ 含 rⁿs ⟹ 合法性可群层判定 ⟹ P2 冗余 | **循环论证**：该反驳以"τ@任意φ 合法"为前提 = 直接否定 P2；群元素代数存在性 ≠ 物理操作合法性，桥接需独立公理 |
| **C σ-轨道/NR-3** | 轨道不连通 ⟹ τ 仅 φ=0 合法 | NR-3 只规定"纯 h 路径不跨手性（不插 τ 则 ε 不变）"，**不规定含 τ 的路径在何 φ 合法** |

**共同失败模式**（codex 诊断）：三路径都在「代数关系 ⟹ 物理合法性谓词」的跳跃处断链——这正是
§1.2 主张的核心（呈现关系给代数结构，P2 给合法性谓词，二者不同逻辑类型）。

**codex 两处精化（已并入 §2.3 / §3.4）**：
1. **PC1 定理（L0）≠ N9 工程捕获能力（L2/L3）**：N9 区分合法/违规翻转依赖 `is_seam_bar` 判定精度
   （= 555/553 精确走势完成识别）。已并入 §2.3。
2. **539 (b) 须分 (b1)/(b2)**：确认滞后的**存在** = L0 不可约（NR-4 回溯性确认结构必然，非架构 bug，
   N9 不解）；其**净亏到算市场规律** = L3 未决。已并入 §3.4。这比初稿"(b) L3 未决"更强——编排者
   "539 非市场规律"对 (b1) **错**（(b1) 是结构必然），不只是"未证"。

**对编排者命题的最终裁决**：
- "协变是群论必然，539 灾难形式是缺 prove 的架构产物" → **confirmed（强，L0）**。
- "协变是 srs=r⁻¹ 的必然推论" → **归因偏移**（应为 P2；srs=r⁻¹ 必要非充分）。
- "539 残余失血非市场规律" → **(b1) 错**（确认滞后是 L0 结构必然）、**(b2) 未决**（净收益符号 L3）。

---

# 九、完全分类覆盖完备性（主 prove，编排者二次重定）—— N9 在此降为推论

> 编排者纠正：「极性协变 N9 偏窄了，不是解法本身……走势的**完全分类**才是解法。」本节是主体；
> §1-§八（N9 极性协变）降为一个推论。主 prove = `prove_operation_covers_complete_classification`。

## 9.1 两完备性定理 = 状态空间的完全划分（L0，原文 + 群论）

**缠论侧（第17课 + 第21课，逐字原文）**：

| 定理 | 原文 | 作用 |
|---|---|---|
| 走势分解定理一（第17课）| 任何级别任何走势 = 同级别盘整/上涨/下跌三类的连接 | 走势类型穷尽（3 类）|
| 基本原理一（走势终完美）| 任何级别任何走势类型终要完成 | 每段终结 = 一个买卖点 |
| **买卖点完备性定理（第21课）** | **市场必然产生赢利的买卖点，只有第一、二、三类** | 买卖点穷尽（无第四类）|
| **升跌完备性定理（第21课）** | **市场中任何向上与下跌，都必然从三类买卖点中某一类开始及结束。市场走势完全由线段构成，端点是某级别 type1/2/3 买卖点** | **状态空间被完全划分**（每段端点 ∈ type1/2/3）|

买卖点轨道的完全分类（第21课导出）：每个买卖点对应**与最近中枢的关系** × **中枢三态**：
- 中枢下 → 转折（type1，仅下跌/盘整后）；中枢上 → 延续（type3，中枢扩张/新生）；
- type2 = type1 后次级别回抽（次级别 type1 构成，定律一）；
- 中枢三态：**延续 / 扩张 / 新生**；二买/二卖**成败**（成 = 创新低/高续，败 = 中枢形成）。
- 递归：同级别 + 次级别（每级别一套完全分类）。

**群论侧（orbit doc Burnside，§2）**：D∞ 在 (φ,r,ε) 上 Burnside 级联 **506→46→2→1** 穷尽划分
所有状态（轨道-稳定子定理：每状态 ∈ 恰一轨道，506 = Σ轨道大小，无遗漏）。46 σ-轨道 = 径向母线
= 全部 (φ,ε) 类（角向相位 × 手性），每条覆盖整条级别线。

**两侧是同一完备性的两种语言**：买卖点完全分类（缠论语义划分）= φ∈ℤ/23 角向圈上的标注
（type1=φ=0 背驰，type2/3 = 其他 distinguished φ，中枢 = 复现结构）× ε（手性）× 级别递归。
**完全划分 = 升跌完备性（缠论）≡ Burnside 穷尽（群论）**——状态空间无遗漏。

## 9.2 定理 CC（完全分类覆盖，主 prove）

> **定理 CC（L0 划分完备 + 良定义必然）**：设操作映射 Ω: 买卖点完全分类轨道 → 操作。良定义操作
> 系统要求 **Ω 是完全划分上的全函数**——每条轨道（type1/2/3 × 买卖 × 中枢三态 × 二买卖成败 ×
> 级别）有**确定操作**，无轨道落入默认/未定义分支。
>
> 证明骨架：(1) 状态空间被完全划分（9.1，L0 强）；(2) 系统每 bar 必在某状态（无"未定义"）；
> (3) ⟹ Ω 必在每轨道有定义。**漏轨道 = 该状态无确定响应 = 落默认分支（死扣/机械翻转/误触发）**。

**强/弱诚实**：
- 划分完备性 = **L0 必然（强）**：Burnside + 升跌完备性定理。
- Ω 全函数性 = **良定义系统的强必然**（划分完备 + 必须操作 ⟹ Ω 全）。
- **每轨道操作内容 = 缠论语义（中等，原文导出）**：type1 转折→flip、type3 延续→hold/加、
  中枢扩张→升级、二卖失败中枢新生→cover+做多、力度（第19课）→减/不减。非纯群论。
- **故主 prove 的覆盖性是强必然；内容是缠论导出。** 不声称内容是纯 D∞ 推论（诚实分层）。

## 9.3 prove 设计：`prove_operation_covers_complete_classification`

```rust
/// **CC（完全分类覆盖，第21课升跌完备性 + Burnside 穷尽）**：操作映射 Ω 必在买卖点完全分类
/// 每轨道有确定操作。violation = 某走势状态（轨道）落入默认分支（无确定操作）= panic。
/// 轨道 = (BSPKind{Type1/2/3 × Buy/Sell}, 中枢态{延续/扩张/新生}, 二买卖{成/败}, level)，递归。
fn prove_operation_covers_complete_classification(
    orbit: &BspOrbit,        // 完全分类轨道（信号层已产出 BSPKind + 中枢态 + 成败）
    op: Option<Operation>,   // Ω(orbit)：None = 漏轨道（落默认）
    bar: i64,
) {
    assert!(
        op.is_some(),
        "CC 违反@bar {bar}：走势状态轨道 {orbit:?} 无确定操作（落默认分支）\
         = Ω 不覆盖买卖点完全分类（第21课升跌完备性）= 漏轨道架构产物（539）。\
         每条轨道（type1/2/3×买卖×中枢三态×二卖成败×级别）必须显式映射确定操作。"
    );
    // 进一步：op 必须 == 该轨道的缠论规范操作（第21课语义），非任意默认。
    assert_eq!(op.unwrap(), canonical_operation(orbit), /* 规范操作不匹配 = 误覆盖 */ ...);
}
```

判据：**Ω 定义域 = 完全分类（BspOrbit 全集）**，非坍缩的 `{buy,sell}`。violation =
(a) 漏轨道（op=None，落默认 → 死扣）；(b) 误覆盖（op≠canonical，如非买卖点状态触发操作）。

## 9.4 539 重诊断：Ω 坍缩完全分类（代码现场坐实）

**信号层产出完全分类**（`rust/src/recursive_t/types.rs:171-202`）：`BSPKind` = Type1/2/3 ×
Buy/Sell；`divergence.rs` 区分中枢三态（趋势背驰 / 盘整背驰 / type3 中枢离开-回抽不破）。

**但操作接口坍缩**（`rec_engine.rs:277` LevelView 注释逐字「纯 BSP 驱动，**不分 type1/2/3**」）：
- `LevelView` 只暴露 `buy[]`/`sell[]`（二元）+ `t1buy[]`/`t1sell[]`（type1 子集）。
- `route_bsp(j, is_buy, …)` **只消费 `is_buy`**（二元买卖），按**父方向**路由 sink/drain/recover/flip。
- **完全分类（type2/3、中枢延续/扩张/新生、二卖成败）在操作接口被丢弃** ⟹ 多条轨道坍缩进同一
  粗分支或落默认。

**两失效形式 = 539 的两面**：
| 形式 | 机制 | 539 显形 |
|---|---|---|
| **过覆盖**（在非买卖点状态操作）| NEST 用背驰段（≠买卖点）武装翻转（nest_try_flip 仅 type1+背驰段，忽略 type2/3）；经验趋势触发 | 560 读法乙 7/8 穿仓（强牛无真 type1 卖却翻空）= N9 违反的特例 |
| **欠覆盖**（真买卖点轨道无操作）| 二卖失败中枢新生 → route_bsp 无此轨道 → 落父向默认分支 → 死扣 Short / 误 sink | 553 cascade 死扣；做空腿强牛失血（该 cover 不 cover）|

**编排者论证（强，L0 架构）**：强牛中走势没出真 type1 卖（走势未完成，升跌完备性：上涨趋势确定后
只可能有 type3 买，不可能有 type1/2 买卖点）⟹ 若 Ω 只在真买卖点操作 ⟹ 强牛不出卖点 ⟹ s 保持多
（type3 延续）⟹ 不做空 ⟹ **539 在架构层消失**。539 失血来自 fake-trigger（背驰段/经验，过覆盖）+
漏轨道死扣（欠覆盖）——**架构产物，非市场规律**。

## 9.5 与 N9 / (b1) 的关系（推论归位 + 调和）

- **N9 = CC 的一个推论**：N9（极性翻转只在 φ=0 缝）= type1 走势完成**单轨道**的操作规则
  （转折→flip）+ "无 fake-trigger flip" 子规则（禁过覆盖的极性翻转特例）。CC 覆盖**所有**轨道
  （type2/3、中枢三态、二卖成败），N9 只管 type1-flip 一条。**N9 ⊊ CC。** codex confirmed 的 N9
  结果（§八）作为推论保留有效。
- **(b1) 确认滞后调和**：完全覆盖下，(b1) 滞后只作用于**真买卖点触发的稀疏正确操作**（吃回调当
  回调真完成），且被**二卖成败轨道**的退出（二卖失败→cover）**有界化**——不是系统性 regime 税。
  系统性 539 失血来自过/欠覆盖（架构）。故 (b1)（§3.4）从"539 主机制"降为"稀疏正确操作上的有界
  残余"；主机制 = Ω 不完全（CC 违反）。**这调和了 codex 的 (b1)=L0 与编排者的"539=漏轨道"**：
  二者各为一层（(b1) 有界残余 / 漏轨道是主机制）。

## 9.6 L3 红线（不声明膨胀）

"完全 Ω ⟹ 539 消失" = 编排者 L0 架构预测。**净收益验证 = L3**——complete 逐级覆盖能否在强牛
跨标的真正消除 539 失血，正是 **task #22（prop4-bidir 开放轴 C 严格逐级区间套）的判决场**。
本工位（必然性侧）给 L0 架构依据（Ω 必全覆盖），不声称 L3 净收益（formalization-validity-domain
模式 3 禁止有效域=定义域）。task #22 的严格逐级区间套 = CC 在「逐级 type1 嵌套」轨道上的部分实装。

## 9.7 认识论等级（CC 主 prove）

| 命题 | 等级 | 增量 |
|---|---|---|
| 状态空间完全划分（升跌完备性 + Burnside 穷尽）| **L0 必然（强）** | 原文 + 群论穷尽，无新公理 |
| Ω 全函数性（覆盖每轨道）| **L0 强必然**（良定义 + 完全分类）| 当前 Ω 坍缩为 {buy,sell}，违反 |
| 每轨道操作内容（canonical_operation）| **缠论语义（中，原文）** | 第21课延续/扩张/新生 + 第19课力度 |
| 539 = Ω 坍缩完全分类（漏/误覆盖）| **L0 架构**（代码坐实 LevelView 坍缩）| 539 首次定位到「Ω 定义域 ⊊ 完全分类」|
| 完全 Ω ⟹ 539 净消除 | **L3 未决** | task #22 判决场，不声称 |
| N9（type1-flip 轨道规则）| L0 推论（codex confirmed）| CC 的一个子轨道规则 |
