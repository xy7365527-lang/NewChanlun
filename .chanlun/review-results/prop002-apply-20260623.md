---
trigger: task-39-prop002-apply
mode: genome-mutation-apply
result: applied-pending-commit
grounded_in: ["编排者020批准", "PROP-002", "069层面3", "093-约束4", "095", "097", "275", "(c)裁决-20260623"]
epistemic_level: L1
---
# PROP-002 应用：dispatch-dag.yaml (c) 任务-DAG 递归 genome 改动

**工位**：prop002-apply（topo_address: L0.39，parent_callback: main）
**任务**：#39　**日期**：2026-06-23
**认识论等级**：**L1**（YAML 解析 + 消费者回环 + git diff 实读管线正确性自验；**异质审计节点 #54 是 L2 否证机会**——本工位不能自审，约束3）

> #41 = (c) **机制化实装**（hooks + team-topology，蜂群编排逻辑层，非 genome）。本任务 #39 = (c) **genome 落码**（dispatch-dag.yaml 拓扑定义，020-gated 提案模式）。二者合：实装与 genome 声明一致。

---

## 〇、递归判断（强制 dogfood）

**判定：原子 + 异质审计节点不可省。**

| 维度 | 评估 |
|------|------|
| 可分解为 ≥2 独立子单元？ | **否**。三处变更全在单一 genome 文件 dispatch-dag.yaml，全属同一 (c) 递归概念改动，紧耦合（改 recursion_rules 必同步 description/genealogy_ref），无独立子单元 |
| 需独立执行者？ | 应用本身原子；但**审计需异工位**（约束3：执行不可自观——本工位写 genome 不能自审忠实性） |
| 复杂度超出本工位？ | 否（PROP-002 已精确指定 diff，编排者 020 已批准，本工位只忠实落码） |

**结论**：扁平直接执行（应用 genome diff）+ **TaskCreate 一个异质审计节点 #54**（codex-challenger，blockedBy #39）。该节点同时满足约束3（异工位 ≠ prop002-apply）与约束4（异质验证）——对原子任务，单一 codex-challenger 审计节点是最小但完整的结构。**没有异质审计 = 封闭自证循环 = 违约束4（093号硬约束）**，正是 #31-#36 的缺口，本任务不重蹈。

---

## 一、应用的三处变更（逐字忠实 PROP-002 changes 节）

| 变更 | 位置 | 改动 | 忠实性 |
|------|------|------|--------|
| **变更3** | 顶层 `genealogy_ref`（行20） | 末尾追加 `, (c)-task-dag-recursion-20260623` | ✓ 逐字 |
| **变更2** | `fractal_template.genealogy_ref` + `.description`（行630-631） | genealogy_ref 追加同 token；description 改 "子蜂群 = 子任务 DAG（(c)裁决：TaskCreate 子任务，非 TeamCreate 子 team）；skill 全局可用（事件驱动）" | ✓ 逐字 |
| **变更1** | `fractal_template.recursion_rules`（行651-657→651-660） | 6 项 → 7 项重写 + 2 行 (c)裁决注释。删 spawn 子蜂群/TeamCreate，改 TaskCreate 子任务 unowned + 四类节点 DAG + flat roster（teammate 不 spawn，Lead 唯一 spawn 源）+ 不动点定义加 "TaskList 无无主/未阻塞/in_progress" + parent_callback SendMessage 汇报 + 结构工位例外改 TaskCreate 同类型子任务 | ✓ 逐字 |

**git diff**：1 file changed, 12 insertions(+), 9 deletions(-)。**严格只动 PROP-002 指定的三处**，无夹带。

### (c) 一致性核验（与 #41 实装对齐）
- recursion_rules 项[1]「四类节点（任务/审查/异质审计/结晶）+ blockedBy 边 + metadata.agent_type」↔ #41 team-topology.json spawn_mandate 四类节点模板 ✓
- recursion_rules 项[0]「TaskCreate 子任务（unowned）向下递归」↔ #41 (c) 生产端机制化 ✓
- recursion_rules 项[2]「teammate 不 spawn teammate；Lead 唯一 spawn 源」↔ #41 消费端（Lead 扫无主任务 spawn）+ flat roster 硬约束 ✓
- recursion_rules 项[5]「parent_callback SendMessage 汇报」↔ sub-swarm-ceremony SKILL.md §4 结晶节点 SendMessage 父工位 ✓

genome 声明与 #41 实装一致，无声明-能力背离（spec-execution gap 已闭合）。

---

## 二、验证（L1 管线正确性自验）

| 检查 | 命令 | 结果 |
|------|------|------|
| YAML 有效 | `./.venv/bin/python -c "import yaml; yaml.safe_load(...)"` | ✓ YAML OK |
| 顶层 key 完整 | 15 个 key 全在（version…definition_partial_order） | ✓ 无结构破坏 |
| 变更落实 | top/ft genealogy_ref tail = (c)-task-dag-recursion-20260623；recursion_rules count=7 | ✓ |
| diff 范围 | git diff = 仅三处（12+/9−） | ✓ 无夹带 |

> 注：本工位自验是 **L1**（作者自验管线正确性，信息增量零——验证 YAML 没 bug，不验证 PROP-002 应用是否在概念上忠实）。**忠实性的 L2 否证落在 #54 异质审计节点**（约束3：异工位 + 约束4：异质 codex）。

---

## 三、结果包六要素（完整版——genome 概念层改动）

**1. 结论**：PROP-002 三处变更已忠实应用到 dispatch-dag.yaml（genome），递归载体声明从 team 拓扑（spawn 子蜂群/TeamCreate）扬弃为任务拓扑（TaskCreate 子任务 DAG / 四类节点 / flat roster / Lead 唯一 spawn 源）。YAML 有效、结构完整、与 #41 实装一致。**未 commit**（genome 改动由 Lead commit，本工位不 commit）。

**2. 定义依据**：PROP-002 changes 节（三处变更逐字规格）+ 编排者 020 批准（genome 自修改合规路径，069号层面3：结构修改走提案模式）。输入特征满足：dispatch-dag.yaml 现存 recursion_rules 含 "必须 spawn 子蜂群（095号）"/"TeamCreate 创建独立 team"（满足 PROP-002 "当前 stale 模型" 定义）→ flat-roster harness 禁止（满足 "已上浮 spec-execution gap"）→ 应用 (c) 任务-DAG 模型。sub-swarm-ceremony SKILL.md (c)裁决节 + 097号四类节点模板为目标态定义依据。

**3. 边界条件（结论翻转）**：
1. **harness 恢复 teammate→teammate spawn** → 095号 TeamCreate 真递归复活，PROP-002 应用的 recursion_rules 需回滚/再修订（"机制从子 team 改为子任务 DAG" 前提翻转）。
2. **135号 stale comment 残留**（行650 `# 135号修正：平台（Agent Teams）支持子蜂群递归，073b 平台约束前提已被095号事实否定`）现与 (c)裁决（flat roster 禁 teammate spawn）**矛盾**。本工位因 PROP-002 未授权该行而**未改**（不夹带——硬约束）。**若异质审计判定该残留是 PROP-002 scope 不完整 → 需补提案处理该行**（不在本任务自决范围，留 #54 codex 判定 + Lead/编排者裁决）。
3. **异质审计 #54 否证忠实性** → 若 codex 发现某处变更偏离 PROP-002 或破坏消费者解析，结论从 "已应用" 翻转为 "需修正"。
4. **PROP-002 status 仍 pending_review** → 本应用是 020 批准后的实施动作；提案 frontmatter 的 status 应由具写权限工位改 `implemented`（PROP-002 approval_condition 末句）。本工位未改提案文件 status（避免夹带提案文件改动；建议 Lead commit 时一并处理或派生工位）。

**4. 下游推论**：
- genome 与 #41 配套实装（hooks/team-topology/skill）现一致——批准前 genome stale 的 069 层面3 代价已消除。
- 后续所有读 dispatch-dag.yaml 的工位（ceremony_scan、子蜂群作者）将获得 (c) 任务-DAG 递归声明，与 Stop-Guard (c)spawn mandate 同源指向四类节点 sub-swarm-ceremony。
- genealogist 应将 `(c)-task-dag-recursion-20260623` 落为 settled 谱系条目（现为三处 genealogy_ref 的前向引用 token，尚无对应 settled 文件——见影响声明）。
- 135号 stale comment 的处置是新的 micro-gap（边界条件2）。

**5. 谱系引用**：本领域（蜂群递归机制载体从 team 拓扑→任务拓扑）有明确谱系生成史与概念扬弃——095号（TeamCreate 子蜂群模型，被 (c) 扬弃机制）→ 097号（五特征四类节点 DAG 模板，保留）→ (c)裁决-20260623（载体提升为任务拓扑，扬弃=否定 TeamCreate + 保留真递归默认 + 提升载体）。069号层面3（结构修改走提案模式）+ 矛盾2（genome 修改走提案/批准）是本应用的合规路径依据。275号（局部依赖——全局 DAG 从局部 TaskCreate 涌现）。**谱系存在性确认**：`(c)-task-dag-recursion-20260623` 作为 token 已被三处 genealogy_ref 引用，但 `.chanlun/genealogy/settled/` 下**尚无**同名 settled 文件——这是前向引用，需 genealogist 补写（不在本任务 genome-only scope，列影响声明）。

**6. 影响声明**：直接改 **1 文件**——`.chanlun/dispatch-dag.yaml`（顶层 genealogy_ref 追加 token；fractal_template.genealogy_ref 追加 token + description 重写；fractal_template.recursion_rules 6→7 项重写 + 2 行注释）。新建 **1 子任务** #54（异质审计节点，codex-challenger，blockedBy #39）。落盘本报告 + （#54 将产出 codex-review-prop002-apply artifact）。
**未碰**：permission/security/CLAUDE.md/settings.json；hooks；team-topology.json；scripts/src/tests；dispatch-dag.yaml 的其余所有 section（仅三处 PROP-002 变更）；**135号 stale comment 行650**（PROP-002 未授权，不夹带）；PROP-002 提案文件 status 字段（避免夹带提案改动）。
**Commit 责任**：genome 改动由 **Lead commit**（本工位不 commit，team-lead 指令）。建议 commit message 标注谱系意义（git-workflow 012号：rebase 保留生成史）。

---

## 四、no-workaround 声明

PROP-002 与当前 dispatch-dag 结构**无不可调和冲突**——(c)裁决已扬弃 TeamCreate 模型，三处变更是纯替换/追加，YAML 结构兼容，消费者（ceremony_scan）解析不受影响。135号 stale comment 残留是**文档层不一致**（非 recursion_rules 规则本体的定义冲突），不触发 no-workaround（无需绕过概念矛盾，无需 escalate）——但作为边界条件2 显式记录，交 #54 异质审计 + Lead/编排者判定是否补提案。本任务在 020 批准的精确 scope 内忠实落码，无绕过。
