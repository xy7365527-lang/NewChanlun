# recursive-bsp D1/D2 命题4 嵌套构成范式诊断（经异质审计修正）

> 子蜂群 recursive-bsp。2026-06-23。read-only 诊断，未改代码。诊断对象 main HEAD 7200500b09。
> 子工位 D1（L0 架构归类）+ D2（消解检验+张力）+ HETERO-D（codex-challenger 异质审计，**推翻 D2 初判核心断言**）。整合 sibling short-cover-diag 移交的 GC/BTC 逐笔 L3。
> 注：本文件是抢救性持久化（tmp/recursive-bsp/audit_synthesis.md 随 worktree 清理丢失）。第一轮（完整消费矩阵+命题1/2/3）摘要见末尾附录。

## 0. 编排者重定核心：命题4 嵌套构成范式
缠论核心定理（区间套）：最高级别走势类型内部一切元素 = 次级别 type1 买卖点**首尾相连**构成；有些次级别买卖点同时是最高级别买卖点（级别共振重合点）。
- 推论1：级别不是并列独立腿，是嵌套构成。556 顶层冻结根因=误把级别当独立腿。
- 推论2：吃所有级别涨跌幅 = a0 每个 type1 买卖点首尾相连操作（买点平空做多/卖点平多做空）；高级别涨跌幅=低级别买卖点间涨跌幅之和自动涌现，不等完成。
- 推论3：最高级别三类买卖点 = 次级别买卖点重合点（区间套定位）。
核心对照 = **嵌套构成（a0 首尾相连自动吃所有级别）vs 独立腿（单核心锚/读法B，556 否证）**。

## 1. D1 — L0 架构归类判决（稳固 L0，异质审计未挑战）

**当前 main rec_engine.rs + t_engine.rs = 决定性独立腿范式（单核心锚 highest_active=cc），不是嵌套构成。**

| 独立腿特征 | 命中证据 (file:line) | 嵌套构成特征 | 命中 |
|---|---|---|---|
| 单一核心锚 highest_active(cc) | rec_engine.rs:418-420（逆序返回唯一 Option<usize>） | a0 平权首尾相连无 cc | 0 |
| 单祖先链 nearest_active_parent | :423-425 `(j+1..).find(active)` | a0 无"父"概念 | 0 |
| sink/recover 借仓下放/升回 | sink:613（父减1/3+子开flip短差）/ recover:655（短差全平升回父） | a0 独立操作链 | 0 |
| ascend relabel 升级单核心(敞口不变) | :574-590 prove_relabel_invariant:587 | 新建独立腿(改敞口) | 0 |
| trend_done 等核心走势完成 | :865-878 只读 t1sell[cc]/t1buy[cc] | a0 层 t1 自动涌现 | 0 |

**a0 type1 点 = 不首尾相连；type-盲汇入 cc 核心（M3 单级别完成）**：
- a0 BSP 经 route_bsp(:722) 先 nearest_active_parent(0)；只要更高级别 active，走 Some(p) 分支(:725-754)，注释明文"区间套约束，**永不独立翻转**"。该分支只产 sink/drain/recover——**无一是"平空做多"或"平多做空"翻转**；a0 方向由父级 pdir + is_buy 决定为减仓/恢复信号，**a0 自身 BSP 不决定自己持仓方向**。独立 enter/flip 唯一在 None 分支(:756-780 核心级，无活跃祖先)。
- route_bsp **完全 type-盲**（签名只收 is_buy，无 type 参数；type1/2/3 在 rec_stream.rs:288-296 按 bk%2 坍缩为 buy/sell）。
- type1 唯一专属用途 = trend_done_clear(:865-878 读 [cc] 不是 [0]，清现金，避 545 做空陷阱)，**非 a0 翻转**。
- 核心是单一 cc 锚：三阶段会计挂单一 TRoot（per-instance 三阶段=递归自创已删，:237-244）。一个 campaign = 一个核心锚生命周期。
- rec≡flat bit-exact（九函数逐字/同构，注释多处"= flat"；唯一差异=索引空间偏移+EPS 已对齐 1e-12）。

⟹ **坐实编排者推论1**：当前确实误把级别当独立腿（单核心锚 + sink/recover），不是 a0 首尾相连嵌套构成。

## 2. ★命题4 内部张力（最深发现，异质审计揭示，供编排者裁——选择类）

命题4 推论2 与推论3 是**两种不可同时字面成立的读法**：
- **读法甲（推论2：全 a0 穷尽）**：操作**每个** a0 type1 点首尾相连，顶层涨跌幅=a0 操作链之和自动涌现。**不需识别重合点**，但=机械 a0 翻转 → 收益 L3-未决（千刀凌迟，见 §4）。近邻已否证的 multiscale(2/8)/读法B(1/8)——但那些是独立腿+条件 cover，命题4 是 a0 强制 cover，**未测**。
- **读法乙（推论3：重合点选择）**：只操作"次级别买卖点中的最高级别重合点"（区间套定位）。但识别"哪个 a0 点是顶层重合点"**需要顶层走势结构 = 与 d_top[top] 等价的稀疏信息** → **假消解**（区间套自顶向下展开，仍需顶层锚）。

⟹ 命题4 须先裁取哪个读法（**选择/语法记录类，属编排者**）。两读法导出完全不同的实装与有效域。

## 3. D2 消解检验（异质审计修正：D2 初判"部分消解"过度乐观，撤回）

- **556 顶层冻结**：读法乙 = **假消解**（异质审计攻击面4：重合点识别前提集 ⊇ {顶层走势完成已知}，与 d_top[top] 等价稀疏——换名非消解。要正确区分"a0 点是顶层重合点 vs 震荡盲段点"需顶层走势结构=同等稀疏信息）；读法甲 = 绕过冻结但收益 L3-未决。**非"部分消解"**。
- **555 第二层（消费级别⊥贯通级别）**：读法甲架构上消解（消费=贯通=a0 单级别，正交性定义消失）；但**架构正交消解 ≠ 收益消解**（异质审计：557 PARTIAL 是反例，理论涌现 L0 ≠ 经验涌现 L3）。

## 4. D2 a0 首尾相连 vs 539/命题1 张力（异质审计推翻 D2 否证——范畴错误）

- **D2 的 GC L3 否证无效（范畴错误，异质审计攻击面1）**：D2 用**当前架构**（条件 sink/recover：recover 需 j 预存反父向短差 ∧ 父同向买点，否则 no-op；深层 cover 率 lad6/7=0%）的 L3，否证**命题4 提议架构**（a0 每个买点**无条件强制** cover）的"短命空头"假设。深层 cover=0% 是条件触发架构固有特性（rec_engine.rs:745-752），命题4 强制首尾相连无 no-op 分支、无此坍缩通道。D2"短命/死扣同一对象不可分"声明**撤回**——是不同架构的不同行为。
  - recover 腿数据重解：长持 recover(491笔/−1037)=当前架构延迟 cover，非命题4；**短持 recover(136笔/+50 微盈)** 更接近命题4 强制即时 cover 行为。
- **改判：命题4 vs 539 = L3-未决**（非否证非消解）。命题4 架构下 539 **改形态**（长持死扣 → 高频短持小亏，L0 推论）。**千刀凌迟**（高频小亏在多年牛市累积 vs 多头腿净覆盖，GC 多头腿 +11218 反而赚）= 候选残余失败 = **L3 开放问题**，当前数据不能答。
- **确认侧普适/消费侧 regime 分离 = 否证**（异质审计攻击面3）：type1 识别本身是 regime 函数（556 顶层 type1 跨年稀疏=确认侧 regime；识别 a0 重合点需顶层结构=regime 依赖）。分离声明违反 formalization-validity-domain（L0 代数成立 ≠ L3 经验成立）。

## 5. 净判决（命题4，经异质审计）
1. **L0 事实（稳固）**：当前=独立腿；命题4 嵌套构成**从未忠实实装/回测**（最近邻 556 读法B=独立腿+条件 cover，L3=1/8 否证；不是命题4）。
2. **命题4 内部须先裁读法甲/乙**（编排者，选择类）——决定是否需顶层结构、是否假消解。
3. **消解 556**：读法乙=假消解（L0 逻辑等价）；读法甲=绕过但 L3-未决。
4. **vs 539**：L3-未决（D2 否证范畴错误已撤回），千刀凌迟为候选残余。
5. **唯一解析路径**：忠实实装命题4 架构（删单核心 cc 锚 + 条件 sink/recover；a0 每 type1 买点**强制** cover 做多 / 卖点**强制** close 做空）+ L3 8标的×3。这是回答 L3-未决的唯一方式。属实装，超 read-only 诊断。建议谱系 558 depends_on 540/537/556/539。

## 6. 六要素结果包
1. **结论**：当前 recursive_t（rec+flat 双引擎）是独立腿范式（单核心锚 cc + sink/recover），a0 type1 点不首尾相连（type-盲汇入核心，永不独立翻转）；命题4 嵌套构成从未忠实实装。命题4 含内部读法甲/乙张力；消解 556=假消解(乙)/L3未决(甲)；vs 539=L3未决（异质审计推翻 D2 范畴错误否证）。
2. **定义依据**：缠论区间套定理（最高级别=次级别 type1 首尾相连构成）；第65课 aₙ=f(aₙ₋₁)；type1=走势终完美（divergence judge_divergence）。代码特征：route_bsp type-盲 + nearest_active_parent 单核心 + trend_done_clear@[cc] 满足"独立腿"定义条件，不满足"a0 首尾相连"。
3. **边界条件（结论翻转处）**：(a) 若存在无需顶层走势完成即可识别 a0 重合点的算法 → 读法乙假消解判决翻转（异质审计 HETERO 逻辑链断裂）；(b) 若命题4 架构忠实实装后 L3 证 a0 强制 cover 使空头短命且高频小亏被多头净覆盖 → vs 539 改判消解；(c) 若原文溯源证 type1 识别全 regime 精度一致 → 确认侧普适获原文依据。
4. **下游推论**：命题4 验证须实装新架构（删 cc 锚+条件 sink/recover）；当前独立腿是 556/547/545 已记录死锁/做空亏根因的结构来源；命题4 读法选择决定整个实装方向。
5. **谱系引用**：556（顶层冻结，命题4 消解目标）、555第二层（消费⊥贯通）、539（做空失血，vs 命题4 L3-未决）、540/537（同一-差异双重性根：candidate压缩/confirmed展开不可坍缩）、547（cascade 级别错配=单核心源）、545（方向锚=type2 flip 陷阱）、552（anchor 5/8）。memory：project_interval_nesting_forward（推论3 区间套定位）、project_t_short_leg_regime_function、project_deadlock_dual_open_target。
6. **影响声明**：read-only 未改代码。为编排者提供命题4 重定核心的 L0 事实（当前=独立腿）+ 内部读法张力 + 消解/539 的诚实 L3-未决判定 + 唯一解析路径（实装+回测）。

## 7. 认识论等级
- §1 L0 归类 = **L0**（源码结构，异质审计未挑战）。
- §2 内部读法张力 = **L0**（两读法逻辑分析）。
- §3 消解 = **L0**（假消解=逻辑等价；读法甲 L3-未决）。
- §4 vs 539 = **L3-未决**（D2 的 L2 否证因范畴错误失效；命题4 全 a0 变体从未回测；short-cover-diag GC 逐笔=当前架构 L3，不代表命题4 架构）。
- **异质审计降级声明（诚实）**：Codex API 429 → codex-challenger 同质降级 adversarial 执行（531 先例），异质验证弱化（审计层 Gap，094）。核心攻击（范畴错误）可独立从代码结构（rec_engine.rs:745-752 条件 recover+no-op vs 命题4 无条件 cover）核实，不依赖异质源强度。建议 team-lead 路由 gemini-challenger 做真异质交叉。

## 8. 开放项（四分法，供 team-lead/编排者路由）
| 项 | 四分法 | 处置 |
|---|---|---|
| 命题4 读法甲（全 a0 穷尽）vs 读法乙（重合点选择） | **选择**（编排者命题4 意图） | **本轮最关键 escalate 候选**——决定是否需顶层结构、是否假消解 |
| 命题4 vs 539 L3-未决 | **行动**（须实装+回测） | 忠实实装命题4 架构 + L3 8×3，唯一解析路径（谱系 558） |
| 确认侧 type1 识别是否 regime | 语法记录候选 | 接 556（顶层 type1 稀疏=确认侧 regime 已 L3） |

no-workaround：命题4 内部读法张力已精确描述未绕过（属编排者命题4 意图，选择类，非诊断工位自决）。

## 9. sibling combo-l3 交叉验证（2026-06-23，commit 76fd956474）+ 范畴区分
combo-l3（ANCHOR+top-gate 组合 L3）确认：(a) 我的 type2@cc flip 危险预警在 combo 下已正确隔离（top_short_gate 在 route_bsp **之前**拦截反核心向 cc BSP 含 type2，走 top_gate_hedge+return，本 bar route_bsp 整段跳过；top_gate_hedge 非 flip，anchor 底仓死扣 Long 原地保留，ES/OKLO clears=0 时 COMBO 逐字=ANCHOR 证底仓从未被清）；(b) 其 L3 与我的根因收敛——combo 是少数显式 type2 建空实装，但收益仍 regime（GC −210pp/BRN 穿仓），556 稀疏+539 regime 不可约，判决 PARTIAL+否证。
**★范畴区分（防 team-lead 综合时混淆）**：combo-l3 的 COMBO 仍是**独立腿架构**（单核心 anchor + 机动 top-gate hedge），故其 type2 建空否证是**又一个独立腿架构数据点**，**不关闭**本文件 §4 的「命题4 vs 539 = L3-未决」——命题4 的 a0 嵌套构成强制 cover 架构与 combo 的 anchor+top-gate 独立腿是不同架构（同 §4 异质审计攻击面1 的迁移性逻辑）。combo 否证强化"独立腿 type2 建空=regime-bound"，但不等于"命题4 嵌套构成=否证"（后者仍须忠实实装+回测，唯一解析路径）。

---

## 附录：第一轮摘要（完整递归三类消费矩阵 + 命题1/2/3，抢救自丢失的 audit_synthesis.md §1-8）
- **完整消费矩阵 L0**：两引擎视图层只有 buy/sell/t1buy/t1sell 四旗，无 t2/t3 字段。type2/3 在 collapse 点（rec_stream.rs:293/295、stream.rs:259/263）合并进 buy/sell；route_bsp type-盲分流。唯一 type-aware=核心级 type1→trend_done_clear。⟹「每级别三类都用上」结构性不成立。
- **统一根因（两机制+一推论）**：M1 信号折叠（type2/3→通用 buy/sell）⊥ M3 单级别完成（只读 t1*[cc]，子级完成忽略）+ M2（type2/3 不能闭合 campaign=M1 推论）。M3=556"消费级别⊥贯通级别"+cascade"次级别翻主力"的 L0 机制根因。
- **★type2@cc flip 危险**：main 中 type1 卖@cc→trend_done_clear（清现金，受保护）；type2 卖@cc→flip（清+立即做空=545 陷阱，无 pending_flip 保护，仅 worktree 有）。命题3 部署 type2 到 main 前须补保护。
- **命题1**：完整翻空结构可达（route_bsp 核心反向无门），败于 539 否定线；严格形式=trend_done_clear 清现金（代码 L0）/ 缠论语义"卖点=退出非做空"=L1（需原文溯源）。anchor（仅分支）=治标。
- **命题2**：consume 对偶存在（recover=子级买点平空升回）但 type-盲+级别坍缩+buy_no_recover 计数器坐实欠触发。逐笔 L3（short-cover-diag GC）：clean cover 627笔/−987（2.9%）vs 死扣 2188笔/−32900（97.1%，hold 88×）。
- **命题3**：main 顶层只消费 type1-完成-清仓，三类不完整；type2@L4=36>type1=30>type3≈0（557）；type3 稀疏=尺度性质买卖对称。
- **谱系根接通**：540 §四已结算信号层三类并完备（升跌完备性 93-100%）；我的 M1/M3=540"不可坍缩"根在**操作层**的新实例（540 现有实例是信号层 v5）。残缺=操作层缺陷非信号层。
