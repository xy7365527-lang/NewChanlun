# v96-swarm session
date: 2026-02-28
swarm: v96-swarm
head_start: 95feeb8
settled_start: 237
mode: manual_dispatch（编排者指导：方向性力度优先 + 多TF + fiber-context 并行）

## 触发

编排者消息（两条）：

第一条（v96 方向）：
- 236号 45% polarity 差异 = "直积近似在策略层面失效"
- 237号闭合 ker(D)->力度盲点："缠论传统上用振幅衡量背驰力度，但振幅 ∈ ker(D)"
- **方向性力度优先于多 TF**：理论解优于工程解
- "既然你知道 D 吸收什么、保留什么、放大什么，那就用被保留/放大的量来定义力度"

第二条（验证方向修正）：
- "你不知道谁对"——236号是差异度量，不是正确性判断
- 裁决标准：回测。"不测而测"——缠论通过完全分类产生确定操作，操作结果区分分类好坏
- v97 方向预告：修共振筛选管道，两种组装方式策略对比回测

## 工位

| 工位 | 状态 | 产出 |
|------|------|------|
| directional-force | 完成 | 239号谱系 + directional_force.py + 33测试 + comparison脚本 |
| multi-tf | 完成 | 238号谱系 + multi_tf_adapter.py + 25测试 + prototype脚本 |
| fiber-context | 完成 | 240号谱系 + FiberTradingContext/FiberSignalFilter扩展 + 18测试 + backtest脚本 |

## 产出汇总

### 239号：方向性力度——不依赖幅度的走势强度度量（∉ ker(D)）

**核心（编排者优先）**：

三种方向性力度方法，均 ∉ ker(D)：

| 方法 | 名称 | 信息来源 | 三态位置 |
|------|------|---------|---------|
| A | directional_persistence | 同向线段比例 + 最长连续同向 | preserved |
| B | stroke_density_force | 笔密度（反转频率） | preserved |
| C | zhongshu_drift_force | 中枢 [ZD,ZG] 偏移方向 | preserved |

加高级别 Move-as-component 适配：`level_directional_force()` 直接替代 `_amplitude_force()`。

**代码**：
- `src/newchan/topology/directional_force.py`（1000行，三种方法 + 复合力度 + 高级别适配 + 对比工具）
- `tests/test_topology/test_directional_force.py`（33测试）
- `scripts/directional_force_comparison.py`（SPY 日线对比）

### 238号：多 TF 输入架构——绕过 D2 递归压缩瓶颈

**核心**：MultiTFOrchestrator——每个 TF 独立运行 BiEngine，跨 TF 背驰检测替代单 TF 递归。

**代码**：
- `src/newchan/topology/multi_tf_adapter.py`（adapter 模式，不修改 pipeline）
- `tests/test_multi_tf_adapter.py`（25测试）
- `scripts/multi_tf_prototype.py`（SPY 日线+周线原型）

### 240号：纤维丛上下文集成——FiberTradingContext 接入策略层

**核心**：FiberTradingContext 包装 TradingContext + 纤维丛修正。FiberSignalFilter 提供 polarity 覆盖决策。

**代码**：
- `src/newchan/topology/fiber_pipeline_adapter.py`（扩展：FiberTradingContext + FiberSignalFilter）
- `tests/test_topology/test_fiber_pipeline_adapter.py`（扩展：+18测试）
- `scripts/fiber_backtest_comparison.py`（回测对比脚本）

## 测试结果

2996 passed, 15 skipped, 0 failures（+77 新测试）

## 谱系变化

- **238号**（新增）：多 TF 输入架构——工程架构
- **239号**（新增）：方向性力度——概念发现 + 工程实现
- **240号**（新增）：纤维丛上下文集成——工程集成

## 编排者方向（v97 预告）

编排者在 v96 进行中重新校准了验证路径：
1. 236号 45% 差异的裁决：回测（不测而测）
2. 前提：修共振筛选管道（_build_resonance_signals 双层简化问题）
3. 然后：两种组装方式策略对比回测

## 下一步

1. 修共振筛选管道——`_build_resonance_signals` 的双层简化绕过问题
2. 两种组装方式（直积 vs 纤维丛）策略对比回测
3. 239号方向性力度的实证验证——在真实数据上对比振幅力度 vs 方向性力度的背驰判定差异
