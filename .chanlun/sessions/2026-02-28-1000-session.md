# v100-swarm session
date: 2026-02-28
swarm: v100-swarm
head_start: 5fa793d
settled_start: 245

## 编排者方向

v100 方向：多 TF 集成。238号组件就绪，237号诊断明确。打通后解锁 T6 可达性 + 方向性力度效用 + 共振筛选真实化。

编排者判断：v90→v99 的 K4→纤维丛→共振裁决线已饱和（需要真实策略运行积累，不是系统能自动给出的），换线到多 TF 集成。

## 工位

### multi-tf-integration（完成）

实现 `MultiTFPipelineAdapter`，将 238号 `MultiTFOrchestrator` 输出接入 pipeline.py 交易管道。

核心产出：
- `src/newchan/topology/multi_tf_pipeline.py`：桥梁层
  - `buysellpoint_to_bsp()`：多TF BuySellPoint → nesting BSP
  - `cross_divergence_to_resonance_signal()`：CrossLevelDivergence → ResonanceSignal
  - `detect_cross_level_divergence_directional()`：方向性力度替代振幅力度
  - `_directional_move_force()`：中枢密度倒数 + 线段方向持续性
  - `MultiTFPipelineAdapter`：集成编排器
  - `MultiTFPipelineResult`：聚合结果
- `tests/test_multi_tf_pipeline.py`：测试

关键设计决策：
1. **adapter 模式**：不修改 pipeline.py / multi_tf_adapter.py / directional_force.py
2. **方向性力度替代**：∉ ker(D)，解决 237号"振幅力度 ∈ ker(D)"诊断
3. **T6 可达性**：recursive_levels_equivalent = 有走势输出的 TF 层数。>= 2 时 T6 可达
4. **类型转换**：BuySellPoint→BSP、CrossLevelDivergence→ResonanceSignal，直接兼容 pipeline 入口

## 谱系产出

| 编号 | 标题 | 类型 | 层级 | depends_on |
|------|------|------|------|------------|
| 246 | 多TF pipeline集成——238号架构接入交易管道 + 239号方向性力度替代 | 工程架构 | L2 | [238, 237, 239] |

## 测试

待确认（工位运行了测试但需要 commit 后在 rescan 中验证）

## v90→v99 弧线总结（编排者归纳）

- 纤维丛结构（232）、ker(D)（233）、三态分类（235）——理论工具
- 45% 差异（236）→ 100% 锁定在 C-R 分歧（245）——经验定位
- 公平回测平局（244）——排除了一种裁决路径
- 剩下的裁决只能来自真实策略运行中的积累

## v100 产出

- 246号：多 TF pipeline 集成，T6 从"结构性不可达"变为"有条件可达"
- 打通了 238→pipeline 的最后一公里
- 方向性力度（239号）在多 TF 上下文中激活
