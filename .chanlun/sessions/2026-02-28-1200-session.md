# v102-swarm session

date: 2026-02-28
type: diagnosis
swarm: v102-swarm
workstations: segment-diagnosis, gemini-diagnosis, codex-diagnosis

## 编排者方向

"D2 段引擎是'坏了'还是'对的'？v102 方向：诊断，不是修复。"
"用gemini和codex严格诊断"

## 工位产出

### segment-diagnosis（Claude）

- `scripts/segment_engine_diagnosis.py`：可重复运行的诊断脚本
- `tmp/segment-engine-diagnosis.json`：完整 71 笔序列 + 特征序列分析 + 6 次触发拒绝记录 + 对比实验
- `.chanlun/genealogy/settled/248-segment-engine-diagnosis.md`：诊断谱系

**结论**：BUG。`a_segment_v1.py:segments_from_strokes_v1` 第 488 行。引擎用 `strokes[seg_start].direction` 作为初始段方向。当第一笔方向为 down 且位于绝对底部时，引擎承诺构建 down 段，但后续市场持续上涨，L78 约束正确拒绝每个触发，但引擎无回退机制。对比实验：strokes[1:]（up 方向）→ 70 笔产出 7 段。

### gemini-diagnosis（Gemini 2.5 Pro）

- `scripts/gemini_segment_diagnosis.py`：API 调用脚本
- `tmp/gemini-segment-diagnosis.md`：完整推理链

**结论**：BUG。主因：结算锚前置验证将"新线段破坏旧线段"定理当作操作规程的前置条件。第 67 课的线段划分标准是独立的操作规程，只依赖特征序列分型。L78 前置验证也过严。修复建议：移除结算锚前置验证 + L78 改后置。

### codex-diagnosis（OpenAI o3）

- `scripts/codex_segment_diagnosis.py`：API 调用脚本
- `tmp/codex-segment-diagnosis.md`：完整推理链

**结论**：BUG（3 处实现错误）。(1) `_apply_inclusion()` 包含处理方向写反（up 时 low 取 max 应取 min）；(2) `dir_state` 初始化反转；(3) L78 前置检查过于激进。修复建议：修正包含逻辑 + dir_state 对调 + L78 改后置。

## 三模型共识

| 维度 | Claude | Gemini | Codex/o3 |
|------|--------|--------|----------|
| 判定 | BUG | BUG | BUG |
| L78 前置验证 | 过严（主因） | 过严（建议后置） | 过严（建议后置） |
| 结算锚 | — | 主因（定理当前置） | — |
| 包含处理方向 | — | — | 写反（3 处） |
| 初始方向回退 | 缺失（主因） | — | — |
| **交叉点** | **L78 前置验证过严** | **L78 前置验证过严** | **L78 前置验证过严** |

## 谱系产出

- 248号：段引擎 71→1 压缩诊断（type: 诊断, depends_on: [247, 237, 003]）

## 编排者后续方向

逐个修复、逐个验证。顺序：L78 前置验证 → 包含处理方向 → 结算锚。每步跑全量测试。
