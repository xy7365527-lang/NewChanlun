# 拓扑决策记录（topology-manager 工位产出）
# 格式：每条记录含 id / timestamp / type / trigger_signal / workstations / reasoning / verdict
# 消费者：meta-observer、人类审计（CLAUDE.md 原则3：所有产出必须可质询）
# 谱系依据：562号（结构工位机制强制）、lead-parallel-dispatch（局部依赖原则）
# 注：001-003 由实例A 产出；004-005 由实例B 追加（含 Grep 裁定 + 并发事故记录）

decisions:
  - id: TOPO-20260623-001
    timestamp: "2026-06-23"
    type: 监控报告
    trigger_signal: Stop-Guard 路由 task 24（rec_engine.rs 主树/worktree 隔离碰撞监控）
    workstations: [prop4-nest（主树）, prop4-bidir（worktree /tmp/prop4-diag-wt）]
    reasoning: >
      git worktree 隔离生效。/tmp/prop4-diag-wt/.git 指向 .git/worktrees/prop4-diag-wt，
      worktree HEAD=f11e7f8231（detached），主树 HEAD=adc39bc17a。两者不同 checkout，
      index/工作树物理隔离，无残留主树竞争。诊断到实质 divergence（非碰撞，而是概念分离）：
      两处 rec_engine.rs 的 enable_nest_consume 承载两种 consume 语义——
      主树=次级别反核心向背驰段 ⇒ 平核心仓 1/3（减仓口径，无 557 引用）；
      worktree=持仓中反向买卖点(type1∨2∨3) ⇒ full cover + 反向 enter（整仓翻转口径，引用 557 对偶 3 次）。
    verdict: 隔离生效，无碰撞。divergence 是概念分离（吃跌两失败模式分离主线的体现），非编辑竞争。

  - id: TOPO-20260623-002
    timestamp: "2026-06-23"
    type: 合并预案
    trigger_signal: task 25（prop4-bidir worktree L3 完成后如何合并主树 baseline）
    workstations: [prop4-bidir（worktree）, prop4-nest（主树 baseline）]
    reasoning: >
      worktree reflog 只有一条 detach 记录（0000→f11e7f8231），HEAD 从未移动，L3 工作尚未 commit。
      两 checkout 的 consume 语义已 divergence。直接 rebase/merge 会在 enable_nest_consume
      语义处产生概念级冲突（非文本冲突）——整仓翻转口径 vs 1/3 减仓口径不可机械合并。
    verdict: >
      建议给 Lead（待 worktree L3 完成 commit 后触发）：不用 rebase/merge（概念级冲突）。
      用 cherry-pick + 独立 branch：(1) worktree 先 commit L3 到独立 branch prop4-bidir-consume；
      (2) 由 Lead/编排者裁决两 consume 口径是否结算（尚未结算 ⇒ 并存对照 L3）；
      (3) 若结算为单口径，cherry-pick 存活口径到主树。触发条件：worktree reflog 出现新 commit。

  - id: TOPO-20260623-003
    timestamp: "2026-06-23"
    type: 收缩评估
    trigger_signal: task 26（~13 成员 stale/duplicate 检测）
    workstations: [全 ~13 活跃成员]
    reasoning: >
      按收缩触发信号（概念结算/职责空置/职责收敛）逐项评估：
      (1) 概念结算：consume 两口径分离尚未结算（L3 进行中）⇒ prop4-bidir 与 prop4-nest 均须存活，
          撤销任一 = 压扁概念分离。
      (2) 职责空置：常设结构工位按定义常驻，不因单轮空置 shutdown。
      (3) 职责收敛：gemini-558b（异质质询）⊥ code-verifier（代码验证）职责正交；genealogist 单工位承载多谱系。
    verdict: >
      建议给 Lead：当前无可 shutdown 业务工位（consume 分离未结算）。结构工位常设不撤销。
      唯一 L3 后复审项：consume 口径结算后被否定口径工位可 shutdown。
      边界声明：基于可观测信号，无各成员实时心跳；若 Lead 有 idle 心跳以 Lead 观测为准。

  - id: TOPO-20260623-004
    timestamp: "2026-06-23"
    type: 扩张
    trigger_signal: 562号机制强制（检查1.5）+ team-topology.json 缺 meta-lead/quality-guard
    workstations: [meta-lead, quality-guard]
    reasoning: >
      team-topology.json 定义 6 个 auto_spawn 结构工位，本 session 缺 meta-lead + quality-guard。
      562号（2026-06-23）已将 bootstrap 从文本提示升格为机制强制（ceremony-completion-guard.sh
      检查1.5）：缺任一结构 agentType → Stop-Guard block + 路由 spawn。
    verdict: >
      分类=定理（562号已结算原则的逻辑必然推论，非选择）。建议 Lead 立即并行 spawn
      meta-lead + quality-guard。否则 Stop-Guard 在停机判定时 block。

  - id: TOPO-20260623-005
    timestamp: "2026-06-23"
    type: 元事件（并发duplicate + 覆盖事故 + 判断分歧裁定）
    trigger_signal: 落盘时发现 decisions.yaml 已被另一并发 topology-manager 实例创建
    workstations: [topology-manager（实例A）, topology-manager（实例B）]
    reasoning: >
      两个 topology-manager 实例并发运行（duplicate 工位）。实例B 落盘覆盖了实例A 的 001/002/003
      （已 transcript 抢救合并）。两实例对 consume 给出对立判断：实例A=概念分离 / 实例B=重复。
      Grep 决定性裁定：主树 rec_engine.rs 行70/432/1204 = 背驰段定位平核心仓1/3（无557）；
      worktree 行70-72/1021-1026 = 反向买卖点full cover+反向enter（引用557×3，注释显式"区分脚手架背驰段平1/3"）。
      ⇒ 实例A 对（概念分离两端），实例B 错（非重复）。实例B 另修正实例A 一处事实错误：
      任务18 是未提交（git status M），非已 commit adc39bc17a（562号 docs commit 不含代码）。
    verdict: >
      建议给 Lead：(1) topology-manager 收缩为单一实例（duplicate 是本次覆盖事故根因）；
      (2) prop4-bidir 不可收缩（承载 consume 整仓翻转口径，概念分离一端）；
      (3) consume 口径分离尚未结算，呈请编排者裁决（本工位不判断定义对错，仅标记分离）。
      并发异质性的价值：交叉验证纠正了实例B 的重复误判；代价：覆盖事故。

  - id: TOPO-20260623-006
    timestamp: "2026-06-23T03:00:00+08:00"
    type: 碰撞监控（增量）
    trigger_signal: Stop-Guard 路由"同步处理" + #29 route_bsp 隔离worktree 实装启动
    workstations: [prop4-nest, prop4-bidir, genealogist]
    reasoning: >
      自上次快照(adc39bc17a)的增量变化：
      (1) 主树任务18 读法B 已 commit（HEAD adc39bc17a→3b8259ae5b，rec_engine.rs/divergence.rs
          离开未提交列表）——修正 TOPO-001 的"任务18未提交"状态。
      (2) #29 "route_bsp 二元→轨道映射（隔离worktree）"未观测到新建独立 worktree
          （worktree list 仅 /private/tmp/prop4-diag-wt 一个非-claude worktree，/tmp 下无新 route/orbit 目录）。
          consume worktree rec_stream.rs 从配套增长到 +247——疑似 route_bsp 工作混入 consume worktree。
      (3) 谱系 562 编号碰撞：562-bootstrap-structural-enforce.md（已commit）与
          562-eating-down-piercing-vs-missing-rally-dilemma.md（untracked新建）共用562号。
    verdict: >
      建议给 Lead：(1) 向 prop4-bidir 确认 #29 是否新建独立 worktree——若复用 consume worktree
      则 consume L3 + route_bsp 轨道映射两概念工作混载，commit/merge 时无法分离（声明-执行gap）；
      (2) 562 编号碰撞交 genealogist 处置（非拓扑职责，标记上报）。
      碰撞监控核心结论不变：worktree 物理隔离机制本身有效。

  - id: TOPO-20260623-007
    timestamp: "2026-06-23T03:05:00+08:00"
    type: 碰撞监控（增量，事件驱动：#29 完成）
    trigger_signal: 队列 2→1，#29 prop4-bidir route_bsp 完成
    workstations: [prop4-nest, prop4-bidir, route_bsp(agent-worktree)]
    reasoning: >
      #29 完成后的拓扑状态：
      (1) 隔离隐患否证（修正 TOPO-006 担忧）：#29 route_bsp CC orbit 工作 commit 到独立
          agent worktree（.claude/worktrees/agent-a6cdff06703f0de30，commit 30a6b55372
          "route_bsp CC orbit 升格 561号N9 极性协变 任务29"），未混入 consume worktree。隔离兑现；
          TOPO-006 的"rec_stream.rs +247 疑似 route_bsp 混入"实为 consume L3 自身工作。
      (2) consume L3 已 commit + 分支化：/private/tmp/prop4-diag-wt 从 detached(f11e7f8231)
          → 命名分支 prop4-bidir-consume-C-20260623（fde2d3fdd4 命题4读法乙双向 construct+consume L3），
          工作树干净 ⇒ TOPO-002 merge 预案触发条件（worktree 出现新 commit）已满足。
      (3) 三工作线各自隔离 commit：主树 b302e2114c / consume fde2d3fdd4 / route_bsp 30a6b55372。
          物理隔离全程有效，无碰撞。
      (4) 概念演化信号：#29 route_bsp CC orbit 用"走势完成type1/2/3驱动极性"统一触发器（替换背驰段），
          consume 两口径分离（TOPO-001）可能正被完全分类方案收敛。
    verdict: >
      建议给 Lead：(1) 撤回 TOPO-006 的 #29 混入担忧——隔离实际兑现（独立 agent worktree）；
      (2) merge 可推进：consume 在分支 prop4-bidir-consume-C-20260623，route_bsp 在 agent
          worktree(30a6b55372)，编排者裁决 consume 口径是否被 #29 完全分类方案收敛后 cherry-pick；
      (3) prop4-bidir 主要工作（consume L3 + route_bsp #29）均已 commit，若无 pipeline 后续可收缩。
      consume 口径收敛是概念判断，呈请编排者/genealogist，非拓扑职责。

  - id: TOPO-20260623-008
    timestamp: "2026-06-23T03:55:00+08:00"
    type: 碰撞监控（增量，事件驱动：新阶段 e1+friction 工位 spawn）
    trigger_signal: 队列换为 e1-experiment + friction-model；pending 元规则结算
    workstations: [e1-experiment, friction-model, prop4-diag-wt]
    reasoning: >
      (1) 死锁解除确认：topology-manager 路由 geneal-560 + escalate 的 2 个 pending 元规则已结算——
          check3-revival→564号(stopguard-check3-instruction-overflow)、ghost-owner→565号
          (ghost-owner-task-lifecycle)，移入 settled/。"不越界自结算、路由 genealogist"处理验证正确。
      (2) 新工位隔离：friction-model 用独立双 worktree（friction-bidir 基于 consume 分支 +
          friction-current detached 基于主树 d18b083a03，对比实验，合理隔离）。
      (3) 碰撞风险：/private/tmp/friction-bidir 与 /private/tmp/prop4-diag-wt 共享同一分支
          prop4-bidir-consume-C-20260623（均 fde2d3fdd4）。若 prop4-diag-wt（consume 工位）仍活跃，
          friction-bidir 在该分支 commit 会冲突。
      (4) e1-experiment 在主树编辑 instruments.py/backtest_*.py/data_cache(BTC+CL)，与 friction
          worktree 物理隔离，无碰撞。
      (5) 新 pending：team-topology-auto-spawn-gap（声明-实装不一致），与 TOPO-004 扩张建议
          (缺 meta-lead/quality-guard)同源。
    verdict: >
      建议给 Lead：(1) 确认 prop4-diag-wt 工位是否已 idle——若 consume L3 完成可释放该 worktree
      让 friction-bidir 独占 prop4-bidir-consume-C-20260623 分支；若两者都活跃需分支隔离（避免同分支 commit 冲突）；
      (2) team-topology-auto-spawn-gap 是选择类，呈编排者裁决（与扩张建议同源）。
      死锁解除：check3/ghost-owner 已结算为 564/565。

  - id: TOPO-20260623-009
    timestamp: "2026-06-23T04:10:00+08:00"
    type: 扩张/瓶颈/收缩复审（事件驱动：#37 完成，主矛盾重定位）
    trigger_signal: "#37 bull-bear-flip 完成（切换flip 态确认正确）+ #41 已分解为 4 节点 DAG"
    workstations: [route-bsp-complete(#36→#37→#38 串行), c-producer-mechanize(#41+子DAG), prop4-diag-wt(consume worktree)]
    reasoning: >
      (1) 瓶颈：route-bsp-complete 单工位串行承载 #36→#37→#38 整条吃跌 alpha 业务链。
          #38 in_progress 捆绑两单元（全仓会计实装 + 真穿仓/浮亏诊断，串行数据依赖可接受），
          但缺 sub-DAG/异质审计节点。#38 是 L3 实证（穿仓真伪判决携概念信息差），
          按 #41 spawn_mandate.exemption「L2+L3 实证类不可免除异质审计」⟹ #38 须有异质审计节点。
          #38 spawn 早于 #41 机制落地（#43 修复触发载体），未注入 template ⟹ 漏节点。
      (2) 扩张（概念分离）：#37 将主矛盾从「切换正确性」重定位到「牛市侧资本流失
          （cc_orbit churn + #36 add 税 destroys capital base before bear）」。这是未被任何 task
          承载的独立单元，与 #38（穿仓会计侧）正交（#37 报告 line109/112 自证）。候选新工位=牛市侧
          cc_orbit churn 减少。门控=编排者是否现在优先牛市侧 alpha 回收（选择类）。
      (3) 收缩复审：consume 两口径（整仓翻转 vs 1/3减仓）经 #37 确认为不同递归级别互补
          （最高级别走势完成翻主力 vs 次级别 sink 1/3，对齐 deadlock-dual-open 多空双开目标），
          非竞争待裁单口径 ⟹ 修正 TOPO-005「待结算后撤被否定口径」预期：两端并存非择一，
          均不可 shutdown。prop4-diag-wt 若 #38 不复用 ⟹ idle watch（非立即 shutdown，下轮复审）。
      (4) 健康形状确认：#41 已正确分解为 4 节点 DAG（#42/#43 任务 + #44 审查异工位 +
          #45 codex-challenger 异质审计 + #46 结晶）= sub-swarm-ceremony 正确形状。
          约束3：#44 审查节点 owner 须 ≠ c-producer-mechanize（执行不可自观）；
          约束4：#45 须 codex-challenger。#38 应参照此形状补审计节点。
    verdict: >
      建议给 Lead：
      (A) [定理] #38 补异质审计节点：L3 实证结论（真穿仓 vs 浮亏区分、全仓救穿仓 claim）
          crystallize 前须经异质审计（codex-challenger/gemini-challenger），否则=封闭自证循环（约束4）。
          分类=定理（#41 exemption 已结算 L3 不可免除），非选择——建议 Lead 直接补 blockedBy #38 的审计节点（参照 #45 模板）。
      (B) [选择] 牛市侧资本流失工位=扩张候选，门控编排者优先级，呈报不自决。
      (C) [行动] #44 审查节点须指派 ≠ c-producer-mechanize 的异工位（约束3）；#45 须 codex-challenger（约束4）。
      (D) [观测] prop4-diag-wt consume worktree 进入 idle watch，下轮复审是否释放。
      边界声明：基于文件系统+TaskList 可观测信号，无各成员实时心跳；若 Lead 有 idle 心跳以 Lead 观测为准。
  - id: TOPO-20260623-010
    timestamp: "2026-06-23T05:00:00+08:00"
    type: 收缩+扩张复审（compact 热启动后结构工位 roster 体检）
    trigger_signal: compact 热启动 + team-lead 委派（①原版结构工位死活检测 ②566/568/569/570 谱系编号异常）
    workstations: [meta-lead, genealogist, quality-guard, code-verifier, meta-observer, topology-manager（原版×6）, meta-lead-2/genealogist-2/quality-guard-2/code-verifier-2/meta-observer-2/topology-manager-2（fresh×6）]
    reasoning: >
      读 $HOME/.claude/teams/session-14c95478/config.json（本 Lead team，leadSessionId 匹配）实测 roster：
      (1) [收缩] 6 个原版名结构工位全部 isActive=false = compact 前残留死实例。机制双重盲区：
          check1.5 按 agentType 判存在（-2 版已提供同 agentType ⇒ 不触发 re-spawn）；check2.5 idle reaper
          按 name∈STRUCTURAL 排除原版名 ⇒ 永不被 flag。⇒ 机制无法自清，须 Lead 手动 shutdown 这 6 个死成员（职责收敛/duplicate）。
      (2) [扩张/功能缺口] meta-lead 与 quality-guard 两 agentType 的原版 AND -2 版均 isActive=false
          （meta-lead-2 / quality-guard-2 都 false）⇒ 这两个结构角色当前无活实例=功能性死亡。check1.5 不拦
          （只查 config 成员 agentType 存在性，不查 isActive 活性）⇒ 机制盲区。后果：无活 meta-lead=概念分离中断#1
          无路由器；无活 quality-guard=结果包六要素无守卫。
      (3) [机制 gap 观测] check1.5 存在性判据=config 成员 agentType 集合，不含 isActive ⇒ "结构工位存在但全部死亡"
          可通过 check1.5（声明-执行 gap，与 564/566 同模式）。交 meta-observer/genealogist 评估是否升语法记录（非拓扑职责）。
      (4) 谱系编号 566/568/569/570=已结算 meta-rule（settled/），非 block-topology 概念块；relations 覆盖块 531-548，
          meta-rule 本无块映射 ⇒ scan "异常" 是假阳性。属 genealogist 职责，非拓扑。
    verdict: >
      建议给 Lead（分类标注）：
      (A) [行动/定理] 手动 shutdown 6 个原版名死结构工位（isActive=false 残留，机制无法自清——check2.5 按 name 排除结构工位）。
          前置核验：以 Lead 实时心跳为准，确认无活 pty 后清理。
      (B) [行动/定理] re-spawn 活 meta-lead + quality-guard（两 agentType 当前无活实例，check1.5 盲区）。562号精神=结构工位须存在且活；
          当前仅"存在"未"活"⇒ 补活实例。前置核验：Lead 确认 meta-lead-2/quality-guard-2 确为死（非 transient turn 边界 isActive=false）。
      (C) [观测/上报] check1.5 存在性≠活性=机制盲区，呈 meta-observer/genealogist 评估语法记录。
      (D) [观测] 566/568/569/570 scan 异常=假阳性（meta-rule 无 block 映射），呈 genealogist。
      边界声明：基于 config.json isActive 快照（平台活性字段，比 owner 可靠但仍为快照）；若 Lead 实时心跳与此不符，以 Lead 观测为准。topology-manager 不执行变更，仅建议。

  - id: TOPO-20260629-011
    timestamp: "2026-06-30T01:12:02+08:00"
    type: 扩张评估（K_i 主线，648 实装后复审）
    trigger_signal: Lead 委派——K_i 实装是否暴露异质审查/子分解需求；646/648 结算后拓扑复审
    workstations: [k-i-subvoice, perf-fullwindow-verify, l2l3-fullwindow-rejudge]
    reasoning: >
      (1) [扩张否] task#14 K_i 已实装(commit 37d282abb0)：coverage.rs::extract_carrier_forest(行264)+
          pi_bsp_timing.rs host^op→K_i(方案D)，带 2 测试。无需新工位实装 K_i。
      (2) [异质审查否] dedup 危险(同 LeveledMove 作根+作子重复)已被 codex 异质审查 NO#2 命中并修
          (coverage.rs:255/272)。codex-challenger 已触达 K_i，不重复 spawn。
      (3) [收缩否+下游解锁] 646/648 均已结算待 /ritual(019c)。K_i 实装 ⟹ task#3 l2l3-fullwindow-rejudge
          前置满足(648 downstream)，行动类解锁非新工位。
      (4) [瓶颈观测] perf-fullwindow-verify 已实测 engine exp≈2.0 O(n²)残留(classifier增量塔+coverage extract/merge)，
          真修=task#16 ElementView 大重构(需 fresh session)，本轮不碰。
    verdict: >
      (A) [定理] 不为 K_i 实装新建工位(已落地)；不重复 spawn 异质审查(codex NO#2 已审已修)。
      (B) [行动] task#3 l2l3-fullwindow-rejudge 前置已满足(K_i 实装)，可由现有业务工位推进。
      (C) [边界] 翻转点=L2 实测若子声部仍=0 ⟹ 648 被否证回退 C；当前 OKLO 已激活(腿20≠0)，不回退。
      (D) [观测] session 68088c95 在 ~/.claude/teams 无对应 config，roster 活性以 Lead 实时心跳为准。

  - id: TOPO-20260629-012
    timestamp: "2026-06-30T02:00:00+08:00"
    type: 扩张评估（l2-fullwindow-8sym K_i 接入全窗 runner 评估）
    trigger_signal: Lead 委派——l2-fullwindow-8sym 若需把 K_i 接入全窗 runner（跨文件 ripple）是否需 architect/codex 工位或子分解
    workstations: [l2-fullwindow-8sym]
    reasoning: >
      关键拓扑事实（L2 源码实测）：K_i 视图（coverage.rs::extract_carrier_forest）当前只接入
      standalone probe bin pi_bsp_timing.rs（TOPO-011 commit 37d282abb0），**未接入全窗 runner 路径**。
      grep 实测 runner.rs + l3_fullwindow.rs：extract_carrier_forest / pi_bsp / subvoice / host^op / K_i
      **零引用**。l3_fullwindow.rs 跑 run_theta_v0（runner 路径），其诚实标（行23-26）显式声明
      『#5 多声部 depth=0 未补（次级别对冲 alpha 未启）』——这正是 K_i 子声部要填的缺口。
      ⟹ l2-fullwindow-8sym 有两种 scope，拓扑后果不同：
      (S1) 跑现有 v1 全窗 runner 原样（subvoice depth=0，诚实标已覆盖）= 无 refactor、无新工位。
      (S2) 把 K_i 子声部接入 run_theta_v0，使全窗 L3 含多声部对冲 alpha = 真跨文件 ripple
           （coverage K_i → interp 父 voice 生成 §10 定理1 → runner sizing → l3_fullwindow 归因），
           是 pi_bsp_timing.rs probe 从未建过的 bridge（probe 是隔离读数，runner 是回测主路径）。
      子分解判据：S2 触及 ≥3 文件且引入新 sizing 语义（子声部 leg.units 进 voice-state，与 646
      depth_weight sizing 层交互）⟹ 满足『上下文膨胀 + 领域正交（结构 K_i 视图 ⊗ 回测会计）』扩张信号。
      异质审查判据：S2 产出是 L3 实证（全窗多声部对冲 alpha 可证伪）⟹ 按 #41 spawn_mandate
      『L2+L3 实证类不可免除异质审查』须配 codex/gemini-challenger 节点（同 TOPO-009-A 定理）。
    verdict: >
      (A) [条件扩张] 若 l2-fullwindow-8sym 暴露 S2（K_i 接入全窗 runner），建议 Lead 子分解为 DAG：
          ①设计节点（architect：K_i 子声部如何进 run_theta_v0 sizing，与 646 depth_weight 层关系）
          →②实装节点→③异质审查节点（codex-challenger，L3 实证不可免，#41 定理）→④归因/结晶。
          理由：跨 ≥3 文件 ripple + 新 sizing 语义 = 单工位上下文膨胀，复杂度应靠工位数扩展（核心原则）。
      (B) [扩张否] 若 l2-fullwindow-8sym 实为 S1（跑现有 runner 原样，subvoice depth=0），
          诚实标已覆盖缺口 ⟹ 无 refactor、无新工位、无子分解。直接由现工位推进。
      (C) [scope 自决归属] S1/S2 的判别是 l2-fullwindow-8sym 自身按其 task 定义可读出的事实
          （它要不要 subvoice 信号），非拓扑职责裁定 —— 拓扑只提供条件建议，Lead 执行。
          建议 Lead 先让 l2-fullwindow-8sym 声明 scope（读不读 K_i），再据本条 A/B 分派。
      (D) [边界翻转] 若后续证实 runner 已通过其它入口间接消费 K_i（本次 grep 未覆盖的动态路径），
          则 S2 ripple 评估失效，退回 S1。当前 grep 实测 runner.rs/l3_fullwindow.rs 零 K_i 引用。
      (E) [观测] architect 工位仅在 S2 设计节点需要时 spawn（按需，非常设）；codex-challenger
          已在本主线触达（TOPO-011 NO#2 审 K_i dedup），S2 复用同工位类型，非新增类型。

  - id: TOPO-20260630-013
    timestamp: "2026-06-30T02:30:00+08:00"
    type: 扩张评估（signal_resolution_1s_bi_a0 主线：backtest 执行工位触发判定）
    trigger_signal: Lead 委派——filter-spec L2/L3 证伪条件设计完成后是否 spawn backtest 执行工位
    workstations: [filter-spec, barspec-impl, backtest-e1（候选新工位）]
    reasoning: >
      (1) [触发满足] filter-spec 产出已落盘（review-results/filter-spec-a0-stroke-20260623.md）：
          E1 实验设计完整（控制变量表 + 否证条件 BTC P1 final_nav < +664%）+ 工程可行性确认
          （a0=Stroke 代码就绪 types.rs:72/stream.rs:111/rec_stream.rs:117，runner 传参约 5-10 行）。
          ⟹ Lead 预判的 backtest 执行工位触发条件（证伪条件设计完成 + 需实际回测）已满足。
      (2) [依赖结构修正 Lead 描述] Lead 描述 backtest 串行依赖 barspec-impl(1s通路)+filter-spec(判据)。
          实测 filter-spec 报告四节(E1)：E1 先跑 1min（"1min a0=笔是最小可否证单位"），
          E2(1s+a0=Segment) 才用 1s。按 275 局部依赖：E1 阶段不消费 barspec-impl 的 1s 输出
          （只需 a0=Stroke 传参，已就绪），不被 barspec 阻塞。barspec 只阻塞 E2 阶段。
          backtest 工位应分阶段：E1（不依赖 barspec，可立即推进）→ E2（依赖 barspec 1s 通路）。
      (3) [前置依赖未消解] filter-spec 报告三节明确 E1/1s 实验"可解读的必要前提"=547号 cascade 修复
          （e_level==最高活跃走势级别）。但本轮代码已大幅重构（K_i 主线 commit 37d282abb0+），
          grep 实测引擎源码 fn cascade_reverse_to_core / fn core_flip 零匹配——filter-spec 报告
          引用的修复位置坐标（rec_engine.rs::cascade_reverse_to_core）已漂移。这是 backtest 工位
          执行时的自检事项（诊断坐标系≠生产坐标系，关联 633/635），非拓扑裁定。
      (4) [异质审查定理] backtest E1 产出是 L2 实证（捕获率提升可证伪，携概念信息差），按 #41
          spawn_mandate「L2+L3 实证类不可免除异质审查」（同 TOPO-009-A 定理）须配异质审查节点。
    verdict: >
      (A) [扩张建议] 建议 Lead spawn backtest-e1 执行工位，scope=E1（1min, a0=Segment→Stroke 单变量，
          BTC+CL，PerfectionMode=Structural 固定）。该工位不被 barspec-impl 阻塞（E1 不消费 1s 通路）。
      (B) [定理] backtest-e1 是 L2 实证，须 blockedBy 一个异质审查节点（codex-challenger/gemini-challenger），
          否则 crystallize=封闭自证（#41 exemption 已结算，非选择）。
      (C) [依赖分阶段] E2（1s+a0=Segment）阶段才依赖 barspec-impl 的 1s 数据通路；若 barspec-impl
          未完成，仅阻塞 E2 不阻塞 E1。Lead 不应把 E1+E2 捆为单工位串行等 barspec（275 局部依赖：
          把不依赖的工作误判为不可推进=161 镜像）。
      (D) [边界翻转] E1 否证（BTC P1 < +664%），1s+笔组合失去依据，E2/barspec 路线一并失效，
          backtest-e1 是该主线的判决点。E1 成立，E2 才有意义、barspec 1s 通路才被消费。
      (E) [自检移交] 547 cascade 修复坐标漂移 + a0=Stroke runner 传参就绪性，由 backtest-e1 工位
          执行时自检（生产同坐标系验证），非拓扑职责。
      (F) [边界声明] 基于文件系统可观测信号（filter-spec 落盘产出 + roadmap + 引擎 grep），
          无各成员实时心跳；roster 活性以 Lead 实时心跳为准。topology-manager 不执行变更，仅建议。
