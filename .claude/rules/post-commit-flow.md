# commit/push 后的默认动作

commit/push 后，输出**简要总结**（让编排者观察系统演化），然后：

```
→ 接下来：[具体动作]
```

紧跟 tool 调用。不允许出现等待信号。

## 原理

编排者需要观察系统演化轨迹。总结是观察的载体，不应被禁止。
禁止的是"总结 → 等待"模式。正确模式是"总结 → 行动声明 → 执行"。

## 例外

所有可自主推进的工位均已完成（无生成态谱系、无失败测试、无待做行动）时，输出状态快照而非行动声明。
