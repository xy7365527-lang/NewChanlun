# Codex Review — OQ-9 扩维实现审查
**session_id**: 019f0318-fa07-7c21-b1ec-8a41ad3f67d8  
**模式**: review  
**时间**: 2026-06-26 08:43–08:45  
**tokens**: 73,745  
**model**: gpt-5.5 xhigh  

---

## 审查对象

- `/Users/silencehan/Projects/NewChanlun/formal/Tlayers/Accounting/TotalWealth.lean`（OQ-9 gate §1.5）
- `/Users/silencehan/Projects/NewChanlun/formal/Strict/HybridAssembly.lean`（twState 接入闭环 §6.5）

---

## Codex Prompt（完整）

[见 /tmp/codex-oq9-review-ctx.md — 2026-06-26 写入，四问审查]

---

## Codex Response（完整）

### 问题1：rawStep/legalStep 双层

裁决：**需修正**

推理链：
- cumNetCash 与 TW 三量确实正交（closeShareLeg 只动 openShareLegs/cumNetCash，twStep_preserves_tw 覆盖所有事件）
- raw witness 在同一个 twStep 调用下同时展示 cumNetCash 下降和 stage 保持，不是两路径拼接
- 但 oq9_witness_path_illegal 只证"合法 enterEarning 后 openShareLegs=0"，没有证 raw witness 在 legal trace 中不可达
- 更关键：LegalTransition 允许 openShareLeg 在任何 stage 发生（_ => True）。合法进入 earning 后仍可合法 openShareLeg，再合法 closeShareLeg p，产生 stage=earningShares ∧ openShareLegs=1 的 legal 可达形状

具体修正：
- 要么把 openShareLeg 的合法性限制为 s.stage ≠ TStage.earningShares 或 s.stage.rank < TStage.earningShares.rank
- 要么拆维度：openLegacyShareLegs 与 earning 阶段新腿分开，OQ-9 gate 只约束 legacy counter
- 同时增加 trace 级定理：legal_enter_and_no_earning_open_preserves_no_legacy_legs，再推出 ¬ LegalTransition reachableState (closeShareLeg p)

### 问题2：assemblyStep_twState_changes witness

裁决：**PASS，但范围很窄**

推理链：
- riskAdapter 展开为 defaultRiskGrid.gridProject，grid=[0,1,2,3]，cost=id，gridProject foldl pick 选最小保留 0
- scheduleAdapter 0 走 else 分支，产 TWEvent.recoverCapital 1
- transitionAdapter 消费的是 o.twEvent（OrderOut），不是 e.twEvent（AssemblyEvent）
- revert hstage; decide 已由 lake build 证明可终止并成立

范围限制：只证 twState 非恒等挂件，不能证 AssemblyEvent.twEvent 被消费，不能证 OQ-9 的 cumNetCash/closeShareLeg 路径进入闭环。

### 问题3：AssemblyEvent.twEvent 是否死字段

裁决：**声明膨胀**

推理链：
- AssemblyEvent.twEvent 是死字段：recAdapter 只用 parseEvent，transitionAdapter 的 twState 只用 OrderOut.twEvent
- 类比 ledgerEvent 成立：ledgerState 同样只用 o.ledgerEvent，不是 e.ledgerEvent
- "外部 schema 占位"可以成立，但"非新增死字段"不成立——它是 intentional ignored field，不是被 T 消费的外部输入

具体修正：
- 文档改成：AssemblyEvent.{ledgerEvent,twEvent} 是外部意图占位，当前闭环明确忽略，权威账本事件来自 OrderOut
- 加 assemblyStep_twState_ignores_event_twEvent 定理：证明只要 parseEvent 相同，换掉 AssemblyEvent.twEvent 不影响后态 twState
- 若目标真是外部事件驱动 TW，则必须改 transitionAdapter 消费 e.twEvent，或增加 o.twEvent 与 e.twEvent 的一致性校验

### 问题4：workaround / 漏张力

裁决：**需修正**

推理链：
- clearCampaign 无条件把 openShareLegs := 0, cumNetCash := 0（TotalWealth.lean:200）。不是合法闭合，是强制抹账。campaign 结束前仍有腿时绕过 gate
- closeShareLeg 在 openShareLegs=0 时 Nat 饱和减法保持 0，但仍修改 cumNetCash（TotalWealth.lean:194）。Legal 层排除了它，但 raw 层存在"幽灵腿闭合"副作用
- scheduleAdapter 只产 shortDiff/recoverCapital，从不产 openShareLeg/closeShareLeg（HybridAssembly.lean:423）。cumNetCash 在当前 assembly 闭环中永远不动——是 TotalWealth raw witness 载体，不是闭环实际触发的数值通道

具体修正：
- LegalTransition clearCampaign 应至少要求 s.openShareLegs = 0，否则另设 forceClearCampaign 并明确为非法/边界清算事件
- closeShareLeg 建议改成 guarded raw step，或至少加 theorem 标注 zero-close 是非法 ghost
- 若要声明 OQ-9 接入闭环，需让 schedule/OrderOut 能产生 leg open/close 事件，并证明 legal 路径下 earning 后 legacy close 不可达

### 综合裁决

**部分通过，必须修正上述声明与 legal reachability 缺口后才可整体 PASS。**

当前实现通过构建，raw witness、TW 正交、twState 线程化、闭环非恒等都是真的；但"raw witness 在 legal 层不可达"尚未被充分形式化，AssemblyEvent.twEvent 是被忽略的 schema 字段，clearCampaign 与 zero-close 留下了真实张力。

---

## 代码层异质审查工位判定

见 SendMessage 至 cc-account-expand。
