# Task

你在一个隔离沙盒里，工作目录是仓库根。只做一件事：

1. 在仓库根创建文件 `SANDCASTLE_SMOKE.md`，内容一行：`sandcastle + prime-agent smoke OK`。
2. 用 git 提交这个文件，commit message：`smoke: sandcastle prime-agent 首跑`。

不要 push，不要碰任何其他文件，不要操作 issue。做完就结束。