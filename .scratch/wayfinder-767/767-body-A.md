## Destination

一份落仓的**工作流正本**（并进**已存在的** `docs/agents/wayfinder-workflow.md`，`AGENTS.md` 加指针——原写「新建 `wayfinder-routing.md`」，#779 查实正本已于 2026-07-30 09:22 落盘，改为并入不新建），写清四件事：本仓什么信号进 wayfinder 流、四种票型在本仓各自长什么样及准入判据、一票一会话的并行纪律与 AFK/HITL 载体分叉、图的寿命与 `/to-spec` 交棒边界。写完即到达——本图不改任何 skill、不改任何代码。

## Notes

- **域**：元层工作流设计。对照源 = Matt Pocock `_wayfinder_ Nothing is too big to plan anymore.txt`（本地 `~/Downloads/`）+ `~/.claude/skills/{wayfinder,ask-matt,prototype,to-spec,to-tickets,implement,grilling,handoff,claude-handoff}/SKILL.md`。
- **每会话必读**（#779 裁定③④定的默认清单，只列仓内文件；全局 `~/.claude/CLAUDE.md` 已移出——Claude Code 自动注入、Kimi 读不到，其要点由 #773 补进 `AGENTS.md`）：
  1. `docs/agents/wayfinder-workflow.md`（本仓 wayfinder 正本）
  2. `docs/agents/issue-tracker.md`「Wayfinding operations」段（tracker 物理操作）
  3. `AGENTS.md`（#517 定的全 harness 唯一正本入口）
- **已定前提（不再重议）**：
  1. skill 本体不改。用户明示「假如我们的 skill 版本没有问题 也不需要改变 skills」——问题在路由，不在 skill。
  2. AFK 票（research / 可无人值守的 task）载体 = **本体在当前会话用 Agent 工具派子代理**，按 `~/.claude/CLAUDE.md` 模型路由表从便宜档往贵档挑第一个够格的；结果回流本体验收后写回 tracker。
  3. 一票一会话 + 多会话并行 = **现状已如此**，不是待改项，是待补纪律项。
  4. **HITL 吞吐纪律**（#776 裁定，2026-07-30；节奏一条已被 #779 覆盖）——本图剩余 grilling 票即刻适用：默认入口 = **`/grilling` 一次一问 + `/domain-modeling` 落文档**（回到 `wayfinder-workflow.md` 已落盘的原口径；#776 裁定①的 `/batch-grill-me` 节奏与「超 4 题拆两轮」作废，成因见 [#779](https://github.com/xy7365527-lang/NewChanlun/issues/779)）；票内查事实按代价分流；一票一会话例外面 = 全部 AFK 票。条款正文与论证见 [并行吞吐：HITL 瓶颈与 batch 化 #776](https://github.com/xy7365527-lang/NewChanlun/issues/776)，跨图通用部分由 #773 外移正本，此处只留指针。
- **本图带实装**（#770 裁定③ 自指落地，2026-07-30）：[正本落盘 #773](https://github.com/xy7365527-lang/NewChanlun/issues/773) 交付 destination 本身，属标准甲类。理由 = 本图 destination 就是一份文档，写它即到达，另起 to-spec 链是空转；该票走 AFK 子代理落盘 + 本体验收。

- **本图开图时的事实基线**（2026-07-30 点查，21 张图 / 271 张已关子票）：
  | 票型 | 已关 | 未关 |
  |---|---|---|
  | task | 102 | 22 |
  | grilling | 85 | 3 |
  | research | 84 | 4 |
  | **prototype** | **0** | **0** |

## Decisions so far

<!-- 一行一张已关票：够判断相关性即可，细节回票里看 -->

- [task 票名分收束：解阻塞 vs 交付](https://github.com/xy7365527-lang/NewChanlun/issues/770) — 「收还是不收」问错了：task 标签下**五种**东西（甲纯实装/乙探针喂决策/丙实验跑批/丁文书簿记/戊标签贴错），乙丙本来就够格。甲类口径 = **带实装的图 Notes 须显式声明 + 理由，没声明 = 纯决策图、实装票不准挂**（声明制自执行，不设巡查/CI 硬门，与 delivery-discipline 同款）。21 张图仅 2 张声明过。存量：#695/#529/本图补声明，#737（task 0/4）本就合规不需要，已关 16 张不动。判别一句话：**这张票做完，是解开了一个决策，还是交付了一件成果？**

- [三方口径差异清单：视频 vs skill 正文 vs ask-matt](https://github.com/xy7365527-lang/NewChanlun/issues/768) — ask-matt 与 wayfinder 两份正文**都没提 prototype 票型**（本仓 0/0 的成因）；`wayfinder/SKILL.md` 对「图完成之后去哪」完全没写、从不点名 `/to-spec`（三方最大空白）；「一票一会话」skill 里只有间接表达；上下文纪律那条**不是矛盾是接缝留白**（订正开票时的原判）。

- [并行吞吐：HITL 瓶颈与 batch 化](https://github.com/xy7365527-lang/NewChanlun/issues/776) — **⚠ 裁定①已被 [#779](https://github.com/xy7365527-lang/NewChanlun/issues/779) 覆盖：节奏回到一次一问，`/batch-grill-me` 与「超 4 题拆两轮」作废；其余五裁仍有效。** 订正开票前提：本仓 grilling 票现行默认是 `grill-with-docs`（本地 grill 家族共四个，开票只点了两个），而它转调 `grilling` 故自带一次一问、拿不到 batch 节奏。裁定 grilling 票默认 = **`/batch-grill-me` 节奏 + `/domain-modeling` 落文档并调**（`batch-grill-me` 是 `grilling` 的严格泛化，其 frontier 规则本身禁止把有依赖的题并轮，链式题不会被伤；不新建合体 skill）；超 4 题拆两轮按下游解锁数排序，不挪用 `to-questionnaire`。另定三条 HITL 吞吐纪律：票内查事实**按代价分**（单条自查／多文件强制派子代理）、push right **按可逆性分界**（写仓的合入点逐次批准不动，纯决策纯读右移攒 brief）、一票一会话例外面**扩到全部 AFK 票**（该规则保护的是人的上下文，AFK 不消耗）。**Notes 预授权四条**：沉淀并入开图第二轮 + 走图随手回写；预授权边界**按有无先例**（有判例可套用，新取舍／风险偏好／taste 仍须当场拍）；判错痕迹落 **comment 不落 body**（#772 已查实 body 编辑史 API 取不到）——Notes 条目编号 + 票内声明「依 Notes N-k 判定」；膨胀**按归属分流**（跨图通用外移正本，Notes 只留指针，软信号约 10 条）。条款落盘归 #773。

- [并行纪律：多会话同写一张图的冲突口径](https://github.com/xy7365527-lang/NewChanlun/issues/772) — 订正开票前提：**丢的是索引不是决策**。决策正本在子票的 resolution comment（append-only，并发碰不到），图正文 Decisions-so-far 按设计只是索引，且**可从已关子票列表重建**——原判的「无解的静默丢失」降级为「可检测、可重建的索引漂移」。据此五裁：①写入协议**保留正文整体重写**（不换评论流——那会牺牲图「一次加载看全貌」的核心价值），配三条纪律：写的那一刻才读正文、写前用 `updated_at` 重比一次（应用层读后重比，仍有毫秒级残窗，挡的是秒级真实竞态；冲突重试一次，再冲突报人不盲循环）、外加④的对账；②**认领竞态不管**——assign 是加进集合本就锁不住，HITL 撞车当场可见、AFK 撞车只白跑一个子代理；③**谁清的雾谁毕业**，重复票看得见能关，后发现者关后开的那张指向先开的；④**对账每次走图会话开头跑**（顺手一条查询，丢行最多存活一个会话）；⑤git 层冲突移出本票挂 `/resolving-merge-conflicts` 指针，**写仓的 AFK 子代理强制 worktree、只读的不强制**（本仓已实跑几十个 worktree，成本零点几秒；主仓撞车有 `git stash` 抹改动的实伤前例）——声明制自执行，不设巡查/CI 硬门。条款落盘归 #773。

- [两种图分家：一次寻路图 vs 长驻总账图](https://github.com/xy7365527-lang/NewChanlun/issues/771) — 订正开票前提：**没有两族**，22 张图在存活天数/子票数/关图时雾是否清空三维上切不出干净分野（唯一断层在子票数一维：16 张 4–13 vs 3 张 34–59，不与另两维对齐），实际是连续谱。五裁：①**不切分类，改按声明分**——图在 Notes 写一行寿命预期，**没写 = 默认一次寻路图**（复用 #770 同款机制；声明是开图人当时的意图，子票数是事后结果，读图人要的是前者；声明制自执行，不设巡查）；②关图**不要求雾清空，但残雾必须有去向**——毕业成票／移进 Out of scope／明写「搁置无人跟进」三选一，不许沉默带走（22 张仅 2 张关图时雾为空，skill 原义与本仓实践不符）。「图关闭 = 到达」信号由此恢复：到达 ≠ 疑问归零，= **所有疑问有明确归属**；订正原票问题③——「雾是否清空」测错了对象（测存量，该测去向），该维度不承重；③载体 = **Notes 一行 + 长驻图标题 📒 前缀，不新增 `wayfinder:ledger` 标签**（标签二值压不下声明的信息量，且会被 `--label` 查询依赖，漂移是真伤；emoji 无查询依赖，漂移代价低）；④存量**只补 #695／#529** 两张（实测均无声明，#529 连 🗺️ 都没有），其余 3 张开着的图吃默认值不加，已关 17 张不动，动作并入 #773；⑤长驻图的 fog／Decisions 两段**语义不改一个字**——②已把判据从「雾要清空」换成「关图时有去向」，长驻图不关即不触发，原问题④的担心自动消失（实质 = 少加一条规则）。残雾去向：「长驻总账图的交棒时机」移交 [#775](https://github.com/xy7365527-lang/NewChanlun/issues/775)，已从雾段移除。

- [spec 的两种名分：交棒件 vs 总说明书](https://github.com/xy7365527-lang/NewChanlun/issues/775) — 订正开票前提两次：AFK 普查（31 张真 SPEC 票）先把「甲/乙二选一」推翻为三态，本轮再归约——「作废/被取代」那 3 张**不在同一维度**（说的是关票结局，不是交不交棒），剥离后剩 5 张全落在**同一条轴**上（轴 = 下游实施票落在图里还是图外）。**三成中间态 = 二分法在连续轴上留下的接缝**，与 #771 同构第二例。五裁：①正本**整个去掉「甲型/乙型」代号**，只讲「出图／不出图」，中间点（自交付、混合）合法；②**形态不是选的，是被图的实装声明决定的**（#770 直接推论，默认值由此推导而非投票）——纯决策图 → spec 出图牵新票；声明带实装的图 → spec 不出图、是实施总单；载体 = 首行声明一句话（判据只能事后判，开票当下用不上），写反了与图正面打架、肉眼可见，不设巡查。**据此 #756 不是漂移**，是「带实装图」的必然推论；③**溯源密度按链有没有离开图分，不按形态分**——出图的必须逐条挂裁定票（范本 #480，断在图外接不回来），不出图的只要 `Part of #N`，逐条背书是冗余；**#756 现状合规不用补**；④spec 非持久，**关票判据 = 指向的交付点全部落地**（出图等新票全关／不出图等图内那批子票全关，故不出图的 spec 天然活不过它的图）；存量 3 张 open 老 SPEC（#169/#485/#620）各随自己实装链关，不是本图动作；⑤**长驻总账图按批次交棒，不按图的终点交棒**（#771 转来的残雾，本票结清）——判据 = 一批决策的下游实施彼此有依赖、且不再依赖任何还开着的子票，即可出 spec，图继续开。长驻图无需额外规则，②已覆盖；本图新增仅一句：交棒点从「图终点」换成「批次边界」。正本措辞已定稿，落盘归 #773。

- [原型票在本仓的形态与准入判据：0/0 怎么破](https://github.com/xy7365527-lang/NewChanlun/issues/769) — 订正开票前提两处：①勘察报告判「#409 探针最贴近原型」**推翻**——#409 量的是既有系统跑成什么样，是事实问题，标签没贴错；8 张先例里只有 #167（改回测口径跑 A/B/C）接近原型。②0/0 **不是「漏了一条腿」**——AFK 子代理扫完全部 90 张已关 grilling 票，本该是原型票的**强候选 0 张**；但捞出 #142 引用过真实抛弃式原型（`/tmp/v4_C5`），**原型活动真发生过、只是没挂票就散在 `/tmp`**。八裁：①判据 = **答案在造出来之前存不存在**（不存在=原型／已在仓里只是没读出来=探针／对话能 settle=grilling）；②反面判据三条（有权威源可援引／摆了 (a)(b)(c) 候选形态但靠生成性规则判／缺数字派探针票去测），三条均由 90 张票的实际 settle 方式聚出；③**不写形态白名单**（形态由判据决定，#775 同款推论；同一个 Rust bin 量既有行为是探针、试新逻辑是原型），只排除 fixture JSON 与「拿现有口径跑一遍看数」；④排除项加限定补真实接缝——**排除的是「拿现有口径跑一遍」，不排除「跑一个还不存在的口径」**（回测跑数两边都能落，缝补在口径存不存在处）；⑤抛弃纪律**绑「原型」名分不绑「探针 bin」物理形态**——存量 38 个探针 bin 不动、未来原型码落 `proto/<票号>` 抛弃分支、探针码继续留 main（实证：`p409_pan_live_probe` 被 #601/#527/#465 反复引用，探针是可复用测量装置，删了要重造）；⑥抛弃分支**永久留存**（`/prototype` 规则 6 原义 = 一手史料），关票留分支指针，无人负责删；⑦「便宜」**不定量化门槛**（Rust/回测侧耗时仓内零记载，最需要基线的一侧恰无基线），定性 = 一个会话内造得出来、跑一次不用切走干别的；⑧现状说明**只作说明不作规则**、**不设正向触发**，只留自检提示「这个不造出来能判吗」——有需求才开票，仍是 0/0 可接受。引用源订正：「保留失败的生成史」出自 `meta-orchestration/SKILL.md:6`，不在 `generation-constitution.md`。条款落盘归 #773。残雾去向：「票型 × 模型档位」已毕业成 [#781](https://github.com/xy7365527-lang/NewChanlun/issues/781)，从雾段移除。

- [走图会话的开场：HITL 票要不要固化开场模板](https://github.com/xy7365527-lang/NewChanlun/issues/779) — 订正开票前提两处：①**正本已经存在**——`docs/agents/wayfinder-workflow.md`（2026-07-30 09:22 落盘）已写五段管线/票型口径/一票一 session，其 grilling 条写的正是「一次一问」，与 #776 裁定①直接冲突；#776 那场会话没读到它。**这起事故就是本票的核心证据**：要固化的不是「开场输什么」，是「哪些文件必须真进上下文」。②**Kimi 侧无全局注入文件**（`~/.kimi-code/config.toml` 无 instructions/system_prompt），主力运行时读不到 `~/.claude/CLAUDE.md`。五裁：①**走图会话一次一问**，覆盖 #776 裁定①（#776 其余五裁不动；②随①归零）——代价是 HITL 吞吐低，但 #776 自身就是 batch 节奏的实证失败（疲劳上下文一轮裁六条，与正本冲突而无人察觉）；②**固化在图上不固化在会话第一句**——不做开场模板、不新建本仓 skill/slash 命令（本仓虽有 `.agents/skills/` 121 文件 + 7 个自建命令、成本极低，仍不建），第一句仍是 `/wayfinder <图链接>`；否掉「什么都不固化」因为对账/压 `updated_at`/一次一问都是本仓特有、skill 正文没有，否掉「做模板」因为「记得粘贴」与「记得读正本」是同一失效模式；**这条同时解掉待裁 3（载体）= 正本文件 + 图 Notes 一段清单**；③**清单由正本给默认版、开图照抄、各图按域追加**——手写就是会漏（本图原清单漏的恰是正本），且正本改名只改一处；代价是每图 Notes 多一段样板，花的是「一次加载看全貌」的预算；④**默认清单只列仓内三份**（正本 / `issue-tracker.md` Wayfinding 段 / `AGENTS.md`），全局 `~/.claude/CLAUDE.md` 移出——它在 CC 里本就自动注入（点了白点）且 Kimi 读不到，其三段要点改写进 `AGENTS.md`；否掉「保留 + 注 Kimi 改读」因为清单里带 harness 分叉就要人先判断自己在哪个 harness，而清单本是拿来免判断的；碰代码的图按域追加 `delivery-discipline.md` 等；⑤**AFK 票不做同款固化**——本票治的是「新会话上下文空」，派子代理时上下文不空、无「忘了读」这个失效模式，且 #776③/#772⑤已管派发与 worktree，再定骨架三处口径打架；子代理带什么归 [#781](https://github.com/xy7365527-lang/NewChanlun/issues/781)。另查实：仓内与全局的 wayfinder/grill 家族逐字一致，加载哪份无歧义。落盘归 #773（含正本文件名订正）。

## Not yet specified

- **丁类与戊类票的归属**（#770 浮出）：丁 = 文书/名分/口径落文这类簿记票（#734 ADR 文本债、#725 文书归置、#616 落文书、#520 注释订正），既不产决策也不产功能；戊 = 标签与标题打架（#157 标题 `[grilling]` 贴 task 标签、`[debt]` 一批、#564 是 bug、#670 是评审）。两者同源——非甲非乙的票该贴什么标签。粗于一票，可能并成正本里一行，也可能各开一票。已关票的重贴标签在范围外，只议未来。
- **正本与既有三份 agents 文档的边界**：`delivery-discipline.md` / `generation-constitution.md` / `stat-provenance.md` 已有交付纪律口径，正本写到哪停、哪些只挂指针。

## Out of scope

<!-- 越过 destination 的活：已关闭，永不毕业 -->

- **改 skill 本体**（`~/.claude/skills/*`）——用户明示 skill 版本无问题、不需要改。本图只产路由口径，不动 skill 文件。
- **非 wayfinder 流程的路由**（`/triage`、`/diagnosing-bugs`、`/improve-codebase-architecture` 等 on-ramp）——本图只覆盖「Matt 视频那套」= wayfinder 主体 + `/to-spec` → `/to-tickets` → `/implement` 交棒链。
- **回溯改判 271 张已关票**——历史票不重贴标签、不重开。正本只约束未来。







