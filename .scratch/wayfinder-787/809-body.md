Part of #787

## Question

[总缝规则 #804](https://github.com/xy7365527-lang/NewChanlun/issues/804) 已裁完（三条裁定，正本落 `AGENTS.md`「总缝规则：同一判定每级必须是同一个」节，commit `dac8ff9fa1`）。其边界写明：**本票裁完后重新清点剩余分歧面，再定第二批开几张、按什么次序。**

本票就是那次清点。**这是普查票（乙类 task，AFK），不裁任何概念的口径。**

### 要过筛的东西

[重复实现普查 #789](https://github.com/xy7365527-lang/NewChanlun/issues/789) 数出的 **70+ 实体 / 约 40 套独立教义口径**（七概念全部有教义层分歧，20 条带理由），报告 `b7eaff1097`。

### 判据（三条，全部来自已关票，本票不新创）

按 `AGENTS.md`「缠论教义正本」节现有三条通则逐条过：

1. **总缝规则**（[#804](https://github.com/xy7365527-lang/NewChanlun/issues/804)）——同一判定每级必须是同一个。**判别式是语义不是形状**：把两级输入换成同一份数据，输出不逐位相同即两个判断。**注意：不要拿 `level ==` 的 grep 命中当判据**，那只是分诊线索；也不要因为零命中就判合规。
2. **收敛通则**（[#799](https://github.com/xy7365527-lang/NewChanlun/issues/799) 裁定十）——同一判断不得有宽严两档，更不得让宽松那档接管严格那档的失败。判别式：**看两套判据是不是接在同一个 if-else 上**。三条限定词（不同判断 / 诊断模式 vs 生产模式 / 已声明的独立对照臂）见 `AGENTS.md`。
3. **管辖**（[#799](https://github.com/xy7365527-lang/NewChanlun/issues/799) 裁定六）——只管**生产判定路径**；对照臂 / 只读观测器 / 验证装置属「**不适用**」而非「例外」，单独归一类，不计入分歧面。

### 产出（一张表 + 三个数）

每套口径一行，判成四类之一：

| 类 | 含义 | 后续 |
|---|---|---|
| **违规-已定性** | 踩上述通则，且违规事实清楚 | 进第二批某张概念票的「受影响代码清单」 |
| **合规例外候选** | 有举证书（或写得出来），按 [#804](https://github.com/xy7365527-lang/NewChanlun/issues/804) 裁定二三条判 | **注意全仓上限 3 条**，已知占用 1 条（`compose_level` 的 `is_l0`）；本票须报**候选总数**，超 3 即触顶信号 |
| **真分歧-待裁** | 通则判不了，须由概念票裁教义口径 | 决定第二批开几张的**主依据** |
| **不适用** | 落在管辖外（对照臂/观测器/验证装置） | 不计入分歧面 |

三个数：**违规几条 / 例外候选几条 / 真待裁几条**，后者按概念分组（已知至少三块必进：中枢、区间套含算子 N、背驰）。

### 边界

- **不裁任何概念的口径**——本票只分类，不定「收敛到哪一套」。
- **不动代码**（本图不带实装）。允许只读测量。
- 已知会落在「真分歧-待裁」的两条，直接记账不必重推：中枢的方向交替该不该要（[#804](https://github.com/xy7365527-lang/NewChanlun/issues/804) 明记归第二批）、次级别背驰的 Extreme 该不该要（[#799](https://github.com/xy7365527-lang/NewChanlun/issues/799) 裁定十③ 明记归第二批）。

### 纪律

AFK，写报告故强制开 worktree（`Agent` 工具 `isolation: "worktree"`）。报告落 `.chanlun/review-results/`。090 照实：判不了的写「判不了」，**不许把「查不到」写成「不受影响」**。
