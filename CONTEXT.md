# NewChanlun — 缠论多级别赋格交易

缠论买卖点在多空双开、多级别多重赋格下的策略域。本词汇表为唯一口径来源；定义变更须同步 `docs/adr/` 与 spec。

## Language

### 声部与生命周期

**声部（Voice）**:
一次建仓的完整生命周期实体，`v = (carrier, γ, σ, n)`。一经建立不可变，出场即终结。
_Avoid_: 仓位、持仓、头寸（这些是数量/暴露，不是实体）；"改仓"（不存在同一声部改仓）

**证书（Certificate, γ）**:
经区间套 + 背驰**确认成立**的买卖点。确认发生在证书成立层；出场层只消费已确认证书。
方向构造律：卖点只能由上涨（其背驰段）构造，买点只能由下跌构造。
_Avoid_: 信号（指未确认的候选）；笼统的"买卖点"；"下跌升级成卖点"（下跌不构造卖点）

**活动集（Active Set, A_t）**:
当前存活声部的集合，按 `A_{t+1} = AncOK[(A_t ∖ D_t) ∪ O_t]` 更新。

**子树清仓（Close-Subtree）**:
父声部终结时，其全部后代声部被 AncOK 剪除。属活动集层结构不变量，**不是**通道谓词。
_Avoid_: "父亡子清"（口语，仅解释用）

### 级别与代际

**级别（Level, ℓ）**:
走势结构的递归级别：由次级别走势类型逐层递归构造（`Move[k]/Center[k]`），是纯结构属性。与代际无固定数值关系（根声部可出现在任意级别）。
_Avoid_: 用时间周期（1F/5F/30F、日线等）指级别；用"深度""层级"指级别

**级别身份（Level Identity）**:
某个买卖点/结构对象归属哪个塔级别，是**参照系视图，非存储事实**（同「反向根」条「事后恰好性」注：身份非存储事实，账本零身份字段——本条是该原则在级别维的实例）。唯一存储事实是 `LevelProjectionLayer.identity.level`；调用方需要级别时按参照系当独立参数传入（`build_candidate_element` 的 `lvl` 参数），不在结构对象自身存一份可能漂移的拷贝。
_Avoid_: 在买卖点/证书等结构对象上加同形字段存级别下标（删除墓碑：`BspPoint.level_origin` 曾这样做，全仓恒为 0、无下游级别语义消费者，仅有恒真 equality 分量及 Debug/digest 机械消费；SPEC #455 已在 commit `2d1abf9786` 删除）

**代际（Generation, n）**:
声部在赋格树中的嵌套代数：根声部为 0，子声部 = 父代际 + 1。方向由逐层关系 `σ_u = −σ_{p(u)}` 给出。代码字段 `depth` 即此概念。
_Avoid_: "深度"（易与区间套深度混淆）；奇偶/相位表述（撞词已降格的实证"奇偶交替"，方向教义只用 `σ_u = −σ_{p(u)}`）

**区间套深度（Nest Depth, Ndepth）**:
证书成立层区间套确认的梯级数，满足恒等式 `ℓ = e + Ndepth`。属证书层概念，与声部代际无关。
_Avoid_: 简称"深度"

### 确认结构

**链（Chain）**:
跨级确认的唯一结构：自下而上递归连续，每个级别的确认是该级自治的自上而下区间套——L1 证书由 L1 自己级别的区间套确认，L2 从 L2 自己级别另起一次。级别间不存在跳级查询。
_Avoid_: 桥（并集跳查、任级命中即算确认）

**恰好存在（Co-location）**:
递归连续性保证拐点 x 同时坐落在 L0/L1/…/Lℓ：L2 走势终点 = 其最后 L1 子走势终点 = … = L0 某根 K。是结构性质，非额外假设。

**不变量（Invariants）**:
跨级别同一拐点的身份判据 = **同点递归**：分型在各级递归（区间套）落到同一个点，即为同一事件；落不上 = 递归失败（2026-07-23 #206 裁定）。身份锚 =（**极值价**, 合并组锚）：极值价 = x 处 L0 分型价，严格跨级不变、精确等值无容差；合并组锚 = 组内首根序号，包含层事实、确认后稳定；同价双脚（W 底）由合并组区分。K 序号在包含合并下漂移，不作身份依据。**方向/分型类型不参与身份判定**——点是什么分型（顶/底）是各级自己的结构事实，同一点可在不同级别分别为顶/底；买卖标签属交易层，与身份解耦（2026-07-23 #206 裁定措辞）。
_Avoid_: 用序号数值相等做跨级身份匹配；把方向（买/卖）当身份分量（2026-07-23 #206 裁定：方向完全多余，递归即判据）

**值桥（Value Bridge）**:
候选点与证书事件的同一性判定 = **同点递归**：证书在各级递归落到候选所在的同一个点即同一事件，落不上即非（2026-07-23 #206 裁定）。锚 =（极值价， 合并组锚）——x 处 L0 分型价（精确等值无容差）+ 组内首根序号（包含层事实，不漂移）；**不含方向/分型类型**（同一点可跨级分别为顶/底；方向是交易层标签，2026-07-23 #206 裁定自初版三元锚退役）。旧锚 `source_index == seg_c_full.1`（单序号相等）已废（2026-07-22 裁定换锚）。
_Avoid_: 单序号相等匹配（2026-07-22 裁定换锚）；按方向（买/卖）过滤身份（2026-07-23 #206 裁定）

**归属（Ownership）**:
点与其判定走势的身份关系：owner 判定的教义语义 = 点属于本事件所确认的走势（同走势归属，2026-07-24 #211 裁定）。归属是**同级别**关系，参照物按类取各自的同级别定义对象：一类 = 被破的最后中枢（破 B 即其定义）；三类 = 所离开回抽的中枢；二类 = **该走势的一类点**（二类定义只要求回拉不破一类极值，与本级别中枢无关）——二类误取次级别中枢 c1（确认层对象）即级别错配（2026-07-24 裁定修正）。判同机制按参照物类型分两族（全部精确等值无容差，禁序号判同——#206 裁定；XZD #44 证伪先例）：**点用身份锚（极值价, 合并组锚）；中枢用核心区间 (zd, zg)**（中枢身份 = 其定义内容价格带；延伸改带不改序号，带比序号准；dd/gg 外包络为收紧预留位）。**中枢参照取法 = 归属的走势类型的最新中枢，无论同级别还是次级别**——归属关系是级别对应与找中枢的前提：归属一定，参照中枢即该走势最新中枢，无第二候选（2026-07-24 编排者教义补充）。归属 ≠ 同点（点↔事件同一拐点是值桥谓词，两层不重叠接管）。
_Avoid_: 中枢序号判同；拿次级别/确认层结构当归属参照（级别错配在案）

**确认（Confirmation，买卖点两层之一）**:
买卖点成立的次级别关系：区间套背驰（次级别走势结构 + 背驰判定）证点成立。与归属分层：**确认管「点成不成立」（次级别），归属管「点属于谁」（同级别），两层都必须成立、各用各的结构对象、不可混载**——二类的 c1 是确认层对象（识别用），其一类点才是归属层对象；归属载体错装确认层对象 = 级别错配根因（2026-07-24 #211 烤制结晶）。确认层实装已在识别侧（结构识别 + 背驰判定），归属层归 owner 判定。
_Avoid_: 拿确认层结构回答归属问题（混载）；只确认不归属（冒领）或只归属不确认（虚证）

**活假设（Live Hypothesis）**:
尚未坐实的背驰段判断。教义源 061:26——走势没实际走出来时「先假设是进入背驰段」，「在没有证据否定背驰之前」持续观察其内部结构。与证书**分层而非强弱**：活假设是同一判断**跨当下的生命史**（状态沿时间轴演变、可被后续证据否证并留档）；证书是**每个当下各自独立重算的一次性判定**。两者唯一重合面是基例的力度谓词——活假设**不含区间套、不含终端买卖点背书**（2026-07-27 考证，ADR-0003）。
_Avoid_: 「未确认证书」（证书无未确认态）；把活假设的确认读成「区间套背驰确认了」（过载表述）

**结构完成（Structure Completion）**:
活假设所辖背驰段走完的时点——由数据源**通道切换**给出：完成事件仅在段已走完时产出，故该切换本身即完成信号（ADR-0003）。完成即停延展。粒度上小于中枢震荡结束（后者以某级别三类买卖点为界，092:16）。
_Avoid_: **「闭合」**（已被跨级链完整性占用，见下）；把它等同于中枢破坏或震荡结束（粗一级）

**背驰否证（Divergence Falsification）**:
活假设的两种否定终局，教义上必须分开：**被反超**——曾构成背驰、后段力度大于前段而「可以断定背驰段不成立」（061:26）；**从未构成**——始终未创新高，「背驰如果没有创新高，是不存在的」（061:28）。同归终态、以原因码区分；终态一律留档不删（谱系学保留）。
_工程口径注记_（2026-07-27，#426 转呈 #421 收口，主控执行）：机器实判据 = 力度谓词**从未可证**（`first_provable` 从未写入），非价格面「始终未创新高」的直译；教义锚不变。
_Avoid_: 把「从未构成」并入「被反超」（教义两回事，前者未曾成立、后者曾成立后被推翻）

**中枢归一性（Center Continuity）**:
同一价格带 + 时间连续的震荡链的教义身份判读：**排除新生式分立**——中枢新生要求与前中枢（含瞬间波动）零重叠（020:22），同带即最大重叠；教义只容许两种归一读法：(a) 同一中枢延伸（同级别归一，ZG/ZD 前三段冻结），(b) 死后同带重组经扩张并为一个高级别中枢（高级别归一）。两读法下「同一价格带 = 一个中枢身份」都成立，故 (zd,zg) 带判同是教义唯一容许读法而非放宽（2026-07-24 #228 核查，博文一级）；读法 (b) 必伴高级别扩张产物，塔内无记录则只余 (a)。中枢**破坏**判据 = 次级别离开 + 回抽不重回区间（定理三，018:64）；三类买卖点 = 延伸结束（038:180）；中枢震荡以某级别三类买卖点结束（092:16）。
_Avoid_: 把连续同带区判为「多个独立同级别中枢并存」（无此教义读法）；拿带碰撞当放宽证据（归一性相反）；**「中枢死亡」**（两级原文均无此词——一级博文作「破坏」、二级编纂版作「终结」，2026-07-27 考证订正，见 ADR-0003）

**中枢生命周期出口（Center Lifecycle Exit）**:
中枢命运只有三种登记：**破坏（Broken）** = 三类买卖点（定理三充要条件，唯一终结）；**更替（Supersession/Absorption）** = 在场身份让位——含被新中枢取代（Superseded）、九段升级被更大级别中枢吸收（033:14）、相邻扩展并入更大级别中枢（020/021/023 课）——**不是破坏**：挂起延续（补充十一），旧框（ZG/ZD + 起止两边，补充十三）冻结、三类点判据继续适用，直到旧框自己的三类点清算——级别跟中枢不跟收编者（补充十四）；**延伸（Extension）** = 无事件（身份不变）。一类点（Reset）不在出口全集内——它是走势类型终结的广播信号，不破坏中枢（#474 裁定，2026-07-27）。
_Avoid_: 把九段升级/相邻扩展登记成破坏（发明第二种终结，升级与扩展均常见，会成批误杀）；「清空整场」（#474 已作废）；一类点当出口（它只是广播）

**利润率最大操作模式（《市场天道》第 6 章机械化判别程序）**:
第六章典型呈现（正文 335-340 / PDF p353-358，汇编 49 课全文 + 编者整理）：**三情况 × 三类点状态矩阵**——找当下操作级别最后一个中枢：当下在中枢之中 ⟹ 不操作是最好的操作（技术高者可参与次级别二类买点）；之下：未出三卖 = 震荡依旧（类盘背比较力度 + 区间套定买点），已出三卖 = 中枢已结束（背驰定买点，更干脆 = 不参与——「不参与操作级别及以上的下跌与超过操作级别的盘整，这种习惯必须养成」）；之上：未出三买 = 等待，已出三买 = 离买点不远可介入（刚形成时最佳）。机械规则：上移满仓、上移段不做差价；三卖出现不回补；上移背驰全抛（走势类型完成）、不背驰继续短差；两类背驰严格区分（分列三类买点前后，049:62）；震荡本质全仓、宁愿抛错也严格按方法（抛错 50%、三买可回、抛对 10%+）。激进版：只在三类买点买入、新中枢形成即退出。博文可回溯主体 = 49 课全文（049:60 模式本体、049:58/72 两定理、049:28 分类算子）。用户澄清（2026-07-28，ADR 补充十七）：「3-1-2」不作次序解，作机械化判别程序解——本模式即典型。
_Avoid_: 把 049:60 读成「一类点入场」（入场锚是三类买）；持股段做短差（中途不短差）；上移段做差价（原文：上移应满仓）；引 chan99 0042（仅 12 行、承重内容全缺）

**类第一类买卖点（Quasi-First-Class BSP）与否则域（37:18 Otherwise-Domain）——★实装完成态（2026-07-29，#606/#607/#608）**:
027:66/027:68 正式命名——盘整背驰构成的买卖点（盘背点 = 类第一类；其后回抽不创新极值 + MACD 类背驰 = 类第二类）。**否则域** = 双中枢趋势结构 c 无 T3 的案例域：037:18 判词「可以看成是 B 中枢的小级别波动，完全可以用盘整背驰来处理」——趋势的壳、盘背的名。与真一类的分界 = **T3-in-c 资格检验**（091:278：连三类买点都没出现「哪里会有背驰」；资格读法下限缩定理：真一类 ⟹ T3 先行，2026-07-28 #583 裁定）。类第一类无终结权（中枢死亡证明权唯一归三类点，补充十三）；盘整本级别无真一二类点（037:16 没有趋势没有背驰），次级别全有点族（086:70），回溯不洗白名分（086:70「这只是比喻」）。**实装落地**：判据分级器 `judge_first_cached`（S1 merge `496353f346`）+ 否则域不置一类 bit 大闸（S2 merge `df542eb5cf`）；wf8 金标准新锚 834 行（Reset 59→22，native/otherwise=22/37）；wf7/p3fold 对照读数（#608 S3）native/otherwise=17/16、37/50——判据落地归 #589，分档三票（#606/#607/#608）全闭合。
_Avoid_: 拿一类标签贴否则域（混流名不副实——#584 已裁 37:18 分档（2026-07-28，补充十六），T3-in-c 资格检验，趋势一类/类第一类拆档，**判据已落地**（signal.rs `judge_first_cached`，非仅裁定文字）；回溯追封真一类（086:70 括注禁止）；把盘背当出口（补充十四：不产生命周期事件）

**链顶（Chain Top）**:
链式确认的起算级 = 以 x 为拐点的最高级别走势类型所在级。由恰好存在给出（x 只恰好存在于它是拐点的级），是结构对象而非移动窗口。
_Avoid_: 塔顶（随时间生长）；固定 ℓ+1 起算（2026-07-23 裁定）

**闭合（Closure）**:
跨级链从链顶逐级向下延伸至 L0 的完整性要求：逐级有该级自治证书且过 `n_delta`。缺环即拒、断环即拒——lvl+1 单级过证不算，高级有证而中间断不算。
_Avoid_: 浅链准入（单级过证即算）；跳链准入（并集残留，2026-07-23 裁定）

**候选过滤门（candidate-filter gate）**:
π 世代在 fold 之前过滤进出场共享的 gamma 候选集：一道过滤同时覆盖开仓消费与规则2反向证书平仓消费，候选被拒即两边都不可消费。门判据来自 `nest.rs` 的 `n_delta` 递归核；过滤器只消费该判据，不另造出场查法。

**决策注入门（decision-injection gate）**:
v1/dual 世代在逐持仓退出决策里向 `closePred` 注入反向析取项，消费单位是持仓决策，不是共享候选集。它与候选过滤门的确认判据同源于 `nest.rs` 的 `n_delta` 递归核，但消费形态分代；v1/dual 及该形态已 deprecated，退役见 #499。

**名分五态（Lineage Status）**:
仓内代码/文件/工位的世代名分判定：现役 / 新生入口待驱动 / deprecated 待退役 / 孤儿待删 / 垃圾待清。机械判据、现役线名单（引擎 = π、git 线 = main、工位 = 白名单制）与开票门单源见 `docs/agents/generation-constitution.md`（ADR 0004，2026-07-28 裁定；第五格「新生入口待驱动」2026-07-29 #707 裁定，ADR 0004 补充一）。与「代际（Generation, n）」（声部嵌套代数）是两个词——本条指代码谱系的世代。
_Avoid_: 用「老代码/新代码」口语判名分；触及 deprecated 代码开实施票而不先核名分

**原生 Cand^δ（tower-native candidate）**:
塔内原生化的背驰段候选 = **结构宽候选**：级别 ℓ 一次破中枢结构候选的完整证据包（破中枢 ∧ A 配对 ∧ 映射成立），力度确认**不进候选身份**——力度是候选之后的独立门，各级门政策一级一裁（level0 免门见 #522）（2026-07-28 #537 裁定，ADR-0006）。与 nest 管线三套 Cand^δ 实装（互相不等值、独立对照）是两个词。
_Avoid_: 用「候选」指力度已确认的对象；把力门判据焊进候选定义

**Sub 边（区间套包含边）**:
相邻级区间套的包含判据 = **C⊆C**：子级背驰段区间 ⊆ 父级背驰段区间，双边同口径 interval_b（source_index 坐标域，闭区间包含，相切算包含——#246 相切=重合全域生效）（2026-07-28 #537 裁定，ADR-0006）。旧 nest 门的 I(A)⊆D_parent 形态留对照侧，不进原生定义。
_Avoid_: 拿子的 A 段套父的 D 段当区间套判据（两边不同种对象）

**背驰段候选事件（divergence-segment candidate event）**:
塔内原生一等事件对象（2026-07-28 #540 裁定，ADR-0007）：承载 E2E-D1-D3 结构载荷 + 背驰段区间（C 段，interval_b 同口径）+ 状态与钟；classify_impl 主路径按级内联产出（非 sidecar 事后扫），每级 append-only 事件流存储，生命周期走 E2E-O 修订协议（修订只追加、observed/first_provable 一次写、终态不复活、同 as_of 重跑零 Delta）。身份键 = 结构身份键（level + 中枢对 + seg_a + c_start + B_p 指纹；c 段右端与 as_of 不入键，生长走修订）。
_Avoid_: 当每个走势单元的固有字段（事件非属性）；用删除模拟失效；事后 sidecar 重扫冒充主路径产出

**级别链证书（tower chain certificate）**:
塔内原生一等谱系对象（2026-07-29 #636 裁定、#641 落地；`TowerChainCertificate` 独立于 NestCertificate）：节点 = 背驰段候选事件身份键（沿用 N1 键），边 = C⊆C Sub 包含（N2 谓词在存活端点间统一裁）+ skip 边（缺席/跳级如实记，禁伪造中间级证书）。终态：Closed = 链头 Confirmed + 链不可再扩展 + **≥1 有效链段**（谓词判过的边，含 skip 边，空集不真空成立）；Invalidated = 谓词判不过或链头 Invalidated（不复活）；否则 Open。证伪节点留痕不判死上级；Absent（查无）非证伪，不落 Invalidated。谱系 append-only，路径扩展走 extends_lineage_key（查簿命中才写）。
_Avoid_: 链头独活真空 Closed；把查无当证伪判死；伪造中间级补链；与 NestCertificate 同名混用

### 运行边界

**「生产」拆三义（three meanings of production）**:
**π 引擎**指 canonical `run_theta_v0_pi`/π fill 的策略与执行家族本体；**最小生产路径 CLI** 指 `theta_backtest` 用仓内数据加载、π 引擎与内部 fill 串成的可运行闭环，它证明 CLI 可驱动引擎，但不等于交易场所集成；**NT 真生产**指 NautilusTrader 的 Backtest/Live runtime、真实 portfolio/order 生命周期与 broker adapter 驱动同一策略。三者须按全名使用，不以裸称“生产”互相替代。

### 出场判定

**谓词（Predicate, P_j）**:
描述市场/声部状态的原始条件，多个可同时为真。

**通道（Channel, C_j）**:
first-match 互斥化后的谓词：`C_j = P_j ∧ ⋀_{k<j} ¬P_k`。每声部每时刻恰命中一条通道。
_Avoid_: 判断题、规则、分支

**兜底通道（C0）**:
所有 P_j 均不命中时命中的通道，裁决为 Hold。

**出场裁决（typed exit）**:
通道命中后每声部每时刻恰得一枚：`{CloseRoot, ReduceCore, CloseShortDiff, RiskExit, Hold}`。Hold（继续持有、本刻无动作）也是一种合法裁决。英文保留 typed exit 作文献对照。
_Avoid_: 出场类型（Hold 不是出场）

**CloseRoot**:
一类反向证书触发的本级该方向全部清仓。

**ReduceCore**:
三类反向证书触发的核心仓减仓，不全清。

### 短差与记账

**短差（ShortDiff）**:
父声部持仓期间，由子级反向证书**显式**开启的子级反向对冲声部（`σ_u = −σ_{p(u)}`，父仓不动）；子级同向证书平掉该声部。
_Avoid_: "清仓再入场"（净头寸轨迹不等价，不是同一策略的另一种记法）；"自动涌现"（净额视角不可见，已被证伪）

**反向根（Reverse Root）**:
仅存在于最高级别走势类型：头顶无有方向父容器时，旧根声部子树清仓后，以**根声部身份**开立的反向声部。准入即本级别反向证书（证书已含区间套＋背驰确认），无额外门。与短差互斥且完备：任一反向声部，有父即短差、无父即反向根，不存在第三种，也不存在同时双记。
**时序定理（涌现互斥）**：反向根在世期间，涌现天花板不可上移——更高级别涌现的必要前件（本级走势完成＋反向重叠）包含本级买点证书成立，而买点证书即反向根的对称平仓。故不存在"持仓中身份跨涌现边界"的场景。
**事后恰好性**：涌现之后回望，已闭合的反向根腿在账上占据的位置，恰好等于新参照系下一条短差——身份是参照系视图，非存储事实，账本零身份字段。
_Avoid_: "独立做空"（不是绝对方向，是相对无容器）；"一笔两账"/"跨级别双记"（不存在该情形）；"改挂/认父/重分类"（时序定理下无此场景）

**腿（Leg, e_v^±）**:
分账本中一个声部的多、空两条相互独立的记账线。

**分账本（P_sep）**:
按腿分记的账本 `∏_v (R≥0 e_v^+ ⊕ R≥0 e_v^−)`，禁止只记净额。
第一性理由：多腿平于卖点、空腿平于买点，两腿出场点分属不同结构，不存在"同时同价平净额"的一点——故逐仓分腿是结构必然，非风控偏好。

**毛暴露（Gross Exposure）**:
两腿绝对值之和。资金分配递归按毛暴露约束（子对冲额度 ⊆ 父级短差额度）。
_Avoid_: 不区分毛/净地说"仓位大小"

**净暴露（Net Exposure）**:
两腿之差。仅净额（NAV）视角只能识别净头寸，看不见短差。

**亏损往返（Losing Round-trip）**:
买回价 > 卖出价的短差往返。记账口径（2026-07-27 #367 项四裁定）：**如实入账**——现金门分两族：货透支（holding<0）=记账错误照拒不误；现金变负（free<0）由真实亏损造成 = 放行入账。桶现金为负的语义 = 短差累计倒贴、主仓成本基承担（成本升高的账本表达），影子账（报告层）含负如实呈现。
_Avoid_: "被拒的往返不记账"（旧口径，系统性偏乐观已废）
