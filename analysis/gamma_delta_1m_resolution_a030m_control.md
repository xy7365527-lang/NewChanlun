# 30m 分辨变量搜索结果：Γ→交叉边 剩余熵压缩（**对照实验**：同期货实例只改 a0，隔离 a0 效应——回应 Gemini 裂隙2）

> 生成：2026-06-08 12:35　脚本：`analysis/gamma_delta_1m_resolution.py 30m`

> **a0 = 30m**（databento 1min UTC→30m OHLC 重采样）；**numeraire M = DX 期货**；窗口 2018-12-26 → 2026-06-05（~7.4yr）；网格 81,356 bar；日采样 2297 天；缠论 σ 全部 **Rust** `newchan_rust.RecursiveOrchestrator`。

> 上游对照：30m a0 版（`gamma_delta_full_results.md`）H(X|Γ)=2.461 → 贪心 0.812 bits。

> **⚠️ 注意实例不同**：30m 是**正典** {SPY,DBC,VNQ,UUP}（X=**3** 交叉边，H(X)=3.941）；本 1min 是**期货折叠通道** {ES,GC,CL,DX}（X=**2** 交叉边，H(X)=3.126）。顶点/X 维度/窗口/numeraire 全异 → 数值不可同表比较，只能比结构结论。


---

## 核心解读（编排者假设的否证）

**编排者假设**：30m 用日线 a0 致配置 σ 退化（10 年仅翻转 1-2 次），换 1min a0 让 σ 不退化 → 剩余熵可补到 0。

**实测裁决：假设前半存疑，后半否证。**

1. **σ 非退化 = 存疑**：1min 顶层走势 σ 在 2297 天翻转 **17–87 次**（30m 仅 75 次），+/0/− 占比近 1:1:1。配置 σ 翻转有限——假设的诊断成立。

2. **剩余熵补到 0 = 否证（强否定性结果，L2）**：贪心从 H(X|Γ)=2.275 仅经 5 步（['6E', 'DX', 'BRN', 'ω', 'VIX']）即停在 **0.418 bits**，其余候选真实增益 < 0.005 阈值。**不是「补到 0 需变量暴增（过拟合）」——是变量根本用尽仍到不了 0**（样本/cell=4.38 健康，贪心因真实增益转负而提前停，非样本切碎假象）。

3. **机制——非退化与可定性是对立的**：a0 越细 → σ 翻转越频繁 → 交叉边 X 自身携带的熵越大 → Γ+外部变量能解释的**份额反而越小**。Γ→交叉边的一对多（294号 D 算子非线性、527号 σ 不保比值同态）是**分辨率不变的结构性质**，不是粗采样假象。细化数据只**暴露更多不可约熵**，不能消除它。

4. **跨域结构结论**：唯一合法跨 a0 论断——σ 非退化假设确认，但非退化**未带来** Γ→X 可定性提升（反向）。「配置不退化」不蕴含「配置可被低维变量集确定」。

**下游影响**：强化 k4_path_c「六边各自跑递归」——交叉边信息无法从 Γ+外部 regime 变量恢复，且该不可约性在最细 a0（1min）上**更强**而非更弱。M2 选股读交叉边不可用 Γ 极性+regime 代理。


## σ 翻转诊断（验证 1min 顶层走势非退化）

| 边 | 日 σ 翻转次数 | +/0/− 占比 |
|----|------------|-----------|
| ES/$ | 87 | 49%/31%/20% |
| GC/$ | 77 | 58%/25%/17% |
| CL/$ | 78 | 48%/27%/25% |
| ES/GC | 69 | 37%/36%/28% |
| ES/CL | 62 | 40%/33%/27% |
| ω=GC/CL | 68 | 44%/26%/30% |
| ZN/$ | 64 | 42%/28%/30% |
| 6E/$ | 17 | 17%/36%/47% |
| BRN/$ | 70 | 44%/32%/24% |
| DX(abs) | 76 | 41%/36%/23% |

> 30m 版顶层 σ 在 2620 天翻转 75 次。1min 翻转数见上——若显著 >0 则未退化。


## 结果包（六要素）

**1. 结论**：
(a) 基线 H(X)=3.126，H(X|Γ)=2.282，H(X|Γ,ω)=1.842（ω 增益 0.440）。
(b) 全 8 候选+ω 贪心选中 **['6E', 'DX', 'BRN', 'ω', 'VIX']**，终态 H(X|Γ,选中)=**0.418 bits**（**未到 0**，残余结构性不确定）。
(c) **剩余熵补到 0 = 否**。1min a0 + 全候选仍残余 0.418 bits → 见过拟合诊断与边界条件。


**2. 定义依据**：Γ=(σ_P,σ_C,σ_R) 折叠通道期货实例 {ES,GC,CL,$=DX}（`config_space.Configuration`，528号折叠通道）；σ=涌现最高级别走势方向（`current_moves()[-1]`，Rust 引擎逐 bar 递归 6 级）；交叉边 X=(σ(ES/GC),σ(ES/CL))，GC/CL 剔除（=ω 防循环，30m 版同口径）。

**3. 边界条件**：
- a0=1min，M=DX 期货（非 UUP——UUP 是 ET/RTH 无时区，与 databento UTC 24h 不对齐）；
- 窗口 2018-12-26+（DX 起始限制），7 标的 1min 交集 81,356 bar；
- 跨 a0 数值**不可与 30m/日线同表比较**（不同有效域：a0/窗口/numeraire 全异，formalization-validity-domain 规则）——只比结构结论；
- gold=σ(GC/$)、oil=σ(CL/$) 在期货实例下 **= Γ 主边**（冗余），预期真实增益≈0；ZN/$ 是 10Y 票据 vs 美元（久期/利率代理），**非曲线斜率**（单期限无法构曲线）；
- 多变量条件熵含有限样本伪增益（已 shuffle 扣减；真实残余 ≥ 报告值）；
- 翻转**结论翻转条件**：若 σ 翻转诊断显示某主边翻转≈0（退化），则该边的 Γ 分量无信息，H(X|Γ) 基线本身失真，需换 a0 或 numeraire。

**4. 下游推论**：
- 剩余熵未到 0（0.418 bits）：Γ+全候选仍不足以确定交叉边 → **强化** k4_path_c「六边各自跑递归」的信息论依据（交叉边信息无法从 Γ+外部变量恢复）；
- M2 选股读交叉边不能用 Γ 极性+regime 变量替代联合读数。

**5. 谱系引用**：527（σ 走势方向态）、k4_path_c（六边各自跑）、482（ω 部分分辨）、§10（Δ 守恒分类）、294（D 算子非线性，Γ→交叉边一对多的算子层根因）、528（折叠通道顶点）、`project_recursive_orchestrator_rust`（Rust 引擎）；不确定是否有「1min 分辨」专门谱系——若结论稳定建议新建。

**6. 影响声明**：新增 `analysis/gamma_delta_1m_resolution.py` + `gamma_delta_1m_resolution_a030m_control.md`；不改源码、不改 30m 版结果（不同有效域并存）。


## 单变量真实增益（在 Γ,ω 之上）

| 候选 | 观测增益 | shuffle 伪增益 | **真实增益** | states | N |
|------|---------|--------------|-----------|--------|---|
| 6E/$ | 0.572 | 0.117 | **+0.455** | 3 | 2297 |
| ZN/$(rate) | 0.505 | 0.117 | **+0.387** | 3 | 2297 |
| DX(abs) | 0.470 | 0.119 | **+0.351** | 3 | 2297 |
| slope(2s10s) | 0.462 | 0.120 | **+0.342** | 3 | 1857 |
| VIX | 0.440 | 0.140 | **+0.301** | 3 | 1871 |
| BRN/$ | 0.417 | 0.119 | **+0.298** | 3 | 2297 |
| gold(GC/$) | 0.000 | 0.116 | **-0.116** | 3 | 2297 |
| oil(CL/$) | 0.000 | 0.118 | **-0.118** | 3 | 2297 |

## 最小变量集贪心搜索

| 步骤 | 加入 | H(X\|...) | 真实增益 |
|------|------|----------|---------|
| 0 | Γ | 2.275 | — |
| 1 | 6E | 1.758 | +0.412 |
| 2 | DX | 1.248 | +0.367 |
| 3 | BRN | 0.851 | +0.230 |
| 4 | ω | 0.570 | +0.132 |
| 5 | VIX | 0.418 | +0.039 |

> 选中集：**['6E', 'DX', 'BRN', 'ω', 'VIX']**；终态 H(X|Γ,选中)=0.418 bits。


## 过拟合诊断

- 终态上下文 cell 数 = **424**（理论上限 6561）；
- 样本/cell = **4.38**（公共样本 N=1856）；
- **过拟合风险 = 中/低**：样本/cell ≥3 → 条件熵估计相对稳健，但跨 a0 外推仍受单窗口限制（L2，非 L3 多窗口）。
- 变量数：选中 5 个达到 0.418 bits。变量数膨胀（≥5）且未到 0 → 典型过拟合症状（加变量只切碎不增信息）。

