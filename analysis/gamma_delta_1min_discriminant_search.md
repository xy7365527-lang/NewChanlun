# 1min 分辨变量搜索（O(N) 引擎重跑）：Γ→交叉边 剩余熵 H(X|Γ) 压缩

> 生成：2026-06-08 23:12　脚本：`analysis/gamma_delta_1min_discriminant_search.py`　总耗时 654s

> **引擎切换 O(N²)→O(N)**：上游 `gamma_delta_1m_resolution.py` 用流式 `RecursiveOrchestrator.process_bar`（笔尾变化全量重算 segments → O(strokes²)）。本脚本用 **day-batch**：独立 `BiEngine` 流式产笔（O(N) 摊还）+ 仅每日末对 stroke 前缀跑同一批量管线（~2297 次而非 ~strokes 次）→ O(days×strokes)=O(N)。

> **bit-exactness**：σ 仅依赖 `current_moves()`，process_bar 内部正用同一批量管线对同一 stroke 前缀算 moves（短路缓存=纯性能）→ day-batch 输入相同 ⟹ 输出逐位等价。`analysis/_verify_on_engine.py` 实测 400k 前缀 abs/ratio 均 **0 diff**（stream 9.1/12.7s vs batch 2.2/2.7s）。**引擎切换不改任何 σ 数值 → 不改 H 结论的有效域。**

> **a0 = 1min**（databento UTC，无重采样）；**numeraire M = DX 期货**；窗口 2018-12-26 → 2026-06-05；网格 1,878,657 bar；日采样 2297 天；σ = L1 走势方向（`current_moves()[-1]`，非最高递归级别——见 project_highest_level_sigma_frozen）。

> 实例：期货折叠通道 {ES,GC,CL,DX}（528号；X=**2** 交叉边 ES/GC,ES/CL，GC/CL=ω 剔除防循环）。跨 a0/实例**不可与 30m 正典 {SPY,DBC,VNQ,UUP}（X=3）同表**（formalization-validity-domain）。


---

## 核心解读

**目标**：把 H(交叉边|Γ) 压向 0（=Γ 完全确定交叉边 → k4_path_c 六边各自跑可被推翻）。

**实测裁决：剩余熵补到 0 = **否证**（强否定性结果，L2）。**

1. **σ 非退化 = 确认**：1min L1 走势 σ 在 2297 天翻转 **638–984 次**（粗 a0 仅个位/十位数）。

2. **剩余熵补到 0 = 否证**：贪心从 H(X|Γ)=2.763 经 2 步（['ω', '6E']）停在 **2.074 bits**，其余候选真实增益 < 0.005。**变量用尽仍到不了 0**——非过拟合切碎假象（样本/cell=7.93，贪心因真实增益转负提前停）。

3. **机制（非退化与可定性对立）**：a0 越细 → σ 翻转越频 → 交叉边 X 自身熵越大 → Γ+外部变量能解释的份额反而越小。Γ→交叉边一对多（294号 D 算子非线性、527号 σ 不保比值同态）随分辨率细化**单调放大**不可约熵。

**下游影响**：强化 k4_path_c「六边各自跑递归」——交叉边信息无法从 Γ+外部 regime 变量恢复，且该不可约性在最细 a0（1min）上更强。M2 选股读交叉边不可用 Γ 极性+regime 代理替代。


## 结果包（六要素）

**1. 结论**：
(a) 基线 H(X)=3.165，H(X|Γ)=2.762，H(X|Γ,ω)=2.517（ω 增益 0.245）。
(b) 全 8 候选+ω 贪心选中 **['ω', '6E']**，终态 H(X|Γ,选中)=**2.074 bits**（**未到 0**，残余结构性不确定）。
(c) **剩余熵补到 0 = 否**。


**2. 定义依据**：Γ=(σ_P,σ_C,σ_R) 折叠通道期货实例 {ES,GC,CL,$=DX}（528号折叠通道）；σ=L1 走势方向（`current_moves()[-1]`，O(N) day-batch 引擎逐 bar 产笔 + 每日末批量 seg→zs→move）；交叉边 X=(σ(ES/GC),σ(ES/CL))，GC/CL 剔除（=ω 防循环）。

**3. 边界条件**：
- 引擎从 O(N²) 流式换 O(N) day-batch，**bit-exact**（_verify_on_engine.py 0 diff）→ σ 数值与流式版完全一致，H 数值差异**仅来自数据/窗口**（若与上游 1m_results 同数据则应一致）；
- a0=1min，M=DX 期货；窗口 2018-12-26+；7 标的交集 1,878,657 bar；
- 跨 a0/实例数值不可与 30m/日线同表（不同有效域，formalization-validity-domain）；
- gold=σ(GC/$)、oil=σ(CL/$) 在期货实例下 **= Γ 主边**（冗余，预期真实增益≈0）；ZN/$ 是 10Y 票据 vs 美元（久期/利率代理，非曲线斜率）；
- 多变量条件熵含有限样本伪增益（已 shuffle 扣减，真实残余 ≥ 报告值；Miller-Madow 偏差使稀疏 cell 下 H 被低估 → 报告残余是**下界**）；
- **结论翻转条件**：若某主边 σ 翻转≈0（退化），该 Γ 分量无信息，H(X|Γ) 基线失真，需换 a0/numeraire。

**4. 下游推论**：
- 剩余熵未到 0（2.074 bits）：Γ+全候选不足以确定交叉边 → **强化** k4_path_c 六边各自跑；
- M2 选股读交叉边不能用 Γ 极性+regime 变量替代联合读数。

**5. 谱系引用**：527（σ 走势方向态）、k4_path_c（六边各自跑）、482（ω 部分分辨）、294（D 算子非线性，Γ→交叉边一对多根因）、528（折叠通道顶点）、project_recursive_orchestrator_rust（Rust 引擎）、project_bi_zhongshu_on2_two_walls（O(N²) 墙）；不确定是否有「1min 分辨」专门谱系——若结论稳定建议新建。

**6. 影响声明**：新增 `analysis/gamma_delta_1min_discriminant_search.py`（O(N) 引擎）+ `analysis/_verify_on_engine.py`（bit-exact 验证）+ `gamma_delta_1min_discriminant_search.md`；不改源码、不改流式版结果（同 σ 值，仅性能）。


## σ 翻转诊断（验证 1min L1 走势非退化）

| 边 | 日 σ 翻转次数 | +/0/− 占比 |
|----|------------|-----------|
| ES/$ | 955 | 42%/30%/28% |
| GC/$ | 972 | 36%/33%/31% |
| CL/$ | 984 | 38%/32%/30% |
| ES/GC | 976 | 34%/34%/32% |
| ES/CL | 959 | 34%/31%/35% |
| ω=GC/CL | 959 | 31%/34%/35% |
| ZN/$ | 924 | 33%/31%/36% |
| 6E/$ | 638 | 33%/34%/33% |
| BRN/$ | 973 | 38%/32%/30% |
| DX(abs) | 904 | 38%/30%/32% |

## 单变量真实增益（在 Γ,ω 之上）

| 候选 | 观测增益 | shuffle 伪增益 | **真实增益** | states | N |
|------|---------|--------------|-----------|--------|---|
| 6E/$ | 0.429 | 0.345 | **+0.084** | 3 | 2297 |
| DX(abs) | 0.421 | 0.345 | **+0.076** | 3 | 2297 |
| BRN/$ | 0.402 | 0.342 | **+0.060** | 3 | 2297 |
| ZN/$(rate) | 0.389 | 0.344 | **+0.045** | 3 | 2297 |
| slope(2s10s) | 0.327 | 0.312 | **+0.016** | 3 | 1857 |
| VIX | 0.385 | 0.377 | **+0.008** | 3 | 1871 |
| gold(GC/$) | 0.000 | 0.345 | **-0.345** | 3 | 2297 |
| oil(CL/$) | 0.000 | 0.345 | **-0.345** | 3 | 2297 |

## 最小变量集贪心搜索

| 步骤 | 加入 | H(X\|...) | 真实增益 |
|------|------|----------|---------|
| 0 | Γ | 2.763 | — |
| 1 | ω | 2.515 | +0.059 |
| 2 | 6E | 2.074 | +0.022 |

> 选中集：**['ω', '6E']**；终态 H(X|Γ,选中)=2.074 bits。


## 过拟合诊断

- 终态上下文 cell 数 = **234**（理论上限 243）；
- 样本/cell = **7.93**（公共样本 N=1856）；
- **过拟合风险 = 中/低**：样本/cell ≥3 → 条件熵估计相对稳健，但单窗口 L2（非 L3 多窗口）。

