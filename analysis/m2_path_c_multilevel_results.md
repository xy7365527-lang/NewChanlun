# K4 C 路径多级别递归 → E 背驰定位器端到端回测

工作周期：5min 聚合期货。**单级别** = level-0 走势方向 + 最近买卖点；**多级别递归** = L2 PH 最高级别方向 + L1 操作级背驰买卖点（与 E 版本进出场同构）。
K4 时间线：754 天（2024-01-01 → 2026-06-04）。

## C 路径诊断（圈闭合纠错 + 排序分布）

### 单级别（level-0 σ）

- 圈闭合纠错：741/754 天检出读数矛盾（dissent>0），累计纠错估计数 2064。
- equity favored 方向分布：{'FLAT': 28, 'DOWN': 325, 'UP': 401}
- 最强做多资产排序首位分布：{'ES': 250, 'CL': 225, 'GC': 279}

### 多级别递归（L2 σ）

- 圈闭合纠错：541/754 天检出读数矛盾（dissent>0），累计纠错估计数 1588。
- equity favored 方向分布：{'FLAT': 257, 'UP': 237, 'DOWN': 260}
- 最强做多资产排序首位分布：{'ES': 289, 'GC': 326, 'CL': 139}

> dissent>0 = 某派生边直接读数被另外两条估计路径（过现金/过第三资产）否决——六边冗余相对三边代数推导的检错增益。多级别用 L2 σ 做圈闭合（任务要求级别对齐，不混级别）。

## 对比表：E vs E+C(单级别) vs E+C(多级别递归)

| 版本 | 交易数 | 复利收益% | 胜率% | Sharpe | MaxDD% | 相对 E 留存 |
|------|--------|-----------|-------|--------|--------|-------------|
| E（无方向输入，纯背驰基线） | 7 | +932.77 | 42.9 | 0.496 | -42.54 | 100% |
| E+C 单级别（level-0 方向+最近买卖点） | 7 | +872.97 | 42.9 | 0.491 | -42.54 | 94% |
| E+C 多级别递归（L2 方向+L1 背驰买卖点） | 4 | +94.03 | 50.0 | 0.491 | -27.37 | 10% |
| Buy&Hold | — | +307.08 | — | — | — | — |

> **核心判定（L2）**：多级别递归 **劣于** 单级别（多级别 +94.03% vs 单级别 +872.97%）。

## 门控统计

| 版本 | bars | 起 | 止 | 交易日 | 拦截日 | 掩码入场 |
|------|------|----|----|--------|--------|----------|
| 单级别 | 447739 | 2024-05-10 | 2026-06-02 | 516 | 239 | 44 |
| 多级别 | 447739 | 2024-05-10 | 2026-06-02 | 516 | 377 | 69 |

## 结果包六要素

**结论**：见上述对比表（E vs E+C 单级别 vs E+C 多级别递归）+ 双诊断。

**定义依据**：
- 六条边 = 主边 ES/$,GC/$,CL/$（生成树）+ 派生边 ES/GC,ES/CL,GC/CL，各自独立跑引擎（compute-once 缓存，每边算一次）。
- **单级别** σ = level-0 settled-move 方向；买卖点 = type1/2/3。
- **多级别** σ = L2 PH 翻转累积态（最高级别走势方向，walk 取最高级）；L1 买卖点 = 操作级走势背驰（底背驰买 down_move_settled∧entry_div_ok / 顶背驰卖 up_move_settled∧exit_div_ok，37 课 MACD 面积背驰确认内化）。
- 圈闭合纠错 = K4 每资产对 3 条估计路径多数表决（k4_path_c.closure_consensus，**同级别 σ 闭合，不混级别**——单级别用level-0 σ、多级别用 L2 σ，由 EdgeReadingLike.sigma 协议保证）。
- 联合读数 = ES-incident 边方向票+买卖点票之和（k4_path_c.joint_reading，target=EQUITY，OKLO 是动产）。
- E = run_swing_trading(MODE_NONE)：底背驰买入、L2 趋势顶背驰清仓，纯多头。

**边界条件**：
- 六边建立在 ES/GC/CL 公共时间戳交集上，圈闭合恒等式价格层精确成立。
- 多级别方向票/买卖点票权重 W_DIRECTION : W_L1_DIVERGENCE_BSP（默认 1:1）是 [新缠论:选择] 可调常数——比值>1 买卖点主导（反转择时）、<1 方向主导（趋势跟随）。改比值移动 favored 临界 → 翻转门控结果。
- 门控仅作用于入场，不强制平仓（E 引擎自身 L2 顶背驰离场）。
- 无前视：date X 仅用 date<X 的 C 路径 favored（滞后一日）。

**下游推论**（本次 OKLO 运行的**确定结论**，非条件假设）：
- **多级别递归劣于单级别**（+94.03% vs +872.97%，留存10% vs 94%）：级别细化（L2 方向+L1 背驰买卖点）在 OKLO 强上行单股票上**收缩了有效域**——L2 方向latch 更保守（多级别 favored FLAT 257 天 vs 单级别 28 天），拦截更多（377 vs 239 日）→ 入场更少（4 vs 7 笔）→ 牛市削减暴露更狠。**不得包装为多级别'有效'**（formalization-validity-domain）。
- **两路径均 < E 基线**（+932.77%）：方向门控本身在牛市削减暴露——确认 project_divergence_gate_entry_exit_falsified（门控砍入场=减暴露=强趋势中跑输），级别细化不改变该否定方向，只改变削减幅度。
- 多级别在 60min/30min TF 下退化为空有效域（走势层不 settle、L2 PH 不翻转）；本结论仅在 TF=5min（走势层富集）成立——级别体系的成立依赖足够细的工作周期。

**谱系引用**：project_omega_regime_falsified（C 路径不走 ω 压缩）、project_divergence_locator_entry_exit（E 版本进出场，多级别与之同构）、527号（config σ 走势方向态）、project_backtest_benchmark_falsifiability、526号（a0 递归本体论）。

**影响声明**：k4_path_c.py 增 EdgeReadingLike 协议 + LeveledEdgeReading（圈闭合/联合读数泛化为级别对齐，单级别零行为变更）；本回测脚本增多级别时间线 + 三栏对比。不修改引擎/k4_integration/config_space/fugue。

**认识论等级**：圈闭合纠错机制 L0；单/多级别单边读数 L2；管线连通 L1；三栏收益对比 L2（单标的，可证伪；非 L3 交叉验证）。
