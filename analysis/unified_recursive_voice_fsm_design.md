# 统一递归 voice FSM 设计 v2——双向对称×矛盾驱动推导

> v2（2026-06-12）：按编排者两项修正重写——(1) 双向对称根本约束（voice 方向跟随级别走势，卖点=翻空、买点=翻多，无 FLAT 驻留，子 voice 方向独立，绩效=Σ|各段涨跌幅|）；(2) 方法论倒转——从 15 条矛盾向下推导 FSM（§6 完整性矩阵），而非从模块向上抽象。
> 推导过程：10 个并行矛盾推导/对抗工位（A1-A8 推导 + C1/C2 对抗，其中 A5/A6 由综合者按在册判决补推导），全部产出以（文档:章节, 验证等级, 逐字引用）锚定。
> 认识论等级：本文档主体 L0（设计推导），全部经验主张标注其在册等级；§9 预注册等待 L3 裁决。
> **v1→v2 的三处自我否定**（090号诚实义务）：
> ① v1 发现三的 nest 归因是**误读**——代码核验（positional_fusion.rs:1018 `sell_any.get(k) || nf_sell[k]` 同分支过停削；:1244-1250 注释"门在词汇之后…一视同仁"）与来源文档逐字（nesting §5.4"v1 实装对趋势相停削已天然免疫（停削窗口内 nest 触发同样被拦）"）一致否证"nest 绕过停削门"；BTC/CL 伤害的真根因 = **Φ 读数假阴性**（trend 行假但实为大牛市——26:80 豁免未下沉的覆盖缺口）。门共享从"白名单消解的充分修复"降格为"防未来绕过的架构原则"（C2 裁决）。
> ② v1 行 4（49:54 出口）缺正域守卫——BTC 上收窄豁免的全部 L3 历史是两次崩塌（fusion_p 比值 0.300 / fusion_pr 0.366，P6/P7 在册），49:54 短差义务重开会在 BTC/CL 牛市放血。v2 §9 U3 增设 BTC/CL ≥0.95 守卫分量。
> ③ v1 的 17 行行为表无 type2 专属行（矛盾 13 未被承载）、Φ 时钟单向二值（在册代码注释逐字"trend×dir==Up → MoveUp，否则 Osc（无 MoveDown）"）——双向公理要求三值化。v2 §4 补行 R14-R20。
> 另：勘误——任务背景中"44课：第二类买卖点一般是介入最好的位置"引文**不存在于原文**（grep 全文核验），正确锚点 = 086:80（正文介入程序）/ 053:28（二分定理）/ 014:324（答疑级）。

---

## §0 编排者公理的严格形式（对抗审查后的存活形态）

### 0.1 双向对称公理：逻辑层全称成立 × 物理暴露三值化

编排者公理："voice 方向跟随该级别走势方向，卖点=翻空、买点=翻多，永远在市场有方向，无 FLAT 驻留。"

C1 对抗审查：六条攻击在公理的**字面无条件形式**上全部命中（ES/QQQ 反手做空全层全形态为负且镜像 anc 窗口救不了 ES，L3；空头增量 = (标的×层级) 二维 regime 符号函数，BTC move +1.583 / seg −2.161 同标的内分裂，L2；84:316 为语料唯一直接做空判决且为负）。存活形式 = 三处结构修改：

**修改一（方向/暴露分离）**：公理在**逻辑状态层**全称成立——voice 极性 d ∈ {+1, −1} 恒有定义、每个反极性卖/买点恒触发翻转判定、FLAT 不是可驻留逻辑状态（双向会计 §5.3：FLAT = 翻转断面 bar 内测度零过渡，L0 在册）。但**物理暴露三值化** exec ∈ {linear（真实多/空头）, gated（零暴露+活跃义务）, put（凸性载体）}：

- **Gated ≠ FLAT**。Gated 是 d=−1 逻辑状态的零暴露执行形态：携带 MoveUp 强制回复、买点+R2（c≤ZD）回复、MoveDown 停回复三组活跃义务——方向承诺非空、市场暴露为零。**声明义务**（A2，090号）：任何描述 Gated 的文本必须同时给出"方向承诺"与"市场暴露"两个量，只说前者是声明膨胀。
- 在册 L3 直接支持：BTC 实盘默认候选 fusion_btrg_s34 本身就是 Gated 驻留臂（+6651.8，拿走双向增量 82.9%；bull α ghost −0.2508 ≈ tr −0.2505 **零差**——纯回复门牛市年零额外伤害）。编排者公理与零空头暴露部署由此兼容。

**修改二（翻转的层域条件化）**："每个卖点=翻空"的全称量词删除——物理穿零仅在：层 ∈ 翻转域（当前 D 类部署声明，见 §8.2）∧ 载体允许 ∧ 过成本门。细层卖点恒以削减-回补表达（永不穿零）；树根级翻空禁止（树根=出场；GC recL4 −0.806 / 84:316 逼空形态为尾部风险实证）。

**修改三（子 voice 反向 = 会计描述非独立机制）**：父持多 N、子开空 m≤N 与"削减 m 再回补"逐点等价（净额等价定理，双向会计 §3.3 L0）；cascade settle 禁 sign-label 原地翻转（父翻转 ⇒ 子树预算基消灭）。编排者句式的严格重写："子 voice 在反向线段中执行削减-回补（账上等价于持空 m≤N）；独立空头书仅在某层翻转断面成立（极性穿零，m>N 域）时按 cascade settle 四步序重开。"方向语义免费获得，FSM 零新状态。

**标注义务**：全部空头侧机制无原文锚（84课做空程序 = 意向串行切换、头寸恒非负，fatal 在册；38:36 字面授权的镜像是判断-动作序列与库存↔现金循环——即 Gated 形态，不授权净空头；38:20"避开所有下跌类型"是反向证据）。编排者公理的合法身份 = **原文沉默域的镜像填充 + 编排者决断**，全部空头条款永久携带 [镜像推导] 标注。

**直积表述的处置**：编排者的 {LONG,SHORT}×{走势类型}×{操作模式} 直积是**定义域声明非有效域声明**（230号直积退化同型风险：大量胞腔不可达，如 SHORT×Trend↑×停削；231号）。其严格替代 = §2 状态最小化三元组，操作模式不是独立维度而是 (d, Φ) 的查表。

### 0.2 绩效公理的严格形式

编排者："绩效 = Σ|各段涨跌幅|，不是跑赢 BH，牛熊震荡都赚。"

Σ|各段涨跌幅| = M·TV（望远镜恒等式，L0 在册），是**被完美预知达成的不可达上确界**（53:26 原文自承"按分笔的买卖点……在实际中是不可能的"）；细层因果捕获率仅 0.5-3.7%（BTC seg 甚至 −1.7%——细层连方向跟随都亏，slow_bull §7.2 L2），recL2 层 10.3-29.7%。47/53 分割（跨资产不变，在册）：约 53% 反向振幅在笔级 = a0 存在论下限以下，对**两个极性同等不可达**——双向不把笔级振幅升格为可达（持仓方向自然暴露段内笔级波动是 β 非 α）。

严格形式（C1 修复采纳）：
1. **度量轴**：公理作为绩效度量的定义成立——每级别报告捕获效率 **η = 实现 / M·TV_adm**（成本门截断后的可达全变差为分母），替代 BH 作为会计基准。双向相对单向的理论增量恰 = Σ|下行段|（admissible 层）。
2. **"牛熊震荡都赚"的可证伪形式**：bear α 与 bull α 分桶**同时非负**（§9 U7）。在册首个达成实例：btrg BTC bear +0.444 nats 且 bull 零差。
3. **通过线**仍由对照臂×份额线定义（不可达上确界不作通过线——231号有效域膨胀禁止）。

---

## §1 voice 的存在论（v1 §1 修订）

voice@k = 38课操盘程式在级别 k 上的双向实例：持有该级别配额 slice，极性 d 跟随该级别走势方向，按 026:34 恒仓量纲（M=N，单位数）操作。原文依据不变（026:34 / 038:28/30 / 084:14），新增双向限定：038:36"向下段的运作刚好相反，是先卖后买，从刚才向上段结束的背驰点开始，所有操作刚好反过来就可以"——字面授权**判断-动作序列的完整镜像**（在册裁决："镜像不含状态空间扩张"），即 Gated 形态；净空头是镜像填充。唯一参数 = a0（v1 §1.3 不变）。

---

## §2 状态空间 v2：双向最小化

### 2.1 存储状态（三元组，v1 定理保持）

```
VoiceState@k = {
  d: Polarity,                  // 逻辑极性 ∈ {+1, −1}，恒有定义（修改一）
  exec: Exposure,               // d=−1 时 ∈ {Linear(ShortBook), Gated, Put}——载体参数非状态维度
  sub: Riding | Out{snapshot} | Hinged{q},   // 段内短差/铰链在外态
  latches: { nest_win: Option<NestWin>,      // 矛盾1：candidate 武装窗口
             t2w: Option<T2W>,               // 矛盾13：type2 第二翻转窗口
             dead: bool,                     // 三卖否决 latch（49:52）
             restore_due: bool }             // 矛盾6：回补义务锁存（D7）
}
```

配对快照（开仓 bar 冻结，事后不可重读）：锚中枢 cs/ZD/ZG、candidate 极值、seg1_low、卖出股数、proceeds_basis（Linear）/ premium_basis+合约元组（Put）。

**earning 不入状态空间**（A3 四步推导，v1 缺页补全）：(i) earning 不是账本事实——相变前后 slice 物理位置不变，只切换守恒律算术分支，行为表无一行以它为触发；(ii) 测度：十标的 10,651 笔仅 1 笔触发（0.0094%），且唯一样本是 T5 疑点（相变宣告时挂着 −59K 旧腿，"earning 状态实质虚假"，定义冲突三候选已上浮未裁决）；(iii) 对称恢复：降为账本内部标志后，FSM 状态空间在两极性严格同构，相变能力差异封装进 DirectionalBook 类型（Long 有相变 / ShortBook 单相单律 / PutVoice 复用 Long）——非法状态不可表示由类型系统承担；(iv) 15:976 earning 后行为差异（"只持有成本是0的仓位等待第二波"）是其唯一合法接触点 = gates 的第四个每 bar 读数（可重读账本事实），仍非状态维度。**架构选择显式声明**：若统一 FSM 收敛 fusion 基座（权益传导路径，无 cost_basis 概念），earning 连账本标志都不存在——该消解是架构选择非论证，登记 §6 矛盾14 行。

### 2.2 双坐标系分离定理（矛盾3 的承载，A3）

FSM 编码【对标的走势的 exposure 意向】，账本编码【对操作对象 K 线的持仓极性】，由载体符号连接：

```
exposure_dir(标的) = carrier_sign × book_d
线性空头: carrier_sign=+1, book_d=−1 ⇒ exposure=−1   （ShortBook：单相单律，损失无界）
认沽期权: carrier_sign=−1, book_d=+1 ⇒ exposure=−1   （PutVoice≅OrganicLedger，d=+1 多头书，有限期不败）
Gated:    carrier_sign=0             ⇒ exposure=0    （FSM 驻留 bearish 相，消费回复门词汇）
现货卖出:  = Gated 的物理实现
```

载体无关的 FSM 事实：翻转断面触发判据、cascade settle 四步序、M=N、MoveUp 强制回复、空侧 R2 门（c≤ZD）。载体相关渗漏封装在三处适配层：(i) 账本类型（DirectionalBook 变体）；(ii) 存活约束维度（线性=价格维影子价格 P_neg_short / 认沽=时间维 DTE 三区，roll 为合约级断面 FSM 零感知 / Gated=零约束）；(iii) 摩擦面（funding/借券 vs theta+spread vs 零）。账本不变式分类——载体无关：单位数恒正、协变传导 `basis ← basis − d·π/units`、cascade 断面、NAV 重建、嵌套守恒；载体相关：d 取值、相变可达性、金额守恒腿合法性、配额量纲（张数；delta 配额被 436号命名纪律否决）、存活维度。

**矛盾3 的非对称残余（诚实声明）**：空头 earning 不动点在线性载体 L0 不可构造（非对称定理两支：损失无界 ⇒ 不动点对任何有限 proceeds_basis 不可达；空头开仓是负债创设非现金换单位）。认沽载体两支同时消解但残余"有限期不败"（免费认沽到期蒸发）+ theta 负 carry + L4+ 容量帽（D_min≈958 天）。FSM 形式完全对称，账本律非对称，载体层吸收非对称——这是矛盾3 在 FSM 中的永久形态，不是待修复缺陷。

### 2.3 结构读数（每 bar 纯函数，零存储）

**Φ 三值化**（矛盾2，A2——v1 的关键缺陷修复）：

```
Φ(k) ∈ {MOVE↑, MOVE↓, OSC}，基座 = kind(k)×dir(k) 直读
极性协变豁免（freeze 读数，统一 v1 行1 与行15）：
freeze_d(k) = (trend(k) ∧ dir(k)==d 的有利方向) ∨ freeze_d(k+1)    // 递归传导，275号同构
  d=+1 ⇒ freeze=停削（冻结出口侧全部词汇，含 nest_sell）
  d=−1 ⇒ freeze=停回补（冻结 cover_buypt 与 cover_nest）[镜像推导]
```

错位定理双侧成立（L0）：in_trend 是 MOVE↑ 相的严格超集（在册 §2.2 定理）；镜像地 in_movedown 是 MOVE↓ 相的严格超集——下跌趋势内第 N≥2 个下移中枢的震荡期，49:54 镜像规定空侧短差应恢复，当前 MoveDown 停回补在该窗口持续拦截回补，结构上精确镜像 GC/DX 多侧失血机制。**但超集的"错误"部分有益的 P6 评价反转同样可能镜像成立**——双侧 49:54 出口都必须带正域守卫（§9 U3）。

gates（与 v1 同）：amp = θ_q(k) ≥ k_cost×friction（35课）；pos = c vs ZG/ZD 位置（d 协变：d=+1 削减@c≥ZG / d=−1 回补@c≤ZD）；第三读数 = 93课 g2 强弱震荡；第四读数 = ledger.phase（earning 标志，见 2.1）。

---

## §3 主要矛盾的承重结构

### 3.1 矛盾1（确认滞后 vs 操作时效）——FSM 承载而非消解

精确形式：confirmed = 右侧结构完成，定义上后验（1s 同尺度原生滞后仅压 −11~−24%，不可由细化 a0 消解）；滞后是级别阶梯函数（~12b/~120b/~3000b/~50000b，×10-25/级，八标的一致）。零滞后确认逻辑不可能（27课必要条件逻辑：次级别背驰是本级别背驰的必要非充分条件）。

FSM 的承重机制 = **翻转断面三段式**（candidate/confirmed 双流 + 否定守卫是矛盾的永久形式）：

```
武装（candidate Type1/Type3 → NestWin 锁存）
→ 提前段（次级别第一同侧证据 ⇒ 与 confirmed 同词汇地位的更早时间坐标；44:44 铰链先出一部分）
→ 完成（confirmed ⇒ M=N 全断面）/ 否定（close 越过 candidate 极值 ⇒ 窗口作废，027:25 逆否）
```

1s 嵌套塔 = 同一 NestWin 机制的**证据源精化**（条件化嵌套 2.8-3.0×，K≥3 带；1s=定位精化器非操作床位，在册判决保持）——零新状态范畴。

**归约假说否证记录**（A1，L2 探索性）："矛盾7 归约到矛盾1（空头层级域 = 滞后/段长比阈值）"被三重反例否证：(一) 比值随层级**上升**而非下降（5/6 标的，BTC 0.018→0.149）；(二) 单一阈值判别力 17/24 = 平凡基线"全负"；(三) BTC seg 是全表最小比值格（0.018）却最负（−2.161），CL 的 +,−,+,− 层级交替不可能由任何单调于层级的标量产生。**推论**：空头层级域无已验证的内生标量读数，Short 准入留部署层白名单（§8.2）；任何内生化候选必须先通过 24 格符号表 ≥21/24 离线复现（含三个指定证伪格）才许进引擎（§9 M7-b）。

滞后轴的从属地位（A1）：ES gap 收窄 anc 50.1% vs nest 14.5%——慢牛标的主导项是削减义务有效域（26:80 豁免轴），滞后轴居从属。"解决滞后=全面超 BH"全称命题已被 N3 否证。

### 3.2 矛盾2（走势完全分类承重墙）——Φ 三值化 + 极性协变消费

分类不可消去（行为表每行以它为判别量）；可消去的是消费它的开关。已弥合：消费下沉为 trend_row/dir_row 磁带行局部直读（GDX 否证把年度 regime 从机制语言驱逐——"层级 move kind 与年度 regime 是不同分辨率的对象"）。残余三层：① kind×dir 超集错位（49:54 出口缺失，双侧）；② 消费符号按标的分裂（D 类）；③ anc 的 ∃j 全局扫描违反局部依赖 → 递归传导 exempt_d(k) 替代（双向参数化，TrendScope 枚举与 anc_down 谓词同时消失）。

下行侧的在册证据：in_movedown 是 ghost/btr 臂的**主导拦截器**（BTC 3,034 / CL 22,452 次，R2 仅 19/50 次）且承载 bear α 主体——但 MoveDown 停回补从未单独消融（与 R2/MoveUp/买点时点在 ghost 臂捆绑），拦截计数大 ≠ 贡献大（§9 M2-b 消融臂）。

### 3.3 矛盾3（空头 earning 不动点）——已由 §2.2 双坐标系分离承载

见 §2.2。开放轴：认沽第三分支（行 R14 的 exec=Put）待期权 K 线/小实盘 L2（合成 K 线 = L1 陷阱禁产 alpha，在册）；信号层双流（标的结构定战役方向 + 认沽自身结构定操作点）要求 voice 绑定第二条 bars 流——当前 LayerState 架构没有的自由度，列 §10 开放轴首位。

---

## §4 行为表 v2（双向镜像，R0-R20）

voice@k，逻辑极性 d，物理执行 exec。同 bar 优先序：**R2/R6 回补义务 ≻ R7 铰链 ≻ R1 豁免判定 ≻ R5/R12 新开 ≻ R10 路由**（空侧回补义务因损失无界升至最高优先，先于一切多侧动作）。

| # | 条件 | 行为 | 矛盾锚 / 在册依据 |
|---|------|------|------------------|
| R0 | 树根 Flat→Armed→开仓 | 区间套入场（candidate 武装+次级别确认） | 27课；runner FLAT/ARMED 在册 |
| R1 | freeze_d(k) 真 | **豁免**：冻结对极性不利侧全部词汇（d=+1 停削 / d=−1 停回补[镜]）——confirmed 与 nest 触发同被冻结（已是代码事实） | 矛盾2/5/8；26:80/49:52/53:34；空侧[镜像推导] |
| R2 | freeze_d 真 ∧ sub=Out | 强制回补/回平（满仓义务，资金不足 restore_due 逐 bar 重试） | 49:52；counter_sub 先例 |
| R3 | MOVE(d) 段背驰（trend div） | 全动作：树根=出场；中间层=翻转断面候选（R14 入口） | 49:54 前半；矛盾10 |
| R4 | MOVE(d) 内新中枢形成 ∧ 不背驰 | 该层 freeze **局部退出**，短差词汇重开（双侧；多侧 L0 / 空侧[镜]） | 49:54 后半；矛盾2 错位修复轴。**正域守卫**：U3c（BTC/CL ≥0.95 vs fusion_t——fusion_p 0.300/fusion_pr 0.366 两次崩塌负先验在册） |
| R5 | ¬freeze ∧ Φ=OSC_strong ∧ amp ∧ pos(d) ∧ d 侧卖证据 | 短差开：d=+1 c≥ZG 卖出全 slice / d=−1 c≤ZD 回补全部[镜]（角色互换：卖↔买证据、ZG↔ZD） | 矛盾9；49:64；osc_domain L3 |
| R6 | sub=Out ∧ d 侧买证据 ∨ 边界触线 ∨ 中枢死亡(非三卖) ∨ 中枢上移 | 如数接回（d 协变） | hold26/osc 出口集；49:54 语义 |
| R7 | sub=Out ∧ 本层 d 侧主卖点先到 | 铰链升级：短差身份事后授予为减仓出清→R14 三岔 | 矛盾8；44:44；双向会计 §5.1 渐进翻转 |
| R8 | 锚中枢被 confirmed type3(d 协变) 终结 | dead latch：回补通道收窄为触线/铰链/树根清算 | 49:52"不能回补了"；o3s |
| R9 | candidate type3 未决（离开段未回边界） | 冻结逆向开腿（只挡开不挡闭） | H1；49:68 |
| R10 | OSC ∧ ¬amp | 本层不操作，路由上扫 j>k 第一个 OSC_strong∧amp 层代管 | 35:30；H3/H4/fusion_u |
| R11 | OSC_weak（g2：区间脱离最近 Up 期末中枢） | 不参与 | 93:26；矛盾2 |
| R12 | ¬freeze ∧ candidate ∧ 次级别第一同侧证据 ∧ ¬破极值 | 顺向提前削减/回复（nest 正向）。**归因更正**：本行在 freeze 窗口内已天然被 R1 拦截（代码在册）；其负域伤害源于 freeze 假阴性（26:80 未下沉缺口），修复轴 = freeze_d 递归传导的覆盖质量，非本行接线 | 矛盾1/5；27课+038:258 |
| R13 | close 越过 candidate 极值 | nest/T2W 窗口作废 | 027:25 逆否 |
| R14 | Φ(k)=MOVE(−d) 确认 ∧ k ∈ 翻转域（D 类部署声明）∧ 树根除外 | cascade settle 四步序（①子树结算→②旧书平→③新书开 M=N→④子腿惰性实例化）→ **默认落点 Gated**；Short 升级 = 载体×白名单条件（A1 提案：ghost 拿走 82.9% 增量 + bull 零差 = 符号安全默认） | 矛盾10；38:36[镜]；双向会计 §5.2 |
| R15 | d=−1（Gated/Short）∧ −d 侧买点 ∧ pos（c≤ZD） | 回复/平空翻多（M=N） | 空侧 R2 门；在册 |
| R16 | Φ 翻回 MOVE(d_old) | 强制回复/平空（零强平路径） | MoveUp 强制；在册 |
| R17 | （消融位）cover→Gated→确认后翻多两步断面 | 把"提前回补"与"提前翻多"拆为两步——空侧提前动作代价不对称（假买点翻多进下跌市 = 直接亏损+churn，非对称于多侧踏空机会成本） | 矛盾5 双向扩展；ghost 镜像证据；§9 M5-e |
| R18 | confirmed type1 翻转被否（门拒/载体拒）∨ candidate 永不 confirmed（小转大 orphan） | T2W 锁存武装（extreme = type1/candidate 极值） | 矛盾13；53:28 |
| R19 | T2W 武装 ∧ confirmed type2(−d 侧) ∧ 门放行 | 第二翻转窗口：翻转断面（当前代码不存在的路径——否后重试） | 矛盾13；086:80/053:28/017:66 定律一 |
| R20 | close 越过 T2W extreme | 清锁存（86:80 自带止损规则的镜像） | 矛盾13 |

**出口三分注记**（A4）：该不动（R1 freeze）≺ 该提前（R12，震荡相削减的时点精化）≺ 该立即全动（R3，27课区间套的原文本职 = 大转折点定位）——nest 的两个角色分属 R12 与 R3 的时点精化，行为表不混置。

**type2 的非行声明**（A7）：Buy2 专属闭腿不入表（T2c 1次/−0.8pp L2 + cycle38 不可达 L3 双重否证，留 buy_any 匿名词汇由 R6 覆盖）；Sell2 不入 nest 窗口（confirmed 为同 bar 价格比较，无滞后可消，L0 在册）；第一窗口匿名消费（R5/R14 经 sell_any）保留，只补归因计数器。子 voice 入场侧 type2 零新机制（定律一：type2@k ≡ type1@(k−1)，递归 FSM 自动免费）。

---

## §5 递归规则 v2

1. **常驻配额 × freeze_d 递归门控**（v1 §4.1 保持，双向参数化）：voice 树常驻，活动由 freeze_d(k) = (trend∧dir==d有利)(k) ∨ freeze_d(k+1) 门控——每层只看直接父亲（275号），归纳展开 ≡ SelfOrAncestor，TrendScope 三值枚举消失。下行版本的在册对象 = anc_down（B5：BTC +230pp 开 / CL −203.6pp 关——**递归式的下行符号分裂原样存在**，内生化消解的是读数来源不是符号分裂）。
2. **子 voice 方向 = 会计描述**（修改三）：m≤N 域零新机制；净额等价可执行守卫——双账本与单账本 NAV 逐 bar bit-equal，违背 = 偷渡伪新机制。
3. 递归终止三范畴不变（a0 存在论下限 / 35课成本门 / 结构参照不可定义）。
4. 翻转断面的资金自融资：margin = 平多所得，翻转 bar pool 净流转 0（代码在册 :1074-1076）——双向下资金竞争在翻转点被构造性排除（矛盾6 的双向分量；升守卫 §9 M4-d）。

---

## §6 十五条矛盾 × FSM 机制完整性矩阵（编排者方法论的核心交付）

| # | 矛盾 | FSM 承载机制 | 承载类型 | 在册等级 | 预注册 |
|---|------|-------------|---------|---------|--------|
| 1 | 确认滞后 vs 操作时效 | 翻转断面三段式（NestWin 武装→提前→完成/否定）+ 1s 证据源精化 + η 会计；**承载非消解**（candidate/confirmed 双流是永久形式） | 锁存+转移+读数 | L3（nest 5/8 分裂）/L2（1s 2.8-3.0×） | M1-a~d |
| 2 | 走势完全分类承重墙 | Φ 三值化 + freeze_d 极性协变 + 双侧 49:54 出口（R4，带正域守卫）+ 递归传导 | 读数 | L0 定理+L3（fusion_t）；下行镜像 L0[镜] | M2-a~c, U3 |
| 3 | 空头 earning 不动点不存在 | 双坐标系分离（exposure=carrier_sign×book_d）+ DirectionalBook 类型封装 + 载体适配三层 | 账本律+载体参数 | L0（两支证明）；认沽扬弃 L0 | M3-a~c |
| 4 | 门组合非单调 | 负交互三分类：交集收缩型（门共享**不解**——同范畴双门禁默认合取+交互项预注册义务）/ 读数覆盖缺口型（真根因——freeze_d 递归+R4 出口）/ 资金竞争型（已否证，无需机制） | 工程纪律+读数 | L2（VLg −44.9pp）/L3（D5 全否） | M4-a~d, U1b |
| 5 | 削减失血 vs 滞后失血 | freeze_d 统一读数（双向四象限）+ R17 两步断面（空侧代价不对称）+ R12 归因更正 | 读数+消融位 | L3（nest 二分）；空侧 L0[镜] | M5-a~e, U4 |
| 6 | 资金池耦合 | 共享池保留（earmark 否证）+ 义务优先序（空侧回补最高）+ restore_due 锁存 + 翻转自融资守卫；追价拒绝词汇→铰链出清列开放轴 | 优先序+锁存+守卫 | L3（D5 全否：推迟 +20~32% 反升） | M4-d |
| 7 | 空头增量二维 regime | **无内生标量读数**（归约假说三重否证）——Gated 普适默认（R14）+ Short 白名单留部署层 + 内生化唯一许可路径（24 格符号表门槛） | 部署层声明 | L2（24 格符号表；CL 交替） | M7-a~d |
| 8 | 级别错配（44课） | R1 freeze 递归传导 + R7 铰链三岔（+Q→+Q−q→{回补/强制回补/翻转}）；双向形式 = 双向会计 §5.1 渐进翻转 | 转移 | L3（hold26/anc 白名单） | U2/U3 |
| 9 | osc 白名单 | Φ/amp/g2 读数极性中立 + R5/R6 极性参数化 + 防膨胀定理（Long 子层反向恒为卖出-接回） | 读数 | L3（osc_domain 10/10 反相关）；空侧配对腿零数据 | M9-a |
| 10 | BTC 超 BH（双向增量） | R14-R16（翻转断面+停回补+强制回复）= fusion_btra_s34 的 FSM 化；fusion_btra_s34 +7368 = R1+R5/6(×pos)+R14@{move,recL2}+R15-16 的组合 | 转移 | L3（+7368/+543.1） | U1 守卫 |
| 11 | 分类消费内生化 | trend_row 局部直读（已弥合）+ 消费符号分裂残余入 D 类；Gated"方向承诺/市场暴露"双量声明义务 | 读数+声明纪律 | L3（GDX 否证） | M2-c |
| 12 | 47/53 分割 | 可达域边界 = a0 下限对两极性对称；η_adm 分母按层截断；双向增量 = Σ\|下行段\|（admissible 层）——笔级 53% 永久不可达，FSM 无机制可改（**承载 = 诚实上限会计**） | 会计口径 | L0（望远镜恒等式）+L2（捕获率表） | U7（η 报告） |
| 13 | type2 从未系统消费 | R18-R20 第二翻转窗口（T2W 锁存）+ 归因计数器；闭侧/nest 侧非行声明 | 锁存+转移 | L2（T2o 5/5 N=5）/L3（闭侧不可达） | M13-a~e |
| 14 | earning 近空域 | 不入状态空间（四步推导）——账本标志+gates 第四读数；fusion 基座下连标志都不存在（架构选择显式声明）；T5 定义冲突已上浮待裁决 | 不承载之论证 | L3（1/10,651） | M14-a~c |
| 15 | 信号层基线漂移 | **无 FSM 状态**（判决有效域层非行为层）——四件工程守卫：磁带三元组 (data_fp, engine_commit, tape_fp) / 判决登记协议（绝对数字=代次绑定，跨代仅结构性结论）/ 行为表读集矩阵 / 代次无关不变式锚先跑。双向放大修正："平方级"不成立，严格表述 = 配对上界消失+敞口×2+凸性尾部（Gated 居中靠 long 侧，可证伪） | 工程守卫 | L2（双代交叉复现一次）；混表事故在册 | M15-a~d |

**反向冗余检验**（每个状态/锁存/转移 → 矛盾）：

| FSM 元素 | 对应矛盾 | 判定 |
|---------|---------|------|
| 极性 d（Long/Short 逻辑） | 10（Σ\|Δ\| 双向增量） | 承重 |
| exec 三值（Linear/Gated/Put） | 3（earning 不动点非对称）+7（regime 条件化） | 承重 |
| sub=Out（短差在外） | 9（osc）/8（级别错配的短差承载） | 承重 |
| sub=Hinged | 8（44:44 铰链——滞后下的渐进断面） | 承重 |
| NestWin 锁存 | 1（滞后压缩）+5（失血二分的"该提前"支） | 承重 |
| T2W 锁存 | 13 | 承重 |
| dead latch | 9 的出口收窄（49:52 三卖否决） | 承重 |
| restore_due | 6（义务恒在 vs 资金时点） | 承重 |
| 配对快照 | 1（比较基准不可重读——candidate 极值/锚 ZD） | 承重 |
| Φ/freeze_d/gates 读数 | 2（承重墙本体） | 承重（非状态） |
| 树根 Flat/Armed | 入场前态——对应矛盾 1 的入场侧（区间套） | 承重 |
| ~~earning 相~~ | 14 ⇒ 不入状态 | **已删除**（冗余检验的阳性案例） |
| ~~FLAT 驻留~~ | 0.1 修改一 ⇒ 测度零过渡 | **已删除** |
| ~~TrendScope/anc_down 谓词~~ | 递归传导吸收 | **已删除** |

无未锚定状态残留。完整性：15/15 条矛盾各有承载行（其中 12/14/15 为"论证不承载"型——同样是方法论要求的合法形态：矛盾的解不需要状态时，论证必须显式）。

---

## §7 模块映射修订（相对 v1 §5 的 delta）

v1 映射表整体保持，六处修订：
1. 趋势停削/anc：合并为 freeze_d 递归传导（双向参数化）；GC/DX 修复轴照旧 R4 但**加 per-标的失败语义拆分**（C2：GC 带负先验——"任何停削窗口都伤它"P6 §3.2，GC 失败归 D 类残差不算框架否证；DX 带正先验 fusion_pu +16.4）。
2. 双向 S3/S4：R14 默认落点改 Gated，Short=条件升级；**s34/s24 层掩码无法由断面完备性内生推导**（C2 攻击2：BTC move 正/seg 负同为完整断面层）——层域降为 D 类部署声明，v1"由确认级别内生"的无证据声明撤回。
3. nest_forward：R12 归因注释更正（见 §0 自我否定①）。
4. 41课门：维持不入表，但其语义由 R16（强制回复）+R1 联合承载的声明补充空侧分量。
5. 新增 type2 模块行（R18-20）——v1 否证处置清单中 sell2_trigger（legacy）维持删除，sell2_open/buy2_close（rev_paired 语境）的证据迁移为 L0 类比待 M13 系列裁决。
6. OKLO/BRN 声部缺口显式化（C2 攻击4）：在册最优是 organic/ht 声部系（+2436.2/+681.3），fusion 系历史最高仅 378.9/86.0（差 5.1×/6.3×）——v1"大部分被 fusion 侧同语义吸收"一句 L0 声明**低估了规模**，REV 配对腿/master ht 出场/θ 自适应整套声部机器的吸收是独立工程（§9 U6 独立判据）。

---

## §8 零 flag 形式化修订

### 8.1 分类保持（v1 §6 的 A/B/C 类不变），D 类细化

**D 类（regime 白名单）拆为三个子类**（C2 逐例分类）：
- **D1 可消解**（有 L0 机制）：TrendScope 三值枚举（递归传导，干净）。
- **D2 赌注类**（有机制但先验混杂）：fusion_t GC/DX（R4 出口；GC 负先验/DX 正先验）、nest 负域（freeze_d 覆盖扩展；弱先验 ta≈ft——祖先分量在 BTC −48pp 近零/CL 为负，**覆盖不足是 U4 失败的第一解释**）、双向 ES/QQQ（Gated 退化）。
- **D3 不可消解候选**（标的物理/分辨率/口径）：GC range 主导结构、DX 无 bull 年、OKLO 真特异（C 轴判决"非资源伪影"）、QQQ 单边窗口、CL 空头构成比 48/52（载体物理）、s34/s24 层掩码、H4 摩擦口径、VLg 交互项变号。**这些失败按部署层残差结算，FSM 保持零 flag**（标的选择本来就是部署层动作，026:36）。

### 8.2 不可内生化清单（v2 新增的诚实声明）

1. **Short 升级白名单**（矛盾7）：无已验证标量读数（归约假说三重否证）。内生化唯一许可路径 = 候选读数先离线复现 24 格符号表 ≥21/24（含证伪格：BTC seg 负、CL move 负、CL seg 正——任何单调于层级的标量自动死于 CL 交替）。
2. **翻转层掩码**：断面完备性推不出 per-标的掩码（C2）；§9 U5b 对账观测，不能复现 ⇒ D 类。
3. **相位粒度**：门共享是语法修复，粒度×词汇耦合是经验定点（P6→P7→535 链：粒度选择由 L3 数据非原则决定）——kind×dir 基座继承 L3 定点，不从原则推导。

---

## §9 预注册体系 v2

### 9.1 核心判据（U 系列，吸收 C2 全部修正）

| 判据 | 内容 | 通过线 | 失败语义 |
|------|------|--------|---------|
| U1 | fusion_v ≥ fusion_t | 8/8 | 统一接线负交互 |
| U1b | 逐门对交互项 I(Φ×amp / Φ×pos / amp×pos)：双门放行腿群 wr/p50 vs 单门基线 | 正域上每对 I≥0，或 I<0 时转折窗口桶（开腿距 kind 翻转 ≤P50 中枢时长）p50<0 且占比>50%（交集收缩签名，锚=VLg 531对/p50 −25.9） | 按门对定位；修复轴=Φ 精化，禁删门 |
| U2 | 纯净检验域 {BTC,CL,ES,QQQ}：fusion_v ≥ 在册最优臂×0.8 | **4/4 硬线** | 门共享+读数内生化命题否证 |
| U2x | 预名允许失败集 {GC,DX}（负先验在册引用） | 失败按 D3 残差结算 | 不计入框架否证 |
| U3 | R4 出口的 GC/DX 修复 | GC/DX 失血回收 ≥50%（n_trend_holds 按锚中枢序数 N 分桶可观测） | **per-标的拆分**：DX 失败⇒出口轴定位错误；GC 失败⇒印证"拒一切窗口"归 D3 |
| U3c | **R4 正域守卫**：出口开启态 BTC/CL 比值 ≥0.95 vs fusion_t（P6a 先例字面）+ R4 触发可观测量（EXEMPT 内新中枢事件数×退出后削减词汇逐桶现金） | 2/2 | 出口在正域放血——R4 多侧降级为消融位 |
| U3m | R4 镜像（空侧 49:54 出口）：bear α 不降（BTC ≥2.332/CL ≥2.336，容差 −0.05）∧ movedown 拦截 N≥2 桶转化短差恢复笔数>0；N≥2 桶占比<5% ⇒ 镜像出口 no-op（空集消解，与多侧 §5.1 同型） | 见左 | 错位镜像降格纯形式对称 |
| U4 | nest 负域修复：freeze_d 覆盖扩展下 BTC/CL nest 增量 ≥0（对照 −676.9/−69.3pp）。机制重述：递归 EXEMPT 的**祖先分量**覆盖 26:80 缺口处 orphan；可观测量 = 被 R1 拦截的 nest 触发集中于"祖先-only 豁免 bar"（自层 trend 假∧祖先真）且该桶拦截×基线该桶现金≈预期回收量；**须在 R4 开启态下结算**（否则 U3/U4 互为混杂） | 2/2 | **第一解释=祖先覆盖不足**（弱先验 ta≈ft 在册），非因果定位错误 |
| U4g | 归因拆分三臂（A4 G2）：(i) freeze 扩展+nest off；(ii) freeze=自层+nest on；(iii) 扩展+on。预言 (ii) BTC 仍负、(iii) ≥0 | 见左 | (ii)≥0 ⇒ v1 接线归因复活 |
| U5 | 机制对齐计数器（v1 保持）+ **U5b 翻转层掩码对账**：fusion_v 由 MOVE↓ 断面内生产生的实际翻转层集，逐标的 vs s34/s24 | 复现 | 不能复现 ⇒ 层掩码升 D 类部署残差 |
| U6 | 声部吸收判据（OKLO/BRN 拆出）：fusion_v ≥ ht 线×0.8（+2436.2/+681.3 的 0.8）+ REV/master 特征计数器对齐 | 2/2 | 失败=声部机器吸收不足，**非门架构失败** |
| U7 | 绩效公理可证伪形式：bear α ∧ bull α 分桶同时非负 + 每级别 η = 实现/M·TV_adm 报告（理论分母强制附带） | 全标的 | 公理在该形态未达成（首例锚：btrg BTC bear +0.444 ∧ bull 零差） |

### 9.2 机制判据（M 系列，按矛盾编号；摘要——完整版以各工位产出为准）

- **M1-a** nest 相位门控臂（=U4）；**M1-b** 1s 嵌套长窗口 L3（ES 1yr 含深回调段；通过=加速比 p50≥2× ∧ T_B(true)>随机基线 ∧ moved_B p90 < moved_A p90×0.6）；**M1-c** 滞后轴天花板（否定性）：三机制合取后 ES/QQQ gap 收窄仍 <25% ⇒ 滞后轴从属地位终结算；**M1-d** 嵌套档位 K 闭式（禁扫参）。
- **M2-b** MoveDown 单独消融：ghost 关 movedown（保 R2+MoveUp）vs 在册 ghost，预测 BTC Δ≤−1500pp；Δ≈0 ⇒ 拦截计数大≠贡献大，停回补 L2 证据作废。**M2-c** 三值 Φ 落码守卫：多头侧全部在册臂 trades bit-exact。
- **M3-a** 载体不变性守卫（**可立即执行**）：btrg 与 btr 同磁带翻空断面 bar 集合逐位一致——任何不一致 ⇒ 载体渗入 FSM 转移判据，双坐标系分离失败；**M3-b** 认沽三臂对照（数据/小实盘后）；**M3-c** 首波兑现律可达性（≥50% 回收线）。
- **M4-a** =U1b；**M4-b** =U4g；**M4-c** 空侧合取转折窗口分桶（bear 年 cover 腿按距 MoveDown 翻转 bar 数）；**M4-d** 翻转 bar pool 净流转=0 逐位断言升测试守卫；追价 regime 桶 topup>60% ⇒ 铰链出清试验臂（先查 44课原文边界）。
- **M5-a** 双向失血四象限表（每笔出口动作按 ex-post regime × freeze_d 读数分桶；预言 [freeze 假∧事后顺趋势] 桶承担 BTC nest 负增量 ≥80%）；**M5-b** 诱多回补（bear 年 cover_nest 腿净现金 ≥ cover_buypt 的 0.8×）；**M5-c** 轧空尾部（nest 开启不增加 short_liquidated，>0 立即停臂）；**M5-d** 空侧滞后镜像表（买 candidate→confirmed 反弹错过%——"回补滞后失血"从 L0 命名升 L3 测量）；**M5-e** R17 两步断面消融（两步 bear α 损失 ≤0.05 nats ∧ 直接亏损显著减少）。
- **M7-a** Gated 普适性臂（**关键新预注册**）：fusion_btrg 跑剩余六标的，Δ(ghost−在册最优基座) ≥ −3pp 于 6/6（符号安全论证：Gated 最坏=晚回复几 bar，无轧空路径）；≥4/6 严格为正 ⇒ Gated 升 FSM 普适默认；≥2 标的 <−3pp ⇒ Gated 也是 regime 函数，公理需进一步退化并 escalate 编排者。**M7-b** Short 内生化 24 格门槛（§8.2）；**M7-c** 翻空开仓 1s 嵌套门控（bull 假翻空赤字回收 ≥50% ∧ bear 损失 ≤0.1 nats）；**M7-d** ghost×anc 臂（BTC ≥+115pp）。
- **M9-a** 空侧 osc 配对词汇差分臂（Short 极性开放完整 osc 镜像 vs 当前形态；bear 年 Δα>+0.05 ⇒ 真实增量且预期 −sign 分裂复现 / |Δ|≤0.02 ⇒ 防膨胀定理经济面确认零新机制关闭）。
- **M13-a** T2o 样本扩张（BRN+BTC+CL，N≥10 ∧ 胜率≥55%；<50% ⇒ N=5 伪影撤回 R19）；**M13-b** 第二翻空窗主判决（_t2w 臂 vs 在册，BTC/CL Δ≥0 ∧ sell2 腿 payoff 中位 ≥ sell1 腿）；**M13-c** 价格非时间机制检验（入场价位置分位 ≤0.33）；**M13-d** 闭侧阴性对照（Buy2 可达次数 ≤2/标的，>10 ⇒ 重开整臂）；**M13-e** 小转大域非空性（n_sell2_first_confirmed 全零 ⇒ 53:28 唯一窗口分支死代码）。
- **M14-a** 维度冗余判定（n_earning_reached ≤0.1% ∧ 窗口行为无差 ⇒ 降级固化）；**M14-b** T5 反事实重放（候选(ii) 在 OKLO 唯一笔）；**M14-c** 空侧 earning 测度（认沽 premium_basis≤0 达成率；与多头同 <0.1% ⇒ "空头 earning 同样虚弱"升 L2，认沽价值论证退回损失有界化）。
- **M15-a** 指纹强化（分层分流内容哈希+(data_fp, engine_commit) 头部；注入保计数扰动测试 2/2）；**M15-b** 重验最小集协议（行为表读集矩阵先登记；引擎修复后分层 md5 差分推导免疫集全部重跑）；**M15-c** 双向漂移放大实验（受控扰动 N≥20 种子三臂：median|ΔNAV|(btr)/(t) ≥2 ⇒ 放大 L2 成立；ghost 居中靠 t 侧）；**M15-d** U 判据代次钉死（fusion_v 回测入口注册三元组对照 p7_conj 指纹，漂移 ⇒ 全部读数作废）。

---

## §10 工程守卫与实装路径 v2

**Phase 0（可立即执行，零回测）**：M3-a 载体不变性守卫（在册磁带）；M4-d 自融资断言入测试；M15-a 指纹强化；行为表 R0-R20 读集矩阵登记（M15-b 前置）。

**Phase 1（接线）**：`fusion_v` 变体——(a) Φ 三值化+freeze_d 递归传导（多头侧 bit-exact 守卫 M2-c）；(b) R4 双侧出口（带 U3c/U3m 守卫计数器）；(c) R14 默认 Gated+Short 条件升级；(d) R18-20 T2W；(e) unified_osc 三门读数提升为公共 Φ/gates。在册臂全部零接触（GH2 先例）。

**Phase 2（验证）**：八标的 L3，§9 判据裁决；磁带三元组钉死（M15-d）。其中 M7-a（Gated 六标的）与 U 系列同批。

**Phase 3（收口）**：按裁决删 flag / D 类上移部署层声明 / OKLO-BRN 声部吸收独立工程（U6 失败时）。

**开放轴登记**：(i) 认沽第三分支与双 bars 流架构（§3.3）；(ii) nest 多层递归（一层截断 vs"直到最低级别"）；(iii) 追价拒绝词汇→铰链出清（44课边界待查）；(iv) 92课 Zn 监视器（g3/g4）；(v) g2 退出词汇 declared 近似；(vi) T5 相变定义冲突（已上浮待编排者）；(vii) QQQ 窗口混淆（二分线在牛市形态非 BH 域标签——白名单时变性未处理）；(viii) ghost×anc（+230pp 差分未被 Gated 形态吃到）。

---

## 结果包（六要素）

1. **结论**：双向统一递归 voice FSM v2——编排者公理以"逻辑层方向恒定（d 恒有定义、FLAT=测度零）× 物理暴露三值化（Linear/Gated/Put）"的形式存活对抗审查；状态最小化三元组 {d×exec, sub, latches} + 21 行双向行为表（R0-R20）+ freeze_d 极性协变递归传导；15 条矛盾全部获得承载（§6 矩阵：11 条机制承载 + 3 条论证不承载 + 1 条部署层声明），反向检验删除三个冗余元素（earning 相 / FLAT 驻留 / TrendScope 枚举）。v1 的发现三归因、行 4 无守卫、type2 缺行三处错误已自我否定并修正。
2. **定义依据**：026:34（恒仓 M=N 量纲）、038:36（镜像授权的字面边界——判断-动作序列与库存循环，不含状态空间扩张）、049:52/54（二相+出口，双侧）、053:28/086:80/017:66（type2 三角色）、035:30（成本门）、044:44（铰链三岔）、027 课+038:258（区间套正向）、053:26（上确界不可达自承）；双向会计 §2.4/§3.3/§5.2（非对称定理/净额等价/cascade settle，L0）；认沽调研 §0.3/§1.2（凸性载体扬弃，L0）。
3. **边界条件**：U2 4/4 失败 ⇒ 门共享+内生化命题否证；M7-a ≥2 标的 <−3pp ⇒ Gated 也是 regime 函数、编排者公理需进一步退化（escalate）；U3c 失败 ⇒ R4 多侧降消融位（49:54 出口在正域放血）；U6 失败 ⇒ OKLO/BRN 声部吸收是独立工程非本设计范围；M13-a <50% ⇒ R19 撤回；若编排者裁定物理暴露也必须永远非零（Gated 不可接受），则与 ES/QQQ 全层全形态为负的 L3 证据构成正面矛盾，须走 /escalate。
4. **下游推论**：(a) BTC/CL 在册最优臂（btrg_s34/btr_s24）= fusion_v 在 D 类层掩码下的投影，U1/U3c 守卫其不被统一化损伤；(b) 部署矩阵的消解上限被 C2 逐例分类钉死——D3 子类（GC/DX/OKLO 特异等）按部署层残差结算，"零 flag"的可达语义 = FSM 零概念开关 + 部署层标的/载体/层掩码选择；(c) 期权载体接入需要双 bars 流架构（LayerState 当前无此自由度）——IBKR 执行支柱（M4 里程碑）的前置依赖；(d) 信号层任何修复触发 M15-b 重验协议，双向判决重验优先级高于长 only（配对上界消失+敞口×2 论证，待 M15-c）。
5. **谱系引用**：534号（49课二相）、533号（结构滞后三层谱型——candidate/confirmed 双流的谱系根）、275号（局部依赖→freeze_d 递归）、436号（命名纪律——delta 配额否决/拓扑词汇）、230/231号（直积退化/有效域——编排者直积表述的处置依据）、090号（三处自我否定与 [镜像推导] 标注义务）、002号（源不完备——93:28 情形二/92课 Zn 待源头审计）、161号（flag 删除时序）；regime 函数家族 11+ 例（在册编号有碰撞：两文档同称"第九例"——列谱系修正项）。
6. **影响声明**：本产出重写 `analysis/unified_recursive_voice_fsm_design.md`（v1→v2），零代码改动、零在册判决改动；v1 的发现三表述被撤回更正（引用 v1 该段的下游文本须改锚 nesting §5.4 逐字）；任务背景"44课 type2 介入"引文勘误登记；新增两个待编排者决断点：(α) Gated 作为编排者公理的退化形态是否可接受（M7-a 的 escalate 分支）；(β) T5 相变定义冲突三候选（在册已上浮）。预注册体系 U1-U7+M 系列共 ~40 条判据，磁带三元组钉死义务随行。
