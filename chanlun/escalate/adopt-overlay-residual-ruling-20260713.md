# 裁决：ADOPT_ORPHAN 覆盖层验收口径 35→33（task #67 遗留项）

- 日期：2026-07-13（临时）；2026-07-13 转正（同日）
- 性质：**最终裁决**（原为自主临时裁决；用户明示"不需要拍板"，且复核触发条件核验未触发，见 §转正核验）
- 依据：`chanlun/review-results/adopt-overlay-spec-20260713.md` §0.2、§5.3；task67 log `:40-45,69,103-104`

## 裁决内容

1. **采纳 spec 建议冻结口径**：`adopt_v1` 生产验收为 `52→33；+19 获 host；其中 7 Completed；regressions=0`。
2. 旧 DP 重组口径 `residual=35 / regressions=2` 保留为**兼容诊断字段**，命名空间强制 `legacy_recompose_*`，禁止出现在 `adopt_v1_*` 命名下。
3. 拒绝“坚持 residual=35”的替代路径（需把 2 个真实 Assigned 降级为候选并发明新拒绝规则——无原文与工程依据）。

## 裁决理由（摘要）

- 集合恒等式：在 M1（基座 host 不变）、M2（+19）、M3（host⊕ledger 完备互斥）下，账本基数唯一为 `52-19=33`；要求 35 与单调性不可满足（`UNSATISFIABLE_WITH_MONOTONICITY` 运行时断言）。
- 35 的来源是旧 DP 释放了 2 个基座已有 host（`L0#33574`、`L0#37199`），这正是 #64/#67 单调门要拦截的回退行为；把回退产物当验收基准与 CompletedFreeze 防线矛盾。

## 生效范围

- #67 spec §0.2/§11 的 [未完成] 标记改为 [已裁决-临时]，晋级门放行。
- 后续接入 #63 Unassigned 账本生产化时，验收数字以 33 口径为准。

## 复核触发条件（已核验，未触发）

- P0 若推翻本裁决，需同时给出 2 个降级对象的合法拒绝规则；否则 33 口径维持。

## 转正核验（2026-07-13）

- 全库检索：`L0#33574` / `L0#37199` 仅出现于 p68 报告、seed-failure-geometry、adopt-overlay-spec 与本裁决文件四处，**无任何一处给出合法拒绝规则**。
- #68 隔离集成复核：33 口径通过全部验收（守恒 `52=19+33+0`、5 条 property test、双重放 SHA-256 bit-exact），旧口径 35 复现为 `legacy_recompose_residual=35 / regressions=2` 且被 adopt_v1 单调门拦截——即 35 的成因被再次实证为回退行为。
- 用户明示无需人工拍板。据此本裁决由临时升级为**最终**；#67 spec 晋级门标记同步升级为 [已裁决-最终]。
