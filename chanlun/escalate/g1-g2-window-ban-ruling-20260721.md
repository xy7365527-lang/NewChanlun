# 合并裁定：窗口绑定全域禁用 + T1「只能当标签」supersede + G1 约束转化（G1/G2 终裁）

- 日期：2026-07-21
- 性质：**编排者直接拍板**（grill 会话内裁定）。影响票：task-101 G1/G2 关闭；p101 探针退役（票 #79）；T1 条款变更。
- 教义论据：`chanlun/review-results/cert-density-doctrine-research-20260721.md`（密度差教义归因 + 017:72 级别限定 + 二买双口径）。
- 纪律：纯文档裁定，零代码改动；零 git mutation；主仓零写入。

## 1. G2 终裁：窗口绑定全域禁用

**编排者原话**：「不能用这个窗口」「这个方法根本不严格，这是经验主义的」。

- 证书-BSP 的 bar 距离窗口绑定（240/1440，judge_max + 最近同侧 BSP）**全域禁用**：生产路径禁、探针路径禁。该方法是拿时间距离近似结构身份的经验主义近似（240≈L0 平均 BSP 间距 233 bar 是数据拍值，教义无此口径）。
- 替代路径 = **身份绑定**：证书经 `NestEventIdentity{level, turn_source, interval_b}` 直挂其结构确认的 BSP 点，无窗口概念。身份桥（turn_source ↔ source_index 坐标系同一性）确证在跑（票 #75）；桥证否则绑定功能整体挂起，**不得**退回窗口法。
- DRAFT 条款 2（judge_max 绑定规则）、条款 3（240/1440 分档）自此作废；条款 7（39 张簇 |dt|）、条款 8（top 身份迁移）作为探针历史记录保留，不再作任何口径依据。

## 2. T1 supersede：「只能当标签」→「只能当门」

**本裁定替代 `cert-bsp-binding-ruling-20260721.md` §T1（sidecar 打标、判据层不可见）。**

- 教义层三分（密度调研 §5 修正后）：
  - 证书**不当信号源**——恒久成立（教义：证书是买卖点的确认谓词，信号是 BSP 本体；且一买族定义域天然小，~300× 密度差是教义结构非缺陷）。
  - 证书**不当窗口贴标**——本裁禁止（§1）。
  - 证书**当门**——区间套递归确认是教义原生确认机制（027:36/38/46/60、061:26），typed 真链证书作一买族入场准入门（N3 主线，W3 口径「真当门用不是贴标」）；二三类点走锚定路径（某级别一类点，017:72 限定在案）。两路互斥互补。
- T1 的「密度低不能作独立入场源」论据本身成立但指向错误结论——密度差是定义域正确的证据（调研 §3/§5），不构成贴标化的理由。贴标化是断链时代（级别标定断裂、rungs 95.36% 空）的保守圈禁，V1/V2 严格化后前提已消失。

## 3. G1 终裁：升格硬约束（约束对象转化）

**编排者拍板：升格硬约束。**

约束对象随 §1/§2 转化：从「打标产物不得进判据路径」转化为「**窗口绑定产物不得进任何路径**」。

- 实装形态：CI grep 门——`! grep -rn "P101_TAG\|P101_MULTI\|cert_bsp_tag" rust/src/theta_v0/`，挂 `.github/workflows/ci.yml` 新 step（纯 shell，不依赖 Rust 工具链；CI 现状无 cargo step，调研已核）。
- 探针侧：p101 bin（rust/src/bin/p101_cert_bsp_tag.rs）退役登记（票 #79）——**归档不删除**（不可篡改纪律：探针与其历史报告保留，注册位标注「已退役，口径作废见本裁定」）。
- 门消费侧（typed 真链门）不受此 grep 约束——门是教义原生路径，其产物是准入判定不是贴标。

## 4. R1-R3 确认登记（5a/5b 硬阻塞②解除）

编排者 2026-07-21 对 `r1-r3-ruling-confirmation-checklist-20260720.md` 三项各回「确认」：

- **R1**：接受 5b 两条 memo 失效链构造性论证（水位严格 `<`；≥2 后继块门）+ 接受 TURN 残余风险继承（拦截网 = V0 身份校验 + P123_SHADOW 全程对拍 + dump diff）。
- **R2**：5a 游标存放 = bin `LevelDerived` 旁（shadow oracle 走真冷路径）。
- **R3**：5b memo 存放 = bin `RunEntry` 旁（键语义 run 语境）。

**5a/5b 阻塞态照实登记**：阻塞②（R1-R3 未确认）解除；阻塞①（wave-1 未提交）仍在——禁 git mutation 纪律下由编排者自行处置；阻塞③（wave-1 验收封口）已于 2026-07-19 定谳解除。全部阻塞清除后 5a/5b 方可进实装工位。

## 5. 后续票登记

- **#79**（implement）：p101 窗口打标探针退役登记 + CI grep 门实装。✅ 已完成（2026-07-21 关票）。
- **#80**（grilling）：锚定级别关系按 017:72 放开为「某级别」——descend_type1_anchor_depth 现行 ℓ−1 硬锚的教义符合性重审。✅ 已裁（级别分治，anchor-level-split-ruling-20260721.md）。
- **#81**（grilling）：二买双口径（构成论 017:66 + 形态论 053:28/057:90「不创新低或盘整背驰」）的机器表达。✅ 已裁（双口径都装，type2-dual-caliber-ruling-20260721.md），并入 #82。
- **task-101 E1-E3 执行项核销**：E1（66 口径复跑重建 240/1440 分档数字）、E2/E3（p101 探针 per_bsp 语义/主键列修正）——均以窗口绑定口径为前提，本裁定禁用窗口法后失去对象，**核销不执行**（2026-07-21 登记）。

## 6. 复议通道

身份桥证否（#75 结论）时本裁定 §1 的替代路径失效，须复议——届时窗口法仍不得回退，绑定功能挂起待新路径。其余条款推翻以新 escalate 文档显式 SUPERSEDE 为准。

## 签字位

- [x] G2 窗口绑定全域禁用——编排者拍板（2026-07-21）
- [x] T1 supersede：标签→门（typed 真链身份绑定）——编排者拍板（2026-07-21）
- [x] G1 升格硬约束（转化为窗口产物全禁 grep 门）——编排者拍板（2026-07-21）
- [x] R1/R2/R3 确认（5a/5b 阻塞②解除，阻塞①照实保留）——编排者确认（2026-07-21）
- [ ] 编排者复议（空位）
