@AGENTS.md

# Claude Code

- Subagents may partition source discovery, but the main thread verifies every
  decisive quotation, date, rights claim, content hash, and link before landing
  a synthesis or snapshot change.
- Do not use auto memory as source provenance.
