# spec：陈旧信号与最弱二类处置实装方案（#660，地图 #654 收口）

- 日期：2026-07-29
- 上游裁定链：#655（缩到三类，后被 supersede）→ 用户三轴边界修正 → #659（名分）→ #657（滞后根因）→ #658（机制选型）→ #656（最弱二类纪律）→ #647 定稿（破自己的底即拒）
- 证据底座：`.chanlun/review-results/reverse-3axis-attribution-20260729.md`（三轴：待定 10/拒·力度 22/拒·超限 13）、`confirm-lag-anatomy-20260729.md`（lag=结构确认时间，档B 中位 0）、`sys2-entry-falsify-text-20260729.md`（考据+§5.1 订正）

## 一、#647 门执行路径（留）

- **终稿判据**：入场时价格已越过该候选**自身结构位**（1/2 类=点自己的 pivot 极值，3 类=中枢 ZG/ZD）⟹ 拒开仓。全类别同判，无二类口子。
- **工位分支处置**：`/tmp/wt-647`（ticket-647）已 `reset --hard 828809a089`（回退 `cc876f4afe` 的二类全放），备份分支 `backup-647-pre-reset`；注释教义链重写（判据零改动），测试 `entry_stop_recheck` 11/0 绿。
- **合入序列**：opus 影子评审 PASS → 编排层 commit → 用户批准 → `merge --no-ff` 入 main。
- **锚重订**（反证+重订常例）：`THETA_ENTRY_STOP_RECHECK_SKIP=1` 反证跑恢复旧字节 SHA `4b03bd01…1df43`（834 行锚）→ 门臂跑产新锚（预期 trades ≈800 行档）→ ADR 补充九加锚重订条（读数全史追加：分档 −531,132 → 门终稿读数）。

## 二、最弱二类（做，零机制）

门语义「破自己的底才拒」与 101 课最弱情况（二类底低于一类底、价格未破二类自己的底）定义性不冲突——天然放行，无任何机制落地。45 冤单全是破自己的底，最弱二类零出现（#656 在案）。

## 三、陈旧信号机制（不另设，零实装）

候选一次性（确认 bar 一次投放、成交恒为确认+1 bar），「确认后→入场」窗口恒 1 bar ⟹ 过期判定点唯一 = 入场一瞬 = #647 门本身。TTL/结构事件/价格穿越三层均不立（#658 在案）。挂起式入场语义出本图（Out of scope 登记）。

## 四、名分清扫（已落）

ADR 补充九注记⑤订正（13 课名分句有误）；`fengkong.md`、`chanlun-trading-system.md` 降级标注条；`beichi.md` 区间套行订正部分真；考据 §5.1 pivot 误述订正。结构止损名分 = 系统自设风控参数（Θ_risk 设计选择，非缠论判据）。

## 五、验收

- 反证：SKIP=1 恢复 834 行旧字节（SHA 逐字节对拍）；
- 门臂：trades 计数、`entry_stop_recheck_reject` 按类计数、RiskExit 读数对照（wf8 单窗，不作策略含义裁定）；
- lib 测试全绿（门相关 11 例 + 全量回归）；
- 金标准新锚：字节数/行数/SHA-256 三件套入 ADR 补充九，旧锚留底。

## 六、切票建议

无新票。执行全在 #647（合入+锚重订+关票）。力度/区间套线归 map #529（Consume_at 需求输入已登记 2026-07-29）。
