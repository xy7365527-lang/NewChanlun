# 090 镜像修复 — 伪原文标注降级 + 悬空引用补档（2026-07-18）

> 工作区：git worktree `/tmp/kimi-nest-mainline`（分支 `kimi-nest-mainline-20260717`）。
> 主仓 `/Users/silencehan/Projects/NewChanlun` **全程只读核对、零写入**；主仓解禁后按本镜像打正式补丁。
> 纪律：090 号——声明必须与实际能力一致。所有行号均自主仓当前工作树（HEAD=`main`）直读核对；git 事实经只读命令验证。

---

## 条目 1：伪原文标注降级（`docs/recursive_fugue_necessity_proof.md:79`）

### 问题

主仓 `docs/recursive_fugue_necessity_proof.md:79` 把「本级别只有三类买点，其他都是次级别买卖点」标注为**原文**（"原文（reinterp §2.1 引）"）。该句在缠论博文全文中**不存在**——这是用户 session5 的洞察，被两级文档层层转引时误升为「原文」。伪原文标注违反 090 号纪律（声明必须与实际出处一致）。

### 证据（全部主仓只读核对）

1. **误标现场**：`docs/recursive_fugue_necessity_proof.md:77-85`（L3 节），line 79：
   > 原文（reinterp §2.1 引）："本级别只有三类买点，其他都是次级别买卖点。"

2. **误标上游**：「reinterp §2.1」= `docs/spiral_physical_reinterpretation.md` §2.1（`### 2.1 h（角向步进）= 走势内元素的首尾推进`，line 98 起），其 **line 106** 以「（原文："本级别只有三类买点，其他都是次级别买卖点"）」引同一句——**上游文档同样是伪标**，recursive_fugue 是二手继承。

3. **博文全文零命中**：`docs/chanlun/text/blog/` 共 **110** 篇（`ls *.md | wc -l` = 110）；对 `docs/chanlun/text/`（博文 + 编纂版 chan99 全树）检索 `只有三类买点` / `其他都是次级别` / `三类买点，其他` —— **零命中**。该句不是缠师原文。

4. **真实出处 = 用户 session5 洞察**：`docs/memory_snapshot/project_session5_spiral_reinterpretation.md:48-49`（`### 9. 用户关键洞察序列` 首条）：
   > "买卖点首尾相连→本级别只有三类买点，其他都是次级别买卖点"

   同源副本均**正确**归于用户洞察：`docs/memory_snapshot/project_session5_spiral.md:49`、`docs/all_memory_constraints.md:1293-1294`（同在「用户关键洞察序列」节下）——唯独两个技术文档误升为「原文」。

5. **教义锚行号直读核对**（`docs/chanlun/text/blog/`）：
   - **021:62**（买卖点完备性定理）：「对卖点的分析是一样的，归纳起来，就有缠中说禅买卖点的完备性定理：市场必然产生赢利的买卖点，只有第一、二、三类。」
   - **021:66**（升跌完备性定理）：「市场中的任何向上与下跌，都必然从三类缠中说禅买卖点中的某一类开始以及结束。换言之，市场走势完全由这样的线段构成，线段的端点是某级别三类缠中说禅买卖点中的某一类。」
   - **053:32**：「……而在第二、三买卖点之间，都是中枢震荡，这时候，是不会有该级别的买卖点的，因此，如果参与其中的买卖，用的都是低级别的买卖点。」
   - **038:16**：「站在纯操作的角度，由于任何买卖点，归根结底都是某级别的第一类买卖点……」
   - **021:70-72**（思考题注）：线段端点组合只列 1买/2买/1卖/2卖 配对；「绝对不可能出现的是线段端点为3买卖开头即绝对不可能出现的是3买-1卖 3买-2卖 1买-3卖 2买-3卖」。

6. **措辞差（补丁必须一并修）**：
   - 洞察说「三类**买**点」，教义是「三类**买卖**点」——买卖对称（021:62「买卖点」、021:66「向上与下跌」双侧表述）。
   - 作为**边界标签**表述过强：线段端点只由 1/2 类占据（021:70-72 注：3 买卖开头的端点组合绝对不可能）；3 类是**相对于中枢的结构内点**（053:32：第三类买卖点「对付中枢结束」，二、三买卖点之间为中枢震荡、无本级别买卖点）。

### 正确措辞

降级为「**用户洞察（教义一致）**」，教义锚改挂 021:62/66、053:32、038:16。措辞要点：

- 出处标注：`用户洞察（session5，docs/memory_snapshot/project_session5_spiral_reinterpretation.md:49）`，注明「110 篇博文全文检索无此原句」。
- 「三类买点」改为「三类买卖点」（买卖对称，同 021:62/66 教义口径）；引用用户原话时可保留原句但须明确是用户表述。
- 补边界注记：「三类」作为买卖点**类标签**成立（021:62）；作为线段**端点标签**过强——线段端点只由 1/2 类占据（021:70-72），3 类是相对于中枢的结构内点（053:32）。

### 主仓补丁方案（文件:行号 + 前后对照）

**补丁 1-A（主修）**：`docs/recursive_fugue_necessity_proof.md:77-85`

前（现状，直读主仓）：

```markdown
### L3（原文 + T16）：本级别三类买点 + 其余皆次级别

原文（reinterp §2.1 引）："本级别只有三类买点，其他都是次级别买卖点。"由 **T16**（级别=A₀ 递归
实例，每级别独立完整形态学）：k 级别走势内部，本级别 type1/2/3 是稀疏锚点，其余首尾相连的节点
全是 k−1 及以下级别的买卖点。

> **L3**：上涨 k 级别走势中，本级别买卖点 ∈ {三类 BSP}（稀疏），走势内其余所有节点 = 次级别
> (k−1) 买卖点（稠密）。**前提**：原文三类买点 + T16（A₀ 全称实例化 R3）。**螺旋**：本级别 BSP =
> φ=0 母线上的锚（46 条母线之一，H⁰）；次级别节点 = 内圈 (r=k−1) 的 φ=0 点（向心，T49）。
```

后（补丁）：

```markdown
### L3（用户洞察[教义一致] + T16）：本级别三类买卖点 + 其余皆次级别

用户洞察（session5，`docs/memory_snapshot/project_session5_spiral_reinterpretation.md:49`，
教义一致；110 篇博文全文检索无此原句，非缠师原文）："买卖点首尾相连 → 本级别只有三类买卖点，
其他都是次级别买卖点"。教义锚：买卖点完备性定理（021:62，"只有第一、二、三类"）+ 升跌完备性
定理（021:66，线段端点是某级别三类买卖点之一）+ 053:32（"在第二、三买卖点之间，都是中枢震荡
……不会有该级别的买卖点……用的都是低级别的买卖点"）+ 038:16（"任何买卖点，归根结底都是某级别
的第一类买卖点"）。由 **T16**（级别=A₀ 递归实例，每级别独立完整形态学）：k 级别走势内部，本级别
type1/2/3 是稀疏锚点，其余首尾相连的节点全是 k−1 及以下级别的买卖点。

> **L3**：上涨 k 级别走势中，本级别买卖点 ∈ {三类 BSP}（稀疏），走势内其余所有节点 = 次级别
> (k−1) 买卖点（稠密）。**前提**：用户洞察（教义一致，锚 021:62/66 + 053:32 + 038:16）+
> T16（A₀ 全称实例化 R3）。**边界注记**：「三类」作买卖点类标签成立（021:62，买卖对称）；作线段
> 端点标签过强——线段端点只由 1/2 类占据（021:70-72 注：3 买卖开头的端点组合绝对不可能），3 类
> 是相对于中枢的结构内点（053:32）。**螺旋**：本级别 BSP = φ=0 母线上的锚（46 条母线之一，H⁰）；
> 次级别节点 = 内圈 (r=k−1) 的 φ=0 点（向心，T49）。
```

改动语义：①出处由「原文」降级为「用户洞察（教义一致）」；②「三类买点」→「三类买卖点」（教义口径）；③教义锚四挂（021:62/66、053:32、038:16）；④新增边界注记（端点只 1/2 类，3 类为中枢结构内点）。T16 推理链与 L3 命题本体不动。

**补丁 1-B（连带修，误标上游）**：`docs/spiral_physical_reinterpretation.md:106`

前（现状，直读主仓）：

```markdown
**精确化**：φ:0→23≡0 是**一次完整走势**的角向行程（T47："走势是 φ:0→1 的一次角向行程，背驰点 = 行程终点"）。23 个 h 步 = 走完 23 环概念运动链（concept_movement_chain），= 一个走势从涌现到完美。**走势内的买卖点都是次级别**（原文："本级别只有三类买点，其他都是次级别买卖点"），对应 h 在走势内推进时经过的次级别元素边界。
```

后（补丁）：

```markdown
**精确化**：φ:0→23≡0 是**一次完整走势**的角向行程（T47："走势是 φ:0→1 的一次角向行程，背驰点 = 行程终点"）。23 个 h 步 = 走完 23 环概念运动链（concept_movement_chain），= 一个走势从涌现到完美。**走势内的买卖点都是次级别**（用户洞察（session5，教义一致，锚 021:62/66 + 053:32 + 038:16）："买卖点首尾相连 → 本级别只有三类买卖点，其他都是次级别买卖点"；110 篇博文全文检索无此原句），对应 h 在走势内推进时经过的次级别元素边界。
```

**无需改**：`docs/all_memory_constraints.md:1293-1294`、`docs/memory_snapshot/project_session5_spiral.md:49`——两处本就在「用户关键洞察序列」节下，出处标注正确。

---

## 条目 2：悬空引用补档（`TARGET_STRATEGY.md:54` / `TARGET_STRATEGY_MAXFULL.md:270`）

### 问题

主仓两处引用 `docs/formal-chain/有效域定理-20260704.md` 为「定理1 严格证明」出处，但该文件在当前工作树不存在，引用悬空：

- `TARGET_STRATEGY.md:54`：**任一对象的负结论不得外推到另一对象**（严格证明见 `docs/formal-chain/有效域定理-20260704.md` 定理1）。
- `TARGET_STRATEGY_MAXFULL.md:270`：**边界互斥继承 693 §1.4**……（严格证明见 `docs/formal-chain/有效域定理-20260704.md` 定理1）。
- 另：`TARGET_STRATEGY_MAXFULL.md:286`（结果包影响声明）亦列该文件为「引用但未修改」的边界互斥证明。

### 证据（含对任务前提的订正——090 纪律要求如实登记）

1. **工作树缺席属实**：主仓 `docs/formal-chain/` 目录 ls 无此文件（Glob `docs/formal-chain/*有效域*` 零命中）；`git cat-file -e HEAD:docs/formal-chain/有效域定理-20260704.md` 失败（HEAD=`main`）。

2. **前提订正：「git log 均无」不成立**。`git log --all -- <path>` 命中**恰好一条**：

   ```
   4f94b3fdc2  Refactor Chanlun formalization and remove obsolete logic
                （2026-07-10，新增该文件 227 行，diff-filter=A 确认）
   ```

   该 commit 只在分支 `gap3-rework-codex9-fix` 上（`git branch --contains` 唯一命中）；`git merge-base --is-ancestor 4f94b3fdc2 HEAD` = **否**——main 不含此 commit，故 main 工作树缺席。这与项目记忆一致：真实工作线是长寿分支 `gap3-rework-codex9-fix`，当前 HEAD 被错切到 `main`。**「文件不存在」是 HEAD 错切的表象，文件内容在 git 对象库中完整可恢复。**

3. **定理1 内容已完整恢复**（只读 `git show 4f94b3fdc2:docs/formal-chain/有效域定理-20260704.md`，全文 227 行），并与两处旁证互证一致：
   - `.chanlun/genealogy/pending/693-strategy-object-triple-freeze-a11-a10-hard-ruling.md:48-57`「分离的逻辑必然性」：未接模块 M⋆ 仅在状态子集产正收益 ⟹ \(E[R(\Pi_{tested})]\le 0 \not\Rightarrow E[R(\Pi_{full})]\le 0\)，且明文引用该文件为「有效域定理1」；
   - `.chanlun/review-results/formal-chain-deepresearch-20260704.md:128`（判定表 #10）：「M* 反例的 ⇏ 论证严格成立（模态非蕴含只需逻辑可能）」；同文件 :127 亦记「TARGET_STRATEGY.md 冻结表存在（ac3b156eec）+ 有效域定理文档」——该文档在 2026-07-04 深研报告时点上存在。

4. **恢复文件自署权威来源**：`docs/formal-chain/问题1.pdf`（§3 问题C、§3「严格证明：为什么未闭合时不能下结论」、§1 数学化段）。PDF 直读核对（16 页）：§2 = 有效域声明（「当前 alpha 结论的有效域是『已修复五步后的信号层/方向层检验』，不是完整执行层/资金层/多声部」）；**§3 = 「严格证明：为什么未闭合时不能下『完整无 alpha』结论」**（五元组 \(\Pi=(\Gamma,Z,E,M,K)\)、⇏ 陈述、M⋆ 反例骨架）。**订正**：任务备选措辞「PDF §2」不精确——严格证明在 §3，§2 是有效域定性声明，改引时应两节并挂。

### 处置推荐

**推荐 (a) 补档**：主仓解禁后将下方草稿落盘为 `docs/formal-chain/有效域定理-20260704.md`。草稿 = **git 对象库恢复原文逐字**（非重建——恢复本即真迹，693:48-57 与 deepresearch:128 两处重建线索与其逐点吻合，无需再拟）。两条引用（TARGET_STRATEGY.md:54、TARGET_STRATEGY_MAXFULL.md:270/286）随即解悬，**无需改文**。

> 边界说明（诚实义务）：若「主仓解禁」的实际动作是把 HEAD 切回或合并 `gap3-rework-codex9-fix`，该文件随分支到达，引用自动解悬，本补丁无需执行。补档方案面向「main 线需独立持有该文件」的情形。两情形都不需要选项 (b)；(b) 仅在确定**不恢复该文件**时启用。

### 选项 (a)（推荐）：补档草稿全文

落盘路径：`docs/formal-chain/有效域定理-20260704.md`。以下为恢复原文逐字（来源 `git show 4f94b3fdc2:...`，227 行；文末「审查记录（待回填）」为原文件固有状态，照原样保留）：

````markdown
# 有效域定理（三则）——严格证明

> goal `g-20260704T1756Z-strategy-object-freeze-cbc36d` / acc-validity-domain-proof。
> 权威来源：`docs/formal-chain/问题1.pdf`（§3 问题C、§3「严格证明：为什么未闭合时不能下结论」、§1 数学化段）。
> 对象定义引用：`TARGET_STRATEGY.md` §1（策略对象五元组）。
> 本文件认识论层级：**L0**（纯数学命题与证明，零 L2 信息增量）；所有市场量仅出现在反例模型的构造中。

记号约定：策略对象 \(\Pi=(\Gamma,Z,E,M,K)\)（证书集合 / 状态分类 / 出场规则 / 资金管理 / 风险可行集）；市场概率空间 \((\Omega,\mathcal F,\{\mathcal F_t\},P)\)；\(R(\Pi)\) 为在该空间上按 \(\Pi\) 交易的净收益随机变量。\(\varphi \not\vdash \psi\) 表示「\(\varphi\) 不逻辑蕴含 \(\psi\)」——证明方式为构造一个模型使 \(\varphi\) 真而 \(\psi\) 假。

---

## 定理 1（有效域不可外推）

### 陈述

设 \(\Pi_{full}=(\Gamma_{full},Z_{full},E_{full},M_{full},K_{full})\)，\(\Pi_{tested}=(\Gamma_{t},Z_{t},E_{t},M_{t},K_{t})\)，且 \(\Pi_{tested}\subsetneq\Pi_{full}\)（即五层中**至少一层**满足 \(\Gamma_t\subsetneq\Gamma_{full}\) 或 \(Z_t\) 粗于 \(Z_{full}\) 或 \(E_t\ne E_{full}\) 或 \(M_t\ne M_{full}\) 或 \(K_t\subsetneq K_{full}\)）。则：

\[
E[R(\Pi_{tested})]\le 0 \;\not\vdash\; E[R(\Pi_{full})]\le 0 .
\]

### 假设（逐条 + 实装锚点）

| # | 假设 | 实装锚点 |
|---|------|---------|
| H1.1 | 策略对象是五元组 \(\Pi=(\Gamma,Z,E,M,K)\)，其净收益 \(R(\Pi)\) 是 \((\Omega,\mathcal F,P)\) 上的可积随机变量 | `TARGET_STRATEGY.md` §1；样本实现 = `rust/src/theta_v0/backtest/l3_delta_r_alpha.rs:150` `build_mu_from_bars`（逐笔 \(X_\gamma=\delta(P_\tau-P_t)-C\) 汇总） |
| H1.2 | \(\Pi_{tested}\) 与 \(\Pi_{full}\) 在**同一**市场概率空间上取值（同一数据源、同一时点集） | `rust/src/theta_v0/backtest/data.rs`（同一 bars 序列驱动两对象；本仓库所有回测共享该数据层） |
| H1.3 | 「\(\Pi_{tested}\) 不调用模块 \(M^\star\)」的语义：\(M^\star\) 的持仓贡献在 \(\Pi_{tested}\) 的收益过程中恒为 0 | 现实原型 = A11 诊断层：`rust/src/theta_v0/strategy/coverage.rs` 的 `overlay_net_delta` 只写诊断字段、不改真实订单流（问题1.pdf 问题A：「若多声部对冲只存在于诊断层，不改变真实交易」） |

注意：非蕴含（\(\not\vdash\)）的证明**只需**构造一个满足全部假设的模型使前件真、后件假；模型内部结构（如可加性）是构造的自由，不是额外假设。

### 证明

**第 1 步（构造市场模型）**。取概率空间 \(\Omega=\{s_0,s_1\}\)，\(P(s_1)=p\in(0,1)\)，\(P(s_0)=1-p\)。单期市场，交易品价格增量在两状态下由下述交易结果直接给出（点质量分布，一切期望有限，H1.1 满足）。

**第 2 步（构造两个策略对象）**。令 \(\Pi_{tested}\) 在两状态下各执行一笔固定交易，净收益

\[
R(\Pi_{tested})(s_0)=R(\Pi_{tested})(s_1)=-b,\qquad b>0 .
\]

令模块 \(M^\star\) 为「仅在状态 \(s_1\) 上激活、且激活时净贡献 \(+a\)（\(a>0\)）」的模块：

\[
R(M^\star)(s_0)=0,\qquad R(M^\star)(s_1)=+a .
\]

令 \(\Pi_{full}\) = \(\Pi_{tested}\) 接入 \(M^\star\)：\(R(\Pi_{full})=R(\Pi_{tested})+R(M^\star)\)（构造使然：\(M^\star\) 的订单与 \(\Pi_{tested}\) 的订单标的独立、无相互作用，故贡献可加；H1.3 下 \(\Pi_{tested}\) 侧该项恒 0，与 A11 诊断层「不改变真实交易」的实装形态逐点对应）。此时 \(\Gamma_t\subsetneq\Gamma_{full}\)（缺 \(M^\star\) 的证书），\(\Pi_{tested}\subsetneq\Pi_{full}\) 成立。

**第 3 步（验证前件）**。\(E[R(\Pi_{tested})]=-b<0\le 0\)。前件真。

**第 4 步（验证后件为假）**。

\[
E[R(\Pi_{full})]=-b+p\,a .
\]

取任意满足 \(p\,a>b\) 的参数（如 \(p=\tfrac12, a=3b\)，得 \(E[R(\Pi_{full})]=+\tfrac{b}{2}>0\)）。后件假。

**第 5 步（结论）**。存在模型使 \(E[R(\Pi_{tested})]\le 0\) 而 \(E[R(\Pi_{full})]>0\)，故蕴含不成立。\(\blacksquare\)

### 逐层实例化（「任一层不同」都触发同一反例骨架）

| 缺的层 | \(M^\star\) 的实例化 | 对应本仓库现状 |
|---|---|---|
| \(\Gamma\)（证书缺失） | \(M^\star\) = 缺失证书触发的交易 | 旧 AllTrend 锁死使一类证书缺失（定理 3） |
| \(Z\)（状态粗化） | \(M^\star\) = 仅在被混桶淹没的细状态 \(z^\star\) 上交易（定理 2 给出显式分布） | \(d\) 不在 z、ForceState 不在主裁决桶键 |
| \(E\)（出场不同） | \(M^\star\) = 出场规则差 \(E_{full}-E_t\) 引致的收益差 | 谱系 684 号：τ^reverse 出场口径伪影翻转下游结论 |
| \(M\)（资金管理不同） | \(M^\star\) = 仓位函数 \(q(z)\propto 1/d(z)\) 与常数仓位之差 | 定理 2 第二方向 |
| \(K\)（可行集不同） | \(M^\star\) = 保证金/强平约束改变的可行交易集之差 | A10（对 \(\Pi_{signal\text{-}full}\) 为 OUT_OF_SCOPE，见冻结表） |

**推论 1.1（措辞界限）**：当前一切负结论的合法有效域是「\(\Pi_{tested}\)（= 已修复五步后的信号层口径）无 confirmed direction alpha」；对 \(\Pi_{full}\)（含执行层/资金层）**不产生任何**否定性结论。

---

## 定理 2（风险归一化混桶双向错误）

### 陈述

设状态 \(z\) 内结构止损距离 \(D\) 是随机变量（取值 \(d>0\)），逐笔毛收益 \(X\)，风险归一化边际 \(\mu_R(z,d)=E[X/D\mid z, D=d]\)，裸池化边际

\[
\mu(z)=\sum_d \mu(z,d)\,\Pr(D=d\mid z),\qquad \mu(z,d)=E[X\mid z,D=d].
\]

在风险预算仓位 \(q=\kappa/d\)（\(\kappa>0\)）与每笔固定成本 \(C>0\) 下，「d 层可交易」的经济强制定义为

\[
\text{tradeable}(z,d)\iff E[\,q X - C\mid z,D=d\,]>0\iff \mu_R(z,d)>C/\kappa=:\theta>0 .
\]

则以下两类错误均可发生（各给显式分布）：

- **(i) 错杀**：\(\exists d:\ \mu(z,d)>0\)（且 tradeable）但 \(\mu(z)\le 0\)——裸池化桶判「不可交易」，淹没了可交易 d 层。
- **(ii) 错纳**：\(\mu(z)>0\) 但 \(\forall d:\ \lnot\,\text{tradeable}(z,d)\)，且整体净期望 \(E[qX-C\mid z]\le 0\)——裸池化桶判「可交易」，风险归一化后每层与整体均不可交易。

### 假设（逐条 + 实装锚点）

| # | 假设 | 实装锚点 |
|---|------|---------|
| H2.1 | 结构止损距离 \(d>0\) 对每笔信号有定义，且完整策略的仓位函数为 \(q(z)\propto 1/d(z)\)（风险预算恒定） | 问题1.pdf 问题C；实装 = 本 goal acc-route-a-closure(i)（`mu_estimator.rs` 增 `d_bin` 维 + `q(z)=κ/d`；实装前 `MuClass`（`rust/src/theta_v0/backtest/mu_estimator.rs:91`）**无 d 维**——正是本定理刻画的混桶现状） |
| H2.2 | 每笔交易有严格正成本 \(C>0\) | `rust/src/theta_v0/backtest/l3_delta_r_alpha.rs:156-158`（`fee_rate = commission_bps+slippage_bps+tax_bps`） |
| H2.3 | 裸池化 \(\mu(z)\) 是 d-混合（全期望公式） | `rust/src/theta_v0/backtest/mu_estimator.rs:413-416` `observe`：不同 d 的观测推入**同一** `buckets[class]`——池化在代码层逐字发生 |

### 证明

**(i) 错杀方向。**

*构造*：\(D\mid z\) 两点分布：\(\Pr(D=1\mid z)=0.1,\ \Pr(D=10\mid z)=0.9\)。条件收益为点质量：

\[
X\mid(z,D=1)\equiv +1,\qquad X\mid(z,D=10)\equiv -0.5 .
\]

*计算*：\(\mu(z,1)=+1>0\)；\(\mu_R(z,1)=+1\)。取 \(\kappa=1, C=0.2\)（\(\theta=0.2\)）：\(\mu_R(z,1)=1>\theta\)，故 \(d=1\) 层 tradeable。而

\[
\mu(z)=0.1\cdot(+1)+0.9\cdot(-0.5)=-0.35\le 0 .
\]

即裸池化桶显示负边际，可交易的 \(d=1\) 层被 \(d=10\) 层的概率质量淹没。顺带：风险归一化池化 \(\mu_R(z)=0.1\cdot 1+0.9\cdot(-0.05)=+0.055>0\)——归一化视角与裸视角在同一分布上**符号相反**，混桶错误不是边缘现象而是符号级颠倒。∎(i)

**(ii) 错纳方向。**

*构造*：\(\Pr(D=1\mid z)=\Pr(D=100\mid z)=0.5\)；

\[
X\mid(z,D=1)\equiv -0.1,\qquad X\mid(z,D=100)\equiv +10 .
\]

*计算*：裸池化

\[
\mu(z)=0.5\cdot(-0.1)+0.5\cdot(+10)=+4.95>0 .
\]

逐层风险归一化：\(\mu_R(z,1)=-0.1\)，\(\mu_R(z,100)=+10/100=+0.1\)。取 \(\kappa=1, C=0.15\)（\(\theta=0.15\)）：两层均 \(\mu_R\le\theta\)，无一 tradeable。整体净期望（按 \(q=\kappa/d\) 执行全部信号）：

\[
E[qX-C\mid z]=0.5(-0.1)+0.5(+0.1)-0.15=-0.15\le 0 .
\]

即裸池化桶显示强正边际（+4.95），但完整策略（风险预算仓位 + 真实成本）在该桶上每层与整体皆亏。正边际全部来自「止损极远、单位风险收益极差」的 \(d=100\) 层的**未归一化**大数值。∎(ii)

**第 3 步（结论）**。两方向合并：不含 \(d\) 的状态 \(z\)（H2.3 现状）上的 \(\mu(z)\) 符号，对含 \(d\) 的完整策略的可交易性**既不充分也不必要**。故「\(d\) 未进入状态 ⟹ 风险归一化缠论策略尚未被检验」（问题1.pdf 问题C 的严格版）。\(\blacksquare\)

**推论 2.1（实装规格）**：acc-route-a-closure(i) 必须同时输出 \(\mu_R=E[X/d\mid z]\) 与裸 \(\mu\) 并列（两者符号可在真实数据上分叉，只报其一即丢失判别信息），且仓位 \(q(z)\propto 1/d(z)\) 进入回测执行层。

---

## 定理 3（趋势门吸收性锁死——旧否证无效）

### 陈述

设 canonical 中枢链 \(C_1,\dots,C_n\)，关系分类 \(\mathrm{Rel}(C_i,C_{i+1})\in\{\mathrm{Up},\mathrm{Down},\mathrm{Exp}\}\)，方向 \(d\in\{\mathrm{Up},\mathrm{Down}\}\)。定义

\[
\mathrm{AllTrend}_n(d)=\forall i\in[1,n-1]:\ \mathrm{Rel}(C_i,C_{i+1})=d ,
\]
\[
\mathrm{TrendBlock}(a,b,d)=\forall i\in[a,b-1]:\ \mathrm{Rel}(C_i,C_{i+1})=d,\qquad 1\le a<b\le n .
\]

则：

- **(A) 吸收性**：\(\mathrm{AllTrend}\) 以 0 为吸收态——若 \(\exists j:\ \mathrm{Rel}(C_j,C_{j+1})\ne d\)，则 \(\forall m\ge j+1:\ \mathrm{AllTrend}_m(d)=0\)（一个反例永久锁死）。
- **(B) 局部性**：\(\mathrm{TrendBlock}(a,b,d)\) 只依赖 \(\{\mathrm{Rel}(C_i,C_{i+1}): i\in[a,b-1]\}\)；任何 \(j<a\) 处的反例不影响其真值。
- **(C) 旧否证无效**：以 \(\mathrm{AllTrend}\) 为门的信号计数为 0，不构成「市场无一类买卖点」的证据——存在市场路径使局部趋势结构无限多而 \(\mathrm{AllTrend}\) 门下信号数恒为 0；且在温和的市场随机性假设下 \(\mathrm{AllTrend}_n=1\) 的概率随 \(n\) 指数衰减到 0，与市场是否存在趋势无关。

### 假设（逐条 + 实装锚点）

| # | 假设 | 实装锚点 |
|---|------|---------|
| H3.1 | 关系分类函数 \(\mathrm{Rel}\) 对相邻中枢对良定义、逐对独立计算（不引用链的其他部分） | `rust/src/theta_v0/classifier/center.rs` `classify_relation`（`decompose.rs:120` 调用点：仅传 `(&centers[i], &centers[i+1])` 两参——局部性在函数签名层成立） |
| H3.2 | 旧实现的一类信号趋势门谓词 = \(\mathrm{AllTrend}\)（trend_class 作用在全历史中枢链上） | 历史形态记载：`rust/src/theta_v0/classifier/signal.rs:822`（「τ 从『全历史累积链 AllTrend』（吸收锁死谓词）」）、`decompose.rs` 模块头 5-7 行；task #143 删除 |
| H3.3 | 新实现的趋势门谓词 = maximal 等标签 run 分解上的 \(\mathrm{TrendBlock}\)（局部量词） | `rust/src/theta_v0/classifier/decompose.rs:56` `fold_rel`、`:105` `decompose_resume`、`:159` `center_trend_gate` |
| H3.4 | （仅用于 (C) 概率部分）市场关系序列每步为非 \(d\) 关系的概率 \(\ge\varepsilon>0\)（对任意联合分布，仅要求逐步条件概率下界） | 实证支撑：BTC 全历史漏斗（`.chanlun/review-results/final-alpha-20260704.md` §4 F2）L0 层 39,998 候选中环 2（局部趋势门）后剩 1,980——关系序列远非全同向，\(\varepsilon\) 显著为正 |

### 证明

**(A) 吸收性**。设 \(\exists j\le n-1:\ \mathrm{Rel}(C_j,C_{j+1})\ne d\)。对任意 \(m\ge j+1\)，量词域 \([1,m-1]\supseteq\{j\}\)，故合取式含假的合取项 \(\mathrm{Rel}(C_j,C_{j+1})=d\)，于是 \(\mathrm{AllTrend}_m(d)=0\)。又因追加中枢只扩大量词域（H3.1 下已有关系值不因追加改变——`decompose.rs` 前缀稳定性把这一点做成了实装不变量），序列 \((\mathrm{AllTrend}_m)_m\) 单调不增且一旦为 0 恒为 0：0 是吸收态。∎(A)

**(B) 局部性**。\(\mathrm{TrendBlock}(a,b,d)\) 的定义是量词域 \([a,b-1]\) 上的有限合取，其真值是 \(\{\mathrm{Rel}(C_i,C_{i+1})\}_{i\in[a,b-1]}\) 的函数（H3.1 保证每个合取项只依赖对应关系对）。测度论表述：\(\mathrm{TrendBlock}(a,b,d)\in\sigma\big(\mathrm{Rel}_i: i\in[a,b-1]\big)\)，而 \(j<a\) 的反例事件属 \(\sigma(\mathrm{Rel}_j)\)，两 σ-代数由不相交坐标生成。故早期反例不改变任何 \(a>j\) 的 \(\mathrm{TrendBlock}\) 真值。∎(B)

**(C) 旧否证无效**。分两支：

*(C1) 存在性反例（确定性）*。构造关系序列：\(\mathrm{Rel}_1=\mathrm{Exp}\)，其后 \(\mathrm{Rel}_i=d\ (\forall i\ge 2)\)。则由 (A)，\(\forall m\ge 2:\ \mathrm{AllTrend}_m(d)=0\)，以 \(\mathrm{AllTrend}\) 为门的一类信号计数恒 0。但由 (B)，\(\forall b>a\ge 2:\ \mathrm{TrendBlock}(a,b,d)=1\)——局部趋势段无限多，按缠论定义（走势类型分解，PDF §6/§9.2；`decompose.rs` 模块头）一类买卖点候选可在每个趋势块末端产生。故「\(\mathrm{AllTrend}\) 门下计数 0」与「市场存在大量一类结构」**同时成立**，前者对后者零证据力。

*(C2) 概率量化（H3.4）*。对任意满足 H3.4 的市场分布，

\[
P(\mathrm{AllTrend}_n(d)=1)=P\Big(\bigcap_{i=1}^{n-1}\{\mathrm{Rel}_i=d\}\Big)\le (1-\varepsilon)^{\,n-1}\xrightarrow[n\to\infty]{}0 ,
\]

（逐步条件概率上界 \(1-\varepsilon\) 连乘）。即无论市场有无趋势结构，观测「\(\mathrm{AllTrend}\) 门全历史信号 ≈ 0」的似然都趋近 1。一个在几乎所有分布下都以概率 → 1 出现的观测，对区分「市场无一类」与「市场有一类」没有任何贝叶斯更新力（似然比 → 1）。故旧否证（「一类为 0 是市场现实」）无效。∎(C)

**实证对照**（非证明成分，锚点核对）：旧门下 BTC 全历史 L0 有 40,003 候选 0 通过、且锁死发生在数据第一天附近（问题1.pdf §1）；#143/#144 换 \(\mathrm{TrendBlock}\) 门后一类全级别 >0（漏斗环 6：L0=452/L1=7/L2=13/L3=9/L4=2，`final-alpha-20260704.md` §4 F2），2021 顶区 sell1 因果重放正验证——与 (C1) 的结构预言一致。

**推论 3.1**：任何以吸收性谓词为门的「全历史计数为 0」型否证，在换用其局部化谓词前一律无效；此类推理错误的一般形态即定理 1 的 \(\Gamma\) 层实例（门谓词错误 ⟹ 证书集合 \(\Gamma_{tested}\subsetneq\Gamma_{full}\)）。

---

## 三定理的合取含义

1. 定理 1 划定当前一切统计结论的**有效域上界**：\(\Pi_{tested}\) 层面。
2. 定理 2 说明即便在 \(\Pi_{signal\text{-}full}\) 内部，\(d\) 缺维的裸 \(\mu\) 也在两个方向上不可靠——acc-route-a-closure(i) 是信号层结论可靠化的必要条件。
3. 定理 3 说明旧的全历史否证已被谓词层错误消解；「当前无 confirmed alpha」的合法依据只能来自修复后口径（TrendBlock 门 + 三态判定），且服从定理 1 的有效域限制。

合法总结论（与 `TARGET_STRATEGY.md` §4 措辞纪律一致）：**当前已修复信号层没有 confirmed direction alpha；完整执行层和资金层尚未闭合。**

---

## 异质数学审

- 审查工具：codex CLI（`codex exec --skip-git-repo-check --sandbox read-only`，仓库惯例）。
- 审查命令：

```bash
codex exec --skip-git-repo-check --sandbox read-only \
  "审查 docs/formal-chain/有效域定理-20260704.md 的三个定理：逐条检查(1)假设是否完备且锚点真实存在;(2)证明每步是否严格(特别是定理1的非蕴含语义、定理2两方向的数值验算、定理3(C2)的概率上界);(3)反例构造是否显式且自洽。输出 PASS/FAIL 及逐条意见。"
```

- 审查结论：见文末「审查记录」节（由审查执行后回填；回填前本节状态 = PENDING）。

### 审查记录

（待回填）
````

（草稿全文完——与 git 对象 `4f94b3fdc2:docs/formal-chain/有效域定理-20260704.md` 逐字一致。）

---

### 选项 (b)（备选，仅当确定不恢复文件时启用）：改引

把两处引用由悬空文件改为「693 pending 谱系 + 问题1.pdf §2/§3」。**订正任务口径**：严格证明在 `问题1.pdf` **§3**（「严格证明：为什么未闭合时不能下『完整无 alpha』结论」，五元组 + ⇏ + M⋆ 反例），§2 是有效域定性声明——两节并挂才完整，单挂「PDF §2」缺证明本体。

**补丁 2-B-1**：`TARGET_STRATEGY.md:54`

前：

```markdown
**任一对象的负结论不得外推到另一对象**（严格证明见 `docs/formal-chain/有效域定理-20260704.md` 定理1）。
```

后：

```markdown
**任一对象的负结论不得外推到另一对象**（严格证明见 `.chanlun/genealogy/pending/693-strategy-object-triple-freeze-a11-a10-hard-ruling.md`「分离的逻辑必然性」节 + `docs/formal-chain/问题1.pdf` §2（有效域声明）与 §3（严格证明：M⋆ 反例，\(E[R(\Pi_{tested})]\le 0 \not\Rightarrow E[R(\Pi_{full})]\le 0\)）。
```

**补丁 2-B-2**：`TARGET_STRATEGY_MAXFULL.md:270`（同句同改，行内引用串替换即可）；并把 `:286` 影响声明中的 `docs/formal-chain/有效域定理-20260704.md`（边界互斥证明）改为「693 pending 谱系 + 问题1.pdf §2/§3（边界互斥证明）」。

注意：693 条目 status=pending（待 /ritual 追认），改引后出处链降为「pending 谱系 + PDF」，证明力弱于选项 (a) 的 L0 落盘文档——这是推荐 (a) 的理由之一。

---

## 验证记录（本镜像的只读核对清单）

| 核对项 | 命令/方式 | 结果 |
|---|---|---|
| recursive_fugue:79 伪标现场 | Read 主仓 :60-109 | 确认（line 79「原文（reinterp §2.1 引）」） |
| reinterp §2.1 上游伪标 | Grep `docs/spiral_physical_reinterpretation.md` | 确认（line 106「（原文：…）」，§2.1 起自 line 98） |
| 110 篇博文零命中 | `ls docs/chanlun/text/blog/*.md \| wc -l` = 110；Grep `docs/chanlun/text/` 三种措辞 | 零命中 |
| 用户洞察出处 | Read `project_session5_spiral_reinterpretation.md:35-64` | 确认（:48-49「用户关键洞察序列」首条） |
| 教义锚 021:62/66/70-72、053:32、038:16 | Read 主仓对应行 | 全部逐字确认 |
| 有效域文件工作树缺席 | Glob + `git cat-file -e HEAD:...` | 确认缺席（HEAD=main） |
| **订正：git 历史存在该文件** | `git log --all --diff-filter=A -- <path>` → `4f94b3fdc2`（227 行新增）；`git branch --contains` → 仅 `gap3-rework-codex9-fix`；`git merge-base --is-ancestor` → 否 | 任务前提「git log 均无」不成立；文件完整可恢复 |
| 定理1 内容恢复 | `git show 4f94b3fdc2:<path>`（只读） | 227 行全文恢复，与 693:48-57、deepresearch:128 互证一致 |
| 问题1.pdf 节位 | 直读 PDF（16 页） | §2=有效域声明；§3=「严格证明：为什么未闭合时不能下『完整无 alpha』结论」 |

**未做**（按任务边界）：主仓零写入、零 git mutation、零 cargo；正式补丁待主仓解禁后按本镜像执行。
