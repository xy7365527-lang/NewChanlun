# code-review：V1-V3 + #64 diff 双轴独立评审（matt code-review skill）

- 日期：2026-07-21
- 范围：worktree `/tmp/kimi-nest-mainline` HEAD=640609071d 未提交改动中的四片——nest.rs N2 力度门 hunks / nest_lifecycle.rs（untracked 全量）/ level_view.rs #64 additive hunks + mod.rs 注册段 / bin/v1_level_calib_fix.rs（untracked 全量）。level_view.rs/mod.rs 的 wave-1 hunks 超本轮授权面，未评审。
- 方法：matt code-review 双轴（Standards / Spec）并行子代理，本文件为聚合（未合并重排，两轴独立呈现）。
- 修复跟踪：major 发现已立票 **#78**（假反超 / as_of 单调守卫 / 声明越界 / exec-1 下溢）。

## Standards（2 硬违反 + 4 judgement calls）

### 硬性违反

1. **`nest_lifecycle.rs:4-5`** — 模块头宣称「唯一实装落点：新建本文件 + mod.rs 注册，**既有行零改动**」，但同 diff 中 `level_view.rs` 的 `provide_nest_candidate_events` 本体被重写（wrapper 迁移 + wave-1 窗化/dedup，`level_view.rs:708-720, 819-824, 868-878`），`mod.rs` 也新增 wave-1 字段/函数。违反 AGENTS.md 090「声明必须与实际能力一致」。
2. **`level_view.rs:699`** — 「输出与迁移前逐 bit 相等」的承诺无有效锁定：legacy 已变成 `ext.map(.event)` 薄包装（:711-720），测试 `seg_c_full_ext_matches_legacy…`（:1749-1789）只是 wrapper 与自身对拍（恒真）；同 diff 内 wave-1 改了底层计算路径。「逐 bit 相等（相对迁移前）」无任何测试/golden 可证，090 声明越界。同样措辞见 :513。

### Judgement calls

3. **`nest.rs:775`** — rung 级力度门 supersede #97 D1 旧口径，权威来源是「实装卡 §4 / R-1」；裁定链惯例是裁定 supersede 裁定，实装卡自封「裁定登记」是否越权需 Lead 确认。配套测试改写（:1751-1776 父级 false→true）削弱 #97 原断言面，已登记「§7 回归线授权改写」，仍属覆盖后退。
4. **`nest_lifecycle.rs:586-592`** — 钟序不变量（observed ≤ t*）仅靠 `debug_assert`；release 下静默破序，依赖 `assert_invariants` 被调用才暴露。
5. **`v1_level_calib_fix.rs:633-645`** — A3/A5 把对历史数据的经验期望固化为 exit-code 断言，贴近 v3「不用回测验证策略」边缘（探针只读定位已声明 :37-38，建议裁定层确认豁免）。另 :321 硬编码 `"BTC"` 无数据/证书错配校验；`:524` `c.exec - 1` 对 exec=0 下溢 panic。
6. **`nest.rs:472-506`** — `from_parts_for_test` 绕门证书载体，`#[cfg(test)]` + 诚实边界声明合规；与生产装配字段序一致性无构造性证明，仅靠注释承诺。

### 合规确认

- `mod.rs:84` 注册段、`nest_lifecycle.rs` 禁前视（:617 c 窗截 as_of）、等力口径（:65-67 明示工程口径不冒充教义）、trend 反超可达性诚实登记（:117-122）、白名单桥禁入证书真值路径（:107-116）均符合 090/CLAUDE.md。
- `level_view.rs` #64 新增函数（provide_pan_live_windows 拒未来价、validate_pan_live_alignment）无违反。

## Spec（3 缺失/部分 + 3 越权 + 3 疑似实错）

### (a) 缺失/部分

1. **trend 域真实反超未触发（部分）**——V3 卡 §7 T1 要求「判据源 = level_view.rs:538-539 终假路径」；实装 T1 用合成事件直接喂 divergence_confirmed 翻转，绕过真实验证（已披露，nest_lifecycle.rs:117-122 登记）。
2. **裁定 §5 验收线字面冲突**——r43-lifecycle-ruling-20260721.md §5「Invalidated 至少一个真实案例可触发、**可消费**、可查账」vs 实装 T11 断言 Invalidated **不可消费**（nest_lifecycle.rs:1833）。与 §2(a)「消费不放开」一致但与验收线字面矛盾，需裁定方澄清（编排者醒后）。
3. 验收线「cargo test 全绿」read-only 评审无法核验（已由实装工位自报 1785/0，独立复核待 #78 后重跑）。

### (b) 越权/范围外

1. **(d) 授权「加字段」实装为并行结构体**——裁定 §2(d)「授权 seg_c_full 为 provider 正式字段：加字段不改既有字段值」；实装新增 NestCandidateEventExt（level_view.rs:521-528）而非在 NestCandidateEvent 加字段（理由 :504-520：字面构造点在非授权文件）。additive 纪律守住，形式偏离已披露。
2. **`from_parts_for_test` 超 V2 卡授权**（卡 §4 生产改动清单无此项；nest.rs:470）。
3. **wave-1 hunks 超 #64 §5 授权面**——§5 对 level_view.rs 仅授权 (d)、mod.rs 仅导出；diff 内含 wave-1 方案 1/2/3 与 mod.rs 5a/5b 暴露面（~300 行）。wave1 卡存在，授权链需在实装波报告闭合。

### (c) 疑似实错

1. **缺数据判负叠加终态吸收 = 假反超（最重）**——活窗 hist/dif 缺失或坐标映射失败时 `force_ok = false`（nest_lifecycle.rs:811-819「诚实判负」），而 advance 第 5 步 `!obs.force_ok → Invalidated(ForceOvertake)`（:620）终态禁复活。spec §4.1「反超 ≔ 曾可证 ∧ 当前不再弱」——「不可验」≠「不再弱」，一个缺数据 prefix 可永久杀死身份。**→ #78 修复项 1。**
2. **as_of 无单调守卫**——T3 在 as_of=149 后以 125 喂入（:1090）构造「复活」；对 Provisional 身份，倒退 prefix 会写出违反 spec §5.3.3「一切钟 ≤ as_of」的钟（first_provable 取 as_of，:585）。终态吸收让该测试侥幸成立。**→ #78 修复项 2。**
3. **身份键未消费 (d) 交付物**——`LifecycleKey::from_event` 取 `event.interval_b` 作 seg_c_full（:172），确认分支下是收束坐标而非全段；NestCandidateEventExt.seg_c_full 未进键，裁定 (c) 的并轨实质路径未兑现（ disclosed，挂起随实装）。

## V1 标定（无发现）

`v1_level_calib_fix.rs` 与 v1-level-calibration-fix-20260720.md 逐锚吻合（climb:165、dist_of:211、climb_mono:249、`pred = exec - 1`:524、双 main:655-663、|dt|≤1440 门槛 :477/:490）；A5 按设计失败 exit=1（:644-651），与文档 §3.2/§4 落账一致。

## 一行总结

Standards 轴 2 硬违反（最重：level_view.rs:699 逐 bit 承诺无锁定）；Spec 轴 3 疑似实错（最重：nest_lifecycle.rs 假反超——缺数据可永久杀死身份）。修复票 #78。
