# #110 交付报告：投影层骨架 + 级别身份标签（SPEC #109 第一票，expand 阶段）

- 日期：2026-07-21
- 工位：worktree `/tmp/kimi-nest-mainline`（分支 kimi-nest-mainline-20260717）
- 工具：claude CLI opus（fresh context）
- 前置：SPEC #109、ADR chanlun/escalate/adr-level-identity-multiview-20260721.md、#107 调研
- 纪律：禁 git mutation ✓；禁改 Cargo.toml ✓；admission.rs 零改动 ✓；禁第二查法 ✓

## 改动面

- **新文件** `classifier/projection.rs`：LevelProjectionLayer 骨架（每级别一实例：身份信息 + 候选集 Rc 共享 + 跨级确认查询入口 + 时间跨度估计）
- **`bsp.rs:117`**：`pub level_origin: u32` 字段落地，参与 `PartialEq`（:173）
- **`classifier/mod.rs`**：classify_impl（~453-467）+ classify_with_tower_incremental（~2124-2148）stamping 路径接入 level_origin
- **`signal.rs`/`config.rs`**：核心接入（config.rs:368 默认关断言 `!c.level_projection.enabled`）
- 十余处构造器/测试夹具兼容性补 `level_origin: 0`

## 门关零开销

投影构建（Rc::clone、跨度扫描、Layer 分配）完整包在 `enabled` 分支内，关闭时仅留 `None`。

## cargo test 尾行

`test result: ok. 1824 passed; 0 failed; 132 ignored`（release，基线 1819 + 5 新测试）

## 红线核验

- admission.rs 一字不动（git diff 计数 = 0 处改动，by_end/typed_lookup/has_bridge_key 完好）
- 门关默认关有断言 + `constructs_nothing` 测试守护
- 禁第二查法：查询入口只产描述体不持判定；全仓 grep `level_projection` 无 admission/nest/runner 判定消费

## 未决项

1. impl 报告由编排者从 claude CLI 摘要补落盘（本文件）
2. 门关 bit-exact 靠"默认关+全绿"间接保证；更强证据可后续跑 wverify 基线快照比对
3. S-1 重复构建块（评审建议合并，可并入 #111）
4. SPEC story 5 要求"完整级别身份（级别、源级别链、方向）"——本票 LevelIdentity 仅含 level，#111 补齐
