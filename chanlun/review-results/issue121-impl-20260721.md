# Issue #121 实装报告 — signal.rs 浅模块清理（2026-07-21）

执行：GPT-5.6 Sol（codex-implementation，`codex exec -s workspace-write`）；规格与验收：Claude 本体。
性质：纯重构，零行为变化。ticket 未关闭。

## 改动清单

### 1. bsp_bits_disc 单一来源化
- `rust/src/theta_v0/backtest/signal.rs`：`fn bsp_bits_disc` → `pub(crate) fn bsp_bits_disc`（现 line 14）。
- `rust/src/theta_v0/backtest/econ_positive.rs`：删除逐字重复的私有 `fn bsp_disc`（原 ~211–219）；新增 `use super::signal::bsp_bits_disc;`（line 61）；5 处调用点（原 ~315/~4355/~5040/~5339/~5709）改调 `bsp_bits_disc(&p.bits)`；4 处邻近文档注释（现 131/246/278/279）同步改名。
- 范围外未动：`l3_delta_r_alpha.rs` 与 `src/bin/*` 的同名副本保留（issue 只点名 econ_positive.rs:212）。

### 2. 删除 ClassifyAt 死别名
- `signal.rs`：整块 `#[allow(dead_code)] pub(crate) type ClassifyAt<...>` 连同文档注释删除。`use std::rc::Rc;` 保留（`newly_confirmed_step` 仍用）。

### 3. 删除 candidate_stop_dist（任务 3 事实修正）
- 摸底发现该函数**零调用方**：fill.rs:1065 早已手写内联同一计算 `(entry_px - stop as f64 * tick_size).abs()`，故"inline 回调用方"退化为删除死函数 + 订正悬空文档引用。
- `signal.rs`：`pub(crate) fn candidate_stop_dist`（原 ~114–125，★A6 prereg-rev2-20260704 注释块）整体删除。
- 悬空引用原在 admission.rs ~1103，因并发重构已迁至 `rust/src/theta_v0/backtest/risk_gate.rs:61`，在新位置改写为：`// 入场路径（fill.rs 内联 \`entry_stop_dist\` 计算，同源 \`entry_structural_stop\`）用候选…`，语义保持。

## 验收（本体独立复核）

- `grep -rn "candidate_stop_dist\|ClassifyAt" rust/src/` → 零残留。
- `grep -n "bsp_disc" econ_positive.rs` → 仅 `bsp_bits_disc` 形式，无旧名。
- `cargo build --release --lib` → 触及文件无新警告（codex 侧确认）。
- `cargo test --release --lib` → **1837 passed; 0 failed; 132 ignored**（与基线 1837/0 一致，本体复跑确认）。
- signal.rs 由 125 行缩至 99 行。
- 未触碰 Cargo.toml；未执行任何 git 写命令；仅写 /tmp/kimi-nest-mainline。

## 遗留/备注

- econ_positive.rs bins（p100/p101/p105/p106 等 `src/bin/*`）仍各自持有 `bsp_disc` 副本，属 issue 范围外；若要全仓单一来源需另开 ticket。
- 本报告不关闭 #121，等待复核方裁定。
