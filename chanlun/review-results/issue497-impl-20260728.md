# #497 实施车收口报告（claude sonnet）

> 日期：2026-07-28；性质：实施车 stdout 收口报告原文
> 原始日志：/tmp/issue497-dispatch-20260728.log

票 #497 已完工。四个 commit，逐个可编译、逐个测试通过，收口报告如下：

## 结论

**commit SHA 列表**（均已 `cargo test --lib` 验证通过，逐个可编译）：
1. `edb3d9c765` — `ConfirmCursor`/`ConfirmCursorStore`/`ConfirmState` 归位 `level_view_store.rs`（level_view.rs 侧 `pub use` 保路径不变；相关字段/方法提升 `pub(super)`）
2. `5d54530c59` — 折叠测试内 19 处 `provide_nest_candidate_events_resident` 重复展开（新增 `pan_resident` 薄封装）
3. `1c1aa1d537` — `mod tests {...}` 整体迁出为 `level_view/tests.rs`（零改写，仅反缩进）
4. `02ef5f93b1` — `level_view/tests.rs`（2018行）按主题拆为 8 个 ≤800 行子模块

## 验证证据

**指纹/失败集前后对照**：基线 2104 passed / 26 failed / 137 ignored（26 条失败全部是并行车 #621 retrace_ledger/ 与既有 signal.rs digest guard 的既存红，与本票面无关）；本票 4 commit 全部完成后复测为 2130 passed / 1 failed / 137 ignored——**唯一剩余失败与基线唯一非 retrace_ledger 失败逐字相同**（`signal::tests::extract_signals_bit_exact_digest_guard`）。另外 25 条 retrace_ledger 失败在此期间转绿，系 #621 车并发推进所致（未touch该目录，非本票产出）。level_view 面自身：31/31 全程稳定通过，无变化。

**250k dump cmp**：pre=`e520cf7fa4`（本票开工前），post=当前 HEAD，同参 `P116_MAX_BARS=250000` 跑 `p123_fast_replay`。dump 文件 `cmp` exit=0，SHA-256 逐字节相同（`614b83...`）；stdout 唯一差异是 `prefix_s`（墙钟耗时字段，40.658→43.546），与仓内 #69 先例（shadow-494 报告）口径一致，判定零语义变化。

**测试函数名集合 IDENTICAL**：`cargo test -- --list` 剥离模块路径前缀后取纯函数名排序对比，diff exit=0（完全一致）。带模块路径的全限定名因子模块化而变化（`tests::foo` → `tests::confirm::foo` 等），这与仓内既有 #621 `retrace_ledger/tests/` 先例口径一致。

**文件行数前后**：`level_view.rs` 4072→1965 行（远低于票面 ~2100 目标）；`level_view_store.rs` 601→701 行；新增 `level_view/tests/` 下 8 个子文件，最大 701/566 行，全部 ≤800。

**Standards**：`cargo test --lib --no-run` 对新文件零新增警告；无函数超 50 行的新写代码（仅 `pan_resident` 8 行转发）。

## 偏离/存疑

1. **票面锚行号与实际不符**：票面写 2041..3926/:490-604/:3367-3911，均按当前 HEAD 重新核对定位（票面基于旧读数，属正常漂移，票面本身已预期"自行重核"）。
2. **可见性范围扩大**：为让搬到 `level_view_store.rs` 的 `ConfirmCursor` 全字段、`ConfirmCursorStore.cursors` 字段及 `retain_active_for_run`/`cursor_mut`/`poison_for_test` 三个方法能被 `level_view.rs` 侧确认核心逻辑及测试直接访问，从"私有"提升为 `pub(super)`（作用域仍锁定在 `classifier` 子树内，不外泄）。这是本次拆分的**必然代价**，非可选项——原代码大量直接字段访问（`cursor.k0`/`cursor.state`/`store.cursors` 等），跨兄弟模块访问私有字段是 Rust 强制要求。
3. **`ConfirmKey` 未按票面字面移动**：票面只点名 `ConfirmCursor`/`ConfirmCursorStore`/`ConfirmState` 三个类型，`ConfirmKey` 留在 `level_view.rs`（其构造语义绑定 `query`/`pair`，与 store 内部机制天然分属两层），仅 `level`/`run_window` 两字段提升 `pub(super)` 供 store 侧 `retain_*` 方法读取。
4. **测试拆分粒度是新增判断**：票面只给了"如 `level_view/tests*.rs`"的方向,具体切成 8 个主题文件（fixtures 共享夹具 + triple_anchor + projection_pairing + confirm + feature_gating + pan_memo_fixtures/a/b）是我按依赖关系和 ≤800 行约束做的拆分,已用编译+测试全绿验证。
