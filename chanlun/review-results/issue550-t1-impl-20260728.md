# #550 T1 底座切换与收尾实施报告（2026-07-28）

## 结论

本轮工作面已更正为 `ticket-550b`，基座为
`kimi-nest-mainline-20260717@7b4547b62371ac7cd1765323d2ac2f3ab7f4b6f5`。旧工位
`/private/tmp/wt-550` 只读，未改、未删。

三枚旧提交 `a353a45700`、`9c1eb49ed6`、`8c71b899e7` 与返工轮未提交设计已经平移到
新底座，并补上本底座独有的 `OwnedIncrementalClassifier` 事件入口。首轮三个逻辑提交
已由编排 session 落盘：`2c2214d30c`、`b562404307`、`aa7acd22c0`。本轮在
`aa7acd22c0` 上逐条修复双轴评审发现。

## 最终实现形态

- 候选在全量与增量分类器的逐级循环内产出；结构判定唯一调用
  `signal::judge_first_cached`，不从 `CandDeltaEvent` 反投影。
- 候选身份不含 MACD 力度、`force` 或 BSP bit；flat-strength 定向测试仍能产结构宽候选。
- `TowerCache.candidate_book` 跨 bar 持久保存 append-only 修订流；旧区间不会倒写首见钟，
  terminal 不复活。
- 通道返回三元组 `(Classification, tower, CandidateStreams)`：
  `classify_with_tower_events`、`classify_with_tower_events_incremental`、
  `IncrementalClassifier::classify_at_events` 与
  `OwnedIncrementalClassifier::append_bar_events`。
- `OwnedIncrementalClassifier::append_bar` 直走无事件增量入口；
  `append_bar_events` 才走事件变体，两者只共用 parse/tower/cache。
- `CandidateStreams` 为 `Rc<Vec<Rc<Vec<CandidateEvent>>>>`，通道只克隆 `Rc`。
- FNV-1a64 golden 锁定真实 `classify_with_tower_events` 输出流，当前值
  `16287327716591662250`；种子与素数收为命名常量。

## 真实 BTC 电池

只读输入：
`/private/tmp/kimi-nest-mainline/analysis/data_cache/btc_1m_full.json`

命令：

```text
cd /private/tmp/wt-550b/rust
./target/release/issue550_event_battery \
  /private/tmp/kimi-nest-mainline/analysis/data_cache/btc_1m_full.json 100000
```

输出：

```text
ISSUE550_BATTERY bars=100000 compared=100000 stream_levels=3 identities=246 revisions=250 active=244 levels={0: 198, 1: 38, 2: 8} pan_identities=241 pan_active=241
```

口径：窗口为数据首 100,000 bar；逐 bar 对拍借用式真实增量入口
`ParseLayerIncr + classify_with_tower_events_incremental` 与 owned streaming 入口
`OwnedIncrementalClassifier::append_bar_events` 的完整三元组，100,000/100,000 相等。
按 key 取最新 revision 后：身份流 246、总 revision 250、active 244；active
逐级 L0=198、L1=38、L2=8。Pan 身份流 241、active 241；`stream_levels=3`
只表示事件簿级别槽数，不再混作身份计数。

严格边界：100,000 bar 逐 bar电池对拍的是两种生产增量宿主；fresh-cache 全量
`classify_with_tower_events` 与增量三元组的逐字段等价由定向测试覆盖。没有声称对
100,000 个前缀逐个执行 O(n²) fresh 全量重算。

## TDD 与验收命令

- 红：移植返工后 `cand_event` 定向组 5 passed / 2 failed，失败恰为确认钟预期和 FNV
  golden 漂移。
- 绿：`cargo test --release --lib cand_event --no-fail-fast`：
  8 passed / 0 failed。
- 既有二元输出：
  `classify_with_tower_classification_equals_classify`，1 passed。
- fresh 全量三元组与增量三元组：
  `candidate_event_stream_fresh_incremental_equals_full`，1 passed。
- 修订富逐段锁：
  `candidate_event_stream_per_segment_full_replay_equals_incremental`，1 passed；
  真实事件流非空，并显式覆盖同右端投影变化产生 revision。
- 真实 classify FNV：
  `candidate_event_stream_classify_fnv1a_golden`，1 passed。
- 增量 bit-exact：
  `cargo test --release --lib bit_exact_synthetic --no-fail-fast`，3 passed。
- owned 既有入口：
  `cargo test --release --lib owned_ --no-fail-fast`，2 passed / 1 ignored（真实数据旧测试按
  原注解 ignored；本轮 100k 独立电池已实跑）。
- `cargo build --release --bin issue550_event_battery`：通过。
- 目标文件 rustfmt 与 `git diff --check`：通过。
- 全量 lib：`2028 passed / 1 failed / 135 ignored`。唯一失败为既有
  `signal::tests::extract_signals_bit_exact_digest_guard`；本轮 `signal.rs` 零改动，
  失败摘要 `0xe6a263e343e43845` 对既有期望 `0x90c79ee617e11392`，按 090 原样登记。

## 对照纪律与 #535 四类禁项

- `nest` 三件、`CandDeltaEvent`、`NestCandidateEvent`、typed 链：最终 diff 为 0。
- classify 外二次候选塔扫描：新通道无。
- 力度进入候选身份：无。
- 终态几何配首见钟：无；事件簿跨 bar 推进。
- 区间回缩不静默：追加 `Invalidated`；同投影才零输出。

## 二轮双轴评审修复

1. Pan 域直接消费每级既有 `pan_div: Vec<PanDivCert>`，不调用 nest 侧函数、不另造判据；
   `kind=Pan`，比较 `seg_a/seg_c`。
2. 首次进入 Confirmed 时使用 observation 自带确认钟，否则使用本次 `as_of`；已确认后不后移，
   不再覆盖为 `first_provable_at`。
3. revision 闸改为 `same_projection`；同右端但投影变化追加 revision，区间回缩追加
   Invalidated。
4. 增量候选扫描挂到既有 BSP key/frontier：key 未变复用缓存观察，不再每 bar 全级
   decompose+judge。
5. `append_bar` 恢复直调无事件增量函数；事件簿使用 `Rc`，删除全簿 `to_vec()` 深拷贝。
6. 候选观察统一抽到 `cand_event::observations_for_level`，全量与增量调用同一公共函数。
7. `StructuralPredicates` 已诚实注明生产点因 judge 通过而恒全真；部分前提形态归 #551。

## 快照与提交

- 基座 commit：`7b4547b62371ac7cd1765323d2ac2f3ab7f4b6f5`
- 首轮提交：`2c2214d30c`、`b562404307`、`aa7acd22c0`。
- 二轮源码 diff SHA-256（不含报告）：
  `caa29eb55bc1b972ee52b1aca5b830f8d1a118427e8dcbd9c9720cae5d62ac2d`。
- 二轮修复快照 commit：本执行环境无法创建共享 worktree 的 `index.lock`
  （`Operation not permitted`），待编排 session 以本工作树五个 tracked 文件直接落盘；
  不伪造 commit id。

## #550 验收逐条

1. 底座更正与旧工位保留：PASS。
2. 三 commit + 返工设计移植：PASS；三提交已由编排 session 落盘。
3. 每级内联产候选、judge 单一来源、力度不进身份：PASS。
4. `TowerCache` 跨 bar 事件簿：PASS。
5. 三元通道与四个入口：PASS。
6. `OwnedIncrementalClassifier` 透传事件尾：PASS（兼容式
   `append_bar_events`，旧 `append_bar` 不破签名）。
7. BTC 100k 真实逐 bar通道对拍与非真空计数：PASS（口径见上）。
8. FNV golden 诚实更新：PASS。
9. 零既有行为变更三件套：PASS。
10. 对照族零改动、#535 禁项、090、rustfmt、报告落工作树：PASS；二轮报告入 Git
    commit 为 BLOCKED-ENV（全量 lib 的一个既有 signal golden 失败亦按 090 单列）。
