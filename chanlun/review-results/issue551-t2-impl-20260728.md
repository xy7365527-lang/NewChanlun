# #551（N1-T2）E2E-O 修订协议全谱 实施报告（2026-07-28）

分支 `ticket-551`，基座 = #550 tip `20a14b9dd9`。执行器 = claude CLI Opus 5 无头。
本票 4 枚逻辑提交：`4dd9d57c66`、`ca1fc1d9f1`、`88c6087b06`、`47d997e0f7`（+ 本节补登）。

## 结论（一句话）

四态从「宽于产出的空壳」变为**生产可达全谱**：Unresolved 与生长修订在真实 BTC 上机器可证；
空 L0 丢失效已修；裁定(i) 两锁落地并在活假设域上无条件成立。同时暴露一个**必须由编排者裁决
的定义矛盾**（因果簿的历史相关终态语义 ⊥ fresh-full 的历史无关语义），已按 no-workaround
钉死成测试、未用任何 workaround 抹平。

---

## 一、验收逐条

| # | 验收项 | 状态 | 证据 |
|---|--------|------|------|
| 1 | ∅→Provisional / Unresolved / Confirmed | **PASS** | `birth_provisional`/`birth_unresolved`/`birth_confirmed` 三路探针；BTC 100k `trend_states={Provisional:5, Unresolved:2}` |
| 2 | Provisional/Unresolved → Provisional/Unresolved/Confirmed/Invalidated | **部分 PASS** | U→P 全链坐实（`classify_chain_walks_unresolved_to_provisional_with_growth_and_clock`）；P→U 机制锁在案；**P→C / U→C 在 Trend 域生产不可达**（见 §四 登记 1） |
| 3 | Confirmed/Invalidated 终态不复活 | **PASS** | `terminal_block` 探针 + `disappearance_appends_invalidation_and_terminal_never_revives` |
| 4 | 上游身份或规则版本变 → 新 key 禁复活 | **PASS（规则版本）／上浮（上游身份）** | `CandidateKey.rule_version` + `rule_version_bump_mints_new_key_and_never_revives_terminal_one`；**上游塌空—回长**这一支是已上浮矛盾，见 §五 |
| 5 | 生长修订：revision+1 + supersedes 链，旧 revision 保留 | **PASS** | BTC 100k `growth_revisions=13`；合成全链 rev0→rev1 右端 72→80 |
| 6 | 投影相同零输出 | **PASS** | `idempotent_skip` 探针 + 同 as_of 重跑 Delta=∅ |
| 7 | 禁删除模拟失效 | **PASS** | 簿只有 `append`，无删除路径；失效走追加 `Invalidated` revision |
| 8 | observed_at / first_provable_at 一次写入不后移 | **PASS** | `observed_pinned`/`first_provable_pinned` 探针；BTC 实例 interval=(90164,96280) 而 first_provable=92411 |
| 9 | 同 as_of 重跑 Delta=∅ | **PASS** | 见 6；本票另修了破坏该性质的真缺陷（同 episode 归约，见 §二.4） |
| 10 | 候选身份消失 → Invalidated | **PASS** | `invalidated_absent` 探针 + 空 L0 全链锁 |
| 11 | oracle probe 覆盖计数（防真空绿） | **PASS** | `cand_event::event_probe` 16 路计数，测试逐路断言非零 |
| 12 | 全量≡增量逐 bar 仍绿 | **PASS** | 电池 `compared=100000/100000` 两增量宿主逐 bar 相等；`candidate_event_stream_fresh_incremental_equals_full` |
| 13 | golden 翻转走诚实更新先例 | **PASS** | `3608191067574153658 → 3542680779063880892`，翻转四项原因写在断言旁 |
| 14 | 既有面逐字节不动 | **PASS** | 见 §三 |
| 15 | #535 四类禁项逐条核对 | **PASS** | 见 §六 |
| 16 | 裁定(i) 投影等价锁 | **条件 PASS** | 非终态域无条件成立（BTC `payload_differ_live=0`）；终态域是已上浮矛盾，见 §五 |
| 17 | 裁定(i) 失效分叉方向锁 | **条件 PASS** | 未回长场景禁止方向为空（BTC `fork_fresh_only=0 revived=0`）；回长场景见 §五 |

---

## 二、设计决策

### 1. 结构门拆为单一来源（`signal::first_structural_gates`）

第一类的三道结构门（`direction` / `extreme` / `comparable`）从 `judge_first_cached` 内联短路
中提出为一等结构。`judge_first_cached` 改为消费它并保持「三门全成立才产点」的收缩语义 —— 摘要
`16618955402698307653` 未动，debug/release 双跑同签名，bit-exact 已证。

候选事件产出改读**同一个门**，因此 `StructuralPredicates` 三项不再恒真。这满足 audit §6「不复读、
只共享更上游单一来源」：不是新造第二套判据，是把既有判据的分量暴露出来。

### 2. 四态映射（Trend 域）

`comparable ∧ extreme` ⟹ `Provisional`；任一不成立 ⟹ `Unresolved`。

`direction` 恒 `true` 是**结构必然**而非占位：门不成立时 `first_structural_gates` 返回 `None`，
`CandidateKey` 的 `seg_a`/`c_start` 根本没有来源，候选身份不存在。凡入簿事件此项必真。

严格性论证：`Unresolved` 不是「另一类候选」，而是**同一个 key 在其生命史中的未决阶段**。因为
C 右端不入键，037:20 的「同 episode 递延」（`signal` 侧同名测试）保证破核心腿与破极值腿共享
同一 key —— 于是 U→P 是身份不变的状态转移，`Provisional` 集合与 #550 口径**逐字段一致**，
候选定义未被扩张。

### 3. 力度不进候选由约定升为编译期约束

`observations_for_level` 入参删去 `hist`/`dif`/`closes_tick`/`gauge`：候选产出在**类型层**够不到
MACD 序列。`close_src` 保留，仅用于 `comparable` 的坐标可映射性判定（不读价格、不算面积）。

### 4. 同 episode 归约（本票发现的真缺陷）

同一 key 可由同一 episode 内的多条同向腿各产一次门结果。它们是同一候选在**同一时刻**的多份
证据，不是时间序列。#550 路径逐条喂事件簿，导致同 as_of 重跑时靠前的短区间被误判为**区间回缩
→ Invalidated**，US8 幂等被破（本票 TDD 中先红暴露）。

修复口径（`merge_episode_leg`）：
- `interval` 右端取最新腿 —— I(C) = [λ_C, 当前 episode 最新同向段端点]（Q5 区间口径）；
- `extreme` 取**析取** —— 037:20 判的是 I(C) 包络破 b 包络，而「I(C) 包络首次破 ⟺ 某同向段端点
  首次越界」（`signal` 函数头等价性注记），故 episode 内任一腿破极值即整个 I(C) 已破，该事实不
  因后续腿端点回撤而消失；
- `comparable` 取最新腿 —— 可比较性由当前 I(C) 区间决定；
- `first_provable_at` 取**首个**使全谓词成立的腿的结构位。

### 5. `first_provable_at: usize → Option<usize>`

未决期不落钟（禁提前写），首次全谓词成立时一次写入，后续即使退回 `Unresolved` 也不抹去。
这是 #535 红线②「禁未来回填 first_provable_at」的机器载体 —— 旧的非 Option 类型在 Unresolved
上线后会被迫写入一个「尚不可证的可证时刻」，那是伪造。

### 6. `CandidateKey.rule_version`

规则版本变 ⟹ 全体 key 变 ⟹ 旧 key 走既有消失路径判终态、新 key 从 ∅ 起。落实 E2E-O
「规则版本变 → 新 key，禁复活旧 key」。值 1 = #550 结构宽候选口径 + #551 四态全谱映射。

### 7. 空 L0 早退补 `advance`

`classify_with_tower_incremental` 在 `l0_units.is_empty()` 早退且不推进事件簿 —— 本 bar 消失的
候选被推迟到下一个非空 bar 才判失效。**延迟非无痕**：失效钟落到错误的 as_of，且其间已消失的
候选仍挂 active。这是 #535 红线③「丢 Invalidated 路径」的时序变体，本票修掉。修法是对保留下来
的因果簿走一次 `advance(&[], as_of)`，即既有单一路径，不新增判据。

### 8. 增量 memo 判据仍充分

候选观察的输入（centers / blocks / segments / anchors / close_src）是 bsp 输入的**真子集**，
故 `bsp_key` 不变 ⟹ 候选输入不变 ⟹ 候选不变。既有 `cached_candidate_key` memo 保持保守正确。

---

## 三、既有面零改动核验

- 本票总 diff 仅 4 个文件：`cand_event.rs`、`mod.rs`、`signal.rs`、`bin/issue550_event_battery.rs`。
- 对照族 `nest.rs` / `nest_lifecycle.rs` / `nest_index.rs` / `cand_predicate.rs`：`git diff 20a14b9dd9..HEAD` **为空**。
- `CandDeltaEvent` / `NestCandidateEvent` / typed 链：本票 diff 中零出现。
- 消费方零接线：`cand_event::` 在 classifier 外只有 `incremental.rs:125` 与 `streaming.rs:11` 两处
  **类型层通道透传**（#550 既有，本票未改），无任何判据/门/订单消费点。
- `Classification` / tower 逐字段不动：`candidate_event_stream_fresh_incremental_equals_full`
  中 `full.0 == legacy.0`、`full.1 == legacy.1` 断言仍绿。

> **提交卫生教训（登记 1）**：本轮曾对单文件跑 `rustfmt`，它会**递归格式化子模块**，一次污染了
> 18 个对照族文件。已全部 `git checkout` 回滚并改用「拷贝到临时文件后 rustfmt 再 diff」的
> 只读核验法。基线 `signal.rs` 本就有 1558 行不 rustfmt-clean 的既有代码，本票改后**仍是
> 1558 行**且新增区零 fmt 差异 —— 未引入新 fmt 债，也未重排既有面。
>
> **提交卫生教训（登记 2）**：`ca1fc1d9f1` 中一次按行区间的块替换**误删了两枚既有测试**
> （`candidate_event_stream_classify_fnv1a_golden`、
> `candidate_event_stream_per_segment_full_replay_equals_incremental`）—— 它们恰落在替换区间内。
> 通过「基线 `#[test]` 名单 vs 当前名单取差集」发现，已从 `20a14b9dd9` 原文恢复（golden 值同步
> 应用本票的诚实更新）。此后名册对账成为收尾必跑项。**教训**：按行/块区间替换源码时，必须以
> 测试名册差集而非行数增减作为「既有面未动」的判据。

---

## 四、090 登记（诚实边界）

1. **Trend 域 P→C / U→C 生产不可达**。Trend 候选的确认需要力度背书（E2E-D4 力度门 / 终端背书），
   SPEC #547 明列 Out of Scope（「本包事件不含 force_evidence」）。本票不伪造一个结构性「确认」
   判据来点亮这条边 —— 转移表本身允许该边，生产路径不产。
2. **`third_class_proof` 恒 `None`**（继承 #550 登记）：需 `judge_third` 接线，非本票范围。
3. **`extreme_proof` 恒等于 `key.seg_a`**（继承 #550 登记）：E2E-D3 形状位副本，无独立业务信息。
4. **`candidate_group_id` / `pair_id` 与 key 双射**（继承 #550 登记）：无独立信息。
5. **BTC 100k 上 `invalidations=0` / `transitions=0`**：中途消失与跨状态转移在该窗口未触发。
   机制由 `cand_event` 单测与「L0 塌空」全链锁覆盖，但**真实数据非真空证据缺失**，照实登记。
6. **终态载荷分叉的非真空只在真实数据可测**：合成夹具规整、终态候选上游证书不再变动，
   `differ_terminal` 恒 0。lib 锁只固定不变式方向（`differ_live == 0`），非零量度由电池给出
   （BTC `payload_differ=25`）。测试函数头已写明该口径边界，不伪装成全覆盖。
7. **数据源差异**：本票电池用 `~/Projects/NewChanlun/analysis/data_cache/btc_1m_full.json`，
   #550 报告用 `/private/tmp/kimi-nest-mainline/analysis/data_cache/btc_1m_full.json`。两副本
   内容不同，故计数基线本就不同（#550 报 246/250/244；同数据的本票切片 A 前基线为 266/270/264）。
   本报告所有对比均在**同一副本**内进行。

---

## 五、矛盾上浮（待编排者裁决，本票不擅自选边）

### 命题

**E2E-O 的终态语义是历史相关的；fresh-full 的语义是历史无关的。裁定(i) 要求二者在每 key
最新 revision 上逐字段相等 ⟺ 要求终态语义历史无关。二者不可同时成立。**

### 两个可观测面（同源）

**面 A — 终态载荷冻结（生产可达）**：Pan 域候选一入簿即 `Confirmed`（#550 决策：把
`judge_pan_div` 证书投影为确认候选）。终态不改写 ⟹ 其 C 段区间冻结在确认时刻；而
`PanDivCert.seg_c` 此后仍随 bar 推进而变，fresh-full 对同 key 重判得到**当前**区间。
BTC 100k 实测：`payload_differ=25`、`fork_causal_only=34`，且 `payload_differ_live=0`、
`fork_causal_only_terminal=34/34` —— **差异 100% 落在终态候选上，活假设域零差异**。

**面 B — 上游塌空后回长（机制可达，生产未触发）**：上游塌空使候选身份消失 ⟹ 因果簿判
`Invalidated`（E2E-O 正确）；数据回长到**逐字段相同**的结构后，fresh-full 重新产出同一个 key
⟹ 裁定(i) 明令禁止的**复活型分叉**。锁在
`escalated_upstream_regrowth_after_collapse_forks_in_forbidden_direction`。
BTC 100k `invalidations=0` ⟹ 该分叉当前生产不可达。

### 三条出路与各自代价

| 出路 | 做法 | 代价 |
|------|------|------|
| **甲** 收缩裁定(i) 口径 | 等价锁只在**非终态**候选上要求逐字段相等；终态域的分叉作为 E2E-O 的定义后果接受并计量 | 裁定(i) 原文「每 key 最新 revision 集合逐字段相等」需修订；已实测该收缩版无条件成立 |
| **乙** 改 Pan 域状态映射 | Pan 首入判 `Provisional`（活假设），留 `Confirmed` 给真正的终局条件 | 需要一个本票范围外的终局判据（E2E-D4，SPEC Out of Scope）；#550 已落地并经 #554 影子终审 PASS 的决策被推翻；golden 与电池计数大幅翻转 |
| **丙** key 补「上游代次」分量 | 塌空即代次 +1 ⟹ 回长后是新 key，面 B 消解 | fresh-full 无状态、算不出代次 ⟹ 两侧 key 集合不可比 ⟹ 投影等价锁**全面**失效；且不解决面 A |

**实装侧倾向（仅供参考，不代行裁决）**：甲。它是唯一不推翻既有结算、不破坏等价锁本身、
且已被实测支持（`payload_differ_live=0`）的选项；乙的正确性取决于 D4 落地后对「终局」的定义，
属 N3+ 依赖序。

---

## 六、#535 四类禁项逐条核对

| 禁项 | 核对 |
|------|------|
| **终态几何配首见钟** | 无。首证钟只在**首次全谓词成立**的那次观察落位，取该次的结构位；`Confirmed` 的确认钟取入簿 `as_of`（#550 已因果化），二者互不覆盖。BTC 实例 first_provable=92411 ≠ 右端 96280 ≠ 确认钟，三钟分离可观测。 |
| **未来回填 first_provable_at** | 无，且本票强化：类型由 `usize` 改 `Option<usize>`，未决期结构性地写不进去；已写入后 `make_revision` 只取旧值（`prior.and_then(...).or(observation...)`），探针 `first_provable_pinned` 计量。 |
| **丢 Invalidated 路径** | 本票**修掉一处既有实例**：空 L0 早退绕过 `advance` 导致失效延迟到下一非空 bar（§二.7）。其余路径：区间回缩、观察缺席两条均追加 `Invalidated` revision，探针 `invalidated_shrink`/`invalidated_absent` 分别计量。 |
| **吞回试段入父背驰段** | 无。C 区间右端只由 `merge_episode_leg` 取**同一 episode 内同向腿**的端点；episode 边界由 `departure_move_c_start` 单一来源判定（回中枢即断），回试段不同向、不进 I(C)。 |

---

## 七、测试与计数

### 计数三元组（干净工作树，快照 commit `88c6087b06`）

| 口径 | passed | failed | ignored |
|------|--------|--------|---------|
| `cargo test --release --lib` | **2041** | **1** | **135** |
| `cargo test --lib`（debug） | **2041** | **1** | **135** |

- 相对基线（`20a14b9dd9`：2030/1/135）**净增 11 passed、净增 0 failed**。
- 唯一失败 = `signal::tests::extract_signals_bit_exact_digest_guard`，**基线已红**、本票对该测试
  与其被测路径的摘要值零影响（left 侧 `16618955402698307653` 与基线逐位相同）。不将基线红伪报为全绿。

### 本票新增测试（11 枚，= 净增 passed 数）

`cand_event.rs`（5）：`breaking_core_without_envelope_extreme_is_unresolved_not_absent`、
`same_episode_legs_reduce_to_one_observation_per_key`、
`same_key_transitions_unresolved_to_provisional_and_pins_first_provable_clock`、
`first_provable_clock_survives_fallback_to_unresolved`、
`rule_version_bump_mints_new_key_and_never_revives_terminal_one`。

`mod.rs`（6）：`classify_chain_walks_unresolved_to_provisional_with_growth_and_clock`、
`causal_book_terminal_projection_equals_fresh_full_stream`、
`invalidation_fork_only_points_from_causal_book_to_absent_fresh_stream`、
`projection_divergence_is_confined_to_terminal_candidates`、
`empty_l0_bar_invalidates_live_candidates_without_delay`、
`escalated_upstream_regrowth_after_collapse_forks_in_forbidden_direction`。

**既有测试名册逐条对账**：`git show 20a14b9dd9:...mod.rs` 与当前文件的 `#[test]` 名单取
`comm -23`，差集为空 ⟹ 零删除、零改名。`cand_event.rs` 同法：7 → 12。

### TDD 红绿轨迹（择要）

- 结构门拆分后 `signal` 组 55/1/3，摘要 left 值与基线逐位相同 ⟹ bit-exact 先证再改语义。
- `same_key_transitions_...` 首跑**红**：`advance` 第二次非空 —— 暴露同 episode 多腿被当成时间
  序列、靠前短区间被误判回缩的真缺陷（§二.4）。归约落地后绿。
- `rule_version_bump_...` 首跑**红**：断言写成「delta 为空」，实为末轮新 key 缺席也会产失效。
  修正断言口径（旧 key 无新事件）后绿，探针数 `invalidated_absent=2 / terminal_block=1` 对账。
- 三个 classify 全链锁首跑**红**：合成夹具候选全为 Pan（终态），无修订、无非终态活候选 ⟹
  非真空前提不成立。据真实数据画像重构 `lifecycle_rich_layer` 夹具后绿。

### BTC 100k 电池

```text
输入：~/Projects/NewChanlun/analysis/data_cache/btc_1m_full.json（首 100,000 bar）
命令：cd /tmp/wt-551/rust && ./target/release/issue550_event_battery <json> 100000

ISSUE550_BATTERY bars=100000 compared=100000 stream_levels=3 identities=268 revisions=281
                 active=268 levels={0: 217, 1: 41, 2: 10} pan_identities=261 pan_active=261
ISSUE551_LIFECYCLE states={"Confirmed": 261, "Provisional": 5, "Unresolved": 2}
                   trend_states={"Provisional": 5, "Unresolved": 2} with_first_provable=266
                   growth_revisions=13 payload_revisions=0 transitions=0 invalidations=0
ISSUE551_FORK fresh_identities=234 causal_identities=268 payload_equal=209 payload_differ=25
              payload_differ_live=0 fork_causal_only=34 fork_causal_only_terminal=34
              fork_fresh_only=0 revived=0
```

**计数变化解释**（同数据副本，基线 = 本票切片 A 前 `identities=266 revisions=270 active=264`）：

| 变化 | 数值 | 原因 |
|------|------|------|
| identities | 266 → **268** | Unresolved 上线：破核心未破包络极值的候选此前被 `judge_first_cached` 直接丢弃（返回 `None`），现进候选域记为未决。净增 2 = `Unresolved:2`。 |
| revisions | 270 → **281** | 净增 11 = 新增 2 个 Unresolved 身份 + 生长修订被正确记录（`growth_revisions=13`，此前同 episode 多腿部分被当成回缩/重复处理）。 |
| active | 264 → **268** | 归约修复后不再有把同扫描短腿误判为回缩产生的伪失效；`invalidations=0` ⟹ 全部身份存活。 |
| levels | {0:198,1:38,2:8}→**{0:217,1:41,2:10}** | 同上两因合并的逐级分布（旧基线口径把每 key 多腿计入 active，新口径每 key 一条）。 |

**非真空证据（本票核心主张的机器可证部分）**：
- `trend_states` 含 `Unresolved:2` ⟹ **Unresolved 生产可达**，#550 的 090 登记 1 就此解除；
- `growth_revisions=13` ⟹ **生长修订生产触发**，#550 归口本票的登记项完成；
- 实例 `interval=(90164,96280) first_provable=Some(92411) revision=10` ⟹ 首证钟远早于当前右端，
  **「一次写入不后移」在生产上机器可证**（非仅单测机制）；
- `payload_differ_live=0`、`fork_fresh_only=0`、`revived=0` ⟹ **禁止的分叉方向在生产上为零**。

---

## 八、未覆盖项 / 后续线

1. **P→C / U→C（Trend 域）**：依赖 E2E-D4 力度门与终端背书，SPEC Out of Scope（→ N3+）。
2. **中途消失与跨状态转移的真实数据非真空**：BTC 100k 上 `invalidations=0 / transitions=0`。
   更长窗口或多标的电池可能触发，本票未跑（§四 登记 5）。
3. **US17 p123 dump 侧信道 + 双跑 SHA 封印**：归 #553（T4），本票未触。
4. **`candidate_is_sub` 跨级包装重建 + 真值表**：归 #552，本票未触。
5. **`mod.rs` 文件长度**：本票新增测试后仍超 800 行硬线（基线早超），归 #576 拆分票。
6. **矛盾裁决落地**：§五 三条出路择一后的实装（口径修订 / Pan 状态映射 / key 分量）另开工作。

---

## 九、快照与提交

| commit | 内容 |
|--------|------|
| `20a14b9dd9` | 基座（#550 tip） |
| `4dd9d57c66` | 结构门单一来源拆分 + Unresolved 生产可达 + `rule_version` + 同 episode 归约 + 覆盖探针 + FNV 诚实更新 |
| `ca1fc1d9f1` | 空 L0 当场判失效 + 裁定(i) 双锁 + 生命史全谱夹具 + 终态分叉边界机器化 + 电池分解 |
| `88c6087b06` | 上游塌空—回长的复活型分叉边界锁（矛盾上浮，不抹平） |
| `47d997e0f7` | 恢复被块替换误删的两枚既有测试（名册差集对账）+ 本报告入库 |

本节自指：`47d997e0f7` 之后的快照补登由一枚 docs commit 落盘（同 #550「报告快照节补登」先例），
其 hash 见 `git log 20a14b9dd9..HEAD`。

工作树干净；`rustfmt` 对本票四个改动文件均 clean（只读核验法，未递归格式化子模块）。
GitHub issue #551 **未关闭**（§五 待裁决）。
