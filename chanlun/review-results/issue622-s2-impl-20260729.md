# #622 S2 实施车收口报告（claude sonnet）

> 日期：2026-07-29；性质：实施车 stdout 收口报告原文
> 原始日志：/tmp/issue622-dispatch-20260728.log

#622 S2 实装完成。收口报告：

## 1. Commit SHA 列表（逐 commit 可编译，正序）

| SHA | 内容 |
|---|---|
| `41b93bad86` | audit.rs 新建（四类警报 append-only JSONL）+ adapter.rs 两枚新拒收变体 + mod.rs（`CenterRebased` 结构化 + 挂载 audit 模块）|
| `ba0e055f81` | book.rs 核心逻辑：guard_single_active 三分支拆分、kill_as_rebased、reconcile_window、link_restart 简化、两拍守卫、audit 接线、新增不变量 |
| `d27f3bb80b` | 四个新测试文件 + tests/mod.rs 挂载 + state_machine.rs 一条测试更新 |

## 2. 指纹对照

| 项 | 开工基线 | 收口实测 |
|---|---|---|
| `cargo test --lib` | 2143 passed / 1 failed(#491) / 137 ignored | **2175 passed / 0 failed / 137 ignored**（#491 在会话期间因并行线 #610 落地而转绿，与本票无关，`git log -- signal.rs` 可查） |
| `retrace_ledger` 子集 | 61 | **92**（+31 本票新增） |
| `--no-fail-fast` 全量 41 target | — | **全绿，零 error/FAILED** |
| Standards | — | 文件最长 671 行；函数最长 50 行（`guard_single_active`，压线合规）；零新增警告 |

## 3. AC 逐条 → 测试名映射

- **改口场景**（窗口变/中枢消失/同窗口零动作）→ `tests/rebase.rs` 9 条，含 `observe` 碰撞路径与 `reconcile_window` 显式路径两通道，及"真新 departure 不被误判为改口"回归防护
- **死人挂号 vs 迟到吸收**→ `tests/dead_center.rs` 6 条，含 MEDIUM-4 隔代序列证明
- **audit 流四类落流 + append-only + 不进折叠 + 篡改隔离**→ `tests/rebase_audit.rs` 13 条，`tampering_audit_log_never_affects_fold_journal_equals_book` 直接钉死裁定六
- **两拍守卫**（移交③ MEDIUM-2）→ `tests/two_pass_evidence.rs` 5 条

## 4. 移交五条逐项落位

1. **死人挂号判据统一**（MEDIUM-4）→ 判据换成 `death_certificate(anchor).is_some()`，前置于 `guard_single_active`，`link_restart` 内原有的"前任是否 Confirmed"分支因永不可达已删除
2. **两拍证据一致性守卫**（MEDIUM-2）→ `guard_terminal_evidence_consistency`，零改写，新拒收码
3. **audit 流外化**→ `audit.rs`，四类事件 + JSONL 持久化，`CenterRebased` 明确不进本流（真实终态事件走日志本体）
4. **LOW-2 副作用修复**→ 新增不变量 `assert_at_most_one_confirmed_per_anchor`，死人挂号拒收落地后该态已不可达
5. **MEDIUM-1（门卫钟护栏）**→ 未动，doc 已如实标注仍待编排者裁定

## 5. 偏离/存疑

- 并发协作：`mod.rs`/`tests/mod.rs` 与 #623 车共享，期间遭遇一次并发覆盖丢失（我的 `mod` 声明被清空），已发现并重新追加，两次复核确认无进一步碰撞
- `guard_single_active` 卡在 50 行硬上限，未再拆分（拆分会打散判据的单一决策点，权衡后保留）
- 收口验证期间遇到另一并行车（#576，mod.rs 拆分重构）短暂使整仓不可编译，等待其落盘后复测确认与本票无关、现已全绿
