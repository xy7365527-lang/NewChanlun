# #634 影子评审（独立第三视角）

- **票**：GitHub #634（`cand_event.rs` 按域拆 + #612 观察/事件类型分离）
- **日期**：2026-07-29
- **评审工位**：worktree `/private/tmp/wt-634`，分支 `ticket-634`，HEAD = main `7f0bad3607`；评审对象 = **未提交工作区全量 diff**
- **评审模型**：opus（新上下文，未参与实施，未读任何前置评审结论）
- **边界遵守**：只读评审 + 本报告一份；零代码改动、零 git mutation；仅在 `/private/tmp/wt-634` 内活动；前台单线程、无子代理；`cargo` 用仓内默认 `rust/target`
- **立场**：实施报告 `chanlun/review-results/issue634-implementation-20260729.md` 的每条声明一律**不采信**，全部重新实测

---

## 0. 结论

**CONDITIONAL PASS**

代码层面无缺陷：票面三条验收我逐条独立实测**全部成立**，四件套的实质结论（段 2 零行为变化、段 3 零运行时行为变化）我用比实施方**更强的工具**重新复算后仍然成立，未发现任何归不了账的逻辑改动。

条件全部落在**证据与报告的精度**上，不在代码：

1. **必改（关票前）**：实施报告 §4① 的「旧文件独有 16 行」与 §2 的「book.rs tests = 13」两处数字失真——前者是证据工具缺陷的产物（`comm` 在 UTF-8 locale 下静默漏配对，我实证漏 3 行），后者把段 2 中间态的数字填进了终态表格（终态实测 12）。四件套是本票命根，其载体数字必须准确。
2. **建议（编排方裁）**：段 2 中间态未固化为 commit（票面禁 git mutation 的连带后果），四件套 ①②③ 的对照物在交付面上**不可机器复算**。建议提交时拆两个 commit（段 2 纯移动 / 段 3 类型分离），让四件套恢复可复核；否则请在票面明记「段 2 证据由影子评审的归一化多重集复算替代」。

5 条 LOW 不挡关票，逐条列于下。

**发现计数**：HIGH 0 / MEDIUM 2 / LOW 5

---

## 1. 发现列表

### MEDIUM-1｜四件套①的证据管线会静默漏行，申报数 16 低估（实为 17）

- **位置**：实施报告 §4①（`chanlun/review-results/issue634-implementation-20260729.md:143-147`）
- **事实**：报告用 `comm -23 old_lines.txt new_lines.txt` 做「仅旧行」提取。`comm` 要求两侧输入按同一 `LC_COLLATE` 排序，而 `sort` 在 macOS 默认 UTF-8 locale 下对中文行的次序与 `comm` 的逐行比较不一致 → `comm` **静默漏配对**，不报错、不提示。我在终态上实测：

  ```
  comm -23（UTF-8 locale） →  92 行
  LC_ALL=C comm -23        →  95 行
  Python collections.Counter 多重集差 → 95 行
  ```

  漏掉的行里包含真实差异行，例如 `///    畸形态就此钉死且不可修复。`、`/// （issue550_event_battery / payload_eq）共用本口径，不再各列一份字段表。`
- **连带的具体低估**：A 类第 2 条 doc 重锚（`[`CandidateEventBook::advance_observation`]` → `[`super::book::CandidateEventBook::advance_observation`]`）因新路径变长触发 rustdoc 行的**折行重排**，旧侧实际有 **2 行**进「仅旧」（旧 `cand_event.rs:559-560` 两行 → 新 `key.rs:222-223` 两行，内容守恒、断行位置移动）。报告 A 类表格只把它记作 1 条。
  故段 2 中间态的旧独有行数 = A 类 4 + B 类 7 + C 类 6 = **17**，报告写 16。
- **判定**：数字失真，**结论不变**。见 §3 任务 1 —— 我用 Python 多重集复算全部 95 行，零归不了账的逻辑行。
- **要求**：报告订正为 17 并注明工具缺陷；机械证据类命令一律加 `LC_ALL=C`，或直接改用多重集计数（`comm` 不适合含中文的行集对比）。

### MEDIUM-2｜段 2 中间态未固化 ⟹ 四件套 ①②③ 的对照物在交付面不可机器复算

- **位置**：交付形态（全部改动留未提交面）+ 报告 §4 全节
- **事实**：四件套 ① 的对照物是「段 2 拆分完成、段 3 未开始」的中间态，③④ 的对照物是干净基线的 `--list` 清单（2721 条）与读数（2583/0/138、55 warnings）。这两个态在交付面上都不存在：worktree 只有终态，基线要 `git stash`/`checkout` 才能复现，而本票禁一切 git mutation。
  实测后果：我无法直接复跑报告 §4①③④ 的任一条命令得到同一数字，只能改用**间接手段**——(a) 段 3 换名归一化后的多重集复算，(b) 从 `git show HEAD:` 的旧文件静态提取 `#[test]` 名做叶名对拍，(c) 只对终态跑 `cargo test --lib`。
- **判定**：这是流程约束的连带后果，不是实施方的过失（报告 §7 也如实登记了「不关票、改动留未提交面是编排方指定形态」）。但它使本票**最核心**的验收项失去机器可复核性 —— 任何后续复核者都只能重复我这套间接手段。
- **要求（编排方裁）**：提交时拆两 commit（段 2 / 段 3），四件套即恢复 `git diff <commit1>^ <commit1>` 的机器复核；或在票面明记降级口径。

### LOW-1｜报告 §2 表格「book.rs tests = 13」与终态实测 12 不符（同表混用两个态的数字）

- **位置**：报告 §2 表格 book.rs 行（`:65`）
- **实测**：`grep -c '^\s*#\[test\]'` → key.rs **4** / book.rs **12** / observe.rs **3** / fixtures.rs **0**，合计 19。
  `cargo test --lib -- --list | grep cand_event` 也是 19 条（book 12 / key 4 / observe 3）。
- **归因**：13 是**段 2 中间态**的 book 域测试数（4+13+3 = 20 = 基线 20，与报告 §4③ 的映射自洽）；段 3 移除 2 个 `should_panic`、新增 1 个 → 终态 12。同一张表的「653 行」是终态数字，「13」是中间态数字 → 表格内部口径不统一。
- **要求**：订正为 12，或在表格明标该列为段 2 中间态。

### LOW-2｜新增的编译期锁测试落在 `book::tests`，但只依赖 key 域

- **位置**：`rust/src/theta_v0/classifier/cand_event/book.rs:430`（`observed_state_domain_excludes_invalidated_and_lifts_pointwise`）
- **事实**：该测试体（`:443-465`）只触及 `ObservedState`、`CandidateState`、`From<ObservedState>`、`is_terminal()` —— 全部定义在 `key.rs:87-102`，零 `book` 域依赖（不建 `CandidateEventBook`、不调 `advance`）。
- **判定**：与票面「三域各为单域」的划分原则有张力。有继承理由（它接替的两个 `should_panic` 原在 book 域），但按「主要被测对象」这条报告自陈的分配依据，它应落 `key::tests`。
- **不挡关票**：测试路径变化本身已在报告 §4③ 登记为降级说明；域归属属品味级问题，且移动它会再改一次测试完整路径。

### LOW-3｜`book.rs:44-48` 退役注释称「深度防御仍在 on_append」，未标明该防御只在 `#[cfg(test)]` 下生效

- **位置**：`rust/src/theta_v0/classifier/cand_event/book.rs:48`
- **事实**：`event_probe::on_append` 的**唯一调用点**是 `book.rs:117-118`，带 `#[cfg(test)]`：

  ```
  117:        #[cfg(test)]
  118:        event_probe::on_append(prior.as_ref(), &next);
  ```

  故两处 `unreachable!`（`:220` / `:236`）在**生产二进制里根本不被执行**。段 3 移除的 `reject_unobservable_invalidation` 反而是唯一无 `cfg` 的运行期锁（原在 `advance()` 入口）。
- **判定**：实质无害——被移除的边已由类型层保证，覆盖所有 build（强于原运行期 assert）。但该注释单独读会让人以为生产 build 里仍有一道运行期防御。报告 §2 别处明说了 hook 是 `#[cfg(test)]`，信息在案，只是这条注释的作用域没写。
- **要求**：注释补一句「（`#[cfg(test)]` 编译下生效）」即可。

### LOW-4｜cargo doc 实测补空白：cand_event 域 6 处 `private_intra_doc_links` warning

- **位置**：报告 §8 遗留 3 自陈「未验证（`cargo doc` 未跑）」
- **我补跑了**（`cargo doc --no-deps --lib`）：cand_event 域命中 6 处
  `book.rs:207`（`super::CandidateEventBook::advance_observation`）、`book.rs:208`（`super::make_revision`）、`book.rs:214` / `book.rs:217`（`super::invalidate`）、`key.rs:104`（`super::super::signal::FirstStructuralGates`）、`key.rs:222`（`super::book::CandidateEventBook::advance_observation`）。
- **性质判定**：**拆分前既有，非本票引入**。旧文件的对应 doc 链同样指向私有项（`advance_observation` 是私有 `fn`，实测 `book.rs:83` 无 `pub`；`make_revision`/`invalidate`/`signal::FirstStructuralGates` 同样私有），rustdoc 对旧代码会报同名 warning。全仓 `cargo doc` 既有 **542** 条 warning，属仓库既有状态。
- **不挡关票**：`cargo doc` 不在票面验收项内；本票 warning 口径是 `cargo test --lib` 的 55，实测相等（见 §4）。

### LOW-5｜仓外 `cand_event::tests::` 完整路径引用未核（报告已登记，我只能复核仓内）

- **位置**：报告 §8 遗留 4
- **我复核**：`grep -rn "cand_event::tests"`（全 worktree，排除 `.git/` 与 review-results）→ **0 命中**；`.github/workflows/` 内无 `cand_event` filter。仓内确无失配点。
- **仓外**（他人脚本 / 文档 / 其他 worktree）无法在只读评审内核实，与报告口径一致。属如实登记的开口风险，不挡关票。

---

## 2. 未成立的质疑（我查过但不构成发现）

逐条列出，以免后续复核重走：

1. **反序列化旁路**：若 `CandidateObservation` / `ObservedState` 可从外部数据反序列化，`Invalidated` 可能绕过类型锁。实测 `grep -rn "serde|Deserialize|Serialize" cand_event/` → **零命中**，无反序列化路径。`reject_unobservable_invalidation` 的死代码论证成立。
2. **`merged()` 的 match 因换三态而改语义**：实测 `observe.rs:119-125` 与旧 `cand_event.rs:673-679` **逐字等价**（同带 `_ =>` 兜底，仅枚举名换），行为不变。
3. **`From` 不是单射 / 像含 `Invalidated`**：实测 `key.rs:93-102` 三臂一一对应，像 = 四态去掉 `Invalidated`，穷尽无兜底。
4. **impl 块内 pub 方法在拆分中被改了可见性**：多重集对比的「仅旧」95 行里，含 `pub` 的只有 2 行（`pub fn resolved_state(self) -> CandidateState`、`pub state: CandidateState`），二者都是段 3 授权的类型收紧 → 其余全部 pub 方法逐字保留。
5. **域夹带**：`observe.rs` 提及 `CandidateEventBook` **0** 次；`book.rs` 生产段（1-348 行）提及 `structural_observations`/`pan_observations` **0** 次；`key.rs` 提及 `CandidateEventBook` 仅 1 次且在 doc 链接内。三域边界干净。
6. **「加变体即编译红」被夸大**：实测成立且有两道——`key.rs:96` 的 `From` match（生产侧）与 `book.rs:450` 的测试 match 均无 `_` 兜底，加变体触发 E0004；若开发者补臂绕过，`book.rs:455` 的 `assert_ne!(lifted, CandidateState::Invalidated)` 运行期兜住。报告未越界宣称「不可能」。

---

## 3. 任务清单 8 项逐项结论

### 任务 1｜四件套独立复算 —— **① PASS（工具订正）② PASS ③ PASS ④ PASS**

#### ① 纯移动：我自己做的行级多重集对比

方法（不用报告的 `comm`，改用 Python `collections.Counter` 精确多重集差，规避 locale 与重复行两个坑）：

```
基线 rust/src/theta_v0/classifier/cand_event.rs（git show HEAD:）非空去缩进行 1331
终态 cand_event/{mod,key,book,observe,fixtures}.rs 合并             非空去缩进行 1465
多重集差：仅旧 95（87 个不同行）  /  仅新 229
```

因终态含段 3，我先做一次**段 3 换名归一化**（`ObservedState` → `CandidateState`）压掉换名噪声，剩 61 个不同行；再把 95 行逐条归账，全部落入四类：

| 类 | 条数（旧侧） | 内容 | 归账依据 |
|---|---|---|---|
| **段 3 授权移除** | 51 | `reject_unobservable_invalidation` 全体（签名 `:61`、assert 体 `:41/:54/:64/:76`、调用点 `:80`、其 doc 约 20 行）、2 个 `should_panic` 测试全体（attr ×2、`#[test]` ×1、fn 名 ×2、测试体 6 行、注释 6 行）、`on_append` doc 推导链第 2 条改写 | 票面段 3 明写「运行期 panic 锁退役（实施定，说明理由）」 |
| **段 3 授权换名** | 25 | `resolved_state` 返回类型与函数体、`pub state:` 字段、`make_revision` 的 `state:` / `confirmed_at:`、`merged` 的 match 臂、Pan 域 `state: Confirmed`、测试与夹具内的 `CandidateState::X` → `ObservedState::X` | 票面段 3「观察/事件类型分离」+「构造点改动随拆分落地」 |
| **A 类 doc 重锚** | 4 | `[`signal::FirstStructuralGates`]`（1 行）、`[`CandidateEventBook::advance_observation`]`（**2 行**，含折行续行）、`[`StructuralPredicates::resolved_state`]`（1 行，随 reject 移除消失） | 跨文件后原路径不可解析 |
| **B 类路径重锚** | 7 | `use super::decompose::center_trend_gate`、`use super::signal`、`use super::divergence::{..}`（rustfmt 拆多行）、`use super::super::types::{Center,Direction,Segment,Side}` 按域分拆、`use super::super::super::types::Tick`、`blocks: &[super::decompose::MoveBlock]` ×2 | 相对路径深度 +1，指向**同一** item |
| **C 类夹具可见性** | 6 | `fn center/segment/observation/deferred_envelope_break_scan/scan_observations/extended_episode_scan` → `pub(super) fn` | 夹具改由两域 tests 共用；`fixtures.rs` 整文件 `#[cfg(test)]`，生产面零影响 |

**归不了账的逻辑行：0 条。**（229 行「仅新」抽查同样零逻辑行：模块头 doc、`mod`/`pub use`/`use` 声明及 rustfmt 续行、`pub(super) fn` 签名、B 类新路径形式、段 3 的 `ObservedState` 类型 + `From` impl + 新测试。）

对 A 类第 2 条的守恒逐字核过：旧 `cand_event.rs:559-560` 两行 ↔ 新 `key.rs:222-223` 两行，文本守恒、仅断行位置移动（这正是 MEDIUM-1 里报告漏计的那 1 行）。

**注意口径**：以上是**终态 vs 基线**的归账，不等于报告 §4① 的段 2 中间态数字（见 MEDIUM-2）。段 2 的旧独有行数由归账推得 = A4 + B7 + C6 = **17**。

#### ② pub 项原样导出

旧文件顶层 pub 项 **21** 个，与 `cand_event/mod.rs:32-47` 的 re-export 面逐项对拍：

| 旧 pub 项（21） | 新导出路径 |
|---|---|
| `FNV_OFFSET_BASIS` `FNV_PRIME` `CANDIDATE_RULE_VERSION` `CandidateKind` `ParentFingerprint` `CandidateKey` `CandidateState` `StructuralPredicates` `CandidateEvent` `CandidateStreams` `interval_is_degenerate` `interval_is_sub` `intervals_touch` `intervals_are_disjoint` `CandidateProjection` | `pub use key::{..}`（`mod.rs:33-38`） |
| `CandidateEventBook` `event_probe`(pub mod) | `pub use book::{event_probe, CandidateEventBook}`（`mod.rs:32`） |
| `CandidateObservation` | `pub use observe::CandidateObservation`（`mod.rs:40`） |
| `observations_for_level`(pub(crate)) | `pub(crate) use observe::observations_for_level`（`mod.rs:39`） |
| `structural_observations_for_level` `pan_observations_for_level`(pub(crate)) | `mod.rs:47`（带 `#[allow(unused_imports)]`） |

**21/21 覆盖，可见性逐项相同，新增仅 `ObservedState`（段 3 授权）。** `mod book/key/observe` 是私有 mod ⟹ 外部只能经 re-export，`cand_event::X` 路径逐字不变。impl 块内 pub 方法零变化（见 §2 第 4 条）。

下游 diff（`classifier/mod.rs:3084/4647`、`cand_sub.rs:423` + import、`chain_cert/tests.rs:47/60/275/511/806` + import、`bin/p123_fast_replay.rs:4322` + import）逐行审毕：**除段 3 的 `CandidateState::X` → `ObservedState::X` 换名与 import 增名外，零改动**。`CandidateState` 仍留在 `cand_sub.rs` / `chain_cert/tests.rs` 的 import 里 —— 正确，事件侧读数仍用四态。

#### ③ 测试名 IDENTICAL

基线 `--list` 清单不可复现（禁 git mutation），改用静态提取 `#[test]` 后紧邻的 `fn` 名做叶名对拍：

```
基线 cand_event 测试名: 20    终态: 19
仅基线: observed_invalidation_at_birth_is_rejected_by_the_book_entry_lock
        observed_invalidation_on_live_candidate_is_rejected_by_the_same_lock
仅终态: observed_state_domain_excludes_invalidated_and_lifts_pointwise
```

差集**恰是**段 3 的三条授权变动，其余 18 条叶名逐字一致。终态 `cargo test --lib -- --list` 实测 cand_event 域 19 条（book 12 / key 4 / observe 3），与静态提取吻合。

完整路径确实插入了域段（`cand_event::tests::X` → `cand_event::{book|key|observe}::tests::X`）—— 这是票面「三域各带 `mod tests`」的定义后果，Rust 测试路径由模块路径决定，无从规避。报告把它登记为「降级说明而非豁免」并附逐条映射，口径诚实、我同意该判定。`fixtures.rs` 零 `#[test]`（实测 `grep -c` = 0）⟹ 夹具不产测试名，成立。

#### ④ 失败集逐字一致 + 对拍零 diff

我在当前终态亲跑（`cd /private/tmp/wt-634/rust`，前台，仓内默认 target）：

```
cargo test --lib --no-run   → warning: `newchan_rust` (lib test) generated 55 warnings；0 error；18.5s
cargo test --lib            → test result: ok. 2582 passed; 0 failed; 138 ignored; 0 measured；13.2s
cargo test --lib -- --list  → 2720 条 ': test'
rustfmt --check（cand_event/mod.rs，递归覆盖 4 子文件）→ exit 0，干净
```

与报告终跑读数（2582/0/138、2720、55）**逐字相符**。四个对拍/金值测试在同一跑里全绿：
`chain_certificate_book_classify_fnv1a_golden`（FNV1a 字节级金值）、`candidate_event_stream_per_segment_full_replay_equals_incremental`、`chain_certificate_book_incremental_equals_full_replay`、`causal_and_terminal_projection_drives_agree_on_common_chain_keys` —— 候选事件产出未变，全量≡增量成立。

known flaky `incremental_tower_scaling_dominates_full_synthetic` 在我这一跑里**绿**。它是耗时比断言（性能标度），与 cand_event 域无因果（拆分零算法改动，见 ① 归账），报告按 #619 L9 如实登记一红一绿并附单测 3 次全绿 —— 处置合规。

`formal/` 零改动（`git status --short` 无 `formal/` 条目）⟹ CLAUDE.md 的 fixture 漂移 gate 豁免成立，我未跑 `check_fixture_drift.py`。

**注**：55 warnings 与 2721 → 2720 的基线值无法在只读面独立复核。间接佐证：`cargo test --lib --no-run` 的 55 条 warning 中命中 `cand_event` 的 **0** 条 ⟹ 拆分未引入新 warning；全仓 `--list` 差额 −1 与 cand_event 域的 20→19 完全吻合，下游 diff 无任何 `#[test]` 增减。

### 任务 2｜段 3 转向忠于票面意图 —— **PASS**

- **「∅→Invalidated 编译期不可构造」达成**：`key.rs:87-92` 三态 `ObservedState`（无 `Invalidated`）；`observe.rs:26+` 的 `CandidateObservation.state: ObservedState`；`key.rs:93-102` 的 `From` 单射、像 = 四态去掉 `Invalidated`、穷尽无兜底；`book.rs` 的 `make_revision` 用 `observation.state.into()` 作唯一提升点。**无 serde 旁路**（cand_event 域 serde 零命中）⟹ 无任何路径能造出携带 `Invalidated` 的观察。
- **「观察侧构造点 grep 零 `Invalidated` 引用」实测**：`grep -rn "ObservedState::Invalidated" src/ tests/` → **0**；`grep -rn "CandidateObservation" src/ | grep -i invalidat` → **0**。全仓观察侧构造点（`p123_fast_replay.rs:4322`、`classifier/mod.rs:3084/4647`、`cand_sub.rs:423`、`chain_cert/tests.rs:47/60/275/511/806`、`observe.rs:257` + 经 `resolved_state()` 派生的路径）全部三态。**验收条款成立。**
- **事件侧保留四态的正当性成立**：`invalidate()` 是 `Invalidated` 的唯一构造点，是事件簿对既有 revision 的判决（写 `invalidated_at`、递增 `revision`、置 `supersedes_revision`）；`chain_cert/tests.rs:350` 断言 `Some(CandidateState::Invalidated)` 读的是 `ChainNode.state`（链节点证伪留痕）—— 是正当读取，不是漏改。段 3 只收紧观察侧入口，事件侧/链侧语义一字未动，方向正确。
- **票面前提订正的处置**：编排方已核（票面「`CandidateObservation` 不含 `Invalidated`」与 main 现状不符）。我复核 main 侧事实：旧 `cand_event.rs:62` 四态 `CandidateState` 同时担任 `CandidateEvent.state` 与 `CandidateObservation.state` —— 报告 §1 的订正描述与实测一致。改法（新增三态把边搬到编译期）**比票面记载的起点更靠前一步，但达成的是票面要的同一目标**，忠于意图。

### 任务 3｜panic 锁取舍 —— **PASS**

- **`reject_unobservable_invalidation` 移除**：死代码论证**成立**。它守的谓词是 `observation.state == Invalidated`，段 3 后该状态在观察类型上不可表达；且无 serde/反序列化旁路（§2 第 1 条）、无 `unsafe`/transmute 构造路径。留一个无边可守的 assert 是死代码，且会误导读者以为该边在类型层仍开放 —— 我同意移除。原位 `book.rs:44-48` 留了退役说明注释（含替代物与理由指针），处置得当（唯一瑕疵见 LOW-3）。
- **`on_append` 两处 `unreachable!` 保留**：论证**成立**。入参 `next: &CandidateEvent`，而 `CandidateEvent.state` 必须是四态（`invalidate()` 的产物就是 `Invalidated`）⟹ 此处「`next.state != Invalidated`」是**调用点纪律**（失效走 `invalidate()` + `on_invalidate()`，不经 `on_append`），不是类型保证。要变成类型保证须给事件侧再分类型（「非失效事件」/「失效事件」），那是事件簿数据模型重设计，确实超出「只拆不修」范围。保留使纪律破坏当场失败而非静默滑过 —— 判断正确。`book.rs:207-219` 的推导链第 2 条已如实更新为编译期口径。
- **2 个 `should_panic` 随 reject 移除，合理**：它们的测试体要构造 `observation((30,35), CandidateState::Invalidated)`（旧 `cand_event.rs` 内），段 3 后**编译期就构造不出来**（这正是段 3 的目的）。接替物 `book.rs:430` 的穷举 match 测的是编译期性质本身，且双层兜底（match 无 `_` + `assert_ne!` 像不含 `Invalidated` + `is_terminal` 逐点断言）。覆盖没有净损失：所测边从运行期前移到编译期，且编译期锁覆盖所有 build，而原 `should_panic` 只在 test build 生效。

### 任务 4｜指纹 −1 对账 —— **PASS（独立复算）**

| 项 | 报告 | 我实测 | 判定 |
|---|---|---|---|
| 终态 `cargo test --lib` | 2582 / 0 / 138 | **2582 / 0 / 138** | ✓ 相符 |
| 终态 `--list` | 2720 | **2720** | ✓ |
| 终态 warning（lib test） | 55 | **55** | ✓ |
| cand_event 测试数 | 19（隐含） | **19**（book 12 / key 4 / observe 3） | ✓ |
| −1 差额构成 | −2 `should_panic` +1 穷举 match | **静态叶名对拍逐条命中同三条** | ✓ |
| 各文件 ≤800 | 最大 653 | **653 / 400 / 326 / 136 / 47** | ✓ |
| 基线 2583 / 2721 / 55 | 报告读数 | **不可独立复核**（禁 git mutation） | 见 MEDIUM-2；间接佐证见任务 1④ |

**2583 − 2 + 1 = 2582 的等式成立**（左端基线值取报告读数，右端我实测）。

### 任务 5｜拆分域划分 —— **PASS**（一处品味级张力见 LOW-2）

| 域 | 生产/测试 | 单域性实测 |
|---|---|---|
| `key.rs` 326（生产 258 + tests 起 259） | 常量、`CandidateKind`、`ParentFingerprint`、`CandidateKey`、`CandidateState`、`ObservedState`、`From`、`StructuralPredicates`、`CandidateEvent`、`CandidateStreams`、四个闭区间谓词、`CandidateProjection` | imports 仅 `std::rc::Rc` + `types::Side` ⟹ **零外部调用、零可变状态**，与模块头自述一致；提及 `CandidateEventBook` 仅 1 次且在 doc 链接内 |
| `book.rs` 653（生产 348 + tests 起 349） | `CandidateEventBook`（`advance`/`invalidate_unseen`/`advance_observation`/`latest_event`/`append`）、`event_probe`、`invalidate`、`make_revision`、`same_projection` | 生产段提及 `structural_observations`/`pan_observations` **0** 次 ⟹ 不夹带投影产出 |
| `observe.rs` 400（生产 308 + tests 起 309） | `CandidateObservation`、`structural_observations_for_level`、`merged`、`StructuralScan`、`structural_observation`、`trend_observation`、`pan_observations_for_level`、`observations_for_level`、`stable_id`、`PAIR_ID_SEED` | 提及 `CandidateEventBook` **0** 次 ⟹ 不夹带修订协议 |
| `fixtures.rs` 136 | 6 个共用夹具 | 整文件由 `mod.rs:29-30` 的 `#[cfg(test)] mod fixtures;` 门控；6 个 `pub(super) fn`，可见面 = `cand_event` 及其后代；**零 `#[test]`** ⟹ 不进测试名清单 |

`pub(super)` 是本次拆分下的**最小**可见性（`pub(crate)` 会过宽），且仅在 `#[cfg(test)]` 编译单元内存在，生产面零影响 —— 处置正确。

### 任务 6｜Spec 逐条对账 —— **全部成立**

**范围（段 2 两硬事实）**

1. **「迁测试治不了本 ⟹ 必须真拆生产部分」**：实测拆分后单域**生产**行数 258 / 348 / 308，最大 348 ≪ 800（对照：基线生产 862 行、单文件 1417 行）。**治本成立**，不是靠迁测试凑数。
2. **「`event_probe` 是 `#[cfg(test)]`，必须留本 crate」**：五个新文件全在 `newchan_rust` lib crate 内（`src/theta_v0/classifier/cand_event/`），**未迁 `rust/tests/`**；hook 调用点仍是 `book.rs` 内的 `#[cfg(test)]` 行（`:75/:93/:103/:113/:117`）。探针断言有效性保住（`growth_revision_counts_same_state_right_end_extension`、`invalidated_shrink_counts_the_stale_geometry_path`、`birth_confirmed_counts_pan_domain_first_revision` 三个非零锁在我的终跑里全绿）。

**验收 3 条**

| 票面验收 | 判定 | 证据 |
|---|---|---|
| 拆分后各文件 ≤800 | **PASS** | 653/400/326/136/47，最大 653 |
| 观察侧构造点 grep 零 `Invalidated` 引用 | **PASS** | `ObservedState::Invalidated` 全仓 0；观察构造点全部三态；类型层已不可表达 |
| 零行为变化机械证据四件套（#359） | **PASS（证据精度有两处订正）** | 任务 1 四小项；订正见 MEDIUM-1 / LOW-1 |

其余票面条款：评审链（本报告 = 影子评审票）；`worktree /tmp/kimi-nest-mainline` 一条已被编排方改为 `/private/tmp/wt-634`（我在 wt-634 内评审，未触碰封存目录）；禁 git mutation —— 我全程只读，`git status --short` 与开工时一致。

### 任务 7｜报告复核 —— **两处数字失真，其余诚实**

| 报告声明 | 我实测 | 判定 |
|---|---|---|
| §4① 旧文件独有 **16** 行 | 段 2 应为 **17**（工具漏计 + 折行续行漏归） | **MEDIUM-1** |
| §2 表格 `book.rs` tests = **13** | 终态 **12** | **LOW-1** |
| §4③ 全仓 2721 条、cand_event 20 条叶名零差异 | 差额与我静态提取一致；2721 不可独立复核 | 可信（对照物缺口见 MEDIUM-2） |
| §4④ 段 2/段 3 测试边界声明（四件套只判段 2，段 3 证据单列 §5/§6） | 边界划得清楚，且段 3 的运行时等价我另行验证（§2 第 2/3 条） | **诚实**，无混淆 |
| flake 登记与 #619 L9 一致性 | 一红一绿如实登记 + 单测 3 次全绿 + 无因果论证；我这一跑绿 | **合规**，未粉饰 |
| §4① 末「首版多加一个 doc intra-doc 链接、行级对比暴露后改回」 | 终态 `fixtures.rs` 内该处确为反引号非链接 | 事故照实申报，**加分** |
| §7 关票门五子句 / 豁免声明 / 名分四态 / 只拆不修 | 逐条与实测吻合；「不关票、由编排方提交」的形态如实登记 | 合规 |

报告整体质量高：段 2/段 3 边界、票面前提订正、负控实验、遗留项 5 条都主动登记，没有把段 3 的有意变化混进「零行为变化」里。失真集中在两个数字，且都是**低估/串态**而非夸大战果。

### 任务 8｜残留项评估 —— **均不该挡关票**

| 残留项 | 我的判定 |
|---|---|
| `mod.rs:46` 的 `#[allow(unused_imports)]` | **不挡**。它换来的是「可见面逐字不变」（四件套② 的验收条款）与 warning 数严格等于基线 55；紧邻 `mod.rs:41-45` 有完整理由注释。对照佐证：同为 `pub(crate) use` 的 `observations_for_level`（`:39`）**未**加 allow ⟹ 该 attribute 精确覆盖真无消费者的两项，不是一刀切。缩面属行为变化，报告已列为独立票候选，处置正确 |
| `CandidateProjection` doc 链私有 method（`key.rs:222`） | **不挡**。我补跑 `cargo doc` 实测（LOW-4）：确有 `private_intra_doc_links` warning，但性质与拆分前相同（旧文件同样链私有 `advance_observation`），全仓既有 542 条 doc warning。`cargo doc` 不在票面验收项内 |
| 测试完整路径插入域段、仓外引用未核 | **不挡**。仓内零命中我已复核（LOW-5）；路径变化是票面「三域各带 `mod tests`」的定义后果，无从规避；报告已如实登记为降级说明 + 开口风险 |

---

## 4. 指纹实测（本次影子评审亲跑，前台单线程）

```
工位     /private/tmp/wt-634/rust（分支 ticket-634，HEAD 7f0bad3607 + 未提交改动）
git status --short  M rust/src/bin/p123_fast_replay.rs
                    D rust/src/theta_v0/classifier/cand_event.rs
                    M rust/src/theta_v0/classifier/cand_sub.rs
                    M rust/src/theta_v0/classifier/chain_cert/tests.rs
                    M rust/src/theta_v0/classifier/mod.rs
                   ?? chanlun/review-results/issue634-implementation-20260729.md
                   ?? rust/src/theta_v0/classifier/cand_event/
                   （评审结束时与开工时一致，零 mutation；本报告为新增第 8 项）

cargo test --lib --no-run   0 error / 55 warnings（cand_event 域命中 0 条）/ 18.5s
cargo test --lib            ok. 2582 passed; 0 failed; 138 ignored; 0 measured / 13.2s
cargo test --lib -- --list  2720 条（cand_event 域 19 = book 12 + key 4 + observe 3）
rustfmt --check             cand_event/mod.rs 递归 4 子文件 → exit 0
cargo doc --no-deps --lib   全仓 542 warnings；cand_event 域 6 条 private_intra_doc_links（既有性质）
行数                        book 653 / observe 400 / key 326 / fixtures 136 / mod 47（全 ≤800）
生产行数                    key 258 / book 348 / observe 308（基线单文件生产 862、总 1417）
多重集对比                  非空行 1331 → 1465；仅旧 95 / 仅新 229；归不了账的逻辑行 0
叶名对拍                    cand_event 基线 20 → 终态 19；差集 = −2 should_panic +1 穷举 match
观察侧锁                    ObservedState::Invalidated 全仓 0；cand_event 域 serde 0
formal/                     零改动 ⟹ fixture 漂移 gate 豁免（未跑 check_fixture_drift.py）
```

---

## 5. 给编排方的处置建议

1. **合入前要实施方改两个数字**（MEDIUM-1 / LOW-1）：§4① 的 16 → 17（附 `comm` locale 缺陷说明），§2 表格 book tests 13 → 12（或标注为段 2 中间态）。这两处是报告文本修订，不动代码，不需重跑。
2. **提交时拆两 commit**（MEDIUM-2，可选）：commit A = 段 2 纯移动（5 新文件 + 删旧文件，下游零改动），commit B = 段 3 类型分离（`ObservedState` + 4 个下游构造点）。这样四件套 ① 恢复 `git diff` 机器可复核，也让「段 2 下游零改动」这条声明落成可验证的事实而非报告断言。若不拆，请在票面明记降级口径。
3. **三条一行注释级修订**（LOW-2/LOW-3，可选，或另开票）：`book.rs:48` 补「（`#[cfg(test)]` 编译下生效）」；`observed_state_domain_excludes_invalidated_and_lifts_pointwise` 若要严格单域可移入 `key::tests`（会再改一次测试完整路径，建议不动）。
4. **代码本身可以合**：三条票面验收全成立，零行为变化的实质结论经独立复算成立，无 HIGH 发现。
