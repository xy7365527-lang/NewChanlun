# #712 收口批：#645 影子评审 PWC 条件清偿（HIGH-1 / MED-1 / MED-2 / LOW×3）

- **日期**：2026-07-29
- **工位**：`/private/tmp/wt-712`（分支 `ticket-712`，main 尖端 `dc79d85ce5`）
- **评审基准**：`chanlun/review-results/shadow-645-review-20260729.md`（⚠HIGH-1 / ⚠MED-1 / ⚠MED-2 / ⚠LOW-1/2/3）
- **参照面**：`/private/tmp/kimi-nest-mainline`（封存只读，仅 `git show`/`grep`，HEAD 实为 `632ae9eb58`；评审报告点名的
  `797c9ad35c` 是该工位早于本次收口时的 tip，两者对本票涉及的文件内容一致，均已核验）
- **碰撞核查**：`#668`（`ticket-668b`）对 `classifier/mod.rs` 的唯一改动是 `:100-103` 新增 `pub mod bsp_bridge;`
  模块声明，不触达本票的 `:1802-2790`（`classify_with_tower_incremental` 函数体）区块，无碰撞。

---

## 基线 vs 终态

| | passed | failed | ignored |
|---|---|---|---|
| 基线（main 尖端，`cargo test --lib`） | 2582 | 0 | 138 |
| 终态（HIGH-1+MED-1+MED-2+LOW×3 后） | 2583 | 0 | 138 |

终态 +1 = 新增的 `l0_units_stays_in_sync_with_tower0_on_segment_ledger_shrink` 回归测试；**零新增红**。

---

## HIGH-1：段账本回缩检测前移到 `00_l0_units_build` 之前

**kimi 位点**：`797c9ad35c:rust/src/theta_v0/classifier/incremental.rs:66-91`（回缩检测 `if l0.segments.len()
< cache.last_l0_segments_len { cache.clear(); }` 是函数内**第一条语句**，先于 `00_l0_units_build`）。

**main 重放位点**：`rust/src/theta_v0/classifier/mod.rs:1802-1922`（`classify_with_tower_incremental` 函数头）。

**等价论证**：main 侧比 kimi 该处多带 `#543 D1a`（`rebase_txn` 观测 seam）——回缩清空前要把旧塔快照按级别
snapshot 成 `txn_cleared`，供后续三流对账；这段逻辑在 kimi 该 blob（早于 #543）里不存在，故不能逐字 cherry-pick，
只能语义重放：

1. 把 `txn_on`/`txn_bar`/`txn_cleared` 三个声明（原 `:1842-1850`，位于旧 `00_l0_units_build` 之后、旧回缩检测
   之前）连同回缩检测本体（原 `:1891-1906`，含 `#543 D1a` 的 `txn_cleared` 快照块整体）一并前移到
   `let l_max = ...;`（`:1808`）之后、`00_l0_units_build`（原 `:1813`，现 `:1855`）之前——三个声明必须先动，
   因为回缩检测块内部读 `txn_on`/写 `txn_cleared`，纯移动检测块而不移动声明会编译失败（未声明先用）。
2. 删除两处旧位置的重复声明/检测（原空 L0 分支后的 `:1884-1892` 声明块、`:1933-1948` 检测块），避免变量重复
   绑定。
3. 下游（level 循环 `:2100+`、循环尾 `:2696+`）对 `txn_on`/`txn_bar`/`txn_cleared` 的消费点位置不变，语义不受
   影响——它们读的是同一批变量，只是变量的声明位置前移，值不受求值顺序影响（`txn_on`/`txn_bar` 是对 `l0`
   参数和环境变量的纯读，不依赖 cache 状态；`txn_cleared` 仍在同一个 `if` 分支内首次赋值）。

移动后 main 侧函数体顺序与 kimi 侧一致：回缩检测 → `00_l0_units_build`（reuse 计算用的是**已清空**的
`cache.l0_units_cache`，回缩 bar 上 reuse=0 ⟹ 全量重建）→ `00b_l0_units_clone` → DIAG → 空 L0 检查 →
`cache.last_l0_segments_len` 更新 → 级别循环。

**验红证据**（HIGH-1 修复前，MED-1 测试先行搬入）：

```
thread '...l0_units_stays_in_sync_with_tower0_on_segment_ledger_shrink' panicked at mod.rs:3200:9:
assertion `left == right` failed: ★F2 不变式：回缩 bar 上 level_scan_units(1) 不得为空/失步（#613 收 #609 F2）
  left: Some(0)
 right: Some(4)
```

（与影子评审报告的复现读数 `0 vs 4` 完全一致。）

**变绿证据**（HIGH-1 修复后）：

```
test theta_v0::classifier::tests::l0_units_stays_in_sync_with_tower0_on_segment_ledger_shrink ... ok
test result: ok. 1 passed; 0 failed; 0 ignored
```

---

## MED-1：护栏随入活代码（`debug_assert_l0_units_in_sync` + 回归测试）

**kimi 位点**：
- 断言：`632ae9eb58:rust/src/theta_v0/classifier/incremental/mod.rs:357-366`（`debug_assert_l0_units_in_sync`），
  调用点 `:290`（`bump_tower_epochs` 之后、函数返回之前）。
- 回归测试：`632ae9eb58:rust/src/theta_v0/classifier/tests/cache_and_units.rs:33-61`
  （`l0_units_stays_in_sync_with_tower0_on_segment_ledger_shrink`）+ 夹具 `:133-156`
  （`segment_ledger_shrink_fixture`）。

**发现**：main 工位上 `classifier/tests/cache_and_units.rs` 与 `classifier/incremental/` 目录**均为死代码**
（`classifier/mod.rs` 无 `mod tests;`/`mod incremental;` 挂载该目录/文件），且该测试文件里已有的另外三个测试
（`p52_frontier_diagnostics_...`、`incremental_macd_dif_and_closes_tick_bit_exact`、
`cand_cache_guard_accepts_incremental_contract`）在 main 的**活代码**内联 `mod tests`（`mod.rs:3005`）里已有
逐字/等价副本——唯独 `l0_units_stays_in_sync_with_tower0_on_segment_ledger_shrink` 这一条从未进入活代码。

**main 重放位点**：
- 测试 + 夹具：搬入 `mod.rs` 内联 `mod tests` 块，插在 `incremental_macd_dif_and_closes_tick_bit_exact` 与
  `cand_cache_guard_accepts_incremental_contract` 之间（测试体 `:3228-3260`，夹具 `segment_ledger_shrink_fixture`
  `:3262-3285` 附近）。
- 断言函数：新增 `fn debug_assert_l0_units_in_sync(cache: &TowerCache, tower_snapshots: &[Rc<Vec<LeveledMove>>])`，
  紧接 `classify_with_tower_incremental` 函数体之后（`:2793-2801`）；调用点插在函数尾
  `oracle_probe::on_forest_dirty(...)` 之后、`return` 表达式之前（`:2781`），对应 kimi「T10 次序」
  （四缓冲放回 + epoch bump 之后）。

**等价论证**：main 无 `bump_tower_epochs` 独立函数（该逻辑内联在 `classify_with_tower_incremental` 尾部），故
调用点选在 main 侧语义等价的位置——`forest_epoch`/`generation` 两个 epoch bump 全部落定、`(Classification,
tower_snapshots)` 返回值构造之前。断言体逐字对齐 kimi（除注释里的符号名从 kimi 的 `l0_units()` 改为 main 的
`level_scan_units(1)` 对应字段 `l0_units_cache`，字段本身同名同类型）。

夹具测试改用 main 的实际公开访问器 `cache.level_scan_units(1)`（main 无 `l0_units()` 别名方法，
`level_scan_units(1)` 内部即 `Some(&self.l0_units_cache[..])`，语义完全等价，评审报告本身也是用
这个访问器复现的）。

**先验红 → 后变绿**：见 HIGH-1 节证据（同一条测试）。

---

## MED-2：`p123_fast_replay.rs:2845-2850` 守卫注释复核

**文件锚**：`rust/src/bin/p123_fast_replay.rs:2845-2850`（现行号，原评审锚 `:2845-2850` 未偏移）。

**复核结论**：注释原文「根因已在 classifier 侧修死（段账本回缩的 `cache.clear()` 前移到 units 构建之前…），
本守卫是契约面的独立可观测…不变式成立时本码恒 0」——HIGH-1 落地后，`cache.clear()` **确已**前移到
`00_l0_units_build` 之前，逐字对照实物**成立**，无需订正。

**补充论证（为何 L2/L3 级别同样受益，不止 L0）**：回缩检测原先只在函数体中部（空 L0 检查之后）执行一次，
前移后位于函数体最前——移动前后它始终是**整个函数体唯一一处** `cache.clear()` 调用点（回缩分支），前移只改
变了它相对 `l0_units_cache` 构建的先后关系，不改变它对后续**整个级别循环**（L1..L_max，含 `projected_units`
的增量重建）的覆盖范围。故 `scan_units_out_of_sync` 守卫（消费 `level_scan_units(level)`，level≥2 时读
`levels[level-2].projected_units`）在回缩 bar 上同样从「会命中」变为「恒 0」，与本条守卫注释的断言域一致。

未额外跑 `p123_fast_replay` 二进制端到端复现（不在收口四件的必需步骤内；HIGH-1 的单测已直接坐实
`level_scan_units` 契约恢复，逻辑覆盖同一段代码路径）。

---

## LOW 三条处置

### LOW-1：三访问器 ±1 下标口径互指注释 —— 已做

**kimi 位点**：`632ae9eb58:rust/src/theta_v0/classifier/tower_cache.rs:284-354`（长文档，逐一说明
`level_scan_cursor`/`level_scan_units`/`freeze_boundary` 三者对同一个 `level` 实参的偏移差异）。

**main 位点**：`rust/src/theta_v0/classifier/mod.rs:1254-1264`（三访问器 impl 块前的说明性注释块）。

**处置**：未整段照搬 kimi 长文档（避免与 main 侧已有的各访问器独立文档重复膨胀），改为在现有的
「#614 并线」说明块尾部追加一句互指小结，钉死三者的下标口径差异（`freeze_boundary(level)` 直读
`levels[level]`；`level_scan_cursor(level)` 读 `levels[level-1]`；`level_scan_units(level)` 读
`levels[level-2]`，level==1 特例为 `l0_units_cache`），并声明调用前须按各自文档核对，不可假设同口径。

### LOW-2：#563 L5 注释缺归档指针 —— 已做

**main 位点**：`rust/src/theta_v0/backtest/fill.rs:3721-3722`（`#563 L5` 订正注释）。

**处置**：追加一句，补上评审报告 ⚠LOW-10 给出的 blob 恢复指针——设计稿
`chanlun/review-results/gap2-gamma-candidate-dump-design-20260719.md` 已随 main #504 归档整删，恢复指针 =
kimi 侧 blob `d290516b34`（`797c9ad35c` 树下；本工位已用 `git cat-file -t d290516b34` 核实该 blob 在
`/private/tmp/kimi-nest-mainline` 存在，类型 `blob`）。

### LOW-3：`write_step` 下游收窄清单补 #647 —— 已做

**main 位点**：`rust/src/theta_v0/backtest/gamma_dump.rs:200-201`（`write_step` 文档）。

**处置**：追加一段，声明 #647 在 Nest/Xzd 门之后又加了一道入场结构复检门（第三次收窄同一份候选 Vec），
`chi_admit` 字段语义本身未破，但 `admitted − opened` 差的归因清单需相应补一项（Nest/Xzd + 入场复检 + 开仓
判据三者共同解释）。

---

## 停手项

无。收口四件（HIGH-1、MED-1、MED-2、LOW×3）全部完成，无遗留放弃项。

## 复现

```bash
cd /private/tmp/wt-712/rust

# 基线（对照，需先 git stash 本票改动）：cargo test --lib → 2582 passed / 0 failed / 138 ignored

# 终态
cargo test --lib theta_v0::classifier::tests::l0_units_stays_in_sync_with_tower0_on_segment_ledger_shrink
cargo test --lib                     # 2583 passed / 0 failed / 138 ignored
```
