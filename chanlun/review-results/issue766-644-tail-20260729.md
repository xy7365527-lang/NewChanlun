# #644 尾部收口批（issue #766，承 #758 MED/LOW 六+三件 + 终审后续）

- 日期：2026-07-30
- 票据：GitHub Issue #766（#644 尾部收口）；上游评审 = `chanlun/review-results/shadow-644-review-20260729.md`
  （issue #758），终审回退 = commit `5f7a5cb01e`（HIGH-1，恢复 m8.rs/report.rs/issue71_chi_gamma.rs 三件）
- 工位：`/private/tmp/wt-766`，分支 `ticket-766`（从 main 尖端切出后 main 又并入 #761，
  两分支正常分叉，本票 5 个提交自成一体，不受影响——见文末「分支说明」）
- 执行：全程前台单线程，禁子代理，禁后台任务
- 验收基线（终审提交 `5f7a5cb01e`，本票开工前）：`cargo test --lib` = **2604 passed / 1 failed / 138 ignored**
  （唯一失败 `theta_v0::env_registry::tests::no_stray_env_literals_outside_registry`，见下）
- 验收终态（本票收尾）：`cargo test --lib` = **2605 passed / 0 failed / 139 ignored**
  （+1 ignored = 新增 BTC 对拍 harness；红清零）

---

## 逐件处置（评审位点 → 处置 → 论证）

### 1｜tests.rs 重审（MED①）

**评审位点**：`shadow-644-review-20260729.md` MED-1——「纯重复零净增」判词只对 report.rs/m8.rs
各 2 个函数成立，20 个符号 main 零命中。tests.rs 未被该条直接覆盖（不在 15 件禁删清单内，
`shadow-644-review` :92 明言「不受 HIGH-1 约束」），但其自身删除理由（`use super::m8::*` 硬依赖
「m8.rs 已判清理」失去宿主）随 HIGH-1 恢复 m8.rs 而前提失效，需重新逐符号判定。

**处置**：**维持删除**，理由订正。

**论证**：`bcf83cf386^1:rust/.../tests.rs`（680 行，31 个 `#[test]`）逐符号核对 main
`wverify_run.rs::mod tests`（3546 行起）：
- 6 个符号（`deltafree_dump_roundtrip_and_pooling`/`symbol_stride_isolates_strata`/
  `q4_date_helpers_hand_calc`/`sigma_stdev_matches_hand_calc`/`profile_est2_shared_classify`/
  `deltafree_verdict_key_is_delta_free_four_tuple`）**逐字节比对**（结构化 diff）：main 版本
  是权威版——含两处实质差异（`ExitType::CloseShortDiff→CloseReverseOpen` 改名、
  `"DELTAFREE_DUMP"` 字面量→`env_registry::DELTAFREE_DUMP` 常量迁移），tests.rs 是改名/
  迁移**前**的旧快照。main 侧全绿（`cargo test --lib theta_v0::backtest::wverify_run::tests::`
  = 5 passed / 0 failed / 1 ignored）。零净增确认。
- 其余 25 个符号（`m8_*`/`layer4_*`/`fee_datum_*` 等）专测 m8.rs/report.rs 内符号——两文件虽经
  HIGH-1 恢复文件字节，但**未恢复 `backtest/mod.rs` 的 `mod m8;`/`mod report;` 声明**
  （`backtest/mod.rs:66` 仍只 `mod wverify_run;`），是不进编译的诊断存档件（详见项 2/8）。
  无可编译目标可测，零净增确认。

**订正**：删除理由从 #644 原判「宿主已删」改为「宿主诊断化（未进编译），且核心测试已在
main 侧以更新版本存在」。

**commit**：`7341f976e1`

---

### 2｜lee_row_cells 恢复登记（MED②）

**评审位点**：MED-2——`report.rs::lee_row_cells`（M4 接线「四层已在场」claim 的第四层，
m8 报表列生成器）随 #644 删除，抬高 #755 第二档接线成本，未作跨票登记。

**处置**：确认在位 + 状态精确化 + 跨票影响登记（本地记录，未推送 GitHub，见「停手项」）。

**论证**：`grep -n "fn lee_row_cells" report.rs` → `report.rs:378`，HIGH-1 已恢复。但同项 1
的发现——report.rs 未恢复 `mod` 声明，`lee_row_cells` 目前**不可被任何编译单元调用**。
`sizing.rs:271-280` 原「四层已在场」→「三层……不再指向它们」的 #644 订正（commit
`51b956fa6a`）前提是「report.rs/m8.rs 已删」，现已不成立，doc 已重写为准确现状：

> 四层符号（`cap_narrowed_levels` 字段本身、`n_cap_narrowed` 统计、逐级 sparsity 判据、
> `lee_row_cells` m8 报表单元格生成器）**均已在场**，但后者仍需先补 `mod report;` 声明才可
> 编译消费；#755 若要复用它而非重写内联版，接线成本 = 补声明 + 唯一填入者，比「三层」订正版
> 的估算（只差唯一填入者）更高，但也比 #644 原判要求「从头设计接口」更低。

**#755 跨票登记状态**：本票只在本地 commit（`b7f59d1569`，与项3捆绑）与本报告内登记，
**未**通过 `gh issue comment` 推送到 GitHub #755——`gh auth status` 已确认可用，但张贴公开评论
是不可静默撤回的可见动作，且本分支本身未 push（票面禁 push），登记内容目前只有本地可见，
先在报告内留痕，是否代为张贴 GitHub 评论留给用户决定（见文末「停手项」）。

**commit**：`b7f59d1569`（sizing.rs 部分）、`9ba6196b50`（m8.rs/report.rs 文件头补注）

---

### 3｜归因层门控（MED③）

**评审位点**：MED-3——`level_clock`/`level_attrib` 两段在 `pi_theta_fill_loop_overlay` 内无条件
逐 bar 执行（`collect_ticks` + `attribute_total`，BTC 全量量级每 bar ~8 次堆分配），但净额臂
（`pi_theta_fill_loop`）与声部臂（`pi_theta_fill_loop_voice`）都以 `level_ledger=None` 调用，
产物落进 `RunResult`（无此二字段的基础结果结构）逐 bar 白算白丢——`OverlayRunResult` 才有
`level_clock`/`level_attrib_n_*` 出口。

**处置**：**门控**（非新增出口）。

**论证**：核实调用关系——`pi_theta_fill_loop`（fill.rs:767）与 `pi_theta_fill_loop_voice`
（fill.rs:806）均以 `level_ledger=None` 转调 `pi_theta_fill_loop_overlay`；只有
`run_theta_v0_pi_overlay` 传 `Some(level_ledger)` 且只有它的返回类型 `OverlayRunResult`
携带这两段读数的出口。用既有 `level_ledger` `Option` 信号（M1 段已有的门控纪律）包住两段，
零新增状态位；两段读数均为纯累计只读诊断（`level_clock_stats.observe`/`attribute_total`
均不回写 `order`/`cash`/`units`），门控前后不影响任何判据。

**验证**：`cargo build --lib` 0 error；`cargo test --lib` 2605/0/139（不变，纯性能收益，无行为
变化）；`lee_readonly_layer_does_not_perturb_net_result_644` 回归锁仍绿。

**commit**：`b7f59d1569`

---

### 4｜pan_levels 真超集登记（MED④）

**评审位点**：MED-4——main `fill.rs` 的 `pan_levels.push` 无条件执行，是 kimi
`pan_candidates`（只收 `PreparedPanDiv::Candidate` 分支）的**真超集**（main 经 #282 收缩后
已无 Candidate/Record 二分）；#644 报告称「语义等价」不准确，且该出入点未进 #693 裁定②要求的
接口清单。

**处置**：代码位点补登记注释（非 bug，不改语义），登记进本报告接口清单（呼应项 6）。

**论证**：`fill.rs` 的 `pan_div_state.prepare(gated)` 之后 `pan_levels.push(lvl as u32)`
无条件执行——已在该行补一段登记注释，点明 kimi 侧 `Record(_reason)`（"无合法 parent/lot
identity 时不伪造候选"）分支在 main 不存在对应分叉，是模块头 E6「首见并过门」定义的更贴合
实现，但与 kimi 取数源不等价。实测影响面：默认 `ThetaConfig` 下 pan_div 惰性
（`level_attrib.rs:41`），该通道大概率实测恒空，但 #644 报告的 BTC 跑批未按 7 通道拆分验证，
无法确认是否响过——本票不新增验证脚手架（超出票面收口范围），如实登记待定。

**commit**：`6d3662068c`

---

### 5｜对拍载体 + 锁强化 + 降级追认（MED⑤）

**评审位点**：MED-5 三层问题——① §件4 三条不变量数字（`obs=14386 max residual=0
max|N|=15603`、`14386/106/11`、`14386/0/2533`）只来自一次性手跑，无脚本/无 `#[ignore]`
测试/无留痕命令，只能采信不能复核；② 回归锁 `lee_readonly_layer_does_not_perturb_net_result_644`
的非平凡性断言被削弱（只测 `n_observations>0`/`n_decisions>0`，跑过就必然成立，测不出结构钟
从未响这类退化）；③ #693 裁定①「与 kimi 读数对拍」被 #644 单方降级为「结构不变量复现」，
技术理由站得住但未回裁。

**处置**：① 落 `#[ignore]` harness；② **核实后未强改**（见下，原因是实测数据不支持）；
③ 随本报告提交编排者追认，不越权自裁。

**论证**：

1. **对拍载体**：新增测试 `lee_readonly_layer_btc_structural_witness_644`
   （`runner_tests.rs`，`#[ignore]`），窗口沿用 #644 报告原始选择（BTC 1 分钟，
   2017-08-17..2017-08-31，21360 bar，理由不变：数据集首段+首 10 条离场声部 pnl_v 幅度均
   >10 万+无需精选窗争议）。可复跑命令固化在测试文档注释：
   `cargo test --release --lib theta_v0::backtest::runner::tests::lee_readonly_layer_btc_structural_witness_644 -- --ignored --nocapture`。
   本工位无 BTC 数据缓存（`analysis/data_cache/btc_1m_full.json` 不存在），测试走既有
   DATA BLOCKER 优雅降级路径（同 `t1_anchor_supply_real_data_probe` 先例）——**本票未能在真实
   数据上重跑验证 §件4 的具体数字**，只完成了「载体固化」，数字本身仍待有数据的工位跑一次
   补全。断言用强见证 `identity_witnessed()`/`sparsity_witnessed()`。

2. **回归锁强化——核实结论：原计划不可行，已如实记录**。尝试把常跑锁的弱断言换成
   `w.identity_witnessed()`/`ov.level_clock.sparsity_witnessed()`，实测（debug 打印）该锁
   60-bar 手工锯齿数据下 `max_abs_net=0`、`n_bars_with_structural_tick=0`——**从未触发任何
   真实买卖点/结构**，强见证在此数据上恒假。换上去会让这条**每次 `cargo test --lib` 都跑**
   的锁变红，而非揭示真退化。保留原弱断言（仍有非死代码价值：至少确认两条读数管线真的产出了
   实例），强见证移到上面新增的 BTC harness——这正是 MED-5②「这两个 witness 方法已存在且
   零成本，未被调用」背后的真实原因：常跑锁的合成数据结构强度不够，不是疏漏、是数据局限，
   已在锁的注释里如实记录，避免下一个执行者重复踩坑。

3. **降级追认**：#693 裁定①要求「对拍证等价」被 #644 换成「结构不变量复现」——本票核实
   #644 报告 §件4 的论证（两树在 M2/M3 决策门控处已分叉，逐值比较分母不同源）清晰且成立，
   **本票不代为裁决**，追认请求随本报告提交编排者。

**commit**：`6d3662068c`

---

### 6｜冲突清单订正（MED⑥）

**评审位点**：MED-6——#644 报告「接口冲突清单=0 条」的核查方法（`git log --oneline --all
| grep "#<n>"`，只看 commit 标题）不足，实测 #355/#363/#369/#376 四票均触 `fill.rs`，其中
#363/#369/#376 三票另触 `runner.rs`——与「全部集中在 level_order.rs」的中间陈述不符（终点
结论「零一处触及 level_ledger.rs/level_clock.rs/level_attrib.rs」经复核仍成立）。

**处置**：本票独立复核实测，订正依据表述，结论维持不变。

**论证**：`git log --all --grep "#<n>" --name-only --pretty=format: -- 'rust/**'`：

| 票 | 实际触及的 rust 文件 |
|---|---|
| #355 | `backtest/fill.rs`、`strategy/level_order.rs`、`strategy/coverage/{sizing.rs,sizing_tests_2.rs}` |
| #363 | 同上 **＋ `backtest/runner.rs`** |
| #369 | 同上 **＋ `backtest/runner.rs`** |
| #376 | 同上 **＋ `backtest/runner.rs`** |

四票**均**触 `fill.rs`；#363/#369/#376（三票）另触 `runner.rs`——与 MED-6 报告的复核结果
逐字吻合。零一处触及 `level_ledger.rs`/`level_clock.rs`/`level_attrib.rs` 复核同样成立，
「接口冲突清单=0 条」的**终点结论不变**；订正的只是中间过程陈述「全部集中在
level_order.rs」——应改为「均触及 fill.rs/runner.rs，但与本票（level_ledger/level_clock/
level_attrib 三新模块）零交叠」。项 4 的 pan_levels 取数源差异应计入接口清单（清单条数
0→1，性质是「口径选择差异」非「冲突」）。

---

### 7｜LOW 三件

**LOW-1（h12「两侧都不存在」失实）**：核实 kimi `fill.rs:2278` 确有 `plan_and_fill_mtm`，
`runner.rs:6504` 确有测试 `short_leg_end_to_end_trade_marked_short`；只 main 侧因 #499
v1 引擎退役无宿主函数（`grep -n "fn plan_and_fill_mtm"` 零命中）。**放弃移植的结论不受影响**，
仅措辞订正——已在本报告项 1 引用处如实登记，不改代码（原表述的错误在 #644 自己的报告文件里，
本票不重写历史评审文档，订正落在本次留痕）。

**LOW-2（`n_rescaled ⊇ n_residual` 未注明包含关系）**：`level_attrib.rs::attribute_total`
的 `Σbasis=0` 分支同时返回 `(true, true)`（残差桶⟹必也计入缩放），两个计数**不是互斥并列**。
已在函数文档头补登记该包含关系；`bin/theta_overlay.rs` 打印行标签加「(⊇残差桶)」提示，防止
非零残差桶时读者把同一批 bar 数两次相加误读。`cargo check --bin theta_overlay
--features backtest_bin` 单独复核 0 error。

**LOW-3（`forced_flat_anchor` 无条件 O(n) 扫描）**：`fill.rs` 单源化后 `(0..n).rev().find(...)`
在 overlay 与 level_ledger 均为 None 时（净额臂/声部臂）也执行一次全窗反向扫描。核实与
kimi 原版（`fill.rs:1952`）同款无条件扫描，数值零影响，**评估结论：不修**（不为此偏离 kimi
形状，全窗一次 O(n) 而非逐 bar，成本可忽略），已在该行补登记注释备查。

**commit**：`4ad460ee77`

---

### 8｜sizing.rs doc 与恢复件 reconciliation

**评审位点**：#644 的 `51b956fa6a`（「sizing.rs 悬空 doc 引用订正」）把 `sizing.rs:271-280`
的「四层已在场」改写为「三层……本条不再指向它们」，前提是 report.rs/m8.rs 已删；HIGH-1 终审
已恢复两文件，doc 表述需核对现状一致性。

**处置**：doc 已重写（随项 2 一并处理，见项 2 详述），核实现状：四层符号均已在场，但
report.rs/m8.rs 因未恢复 `mod` 声明仍不可编译消费，doc 已准确反映这一中间状态（既非
「三层」也非「四层可直接用」，是「四层字节在场，第四层待补接线才可用」）。另在 m8.rs/
report.rs 文件头各补一行状态说明，供后续读者（尤其 #755 执行方）快速判断可用性。

**commit**：`b7f59d1569`（sizing.rs）、`9ba6196b50`（文件头）

---

## 基线 vs 终态

| | passed | failed | ignored |
|---|---|---|---|
| 终审提交 `5f7a5cb01e`（本票开工前） | 2604 | 1 | 138 |
| 本票收尾 | 2605 | 0 | 139 |

唯一红（`no_stray_env_literals_outside_registry`）是 HIGH-1 终审恢复 m8.rs/report.rs 携带的
#746 env_registry 收口前遗留裸字面量（`M8_FEE_DATUM`/`M8_LEVEL_CAP`/`M8_SYMBOL`/
`M8_REPORT_PATH`）触发，非本票引入——本票补齐 registry 四键并改引用清零（`7341f976e1`）。
+1 ignored = 新增 BTC 对拍 harness（项 5）。

## commit 清单（ticket-766 分支，main 分叉后）

1. `7341f976e1` — item1（tests.rs 重审）+ env_registry 补 4 键清红
2. `b7f59d1569` — item3（level_clock/level_attrib 门控）+ item2/item8 的 sizing.rs doc 部分
   （未拆分提交，见下方说明）
3. `6d3662068c` — item4（pan_levels 登记）+ item5（对拍载体/锁核实/降级追认）
4. `4ad460ee77` — item7（LOW 三件）
5. `9ba6196b50` — item8（m8.rs/report.rs 文件头状态说明）

**commit 拆分说明**：item2 的 sizing.rs doc 更新在实施顺序上早于 item3 的代码改动，但两者
在同一次 `git commit` 前完成，被一并收进 `b7f59d1569`（提交信息只声明了 item3，item2 的
sizing.rs 改动客观上也在该 diff 里）——非刻意合并，是执行时commit时机把握有误；不影响
可审计性（`git show b7f59d1569 -- rust/src/theta_v0/strategy/coverage/sizing.rs` 可单独核对
该文件的改动）。

## 停手项（需用户/编排者决策，未擅自处理）

1. **#755 跨票登记未推送 GitHub**：item2 要求「跨票登记 #755」——本票只在本地报告与 commit
   message 内登记，未执行 `gh issue comment` 推送到 GitHub #755（该操作对外可见、不可静默
   撤回，且本分支未 push，先本地留痕更保守）。需用户确认是否代为张贴。
2. **#693 裁定①降级追认**：项 5 第 3 点——「与 kimi 读数对拍」降级为「结构不变量复现」的
   技术合理性已核实，但正式追认权在编排者，本票不越权代裁。
3. **BTC 21360-bar 对拍数字未能在本工位重新核验**：新增的 `#[ignore]` harness 已固化命令与
    强断言，但本工位无 BTC 数据缓存，§件4 原始数字（`obs=14386` 等）仍停留在「一次性手跑」
    状态，只是现在有了可复跑的机器化载体——需要有数据的工位跑一次 `--ignored --nocapture`
    补全，并核对是否与 #644 报告原始数字一致（若代码逻辑无变化，理应一致；本票在 fill.rs
    做的门控改动不影响 level_ledger=Some 的 overlay 臂，见项 3 论证）。
4. **`cargo check --all-targets --features backtest_bin` 的 issue550_event_battery.rs
   E0308**：`git stash` 复核确认本票改动前后均存在，与本票无关（不在 8 件范围内），未处置。
