# #82 实装卡：T2 锚定「某级别」分治 + 二买双口径（#80/#81 裁定合并实装）

- 日期：2026-07-21
- 裁定依据：`chanlun/escalate/anchor-level-split-ruling-20260721.md`（#80：T3 守 ℓ−1 / T2 放开某级别）；`chanlun/escalate/type2-dual-caliber-ruling-20260721.md`（#81：构成论+形态论都装）。
- 教义锚（全部亲读命中）：017:60/017:66/017:72、020:60、021:44、053:28、057:90、054:172、027:60。
- 前置：#78 落地后开工（同 crate 串行）；实装文件 `rust/src/theta_v0/backtest/econ_positive.rs`（descend_type1_anchor_depth :712-740 及调用方）。
- 纪律：禁 git mutation；禁改 Cargo.toml；TDD 先行；090（禁简化/补丁，声明=能力）。

## 1. 现状

`descend_type1_anchor_depth`（econ_positive.rs:712-740）：Type2/3 入场锚定到「次级别（ℓ−1）一类背驰点」，级别关系硬钉 ℓ−1，口径只有构成论。

## 2. 改造一：级别分治（#80）

- **T3 路径**：ℓ−1 硬锚**逐 bit 不动**（020:60/053:66 刚性）。回归锁：T3 相关既有测试全过、生产 T3 决策不变。
- **T2 路径**：候选枚举放开为「某级别」一类点：
  - 候选域：父走势区间内、级别 **ℓ' ≤ ℓ−1** 的一类背驰确认点（更深级别允许）；**同步共振**情形（多个级别在同一 source_index 同时出一类点）合法，按 017:72 明文。
  - 枚举序（确定性，禁随机）：先 ℓ−1，再 ℓ−2…逐级向下；同级多点取 source_index 最近者（向左，禁未来函数——只用锚定 bar 因果前缀内的点）。
  - 注释必须引 017:66/017:72 行锚（裁定书 §边界）。
- **行为变化照实登记**：T2 生产决策会变（这是裁定目的，非 bit-exact 对象）。验收以「T1/T3 路径逐字节不变 + T2 变更计数与逐案归因落账」为口径，不冒充全路径 bit-exact。

## 3. 改造二：形态论口径（#81）

新对象（建议 `Type2MorphVerdict`）：对 T2 候选点判定形态论分支——

- **形态骨架**：次级别上、次级别下（买点；卖点反向）。段结构取自塔既有次级别 move 序列，不新造分段机器（禁第二查法）。
- **分支 A 不创新高/新低**：纯价格比较（买点 = 次级别下的低点 > 前低）。**禁给它强加背驰前置**（否则偷换形态论为构成论，#81 裁定 §边界明文）。
- **分支 B 盘整背驰**：复用 divergence.rs 盘背机器（同口径原语，禁第二套力度引擎）。
- **小转大情形**（053:28）：该级别无一买时，形态论确认的二买即最佳点——不挂「一买后回调」框架前提，单独路径。
- **双口径登记规则**：任一口径确认即构成二买；两口径不一致时**双登记**（谱系学保留，输出字段记 morph_verdict/construct_verdict 两列），禁静默择一。

## 4. 测试（先行）

1. T3 ℓ−1 回归逐字节不变（既有测试锁）。
2. T2 更深级别锚定案例（ℓ−2 一类点被收）；共振案例（两级同 source_index 一类点）；非一类点排除（无背驰确认者不收）。
3. 形态论分支 A：无背驰场景下不创新低 → 确认，且断言**未调用**背驰机器（计数器/构造证明）。
4. 分支 B：盘背场景确认。
5. 双口径冲突案例 → 双登记两列全在、无静默择一。
6. 小转大案例（该级别无一买，形态论确认）。
7. 因果性：锚定 bar 之后的点不得入候选（前视负测试）。

## 5. 验收线

- `cargo test --release --lib` 全绿零变红（基线 = #78 落地后读数，当前 1799）。
- T1/T3 路径逐字节不变；T2 变更计数 + 逐案归因入交付报告。
- 冒烟（截断窗）：T2 锚定命中构成 vs 形态论分支分布读数。

## 6. 交付报告

改动文件+行号、候选枚举规则定稿、测试清单、cargo test 尾行、T2 行为变更归因、未决项（照实）。
