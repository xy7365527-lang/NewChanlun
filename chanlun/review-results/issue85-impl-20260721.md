# Issue #85 实施报告（B-M5：opsem_dump.rs 迁移）— 2026-07-21

## 状态：完成 ✅

## 接手时的现场
接手时 #85 处于 **expand 已做、contract 未做** 的中间态：
- `rust/src/theta_v0/backtest/opsem_dump.rs`（590 行）已存在，头注标注「#85 自 runner.rs 纯移动」，
  含全部目标符号，且 `mod.rs:82` 已声明 `mod opsem_dump;`。
- 但 `runner.rs` 中旧副本原封未动（10175 行），且无任何 `opsem_dump::` 引用——
  新模块当时是**死代码副本**（两份同名符号分属不同模块路径，可并存编译）。

## 本次执行（contract 步骤）
1. **基线**：`cargo test --release --lib` → 1817 passed / 1 failed / 132 ignored。
   唯一失败为 `econ_positive::tests::type2_dual_caliber_conflict_double_registered`
   （#82 禁触文件，既有失败，与本工作无关）。
2. **删除 runner.rs 旧副本**（10175 → 9593 行，净删 582 行；opsem_dump.rs 590 行，吻合）：
   - 区域 A（原 117–247 行）：`StrictNestCertificateRecord`、`StrictNestSidecarSummary`、
     `StrictNestSidecarCollector`（struct + impl）、`strict_nest_side_i8`、
     `summarize_strict_nest_certificates`、`strict_nest_sidecar_enabled`。
   - 区域 B+C（原 3579–4029 行）：opsem-dump 谱系注释块、`struct OpsemDump` + impl +
     `Drop`、字符串辅助函数 `opt_u8_str`/`lex_top3_json`/`opt_f64_str`/`opt_str_quoted`/
     `opt_pair_str`/`exit_type_str`/`t_stage_str`/`voice_side_str`/`vertical_str`/
     `operation_role_str`/`risk_mode_str`/`eta_bucket_str`/`force_state_str`。
3. **补导入**（runner.rs use 块）：
   - `pub use super::opsem_dump::{StrictNestCertificateRecord, StrictNestSidecarSummary};`
     ——保持 `runner::StrictNestSidecarSummary` 公共路径不变（`RunResult.strict_nest_sidecar`
     字段类型路径向后兼容；grep 确认当前无外部文件引用）。
   - 私有 `use`：`eta_bucket_str, force_state_str, operation_role_str, risk_mode_str,
     strict_nest_sidecar_enabled, summarize_strict_nest_certificates, t_stage_str,
     voice_side_str, OpsemDump, StrictNestSidecarCollector, OPSEM_DUMP_DIR_OVERRIDE`。
4. **唯一的非纯移动改动**：`opsem_dump.rs:34` `StrictNestSidecarCollector.enabled`
   字段 `enabled: bool` → `pub(super) enabled: bool`（runner.rs:467 跨模块读该字段；
   仅可见性放宽，零语义）。

## 验证
- `cargo test --release --lib` → **1818 passed; 0 failed; 132 ignored**（1.01s）。
  含 `opsem_dump_env_gated_bit_exact` 与 strict-nest sidecar 相关测试全过。
- 基线中失败的 `type2_dual_caliber_conflict_double_registered` 本轮通过——判为
  间歇性/环境相关（econ_positive.rs 本次未触碰），供 #82 负责方参考。
- 零生产语义改动：纯删除 + 导入重指向 + 一处 `pub(super)` 可见性放宽。

## 约束遵守
- 仅写 /tmp/kimi-nest-mainline；无 git 变更；未触 Cargo.toml、econ_positive.rs、
  NestChainGate 区、ExitNestGateCtx 区（删除区域均在其外；行号整体前移属删除的
  机械效应，两区内容逐字未动）。
