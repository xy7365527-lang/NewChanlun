# Issue #86 实施报告（B-M4：ledger.rs 类型层）— 2026-07-21

## 状态：完成 ✅

## 范围（设计文档 §迁移节拍 2）
新建 `rust/src/theta_v0/backtest/ledger.rs`（277 行），自 runner.rs **纯移动** 5 个符号：
- `TypedTrade`（pub struct，原 runner.rs:3276，含 G4 谱系 doc 块）
- `TYPED_TRADE_SCHEMA_VERSION: u32 = 4`（pub const，原 :3350）
- `LedgerOpen`（pub(super)，原 :3353，含 entry_stop 族A 冻结止损）
- `OpsemEntrySnapshot`（pub(super)，原 :3386，dump 快照，供 M5/opsem_dump）
- `track_position_transition`（原 :4293 私有 fn）

`typed_ledger_from_bars` **不在此拍**（设计文档 :213 未列入类型层步；留在 runner.rs，
归后续 fill/装配拍）。

## 执行
1. 基线：1818 passed / 0 failed / 132 ignored（#85 交付后状态）。
2. 切块：区域 A = runner.rs 3259–3452（TypedTrade doc→OpsemEntrySnapshot 闭括号，
   连续 194 行）；区域 B = 4274–4347（track_position_transition doc+fn，74 行）。
   脚本化提取+删除，边界断言校验。runner.rs 9599 → 9331 行。
3. `backtest/mod.rs`：`mod opsem_dump;` 前插入 `mod ledger;`（私有模块，同 opsem_dump 先例）。
4. runner.rs 门面（use 块，紧随 opsem_dump 的 pub use 之后）：
   - `pub use super::ledger::{TypedTrade, TYPED_TRADE_SCHEMA_VERSION};`
     ——外部路径 `runner::TypedTrade`（l3_delta_r_alpha.rs:168/191、selector、
     mu_estimator、wverify_run 等）逐字不变。
   - `pub(super) use super::ledger::{track_position_transition, LedgerOpen, OpsemEntrySnapshot};`
     ——opsem_dump.rs:7 `use super::runner::{LedgerOpen, TypedTrade}` 路径经再导出继续命中。
5. **唯一非纯移动改动**：`track_position_transition` 私有 → `pub(super)`
   （跨模块可见性放宽，零语义；同 #85 先例）。所有 struct 字段原本即 pub/pub(super)，
   未动任何字段可见性。

## 验证
- `cargo check --release --lib`：干净（仅既有 dead_code 警告，无新增、无 unused import）。
- `cargo test --release --lib` → **1818 passed; 0 failed; 132 ignored**（1.01s）。
  含 `opsem_dump_env_gated_bit_exact`、track_position_transition 单测（runner.rs tests
  模块内，经 use 命中）、l3_delta_r_alpha/wverify_run 消费方测试全过。
- 零消费方文件改动（12 个引用文件一个未碰，含禁触的 econ_positive.rs）。

## 约束遵守
仅写 /tmp/kimi-nest-mainline；无 git 变更；未触 Cargo.toml、econ_positive.rs、
NestChainGate 区（~1137–1468 逐字未动）、ExitNestGateCtx 区。删除区（3259+/4274+）
均在两禁区之外；禁区行号未受影响（删除区在其后）。
