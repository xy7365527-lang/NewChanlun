# Issue #87 实施报告（B-M3a：fill 执行原语迁移）— 2026-07-21

## 状态：完成 ✅

## 范围（设计文档 §迁移节拍 3）
新建 `rust/src/theta_v0/backtest/fill.rs`（304 行），自 runner.rs **纯移动** 5 个符号
（单一连续区 4101–4396，296 行）：
- `simulate_fills`（原私有 fn :4128，Θ_exec 基础形态）
- `apply_order`（pub(crate)，原 :4202）
- `apply_fill`（pub(crate)，原 :4247）
- `FillOutcome` + `impl`（pub(crate)，原 :4363）
- `bar_returns`（原私有 fn :4388）

fill loop 本体 / `plan_and_fill_mtm(_dual)` / `FillOutput` **不在此拍**（设计文档 :219
归 contract 后续拍）。

## 执行
1. 基线：1818 passed / 0 failed / 132 ignored（#86 交付后状态）。
2. 切块删除：runner.rs 9331 → 9036 行；fill.rs 头部导入
   `super::super::config::ThetaConfig` + `super::super::types::{Bar, Order, StrictAction}`。
3. `backtest/mod.rs`：`mod ledger;` 前插入 `mod fill;`（私有模块）。
4. runner.rs 门面：
   - `pub(crate) use super::fill::{apply_order, FillOutcome};`
     ——`dual_ledger.rs:472` D8 嵌入恒等对拍的 `runner::apply_order` 路径逐字保持，
     可见性不扩大（设计文档 :142/:214 要求）。
   - `use super::fill::{bar_returns, simulate_fills};`（runner 内部调用点继续命中）。
5. 可见性放宽（跨模块必需，零语义）：`simulate_fills`、`bar_returns` 私有 → `pub(super)`。

## 偏差说明（1 处，报请知悉）
设计文档 :142 列 `apply_fill` 也保持 `runner::` 门面；但 contract 后 runner.rs 对
`apply_fill` **只剩注释引用、零代码调用点**（唯一调用方 `apply_order` 已同迁 fill.rs 内
直调），`pub(crate) use` 死门面触发新警告 `unused import: apply_fill`（违反零新警告纪律）。
处置：从 runner 门面移除 `apply_fill`（fill.rs 内保持 `pub(crate)` 原样）。全 crate grep
确认 `runner::apply_fill` 路径零消费方；若后续拍需要可一行恢复。

## 验证
- `cargo test --release --lib` → **1818 passed; 0 failed; 132 ignored**（0.99s）。
  含 `dual_bitexact_embedding_vs_apply_fill`（dual_ledger.rs:440，D8 逐字节对拍）、
  `simulate_fills_open_close_with_fees_l1`、`opsem_dump_env_gated_bit_exact` 等回归锁全过。
- 无新增警告（`unused import` 已在交付前消除）。
- 消费方零文件改动（dual_ledger.rs 未碰）。

## 约束遵守
仅写 /tmp/kimi-nest-mainline；无 git 变更；未触 Cargo.toml、econ_positive.rs、
NestChainGate 区、ExitNestGateCtx 区（删除区 4101+ 在两禁区之外，禁区内容逐字未动）。
