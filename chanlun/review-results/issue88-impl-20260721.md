# Issue #88 交付报告（B-M1：signal.rs 迁移）- 2026-07-21

## 状态：完成 ✅（1818 passed / 0 failed / 132 ignored，cargo test --release --lib 全绿）

## 迁移内容（纯移动，零语义变更）

自 `rust/src/theta_v0/backtest/runner.rs` 移出至新建 `rust/src/theta_v0/backtest/signal.rs`：

| 符号 | 原位置（本步动手前行号） | 可见性变化 |
|------|------|------|
| `bsp_bits_disc` | runner.rs:784–794（含 doc） | `fn` → `fn`（signal.rs 内私有，仅 `newly_confirmed_step` 消费，不变） |
| `newly_confirmed_step` | runner.rs:796–836 | `fn` → `pub(crate) fn`（runner 经 `use super::signal::…` 消费） |
| `entry_structural_stop` | runner.rs:3105–3137 | `fn` → `pub(crate) fn`（同上） |
| `candidate_stop_dist` | runner.rs:3138–3150 | `fn` → `pub(crate) fn`（A6 预注册保留符号，见下） |
| `ClassifyAt<'a>` 类型别名 | （新增，设计 §M1 签名级 interface） | `pub(crate) type` + `#[allow(dead_code)]` 预留 |

函数体逐字搬移（sed 区间抽取，非手抄）；`super::super::*` 相对路径在同级模块下原样有效，未改一处路径。

## 接线

- `mod.rs`：新增 `mod signal;`（私有模块 + 门面说明 doc）。原函数在 runner 内全私有、无外部路径 ⟹ 无需 `pub use` 门面（设计文档 §196 的门面在符号公有化时才需要；l3_pi_probe/bin 侧 ×9 处引用**全部是文档注释**，无真实调用，l3_fullwindow.rs:745 自证"该函数私有"）。
- `runner.rs:60`：`use super::signal::{entry_structural_stop, newly_confirmed_step};`。唯一真实调用点 runner.rs:2143（`newly_confirmed_step`）与 :2518（`entry_structural_stop`）不动。

## 设计偏差登记（090：如实登记，非顺手改）

1. **`ClassifyAt` 未接线**：设计 §M1 列了该别名，但现 runner 各 fill loop 以泛型 `F: FnMut(usize) -> (…)` 持有闭包（runner.rs:1770 bound 与别名逐字同形）。dyn 别名替换泛型 = 语义相邻改动（vtable 间接层），违反"迁移步=纯移动"纪律 ⟹ 本步只落别名定义 + `#[allow(dead_code)]`，泛型改写单独立步（设计 §348 明文要求）。
2. **`candidate_stop_dist` dead_code 告警保留**：`git show HEAD` 复核确认迁移前该符号即零调用（HEAD runner.rs:2003 定义，全仓唯一出现；工作树亦然，仅 :1581 文档引用）——A6（prereg-rev2-20260704）预注册符号，既有告警面原样保留，不加 `#[allow]` 顺手清理。
3. **rustdoc 链接**：搬移的 doc 里 [`k_theta_risk_gate`]/[`LedgerOpen::entry_stop`] 等 intra-doc link 在 signal.rs 作用域不可达（rustdoc warning 级，不影响编译/测试）。留给文档整理步。

## 验证

- `cargo test --release --lib`：**1818 passed，0 failed**，132 ignored（与 #87 交付时基线同数）。bit-exact 回归锁（D8 逐字节对拍、opsem-dump 快照、strict_nest sidecar 等）全部包含在 lib 套件内，全过。
- 新告警面差量：仅修复了本步自己引入的 unused_import（`candidate_stop_dist` 不再 import 进 runner）；无其它新增告警。
- 禁区确认：未触 rust/Cargo.toml、econ_positive.rs、NestChainGate 区域（~1137–1468）、ExitNestGateCtx（~1540–1700）——本步两块切割区间（784–838 / 3105–3150）与禁区无交集。

## 下一步

链上 #89 即 NestChainGate 区域 ⟹ 按 HARD RULES 停手，冲突登记见工位说明（与 #93 抢占同区域）。
