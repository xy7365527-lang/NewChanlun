# #951 实施报告：theta_v0 PyO3 出口（建新）+ recursive_t 执刀（杀旧）

- 工作 HEAD：`main` 顶点 `7aea975026`（`chore: roster 并行派 #951 执刀（impl-951）`），分支 `impl/951-theta-pyo3-bridge`，全部改动在 worktree `/tmp/wt-951`。
- 提交：分支 `impl/951-theta-pyo3-bridge` 顶点（`git rev-parse impl/951-theta-pyo3-bridge`；冻结声明里的 `git checkout impl/951-theta-pyo3-bridge` 即指本提交）。
- 标注约定：【实测】= 本会话亲手跑出；【推断】= 由已读事实推出未验证；【未修】= 修不动/查不到，照实。
- 真编译用 `cargo build --lib` / `cargo test --no-run --features backtest_bin`（未用 `cargo check` 当验证）。
- ⚠️ #947（`p938_bound_center_openness` E0433）在本 HEAD **已修**：`rust/Cargo.toml` 已给两个 p938 探针 bin 声明 `required-features = ["backtest_bin"]`（commit `2f62fc11a4`，票面所称「会挡住全量真编译」的阻塞已不存在）——本票无需动它，全量 `cargo test --no-run --features backtest_bin` 零 error 通过【实测】。

## 0. seam（/tdd 先定）

> **`ThetaPiStream.push_bar(bar, p_t, nav) -> f64`**（返回目标净敞口 `p_star`）——
> 与 `RecTStream.push_bar(o,h,l,c)` 同范畴同量纲；订单由 Python 侧算 delta，成交回执不回填 Rust，
> `p_t` 由调用方每 bar 显式传入（设计稿 §1.3 状态裁定）。
> PyO3 出口面（§2.1）：`ThetaStream.push_bar(o, h, l, c, p_t, nav) -> f64`。

每个测试打的就是这条 seam，不 mock 内部协作者、不测内部实现；期望值用工作例/字面量（确定性逐位、`qty==round(|p_star−p_t|)`、对账恒等残差），不源自实现同源推导。

---

## 一、建新：theta_v0 的 PyO3 出口（D1）

### 1.1 决策内核 `ThetaPiStream`（新增 `rust/src/theta_v0/stream.rs`，无条件编译）

- `ThetaPiStream` 定义 `stream.rs:44`；`push_bar` 在 `stream.rs:103`。
- 逐 bar 调用的决策内核与批量 fill loop **同源**：同一份
  `coverage::pi_theta_step_traced_with_risk_seeds`（`stream.rs:128`）、同一份
  `OverlayState::step`（`stream.rs:151`）与 `LevelLedgerMirror::step`（`stream.rs:152`）、
  同一 `LEE-Net` 见证 `observe_lee_net`（`stream.rs:153`）。
- 分类器走自持缓冲区 `OwnedIncrementalClassifier`（`classifier/streaming.rs`，无条件编译）；
  新增 `forest_epoch()`/`tower_generation()` 两个只读访问器（`classifier/streaming.rs:81/88`，
  与借用变体 `IncrementalClassifier` 同源直读内部 `tower_cache`），供 `TreeCache` O(1) 命中判据。
- 状态裁定：无状态持仓口径——`p_t` 每 bar 传入，不维护影子仓位；无 TW 源（`tw=None`）⟹ P2/P3/P4 跳过
  （`stream.rs:141`，即 `pi_theta_step_prebuilt` 的「旧调用方 bit-exact 原路径」口径）；风控门 `open()`
  （`stream.rs:117`，无保证金/无 typed ledger 的退化）。
- 模块头明写「共核在哪、不共核在哪」（`stream.rs:1-43`）：共核 = π 决策函数 + per-leg 簿步进；
  不共核 = fill loop 的震荡 campaign/PanDiv/nest-gate/voice-exec/TW 推进/typed 簿等批量专有机械。

### 1.2 PyO3 出口（新增 `rust/src/theta_v0/ffi.rs`，无条件编译）

- Python 类名 `ThetaStream`（Rust `PyThetaStream`，`ffi.rs:60`）；`#[pyclass(unsendable)]`（内核含 `Rc`）。
- `push_bar(o,h,l,c,p_t,nav) -> f64`（`ffi.rs:94`）；`push_bar_selfstate(o,h,l,c)`（`ffi.rs:103`，研究对拍便利重载，docstring 明写非生产语义）；`target_net_units`（`:110`）；`last_order`（`:117`）；`snapshot`（`:123`）；`sep_legs`（`:132`）；`finish_full`（`:157`）。
- ElementId 投影定案（设计稿 §5.1 未决项）：`ElementId = { level: u32, ordinal: u64 }`（`classifier/recursive_tower.rs:83` 实测）⟹ 投影 `(u32, u64)` 元组。
- 注册：`theta_v0/mod.rs:79/82`（`pub mod stream;` / `pub mod ffi;`）；`lib.rs:2703` `m.add_class::<theta_v0::ffi::PyThetaStream>()?`。

### 1.3 红→绿证据（seam 测试锁）

**Rust 集成测试** `rust/tests/theta_pi_stream.rs`（新增，2 测试）：

- 红：先写测试再实现，`cargo test --test theta_pi_stream` 报 `E0432 could not find stream in theta_v0`（实现前编译失败）。
- 绿：实现后 `cargo test --test theta_pi_stream` → `2 passed; 0 failed`【实测】。
  - `push_bar_is_deterministic_and_finite`：两条独立流同输入 ⟹ p_star 逐位相同（`to_bits` 相等）+ 有限。
  - `last_order_consistent_and_ledger_reconciles`：`last_order.qty == round(|p_star − p_t|)`；`reconcile_residual < 1e-6`；`lee_net_witness.max_abs_residual == 0`。

**Python 测试** `tests/test_theta_pyo3_bridge.py`（新增，5 测试）：

- `pytest -m "not slow" tests/test_theta_pyo3_bridge.py` → `5 passed`【实测】（maturin develop --release 构建扩展后）。
- 断言面（设计稿 §4.2 验收 B1/C1/C2 的测试锁）：① push_bar 返 float；② 确定性（pa==pb）；③ last_order action ∈ 七构造子；④ |p_star| 名义上限内；⑤ finish_full reconcile_residual<1e-6 + lee_net_witness.max_abs_residual==0 + active_voices 9 元组/closed_voices 10 元组形状 + sep_legs 6 元组形状。

**真实数据实测（报告级留档，非 CI 锁）**：BTC 1m 前 5000 bar，`push_bar` 产出 **2663 根非零 p_star**、n_orders(ΔN≠0)=408、`reconcile_residual=2.18e-11`、`lee_net_witness=(5000, 0, 304, True)`（恒等残差 0 且曾见非零净敞口 304）——π 决策路径与 per-leg 账本真实驱动，非空转【实测】。

---

## 二、杀旧：recursive_t 执刀（#808 判死条件逐条核实）

### 2.1 D1（继任者出口暴露目标净敞口 + per-leg 账本）：现 2/2 ✓

| 出口 | 状态 | 证据 |
|---|---|---|
| 目标净敞口 | ✓ | `ThetaStream.push_bar -> p_star`（`ffi.rs:94`）；BTC 实测 2663 根非零【实测】 |
| per-leg 账本 | ✓ | `sep_legs()`（`:132`）+ `finish_full()`（`:157`，含 active_voices/closed_voices/level_nets/lee_net_witness/reconcile_residual）；BTC 实测 reconcile=2.18e-11、witness=True【实测】 |

### 2.2 D2（九个消费者逐个有归宿）：13/13 判完 ✓

| # | 单元 | 类 | 处置 | 落点 |
|---|---|---|---|---|
| 1 | `trading_system/strategy/rec_t_strategy.py` | 回测策略 | **迁** | 删（`git rm`），继任 `strategy/theta_rec_strategy.py`（新） |
| 2 | `trading_system/backtest/rec_backtest.py` | 回测入口 | **迁** | 删（`git rm`），继任 `backtest/theta_backtest_py.py`（新） |
| 3 | `trading_system/backtest_t_fugue.py` | 回测（第 2 链） | **冻结** | `# FROZEN(#951)` 头（绑 TFugueStream） |
| 4 | `analysis/capture_ratio_matrix.py` | 研究库 | **冻结** | `# FROZEN(#951)` 头（绑 finish_full 九字段） |
| 5-10 | `analysis/tc_churn_{diagnostic,discriminant,exhaust,reason,safety_margin,stoploss_probe}.py` | 一次性研究 | **冻结** | 各加 `# FROZEN(#951)` 头 |
| 11 | `analysis/t3_exit_trigger_diag.py` | 一次性研究 | **冻结** | `# FROZEN(#951)` 头 |
| 12 | `analysis/t_vs_v3_comparison.py` | 对照臂 | **留作对照臂**（不迁不删） | 未动（#799 裁定六「不适用」） |
| 13 | 导出 `run_t_fugue` | 已死 | **删** | `recursive_t/ffi.rs` 函数删 + `lib.rs` 注册行删 |

- D2 禁令 grep gate：`grep -rn "RecTStream" trading_system/` → **0 命中**【实测】（旧文件已删，新文件 docstring 避用旧导出名）。
- E2 冻结声明 grep：9 个冻结脚本逐个含 `FROZEN(#951)` → **9/9**【实测】。
- E1 删导出 grep：`grep -n "run_t_fugue" rust/src/lib.rs` → **0 命中**【实测】。
- **删 `run_t_fugue` 不产生孤儿**（#808 附带订正复核）：`TFugueStreamCore` 仍由 `PyTFugueStream`（`recursive_t/ffi.rs:221`）持有，`t_result_to_dict` 仍由 `PyTFugueStream.finish` 消费【实测，编译通过】。`RecTStream`/`TFugueStream`/`run_recursive_t` 三个导出**留在 pymodule 不动**（冻结件复现自身历史结果的剩余价值）。

### 2.3 D3（一组对拍留档，不要求 bit-exact）：✓（报告级留档）

两侧走**同一份 Python rebalance 模拟**（`delta = target − cur`，min_rebalance_units=1e-4，成交即 cur=target），只换引擎、不换策略逻辑（S4 迁移对照面）。BTC 1m 前 50,000 bar【实测】：

| 元组 | recursive_t (RecTStream) | theta_v0 (ThetaStream) |
|---|---|---|
| bar 数 | 50000 | 50000 |
| 下单次数 | 0 | 4023 |
| 终值 | final_nav=100647.50（引擎内部模拟 100k） | account_price_pnl=−176761.17（ΣN·ΔP，无内部资金账本） |
| 多/空/平 bar 占比 | 0/0/50000 (0%/0%/100%) | 7303/12767/29930 (14.6%/25.5%/59.9%) |

⚠️ **净发现（照实）**：`RecTStream` 在所有 mode（structural/and/or）× a0（segment/stroke）组合下，前 300,000 bar 的 `push_bar -> lu−su` **恒为 0**（零非零目标）【实测】——旧引擎按其生产策略原样驱动时不产任何目标敞口。这与 #808「生产 0 个消费者」、#949「live 从未真跑过」互相印证；两侧数量级差异是**事实**，非对拍偏差，也不要求 bit-exact（#808 D3 原话）。

### 2.4 接不了的 4 项（B1-B4）逐条处置 ✓

- **B1**（`push_bar` 返回 lu−su vs Order 的范畴差）：设计稿 §1.4 已订正——`p_star` 与 `lu−su` **同范畴**（`schedule_order` 是现成翻译器）。本票落成 `ThetaStream.push_bar -> p_star`，范畴差在决策核心层不成立。
- **B2**（`finish_full` 九字段，8 个研究脚本绑死）：`capture_ratio_matrix` + 6 个 `tc_churn_*` + `t3_exit_trigger_diag` = 8 个冻结（见 2.2 表）。
- **B3**（mode/a0 两实验自变量，24 缓存不可复现）：`backtest_t_fugue.py`（TFugueStream 链）冻结；theta_v0 无 mode/a0 轴（`ThetaRecStrategy` 已删 mode）。
- **B4**（`t_vs_v3_comparison.py` 对照另一方是 fugue_v3）：留作对照臂（#799 裁定六「不适用」），不迁不删。

### 2.5 没人要的 4 项 ✓

- 删 `run_t_fugue`（唯一「已死」导出）；`RecTStream`/`TFugueStream`/`run_recursive_t` 留（冻结件复现）。#808 附带订正（删导出不孤儿子簇）已复核（见 2.2）。

---

## 三、两轴评审（/code-review）

### 3.1 Standards（docs/agents/* + 编码规范）

| 项 | 结论 | 证据 |
|---|---|---|
| `cargo fmt` | ✓ | `cargo fmt --check` 零 diff【实测】（只格式化本票新文件） |
| 真编译不用 `cargo check` | ✓ | `cargo build --lib` + `cargo test --no-run --features backtest_bin` 均 `Finished` 零 error【实测】 |
| 中文注释 + 模块头 | ✓ | stream.rs/ffi.rs 模块头中文，含认识论等级（L0/L1）与边界声明 |
| 名分五态 | ✓（新生入口） | `ThetaStream` 有测试调用者（theta_pi_stream.rs + test_theta_pyo3_bridge.py）+ Python 消费者（theta_rec_strategy.py）；**无 live 生产调用者**（仓内 live 从未真跑，#949 已实测）——如实登记，不虚标「现役生产」 |
| 删导出先核孤儿 | ✓ | `run_t_fugue` 删前核 `TFugueStreamCore`/`t_result_to_dict` 仍被 `PyTFugueStream` 消费【实测编译】 |
| 反模式三禁 | ✓ | 测试不 mock 内部协作者、期望值用字面量/工作例、tracer bullet 一次（红→绿→下一 slice） |

### 3.2 Spec（符合 #951 两件交付 + #808 判死条件）

- **建新（D1）**：目标净敞口 ✓ + per-leg 账本 ✓（§1，2/2）。
- **杀旧（判死条件）**：D1 ✓ / D2 ✓ / D3 ✓（§2，三条全满足 ⟹ 可动刀；本票动刀范围 = 删 `run_t_fugue` + 迁 2 + 冻结 9，`recursive_t` 变成「无生产消费者、只服务历史复现的冻结件」——与设计稿 §4.3/§5.6 的「冻结不删」推荐一致）。
- **建新与杀旧同票同人**：✓（本票内完成，未分票分人）。

---

## 四、未修项照实（090）

1. **【未修】S1 的批量 fill loop 改造未做**：设计稿 §4.2 S1 的「`pi_theta_fill_loop_overlay` 改为逐 bar 调 `ThetaPiStream`」未落地。理由照实：fill loop 单 bar 段要连带抽出的跨 bar 状态远大于 π 决策核（震荡 campaign / PanDiv / nest-gate / voice-exec / TW 账本推进 / typed+shadow 簿 / level clock），设计稿 §5.2 自陈「状态清单只列出一半、需通读 fill.rs 4369-6000 才能穷举、工作量 ≈ 一整程」。本票的 `ThetaPiStream` 与 fill loop **在决策函数层共核**（同一 `pi_theta_step_traced_with_risk_seeds` + `OverlayState::step` + `LevelLedgerMirror::step`），但**不共核** fill loop 的批量专有机械。⟹ 设计稿 §4.2 的验收 **A1**（重构前后逐位相同测试锁）与 **A2**（`! grep pi_theta_step_traced fill.rs` grep gate）**未做**，如实不冒充。
2. **【未修】TW 谓词（P2/P3/P4）未进流式驱动**：`ThetaPiStream` 走 `tw=None`（旧调用方 bit-exact 原路径），P2 CloseOverlay/P3 RecoverCapital/P4 EnterEarning 不评估。⟹ 流式 `p_star` 与 fill loop 在 TW 谓词触发 bar 上会分叉。代价照实：TW 状态机（`TwState`/`TwStepCtx`/entry_v 映射/eta_correction）跨 bar 演化且与 fill loop 的 cash/units 结算深度耦合，抽进流式驱动需要补 TW 账本推进，留后续。
3. **【未修】nest-gate / 震荡 campaign / PanDiv 未进流式驱动**：同 1，属批量专有机械；流式走 χ≡1（与 `run_theta_v0_pi_overlay` 的 `chi=None` 同款）无候选准入过滤。
4. **【未修】HEDGING 账户穿零**：设计稿 §5.3 明写「不设计」，本票照抄（NETTING 净额语义）。
5. **【未修】拒单无限重试**：设计稿 §5.4 明写「照抄现状不假装补上」，本票照抄（无退避）。
6. **【未修】`bsp_bits_disc` + `newly_confirmed_step` 的 3+25 行小复制**：门控源 `backtest::signal`（`backtest_bin` 门控）不能从无条件 `stream.rs` 引用 ⟹ 逐字复制到 `stream.rs:226/240`，登记为「故意小复制，收敛到共享无条件模块留后续票」（stream.rs 内注释 + 本节）。
7. **【未修】预存在红：`theta_v0::env_registry::tests::no_stray_env_literals_outside_registry`**：全量 `cargo test` 2655 passed / **1 failed** / 151 ignored。该失败指向 `issue841_probe.rs`/`issue837_probe.rs` 的裸 env 名字面量，**与本票无关**（本票 diff 零触这三文件；`git diff main -- <三文件>` 空）。设计稿 §4.0 记的 `theta_v0_classifier_parity` 2 条红（#811）在 default-feature 全量跑里未出现（该测试需要 `--features backtest_bin` + 真实数据，本票未全量重放该 feature 的测试执行面，只做了 `--no-run` 编译验证）。
8. **【未修】`ThetaRecStrategy`/`theta_backtest_py.py` 未跑 NT 全回测**：需 nautilus_trader + 314MB 数据 + 全量引擎，本票未执行；D3 对拍用同一 rebalance 模拟在引擎层完成（§2.3）。语法面：新增/改动 Python 文件全部 `python -m py_compile` 通过【实测】，但未 import 校验 nautilus 依赖（照实）。

---

## 五、检索式与覆盖目录（承重断言）

| 断言 | 检索式 | 覆盖 | 结果 |
|---|---|---|---|
| RecTStream 零命中 trading_system/ | `grep -rn "RecTStream" trading_system/` | trading_system/ 全目录 | 0 命中 |
| run_t_fugue 零命中 lib.rs | `grep -n "run_t_fugue" rust/src/lib.rs` | 单文件 | 0 命中 |
| 9 冻结脚本有 FROZEN(#951) | 逐个 `grep -q "FROZEN(#951)"` | 9 文件 | 9/9 |
| theta_v0 零 PyO3 暴露（票面前置，复现） | `grep -rn "pyclass\|pyfunction\|pymodule" rust/src/theta_v0/`（HEAD 前） | theta_v0/ 全子树 | 本票前 0 命中（#949 已核），本票后 +2（stream/ffi） |

---

## 附：改动文件清单

- 新增：`rust/src/theta_v0/stream.rs`、`rust/src/theta_v0/ffi.rs`、`rust/tests/theta_pi_stream.rs`、`tests/test_theta_pyo3_bridge.py`、`trading_system/strategy/theta_rec_strategy.py`、`trading_system/backtest/theta_backtest_py.py`
- 改：`rust/src/theta_v0/mod.rs`（+stream/+ffi）、`rust/src/theta_v0/classifier/streaming.rs`（+forest_epoch/tower_generation）、`rust/src/lib.rs`（+ThetaStream 注册、−run_t_fugue 注册）、`rust/src/recursive_t/ffi.rs`（−run_t_fugue 函数）、`rust/src/recursive_t/stream.rs`（GUARD-ROLE 订正）
- 删：`trading_system/strategy/rec_t_strategy.py`、`trading_system/backtest/rec_backtest.py`（迁）
- 冻结声明：`trading_system/backtest_t_fugue.py` + `analysis/capture_ratio_matrix.py` + `analysis/tc_churn_{diagnostic,discriminant,exhaust,reason,safety_margin,stoploss_probe}.py` + `analysis/t3_exit_trigger_diag.py`（9 文件各加 `# FROZEN(#951)` 头）
