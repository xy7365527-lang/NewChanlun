# #218 owner 归属修正（路线 B）——wf7 重放读数对账报告

- 日期：2026-07-24 ｜ 票据：issue #218（spec #219 v2；#211 裁定路线 B）
- 验收口径（范围裁定 2026-07-24）：**wf7 单窗必跑**；p3fold/wf8 一律不跑（如实标「未跑」）
- 重放命令（spec ID-7）：`THETA_NEST_CERT_GATE=1 OPSEM_DUMP_DIR=<dir>/opsem
  T5A_CHAIN_DUMP_DIR=<dir>/dump M8_WIN_FILTER=wf7 <release test bin> --ignored --nocapture
  --exact theta_v0::backtest::wverify_run::m8_e2e_all_systems_oos`
- 对照基线：#214 留档（`/tmp/nest214/after`，旧判同口径 + #214 装置行族）；
  校验 = `owner-attribution-fix-verify-20260724.py`（25 项断言退出码 0，
  留档 `owner-attribution-fix-verify-output-20260724.txt`）
- 090 约定：【确证】= 全量计数直接读出；【推断】= 基于计数结构的解释；【未跑】= 范围裁定不跑项

## 1. 验收核心读数（wf7 窗）【确证】

### 1.1 新口径点级相等率（对照 #214 基线）

| 点类 | #218（两族锚判同） | #214 基线（start_index 序号判同） | 方向 |
|---|---|---|---|
| 一类 | 4 / 8（0.5000） | 4 / 8（0.5000） | 零变化 |
| 二类 | **0 / 40（0.0000）** | 9 / 40（0.2250） | **降至 0** |
| 三类 | **23 / 124（0.1855）** | 14 / 124（0.1129） | **上升 +9** |
| 二+三类 | 23 / 164（0.1402） | 23 / 164（0.1402） | 合计巧合持平 |

口径：事件窗内逐窗各计（钉口径①），分母覆盖成功+失败全体 Trend 事件窗内同向点
（172 点次 = 8+40+124，与基线逐值一致）。**二类真锚判同在全窗无一判等**——旧 proxy
的 9 件「判等」全部坐实为级别错配假等值（§4 逐件归因）。

### 1.2 四桶新读数（对照 #214 基线）【确证】

| 窗 | trend | success | owner 锚不等 | 窗口外 | 异向 | 无合法点 | 锚不可解/实不等 | 带碰撞 |
|---|---|---|---|---|---|---|---|---|
| wf7（#218） | 38 | **14** | **10** | 1 | 13 | 0 (0/0) | 0 / 145 | 9 |
| wf7（#214 基线） | 38 | 16 | 8 | 1 | 13 | 0 (0/0) | 0 / 145 | n/a |

平账（校验 B 组断言全过）：trend = success + 四桶合计；Σne(145) = anchor_missing(0) + real_neq(145)。
锚不可解子计数 = 0：全窗二类判同两侧锚供给全命中（点侧 fractal/组锚、事件侧两元锚均无缺失）——
判等率 0/40 是**实不等**不是供给缺失。

### 1.3 产量与按级分解（对照 #214 基线）【确证】

| 指标 | #214 基线 | #218 | Δ |
|---|---|---|---|
| NEST_GATE_INDEX base_events | 878 | 878 | 0（上游红线 ✓） |
| assembled | 88 | 86 | **−2** |
| indexed | 88 | 86 | −2 |
| rungs_0 / rungs_1 / rungs_2p | 83 / 1 / 4 | 82 / 1 / 3 | −1 / 0 / −1 |
| assembled Trend L1/L2 | 14 / 2 | 13 / 1 | −1 / −1 |
| single_level_share | 0.9432 | 0.9535 | — |

- **delta 对照 spec ID-5 预期**：owner 桶预期缩小（≤ 8）⟹ 实测 10（反向 +2）；success 预期上升
  （16 → ≤ 24）⟹ 实测 14（反向 −2）；assembled 预期增量上限 ≈ +8（88 → ≤ 96）⟹ 实测 86（反向 −2）。
  **三项全部与预期方向相反，机制解释在 §4（spec：超出须机制解释）**。map #126 Destination
  「显著上升 ✓ 或如实判定」的裁定输入：**wf7 实测为如实判定的负向分支（净收紧 −2）**。

### 1.4 不变量断言集（校验 F 组全过）【确证】

- 异向桶 13 == 13、窗口外桶 1 == 1、Pan 域 72/768 == 72/768；
- 点级 c_i total 8/40/124 逐项一致（窗口/方向谓词产物，owner 判同不影响）；
- 遍历计数 scan_pts（#218 新字段）：book_total=26899 / in_window=377 / same_side=172
  （#214 基线无对照面——不变性由 c_i total 逐项对照 + 谓词共核承担，如实标注）。

## 2. 上游红线（校验 E 组全过）【确证】

| 红线 | 结果 |
|---|---|
| tower_events.jsonl MD5 | **逐字节一致**（70073e449f10…，与 #214 基线同） |
| NEST_GATE_INDEX base_events | **逐值一致**（878 == 878） |
| t5a_chain_dump 行键集（候选流供给面） | **逐键一致**（1665 == 1665） |
| trades.jsonl MD5 | **逐字节一致**（7735013bf326…） |

- t5a_chain_dump verdict/admit 列（背书下游，允许变面）：**恰好 1 行差异**（§3.2 归因）。
  spec ID-5 将 dump MD5 列入红线的字面口径在此不成立（verdict 列是背书判定的下游，
  判定集合按裁定改变后该行必然变化）——按「上游不变 + 下游按账变」精神落账：
  供给面（行键集）零变化为红线成立，verdict 差异行逐行归因（§3.2）。
- NEST_GATE_STATS/CHAIN/T3 行族（允许变面，逐行归因）：各行恰 ±1 件联动
  （STATS admitted 609→608、nest_pass 33→32、xzd_gate_fail 959→960；CHAIN xzd_fallback
  1093→1094、agree 423→422、old_pass_new_rej 825→826；T3 chain_pass 33→32、
  chain_none 1591→1592）——全部归因 §3.2 唯一差异候选。

## 3. 逐事件双向翻转清单（spec US-02/ID-5 必落账）【确证】

产生方法：塔溯源探针（NEST218_DEBUG 临时装置，产毕即删）逐 Trend 事件对照 before
（旧 start_index 序号判同——一/三类点 `center.start_index == b_center_start` 精确重算，
二类点 c1 经塔溯源 `parent Compose centers.first()` 与生产 mod.rs 实参同一取法）与
after（现行两族锚判同，经测量入口单一来源复评）。聚合交叉验证全对上：
before-success=16（#214 行 16）、after-success=14（#218 行 14）、
翻转合计 8 件 = 双向 5+3（success 16−5+3=14、owner 桶 8+5−3=10、assembled 88−5+3=86，逐项闭合）。

### 3.1 success→owner（5 件，反向分量——全部归面 A+面 B 二类真锚判同收紧）

| turn | level | b_start | 驱动点（旧判等来源） | c1（塔溯源，si == b_start ✓） | 点锚 vs 事件锚 |
|---|---|---|---|---|---|
| 30771 | 1 | 29811 | 二类 pt_src=30390（m1_end=30341） | L1[29811, 30341] | (2009868000000, 30341) ≠ (2231801000000, 34934) |
| 75768 | 1 | 74543 | 二类 pt_src=75014（m1_end=74969） | L1[74543, 74906] | (2830702000000, 74969) ≠ (2968133000000, 76187) |
| 80852 | 1 | 79843 | 二类 pt_src=80327（m1_end=80215） | L1[79843, 80215] | (3053576000000, 80215) ≠ (2894500000000, 89509) |
| 92368 | 2 | 89247 | 二类 pt_src=91627（m1_end=91118） | L2[89247, 91627] | (2817394000000, 91118) ≠ (2831342000000, 99093) |
| 115909 | 1 | 115141 | 二类 pt_src=115573（m1_end=115453） | L1[115141, 115641] | (2821654000000, 115453) ≠ (2685555000000, 120661) |

机制：5 件的窗内均无一/三类旧判等点，before 背书**必然**全部来自二类点旧 proxy
（`c1.start_index == b_center_start`）判等；换真锚后点侧一类点锚 ≠ 事件 seg_c.1 锚
（该走势 ≠ 事件走势）⟹ 诚实判负失背书。c1 与事件 B 的 start_index 相等属跨级别
序号碰撞（同一 si 在 L1/L2/L3 多级各有一个中枢，tower_events 可证）——正是 #44 证伪在案
的「同中枢序号 ≠ 同归属链」形态，旧 proxy 假等值被真锚证伪。**无一反向翻转来自
一/三类带判同收紧**（start-eq ∧ band-neq 的收紧案例零发生）。

### 3.2 owner→success（3 件，正向——全部归面 B 一/三类带判同放宽（带碰撞判等））

| turn | level | b_start | 驱动点（新判等来源，全部碰撞-eq） | 点 band == B 带 | 点 start ≠ b_start |
|---|---|---|---|---|---|
| 59147 | 1 | 49876 | pt_src=50959/51191/51439 | (2826025000000, 2834083000000) | 50239 / 50504 / 50504 ≠ 49876 |
| 237789 | 1 | 226834 | pt_src=228340/228472/228614 | (2907707000000, 2915146000000) | 227605 / 228031 / 228031 ≠ 226834 |
| 237909 | 1 | 226834 | pt_src=228340/228472/228614 | (2907707000000, 2915146000000) | 227605 / 228031 / 228031 ≠ 226834 |

机制：3 件 before 态窗内一/三类点序号判同全负（start ≠ b_start）且二类旧 proxy 全负
（before=false 塔溯源精确）；换带判同后点 center (zd,zg) == B (zd,zg)（同带不同中枢）
⟹ 判等得背书。**带碰撞观察读数 band_eq_start_neq=9 点次全部落在这 3 事件**（3+3+3），
即 US-08「同 (zd,zg) 但非同中枢」可疑量级 = 9 点次且 100% 改变了事件结果——带判同
放宽是实体效应（若后续收紧判同元组加 dd/gg，这 3 件将回落，收紧与否的裁定输入在此）。

### 3.3 下游传播（chain_dump / trades）

- t5a_chain_dump：**恰好 1 行差异**——候选 `(34998, 0, 34934, Long)`：verdict pass→no_chain、
  admit True→False、channel nest_pass→xzd_gate_fail（其 L0 链级 closed→missing_causal）。
  归因：flip #1（turn=30771，其 seg_c.1 锚 = (2231801000000, 34934) 正对该候选脚锚）失证书，
  链查退化为因果剔除。其余 7 件翻转未触及任何候选 verdict（其证书增减不被候选消费）。
- trades.jsonl：**逐字节零变化**（唯一差异候选经 xzd 回退亦被拒，未达成交面）。
- NEST_GATE_STATS/CHAIN/T3 行族：各行 ±1 件联动，全部归因该唯一差异候选（见 §2）。

## 4. delta 机制总结（spec ID-5「超出须机制解释」）

- **二类（面 A+面 B）**：真锚判同 0/40。wf7 窗内 40 个二类点次全部属于**更早的子走势**
  （其 m1 终点落在事件窗内、但 ≠ 事件走势终点 seg_c.1）；事件自身走势的二类点
  （m1 终点 == seg_c.1）在全窗无一在场（诊断行实测：窗右端 t* 多越过 seg_c.1 留出
  回拉空间，但相应二类点未被产出/不在窗内）。旧 proxy 9/40「判等」全部 = 跨级别
  start_index 碰撞假等值（§3.1 五件驱动 + 四件已被吸收于已成功事件）。净效果：
  5 件事件失背书（success→owner）。
- **一/三类（面 B 带判同）**：一类 4/8 零变化；三类 14→23（+9 点次，全部碰撞-eq）。
  带判同与序号判同非包含关系的实测样本：放宽 3 件事件得背书（owner→success），
  收紧 0 件（无一/三类点因带不等失判等）。
- **净值**：success −2（16→14）、owner 桶 +2（8→10）、assembled/indexed −2（88→86）、
  rungs_0 −1 / rungs_2p −1。与 spec ID-5 预期方向相反，归因闭合（5 收紧 − 3 放宽 = −2）。

## 5. GOLDEN / digest 翻转登记（spec ID-6 受控流程 + ID-6.5 切分）

| 面 | 旧值 | 新值 | 归因 |
|---|---|---|---|
| `extract_signals_bit_exact_digest_guard` GOLDEN 实际值 | `0x90c7_9ee6_17e1_1392`（GOLDEN 常量；**#110 工作线既线失败在案**：本票动手前实际值已 ≠ GOLDEN，属 #110 线未提交改动，勿修勿归因） | `0xe6a2_63e3_43e4_3845` | **既线失败在案 + 实际值二次漂移归面 A**：`BspPoint.center` 字段类型 `Option<Center>` → `Option<OwnerRef>` 的 Debug 包装形态变化（`Some(Center{…})` → `Some(Center(Center{…}))`）；点内容逐值不变由 `extract_signals_bit_exact_vs_orig_per_case` 逐 case 对拍锁定（该对拍**全绿**）；二类点不在该电池输入域，一/三类载体内容逐值不变。诚实重算实际值并登记，不沿用旧值放行，不自定义 Debug 掩盖 |
| NEST_GATE_FAIL 行 schema | owner_start_neq / owner_pts_id_missing | owner_anchor_neq / owner_pts_anchor_missing + band_eq_start_neq + scan_pts 三计数 | 面 B 装置口径（#214 新增行族、非冻结红线面，schema 演进随票登记，ID-4） |
| tower_events / base_events / trades / 候选流键集 | #214 基线 | 逐字节/逐值/逐键一致 | 零变化（诚实落账） |
| NEST_GATE_STATS/CHAIN/T3 行族、chain_dump verdict 列 | #214 基线 | §2/§3.3 逐项 ±1 | 面 B 下游（归因 flip #1） |

## 6. 未验证项（090 如实）

- **p3fold / wf8 未跑**（范围裁定 2026-07-24，不跑不挡验收）——三窗读数缺后两窗。
- 遍历计数 scan_pts 无 #214 对照面（#218 新字段，不变性由 c_i total + 谓词共核承担）。
- `Exact` 臂探针零值仅单测覆盖（生产不走 Exact，#214 同）；中枢参照 B 查无防御臂
  生产不可达（合成测试覆盖）。
- 研究 bin ×8 二类判同锚不可解降级（未接事件锚账本，诚实判负；一/三类带判同全功能；
  各 bin 头部注释在案）。
- flip #2（turn=92368，level 2）的 c1 精确识别（L2[89247, 91627]，si == b_start ✓）与其余
  四件同法（tower_events + 塔溯源探针双证）；flip 清单产生装置（NEST218_DEBUG 临时探针）
  已删，方法与本报告读数留档可复算（重放 + 探针注释形状见 impl §6）。

## 7. US-08 观察读数汇总

- **锚不可解子计数 = 0**（wf7）：二类点侧 fractal/组锚供给与事件侧两元锚全命中——
  点侧锚供给缺失量级为零（锚解析供给线无缺口）。
- **带碰撞观察 = 9 点次**（band_eq_start_neq）：全部落在 3 件 owner→success 事件
  （§3.2）——「同 (zd,zg) 但非同中枢」非零且 100% 改变事件结果：带判同收紧
  （判同元组升级 (zd, zg, dd, gg)）将使这 3 件回落 owner 桶，收紧与否的实测依据在案。
