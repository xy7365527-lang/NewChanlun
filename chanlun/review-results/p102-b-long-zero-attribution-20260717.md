# #102 归因：「B 口径 Long 侧 nest 证书 0 张」

日期：2026-07-17 ｜ 数据：btc_1m_full.json（4,613,599 bar）｜ 状态：**定稿**
探针：`rust/src/bin/p102_b_long_zero_probe.rs`（新写，只读；自动发现 bin，未动 Cargo.toml）
探针输出：`/tmp/p102_full.txt`（全量）／`/tmp/p102_smoke.txt`（250k 冒烟）
复核输入：`/tmp/p92_ckpt_dump.txt`（只读，下称 dump，行号引其）

## 0. 结论（三分裁定）

**(a) 数据事实**。在已裁定的 B 口径（背驰段 C、不含回试段）下，本数据集不存在合格的 Long 侧
多级区间套链；装配层 `is_sub@interval_b` 门对 Long 的判负全部成立，且是大位移判负
（最小 1,418 bar），非边界擦线。排除 (b) 口径必然与 (c) 实现 bug：门与区间构造两侧对称
（代码锚点见 §3），候选池与区间宽度两侧均衡（§4），Short 侧在同一门下产出 1 张合规多级
证书（§5 对照）。「Long 侧高级别买卖点几乎不形成」是**市场结构真相**（按现行裁定定义），
不是实现缺陷。

## 1. 题名澄清：「0 张」的出处与实测

- 出处：`p92-caliber-d3-probe-20260716.md:54`「**B 口径全量 0 张 Long 侧证书**」（#105 修复前
  A=46/B=45 口径）。该句与同源材料自相矛盾：#100 旧报告在同一 91 张证书集上实测
  **B Long=11**（`nest-cert-bsp-recon-20260717.md:19-20`）；#105 口径实测 **B Long=12/25**
  （`p100-cert-bsp-recon-20260717.md:64-66`）。`p101-ruling-gap-analysis-20260717.md:61`
  已指出题名与两个口径实测均不符。
- **可复现的真实现象**：B 口径 Long 侧 **exec=1 且 top≥2（多级）证书 = 0 张**（A 口径有 5 张）。
  该现象跨 #105 稳定：修复前 #100 在 91 张证书集上实测 B Long=11 且全部为 top=1
  （`nest-cert-bsp-recon-20260717.md:19-20`，即多级同样为 0）；修复后 B Long=12（11 top=1
  ＋ 1 exec=3 trend），多级仍为 0。注意链身份在两快照间有迁移：#105 只改 Trend C 终段
  （commit `1a377f68a6` 仅触 `level_view.rs` +4/−1），pan 成员的 `interval_b` 与之无关，
  但修复后 5 张 A 独有链为 **3 张全 pan ＋ 2 张 mixed**（链 1588321 的父级 1574631、
  top=3 链（基例 706241）的 L3 父级 689893 均为 Trend 事件，其 C 段是修复后正向
  find 口径）——跨快照稳定的是「B Long 多级 = 0」这一现象本身，本报告的逐边几何
  证据全部基于修复后（现行）事件集。
- 台账题名建议改写为：「B 口径 Long 侧**多级** nest 证书 0 张」。

## 2. 滤除点定位：不在 CKPT→CERT 下游，在装配层 `is_sub@B` 门

- **CKPT→CERT 无 Long 丢失**：dump 中 CKPT 层 B Long = 138 行 = 130 行 exec=1 top=1
  ＋ 8 行 exec=3 top=3（跨 18 个检查点重复计数同一在场集合）；终 CKPT（as_of=4500000）
  与终 CERT（as_of=4613598）的 B Long 身份集合**逐字相同**（各 12 张，diff=0）。
  B Long 的 exec=1 top≥2 在**全部 18 个检查点均为 0**——现象贯穿整条重放，非末端伪象。
  （对照：B Short CKPT = 86 top=1 ＋ 18 top=2 ＋ 31 exec=2 top=2 = 135。）
- **唯一口径相关门**：`assemble_typed_certificate` 的基例门（`divergence_confirmed` ∧
  `terminal.confirm_side`，`rust/src/theta_v0/classifier/nest.rs:476-481`）与口径无关——
  5 张 A 独有链的 4 个基例（turn=706241/1588321/1834972/3745754）全部持有 B top=1 证书
  （dump:727/729/731/735）。口径只经 `typed_interval`（`nest.rs:448-458`）进入相邻级闭包含
  门 `is_sub(child, parent)`（`nest.rs:555-556`；闭口径定义 `nest.rs:65-67`）。
  ⟹ Long 多级链**唯一**被滤位置 = 该门在 `interval_b` 下判负。
- **判负是候选集事实，非 DFS 顺序假象**：探针对 5 张链的 5 条去重相邻边（top=3 链与
  同基例 top=2 链共享 706241 的 L2→L1 边；按证书逐条输出共 6 行）枚举上一级
  **全量同侧候选池**（L2 Long=255、L3 Long=63 个事件），逐候选判定双口径包含：

  | 基例 | 边 | 父级 kind | 池中 A 父级 | 池中 B 父级 |
  |---|---|---|---:|---:|
  | 706241 | L2→L1 | Consolidation | 3 | **0** |
  | 1588321 | L2→L1 | Trend | 4 | **0** |
  | 1834972 | L2→L1 | Consolidation | 2 | **0** |
  | 3745754 | L2→L1 | Consolidation | 3 | **0** |
  | 706241（top=3 链） | L3→L2 | Trend | 4 | **0** |

  （`/tmp/p102_full.txt` 的 `P102_EDGE_SUM` 各行。）

## 3. 机制证据：子级背驰段整体落在父级背驰段之外

`left_gap = child_b.start − parent_b.start`（≥0 合规）；`right_gap = parent_b.end − child_b.end`
（≥0 合规）。选中父级（即 A 链实际吸收的父级）几何（`P102_CAND chosen=true` 各行）：

| 基例 | 边 | child_b | parent_b | left_gap | right_gap | 形态 | parent_a（A 口径） |
|---|---|---|---|---:|---:|---|---|
| 706241 | L2→L1 | (706202,706241) | (703483,704358) | +2719 | **−1883** | 子在父右 | (699448,733208)，宽 33,760 |
| 1588321 | L2→L1 | (1588290,1588321) | (1574420,1574631) | +13870 | **−13690** | 子在父右 | (1571823,1592399)，宽 20,576 |
| 1834972 | L2→L1 | (1834875,1834972) | (1832089,1832757) | +2786 | **−2215** | 子在父右 | (1823829,1841447)，宽 17,618 |
| 3745754 | L2→L1 | (3745683,3745754) | (3750141,3750585) | **−4458** | +4831 | 子在父左 | (3739398,3752114)，宽 12,716 |
| 706241 | L3→L2 | (703483,704358) | (688090,689893) | +15393 | **−14465** | 子在父右 | (670003,779653)，宽 109,650 |

- 4 条边「子在父右」：子级背驰发生在父级 C 段**结束之后**——父级背驰后价格继续同向
  破极值（背了又背），子级事件落在父级的回试/反应段。**回试段依裁定②明确排除**
  （`chanlun/escalate/nest-migration-ruling-20260716.md:14`：B = 构成背驰的那段走势类型，
  027:22,44-46 逐字，**不含回试段**）。
- 1 条边（3745754）「子在父左」：子级背驰整体位于父级 C 段**开始之前**（父级 A 段/中枢区），
  同样不在背驰段内。
- A 口径能吸收，是因为 `interval_a = structural_pair_span` =（leave.start → retest.end）
  （`rust/src/theta_v0/classifier/level_view.rs:513-526`），宽度为对应 `interval_b` 的
  **26–98 倍**（上表末列），把回试段与前 C 区域一并吞入。这正是 #98 已定的「父级挑选漂移」
  机制（`p92-caliber-d3-probe-20260716.md:22`），本次在 Long 侧 5 张链上逐边坐实。
- 区间构造两侧对称（排除 (b)(c) 的代码面）：`interval_b = seg_c` 两类型同款
  （Trend `level_view.rs:586`、Pan `level_view.rs:645`；Pan 结构定位 `signal.rs:622-628`
  `seg_c=(lambda_c, seg.end_index)`）；Extreme 门镜像（Trend `level_view.rs:567-570`、
  Pan `signal.rs:649-652`：Long `c.0<a.0` / Short `c.1>a.1`）；Trend C 终段正向 find
  （`level_view.rs:694-700`）方向对称；`is_sub`（`nest.rs:65-67`）方向无关。

## 4. 对称性实证（排除「口径对 Long 结构性不利」）

探针全量统计（`P102_POOL` / `P102_NESTABLE`）：

- 候选池无 Long 饥渴：L1 Long=1433（确认 463）/ Short=1541（501）；L2 Long=255（96）/
  Short=313（113）；L3 Long=63（24）/ Short=65（29）；L4 Long=9 / Short=4。
- `interval_b` 宽度 p50：L1 150/158、L2 580/561、L3 2420/2772（Long/Short）——同量级，
  无宽度偏置。
- 合格基例的可嵌率（存在同侧上一级父级的比例）：L2→L1 **Long：11 基例 → A 父级 4 /
  B 父级 0**；**Short：10 基例 → A 父级 7 / B 父级 1**。注意即使在宽松的 A 口径下
  Long 可嵌率（4/11）已低于 Short（7/10）——不对称先于 B 门存在于市场几何，B 门只是把
  不合教义的伪嵌剔除。

## 5. Short 对照：同一扇门下合规链存在

B 口径唯一 exec=1 多级证书（dump:806）：`2:147126:143038-147126 | 1:147126:147050-147126`，
子 (147050,147126) ⊆ 父 (143038,147126)，父子**同顶收尾**——教科书式区间套（次级别背驰
精确定位父级转折点）。探针复核其父级池：`parents_sub_B=1`（`P102_CONTROL_SHORT`）。
门本身能产出多级证书；Long 侧只是没有同等几何的实例。

## 6. 修法建议

- **无码可修、不应修**：判负全部符合裁定②；任何为增产 Long 多级证书而放宽
  `is_sub@interval_b` 的做法都会把回试段/前 C 区域重新吞入，重蹈 A 口径伪嵌，且直接
  违背已签裁定（`nest-migration-ruling-20260716.md:14,52`）。A 口径维持既有「仅并行诊断」
  定位（`nest.rs:379-386`），不得入生产。
- 若未来需要改变 Long 侧多级产量，合法路径只有两条：(i) 积累更长历史（样本问题）；
  (ii) 经新裁定修改背驰段定义本身（那是新裁定，不是修复）。按 R7 纪律
  （`nest-migration-ruling-20260716.md:55`），0 张在「provider 完整 + 定义忠实 +
  snapshot 无前视」三下成立（`p92-postfix-regression-20260717.md:8-10` 全 PASS），
  可签为**正确市场答案**。

## 7. 对 #101 接入裁定的影响（Long 侧语义）

- DRAFT 开放条款「B Long 仅 11 张、方向不对称权重禁令」（
  `cert-bsp-binding-ruling-DRAFT-20260717.md:35-36`）的前提是「口径缺陷未排除」。
  本归因把前提替换为：**无口径缺陷**——Long 侧 12 张证书张张合规，缺的 5 张是 A 口径
  伪嵌，B 拒绝它们正是教义所求。方向偏置风险从「缺陷固化」降级为「小样本统计涨落」。
- 对 `p101-ruling-gap-analysis-20260717.md:109-110` 两个待填项的回答：
  (i) 「0 张」出自 `p92-caliber-d3-probe-20260716.md:54` 的过宽表述，实测两口径均非 0
  （11/45、12/25）；可复现现象为 exec=1 top≥2 Long = 0（§1）。
  (ii) 方向不对称**非结构性**（§3-§5）——候选 C（加权模式）对 Long/Short 同参无阻碍，
  其「#102 归因闭环」前置条件就方向问题而言已满足；小样本（Long 12 张）自身的统计
  谨慎与「先影子后加权」的推荐序列不受影响。
- 新框架语境（证书 = 级别买卖点存在的构成条件）：本数据集在裁定口径下未形成任何
  Long 侧高级别区间套确认买点；5 条候选链属背了又背/前 C 区域，系统拒发证书与
  「消除滞后信号」的主线目标一致——若由 A 口径放行，反而会把回试段伪确认写进买卖点
  存在性。

## 8. 复现性

- 探针从原始 bar 独立重建终态事件集：candidates=3,683，与 P92_YIELD 逐字一致
  （`p92-postfix-regression-20260717.md:16`）；复现证书集与 dump 双向 diff=0
  （A=41、B=25；`P102_CERTLINE` 66/66 全对）。冒烟（25 万 bar）复现 dump 首检查点
  A=7/B=4（dump:8,13）。
- 复跑命令：
  `cd rust && cargo run --release --bin p102_b_long_zero_probe -- analysis/data_cache/btc_1m_full.json`
  （终态分类一遍 + 终态快照装配，约 11 分钟；探针零依赖 nautilus，无需 backtest_bin。）
- 纪律：生产源码零改动；未改 `rust/Cargo.toml`（自动发现 bin，未追加 [[bin]]）；
  未做任何 git mutation；主仓零写入；新增文件仅
  `rust/src/bin/p102_b_long_zero_probe.rs` 与本报告。
