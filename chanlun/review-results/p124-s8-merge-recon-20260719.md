# P124 S=8 收紧语义：归并+对账 交接收口报告（2026-07-19）

## 交接背景
- 原执行方 kimi 会话额度耗尽；其 `merge_and_check.sh` 于 03:35 空跑对账（归并未落线，步骤 6/7 打出 "dump files not ready"）。
- 本会话按用户指令接管；worktree 所有权已按环 3 约定落锁（`/tmp/kimi-nest-mainline/.chanlun/locks/WORKTREE_OWNER`）。
- 归并本体（PID 72171，带 `P124M_DUMP`）于 03:48:52 落线。产物：`/tmp/p124_r2_m.out`、`/tmp/p124_r2_m_dump.txt`（11581 行）。
- 附注（harness 台账环 2 证据）：包装链曾被会话中断 SIGTERM（exit 143），归并本体因脱钩幸存跑完——R1 规则的正面实证。

## 七步结果（对应 /tmp/p124_r2_merge_and_check.sh）
1. shards s0–s7 全齐（`/tmp/p124_r2_s*.out`）。
2. 归并落线 03:48:52（无 wrapper EXIT 标记，原因见上）。
3. 门行全绿：`P124M_PROVIDER complete=true`｜`P124M_SNAPSHOT no_forward=true`｜`P124M_BIT_EXACT` 五项 diff=0（old_path/tower/moves_centers_bsp_pan/lifecycle_cp_ownership/classification_total）｜`P124M_R7 provider_complete=true definition_faithful=true snapshot_no_forward=true B_zero=false`｜`P124M_MISSED missed=0（intake_fallback_events=25）`。
4. 产量对照（vs `/tmp/p92_replay_v3.log`）：candidates=18297、trend=1831、pan=16466、divergence_confirmed=8606、trend_div=836、pan_div=7770 —— **全等**；唯一分歧 `terminal_confirmed` 855 vs 1213（−358，正是收紧维）。
5. dump 行数 11581 vs 81507：**覆盖面差异**——v3 含 65274 CKPT + 36 CKPT_STATS，归并 dump 不输出该类。
6. CERT 主键（judge_at 向量）：5285 vs 7593；集合关系 **归并独有=0 / v3 独有=2308 / 共有=5285 → 严格子集**。FALLBACK 25=25。
7. 逐行 bit-exact 不适用于跨语义版本 dump（`DIFF_LINES=73043` 全部源自覆盖面 + 收紧删减）；有效 bit-exact 以步骤 3 归并内建门为准。

## 判定
- **RECON_PASS（机械收口）**：收紧语义只做减法——零新增 CERT、FALLBACK 逐一吻合、上游产量一字不差、旧路径不变量 bit-exact 全零。
- **移交 C-3（任务 #110）**：(a) v3 独有 2308 条 CERT 的逐类语义归因（每条删除须落到 S=8 收紧条款）；(b) terminal_confirmed −358 与 (a) 的一致性核对；(c) TURN_CLASS 8579→6271 归因。

## 中间产物
`/tmp/p124_r2_vs_v3.diff`（73043 行）｜`/tmp/c_m.txt`（5285）｜`/tmp/c_v3.txt`（7593）
