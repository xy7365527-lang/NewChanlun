# p126 §2.1 前置实装 C-4b 报告（2026-07-19）

**状态：RECON_PASS**（原 BLOCKED / RECON_FAIL；编排者续作收口见 §5，2026-07-19）

代码增量已续作落地，静态差异检查通过；但授权门在 `cargo test --lib` 实际调用 `rustc -vV` 时以 rc=87 拒绝编译。遵照 C-4b「门拒则如实落报告，不得绕门」要求，未重试、未绕过 wrapper，100k release smoke 亦未启动。因此本报告不声称全量单测绿色，也不伪造 smoke 读数。

## 1. 改动清单

1. `rust/src/theta_v0/backtest/wverify_run.rs:935-940`：仅将既有 `q4_margin_model(nav0)` 可见性提为 `pub(crate)`，函数体与 m8_e2e 既有真值源不变。
2. `rust/src/theta_v0/backtest/wverify_run.rs:1073-1076`：仅将既有 `m6_cost_model()` 可见性提为 `pub(crate)`，冻结三常费率函数体不变。
3. `rust/src/theta_v0/backtest/runner.rs:5163-5166`：`m7_l2_witness_treasury_reach_real_btc` 接入 `M7_WITNESS_A10=1`；仅 gate 命中时设置 `margin=Some(q4_margin_model(initial_nav))` 与 `cost_model=Some(m6_cost_model())`。
4. `rust/src/theta_v0/backtest/runner.rs:5267-5301`：`m8_treasury_reach_distribution_real_btc` 接入同一 gate；cost 在循环前注入，依赖各窗 nav 的 margin 在窗内通过同一 `q4_margin_model(nav)` 注入。
5. `rust/src/theta_v0/backtest/runner.rs:5360-5363`：`m7_kappa_sensitivity_grid_real_btc` 接入同一 gate 与同源函数。
6. `rust/src/theta_v0/backtest/runner.rs:5197-5216`：保留既有 `η_T (tw)` 行，additive 增打 `cum_holding_cost` 与 `η_corrected` 两行；`runner.rs:5203-5214` 增加 release 生效的 `assert!`，硬校验 `funding+borrow+liquidation_loss >= 0` 且 `|f64 真值-shadow| < 1`。

未改 `rust/Cargo.toml`；未做 git mutation；未写主仓 `/Users/silencehan/Projects/NewChanlun`；未跑 4.6M bar 全量重放。

## 2. 回归锁代码审读

- 三个测试均从 `ThetaConfig::default()` 起步，新增配置写入严格受 `std::env::var("M7_WITNESS_A10").ok().as_deref() == Some("1")` 控制。env 未设或值非 `1` 时条件体不执行，`margin=None`、`cost_model=None` 保持默认值。
- `let config` 改为 `let mut config` 只提供条件写入能力，不改变值、分支、分类器输入或 fill 调用；helper 仅扩大 crate 内可见性，函数体未复制、未建立第二查法。
- `m8_treasury_reach_distribution_real_btc` 在 gate=false 时不追加 A10 标签，原表头字符串按原字节顺序拼接；窗内 margin 条件体亦不执行。
- release `assert!` 位于 ignored witness 的 fill 完成后，只消费 `r_decomp` 对账值；不会改写生产状态。env 未设时三项持盾成本与 shadow 均为 0，断言成立。旧计算/交易路径保持 bit-exact；witness 文本输出按任务要求仅有 additive 两行。
- `git diff --check -- rust/src/theta_v0/backtest/runner.rs rust/src/theta_v0/backtest/wverify_run.rs`：rc=0。

以上为代码审读证明；编译期/运行期证明因 cargo 授权门拒绝而未取得，故不升级为 RECON_PASS。

## 3. cargo 授权门现场

预检曾返回 `CARGO_GATE_PREFLIGHT=OPEN`；随后实际命令通过 `rust/.cargo/config.toml` 指定的 `rustc-wrapper=/tmp/kimi-nest-mainline/.chanlun/locks/cargo_gate.sh` 进入门时，门观测到 `CARGO_BAN` 并拒绝：

```text
error: process didn't exit successfully: `/tmp/kimi-nest-mainline/.chanlun/locks/cargo_gate.sh rustc -vV` (exit status: 87)
CARGO_GATE: 编译被授权门拒绝 —— p124 重放收口在途，禁止重建 target/。
CARGO_GATE: 详情与退休条件见 /tmp/kimi-nest-mainline/.chanlun/locks/CARGO_BAN
CARGO_GATE: 只读操作（cargo metadata 等不触发 rustc）不受影响。
```

完整现场：`/tmp/c4b_cargo_test_lib.out`。编译未开始，既有测试没有产生 pass/fail 结果。

## 4. 100k smoke 与三行读数

未运行。原因：同一 cargo 授权门已经明确拒绝全量单测；C-4b 禁止绕门或把未跑报告成通过。故没有可原样粘贴的 `η_T (tw)` / `cum_holding_cost` / `η_corrected` 三行，刻意不填伪值。

解阻条件：由 owner 按门规稳定开放授权门后，重新派发并依次执行 `cargo test --lib` 与任务书指定的 100k release smoke；smoke 前须先 `unset KAPPA_BARRIER_NUM KAPPA_BARRIER_DEN`。

C4B_EXIT rc=87

---

## 5. 编排者续作收口（2026-07-19，claude-fable-5 session 438f5dc7）

C-4b 落报后，门按本意提前退休（`.chanlun/locks/CARGO_BAN.retired-20260719`，mv 留痕）。续作发现**二次阻塞根因**：`rust/target/.rustc_info.json` 把门拒探针缓存了下来（`'success': false, exit status: 87`，stderr 即门拒三行）——门退休后 cargo 仍回放缓存拒绝，未进编译。处置：rm 该缓存文件（纯构建元数据，cargo 自动重建）。

### 5.1 cargo test --lib（现场 /tmp/c4c_test.out）

    test result: ok. 1732 passed; 0 failed; 128 ignored; 0 measured; 0 filtered out; finished in 6.24s

④⑤ 单测计数核（p120 §6 五组 26 测）：A6+B4+C4+D8 逐名全绿 + E 组 3 绿 1 **合规 ignore**（E2 `e2e_g5_double_entry_same_number` 按 §6 原文「未接线前标记 ignore 并注释」，注释逐字相符）。

### 5.2 100k release smoke 三行读数（原样粘贴，现场 /tmp/c4c_smoke.out）

    η_T (tw)              : 522773（η_*=1000000, η_T≥η_*=false）
    cum_holding_cost      : 6182（A10 C5 shadow；cost_model=None ⟹ 0 回归锁）
    η_corrected           : 516591（=tw()−cum_holding_cost，判读改用值；η_corrected≥η_*=false）

    test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1859 filtered out; finished in 2.15s
    C4C_SMOKE_EXIT rc=0

算术自洽：522773 − 6182 = 516591 ✓（修正只降不升 ✓）。验收三件（--lib 全绿 / §2 回归锁审读 / smoke 三行）齐备 ⟹ **RECON_PASS**。
