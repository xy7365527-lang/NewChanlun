# 影子评审终局门 · #428 · 对 #425 / commit `639773fe01`

- 评审对象：`rust/src/theta_v0/classifier/nest_lifecycle.rs`（+442 / −34），背驰否证第三类「从未构成」
- 形式：两轴并行、各自独立新上下文、协调工位不下场自评
- 分轴报告：
  - Standards 轴 → `shadow-425-standards-20260727.md`
  - Spec 轴 → `shadow-425-spec-20260727.md`
- **分轴汇总，不跨轴重排**（两轴刻意分离，见 code-review skill「Why two axes」）

---

## 裁决

**HIGH = 0（两轴皆无）⟹ 不回票，#425 维持 CLOSED。**

- Standards 轴：PASS-with-findings —— MED×3 / LOW×4
- Spec 轴：PASS-with-findings —— MED×1 / LOW×2；九条验收 8 满足 + 1 部分满足

## 终局门必查项落实情况

| 必查项 | 结果 |
|---|---|
| 断言强度（构造性恒真） | **真注入五次反例实测**（M1–M5），无一条注入后仍绿 ⟹ **不存在恒真断言** |
| release 构建下是否真执行 | 新增断言是真 `assert!`（非 `debug_assert!`），显式调用时 release 照跑；但 `advance` 内唯一**自动**调用点带 `#[cfg(debug_assertions)]`（既有形态）⟹ release 无自动核验。两轴独立发现，措辞偏强，登记 |
| 教义声明锚名副其实 | 061:26 / 061:28 与原文逐字核对**一字不差**；模块头已诚实登记「本模块判据是力度谓词从未可证，非逐字未创新高」的工程口径差 ⟹ 名副其实 |
| 测试名与实际断言 | T16/T17/T18/T19 四条全部名实相符 |
| 生产路径零改动 | 已提交树零调用点（模块外引用仅 `classifier/mod.rs` 的 `pub mod` 与 untracked 探针 bin） |

## 变异验证实证（Spec 轴执行）

| 变异 | 内容 | 结果 |
|---|---|---|
| M1 | 第 7 步新分支条件改 `false` | T16/T17/T19 红 |
| M2 | 新分支改挂 `ForceOvertake` | T16/T17/T19/**T5** 红 —— T5（真实 provider 夹具）连带红，独立复现「新码在真实事件流上可达」 |
| M3 | T17 测试侧 A/B 期望原因码对调 | 仅 T17 红 ⟹ 分辨断言真 discriminating |
| M4 | 删桥匹配分支倒退守卫 | 全库仅 T18 红、T15 仍绿 ⟹ 既有覆盖缺口经实证成立 |
| M5 | 删第 6 步 `Unavailable` 臂的 `continue` | 仅 T19 红、T14 仍绿 |

> 过程注记：M5 首次经 Edit 注入后全绿，`diff` 复核发现**改动根本未落盘**（本仓已知 harness 现象）。凡「变异后仍绿」必须先验证变异是否真落盘。

工作树还原已验证：`git diff HEAD -- nest_lifecycle.rs` 为空，全程零 git mutation。

---

## Standards 轴发现（原文见分轴报告，此处仅列处置）

| 级别 | 发现 | 处置 |
|---|---|---|
| MED | M1 公共 API 文档被本次改动作废未同步（`StructureCompleted` 仍写「滞留 Provisional 也留此痕」，该组合已不可达；`Invalidated` 仍只列两成因） | 并入 #426 顺带订正 |
| MED | M2 新增不变量 release 下不自动执行，模块头「逐条钉死」措辞偏强 | 并入 #426（接线时在 release 冒烟路径显式调一次，或降措辞） |
| MED | M3 `coding-style.md` 两条硬上限被破且加深：文件 **2212 行**（生产段 978 行仍 >800）、`advance` **194 行** >50。且**票体写的 2153 行与实测不符** | **派 follow-up 独立票**（同族先例 #359 coverage.rs 拆分） |
| LOW | L1 Duplicated Code 搬了家：T16/T19/T17 三处手搭同形状力度序列 | 登记，随 #426 顺带 |
| LOW | L2 非对称：Invalidated 抽了 `invalidate`，Confirmed 仍内联 | 登记不改 |
| LOW | L3 教义绑定停在契约层：创新高预滤只在 provider 侧，`advance` 不校验输入 | 并入 #426（须保证喂入来自产窗侧） |
| LOW | L4 `assert!(invalidated <= last_as_of)` 近乎构造性为真，信息量低 | 登记不改 |

对实装工位自评四项的独立复核：①090 声明超出能力已改 —— 同意；②Duplicated Code 已改 —— **部分同意**（生产侧已消，测试夹具复现，不算清零）；③文件超长登记不改 —— **不同意口径**（实测 2212 非 2153，非测试段亦超限，登记 ≠ 豁免）；④`advance` 登记不改 —— **部分不同意**（第 7 步可低风险抽出）。

## Spec 轴发现（原文见分轴报告，此处仅列处置）

| 级别 | 发现 | 处置 |
|---|---|---|
| MED-1 | **AC2 字面覆盖的「不可验」子集被以教义理由排除**。票体原文「结构完成时，**从未写入首次可证时点的活假设**转入终态并挂新原因码」**无例外条款**；实测 `ForceCheck::Unavailable` 时第 6 步 `continue`、第 7 步不到达 ⟹ 该身份继续以活假设挂账，且因非终态桥迁移照旧延展 ⟹ **本票要消灭的「永久挂账」在该子集原样保留** | **并入 #426，且必须升为显式验收项**（不能只作观测面） |
| LOW-1 | `assert_invariants` release 下不自动执行，commit message「invariant 钉死」措辞偏强 | 同 Standards M2，并入 #426 |
| LOW-2 | 主仓 `CONTEXT.md` 词汇表仍写「从未构成 —— 始终未创新高」，无工程口径注记 ⟹ 只读词汇表者会误读为价格创新高判据 | 并入 #426 一并订正 |

范围蔓延核查：`NestLifecycleEntry::invalidate` 抽取与 T19 均属票外新增，但行为等价 / 只加保护、局限本文件 ⟹ **无实质越权**。

### 对实装工位「不改行为」判断的独立复核（本票点名）

**方向对、覆盖面不对。**「不从缺失数据造否证」确与 #78 / 090 一致，**不主张改判负**；但该例外落在 AC2 字面内，是**实装侧自划、票体未授权**，且只登记为 #426 的「观测面」而非验收项。#409 实测 `Unavailable = 0.00%` 只说明当前不发生，**不构成规格豁免**。

⟹ 该判断**站得住一半**：行为可以不改，但「不改」这件事必须在 #426 变成一条被裁定的显式验收项（补第四原因码 / 显式挂起桶 / 明确接受永久滞留，三选一并留档），而不是留在观测面里自然消失。

---

## 登记状态（重要）

**本工位未对 tracker 做任何写入。** 无 HIGH ⟹ 未 reopen #425、未创建修复票；对 #426 的验收增补建议**尚未落到 issue 上**（权限层拒绝了非 HIGH 情形下的 tracker 写入，判定超出本票授权）。

⟹ 上表中标「并入 #426」「派 follow-up 独立票」的各项，**目前只存在于本报告**，须由主控决定是否落到 tracker。待办清单：

1. **#426 增补验收项**（Spec MED-1）：「完成信号已到但力度不可验」的终局归属须被显式裁定并留档（补第四原因码 / 显式挂起桶 / 明确接受永久滞留，三选一）。
2. **#426 顺带项**：Standards M1（公共 API 文档作废未同步）、Standards M2 + Spec LOW-1（release 无自动核验，措辞降级或加冒烟调用）、Standards L3（接线须保证喂入来自产窗侧）、Spec LOW-2（`CONTEXT.md` 词汇表加工程口径注记）。
3. **新 follow-up 票**（Standards M3）：`nest_lifecycle.rs` 拆分债 —— 2212 行（生产段 978 行）、`advance` 194 行，同族先例 #359。

## 一行小结

Standards 轴：7 项发现，轴内最重为 M3（成文硬上限被破且加深，票体行数自述与实测不符）。
Spec 轴：3 项发现，轴内最重为 MED-1（AC2 的不可验子集被实装侧自划例外排除）。
**两轴均无 HIGH ⟹ 不回票。**
