# #494 影子评审：#69 5a/5b 线形化实装

- 审计时点：2026-07-27T21:50:14-0400
- 工位／分支：`/tmp/wt-69`（实际顶层 `/private/tmp/wt-69`）／`ticket-69`
- 固定范围：`e1e3aa7037df7124592a93463d2f3309d78fa759..e8d5a47f06d4daad8c82ee4ee71659097df211bd`
- 两个提交：`7c4df3e118`（5a）与 `e8d5a47f06`（5b）
- 审计性质：新上下文、禁自评；既有 Standards/Spec 报告只作为待复裁对象，不作为本报告证据替身。

## 结论

**FAIL**

| 轴 | 结论 | 最坏边界 |
|---|---|---|
| 独立运行证据 | FAIL | `cargo test` 未全绿；4,613,599 bars 的 post 尚未完成，最终 `cmp` 未能判定 |
| Standards | FAIL | 两项 HIGH：新增原地可变状态违反仓内 CRITICAL 不可变规则；两个被改文件远超 800 行硬界 |
| Spec | FAIL | “零新增红”成立，但票面／两卡要求的是“全绿”；全量合并封印未闭；5a 三态实测计数未交付 |
| 提交纪律 | PASS | 两提交的 p123 bin 均可编译；改动面、文件模式与提交信息主题均在票内 |

本结论不把尚在运行的 post 推测为成功或失败。即使后续全量 `cmp=0`，当前 Standards 两项 HIGH、全绿 HIGH 与三态计数 LOW 仍未解除，不能自动翻为 PASS。

## 一、证据独立复核

### 1. cargo 双档与失败集合

独立执行：

```text
cd /tmp/wt-69/rust
CARGO_TARGET_DIR=/tmp/wt-69-target cargo test --lib 2>&1 | tail -3
```

得到：

```text
test result: FAILED. 1980 passed; 4 failed; 135 ignored; 0 measured; 0 filtered out
```

独立执行 release 同款，得到：

```text
test result: FAILED. 1983 passed; 1 failed; 135 ignored; 0 measured; 0 filtered out
```

用同一测试命令加 `pipefail` 抽取失败名，debug 为：

```text
theta_v0::backtest::runner::tests::lee_m3_attribution_dimension_is_readable_and_not_residual_only
theta_v0::backtest::runner::tests::lee_m4_cap_on_sparsity_has_no_unexplained_violation
theta_v0::backtest::runner::tests::lee_m4_level_cap_narrows_position_when_enabled
theta_v0::classifier::signal::tests::extract_signals_bit_exact_digest_guard
```

release 仅最后一条。前三条与 GitHub #446 的开放票体逐字一致，digest guard 与 GitHub #491 的开放票体逐字一致；未观察到第五个失败名。因此：

- “debug 1980/4、release 1983/1”：**复核通过**。
- “失败集仅为 #446 三条 + #491 一条、零新增红”：**复核通过**。
- “`cargo test` 全绿”：**不通过**。零新增红不等于全绿，#69 票体及两卡没有给 #446/#491 豁免（5a 卡 `:293-307`；5b 卡 `:338-353`）。

### 2. 250k／1M 统一口径对拍

独立执行：

```text
cmp /tmp/wt69-uc-pre-250000-dump.txt /tmp/wt69-uc-post-250000-dump.txt
cmp /tmp/wt69-uc-pre-1000000-dump.txt /tmp/wt69-uc-post-1000000-dump.txt
```

两条均 `exit=0`。SHA-256 也分别逐对相同：

```text
250000:  614b83323271752a0b569d11f3efb4ea2e472524b584f1570c323b03ca62421f
1000000: ffadc4efcaf18541175212b5c22a13f89e588c5c7cd4e28739e8fb66308a7be5
```

stdout 的独立 `diff -u` 结果：

- 250k：只改 `P123_SPARSE_SUMMARY` 的 `prefix_s`，并在 post 增加 `pan_entries/pan_hits/pan_misses/pan_writes/pan_invalidations`。
- 1M：只改 `P123_TERMINAL_PROGRESS elapsed`、`P123_PREFIX_PROGRESS elapsed`、`P123_SPARSE_SUMMARY prefix_s`，并在 post 增加同一组 `pan_*` 字段。
- 其余语义字段逐字一致；1M 的共同在线读数为 `pending=2218/4091 views=8198 triggers=4816 reevals=8198`。

结论：既有 Spec 报告的“整体两提交缺统一 bars 口径”MED 已由 `/tmp/wt69-uc-*` 证据**关闭，不再成立**。

### 3. SHADOW

独立执行 `grep shadow_mismatches /tmp/wt69-5b-shadow-1m.stderr`，得到：

```text
shadow_checks=27037 shadow_mismatches=0 shadow_term_mismatches=0
```

结论：真冷 SHADOW 门**通过**。forced 两个 resident 参数均为 `None` 的源码锚见 `rust/src/bin/p123_fast_replay.rs:1037-1050`。

### 4. 4,613,599 bars 全量合并封印

pre 已完成，`/tmp/wt69-seal-pre.stderr` 的终态摘要为：

```text
P123_SPARSE_SUMMARY triggers=39448 reevals=79290 reuses=68749 syncs_self=46167 syncs_lower=79290 wm_cross_with_lower=58070 wm_cross_without_lower=0 term_rechecks=18301 term_skips=2 shadow_checks=0 shadow_mismatches=0 shadow_term_mismatches=0 prefix_s=16149.898
```

审计时 post 进程仍在运行：

```text
PID 55334  state=R  cpu=99.0%  elapsed=04:59:01
/tmp/wt69-5b-post/p123_fast_replay analysis/data_cache/btc_1m_full.json
```

`/tmp/wt69-seal-post.stderr` 尚无 `P123_SPARSE_SUMMARY`；最后里程碑为 4.0M。pre/post 去掉墙钟后的共同里程碑逐字如下：

| bar | pending | views | triggers | reevals |
|---:|---:|---:|---:|---:|
| 500,000 | 16,424/18,297 | 8,198 | 4,816 | 8,198 |
| 1,000,000 | 14,425/18,297 | 17,528 | 9,291 | 17,528 |
| 1,500,000 | 12,492/18,297 | 26,497 | 13,771 | 26,497 |
| 2,000,000 | 10,668/18,297 | 34,705 | 17,934 | 34,705 |
| 2,500,000 | 8,910/18,297 | 42,519 | 21,991 | 42,519 |
| 3,000,000 | 7,006/18,297 | 50,845 | 26,005 | 50,845 |
| 3,500,000 | 5,084/18,297 | 60,482 | 30,292 | 60,482 |
| 4,000,000 | 3,256/18,297 | 68,687 | 34,401 | 68,687 |

pre 另有 4.5M 里程碑，post 尚未到达。故：

- 已到共同里程碑的 pending/views/triggers/reevals：**通过**。
- post 终态 `term_skips` 是否同为 2：**未能判定**。
- `/tmp/wt69-seal-pre-dump.txt` 与 `/tmp/wt69-seal-post-dump.txt` 最终 `cmp`：**未能判定；待 post 完成后由编排补跑**。

当前 post dump 是部分产物，不对部分文件执行终态 `cmp`，也不由前缀一致推测终态。

## 二、Standards 轴

**结论：FAIL**

标准源为 `.claude/rules/common/coding-style.md:3-20,39-48`，另逐项核 Fowler 12 臭味。发现如下。

### HIGH-1：新增设计直接违反仓内不可变规则

仓内规则逐字要求 “ALWAYS create new objects, NEVER mutate existing ones”，并把该节标为 CRITICAL（`.claude/rules/common/coding-style.md:3-13`）。本 diff 新增：

- `ConfirmCursorStore` 对既有 map 执行 `retain/entry` 原地改写：`rust/src/theta_v0/classifier/level_view.rs:594-620`。
- `scan_confirm_cursor` 逐项改写 cursor 的 envelope、累积量、状态与下标：同文件 `:846-930`。
- `PanMemo` 对 entries、stats、snapshot 原地 `retain/insert/update`：同文件 `:1122-1188`。
- `RunEntry::update(&mut self, ...)` 原地替换字段：`rust/src/bin/p123_fast_replay.rs:417-428`。
- 测试显式锁定 `pan_memo` 地址跨 update 不变：同文件 `:1757-1766`。

独立裁定：既有 Standards 报告该发现**成立**。本交付分级只允许 HIGH/MED/LOW，故记 HIGH；其所引标准自身的措辞级别仍是 CRITICAL。5a/5b 的 resident 规格需要状态，不构成对仓内不可变规则的自动豁免；当前证据中未找到规则例外。

### HIGH-2：文件组织硬界被显著扩大

仓内标准要求 200–400 行常态、800 行最大值（`.claude/rules/common/coding-style.md:15-21,39-45`）：

- `level_view.rs`：基线 1,720 行，现为 3,926 行；本 diff `+2378/-172`，净增 2,206 行。
- `p123_fast_replay.rs`：基线 1,620 行，现为 1,768 行；本 diff `+207/-59`，净增 148 行。

独立裁定：发现**成立，HIGH 分级恰当**。既有报告把 `level_view.rs` 记为 `+2389/-183`，该数值不准确；正确 numstat 为 `+2378/-172`，但不改变结论。既有报告还漏记了同样被继续扩大的 `p123_fast_replay.rs`。

### MED-1：新增／扩大的超长函数

仓内标准要求函数 `<50` 行（`.claude/rules/common/coding-style.md:39-45`）。当前：

- `trend_confirm_state_core` 106 行：`level_view.rs:738-843`。
- `scan_confirm_cursor` 86 行：`level_view.rs:846-931`。
- `provide_nest_candidate_events_ext_resident` 264 行：`level_view.rs:1406-1669`。
- `assemble_level_view_impl` 199 行：`level_view.rs:1833-2031`。
- `run_targeted_prefix_pass` 340 行：`p123_fast_replay.rs:833-1172`。
- `evaluate_run` 56 行：`p123_fast_replay.rs:1207-1262`。

独立裁定：既有报告的超长函数发现**成立，MED 分级恰当**；本报告补齐了同一 diff 中另外三处。

### MED-2：Data Clumps／过长参数列

- `scan_confirm_cursor` 14 参数：`level_view.rs:846-860`。
- `trend_confirm_state_core`、resident ext provider、`evaluate_run` 均为 11 参数：`level_view.rs:738-750,1406-1418`；`p123_fast_replay.rs:1207-1219`。
- `hist/dif/close_src` 与 projection/blocks/legs/view 参数群跨多层包装反复同行。

独立裁定：这是 Fowler Data Clumps 判断项，**成立，MED 分级恰当**；它增加了 source 坐标、MACD 下标和 count 量纲错接的风险，但不是仓内硬规则的 CRITICAL 级违例。

### LOW-1：Primitive Obsession 部分成立，原 MED 应降级

`PanBlockIdentity` 把已有领域枚举压成 `trend: bool`、`direction: i8`、`completed: bool`，并以 `1/-1/0` 表示方向（`level_view.rs:996-1025`）。这符合 Fowler Primitive Obsession 的启发式，但该结构私有且当前只作 Hash/等值身份，未发现错误分支。

独立裁定：既有报告关于 `bool/i8` 的部分**成立**，但 MED 偏高，降为 LOW；其把所有 `usize` 水位、下标与计数一并定性为违规，证据不足，本报告不采纳该扩大部分。

### LOW-2：测试调用样板重复

pan memo 测试区在 `level_view.rs:3379-3911` 重复展开 resident provider 的长参数调用；`PanResidence { ... }` 构造实数为 20 处（`:3390-3903`），不是既有报告所写的 19 处。

独立裁定：Fowler Duplicated Code 发现**成立，LOW 分级恰当**。

Fowler 余项：Divergent Change 已被 HIGH-2 文件组织问题覆盖，不重复计级；legacy 冷入口的 Middle Man 形态由两卡明确要求，用于保留旧签名和真冷 oracle，故不报；Mysterious Name、Feature Envy、Repeated Switches、Shotgun Surgery、Speculative Generality、Message Chains、Refused Bequest 未找到足以独立立项的新增证据。

## 三、Spec 轴

**结论：FAIL**

### 1. 授权面与 R1-R3

| 要求 | 独立裁定 | 证据锚 |
|---|---|---|
| R1：e_src 严格 `<` | PASS | 单一公式 `signal.rs:1134-1141`；复用守卫 `level_view.rs:1141-1155,1546-1551,1635-1643` |
| R1：至少两个后继块 | PASS | `block_index + 2 < blocks.len()`：`level_view.rs:1038-1049` |
| R2：store 在 bin `LevelDerived` 旁 | PASS | `p123_fast_replay.rs:370-384` |
| R2：dirty resident、forced 真冷 | PASS | dirty `:975-1007`；forced `:1037-1050` |
| R3：memo 在 bin `RunEntry` 旁 | PASS | `p123_fast_replay.rs:386-414` |
| R3：dirty `Some`、forced `None` | PASS | dirty `:1003-1006`；forced `:1048-1049` |
| 禁第二套 e_src 公式 | PASS | 生产公式只在 `signal::freeze_boundary_src`；classifier 写入 `mod.rs:2034-2041`，p123 只读 getter `mod.rs:1010-1023`、调用 `p123_fast_replay.rs:937-938` |
| 旧入口签名不动 | PASS | 基线 43 个文本命中 = 1 定义 + 42 调用；现行 `assemble_level_view` 仍为三参数并委托 `None`：`level_view.rs:1813-1830`。票内 p123 调用改走 resident，新测试新增 legacy 冷路调用；其余旧调用文件不在 diff |
| 禁把 resident 状态塞入 TowerCache | PASS | `ConfirmCursorStore` 只在 `LevelDerived`；`PanMemo` 只在 `RunEntry`。TowerCache 仅新增只读 e_src 水位字段/getter：`mod.rs:850-862,1010-1023` |
| 两卡作用面 | PASS | diff 仅 6 个票内 Rust 文件；`first_retrace_replay.rs`/`level_view_store.rs` 各一行只是补新 sidecar 字段的构造点 |

R1-R3 的确认终态另见 `chanlun/escalate/g1-g2-window-ban-ruling-20260721.md:36-44`；#67、#479 均已关闭。未发现越过两卡授权面的语义改动。

### 2. 验收协议逐项

| 门 | 结论 | 分级／说明 |
|---|---|---|
| `cargo test` 全绿 | FAIL | **HIGH**。debug 1980/4、release 1983/1；失败虽全在 #446/#491，但两卡明确要求全绿且无豁免 |
| 250k/1M diff=0 | PASS | 两档 dump `cmp=0`；stdout 只差墙钟与 additive `pan_*`。既有 MED 口径错位已关闭 |
| `P123_SHADOW=1` 0 mismatch | PASS | `checks=27037 mismatches=0 term_mismatches=0` |
| 在线 pending/views 一致 | PASS（已完成范围） | 1M 以及全量共同里程碑 0.5M–4.0M 逐字一致 |
| 全量双跑合并封印 | 未能判定／未完成 | **HIGH 验收阻断**。post 仍运行且无终态 summary；最终 `cmp` 待编排补跑 |
| 5a 三态新实测计数 | FAIL | **LOW**。5a 卡 `:282-289` 明确要求记录 Confirmed/TerminalFalse/Scanning；`SparseStats` 与输出只有 pan 计数，无三态字段：`p123_fast_replay.rs:431-456,515-535` |

### 3. 对既有 Spec FAIL 报告的独立裁定

- “整体两提交 250k/1M 口径错位”MED：**撤销／已由统一口径证据关闭**。
- “全绿硬门未达”HIGH：**维持**。零新增红是回归边界，不是票面全绿的同义词。
- “全量合并封印未完成”HIGH：**维持到 post 完成并 `cmp=0` 为止**。
- “三态计数缺失”LOW：**维持**。`ConfirmState` 存在于 lib（`level_view.rs:490-505`），但 p123 没有统计或输出三态。
- “scene-ledger 行锚漂移”LOW：**维持**。5a commit message 仍写 `scene-ledger:16`，现行封印依据在 `.chanlun/scene-ledger.md:18-20`；这是证据锚漂移，不改变代码授权面。

## 四、提交纪律

**结论：PASS**

### 1. 逐 commit 可编译性

为避免切换当前工作树，分别用临时 `git archive` 快照和独立 target 执行：

```text
7c4df3e118: cargo check --locked --bin p123_fast_replay -> exit 0
e8d5a47f06: cargo check --locked --bin p123_fast_replay -> exit 0
```

临时目录均已清理；当前工作树未切换。最终提交另由本报告的 debug/release `cargo test --lib` 实际执行到测试阶段，编译成立，运行期仅有前述在册失败。

### 2. 改动面

- `7c4df3e118`：仅 `p123_fast_replay.rs`、`first_retrace_replay.rs`、`level_view.rs`、`level_view_store.rs`，内容对应 5a store／sidecar／接线。
- `e8d5a47f06`：仅 `p123_fast_replay.rs`、`level_view.rs`、`mod.rs`、`signal.rs`，内容对应 5b memo／e_src seam／接线。
- 合并范围共 6 个文件，全部模式 `100644 -> 100644`；`git diff --summary` 无 mode/type 变化。
- 范围内无 `Cargo.toml`、`Cargo.lock`、`data_cache`、symlink 或 `target`。
- `git diff --check e1e3aa7037..e8d5a47f06` 为 0。

### 3. message 与内容

- 5a message 的主题、R2 持有位置、冷/热 seam 与实际 diff 一致。
- 5b message 的 “p123 run 语境细粒度 pan memo” 与 `RunEntry.pan_memo`、R3 接线一致。
- 5a message 的 `scene-ledger:16` 是前述 LOW 行锚漂移；不构成改动面或主题不一致。

## 五、翻转边界与下游

总体结论只有在以下条件全部满足后才可能翻转：

1. Standards 的不可变规则冲突与超大文件／函数硬界得到实质解决，或仓内权威标准先形成明确、可引用的例外；当前不得默认为有例外。
2. debug/release `cargo test --lib` 均全绿；仅声明“零新增红”不足。
3. post 正常退出并产出终态 `P123_SPARSE_SUMMARY`；核 `term_skips=2`。
4. 编排者执行最终 `cmp /tmp/wt69-seal-pre-dump.txt /tmp/wt69-seal-post-dump.txt` 且 `exit=0`；若 `term_skips != 2` 或 dump diff 非 0，保持 FAIL 并保存现场。
5. p123 输出 Confirmed/TerminalFalse/Scanning 的新实测整数计数，满足 5a 卡 `:288`。

下游推论：#69 当前不能按两卡“验收完成”口径关闭或进入终局合并；可以如实登记为“靶向语义对拍通过、零新增红、全量 post 在跑，但 Standards 与硬验收门未闭”。

谱系引用：R1-R3 以 `g1-g2-window-ban-ruling-20260721.md:36-44` 的已确认终态为准；全量合并封印以 `.chanlun/scene-ledger.md:18-20` 及两卡验收节为准。

影响声明：本审计未改任何 Rust 源码、`Cargo.toml` 或既有报告；只新增本报告文件。
