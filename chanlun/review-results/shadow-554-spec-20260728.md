# #554 影子评审 · Spec 轴（2026-07-28，新上下文终局门）

- 评审面：`git diff 7b4547b623...HEAD`（7 commits，9 文件）；只读，未改代码
- 规格源：SPEC #547 全文、#550 票体验收标准、票内 R1/底座更正/裁定(i)
- 自跑复现：`cargo test --release --lib` = **2028 passed / 1 failed / 135 ignored**（唯一红 = 既有 `signal::tests::extract_signals_bit_exact_digest_guard`，本轮 `signal.rs` diff=0）；`cand_event` + `candidate_event_stream` 11/11 绿（含 FNV golden `4851063543183334400`）

## 结论：FAIL（残一条 HIGH；前两轮 HIGH 全部实修）

## 前轮 HIGH 抽验（独立复核，均已修到位）

Pan 域真产出并锁在主缝（`mod.rs:4048-4053`）；`confirmed_at` 不再盖首见钟（`cand_event.rs:263-270`）；revision 闸改投影 struct + derive（`:278-309`）；增量候选挂 `bsp_key` memo 且 cascade 两点与 `cached_bsp_key` 锁步失效（`mod.rs:1919/1975`）；`append_bar` 复原、事件走独立入口（`streaming.rs:84-104`）；FNV 锁真实 `classify_with_tower_events` 产出（`mod.rs:4164`）；`observed_at` 取入簿 `as_of`、五钟不塌缩（`cand_event.rs:245-248`）；bin 计数改按 key 最新 revision（`issue550_event_battery.rs:125-134`）；`nest`/`CandDeltaEvent`/typed 链 diff=0；消费方零接线（全仓无外部调用点）。

## 发现

**HIGH-1 因果簿可被整体删除，终态随之复活** — `mod.rs:1166`：`TowerCache::clear()` 把 `candidate_book` 置 default。生产可达点 `mod.rs:1685`（段账本回缩即 clear）与 `:1679`；clear 后同一 key 以 `revision=0`/`observed_at=当前 as_of` 重新出现，旧 revision 与 `Invalidated` 无痕消失。
> SPEC #547:21 US5「每级一条 append-only 事件流（修订只追加、旧 revision 保留），禁删除模拟失效」；#547:22 US6「Confirmed/Invalidated 为终态不复活」。#550 验收「090 照实登记未覆盖项」——本行为未登记。

**MED-2 裁定(i) 的「文档化」未落** — 编排者裁定：fresh 全量通道定义 = 窗口投影（终态每 key 一条）**并文档化**。`classify_with_tower_events` doc（`mod.rs:606-609`）与产出点（`:561-563`）无此语义说明，全仓 grep「窗口投影」零命中，本轮报告亦未述。

**MED-3 Pan 域 `confirmed_at` 恒 = 几何位、且恒早于 `observed_at`** — `cand_event.rs:445-446` 取 `cert.source_index`（破中枢段端点，`signal.rs:624`）；该段须确认后才入账本，故确认钟恒早于系统可观察 bar。与二轮对 Trend 的因果钟修法口径不一，属 #535 红线①同族形态，未登记未裁。

**MED-4 090 登记不完整：`extreme_proof` 恒 = `key.seg_a`** — `cand_event.rs:396`/`:441` 两域均为 `seg_a` 副本，零独立信息；同类零信息字段 `candidate_group_id`/`pair_id` 已诚实注释（`:83-86`），此项与报告 090 节均缺。

**LOW-5 快照 commit 节仍不闭合** — `issue550-t1-impl-20260728.md:139-144` 止于 `39ce41b896`，收尾小修 `7a9faeb2d0` 未登记，而 2028/1/135 三元组正是该态所测（我在 HEAD 逐字复现）。

**LOW-6 增量≡全量逐 bar 锁为增量-增量对拍** — `mod.rs:4058` 的「全历史重建」两侧都走 `classify_with_tower_events_incremental`，非对全量；对全量只有单前缀一发（`:4038`）。此项已由裁定(i) 转 #551，仅登记不判失。
