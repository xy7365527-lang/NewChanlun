# #555 影子评审（#551 终局门，新上下文只读）

评审面 `git diff 20a14b9dd9...HEAD`（13 commits，4 源文件 +1939/−185）。独立实跑
`cargo test --release --lib theta_v0::classifier` = **479 passed / 1 failed / 6 ignored**；唯一红
`signal::tests::extract_signals_bit_exact_digest_guard`，本 diff 对 signal.rs 的改动**零触碰**该
GOLDEN 常量与摘要行（`git diff -- signal.rs` 无相关增删）⟹ 基线红成立。

## 结论：**PASS**（无 HIGH / 无 MED；2 枚 LOW，均不阻断关票）

## 独立复核（抽验，不采信双轴结论）

| 抽验项 | 结果 |
|---|---|
| 状态机转移矩阵 | **符合** SPEC #547:59。`advance_observation`（`cand_event.rs:355`）终态挡回 + `invalidate_unseen`（`:224`）跳终态 ⟹ 不复活；状态由 `resolved_state`（`:99`）单一来源派生，值域 {P,U}，Pan 恒 C |
| ∅→Invalidated 禁止边 | **有生产锁**。`reject_unobservable_invalidation`（`:452`）是 `assert!`（非 debug_assert）且位于唯一入口 `advance`；`on_append` 两处 `unreachable!` 为 test-cfg 冗余锁 |
| 裁定甲 分域断言 | **符合**。`mod.rs:4400` 非终态 `payload_eq` 硬断言 + `live_checked>0` 非真空前提 + 「fresh 有而簿无」反向仍硬断言；终态仅三分解计量。`mod.rs:4485` 同构：非终态禁向保留（真空已声明），`revived_terminal`/`fork_causal_only_terminal` 转计量 |
| 空 L0 当场失效 | **符合**。`mod.rs:1700-1709` `clear()` 后 `advance(&[], as_of)`；`as_of` 与全量路径同式（`:2429`） |
| 保簿 | **符合**。`clear()`（`:1175-1196`）无一行触 `candidate_book` |
| 投影单一来源 | **符合**。`payload_eq`（`:4340`）、幂等跳过、bin（`:245/272`）三处同读 `CandidateProjection` |
| 090 登记 | 10 条登记逐条可核（`rule_version` 生产不可达、truncate-key 结案、5 路探针无证据、`stable_id` 非单射订正）；探针账 11/2/3 与代码断言逐锚点相符 |

## 发现

**L1【LOW】三项 Standards 发现既未修也未登记**（`chanlun/review-results/issue551-t2-impl-20260728.md:470` §十 表 + `:407` §八）。Standards 轴共 9 项，§十 表只列 8 项状态，遗漏 LOW#7（`latest_by_key` 折叠 4 处重复：`mod.rs:4138` + bin `:133/:228/:314`）、LOW#8（`bin:210 print_projection_fork` 恒 `Ok`；`cand_event.rs:419 let _ = prior`）、LOW#9（`mod.rs:4161-4162` 双空行）——三处现场均仍在，报告全文零处提及。违 SPEC #547:96「090：任何未覆盖项照实登记」。修法 = §八 补三行登记（或修掉），非改码量级。

**L2【LOW】`signal.rs:452` 提取后判序对调**：旧 `judge_first_cached` 是 `extreme` 不成立即 `return None`（先于 λ_C 的 `debug_assert!(false)`）；新 `first_structural_gates` 把 `debug_assert` 前置于 `extreme` 计算。不变式「broke ⟹ λ_C 必 Some」成立 ⟹ 生产不可达，release 双跑摘要 `16618955402698307653` 未动；但 debug 断言的暴露面被扩到 `extreme=false` 分支，属函数头「与旧内联版逐位等价」声明未覆盖的一处差异。

## 影响声明

只读评审，未改任何源码/测试；新增文件仅本报告。
