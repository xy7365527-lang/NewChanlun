# #556 影子评审 · #552 收口终局门（2026-07-28）

评审面 `git diff 8096600a35...HEAD`（8 commits，含收口小修批 5 枚）。新上下文、独立复核、只读未改代码。

## 结论：**PASS**（0 HIGH / 0 MED / 3 LOW）

## 独立复核（自跑，非引用报告）

| 抽验项 | 我的验证 | 结果 |
|---|---|---|
| 谓词语义 | `cand_sub.rs:38` = `child.event_level < parent.event_level && interval_is_sub(child.interval, parent.interval)`，`source_index` 域、闭区间、相切算包含（`cand_event.rs:158`） | 与票体逐字符合 |
| 真值表六格 | 区间轴 10 格 + 级别轴 6 格 + 退化/单点 4 格；相切/相离/严格/反向不包含/同区间/跨级分支全覆盖，同级格附 `assert!(interval_is_sub(inner,outer))` 钉死拒因 | 六格齐 |
| 同源 | `cand_sub.rs` 全文无区间不等式：包含/相切/相离/退化四判据均在 `cand_event.rs:153-177` 共用一条退化守卫 | 无第二套 |
| 不复读 nest | `nest.rs:67` 是 `(start_time,end_time)` 时间域；`cand_sub` 零 `nest::` 引用 | 合 US18 |
| 单测 | 自跑 `cargo test --lib cand_sub` = **9 passed / 0 failed**（报告的 7 是小修批前数） | 绿 |
| 探针非真空 | 自建 release 自跑 100k 窗：`contained=55 touching=22 strict=33 disjoint=9244 degenerate=0`、`same_level non_reflexive=28`，与报告 §九**逐位一致** | 非真空，分母 9307≠0 |
| 零生产变更 | `interval_is_sub` 等四判据在 lib 内**无任何生产调用点**（仅自测 + `cand_sub`）；`cand_sub` lib 外唯一引用 = 诊断 bin；502 行纯新增 | 不可能改行为 |
| 小修批五项 | ①`saturating_sub`+违例单测（`:111`/`:372`）②判据上提+`degenerate` 桶 ③`latest_by_level` 返 `Vec<&CandidateEvent>`、全文零 `clone()`（`:152`）④三函数 O(n²) doc + 实测锚 ⑤`reverse_blocked_by_level` 恒等式 doc 收窄（`:84`）+ §八登记 9/10/11 | **五项皆真修**，非文档搪塞 |

## 发现（LOW，均属 090 声明层，不阻断）

**LOW-1 · `reflexive_blocked` 的「恒等于」在退化输入下不成立** — `cand_sub.rs:96`
doc 称自反对数「恒等于同级事件总数」，但 `count_same_level`（`:246`）经 `interval_is_sub` 入桶，退化事件的自反对判 false ⟹ 不计入，实际是**非退化**事件总数。同处 `SameLevelBlock` 无 `degenerate` 桶，与相邻级桶「退化必须单列可见、不得静默丢格」（`:70` doc）的纪律在同一模块内两套。BTC 实测 `degenerate=0` 故未命中——与他们自己为退化桶给出的「类型层可表达即须处理」论证同型。

**LOW-2 · 报告 §四窗口 B 表是小修批前口径** — `issue552-t3-impl-20260728.md:§四`
500k 表无 `degenerate` 列且未标注为改动前读数；只有 100k 重跑并登记逐位一致。等价性论证成立，但表本身应标注口径。

**LOW-3 · 「四桶划分分母」的暗示** — `cand_sub.rs:70`
部分交叠对（如 `(5,15)×(10,20)`）同样计入 `pairs` 而不进任何桶，与退化对被单列的理由同型。单测注释已限定「本夹具下」，故仅 doc 措辞问题。

## 规格逐条

票体四条验收 + US10/US11/US15/US18 + #246 + audit §6 全部落实；④后半「#544 评论核销」按执行指令未做，已登记（§八6），非本轮门禁项。
