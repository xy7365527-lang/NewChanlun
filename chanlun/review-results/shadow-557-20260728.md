# #557 影子评审（新上下文终局门，2026-07-28）

评审面 `git diff e8496fcaf0...HEAD`（4 commits，单 bin 文件 +265/−1 + 3 份文档）。独立复算，未采信双轴结论。

## 结论：**PASS**（0 HIGH，1 MED，2 LOW）

## 一、亲算复核（全部成立）

| 抽验点 | 亲算结果 |
|---|---|
| env 门控真假 | `EventDump::from_env` `p123_fast_replay.rs:426`；关灯侧 `/tmp/wt553_800k_newoff_events.txt`、`wt553_full_run3_events.txt` **均不存在**（`File::create` 未发生）；关灯 stdout 与开灯 `cmp`=0；`cargo test --bin` = **6 passed / 0 failed** |
| 只写不判 | 生产读点恰 3 处：`:1147` 构造 / `:1428` 写 / `:1595` flush；`revisions`/`seq` 读写仅 `:447/:453/:474`，无第二读点；`&NestCandidateEvent` 不可变借用 |
| 四态既有行 | 800k 既有 dump SHA `38039a6d…396e8` 在改动前 binary（`/tmp/wt553_speed800k.txt`，binary `wt553_p123_base` 尚在）/ 关灯 / 开灯 / 开灯二跑**四侧逐位相同** |
| 双跑 SHA 可复算 | 2M 三面 `c26904ba…`/`627a1227…`/`4ecc346b…` 双侧相同、`cmp` 全 0，行数 8106/8076/19 逐位复现；§5.4 全部分布（rev 8073/1/1/1、kind 7281/795、level 6357/1401/278/40、trend 差 3 且 rev>1 三行确为 `kind=trend`）复现 |
| 全量已跑段 | run1≡run2 三面 `cmp`=0（`cee9e686…`/`25e67dc6…`/`11ebc3ec…`）；`head -c 114647 run1 \| cmp - run3` 通过 ⟹ run3 严格字节前缀；末字节 `n`/`=`（半行）与 events 末字节 `0a` 均如报告 |
| 收口批五项 | A1 已改标（全文无「票体条款」肯定句）；A2 已补 §5.1.1；C1 行数订正为 19（实测 19）/200k 17（实测 17）；C2 边界声明已落 `:386-394`；`EVENT_DUMP_ENV` `:372` 为生产侧唯一字面量。`rustfmt --check` 干净，工作树干净，无 TODO |

## 二、发现

**M1 [MED] C2 边界声明枚举不全——漏 `dump_flush()`**（`p123_fast_replay.rs:386-394`；报告登记 12）
新 doc 只列「P421 flush（`:1592`）与 `EventDump::flush`（`:1595`）不可达」。实测调用链：`observe` 的 `?` → `run_targeted_prefix_pass` → `main:744` 的 `?` → **`main:1057` 的 `dump_flush()` 同样不可达**。而 `DUMP` 是 `static OnceLock<…BufWriter…>`（`:196`）——Rust static **不 Drop**，既有 P116 dump 的缓冲尾字节因此**静默丢失**；新路自己的 `BufWriter` 反而是局部变量、drop 时会冲刷。即开灯写失败时受损的是**既有封印文件**，恰是票体首要保证（「既有 dump 行逐字节不动」）所在的面。属既有形状（P421 同款触发），非本票新引入的缺陷类，但 C2 修的正是这条声明，枚举缺一项 = 声明未穷尽（090）。修法：doc 与登记 12 补一句。关灯路径不受影响。

**L1 [LOW] 声明的可达性未被单测覆盖**：两枚新测只验 `observe`/`flush` 各自返回 `Err`，未验「上抛后收尾语句不可达」这条**跨函数**声明（无 harness 可及）。报告已按 090 记明去向，不构成阻断。

**L2 [LOW] `pending` 轴实测零样本**：三窗 `pending` 恒 1，`pending=0` 行只在单测出现。报告 §八 登记 3 已照实声明「结构上可达、实测未见」，无膨胀。

引规格：#553 票体验收 1/2/4/5；#547 第 17 条（全量双跑，已按执行令宽免、报告未声称 PASS，合规）。

## 三、处置（微修批，本文件入库同批）

| 项 | 处置 | 落点 |
|---|---|---|
| M1 | **已修**（doc-only，无生产逻辑改动） | `p123_fast_replay.rs` 边界段补 `dump_flush()`（`main:1057`）不可达 + `static` 不 `Drop` ⟹ 既有 P116 尾字节静默丢失 + 「P421 同款既有形状，非本票新引入」；行号随之 `:386-394` → `:386-400`。验收报告 §八 登记 12 同步补「枚举补全」段，登记 9 行数订正 2,715 → **2,721** |
| L1 | **不修，已登记在案** | 验收报告 §八 登记 12 末段 (i) + 登记 13（去向：跨函数可达性无 harness 可及） |
| L2 | **不修，已登记在案** | 验收报告 §八 登记 12 末段 (ii) + 登记 3（「结构上可达、实测未见」） |

微修批仅动 doc 与报告，`rustfmt --check` 干净；PASS 结论与各项实测数不因本批改变。
