# ADR 0018：限价成交模型——穿越才成交、开盘取优、先平后开、GTC、限价不扣滑点

**日期**：2026-08-06
**裁定人**：编排者，走 [成交模型保真度 #918](https://github.com/xy7365527-lang/NewChanlun/issues/918)（map [#787](https://github.com/xy7365527-lang/NewChanlun/issues/787)）当场拍板
**状态**：已采纳

## 背景

### 本 ADR 补的是档位 (乙) 的内部假设

[#918](https://github.com/xy7365527-lang/NewChanlun/issues/918) 的**档位**已于 2026-08-05 裁定为 **(乙) 加限价挂单语义、仍用 bar 数据**（见该票 resolution comment）。本 ADR 只补 (乙) 内部的五条成交假设——档位本身不在此重裁。

### 为什么执行层非动不可

缠论的动作有两种，现执行层只表达得了一种：

| 动作 | 触发 | 天然执行形态 | 现状 |
|---|---|---|---|
| **信号驱动** | 三类买卖点确认，收盘才知道 | 市价 | ✅ 已支持 |
| **价位驱动** | 中枢上下沿 ZG/ZD，**提前已知** | **限价挂单** | ❌ 表达不了 |

而价位驱动那一条正是 [ADR 0017](0017-chong-quantity-numerator-shortdiff-amplitude.md) / [量层·分子 #914](https://github.com/xy7365527-lang/NewChanlun/issues/914) 刚裁的短差方向规则所依赖的。**本图刚裁出来的短差层，执行层现在表达不了。**

原文锚，`049-第49课.md:53`【正文】（全文无 `^（注：` 行 ⟹ 该行是正文本体）：

> 「还有，在中枢震荡中，本质上是应该**全仓操作**的，也就是在**中枢上方全部抛出筹码，在下方如数接回**，当然，这需要高的技术精度」

同段上一句 `049-第49课.md:51`【正文】给出该动作的**存续边界**：

> 「在出现第三类买点之前，中枢未被破坏，当然有所谓的中枢震荡，**其后，中枢已经完成就无所谓中枢震荡了**」

⟹ 这一句直接是**裁定四**（挂单寿命绑中枢存续）的原文依据，不是工程侧的推断。

### ⚠️ 上游引用订正（本会话逐条复核，5 处全错，结论均不变）

[#918](https://github.com/xy7365527-lang/NewChanlun/issues/918) 票面与本会话口头断言里的援引，逐条打开复核后订正如下。**每一条的结论都是对的，错的是论据**——即 `docs/agents/wayfinder-workflow.md`「三条证据纪律」之三点名的 ★ 类。

| # | 原断言 | 实测 |
|---|---|---|
| 1 | `049-第49课.md:52`【正文】「在中枢上方仓位减少，在中枢下方仓位增加」 | ❌ **`:52` 是空行**，正文在 `:53`；且该句**非逐字**，是转述。逐字原文见上（更强：是「全仓/如数接回」） |
| 2 | 「`fill.rs:63`：成交价 = 该 bar 的 close」（票面「仓内现状」段） | ❌ **引错对象**。该行在 `simulate_fills` 内，而 `runner.rs:24` 明写其「**生产零调用**，现仅测试消费」；全仓 grep 除 `runner_tests.rs:8319/8355` 外无调用点。**生产成交价那一行是 `fill.rs:4604`**（在 `pi_theta_fill_loop_overlay` 内） |
| 3 | `config.rs:284-285` 把 `simulate_fills` 列为「四条生产成交回路」之一 | ❌ 与 `runner.rs:24` **正面矛盾**，该清单已过期。真回路 = `pi_theta_fill_loop_overlay`（`pi_theta_fill_loop` 是其 wrapper）/ `plan_and_fill_mtm` / `plan_and_fill_mtm_dual` |
| 4 | 「现金约束在 `fill.rs:117`」（本会话口头） | ❌ 实为 **`fill.rs:115`** |
| 5 | `fill.rs:23` 注释引「`types.rs:218`」为 `Order.exec_index` 出处 | ❌ 实为 **`types.rs:364`**（仓内既有注释的行号漂移，非本轮引入） |
| 6 | **本 ADR 初版自己写的**「生产回路 = `pi_theta_fill_loop_overlay` / `plan_and_fill_mtm` / `plan_and_fill_mtm_dual`」 | ❌ **后两个在本仓不存在**（`94e0837f0e` #499 已删；`grep -rn "fn plan_and_fill" rust/` 零匹配）。本 ADR **一边点名 `config.rs:284` 清单过期，一边照它写了回路名单** ⟹ **同一类错在识破它的同一份文档里复发**。订正见下节 |

### 已核实的现状（打开确认）

- **生产成交价 = bar close**：`fill.rs:4604` `let px = bar.close as f64 * config.tick.tick_size;`
- **`fill.rs` 全文用到 `high`/`low` 的只有 4 处**（`:4241`–`:4245`），全部是诊断打印（`.map(...).unwrap_or(f64::NAN)`），**无任何成交语义** ⟹ 限价语义确实是从零加，不是改造既有分支。
- **`Bar` 有 `high`/`low`，且是整数 tick 域**：`types.rs:52-55`（`pub high: Tick` / `pub low: Tick`）。**这一条是裁定一的物理基础。**
- **先平后开**：`fill.rs:106` 记 canonical FULL §20「撤单→先平后开」，`apply_order` 已按它实现单笔内翻转。
- **生产恒 Taker**：`venue_fee.rs:41`、`:58-59`（理由：「无限价挂单语义 ⟹ 声称 maker 档是声明膨胀（090）」），常量本体 `venue_fee.rs:112` `pub const PRODUCTION_LIQUIDITY_ROLE: LiquidityRole = LiquidityRole::Taker;`
- **BTC 现货 maker == taker 逐位相等**：`analysis/data_cache/venue_fee_binance_spot_20260726.json`（VIP0 10.0/10.0，VIP0_BNB25 7.5/7.5）；**仓内早有登记**——`venue_fee.rs:271` 已写明「`maker_bps` 与 `taker_bps` 逐位相等」。
- **滑点加法口径**：`venue_fee.rs:38` 标定档成交费率 = datum 费率 + `slippage_bps/1e4`（`FeeQuoter` 的 `uncalibrated_addon`，`venue_fee.rs:326/387`），该文件自己把价差/冲击 datum 登记为**未做的裂缝 3**。

## 名分速览

| 裁定 | 一句话 | 依据 |
|---|---|---|
| 一 | **穿越才成交**：`low < 挂单价` 严格小于，打平不成交 | 工程（队列）＋ tick 离散域 |
| 二 | **开盘取优**：成交价 = `min(open, 挂单价)`（买）/ `max`（卖） | 撮合语义（限价 = 该价或更优） |
| 三 | **同 bar 三级排序键**：先平后开 → 同类内价格优先 → 并列保出现顺序 | canonical §20 ＋ 中间值定理 |
| 四 | **GTC**，寿命绑中枢存续；**撤单不得用预算到期 bar**（前视） | `049:51`【正文】 |
| 五 | **限价路径不扣 `slippage_bps`**；配双口径报告纪律；maker/taker 按是否预先在簿判 | 物理（限价不吃盘口） |

---

## 裁定一：穿越才成交，打平不成交

**买单 `low < 挂单价` 才成交（严格小于），`low == 挂单价` 不成交。卖单对称（`high > 挂单价`）。** 成交价见裁定二。

**理由**：

1. **价格是整数 tick 域**（`types.rs:52-55`）⟹「`low` 恰等于挂单价」不是浮点巧合，是高频事件。浮点世界里这一问不值得问，离散网格上它承重。
2. **这批挂单挂在 ZG/ZD——全市场盯着的关口**，正是排队最长的价位。价格擦一下即弹走时，队列根本轮不到你。「触及即成交」在**最不该乐观的地方最乐观**。
3. **否则等于按每根 bar 的极值给自己成交**，这是限价回测最经典的过拟合来源；而它伪造出的假 alpha 恰好长得像「短差做得准」——正是本图要验的东西。

**被否的候选**：

- **触及即成交（`low <= 挂单价`）**：见上。
- **触及 + 穿透 k 个 tick 的缓冲**：k **无数据可标定**（本仓无逐笔、无盘口）。`venue_fee.rs` 的 `uncalibrated_addon` 已是一个「看着合理的拍脑袋常数」被其自身文件登记为裂缝的前例，不加第二个。若日后要做，k 走 [#917](https://github.com/xy7365527-lang/NewChanlun/issues/917) 的标定线。

**代价**：实盘中排在队首者那一下**确实成交了**，本裁定判其未成交 ⟹ **系统性悲观**。接受，因为它与现状（close 成交）**偏差同向**，不翻边——而 (乙) 的最大风险恰是把偏差翻到乐观侧。

## 裁定二：开盘已越过限价时，按开盘价成交

**买单成交价 = `min(open, 挂单价)`；卖单 = `max(open, 挂单价)`。**

**理由**：

1. **这是撮合的真实语义，不是优待**。限价单的合约是「此价**或更好**」。开盘价已在 98 而你挂 100 买，你的单可立即成交，撮合按对手价给你。记 100 是**记了一个不存在的价格**。
2. **与裁定一同源**。裁定一判「打平不成交」的理由是队列；价格**已穿过**挂单价时队列问题消失（你前面的单被吃光，价格才走得过去）。「穿过必成交」与「穿过时得更优价」是同一逻辑的两半。

**⚠️ 本裁定的偏差方向与裁定一相反，二者必须分开裁**：裁定一取严，因为取松会**凭空造出**假 alpha；裁定二取松，因为取严会**凭空抹掉**真 alpha。

**最小反例**：BTC 插针，前一根收 100.5，本根 `open=98 / low=97 / close=99`，买单挂 ZD=100。一律按挂单价 ⟹ 记 100.00 买入，实盘 98.00。**每次插针少赚 2%**——而急跌插针正是「中枢下方如数接回」（`049:53`）最赚钱的那一类 bar。一律保守会把 [ADR 0017](0017-chong-quantity-numerator-shortdiff-amplitude.md) 那批规则判成**假阴性**：一个不报警、只让你放弃有效策略的错误。

**遗留有效域**：`open > 挂单价` 而 `low < 挂单价`（bar 内下探触及）时仍按挂单价成交——**bar 内价格路径不可知**，挂单价是唯一有依据的价。本假设不因本裁定改善。

## 裁定三：同一根 bar 内多订单的三级排序键

```
同一 exec_index 内：
  ① 平仓类 (Close/Reduce) 先于 开仓类 (Buy/Sell/Add)   ← canonical FULL §20，跨方向兜底
  ② 同类内按价格优先：买单价高→低，卖单价低→高          ← 中间值定理
  ③ 仍并列 → 保持出现顺序（稳定排序），注释标为未定义有效域
```

**为什么这不是洁癖**：`fill.rs:115` 的现金约束（「开多需 `cash ≥ 含费 cost`」）使**次序改账**——谁先成交谁吃掉现金，后者可能因没钱而不成交。次序不同 ⟹ 持仓量、成本基都不同。

**② 的理由**：价格从 `open` 走到 `low` **必然先经过高价挂单再经过低价挂单**——中间值定理，**不需要知道 bar 内任何路径细节**。这是本问里唯一不用编造信息就能得到的次序。

**① 的理由**：中间值定理只能给**同向**单排序。同 bar 内买单 99 与卖单 101 都被触及时，谁先取决于价格先上还是先下——那正是不可知的东西。故用既有正本兜底：canonical FULL §20「撤单→先平后开」（`fill.rs:106`）。**这是同一条规则换作用域，不是新口径。**
**⟹ 2026-08-06 [#926](https://github.com/xy7365527-lang/NewChanlun/issues/926) 回填：本条在仓内已有实现**——`strategy::plan_orders` 的 `ConflictKey.exit_first`（`strategy/exec.rs:145-158`，排序落点 `strategy/mod.rs:372`）。**落地应复用它，不新造排序键**；且 `ConflictKey` 的后续键（高 level 先 → 1/2/3 类）是本 ADR 裁定三未涉及的额外维度，落地时须一并接受或显式排除。

**③ 的理由**：同为开仓的一买一卖在单标的策略中**不应出现**。为一个从未发生的情形上运行时断言，是把理论问题换成真实崩溃风险，代价方向反了。照 `fill.rs:115` 那句「保证金约束…v0 未建模——**诚实有效域 L0**」的同款处理，一句注释写清即可。

**被否的候选**：**按 OHLC 形状推断 bar 内价格路径**（如「open 离 high 近就假设先上后下」）——bar 内路径**不可知**，推路径是在造数据。

## 裁定四：GTC，寿命绑中枢存续；撤单不得用预算到期 bar

**限价单挂出后一直有效，直到成交或被撤；撤单条件 = 生成它的中枢失效（延伸/升级/结束）。**

**原文依据**（`049-第49课.md:51`【正文】）：「在出现第三类买点之前，中枢未被破坏，当然有所谓的中枢震荡，**其后，中枢已经完成就无所谓中枢震荡了**」——挂单的寿命边界是原文自己划的。

**为什么「当根 bar 作废」会把裁定一~三全部作废**：那等于「只在这一分钟内跌到 ZD 我就买」。而价位驱动的**全部价值**在于「不知道它何时跌到，所以先挂着等」。中枢下沿离当前价通常有距离，一根 1min bar 振幅很小 ⟹ 信号 bar 当根恰好跌到 ZD 的概率极低，这类订单**几乎永不成交**，成交次数趋近 0，[ADR 0017](0017-chong-quantity-numerator-shortdiff-amplitude.md) 那批规则**又一次验不了**——与被否的 (甲) 档同一个结局。
（振幅那句是**未实测的量级判断**，但它不承重：`049:53` 讲的是中枢存续期间一直成立的持仓规则，语义上本就是 GTC。）

### ★ 本裁定的硬约束：撤单不可实现为「预算好的到期 bar」

strategy 层在挂单那一刻**算不出中枢何时失效——那是未来信息**。⟹ 照 `fill.rs:23` 那条既有分层（延迟在 strategy 层算好、fill 只消费 `exec_index` 一个数）给 `Order` 加一个 `expiry_index` 的做法，**用了就是前视**。

唯一可行路径：**strategy 层每根 bar 输出「此刻应挂的限价单集合」，fill 层每根 bar 取最新集合**。撤单变成隐式（不在集合里即撤），无需前视，无需新的撤单消息类型。

**代价是结构性的**：订单接口从**事件流**（「在第 N 根 bar 执行这一笔」）变成**状态流**（「第 N 根 bar 时挂单簿长这样」）。`Order.exec_index`（`types.rs:364`）的语义要重解，生产回路主循环要重写。**该改动不在本 ADR，另立子票承接**（见「移交」）。
**⟹ 2026-08-06 [#926](https://github.com/xy7365527-lang/NewChanlun/issues/926) 回填后收窄**：状态流上游（`p_star`）已在生产，`LevelOrderLedger` 骨架已在场，`entry_tick_for` 接入点已预留 ⟹ **仍是结构性改动，但不是从零造**。详见「勘察回填」节订正三。

## 裁定五：限价路径不扣 `slippage_bps`，配双口径报告纪律

**限价被动成交（裁定一路径）与开盘越过成交（裁定二路径）均不叠加 `slippage_bps`；现有 close 市价路径照旧叠加。**

**理由**：

1. **那 2bp 建模的东西在限价路径上物理不存在**。`uncalibrated_addon` 是**市价单吃穿盘口的冲击成本**（`venue_fee.rs:38`）；限价单不吃盘口，不产生冲击。照旧全扣 = 把市价单的成本模型套在不是市价单的东西上。
2. **限价的真实代价已在裁定一收过**。限价的代价不是滑点，是**没成交**（机会成本）——裁定一的严格穿越判据就在收这笔账，且收得偏保守。全扣等于**同一笔代价收两遍**。
3. **不为限价路径单独标定滑点值**：需要盘口数据，本仓没有（`venue_fee.rs` 自己把它登记为裂缝 3）。

### ⚠️ 本裁定的准入条件（不是可选补充）

本裁定让限价路径比市价路径**便宜 2bp/side**。而本图正在验的恰是「价位驱动的限价短差是否优于信号驱动的市价成交」——**这等于我们自己给结论送了一阵顺风**。

> **任何「限价短差优于市价」的结论，必须在 `slippage_bps = 0` 与照旧全扣两种口径下各跑一遍，两边都成立才算数。**

同一份代码换个常数，零成本；换掉的是一个会伪装成 alpha 的建模选择。**只裁前半不绑这条，本裁定不成立。**

### maker/taker 角色：附带收掉

- **判定规则**：订单在这根 bar **之前**就已挂在簿上 ⟹ **Maker**；信号 bar 当根新挂、挂上去时价格已在更优侧（裁定二路径）⟹ **Taker**。完全由裁定一/二/四推出，无新信息。
- **`PRODUCTION_LIQUIDITY_ROLE`（`venue_fee.rs:112`）须随实装改动**：它现锁死 `Taker`，理由写的是「无限价挂单语义 ⟹ 声称 maker 档是声明膨胀（090）」（`venue_fee.rs:58-59`）。**(乙) 落地后该理由即为假**，注释必须同步改，否则留下一句读起来通顺、实际已失效的溯源——正是本 ADR「上游引用订正」表里那类错的产生机制。
- **BTC 现货 maker == taker（10.0/10.0；BNB25 7.5/7.5）⟹ 本次启用对 PnL 零影响**，`venue_fee.rs:271` 早有登记。**不许拿「启用了 maker」当成本优势去论证任何东西。** 真有 maker 收益的场所（Binance 永续、VIP 高档）目前全是未核项，`VenueFeeBook::resolve` 对其返回 `Err`（fail-loud）。

---

## 有效域

- **L0（形态裁定）**：五条裁定均为口径选择，无实测背书。裁定一「排队导致打平不成交」、裁定四「振幅量级」两条的**经验依据未测**——本仓无逐笔、无盘口、无成交队列数据，(乙) 档的成交概率是**假设不是实测**（这是 (乙) 相对 (丙) 的既有代价，[#918](https://github.com/xy7365527-lang/NewChanlun/issues/918) 档位裁定时已明账）。
- **裁定二的遗留假设**：`open > 挂单价` 而 `low < 挂单价` 时按挂单价成交，因 bar 内路径不可知。
- **裁定三③**：同 bar 反向开仓单的次序**未定义**。
- **生产回路现状已由 [#926](https://github.com/xy7365527-lang/NewChanlun/issues/926) 回填**，见下节。其自身的诚实缺口（测试侧构造点未穷举、`rust/tests/` 未纳入检索、「无重试」的穷举强度仅在 `pending` 这个名字上成立）随该票 resolution，不在此复述。

## 勘察回填（2026-08-06，[#926](https://github.com/xy7365527-lang/NewChanlun/issues/926) AFK 只读勘察）

**订正一：生产净额成交回路只有一条。** `pi_theta_fill_loop_overlay`（`fill.rs:4369`）；`pi_theta_fill_loop`（`:746`）是其 `overlay=None` wrapper，`pi_theta_fill_loop_voice`（`:788`）是 `#[cfg(test)]`。生产入口 `runner.rs:311` / `:525` / `:678`。**`plan_and_fill_mtm` / `plan_and_fill_mtm_dual` 不存在**（`runner.rs:5` 逐字：「已退役的是 v1 runner…#499 退役」）。⟹ `config.rs:284-285` 那份四条清单**坏了三条**，[#928](https://github.com/xy7365527-lang/NewChanlun/issues/928) 严重程度据此上调。

**订正二：裁定三①在本仓已有实现，不该新造排序键。** 净额回路一行排序都没有（队列 `fill.rs:4429`，入队 `:5824`，出队 `:4624-4627` 按 `Vec` 顺序），它靠**每 bar 只产一个净额订单**回避冲突。但 `strategy::plan_orders`（`strategy/mod.rs:302`，NT 生产路径）**已有** `planned.sort_by_key(|(key,_)| *key)`（`:372`），键 = `exec::ConflictKey`（`strategy/exec.rs:145-158`）六元组：`exit_first`（退出=0/开仓=1）→ `level_desc`（高 level 先）→ `bsp_class`（1/2/3 类）→ `timestamp` → `source_index` → `depth`。**其 `exit_first` 即裁定三①，与 canonical §20 同源** ⟹ **裁定三的落地应复用 `ConflictKey`，不新造键**。（声部臂另有硬编码 push 次序 `fill.rs:5835-5878`，注释 `:5833` 自陈「平单先挂、开单后挂」。）

**订正三：裁定四的接口改动不是从零造。** 状态流上游**已在生产**——`p_star`（目标净持仓）每决策 bar 一份，`Order = f(目标 − 当前)`（`strategy/coverage/sizing.rs:524`，`qty==0` 退化 `Hold`/`Wait`）。更完整的 `LevelOrderPlan`/`LevelOrderLedger` 骨架已在场（`strategy/level_order.rs:328-334`/`:458`/`:550`/`:573`）但**无生产消费者**（7 处非定义命中全是注释，三处自陈未接线）。缺的只是把**限价单集合**也做成 per-bar 状态。**且限价接入点已预留**：`nautilus/strategy.rs:317` 的 `entry_tick_for` 忽略入参恒返 `None`，doc `:315-318` 自陈「限价腿 entry 价回填待 `plan_orders` 接口扩展」。

**印证：裁定四否掉的那一档正是现状。** 未成交订单**一律丢弃、零重试**，三个丢弃点：`qty<=0` 不入队（`fill.rs:5821`）／`untradable`·`px<=0` 留在 `pending[i]` 里烂掉（`:4624`，其中 `untradable` 分支实为不可达死代码，因 `exec.rs:46-49` 保证落点可交易）／现金不足与超仓余量进 `rejected_qty` 而**主循环从不读它**（`fill.rs:239-249`/`:271`，6 处命中全在构造与文档里）。

**`Order` 加字段的波及面**：`types.rs:357-365` 仅 3 字段且全部字面量构造（无 `..Default`、无 `#[non_exhaustive]`）⟹ **加字段必然编译失败，不会静默漏**。生产构造点 4 处（`sizing.rs:558`、`strategy/mod.rs:440`/`:462`、`dual_ledger.rs:258`）。既有的「不加字段」规避先例：`LegOrder`（`strategy/mod.rs:945`，doc `:934` 明写「types.rs `Order` 不加字段，冲突面零改」）。

## 移交

- **生产回路现状勘察** → [#926](https://github.com/xy7365527-lang/NewChanlun/issues/926)，**已完成并回填本 ADR**（见「勘察回填」节）。
- **接口改状态流** → [订单接口：事件流 → 状态流 #927](https://github.com/xy7365527-lang/NewChanlun/issues/927)（挂 map [#787](https://github.com/xy7365527-lang/NewChanlun/issues/787)，由 [#918](https://github.com/xy7365527-lang/NewChanlun/issues/918) 与 [#926](https://github.com/xy7365527-lang/NewChanlun/issues/926) blocking）。**其形态空间已被 #926 收窄两处**：裁定三应复用既有 `ConflictKey` 而非新造排序键；裁定四的状态流上游（`p_star`）已在生产、`entry_tick_for` 接入点已预留 ⟹ 「结构性改动」仍成立，但不是从零造。
- **`config.rs:284-285` 的过期清单**：原判「把 `simulate_fills` 列为生产回路」，[#926](https://github.com/xy7365527-lang/NewChanlun/issues/926) 查实**四条里坏三条**（另两条已随 #499 删除）。与在飞决策不挂钩的文本欠账，按 `docs/agents/wayfinder-workflow.md` 入流判据**不进图**，走 triage 挂 `debt` → [#928](https://github.com/xy7365527-lang/NewChanlun/issues/928)。
- **`fill.rs:23` 的 `types.rs:218` 行号漂移**：同上，随 debt 一并订正。
