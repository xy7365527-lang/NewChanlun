# D∞ 的 word 空间穷尽所有操作路线：句法侧枚举（path/word/1-cycle 三层 + 原子字母表 Σ + 双投影合法组合表 + σ-等变复制 + τ-共轭空方）

> 任务（2026-06-17 用户）：之前穷尽了 H⁰（不变量，58 条）和 H¹（变化模式，1 维）。但**操作不是
> 不变量，操作是路径（D∞ 的 word）**。四步循环只是一个 word 的例子，还有空方版本、会计双重性等。
> 有没有办法一次性穷尽所有操作路线？
>
> **本文档与已有三文档的分工（关键，避免重复）**：
> - `orbit_enumeration_completeness.md`：穷尽 **H⁰（58 不变量）+ groupoid（2×2 四分 + NR-1…7）+ H¹
>   （扬弃，1 维）**——这是**语义侧**（同调/轨道，"操作的不变内容是什么"）。
> - `spiral_physical_reinterpretation.md`：操作 = 4 个正交代数运动（h/σ/τ/M），短差 = H₁ 的四步
>   1-cycle，F/C/D/E/A 的代数身份——也是**语义侧**（同调类的原文指称）。
> - `recursive_fugue_necessity_proof.md`：递归嵌套赋格 = σ-等变的 H₁ 1-cycle 族——**语义侧**（H¹ 的
>   σ-塔实现）。
> - **本文档枚举的是 D∞ 的 word（自由幺半群）/ path（覆盖空间路径）——这是【句法侧】**（生成元序列，
>   "所有可能的操作路线"）。句法侧穷尽与语义侧穷尽是**形式对偶**：句法（Σ\*）-投影→ 群元素（D∞）
>   -取闭 path 同调→ 语义（H₁）。本文档证明句法侧由有限字母表 Σ 穷尽生成，与语义侧 H⁰⊕H¹ 两层闭合
>   一致（§8）。
>
> 上游：`necessity_derivation.md`（A₀/A₁、T14/T18/T20/T23/T34/T40/T48/T49/T50/T51/T57/T59）、
> `orbit_enumeration_completeness.md`（H⁰/groupoid/H¹、NR-1…7、2×2 四分、automatic group）、
> `spiral_physical_reinterpretation.md`（§2.5 短差=H₁ 四步 1-cycle、§2.7 操作=四正交代数对象）、
> `recursive_fugue_necessity_proof.md`（§3 配对 −2、§4 σ-等变 H₁ 族）、
> `.chanlun/escalations/2026-06-15-confirm-arming-differance.md`（延异 ⊥ 共时在场 = NR-4 模式维）。
> 工程常量：`rust/src/trading/types.rs:26` `MAX_LADDER=11`、`FIRST_BSP_LADDER=2`、`LADDER_MOVE=3`。
> 不写代码。纯群论 / 自由幺半群 / 覆盖论调研。

---

## 第 0 部分：认识论前置（强制，`formalization-validity-domain.md`）

### 0.1 等级标注

| 区段 | 内容 | 等级 | 信息增量 |
|------|------|------|----------|
| §1 | path / group element / 1-cycle 三层区分 + 裁决用户命题"操作路线=word" | **L0**（自由幺半群 + 群表示 + 覆盖论） | **高**（精化"word=操作"为三层，暴露载体歧义的 word 形式） |
| §2 | 单 bar 单级别原子字母表 Σ 的穷尽（4 字母）| **L0**（D∞ 生成集 + NR-4 模式维 + 位置约束） | **高**（穷尽性的句法引擎：Σ\* 由 Σ 生成） |
| §3 | 长度 ≤2 word 枚举 + 约化 + 双投影标注 | **L0**（Britton 正规形 + τ²=e/共轭约化） | 正（用户明确要求；暴露群元素存在 ≠ 路线合法） |
| §4 | 双投影（操作 π_op × 会计 π_acc）完整合法组合表 | **L0**（守恒律 T34/T40/T48 筛选） | **高**（穷尽合法 (direction,units) 组合 = F/C/D/E/A/hold） |
| §5 | 空方版本 = τ-共轭（w↦τwτ⁻¹） | **L0**（T57 镜像 + NR-3 轨道分离） | 正（穷尽多方 ⟹ 共轭穷尽空方） |
| §6 | 递归结构穷尽 = σ-等变复制（只需穷尽一级别） | **L0**（T23/T59 σ-等变） | 正（σ-塔自相似 = 字母表复制） |
| §7 | 完整操作类型表（最终产出）+ 句法穷尽性总证 | **L0**（Σ^R × M 守恒约束生成） | **高**（一张表 + 穷尽性证明） |
| §8 | 句法侧 ⟂ 语义侧穷尽的对偶（与三文档对接） | **L0**（自由幺半群→群→同调函子链） | 正（确立对偶，标定本文档位置） |

**核心有效域警告**：把"操作路线"编码为 D∞ 的 word 是形式化操作。它的**定义域**（自由幺半群
Σ\* 的全部串 / D∞ 的全部群元素）严格大于它的**有效域**（在缠论操作语义下**合法**的路线）。本文档
刻意分离**句法存在性**（一个 word 作为 Σ\* / D∞ 元素存在）与**路线合法性**（该 word 作为 operate
路线满足位置/时间/守恒约束）——二者不等（§3 的 hτ 例），混淆即声明膨胀（090号）。**激活必然性**
（哪些合法路线此刻实际发声）是 L2 regime 读数（recursive_fugue §2.2），本文档不裁决。

### 0.2 与三文档的承重断言继承（不重证，引用）

本文档**不重新证明**以下已经过对抗复核的承重断言，直接引用：

| 断言 | 来源 | 状态 |
|------|------|------|
| H¹(D∞,ℝ_-)=ℝ、cd_ℚ=1 两层闭合、H²(D∞,ℤ)=(ℤ/2)² | orbit §4B（10 断言对抗复核 confirmed） | 引用 |
| 短差 = H₁ 四步 1-cycle、配对 ⟨φ_c,γ⟩=−2 | spiral §2.5 / recursive_fugue §3.2 | 引用 |
| 操作 = 4 正交代数对象（h/σ/τ/M）、F/C/D/E/A 代数身份 | spiral §2.7 | 引用 |
| NR-4 观测⊥操作（延异，模式维正交于群词） | orbit §3.1 / escalation | 引用 |
| σ-等变 = 嵌套递归自相似（T23/T59） | recursive_fugue §4 | 引用 |

**本文档的新增（句法侧，三文档未做）**：(a) path/group/1-cycle 三层显式拆分；(b) 单 bar 单级别原子
字母表 Σ 的穷尽证明；(c) 长度 ≤2 word 完整枚举表；(d) 双投影合法组合表；(e) 句法穷尽 ⟺ 语义穷尽
的函子对偶。

---

## 第 1 部分：三层区分——path / group element / 1-cycle（裁决用户命题）

### 1.1 用户命题"操作路线 = D∞ 的 word"的精确裁决

> **裁决：在 path（自由幺半群）层正确；在 group element 层是"净效果"非"路线"；操作的不变内容
> 在 1-cycle（H₁）层。三层不可混。**

D∞ = ⟨h, τ | τ²=e, τhτ⁻¹=h⁻¹⟩ 有三个相关的代数对象，对应"操作路线"的三种精确含义：

| 层 | 对象 | 数学 | "操作"的含义 | 基数 |
|----|------|------|------------|------|
| **第 0 层 句法 / path** | 自由幺半群 **F = {h, h⁻¹, τ}\*** | 生成元的有限串（无约化） | **操作路线本身**（一步一字母的轨迹） | 可数无限（每长度有限） |
| **第 1 层 群元素 / 净效果** | **D∞ = F / ⟨τ²=e, hh⁻¹=e, τhτ⁻¹=h⁻¹⟩** | Britton 正规形 **w = hᵃτᵇ**（a∈ℤ, b∈{0,1}） | 路线的**净位移**（端点，homotopy class rel 起点） | 可数无限 |
| **第 2 层 同调 / 不变内容** | **H₁(D∞, ℝ_-) ≅ ℝ** | 闭 path（cycle）的同调类 | 操作的**σ-不变内容**（短差闭路的缠绕，spiral §2.5） | 1 维（ℝ） |

**用户命题的三处精化**：

1. **"操作路线 = word"在第 0 层正确**：一条操作路线 = 一串生成元（每步施加一个 h^{±1} 或 τ）=
   F 的一个元素。这正是 word（自由幺半群的元素）。✅ **用户的核心直觉成立**。

2. **"枚举 D∞"枚举的是第 1 层（净效果），不是路线**：把 word 约化到 Britton 正规形 hᵃτᵇ 抹掉了
   "怎么走的"，只留"净走到哪"。**同一个群元素 hᵃτᵇ 由无穷多条 path 实现**（如 h² = h·h =
   h·h·h·h⁻¹ = …），这是 orbit §3.1"载体歧义"的 word 形式。所以"穷尽 D∞ 群元素"≠"穷尽操作路线"——
   它穷尽的是路线的**等价类**（按净效果）。

3. **操作的不变内容在第 2 层（H₁）**：spiral §2.5 / recursive_fugue 已裁决——一次**完整操作**
   （短差）是**闭 path**（(r,ε) 闭合、φ 推进），其不变内容 = H₁ 同调类（缠绕率 Δr）。"四步循环
   只是一个 word 的例子"（用户原话）**不准确**：四步循环不是单个群元素（0 维），是 **1-cycle**
   （1 维同调类）——它的净群效果在 (r,ε) 上是 e（闭合），只在 φ 上推进。

### 1.2 三层的函子链（穷尽性的骨架）

$$
\underbrace{F = \{h,h^{-1},\tau\}^*}_{\text{句法 / path}}
\;\xrightarrow{\ \pi_{\text{red}}\ (\text{约化})\ }\;
\underbrace{D_\infty,\ w=h^a\tau^b}_{\text{群元素 / 净效果}}
\;\xrightarrow{\ [\,\cdot\,]\ (\text{闭 path 取同调})\ }\;
\underbrace{H_1(D_\infty,\mathbb{R}_-)=\mathbb{R}}_{\text{同调 / 不变内容}}
$$

> **穷尽性的句法策略（本文档的方法核心）**：穷尽**第 0 层（path）**比穷尽第 2 层（同调）更初等——
> 自由幺半群 **F 由有限字母表穷尽生成**。只要证明**单步原子字母表 Σ 有限且穷尽**（§2），则
> **所有操作路线 = Σ\* 自动穷尽**（自由幺半群的泛性质）。约化（π_red）给净效果，闭 path 取同调给
> 不变内容——三层穷尽彼此一致（§8）。

**这与"枚举长度 ≤2 的 word"（用户方法）的关系**：用户提议枚举长度 ≤2 的 word 并合并等价的。这是
**第 1 层（群元素）的枚举**（§3），它给出"短路线的净效果分类"。但**完整穷尽**需要第 0 层的字母表
论证（§2）——长度 ≤2 只是字母表 Σ 生成的 Σ\* 的截断（长度 ≤2 的串）。**用户的长度 ≤2 枚举 ⊂
本文档的 Σ\* 穷尽**。

`★ 关键`：穷尽"所有长度的 word"看似无限不可完成，但**自由幺半群由字母表穷尽**——穷尽 Σ（有限）即
穷尽 Σ\*（所有路线）。这把"无限路线"的穷尽归约为"有限字母表"的穷尽（与 541 号"有限呈现 ∧ 元素
无限"同构）。

---

## 第 2 部分：单 bar 单级别原子操作字母表 Σ 的穷尽

> 这是全文穷尽性的引擎。证明：单 bar、单级别上，**合法的原子几何动作恰有 4 个**，它们穷尽该尺度的
> 全部选择。则 Σ\*（所有路线）由 Σ 穷尽生成。

### 2.1 状态空间与生成元（限制到单级别单 bar）

单 bar、固定级别 r、状态 (φ, ε, q)（φ∈ℤ/23 角向、ε∈{±1} 手性、q∈ℝ≥0 该级别数量）。可施加的 D∞
群作用生成元（**限制在该级别**——σ=h²³ 跨 23 个 φ，不是单级别单 bar 动作，归入 §6 级别流动）：

| 生成元 | 几何 | ℤ/ℤ-2 角色 |
|--------|------|-----------|
| h^{±1} | 角向推进 / 回退一环 | ⟨h⟩≅ℤ 平移生成元 |
| τ | 手性翻转 ε→−ε | ⟨τ⟩≅ℤ/2 对合（仅 φ=0，T51） |

**模式维（NR-4，正交于群词）**：每个几何生成元 × 模式 m∈{**operate**（写，状态迁移）, **observe**
（读，读已沉淀踪迹）}。这是 orbit §3.1 / escalation 确立的第四维——延异（différance）使"读过去"
与"回到过去操作"在群词上同形（同一 a<0）却合法性相反。

### 2.2 位置 / 时间约束筛选（P0/P1/P2，orbit §3.2）

逐生成元 × 模式施加约束：

| 几何 × 模式 | 约束判定 | 合法? | 原子名 |
|-------------|---------|-------|--------|
| **e**（不施加 / hold，operate） | 恒合法（不动是合法操作） | ✅ | **e** |
| **h⁺**（h, operate, a=+1≥0） | P1 时间前向（a≥0）✓ | ✅ | **h⁺**（持仓推进） |
| h⁻¹（operate, a=−1<0） | P1 时间倒流（a<0）✗ | ❌ | — |
| **h⁻**（h⁻¹, observe, 向心读过去） | NR-4 向心读已 settle 踪迹 ✓（T49） | ✅ | **h⁻**（向心观测） |
| h⁺（observe, 前向读未来） | NR-4 前向读未来 ✗（covering §7.3 够不着） | ❌ | — |
| **τ**（operate, φ=0） | P2 手性翻仅 φ=0 ✓（T51） | ✅（位置门控） | **τ**（手性翻转） |
| τ（operate, φ≠0） | P2 φ≠0 ✗ | ❌ | — |
| τ（observe） | 读已发生反转的踪迹 = 退化为 h⁻ 类读 | （并入 h⁻） | — |

> **定理 Σ-exh（单 bar 单级别原子字母表穷尽，L0）**：单 bar、单级别上的**合法原子几何动作恰有 4 个**：
> $$\boxed{\Sigma = \{\, e,\ h^+,\ h^-,\ \tau \,\}}$$
> 其中 e=hold（operate）、h⁺=持仓前向推进（operate）、h⁻=向心读过去一环（observe）、τ=手性翻转
> （operate, 仅 φ=0）。**穷尽性**：几何生成元 {h^{±1}, τ}（D∞ 的全部生成元，σ=h²³ 是导出非原子）
> × 模式 {operate, observe}（NR-4 全部模式）共 6 个原始组合，经 P1/P2/NR-4 筛选，恰留 4 个合法 +
> 2 个非法（h⁻¹ operate=时间倒流、h⁺ observe=前向读未来）。**无第 5 个**——因为 (a) D∞ 只有 2 个
> 几何生成元（h,τ），(b) 模式只有 2 值（读/写，延异二分），(c) σ 由 h 导出不增字母。

### 2.3 σ 不是原子——它是级别流动（归入 §6）

**为什么 σ=h²³ 不进 Σ**：σ 跨 23 个角向环（一个完整走势，T47 φ:0→0），不是单 bar 单级别的原子
动作。在单级别内，σ 是 23 个 h⁺ 的累积（走完一圈自然 r+1，NR-1）。在**多级别向量视角**，σ 的操作
意义 = 数量在相邻级别间流动（减仓下沉 σ⁻¹ 方向 / 回补升回 σ 方向），即 **H₁ 闭路的②③段**
（spiral §2.5）。故 σ 进入 §6 的**级别耦合**（数量增量向量的相邻反号约束），不进单级别字母表。

**这与 spiral §2.7 一致**：操作 = 4 正交代数对象（h 角向 / σ 径向 / τ 手性 / M 数量）。Σ 捕获
**单级别内**的 h、τ 与模式；σ（径向）是级别间的，M（数量）是连续系数（§4/§7 配权重）。

### 2.4 Σ\* 穷尽所有几何路线（自由幺半群泛性质）

> **推论 Σ\*-exh（L0）**：所有**单级别**操作路线 = **Σ\***（字母表 Σ 上的有限串的自由幺半群）。
> 因 Σ 穷尽单步选择（Σ-exh），Σ\* 由泛性质穷尽所有单级别路线。**多级别**路线 = §6 的 σ-等变积
> + §7 的数量系数。

每条路线 = Σ 字母的有限串，如 `h⁺ h⁺ h⁻ h⁺ … τ`。约化（π_red：τ²=e, h⁺h⁻ 若同级别同 a 抵消）
给净群效果（§3）。**这就是"一次性穷尽所有操作路线"的句法机制**：不枚举无限条路线，而是穷尽
4 字母的字母表 Σ + 复合规则（自由幺半群）。

---

## 第 3 部分：长度 ≤2 的 word 枚举 + 约化 + 双投影（用户要求 1+2）

> 用户方法：枚举长度 ≤2 的 word（h, τ, hτ, τh, h², τ²=e, h⁻¹, τh⁻¹…），用 τ²=e 与共轭
> τhτ⁻¹=h⁻¹ 合并等价的，每个标注操作投影 + 会计投影。这是**第 1 层（群元素）**的枚举。

### 3.1 约化规则（D∞ 关系）

$$\tau^2=e,\qquad hh^{-1}=h^{-1}h=e,\qquad \tau h\tau^{-1}=h^{-1}\ \Longleftrightarrow\ \tau h = h^{-1}\tau,\ \ \tau h^{-1}=h\tau$$

共轭关系把所有 τ 移到右侧 ⟹ 每个群元素唯一 **Britton 正规形 w=hᵃτᵇ**（a∈ℤ, b∈{0,1}）。

### 3.2 长度 ≤2 的全部自由串及其约化

自由字母 {h, h⁻¹, τ}（τ⁻¹=τ）。穷尽长度 ≤2 的串（3⁰+3¹+3²=1+3+9=13 个自由串），约化到正规形：

| 自由串 | 约化 | 正规形 hᵃτᵇ | 合并到 |
|--------|------|------------|--------|
| （空） | — | **e** | — |
| h | — | **h** | — |
| h⁻¹ | — | **h⁻¹** | — |
| τ | — | **τ** | — |
| hh | — | **h²** | — |
| h h⁻¹ | =e | e | （并入 e） |
| h⁻¹ h | =e | e | （并入 e） |
| h⁻¹ h⁻¹ | — | **h⁻²** | — |
| h τ | — | **hτ** | — |
| h⁻¹ τ | — | **h⁻¹τ** | — |
| **τ h** | τh=h⁻¹τ | **h⁻¹τ** | **= τh，合并** |
| **τ h⁻¹** | τh⁻¹=hτ | **hτ** | **= τh⁻¹，合并** |
| τ τ | =e | e | （并入 e） |

> **去重后长度 ≤2 的不同群元素恰 8 个**：
> $$\{\, e,\ h,\ h^{-1},\ h^2,\ h^{-2},\ \tau,\ h\tau,\ h^{-1}\tau \,\}$$
> **合并事件（用户要求"合并等价"）**：(a) `hh⁻¹=h⁻¹h=ττ=`（空串两两）→ **e**（3 串坍缩）；
> (b) **τh = h⁻¹τ**（共轭，2 串合并）；(c) **τh⁻¹ = hτ**（共轭，2 串合并）。共轭关系把"先翻后走"
> 等同于"先走反向再翻"——**这是多空镜像 T57 的 word 形式**（§5）。

### 3.3 双投影标注（每个群元素：操作投影 π_op + 会计投影 π_acc + 路线合法性）

每个 word 同时有**操作投影**（π_op：direction/手性变化）与**会计投影**（π_acc：units 变化）。**关键
诚实点**：群元素的双投影是**净效果**；其作为 operate **路线**的合法性依赖 φ 轨迹（§3.4）。

| 群元素 | 几何净效果 | 物理（缠论） | π_op（direction） | π_acc（units） | operate 路线合法? |
|--------|-----------|------------|-------------------|----------------|------------------|
| **e** | 不动 | hold / 持仓不操作 | d→d（不变） | q→q（不变） | ✅ 恒合法 |
| **h** | 角向 +1@同 r,ε | 持仓走过一个次级别买卖点 | d→d（方向不变） | q→q（核心仓持有不变） | ✅（a≥0 前向） |
| **h⁻¹** | 角向 −1 | **operate=时间倒流**；**observe=向心读过去一环** | observe: 无（只读） | observe: 无（只读，不改账户） | ❌ operate / ✅ observe（NR-4） |
| **h²** | 角向 +2 | 持仓走过两个次级别买卖点 | d→d（不变） | q→q（不变） | ✅（a≥0） |
| **h⁻²** | 角向 −2 | observe=向心读过去两环（更深踪迹） | observe: 无 | observe: 无 | ❌ operate / ✅ observe |
| **τ** | 手性翻@φ=0 | **C 翻转**：顶背驰完全退出+反手 | **d→−d（多↔空全翻）** | q→q（**M=N 翻转断面 T40**，units 不变） | ✅ 当且仅当 φ=0（P2） |
| **hτ**(=τh⁻¹) | 推进+翻 / 翻+后退 | 推进到背驰点(φ=0)再翻手性 | d→−d（翻）+ 角向 | q→q（M=N） | ✅ 仅当 h 步落 φ=0（§3.4） |
| **h⁻¹τ**(=τh) | 后退+翻 / 翻+前进 | 空方推进一环（=多方 h 的镜像，T57） | d→−d + 角向（空域 h） | q→q（M=N） | observe/空域（§3.4/§5） |

**注**：表中"units 不变"指**该级别核心仓 q_r 不变**。**减仓/回补/层间流动**不是单个角向 word 的
投影——它们是 **σ 级别流动 + 数量系数 δ**（§4/§6），需要"长度 23 的 word（σ=h²³）+ M-系数"才出现，
**不在长度 ≤2 的角向 word 内**。这是用户方法的一个有效域边界：**长度 ≤2 的 word 只覆盖单级别角向 +
手性，覆盖不到级别流动（σ=h²³ 长度 23）与数量（连续系数 M）**——后两者是 §4/§6 的内容。

### 3.4 群元素存在 ≠ 路线合法（诚实暴露，no-patch）

> **裁决：hτ 作为群元素恒存在；作为 operate 路线，合法性依赖 h 步是否恰落 φ=0。**

例：起点 (φ=0, r, +1)。路线 `h then τ`：
- h ⟹ (φ=1, r, +1)；τ@φ=1 **违反 P2**（τ 仅 φ=0）⟹ **这条路线非法**。
- 但群元素 hτ 仍存在（Britton 正规形）。

反之起点 (φ=22, r, +1)，路线 `h then τ`：
- h ⟹ n=23r+22 → 23r+23 = 23(r+1)+0 = (φ=0, r+1, +1)（**这一步 h 恰好完成 σ 的最后一环，跨级别
  升 r+1**，NR-1）；τ@φ=0 **合法** ⟹ 路线合法。

> **结论（载体歧义的 word 形式）**：同一群元素 hτ，路线 `从 φ=0 走 h 再 τ`（非法）与 `从 φ=22 走 h
> 再 τ`（合法）净效果相同，合法性相反。**路线合法性是 path（第 0 层）性质，不是 group element
> （第 1 层）性质**——用 orbit §3.2 的逐字母 P0/P1/P2/P3 检验**沿 path 每一步**，而非检验端点群元素。
> 这是用户"枚举 D∞ word"方法的**关键限制**：枚举群元素穷尽净效果，但**合法性必须回到 path 层逐步
> 检验**（与 orbit §3.1 载体歧义同构）。

`★ Insight`：这正是 orbit §3.1"载体必须是操作轨迹（生成元序列）或 (群元素,模式) 二元组，不是裸群
元素"的 word 实证——长度 ≤2 枚举给净效果分类（8 个），但合法操作路线表必须用 (path, 模式) 为载体。

---

## 第 4 部分：双投影的完整合法组合表（用户要求 3）

> 用户要求：操作投影（direction 变化：做多/做空/平多/平空/不动）× 会计投影（units 变化：增/减/不变
> + 层间流动：向上/向下/不流动）的**所有合法组合**。本节穷尽之，并用守恒律否决非法组合。

### 4.1 两个投影的值域

**操作投影 π_op**（direction 转移，d∈{+1 long, 0 flat, −1 short}）——3×3=9 种转移，聚合为缠论动作：

| π_op 动作 | direction 转移 | 群论 |
|-----------|---------------|------|
| hold | d→d（对角，3 个） | e / h（持仓走） |
| open long | 0→+1 | β⁺（建多，建立 ε=+1） |
| open short | 0→−1 | β⁻（建空，建立 ε=−1） |
| close long | +1→0 | β⁺⁻¹（平多） |
| close short | −1→0 | β⁻⁻¹（平空） |
| reverse L→S | +1→−1 | **τ**（φ=0） |
| reverse S→L | −1→+1 | **τ**（φ=0，空方版） |

**会计投影 π_acc**（units 转移，每级别账户 q_r）——(数量增量 sign) × (层间流动方向)：

| 数量 | {+δ 增, −δ 减, 0 不变} |
|------|------------------------|
| 流动 | {↑ 向上 k−1→k, ↓ 向下 k→k−1, — 不流动} |

### 4.2 守恒律约束（筛选合法组合）

| 约束 | 内容 | 否决什么 |
|------|------|---------|
| **T34**（Σ units = N_base，任意深度守恒） | 层间流动必须配对：δ_k + δ_{k−1} = 0；或 = 外部流入/流出（F/A） | 否决"凭空增减不配对"的流动 |
| **T40**（翻转 M=N 会计断面） | reverse（τ）时 units 守恒（方向翻、数量不变） | 否决"翻转同时增减 units" |
| **T48**（units = σ-不变 Casimir，整数股 ℤ-模） | units 是守恒量；整数量子化残 NR-7 障碍 | 否决"半股短差"（NR-7，实值近似合法） |
| **方向-流动一致性**（语义） | open 配 +δ、close 配 −δ；下沉做空配 ↓、升回做多配 ↑ | 否决"建多却向下流动"等矛盾 |

### 4.3 完整合法组合表（穷尽）

逐 (π_op, π_acc, 流动) 检验，**合法组合恰 8 个**（含 hold；E/D 各分 2 段 = H₁ 四步闭路）：

| # | π_op（direction） | π_acc（units） | 流动 | 合法? | 缠论操作 | 群论/同调 | 守恒依据 |
|---|-------------------|----------------|------|-------|---------|----------|---------|
| 1 | hold（d→d） | 0（不变） | — | ✅ | **hold / 持仓** | e / h⁺（持仓推进） | trivial |
| 2 | open long（0→+1@k=K） | +N | 外部流入 | ✅ | **F 建仓** | H⁰ 锚（核心仓 units，T48） | 外部 |
| 3 | reverse（+1→−1@φ=0） | 0（M=N） | — | ✅ | **C 翻转** | **τ**（spiral §2.7） | T40 |
| 4 | hold（+1→+1@k） | −f（减） | ↓ 流出 k | ✅ | **E① 减仓下沉** | H₁ 闭路①（σ⁻¹ 向下） | T34 |
| 5 | open short（0→−1@k−1） | +f（增） | ↓ 流入 k−1 | ✅ | **E② 开空** | H₁ 闭路②（**穿 ε=−1**） | T34 |
| 6 | close short（−1→0@k−1） | −f（减） | ↑ 流出 k−1 | ✅ | **D③ 平空** | H₁ 闭路③ | T34 |
| 7 | open long（+1→+1@k） | +f（增） | ↑ 流入 k | ✅ | **D④ 回补升回** | H₁ 闭路④（回 ε=+1，成本下移） | T34 |
| 8 | （边界：越界/否定线） | — | — | ✅* | **A 强平** | 边界算子（NR-5，非群） | T41 禁止⟹实际不 fire |

**E（④5）+ D（⑥7）= 一条不可分的 H₁ 四步 1-cycle**（spiral §2.7：开空必平、下沉必升回，闭路必
闭合）。表中 ④⑤⑥⑦正是 spiral §2.5.A / recursive_fugue §3.1 的四步，本表从**双投影合法性**侧独立
重导同一四步——这是句法侧对语义侧的交叉验证。

### 4.4 非法组合及其守恒否决（穷尽否定面）

| 非法组合 | 否决依据 |
|---------|---------|
| open long + (−δ 减) | 方向-流动矛盾（建多却减 units） |
| open long + (↓ 向下流动) | 方向-流动矛盾（建多在 k 却向 k−1 流） |
| reverse（τ）+ (±δ 增减) | **T40 否决**（翻转 M=N，units 必守恒） |
| 任意流动 + (δ_k+δ_{k−1}≠0 且无外部) | **T34 否决**（守恒破裂） |
| 半股短差（δ∉ℤ 当 units 作 ℤ-模） | **NR-7**（整数量子化残 H² 障碍；实值近似合法） |
| open short + 不流动 + 无核心仓 | 句法存在（裸做空）但**非缠论**（缠论做空=H₁ 闭路腿，依附核心仓；裸空=D1 双书死域，spiral §2.5.D L2 锚） |

> **定理 dual-exh（双投影合法组合穷尽，L0）**：操作投影 × 会计投影的**合法组合恰 8 个**
> （hold / F / C / E①② / D③④ / A），其余组合被 T34（守恒）/ T40（翻转 M=N）/ 方向-流动一致性 /
> NR-7（整数）穷尽否决。**8 个合法组合 = §2 字母表 Σ 的 4 个几何原子（e/h⁺/h⁻/τ）配上 §6 级别流动
> σ 与数量系数 δ 后的全部缠论操作**——句法（Σ）与会计（M 守恒）的积穷尽操作类型。

---

## 第 5 部分：空方版本 = τ-共轭（用户要求 3 的空方）

> 用户要求每个操作含"空方版本"。本节证明：**空方版本不是独立枚举，而是多方枚举的 τ-共轭像**——
> 穷尽多方 ⟹ τ-共轭自动穷尽空方。

### 5.1 τ-共轭算子（w ↦ τwτ⁻¹）

D∞ 的内自同构 c_τ(w) = τwτ⁻¹ 把 ε=+1 域（多方书）映到 ε=−1 域（空方书）。逐生成元：

| 多方 w | τ-共轭 c_τ(w)=τwτ⁻¹ | 含义 | 依据 |
|--------|---------------------|------|------|
| h（多方角向推进） | **h⁻¹**（空方角向推进） | 做空=做多的角向镜像 | **T57**（τhτ⁻¹=h⁻¹） |
| σ=h²³（多方升级） | **σ⁻¹=h⁻²³**（空方"升级"方向相反） | 空方趋势=多方趋势镜像 | c_τ(h²³)=(h⁻¹)²³=σ⁻¹ |
| τ（翻转） | **τ**（翻转的共轭是自身） | 翻转算子手性对称 | τττ⁻¹=τ |
| e（hold） | **e** | hold 手性对称 | trivial |

### 5.2 双投影的空方版本

τ-共轭翻转 π_op 的 direction（+↔−），保持 π_acc 的 units 结构（守恒律 σ-不变于手性）：

| 多方操作 | 空方版本（τ-共轭） | π_op 空方 | π_acc 空方 |
|---------|-------------------|-----------|-----------|
| F 建多（0→+1, +N 流入） | **F 建空**（0→−1, +N 流入） | direction 镜像 | units 同构（T48 σ-不变于 ε） |
| C 翻多→空（+1→−1） | **C 翻空→多**（−1→+1） | 自对偶（τ 共轭自身） | M=N（T40 手性对称） |
| E 减多下沉+开空（穿 ε=−1） | **E 减空下沉+开多**（穿 ε=+1） | 镜像 | T34 同构 |
| D 平空升回多 | **D 平多升回空** | 镜像 | T34 同构 |

### 5.3 NR-3：两书 τ-共轭联系但角向不连通

> **关键（orbit §NR-3）**：多方书（ε=+1）与空方书（ε=−1）由 **τ-共轭联系（镜像，T57）**，但在
> **纯角向作用下不连通**（D₂₃ 在 ℤ/23×{±1} 上 2 轨道）。**唯一连通通道 = φ=0 的手性缝**（τ_seam，
> 莫比乌斯，NR-3/T51）。

**对穷尽性的含义**：

> **推论 conj-exh（空方穷尽 = 多方 τ-共轭，L0）**：穷尽多方操作路线（Σ\* 在 ε=+1 域）⟹ τ-共轭
> c_τ 自动穷尽空方路线（ε=−1 域）。**空方不增加新字母**——它是多方字母表 Σ 在 ε=−1 上的镜像副本
> （h⁺↦空方 h⁻向、τ↦τ）。两书加起来 = Σ\* × {±1}，由 Σ（多方穷尽）+ c_τ（共轭）穷尽。**多空对称
> （T24）的 word 形式 = c_τ 是 Σ\* 的对合自同构**：镜像（共轭）≠ 连通（同轨道），故两书各自完整但
> 仅 φ=0 缝桥接。

`★ Insight`：用户问"还有空方版本"——答案是空方版本**已被多方枚举穷尽**（通过 τ-共轭）。这是 D∞ 的
内自同构结构白送的：不需要为空方重新建字母表，c_τ 把多方表整体镜像过去。这正是"做空=做多镜像"
（T57）在穷尽论上的红利。

---

## 第 6 部分：递归结构的穷尽 = σ-等变复制（用户要求 4）

> 用户洞察：每个级别独立运行 h/τ 序列，级别间耦合只通过 units 转移（1/λ）；递归穷尽性 = σ-等变性
> （T23/T59：每级别复制同样结构），保证只需穷尽一个级别。本节严格化。

### 6.1 级别流动 = 数量增量向量的相邻反号配对

多级别状态 = 向量 (φ_r, ε_r, q_r)_{r=0}^{R−1}。单 bar 的完整操作 = 每级别一个原子 (g_0,…,g_{R−1})
∈ Σ^R + 数量增量向量 (δ_0,…,δ_{R−1}) ∈ M=ℝ^R，受 T34 约束：

$$\sum_{r} \delta_r = \delta_{\text{ext}}\ \ (\text{外部流入}=0\ \text{除}\ F/A);\qquad
\text{层间流动} \Leftrightarrow \delta_k = -\delta_{k-1}\ (\text{相邻反号配对})$$

**σ（级别流动）的精确身份**：减仓下沉 = δ_k=−f, δ_{k−1}=+f（σ⁻¹ 方向，H₁ 闭路②）；回补升回 =
δ_{k−1}=−f, δ_k=+f（σ 方向，闭路④）。**σ 不在单级别字母表 Σ，它是 Σ^R 分量间的数量耦合**——
这兑现 §2.3 的承诺。

### 6.2 σ-等变性：只需穷尽一个级别（T23/T59）

> **定理 rec-exh（递归穷尽 = σ-等变复制，L0）**：所有级别的字母表**结构同构**——σ Σ_r σ⁻¹ = Σ_{r+1}
> （T59：σWσ⁻¹=W，生成算子与 σ 对易）。故**只需穷尽一个级别的 Σ（4 字母），其余级别 σ-等变自动
> 复制**。完整操作空间 = **Σ\* 的 σ-塔副本**，由单级别 Σ + σ-等变穷尽。

证明（引用 recursive_fugue §4.3）：
1. σ-等变 = 嵌套自相似：T59（σWσ⁻¹=W）⟹ 同一原子 g 在每级别复制（"操作下沉一级别"规则 σ-不变，
   A1）= T23 的"voice 自相似 spawn 子 voice"。
2. 级别间唯一耦合 = f=1/λ 数量配额（T18/542，**1 维 H¹ 系数**）——这是用户说的"units 转移 1/λ"。
3. 故递归不爬升上同调度数（cd_ℚ=1，无 H²，orbit §4B.5）——递归是 **σ-塔上的水平自相似**（同一
   字母表复制），不是垂直新结构。**字母表不随级别增加**。

### 6.3 σ-塔的边界（NR-5）

σ-等变复制在**有界阶梯**有边界（orbit §NR-5）：
- 顶层 r=R−1=MAX_LADDER−1=10：σ 无像（升级到 MAX_LADDER 之上非法）⟹ 顶层声部无 D④ 升回的更高
  目标（机动闭路在顶层退化）。
- 底层 r=0=bar/a0：σ⁻¹ 无像（分辨率不可约 T59，memory `1min_resolution_irreducible`）⟹ 底层
  无 E① 下沉的更低目标。

> **递归穷尽的精确陈述**：σ-等变复制在 r∈[1, R−2] 内层完整（每级别 Σ 副本 + 上下流动）；顶/底层
> 字母表退化（缺一个流动方向）。**这是 σ-塔半无限（抽象）vs 有界阶梯（工程 MAX_LADDER=11）的切口**
> （orbit §1.2）——递归"无限嵌套"是抽象 σ-塔，工程截断为 11 层有限副本。

> **⚠ §6 的有效域边界（诚实声明，引出 §6B）**：本部分把级别间耦合简化为"每级别独立跑 Σ + 1/λ
> units 流动"——**这只覆盖级别间关系的 H¹-operate 轴一维**。级别间关系实际不止 units 转移（用户
> 追加洞察），σ-等变**不足以**描述全部。完整分析见 §6B。

---

## 第 6B 部分：五种级别间耦合关系的穷尽——σ-等变不足，级别间关系沿三轴分布（用户追加要求）

### 6B.1 用户洞察：级别间关系不止 units 转移

§6 把级别间耦合简化为"每级别独立跑 Σ + 1/λ units 流动"。**这是有效域膨胀**（诚实修正，
no-patch）：它只覆盖级别间关系的**一个轴（H¹-operate）**。用户正确指出至少 5 种级别间关系：

1. **仓位流动**（units 1/3 转移）
2. **信号确认**（向心回溯 T49，次级别确认高级别）
3. **走势包含**（高级别包含 ≥3 段次级别走势）
4. **中枢构成**（次级别走势重叠构成高级别中枢）
5. **区间套**（多级别信号嵌套收敛定位）

> **本节裁决**：σ-等变性**不足以**描述所有级别间关系。它是级别间关系的 **H¹-operate 轴的复制律**，
> 是五种关系中**仅一种（关系 1）的完整捕获**。级别间关系沿 orbit §6 的**三轴（H⁰/groupoid/H¹）
> 分布**，σ-等变只覆盖 H¹ 轴。

### 6B.2 五种关系的三轴归属 + D∞ 捕获判定

**核心结构发现：5 种级别间关系恰好分布在三轴上**（orbit §6 三轴在级别间的投影）：

| # | 级别间关系 | 三轴归属 | D∞ 捕获? | 机制 / 超出部分 |
|---|-----------|---------|---------|----------------|
| 1 | **仓位流动**（units 1/3） | **H¹**（operate 流动） | ✅ 完全 | σ-operate + M 系数 f=1/λ（§6）；流动律 σ-不变（T18/542）= σ-等变正好捕获 |
| 2 | **信号确认**（T49 向心） | **groupoid**（observe 依赖） | ⚠️ 部分 | σ⁻¹-**observe**（NR-4 模式维跨级别 + T49）；σ-等变给"确认箭头自相似"，但箭头的 observe 内容是 groupoid 轴，**正交于 σ-operate** |
| 3 | **走势包含**（≥3 次级别） | **H⁰**（形态学构成） | ⚠️ 部分 | σ=h²³ 捕获**深度嵌套比 23:1**（定义，非等变）；但"分成 ≥3 段走势"的段数组合是 φ 圈内形态学（T22/走势分解定理二），**平移群不区分** |
| 4 | **中枢构成**（重叠涌现） | **H⁰**（形态学构成） | ❌ 新 | 重叠 = 区间**交集 ∩**（格运算）；中枢 = k−1 走势重叠**涌现**新对象；**涌现 ≠ σ-等变复制**；D∞=⟨平移,反射⟩无交集运算 |
| 5 | **区间套**（嵌套收敛定位） | **groupoid**（observe 依赖） | ❌ 新 | σ-塔的**逆极限** lim←(信号_k)；多级别 observe 合取收敛；**范畴极限超出单 σ 作用** |

**三轴分布的对称结构**：
- **H¹ 轴级别间**（operate 流动）：关系 1（仓位流动）
- **groupoid 轴级别间**（observe 依赖）：关系 2（信号确认）+ 关系 5（区间套）
- **H⁰ 轴级别间**（形态学构成）：关系 3（走势包含）+ 关系 4（中枢构成）

这与 orbit §6"同一级别内三轴正交（H⁰ 不变量 / groupoid 转移 / H¹ 扬弃）"**同构地延伸到级别间**——
**级别间关系也三轴正交**。这是本节核心结构发现。

### 6B.3 σ-等变只捕获 H¹ 轴（定理）

> **定理 INTER-axis（级别间关系沿三轴分布，σ-等变 ⊊ 级别间耦合，L0）**：
> σ-等变性（T59: σWσ⁻¹=W）= 级别间关系的 **H¹-operate 投影的自相似复制律**。它：
> - **完全捕获**关系 1（units 流动 σ-不变，f=1/λ 每级别相同）；
> - **不捕获**关系 2/5（groupoid-observe 级别间依赖）——这是 NR-4 模式维（observe⊥operate）的级别间
>   版本，σ-等变是 operate 侧，给不出 observe 箭头内容；
> - **不捕获**关系 3/4（H⁰ 形态学级别间构成）——σ=h²³ 给深度比，但段数组合（关系 3）与重叠涌现
>   （关系 4）是格/序结构 + 涌现，非群作用。
>
> **故 σ-等变是级别间耦合的必要不充分条件**：它描述"每级别有同构结构"（H¹ 复制），但描述不了
> "级别如何 observe 彼此"（groupoid）与"级别如何构成彼此"（H⁰）。

证明（三轴分别论证）：

**(H¹ 轴，σ-等变充分)**：关系 1 流动律 δ_k=−δ_{k−1}, f=1/λ 是 σ-不变常数（T18/542
`prove_theta_sigma_invariant`）⟹ σWσ⁻¹=W 对流动算子成立 ⟹ σ-等变完整捕获。✓

**(groupoid 轴，σ-等变不充分)**：关系 2 信号确认 = k 级别 fire 依赖 k−1 级别 settle（T49 向心读已
沉淀 type1）= **observe 方向的级别间箭头**（k 读 k−1）。σ-等变保证"每级别都有读下级的箭头"（箭头集
σ-等变），但**箭头本身是 groupoid 的 observe 态射**（NR-4），合法性由 P1'（读过去合法/读未来非法）
判定，**不由 σ-operate 复制律决定**。关系 5 区间套 = 多级别 observe 合取收敛 = lim←——**逆极限是
塔上的范畴极限**（σ 给塔的边 k→k+1，极限给塔的收敛点），不是单个 σ 作用。⟹ σ-等变给塔，
groupoid+极限给 observe 收敛，二者正交。✗

**(H⁰ 轴，σ-等变不充分)**：关系 4 中枢 = k−1 走势的**重叠区间**（中枢定义 T7-T9）。重叠 = 区间交集
∩（**格运算**）；中枢是 k−1 重叠**涌现**的 k 级别新对象。**涌现 ≠ 等变**：σ-等变把 k 的结构复制到
k+1（同构映射），中枢是 k−1 **产生** k（新对象诞生 emergence，非复制 equivariance）。D∞=⟨h,τ⟩
（平移+反射）群运算里**没有交集 ∩**——中枢构成超出群作用。关系 3 走势包含的"≥3 段"同理是形态学
组合（走势分解定理二，知识库 328），平移群 ℤ/23 只看相位平移不看"分几段"。⟹ H⁰ 形态学构成是
格/序 + 涌现，σ-等变不捕获。✗

### 6B.4 groupoid 轴级别间：σ-observe 信号确认 + 区间套逆极限（关系 2/5）

**关系 2（信号确认）的精确代数**：多级别向量上，级别间有**两种分量依赖**，对称分布：

| 依赖类型 | 内容 | 轴 | 守恒/约束 |
|---------|------|----|----------|
| **operate 依赖** | units 流动 δ_k=−δ_{k−1} | H¹ | T34 守恒（§6） |
| **observe 依赖** | 确认 fire_k ⟸ settle_{k−1}（向心读） | **groupoid** | T49 时序 Δt∝λ^{k-1}（NR-4 读过去合法） |

§6 只有 operate 依赖（units），**遗漏了 observe 依赖（确认）**——本节补全。confirm 机制 = σ⁻¹-observe
（读次级别已 settle 的 type1 确认本级别），是字母 h⁻（向心观测）的**跨级别版**。它决定 operate 的
**时机**（何时减仓/回补），但本身是 observe 非 operate（NR-4）。

**关系 5（区间套）= σ-塔的逆极限**：区间套定位（第27课定位型，memory
`project_bsp_sublevel_settled_gate`）= 多级别买卖点嵌套收敛到精确点 = 取所有级别 observe 信号的合取：
$$\text{定位点} = \varprojlim_{k} (\text{信号}_k) = \bigcap_{k} \{\text{级别 } k \text{ 的 BSP 区间}\}$$
这是 σ-塔（σ 链）上的**逆极限**（范畴极限）——σ 给塔的边（k→k+1），"取交收敛"是塔上的极限运算。
**σ-等变给塔的自相似态射，逆极限给塔的收敛点对象——二者不同**（等变是态射性质，极限是泛性质对象）。

### 6B.5 H⁰ 轴级别间：走势包含（σ=h²³ 深度）+ 中枢构成（涌现，非群）（关系 3/4）

**关系 3（走势包含）**：k 级别走势 = σ=h²³ = 23 个角向环（h），即"k 一步 = k−1 二十三步"，被 helix
编码 n=23r+φ 捕获——这是 **σ=h²³ 的定义**（T56），不是 σ-等变。但**"≥3 段次级别走势类型"**（走势
分解定理二，知识库 328）的段数组合是 **φ 圈内形态学**（哪些 φ 点是次级别走势完美点，由 T22/背驰
决定）——D∞ 作为平移群把 φ 当 ℤ/23 的平移，**不区分"23 环分成 3 段还是 5 段"**。故 σ=h²³ 捕获
**深度嵌套比 23:1**，段数组合属 H⁰ 形态学（T1-T16），群结构不捕获。

**关系 4（中枢构成）= 涌现，群结构外**：中枢（T7-T9）= 次级别走势的**重叠区间**。这是级别间最深的
gap：
- **重叠 = 区间交集 ∩**：D∞=⟨平移 h, 反射 τ⟩ 群运算里没有交集——交集是**格/序运算**，非群运算。
- **涌现 ≠ 等变**：σ-等变复制 k 的结构到 k+1（同构映射）；中枢是 k−1 走势重叠**产生** k 级别新对象
  （memory `project_signal_layer_duality`："中枢=域 vs 分型=点范畴分离"——中枢是**域**/区间，不是
  群作用的**点**）。新对象诞生（emergence）≠ 已有结构复制（equivariance）。
- 故中枢构成是 **H⁰ 形态学的级别间格结构**，**完全在 D∞ 群作用之外**——5 种关系中 D∞ 捕获最少的。

### 6B.6 结论：级别间完整耦合 = 三轴投影，σ-等变是 H¹ 轴一维

> **对用户两问的直接回答**：
> 1. **哪些被 D∞ 捕获，哪些新？** — 关系 1（仓位流动）完全捕获（σ-operate + M）；关系 2（信号确认）
>    部分（σ⁻¹-observe，但 observe 内容是 groupoid 新维）；关系 3（走势包含）部分（σ=h²³ 深度比，
>    段数组合是 H⁰ 形态学）；**关系 4（中枢构成）全新**（重叠/涌现，群作用外）；**关系 5（区间套）
>    全新**（σ-塔逆极限，范畴极限超群）。
> 2. **σ-等变是否足以描述所有级别间关系？** — **否**。σ-等变是级别间关系的 **H¹-operate 轴自相似
>    复制律**，必要不充分。完整级别间耦合 = **三轴各自的级别间投影**：
>    - **H⁰ 轴**（形态学构成）：走势包含（σ=h²³ 深度）+ 中枢构成（区间格涌现，**非群**）
>    - **groupoid 轴**（observe 依赖）：信号确认（σ⁻¹-observe，T49）+ 区间套（逆极限）
>    - **H¹ 轴**（operate 流动）：仓位流动（σ-等变，f=1/λ）
>
> **是否还有更多于 5 种？** — 在**结构层（三轴）穷尽**：级别间关系必属 H⁰（构成对象）/ groupoid
> （observe 依赖）/ H¹（operate 流动）之一（orbit §6 三轴正交在级别间的投影）。但**H⁰ 轴的具体形态学
> 构成关系可细分更多**（如走势级别延续定理 305、跨级别背驰力度比较）——这些都是 H⁰ 形态学
> （T1-T16+305），**正是 D∞ 群结构穷尽不了的部分**（这恰恰再证 σ-等变不足）。5 种覆盖三轴主干；
> H⁰ 轴完整形态学列举属缠论形态学公理（A₀ 递归 + 走势分解/级别延续定理），不是 word 句法能穷尽。

### 6B.7 对穷尽性声明的修正（诚实，no-patch）

§7 的 ROUTE-exh（操作路线 = Σ\* × {±1} × σ-塔 × M）穷尽的是 **operate 路线 + units 流动**（H¹ 轴 +
σ-operate）。它**不穷尽**：
- **groupoid 轴级别间**（信号确认 / 区间套）——这是 operate 的 **observe 输入**（决定时机/定位），
  本身不是 operate 路线（NR-4 observe⊥operate）；
- **H⁰ 轴级别间**（走势包含 / 中枢构成）——这是操作的**对象**（在哪个级别的什么走势/中枢上操作），
  是 H⁰ 形态学，不是路线。

> **修正后的穷尽性精确陈述**：**operate 路线**（"怎么操作"）被 Σ\*（4 字母）穷尽（ROUTE-exh 成立于
> operate 域）；但**级别间完整耦合**（"级别如何彼此关联"）需要**三轴**——σ-等变只是 H¹ 轴的级别复制
> 律。**word 句法穷尽操作路线，但穷尽不了级别间的形态学构成（H⁰，含中枢涌现）与 observe 依赖
> （groupoid，含区间套逆极限）**——这两者是 D∞ 群结构有效域之外（orbit §6 已确立三轴正交，本节
> 发现三轴在级别间同样正交分布）。

---

## 第 7 部分：完整操作类型表 + 句法穷尽性总证（用户要求 5）

> 最终产出：一张表列出**每 bar 每级别所有可能操作**（含空方版本 + 双投影），并证明穷尽。

### 7.1 完整操作类型表（每 bar 每级别，含空方 + 双投影）

每 bar，每个 active 级别 r 选一个原子操作。**全部操作类型**（多方域 ε=+1，空方域由 §5 τ-共轭镜像）：

| 操作 | 字母/word | 模式 | π_op（direction，多方） | π_op（空方 τ-共轭） | π_acc（units） | 级别流动 | 位置约束 | 同调层 |
|------|----------|------|------------------------|---------------------|----------------|---------|---------|--------|
| **hold** | e | op | +1→+1 | −1→−1 | 0 | — | 恒 | H⁰（截面） |
| **持仓推进** | h⁺ | op | +1→+1（走过次级别BSP） | −1→−1（空方 h⁻向） | 0 | — | a≥0 | H⁰（沿母线） |
| **向心观测** | h⁻ | obs | 无（读过去踪迹） | 无（镜像读） | 0（只读） | — | φ=0 母线 | groupoid（NR-4） |
| **F 建仓** | β⁺ | op | 0→+1@k=K | 0→−1（建空） | +N 外部流入 | — | 最高级别买点 | H⁰ 锚（T48） |
| **C 翻转** | τ | op | +1→−1 | −1→+1 | 0（M=N） | — | **φ=0**（T51） | τ（spiral §2.7） |
| **E① 减仓下沉** | σ⁻¹·(δ) | op | +1→+1@k | −1→−1 | −f | **↓ 流出 k** | 次级别卖点 | H₁ 闭路① |
| **E② 开空** | (穿缝)·(δ) | op | 0→−1@k−1（**穿 ε=−1**） | 0→+1（穿 ε=+1） | +f | ↓ 流入 k−1 | k−1 起点 | H₁ 闭路②（莫比乌斯） |
| **D③ 平空** | (δ) | op | −1→0@k−1 | +1→0 | −f | **↑ 流出 k−1** | 次级别买点（向心定时 h⁻） | H₁ 闭路③ |
| **D④ 回补升回** | σ·(δ) | op | +1→+1@k（回 ε=+1） | −1→−1 | +f | ↑ 流入 k | 成本下移 | H₁ 闭路④ |
| **A 强平** | 边界 | op | →0（清仓） | →0 | −q 外部流出 | — | NR-5 越界/否定线 | 边界（非群，T41⟹不 fire） |

**表的读法**：
- **每 bar 每级别的 operate 选择** ∈ {hold, 持仓推进, F, C, E①, E②, D③, D④, A}（observe 选择 =
  向心观测 h⁻，与 operate 并行）。
- **E①②③④是一条不可分 H₁ 闭路**（穿 ε=−1，spiral §2.5）——表中分 4 行是为标注 (φ,r,ε) 逐步，
  实际是一个 1-cycle（§4.3）。
- **空方列 = 多方列的 τ-共轭镜像**（§5），不是独立操作。
- **σ（级别流动）出现在 E①②/D③④ 的"级别流动"列**（相邻级别 δ 反号配对，§6.1），不是单级别原子。

### 7.2 句法穷尽性总证（L0）

> **定理 ROUTE-exh（D∞ word 空间穷尽所有操作路线，L0）**：
> 所有操作路线被以下有限句法数据完全穷尽生成：
> $$\text{操作路线全体}\;=\;\Big(\underbrace{\Sigma}_{\text{4 字母, §2}}\Big)^{*}
> \;\times\;\underbrace{\{\pm 1\}}_{\text{手性, τ-共轭 §5}}
> \;\times\;\underbrace{\sigma\text{-塔}_{r=0}^{R-1}}_{\text{σ-等变复制 §6}}
> \;\times\;\underbrace{M=\mathbb{R}\ (\text{系数 } f=1/\lambda)}_{\text{数量, T18}}$$
> 受 T34/T40/T48 守恒约束（§4）。**穷尽机制四件套**：

| 穷尽件 | 内容 | 保证 |
|--------|------|------|
| **(i) 字母表有限穷尽** | Σ={e,h⁺,h⁻,τ}（4 字母，§2 Σ-exh） | D∞ 2 生成元 × 2 模式 − 2 非法 = 4 |
| **(ii) 自由幺半群生成** | 所有单级别路线 = Σ\*（§2.4） | 自由幺半群泛性质（有限字母→所有串） |
| **(iii) 空方 = τ-共轭** | 空方域 = 多方 Σ\* 的 c_τ 镜像（§5 conj-exh） | T57 内自同构，不增字母 |
| **(iv) 递归 = σ-等变** | 多级别 = 单级别 Σ 的 σ-塔副本（§6 rec-exh） | T59 σ-等变，不增字母 |
| **(v) 会计 = M 守恒** | 双投影合法组合恰 8 个（§4 dual-exh） | T34/T40/T48 筛选 |

> **穷尽性**：操作路线虽**可数无限**（任意长串），但被 **4 字母 Σ + 共轭 c_τ + σ-等变 + M 守恒**
> **有限呈现**——与 541 号"有限呈现穷尽 ∧ 群元素无限"、orbit §5.2"有限描述 + 可判定成员"**同构**。
> **可判定**：任一 word 是否为合法操作路线，由逐字母检验 P0/P1/P2/P3（orbit §3.2，automatic group
> 可计算）+ 守恒检验（T34）判定。故"一次性穷尽所有操作路线"= **穷尽 4 字母字母表 Σ + 4 条复合规则**，
> 可计算完成（非有限完成，路线无限）。
>
> **⚠ 有效域（§6B INTER-axis，强制）**：ROUTE-exh 穷尽的是 **operate 路线**（"怎么操作"，H¹ 轴 +
> σ-operate units 流动）。它**不穷尽级别间的完整耦合**——级别间关系三轴分布（§6B）：σ-等变（本定理
> 的 σ-塔项）仅捕获 H¹ 轴（仓位流动）；**groupoid 轴**（信号确认 σ⁻¹-observe / 区间套逆极限）是
> operate 的 observe 输入（NR-4 正交，非路线本身）；**H⁰ 轴**（走势包含 σ=h²³ / 中枢构成区间格涌现）
> 是操作的对象（形态学，**中枢涌现完全在 D∞ 群作用外**）。**故"穷尽操作路线"≠"穷尽级别间关系"**——
> 前者句法可穷尽，后者需三轴（H⁰ 形态学 + groupoid observe + H¹ operate），其中中枢构成（涌现）与
> 区间套（逆极限）超出群结构。

### 7.3 与用户原方法（长度 ≤2 枚举）的关系

用户提议"枚举长度 ≤2 的 word"。本文档定位：

- **长度 ≤2 枚举（§3）= 第 1 层（群元素）的短截断**——给出 8 个净效果分类，暴露共轭合并（τh=h⁻¹τ）
  与群元素存在 ≠ 路线合法（hτ）。
- **完整穷尽（§7.2）= 第 0 层（path）的字母表论证**——不依赖"长度 ≤2"截断，而是 Σ\* 由 Σ 穷尽生成。
- **二者关系**：长度 ≤2 ⊂ Σ\*（截断 ⊂ 全体）。长度 ≤2 足以暴露**所有约化规则**（τ²=e、共轭）与
  **所有原子类型**（e/h/τ 及其组合），但**覆盖不到**级别流动（σ=h²³ 长度 23）与数量（连续 M）——
  这是用户方法的有效域边界，由 §6（σ）/§4（M）补全。

---

## 第 8 部分：句法侧 ⟂ 语义侧穷尽的对偶（与三文档对接）

### 8.1 四文档的穷尽性版图

| 文档 | 侧 | 对象 | 穷尽形式 |
|------|-----|------|---------|
| `orbit_enumeration` | **语义（不变量+转移+扬弃）** | H⁰（58）/ groupoid（NR-1…7）/ H¹（ℝ） | H⁰⊕H¹ 两层闭合（cd_ℚ=1） |
| `spiral_reinterpretation` | **语义（原文指称）** | 操作=4 正交代数对象，短差=H₁ 1-cycle | 核心仓(H⁰)+机动仓(H₁) |
| `recursive_fugue` | **语义（H¹ σ-塔实现）** | 赋格=σ-等变 H₁ 族 | 两层闭合 + 无第三层 |
| **本文档** | **句法（word/path）** | Σ\*（路线）/ D∞（净效果）/ H₁（同调） | **Σ\* 由 4 字母 Σ 穷尽生成** |

### 8.2 句法→语义的函子链（两侧穷尽一致）

$$
\underbrace{\Sigma^*}_{\substack{\text{句法穷尽}\\ \text{4 字母（§2）}}}
\xrightarrow{\ \pi_{\text{red}}\ }
\underbrace{D_\infty=\{h^a\tau^b\}}_{\substack{\text{群元素}\\ \text{Britton 正规形}}}
\xrightarrow{\ H_*\ }
\underbrace{\begin{array}{c}H^0=58\ (\text{不变量})\\ H_1=H^1=\mathbb{R}\ (\text{1-cycle/扬弃})\end{array}}_{\substack{\text{语义穷尽}\\ \text{两层闭合（cd}_\mathbb{Q}=1)}}
$$

> **对偶定理 SYN-SEM（句法穷尽 ⟺ 语义穷尽，L0）**：
> - **句法侧**（本文档）：操作路线 = Σ\*，由 4 字母 Σ 穷尽生成（ROUTE-exh）。
> - **语义侧**（三文档）：操作的不变内容 = H⁰（58）⊕ H¹（ℝ），cd_ℚ=1 两层闭合。
> - **一致性**：约化 π_red 把句法（Σ\*）打到群元素（D∞）；取闭 path 同调 H_* 把群元素打到语义
>   （H⁰/H₁）。**句法的"无限路线由有限字母穷尽" = 语义的"无限群元素由两层上同调闭合"**——同一
>   穷尽性的两个投影。Σ 的 4 字母 ↔ 操作的 4 正交代数对象（h⁺=角向 h / σ=径向 / τ=手性 / h⁻=观测
>   读，对接 spiral §2.7 + NR-4 模式维）。

### 8.3 本文档对"穷尽性版图"的补全

- **不增 H⁰**（58 不变）、**不增 H¹ 维**（仍 1 维）、**不产生 H²**（cd_ℚ=1）。
- **新增句法侧穷尽**：把语义侧的"两层闭合"翻译为句法侧的"4 字母字母表 Σ 穷尽生成 Σ\*"——这是比
  同调闭合更初等的穷尽机制（自由幺半群泛性质），且**直接可计算判定**（逐字母 P 检验）。
- **回答用户"一次性穷尽所有操作路线"**：**有**——穷尽 4 字母字母表 Σ（§2）+ 4 条复合规则
  （自由幺半群 §2.4 / τ-共轭 §5 / σ-等变 §6 / M 守恒 §4）。无限路线被有限句法呈现穷尽。

---

## 第 9 部分：结果包六要素

### 1. 结论

D∞=⟨h,τ|τ²=e,τhτ⁻¹=h⁻¹⟩ 的 **word 空间（句法侧）穷尽所有操作路线**，机制为**有限字母表 Σ 穷尽
生成自由幺半群 Σ\***：

- **三层区分（§1）**：操作路线 = **path（Σ\* 串，句法）**；word（D∞ 群元素 hᵃτᵇ）= 路线**净效果**
  （一对多，载体歧义）；操作的**不变内容** = **1-cycle（H₁，闭 path 同调类）**。用户命题"操作路线=word"
  在 path 层正确，但"四步循环是一个 word"不准确（它是 1-cycle，非 0 维群元素）。
- **原子字母表穷尽（§2 Σ-exh）**：单 bar 单级别合法原子动作**恰 4 个** Σ={e, h⁺（持仓推进）,
  h⁻（向心观测）, τ（手性翻转@φ=0）}——D∞ 2 生成元 × 2 模式（NR-4 延异）− 2 非法（时间倒流 /
  前向读未来）。σ=h²³ 非原子（级别流动，§6）。
- **长度 ≤2 枚举（§3）**：去重后 8 个群元素 {e,h,h⁻¹,h²,h⁻²,τ,hτ,h⁻¹τ}；共轭合并 τh=h⁻¹τ、
  τh⁻¹=hτ（多空镜像 T57 的 word 形式）；**群元素存在 ≠ 路线合法**（hτ 的 τ 须落 φ=0）。
- **双投影合法组合（§4 dual-exh）**：操作投影（direction）× 会计投影（units+流动）的**合法组合恰
  8 个** = hold / F / C / E①② / D③④ / A，其余被 T34（守恒）/ T40（翻转 M=N）/ 方向-流动一致性 /
  NR-7（整数）穷尽否决。
- **空方 = τ-共轭（§5 conj-exh）**：空方版本 = 多方 Σ\* 的内自同构 c_τ 镜像（h↦h⁻¹ 即 T57，
  σ↦σ⁻¹，τ↦τ），不增字母；两书 τ-共轭联系但角向不连通（NR-3，仅 φ=0 缝桥接）。
- **递归 = σ-等变复制（§6 rec-exh）**：σΣ_rσ⁻¹=Σ_{r+1}（T59），只需穷尽一个级别的 Σ，σ-塔自动
  复制；级别耦合 = 数量增量相邻反号配对（f=1/λ，T18）；递归不爬升上同调度数（cd_ℚ=1，无 H²）。
- **【用户追加】级别间关系沿三轴分布，σ-等变不足（§6B INTER-axis）**：5 种级别间关系恰好分布在
  orbit §6 三轴上——**H⁰ 轴**（走势包含 σ=h²³ + 中枢构成涌现）/ **groupoid 轴**（信号确认
  σ⁻¹-observe + 区间套逆极限）/ **H¹ 轴**（仓位流动 σ-等变 f=1/λ）。**σ-等变只捕获 H¹ 轴一维**
  （必要不充分）：关系 1 完全捕获；关系 2/3 部分；**关系 4（中枢=区间交集∩涌现）与关系 5（区间套=
  σ-塔逆极限）全新，完全在 D∞ 群作用外**（重叠非群运算、涌现非等变、逆极限非单 σ 作用）。**word
  句法穷尽 operate 路线，但级别间形态学构成（H⁰）与 observe 依赖（groupoid）超出群结构**——这再次
  印证 orbit §6 三轴正交（本文档发现三轴在级别间同样正交分布）。
- **总穷尽（§7 ROUTE-exh）**：操作路线全体 = Σ\* × {±1}（τ-共轭）× σ-塔 × M（守恒）——可数无限
  但被 **4 字母 + 4 复合规则有限呈现**，可判定，与 541 号"有限呈现穷尽 ∧ 元素无限"同构。
- **句法 ⟺ 语义对偶（§8 SYN-SEM）**：句法侧（Σ\* 由 4 字母穷尽）= 语义侧（H⁰⊕H¹ 两层闭合）的
  函子投影；不增 H⁰（58）/H¹（1 维）/H²（0），新增的是更初等的句法穷尽机制。

### 2. 定义依据

- 群 D∞ + σ=h²³ + 三坐标 (φ,r,ε)：orbit §1.1 / spiral §1.1 / necessity §8.1。
- Britton 正规形 hᵃτᵇ + 共轭 τhτ⁻¹=h⁻¹：D∞=ℤ⋊ℤ/2 标准结果。
- 字母表 Σ 的 4 原子：D∞ 生成集 {h,τ} + NR-4 模式维（orbit §3.1/escalation 延异）+ P1/P2 约束
  （orbit §3.2）。
- 双投影合法组合：T34（Σunits=N_base，necessity §T34）+ T40（翻转 M=N）+ T48（units Casimir）+
  F/C/D/E/A 代数身份（spiral §2.7）。
- 空方 τ-共轭：T57（τhτ⁻¹=h⁻¹）+ NR-3（两书轨道分离）。
- σ-等变递归：T23/T59（recursive_fugue §4）+ T18/542（f=1/λ）。

### 3. 边界条件（结论翻转条件）

- **若把载体取为裸群元素（非 path）**：§3.4 的"群元素存在 ≠ 路线合法"失效，合法性误判（hτ 被当
  恒合法）——回退到 orbit §3.1 已否证的"裸群元素载体"。
- **若 NR-4 模式维被否证**（第30课延异被 source-auditor 推翻，缠师指共时在场）：Σ 退化为
  {e,h⁺,τ}（3 字母，去 observe 维 h⁻），向心观测并入操作，穷尽性表重构。
- **若 σ^N=e 成为真群关系**（非 T44 逐 voice 截断）：D∞ 约化为有限 D_N，Σ\* 有限完成（非仅可判定），
  σ-塔闭合。当前 T44 只逐 voice 截断不约化。
- **若观测模含整数股数（ℤ-模）**：§4 的合法组合残 NR-7 障碍（半股不可短差，H²(D∞,ℤ)=(ℤ/2)²）；
  实值近似下 8 组合全合法。
- **若标的无做空载体**（A 股现货）：E②/D③ 穿 ε=−1 退化为可缩平凡环路（spiral §2.5.D），机动闭路
  alpha 受限；认沽权证（015:976）补足载体提升为非平凡 ℝ_− 闭路。
- **激活层**：本文档穷尽的是**合法路线的可能性空间**（L0）；哪些路线此刻**实际发声**是 regime-gated
  L2（recursive_fugue §2.2，单边上扬 H¹→0）——混淆即有效域膨胀（231号）。

### 4. 下游推论

- **引擎载体修正**：操作的载体必须是 **(path, 模式) 二元组**（§3.4），不是裸群元素或裸 enum——
  合法性逐字母检验（P0/P1/P2/P3 + T34）。这与 spiral §3.1"层结构 vs forest"一致。
- **字母表 = 操作 enum 的封顶**：单级别 operate enum 恰 {hold, 持仓推进, F, C, E①, E②, D③, D④, A}
  + observe {h⁻}——**无第 10 个**（Σ-exh + dual-exh）。引擎若出现不在此表的操作类型，是 bug 或
  越界（NR-5）。
- **空方实现 = 多方代码的 τ-共轭**：不需为空方写独立逻辑——c_τ 镜像（h⁺↔空方推进、direction 翻号、
  units 守恒结构不变）。对接 memory `project_bidir_s1s4_implementation`（双向 S1-S4 = c_τ 实装）。
- **递归不增字母**：σ-塔每级别复用同一 Σ（rec-exh）——引擎递归层 BSP 消费应是单级别逻辑的 σ-等变
  复制，不是每级别新逻辑。对接 memory `project_signal_layer_duality`（信号层 segment/move 双重性）。
- **prove 守卫对应**：字母 h⁺/h⁻=N6 时序（compress≤confirm≤bar）；τ=T14/T51（`prove_t14_root_flip`
  @φ=0）；E①②/D③④=N7（E 自层）+ T34/N8（守恒）；σ-等变配额=`prove_theta_sigma_invariant`（f=1/λ）。
- **语法记录候选**（交 genealogist/编排者）：(a)「操作路线 = path（Σ\* 句法）≠ word（D∞ 群元素净
  效果）≠ 1-cycle（H₁ 不变内容）：三层区分」；(b)「单 bar 单级别原子字母表 Σ={e,h⁺,h⁻,τ} 穷尽 =
  D∞ 2 生成元 × NR-4 2 模式 − 2 非法」；(c)「空方 = τ-共轭镜像，不增字母（穷尽多方⟹穷尽空方）」；
  (d)「句法穷尽（Σ\* 由 4 字母生成）⟺ 语义穷尽（H⁰⊕H¹ 两层闭合）的函子对偶」。

### 5. 谱系引用

- **根**：[[541号]]（螺旋覆盖空间——"有限呈现穷尽 ∧ 元素无限"= 本文档"4 字母穷尽 Σ\*"的母形式）、
  [[542号]]（配额 σ-不变 f=1/λ = 级别耦合系数 = H¹ 系数）。
- **直接上游**：`orbit_enumeration_completeness.md`（H⁰/groupoid/H¹ 语义侧 + NR-1…7 + 2×2 四分 +
  automatic group 可判定性——本文档句法侧的对偶）、`spiral_physical_reinterpretation.md`（操作=4 正交
  代数对象 + 短差=H₁ 1-cycle + F/C/D/E/A 身份——§4/§7 双投影表的语义指称）、
  `recursive_fugue_necessity_proof.md`（σ-等变 H₁ 族 + 配对 −2 + 两层闭合——§6 递归穷尽的语义侧）。
- **定理依据**：A₀/A₁、T14/T18/T20/T23/T34/T40/T48/T49/T50/T51/T57/T59；NR-1…7、A1/A2、cd_ℚ=1。
- **延异 escalation**（`2026-06-15-confirm-arming-differance.md`）：§2 模式维（observe 字母 h⁻）的
  存在论依据——延异使读/写二分（NR-4），**真矛盾∧语法记录，已上浮未结算**。
- **L2 实测锚**：`project_unn_btc_spawn_throwback`（无 regime 门激活 = 踏空，§3 边界）、
  `project_bidir_s1s4_implementation`（c_τ 空方实装，§5）、`project_signal_layer_duality`（信号层
  双重性，§6 递归复制）、`project_constitutive_throughput_falsified`（激活=regime 函数，§9.3 激活层）。
- **概念分离声明（不确定性）**：未在 `.chanlun/genealogy/settled/` 检索到"path/word/1-cycle 三层区分 /
  原子字母表 Σ 穷尽 / 句法-语义对偶"的已结算条目。541/542 是最近根（不变量/配额侧），orbit/spiral/
  recursive_fugue 是语义侧（同调），本文档是**句法侧（自由幺半群/path）**——三侧（句法/群/同调）
  正交。**建议 genealogist 评估**是否结晶为候选「操作路线的句法穷尽 = 4 字母字母表 Σ 生成 Σ\*，
  与语义侧 H⁰⊕H¹ 两层闭合函子对偶」。**不上浮 `/escalate`**——纯句法侧穷尽推论，未制造新矛盾（延异
  矛盾已由既有 escalation 上浮；三层区分是精化非冲突）。

### 6. 影响声明

- **新建** `docs/operation_route_exhaustion.md`（本文档，句法侧穷尽 L0 + L2 激活锚）。
- **不改**任何 `.rs`、任何已结算定理证明、任何守恒律、上游三文档（orbit/spiral/recursive_fugue）及
  540/541/542 谱系。
- **对穷尽性版图的影响**：**H⁰=58 / H¹=1 维 / cd_ℚ=1 数学全不变**；新增**句法侧（word/path）穷尽轴**——
  操作路线由 4 字母字母表 Σ + 4 复合规则（自由幺半群 / τ-共轭 / σ-等变 / M 守恒）穷尽生成。这是
  比同调闭合更初等的穷尽机制，且直接可判定（逐字母 P 检验）。
- **认识论等级**：全文 **L0**（自由幺半群泛性质 + D∞ 群论 + 覆盖论 + 引用已对抗复核的 H⁰/H¹/cd 结果）。
  **否定性增量（正，缩小有效域）**：(a) 裁定"操作路线=word"须分 path/group/1-cycle 三层（四步循环
  是 1-cycle 非群元素）；(b) 暴露群元素存在 ≠ 路线合法（hτ，载体歧义 word 形式）；(c) 标定长度 ≤2
  枚举的有效域边界（覆盖不到 σ 级别流动 / M 数量）；(d) 确立句法 ⟂ 语义穷尽对偶（4 字母 ↔ 两层闭合）；
  (e) **【用户追加，§6B】诚实修正 §6 的"σ-等变穷尽级别间"声明（有效域膨胀）**——σ-等变仅捕获级别间
  关系的 H¹-operate 轴一维；级别间关系沿三轴分布（H⁰ 形态学构成 / groupoid observe 依赖 / H¹ operate
  流动），其中**中枢构成（区间交集∩涌现）与区间套（σ-塔逆极限）完全在 D∞ 群结构之外**——确立"级别间
  三轴正交"（同一级别内三轴正交的级别间延伸），σ-等变是必要不充分。
- **回答用户核心问**：「有没有办法一次性穷尽所有操作路线？」——**有**：穷尽 4 字母字母表
  Σ={e,h⁺,h⁻,τ}（§2）+ τ-共轭（空方 §5）+ σ-等变（递归 §6）+ M 守恒（会计 §4）。无限路线被有限
  句法呈现穷尽，可计算判定，与语义侧 H⁰⊕H¹ 两层闭合对偶一致。
