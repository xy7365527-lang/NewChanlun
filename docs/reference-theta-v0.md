# Reference Θ v0 — 缠论可执行系统的形式化补全参数（Phase 1 规格）

**来源**：codex gpt-5.5 high 编排者全权代理裁决（2026-06-25，/tmp/codex_decisions.md）。编排者"这些问题问codex就好了"委托。
**纲领**（codex 总纲）：**L0 证明"给定 Θ 后系统怎样无歧义地分类和行动"；不证明 Θ 是好 Θ，也不证明市场会奖励这个语法。**
**链**：缠论结构公理 + Θ ⟹ C_Θ（谱系615/617）⟹ π_Θ（谱系616）⟹ Rust 实装 ⟹ L2/L3 检验（可证伪）。

## 相变决策（codex 裁，编排者委托）

- **D1 greenlight = YES**（分层）：Phase 0-3 立即推进；Phase 4-7 阻塞于真实行情数据实体恢复（#20）。
- **D2 Phase 0-3 并行开**（不等 L0 全封版）：
  - Phase 0 冻结 L0 spec snapshot（共同输入）；Phase 1 = 本文 Θ v0；Phase 2 Rust bit-exact 引擎（只对已冻结 Θ v0 实装，L0 收尾只许追加 conformance case 不许静默改语义）；Phase 3 预注册回测协议（数据无关）。
  - 形式化蜂群（Parse/LevelState/Nest/Fugue/RiskProj/Chain）继续收尾；**若与 Θ v0 冲突，必须开 explicit change request，不能边写边漂**。

## 全局约定
- 所有价格量化为整数 tick；无 `tick_size` 默认 `1e-8`。[设计选择,默认值]
- 所有平局按 `(timestamp, source_index)` 升序裁决；已确认结构不可回写重分解。[设计选择,默认值]

## Θ_parse（解析）
- **K线包含**：相邻区间包含即合并；向上 `high=max,low=max`，向下 `low=min,high=min`；方向按前一对非包含 K 的严格高低变化决定，开头无方向则向前看第一个非包含对；全程无方向则只输出 open-tail。[缠论可导,62/65课；开头 tie-break 设计选择]
- **分型**：包含处理后，顶分型=中 K 高低都严格高于左右；底分型=中 K 高低都严格低于左右；等价不成立；第三根 K 收盘确认。[缠论可导,62课；严格等号处理设计选择]
- **笔**：启用**新笔**，旧笔禁用。顶/底分型不共用 K；两极值 K 间按原始 K 计数排除两端至少 3 根；同类连续分型，顶保留更高、底保留更低、等价保留更早。[缠论可导,77/81课；等价 tie-break 设计选择]
- **线段**：67 课特征序列法。向上线段看反向笔特征序列顶分型，向下反之；第一二元素无缺口直接确认段端，有缺口等反向特征序列出现相反分型确认。[缠论可导,67课]
- **中枢边界**：前三连续完成次级别走势 A,B,C，`ZD=max(low_A,low_B,low_C)`，`ZG=min(high_A,high_B,high_C)`；闭区间 `[ZD,ZG]`，`ZD≤ZG` 成立即中枢成立。[缠论可导,17/18课；闭区间设计选择]
- **canonical 分解**：从左到右扫描；候选取最早确认端点；按最早确认时间、最低递归层、最早原始 index 破平局。[设计选择,默认值]
- **未完成尾部**：显式保存 `PendingFractal|PendingStroke|PendingSegment|AliveCenter|PendingMove`（含方向/起点/当前极值/确认条件）；不输出为 confirmed。[设计选择,默认值]

## Θ_level（级别）
- **最低观察级别**：1 分钟 K；无 1 分钟则禁止 Reference v0 回测。[缠论允许1/5分,17课；选1分设计选择]
- **递归**：`L0=1分钟线段账本`；`L(k+1)` 只由 `Lk` 已完成走势类型/Move 构造；禁跳级混级。[缠论可导,17/18课]
- **最高研究级别**：`Lmax=6`；某层无法产生≥3 完成部件则自然终止。[设计选择,默认值]

## Θ_signal（信号）
- **中枢确认**：前三完成次级别走势重叠即确认；`GG=max(highs)`/`DD=min(lows)` 外包络，`ZD/ZG` 核心区间。[缠论可导,17/18课]
- **1买/卖**：同级别趋势≥两同向中枢后，末段相对前同向段背驰；底背驰端点=1买，顶背驰=1卖；确认在端点所属次级别走势完成时。[缠论可导,21/29课；确认时点设计选择]
- **2买/卖**：1买后第一段上行完成，回落不破1买低点或相对前下行段盘整背驰，则回落低点=2买；卖镜像。[缠论可导,21课]
- **3买/卖**：上离中枢后次级别回试低点 `>ZG`=3买；下离后回抽高点 `<ZD`=3卖。[缠论可导,18/30/49课；严格口径：retest==zg=单点重叠仍触及闭区间中枢=非终结，依中枢闭区间定义+中心定理一，codex 裁决 2026-06-27]
- **背驰度量**：结构前提优先；v0 辅助 MACD(12,26,9)，EMA 首值取首 close，`hist=DIF-DEA`；同向段面积严格变小才成立，等值不成立。[MACD辅助缠论可导,29/38/49课；EMA/面积规则 L3经验待标定默认]

## Θ_voice（声部）
- **声部树**：根=当前最高有效决策级别 `L*`；最多 3 层 `L*,L*-1,L*-2`。[设计选择,默认值]
- **方向**：`σ=+1`多/`-1`空/`0`空仓；子声部须 `σ_child=-σ_parent` 否则不开。[设计选择,默认值]
- **资金帽**：深度权重 `w=[0.60,0.30,0.10]`，未用部分保留现金不重分配。[设计选择,默认值]

## Θ_risk（风险）
- 单声部风险 `ρ=0.005 NAV`；父子仓位比 `β=0.5`；总名义上限 `γ=1.0 NAV`；成本倍数 `κ=2.0`。[设计选择,默认值;L3经验待标定]
- **结构止损**：1/2买止损=对应买点 pivot low；3买止损=`ZG`；卖/空镜像 pivot high 或 `ZD`。[缠论可导结构失效；触发设计选择]
- **sizing**：`qty=min(floor(ρ*NAV/(|entry-stop|+κ*cost_per_unit)), floor(w_depth*γ*NAV/entry), parent_cap)`；`qty≤0` 不交易；默认 lot=1。[设计选择,默认值]

## Θ_exec（执行）
- 信号确认后延迟 1 根基础 K，以下一根 open 成交；无下一根等下一可交易 open。[设计选择,默认值]
- **费用**：commission `1bp/side`，slippage `2bp/side`，tax `0bp`；买入加 slippage、卖出减。[设计选择,默认值;L3经验待标定]
- **止损成交**：long bar open 低于止损按 open 出，否则 low 触及止损按 stop 出；short 镜像。[设计选择,默认值]
- **不可交易**：缺 OHLC、`high<max(open,close,low)`、`low>min(open,close,high)`、volume=0、halt flag、或显式 limit flag 阻止该方向。[设计选择,默认值]
- **冲突顺序**：止损/退出先于开仓；高 level 先于低 level；同 level 按 1/2/3 类顺序；仍平局按 timestamp/source_index。[设计选择,默认值]

## 认识论诚实（formalization-validity-domain）
- 标 [缠论可导] = 缠论原文有依据；[设计选择,默认值] = extra-缠论 必须固定的外部参数（非缠论真理）；[L3经验待标定] = 数值需 L2/L3 标定。
- **L0 完备 ⊬ 预测有效；形式系统完备 ⊬ 市场有效；Θ 是外部设计参数。**
- 一次回测失败只否定"这个 Θ + 协议 + 市场/周期/成本"，不否定缠论整体；只有合理 Θ 空间系统扫描后无稳健区域才接近强否证。
