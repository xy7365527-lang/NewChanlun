R3 Phase 3：f(v,w) 的完整代数理论

背景：预测Δβ₁函数 f(v,w) = (c-1) + n_loop 在实验数据上验证为操作类型的内禀判据。f_fold=2.93 < f_random=7.46 < f_negate=15.17。f=0 在 fold 中占 40%，random 中 0%。Jaccard_fold=0.622 >> random=0.014。

已证明（R3 Phase 2）：f=0 ⟺ (c=1∧n_loop=0)∨(c=0∧n_loop=1)；f≤g-1；f⁻¹(0)定义离散梯度向量场。

**最优先：Q-R3-13（f=0 的传递性和等价关系）**

f(v,w)=0 定义了 V 上的二元关系。

1. 对称性：f(v,w)=0 ⟹ f(w,v)=0？（f 的定义中 c 依赖 N(v)∪N(w)，对称。n_loop 也对称。所以 f 应该是对称的——但需要严格验证。）
2. **传递性**：f(v,w)=0 且 f(w,x)=0 ⟹ f(v,x)=0？这是关键。v和w邻域高度重叠，w和x邻域高度重叠，v和x的邻域是否也高度重叠？证明或给反例。
3. 如果传递性不成立——f=0 定义的是什么代数结构？对架构意味着什么？

**Q-R3-11（f 的极值结构）**：
1. f 的值域？能取所有非负整数吗？
2. 固定 v，f_v(w) 的极小值点集的拓扑特征？
3. f⁻¹(0) 在图中形成什么结构——不相交团？更复杂的？
4. f⁻¹(k) 随 k 增大如何变化？

**Q-R3-12（f 和邻域结构）**：
1. c 和 Jaccard J(v,w) 的精确关系？能否证明 c ≤ |N∪N| - |N∩N| + k？
2. c 和共享邻居内部边密度的关系？
3. 能否构造"修正Jaccard" J* 使 f = h(J*) 对某单调递减 h？

**Q-R3-14（统一势函数精化）**：
g 在密集图上饱和。替代方案？Φ = f/d̄（度数归一化）？梯度流和穿越的关系？

**Q-R3-15（f 的持久同调）**：
V×V 按 f 值 filtration 的 barcode 中长 bar 对应什么？

**Q-R3-16（negate 替代判据）**：
g 饱和后的候选：(1) 最短路径上的 critical 边数；(2) 移除 v 后 w 可达集变化量；(3) 共同 settled 环数。哪个在密集图上不饱和？

所有分析在 L0 完成。Q-R3-13 最优先。