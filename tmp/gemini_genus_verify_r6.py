#!/usr/bin/env python
"""Gemini verify round 6: addressing the semantic-syntax binding deadlock."""
import sys, os
sys.path.insert(0, os.getcwd())
sys.path.insert(0, os.path.join(os.getcwd(), "src"))

from newchan.gemini.modes import GeminiChallenger

SUBJECT = (
    "第六轮：你命中了最深层的矛盾——预定义 Key 扼杀异质性 vs 动态 Key 摧毁代数差分。\n"
    "这是真实的二律背反。但它是否真的无解？\n\n"
    "让我尝试第三条路：半结构化 Key 空间（Hybrid Key Space）。\n\n"
    "核心洞察：你揭示的矛盾的本质是'语义到语法的绑定'。\n"
    "但仔细看——这个绑定不需要一步到位。它可以分两步：\n\n"
    "第一步：每轮 LLM 输出包含两层\n"
    "  - 结构层（预定义 Key）：verdict, 已知议题的 stance（如 betti_formula, convergence_method）\n"
    "  - 发现层（动态 Key）：本轮新发现的议题，LLM 自由命名\n"
    "  这不是新东西——当前的 STANCE_OUTPUT_PROTOCOL 已经有 stances: 自由 KV 对。\n"
    "  问题在于：发现层的 Key 如何进入下一轮的结构层？\n\n"
    "第二步：Key 注册（跨轮绑定）\n"
    "  当发现层出现新 Key 时：\n"
    "  a) 计算过程（Python 脚本，不是 LLM）检测新 Key 的出现\n"
    "  b) 新 Key 自动注册到下一轮的 prompt template 中——变成'预定义 Key'\n"
    "  c) 如果双方在后续轮次中都引用了这个 Key → Key 成为正式结构\n"
    "  d) 如果只有一方引用，另一方完全忽略 → Key 在 N 轮后退出\n\n"
    "关键：身份同一性通过'注册+传播'而非'事后匹配'解决。\n"
    "  Round 3 你输出 alternating_subject_oscillation: reject\n"
    "  → 脚本检测到新 Key，加入 Round 4 的 prompt template\n"
    "  → Round 4 我必须对 alternating_subject_oscillation 给出 stance\n"
    "  → 如果我输出 subject_oscillation_issue: accept，这是一个新 Key，不是旧 Key 的重命名\n"
    "  → 两个 Key 共存，直到一方在显式引用中让步\n\n"
    "这解决了你的死锁：\n"
    "  - Key 空间不是预先封闭的（新发现可以加入）→ 不扼杀异质性\n"
    "  - Key 的身份同一性不依赖事后匹配，而是通过注册+传播保证 → 不摧毁代数差分\n"
    "  - 代数差分在'已注册 Key 集合'上做，不在动态 Key 上做\n\n"
    "收敛判据（修正版，结合你的 ADG 洞察的合理部分）：\n\n"
    "不再用 genus/Betti 数。也不用朴素 StanceDiff 不动点。\n"
    "改用：同主体 StanceDiff 稳定性（避免交替震荡）+ Key 空间冻结。\n\n"
    "定义：\n"
    "  - registered_keys_t = 第 t 轮之前已注册的 Key 集合\n"
    "  - gemini_stance_t = Gemini 在第 t 轮的 stance（仅限已注册 Key）\n"
    "  - codex_stance_t = Codex 在第 t 轮的 stance（仅限已注册 Key）\n"
    "  - 同主体 diff：gemini_diff_t = diff(gemini_stance_{t-2}, gemini_stance_t)\n"
    "    （比较同一主体在间隔一轮的两次发言）\n"
    "  - key_growth_t = registered_keys_t - registered_keys_{t-1}\n\n"
    "收敛条件（AND）：\n"
    "  1. Key 空间冻结：连续 N 轮无新 Key 注册（key_growth = empty）\n"
    "  2. 同主体 stance 稳定：连续 N 轮同主体 diff 为空\n"
    "  3. N = 参与主体数 + 1 = 3\n\n"
    "为什么这不退化为投票机：\n"
    "  - Key 注册序列本身就是过程结构——'第 3 轮发现了议题 X，第 5 轮发现了议题 Y'\n"
    "  - 让步轨迹从 Key 注册序列 + stance 变化序列推导\n"
    "  - 死锁状态：同主体 diff 为空（各自立场不变）+ Key 空间冻结 → 收敛\n"
    "    这正确地检测到死锁——双方各自坚持不变 = 不动点\n"
    "  - 交替震荡不存在：因为比较的是同主体间隔一轮的 diff\n\n"
    "residue 定义：\n"
    "  - 收敛时 Gemini 和 Codex 在注册 Key 上有分歧的 Key = unresolved\n"
    "  - 让步 = Key 注册后某方 stance 从 reject→accept 或反向变化\n"
    "  - 消失的 Key = 注册后在 N 轮内被双方忽略 → lack 登记\n\n"
    "请严格质询。特别是：\n"
    "1. 半结构化 Key 空间是否真的解决了你的二律背反？\n"
    "2. Key 注册机制是否引入新的问题？\n"
    "3. 同主体 diff + Key 冻结作为收敛条件是否严格？\n"
    "4. 这个方案保留了多少'过程结构'？是否满足'拓扑痕迹'的要求？\n"
    "5. 如果你接受或部分接受，你在五轮中的哪些立场发生了变化？"
)

CONTEXT = (
    "五轮质询历史：\n"
    "Round 1: SCC 替换 WCC 数学错误 -> fail, 0 concessions\n"
    "Round 2: 无向指标噪声 + diameter活锁 + 累积图不消失 + 因果缺失 -> fail, 0 concessions\n"
    "Round 3: 死锁vs滑动窗口互斥 + 因果不可推导 + 滑动窗口=遗忘 -> fail, 0 concessions\n"
    "Round 4: 交替主体震荡 + 退化为投票机 -> fail, 0 concessions\n"
    "Round 5: 摧毁 ADG（循环依赖）+ 预定义vs动态Key二律背反 -> conditional, 1 concession (naive_adg)\n\n"
    "Round 5 Gemini 让步：\n"
    "  - 放弃朴素 ADG 方案（承认循环依赖不可解）\n"
    "  - verdict 从 fail 变为 conditional\n\n"
    "Round 5 Gemini 发现的核心死锁：\n"
    "  预定义 Key 扼杀异质性 vs 动态 Key 摧毁代数差分\n"
    "  '语义到语法的绑定机制'缺失\n\n"
    "被废弃的方案（全部）：\n"
    "- beta_1 = E - V + C_SCC\n"
    "- 两层指标（无向 + 有向）\n"
    "- diameter + 1 stability_window\n"
    "- 累积图 / 滑动窗口\n"
    "- 因果边从 StanceDiff 推导\n"
    "- StanceDiff 不动点（交替主体震荡）\n"
    "- 朴素 ADG（循环依赖）\n\n"
    "坚持的原则：\n"
    "- 否定方向不可抹掉\n"
    "- 收敛不是消除矛盾而是显式登记\n"
    "- lack 不能被无声消解\n"
    "- 无轮数上限\n"
    "- 过程结构不能被抛弃\n\n"
    "已有基础设施：\n"
    "- StanceDeclaration, StanceDiff, ConcessionTrace (consensus_trigger.py)\n"
    "- compute_stance_diff() (consensus_trigger.py)\n"
    "- parse_stance_declaration() (stance_parser.py)\n"
    "- STANCE_OUTPUT_PROTOCOL — 结构化 YAML 输出，key 可自由定义\n"
    "- trigger_ceremony() -> write_consensus_ceremony() (consensus_trigger.py)"
)

challenger = GeminiChallenger()
result = challenger.verify(SUBJECT, CONTEXT)
print(result.response)
print("\n---MODEL---")
print(result.model)
