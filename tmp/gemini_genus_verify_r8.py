#!/usr/bin/env python
"""Gemini verify round 8: key normalization + lack definition."""
import sys, os
sys.path.insert(0, os.getcwd())
sys.path.insert(0, os.path.join(os.getcwd(), "src"))

from newchan.gemini.modes import GeminiChallenger

SUBJECT = (
    "第八轮：你给出了 pass 条件——Key 归一化 + Lack 精确定义。逐一解决。\n\n"
    "=== 确认你的两个否定 ===\n\n"
    "确认1: (accept, null) 的判定死锁——完全正确。\n"
    "  路径A: 所有 null 算分歧 → residue 爆炸\n"
    "  路径B: 所有 null 不算分歧 → lack 无声消解\n"
    "  需要第三种路径。\n\n"
    "确认2: 同义词无限循环——正确。LLM 是概率模型。\n"
    "  不受控的动态 Key 生成 = key_registry 永远不冻结。\n\n"
    "=== 解决方案 ===\n\n"
    "解决1: Key 归一化——回到预定义 Key，但以正确的方式\n\n"
    "你在 Round 5 否定了预定义 Key 因为它扼杀异质性。\n"
    "但仔细看——异质性发生在哪里？\n"
    "  - 异质性发生在 LLM 回复的自然语言论述中，不在 Key 名称中\n"
    "  - Gemini 说'SCC 替换 WCC 是数学错误'——这是论述内容\n"
    "  - 这个论述被分类到 key='betti_formula_validity' 下——这是分类标签\n"
    "  - 异质创新在论述层，不在标签层\n\n"
    "因此：Key 完全预定义，每轮由脚本（不是 LLM）从上一轮 context 推导。\n"
    "但 LLM 可以在论述中提出任何新论点——新论点通过以下机制进入 Key 空间：\n\n"
    "机制：轮间 Key 更新由脚本执行\n"
    "  1. 每轮 LLM 输出标准 stance YAML（使用预定义 Key 列表）\n"
    "  2. LLM 输出还包含一个特殊字段 new_issues: [string]\n"
    "     LLM 可以在此列出它认为需要新增的议题（自然语言描述）\n"
    "  3. 脚本（Python，不是 LLM）处理 new_issues：\n"
    "     a) 为每个新议题生成标准化 Key（snake_case 转换，不依赖 LLM 命名）\n"
    "     b) 检查是否与已有 Key 语义重复（用简单的字符串相似度，不需要 LLM）\n"
    "     c) 不重复 → 注册新 Key\n"
    "     d) 重复 → 映射到已有 Key\n"
    "  4. 新注册的 Key 加入下一轮的预定义列表\n\n"
    "这样：\n"
    "  - Key 空间严格受控（脚本管理，不是 LLM 自由生成）\n"
    "  - 异质创新不被扼杀（new_issues 字段允许提出任何新议题）\n"
    "  - 同义词问题不存在（Key 生成由脚本而非 LLM 控制）\n"
    "  - Key 冻结有意义（受控空间可以冻结）\n\n"
    "解决2: Lack 的精确定义——三分法\n\n"
    "你的死锁来自二分法（分歧 vs 非分歧）。改为三分法：\n\n"
    "对每个 Key K 在收敛时的状态，按以下分类：\n\n"
    "类型1: 双方有明确 stance 且不同 → unresolved（进入 residue）\n"
    "  例：Gemini K1=reject, Codex K1=accept\n"
    "  无歧义，直接进入 unresolved\n\n"
    "类型2: 双方有明确 stance 且相同 → resolved（不进入 residue）\n"
    "  例：Gemini K1=accept, Codex K1=accept\n"
    "  共识达成，不需要记录\n\n"
    "类型3: 至少一方从未对 K 给出 stance（null）\n"
    "  这就是你说的 (accept, null) 情况。进一步细分：\n\n"
    "  类型3a: K 是由'缺失方'自己之前提出的 → 退行（regressed_lack）\n"
    "    例：Codex 在 Round 3 提出 K1=reject，Round 5-7 停止引用 K1\n"
    "    → Codex 自己提出了议题又放弃了 → 这是显式的 lack 信号\n"
    "    → 进入 residue 的 regressed_lack\n\n"
    "  类型3b: K 是由'另一方'提出的，缺失方从未回应 → 拒绝响应（refusal_to_engage）\n"
    "    例：Gemini 提出 K1=reject，Codex 从 Round 3 到收敛都未对 K1 给出 stance\n"
    "    → Codex 拒绝进入 Gemini 的议题框架\n"
    "    → 这不是 lack 消解——这是主体间的框架不可通约\n"
    "    → 进入 residue 的 unresolved（框架层面的分歧比立场层面更深）\n\n"
    "  类型3c: K 由脚本自动注册但双方都未引用 → 噪声退出\n"
    "    例：脚本从 new_issues 注册了 K1，但双方在后续轮次都未引用\n"
    "    → 安静退出，不进入 residue（这是注册机制的假阳性）\n\n"
    "三分法的性质：\n"
    "  - 不会导致 residue 爆炸：噪声由 3c 过滤\n"
    "  - 不会导致 lack 无声消解：3a 和 3b 都进入 residue\n"
    "  - 区分依据是确定性的（谁提出了 Key + 谁缺失了）——不需要 LLM 判断\n\n"
    "=== 综合方案（最终版）===\n\n"
    "名称：受控 Key 空间状态机 + 同主体 diff 收敛\n\n"
    "1. Key 管理（脚本控制）：\n"
    "   - 初始 Key 集合由 subject 推导\n"
    "   - LLM 通过 new_issues 字段提出新议题\n"
    "   - 脚本生成标准化 Key 并注册\n"
    "   - Key 身份同一性由脚本保证，不依赖 LLM\n\n"
    "2. 收敛条件（AND）：\n"
    "   - Key 空间冻结：连续 N 轮无新 Key 注册\n"
    "   - 同主体 stance 稳定：连续 N 轮 diff(主体X_{t-2}, 主体X_t) 为空\n"
    "   - N = 3（参与主体数 + 1）\n\n"
    "3. Residue（三分法）：\n"
    "   - unresolved: 双方 stance 不同 + 拒绝响应（3b）\n"
    "   - conceded: stance 在历史中发生让步变化\n"
    "   - regressed_lack: 自己提出又放弃的议题（3a）\n"
    "   - 噪声: 安静退出（3c）\n\n"
    "请严格质询。这是我认为可以 pass 的方案。"
)

CONTEXT = (
    "七轮质询历史：\n"
    "Round 1-4: 数学错误、活锁、互斥、震荡 -> 全 fail, 0 concessions\n"
    "Round 5: ADG 循环依赖 + Key 二律背反 -> conditional, 1 concession\n"
    "Round 6: 收敛条件 accept + 零拓扑 + 强制vs忽略 -> fail, 1 concession\n"
    "Round 7: 拓扑放弃 accept + 衰减死锁 + 同义词循环 -> fail, 1 concession\n\n"
    "已达成共识：\n"
    "  ✓ 同主体 diff 作为收敛比较方式\n"
    "  ✓ Key 冻结作为收敛必要条件\n"
    "  ✓ 放弃拓扑声明（扁平状态机可接受）\n"
    "  ✓ 扁平状态机满足 API 桥接需求\n\n"
    "Round 7 Gemini 给出的 pass 条件：\n"
    "  1. Key 归一化机制\n"
    "  2. Lack 精确定义\n\n"
    "坚持的原则（双方一致）：\n"
    "- 否定方向不可抹掉\n"
    "- 收敛不是消除矛盾而是显式登记\n"
    "- lack 不能被无声消解\n"
    "- 无轮数上限\n"
    "- 异质性不可扼杀"
)

challenger = GeminiChallenger()
result = challenger.verify(SUBJECT, CONTEXT)
print(result.response)
print("\n---MODEL---")
print(result.model)
